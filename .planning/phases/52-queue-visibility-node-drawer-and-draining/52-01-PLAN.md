---
phase: 52-queue-visibility-node-drawer-and-draining
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/tests/test_draining.py
  - puppeteer/tests/test_dispatch_diagnosis.py
  - puppeteer/tests/test_node_detail.py
autonomous: true
requirements:
  - VIS-01
  - VIS-03
  - VIS-04

must_haves:
  truths:
    - "Three new test files exist with failing stubs (pytest.fail) covering VIS-01, VIS-03, VIS-04"
    - "Running pytest on the three files produces failures (not errors) — stubs execute and fail with 'not implemented'"
    - "Each stub documents the expected API shape in its docstring so Wave 2 implementors have the contract"
  artifacts:
    - path: "puppeteer/tests/test_draining.py"
      provides: "VIS-04 stubs: drain endpoint, undrain endpoint, pull_work exclusion, heartbeat preserves DRAINING, list_nodes preserves DRAINING, auto-transition DRAINING→OFFLINE"
    - path: "puppeteer/tests/test_dispatch_diagnosis.py"
      provides: "VIS-01 stubs: no_nodes_online, capability_mismatch, all_nodes_busy+queue_position, target_node_unavailable, pending_dispatch"
    - path: "puppeteer/tests/test_node_detail.py"
      provides: "VIS-03 stubs: running_job field, eligible_pending_jobs field, recent_history field, capabilities field"
  key_links:
    - from: "puppeteer/tests/test_draining.py"
      to: "puppeteer/agent_service/db.py"
      via: "imports Node, Job from agent_service.db"
      pattern: "from agent_service\\.db import"
    - from: "puppeteer/tests/test_dispatch_diagnosis.py"
      to: "puppeteer/agent_service/services/job_service.py"
      via: "imports JobService"
      pattern: "from agent_service\\.services\\.job_service import"
---

<objective>
Create three Wave 0 test scaffold files with failing stubs for Phase 52.

Purpose: Establish RED phase for TDD — stubs document the expected API contracts for each requirement before implementation. Plan 02 implements the backend and turns these green.
Output: test_draining.py, test_dispatch_diagnosis.py, test_node_detail.py — all failing with pytest.fail("not implemented").
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/52-queue-visibility-node-drawer-and-draining/52-CONTEXT.md
@.planning/phases/52-queue-visibility-node-drawer-and-draining/52-RESEARCH.md

<interfaces>
<!-- Fixture pattern from existing test_pagination.py -->
From puppeteer/tests/test_pagination.py:
```python
import pytest
import pytest_asyncio
from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine
from sqlalchemy.orm import sessionmaker
from agent_service.db import Base, Job, Node

@pytest_asyncio.fixture
async def db():
    engine = create_async_engine("sqlite+aiosqlite:///:memory:")
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    async_session = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
    async with async_session() as session:
        yield session
    await engine.dispose()
```

From puppeteer/agent_service/db.py — Node.status is plain String column (no constraint).
Node has: node_id (String PK), hostname, ip, status, last_seen, tags, capabilities, env_tag, concurrency_limit

From puppeteer/agent_service/services/job_service.py — JobService.pull_work at line 438.
Job has: guid, status, node_id, target_tags (JSON string), capability_requirements (JSON string), env_tag, created_at
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_draining.py stub scaffold</name>
  <files>puppeteer/tests/test_draining.py</files>
  <action>
Create `puppeteer/tests/test_draining.py` with the async in-memory SQLite fixture (copy pattern from test_pagination.py) and the following failing stubs using `pytest.fail("not implemented")` as the first body line:

1. `test_drain_endpoint_sets_status_draining` — POST /nodes/{id}/drain returns 200 and node.status == "DRAINING"; imports: httpx TestClient or direct DB assertion
2. `test_undrain_endpoint_sets_status_online` — PATCH /nodes/{id}/undrain on a DRAINING node returns 200 and status becomes "ONLINE"
3. `test_drain_rejects_already_draining` — PATCH /nodes/{id}/drain on already-DRAINING node returns 409
4. `test_pull_work_skips_draining_node` — node in DRAINING state: JobService.pull_work returns PollResponse(job=None) without assigning
5. `test_heartbeat_preserves_draining_status` — receive_heartbeat on DRAINING node must not change status back to ONLINE; after heartbeat call, node.status still "DRAINING"
6. `test_list_nodes_draining_preserved` — The GET /nodes response for a DRAINING node carries status="DRAINING", not "ONLINE" (even if last_seen < 60s ago)
7. `test_auto_offline_transition_when_last_job_completes` — node DRAINING with one ASSIGNED job; when report_result called for that job, node.status transitions to "OFFLINE"
8. `test_draining_node_not_reverted_with_active_jobs` — node DRAINING with two ASSIGNED jobs; first result arrives; node.status must remain "DRAINING" (not "OFFLINE")

Each stub: one-line docstring with the expected behavior, then `pytest.fail("not implemented")`.

Import section: `import pytest, pytest_asyncio, json, uuid` from sqlalchemy async + `from agent_service.db import Base, Job, Node`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_draining.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>8 stubs in test_draining.py, all FAILED (not ERRORS), with "not implemented" in failure message</done>
</task>

<task type="auto">
  <name>Task 2: Create test_dispatch_diagnosis.py stub scaffold</name>
  <files>puppeteer/tests/test_dispatch_diagnosis.py</files>
  <action>
Create `puppeteer/tests/test_dispatch_diagnosis.py` with the same async DB fixture and the following failing stubs:

1. `test_diagnosis_no_nodes_online` — no nodes in DB; result has `reason == "no_nodes_online"`, message is a non-empty string
2. `test_diagnosis_capability_mismatch` — one ONLINE node; job requires capability "gpu" >= "1.0"; node has no "gpu"; result has `reason == "capability_mismatch"`, message mentions the missing capability
3. `test_diagnosis_all_nodes_busy` — two eligible nodes both at concurrency limit (assigned_count >= concurrency_limit); result has `reason == "all_nodes_busy"`, result has `queue_position` (int >= 1)
4. `test_diagnosis_target_node_unavailable` — job has explicit `target_node_id` pointing to a node that is OFFLINE or DRAINING; result has `reason == "target_node_unavailable"`
5. `test_diagnosis_pending_dispatch` — eligible node with capacity; result has `reason == "pending_dispatch"`, message says job will be dispatched soon
6. `test_diagnosis_queue_position_is_count_of_earlier_pending` — 3 PENDING jobs created before this job + 2 created after; queue_position == 3

Each stub uses `pytest.fail("not implemented")` as first body line. Include docstring with expected future call: `await JobService.get_dispatch_diagnosis(job.guid, db)`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_dispatch_diagnosis.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>6 stubs in test_dispatch_diagnosis.py, all FAILED with "not implemented"</done>
</task>

<task type="auto">
  <name>Task 3: Create test_node_detail.py stub scaffold</name>
  <files>puppeteer/tests/test_node_detail.py</files>
  <action>
Create `puppeteer/tests/test_node_detail.py` with the same async DB fixture and the following failing stubs:

1. `test_node_detail_running_job_present` — one ASSIGNED job on node; result `running_job` is not None, contains the job guid
2. `test_node_detail_running_job_absent` — no ASSIGNED jobs on node; result `running_job` is None
3. `test_node_detail_eligible_pending_jobs` — two PENDING jobs with tags matching node; result `eligible_pending_jobs` contains both; a third PENDING job with mismatched tags is NOT in the list
4. `test_node_detail_eligible_pending_jobs_capped_at_50` — 55 eligible PENDING jobs; result `eligible_pending_jobs` length == 50 (cap enforced)
5. `test_node_detail_recent_history` — 3 COMPLETED jobs on this node in past 24h, 1 COMPLETED job > 24h ago; result `recent_history` length == 3
6. `test_node_detail_capabilities_returned` — node has capabilities JSON; result `capabilities` matches parsed capabilities dict

Each stub uses `pytest.fail("not implemented")` as first body line. Include docstring with expected future call: `await JobService.get_node_detail(node_id, db)`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_node_detail.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>6 stubs in test_node_detail.py, all FAILED with "not implemented"</done>
</task>

</tasks>

<verification>
All three test files exist and all stubs fail with pytest.fail (FAILED, not ERROR):
```bash
cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_draining.py tests/test_dispatch_diagnosis.py tests/test_node_detail.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"
```
Expected: 20 FAILED, 0 PASSED, 0 ERROR
</verification>

<success_criteria>
- test_draining.py: 8 stubs, all FAILED
- test_dispatch_diagnosis.py: 6 stubs, all FAILED
- test_node_detail.py: 6 stubs, all FAILED
- No ERROR results (only FAILED) — stubs execute and reach pytest.fail()
- Full existing test suite unaffected: `cd puppeteer && pytest --ignore=tests/test_draining.py --ignore=tests/test_dispatch_diagnosis.py --ignore=tests/test_node_detail.py` still passes
</success_criteria>

<output>
After completion, create `.planning/phases/52-queue-visibility-node-drawer-and-draining/52-01-SUMMARY.md`
</output>
