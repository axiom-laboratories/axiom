---
phase: 01-output-capture
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/dashboard/src/views/Jobs.tsx
autonomous: true
requirements:
  - OUT-04
gap_closure: true

must_haves:
  truths:
    - "Copy button in ExecutionLogModal is accessible and does not overlap the dialog close (X) button"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Jobs.tsx"
      provides: "ExecutionLogModal header action row with right-padding clearing the built-in X button"
      contains: "pr-8"
  key_links:
    - from: "ExecutionLogModal header action div"
      to: "DialogPrimitive.Close (absolute right-4 top-4)"
      via: "right padding on the flex row that contains the Copy button"
      pattern: "pr-8|pr-10"
---

<objective>
Fix the Copy button in ExecutionLogModal overlapping the built-in dialog close (X) button.

Purpose: The shadcn DialogContent always renders a DialogPrimitive.Close at `absolute right-4 top-4`. When the modal uses `p-0` and the header action row sits flush to the right edge, the Copy button and attempt selector land in the same pixel region as the X, making both hard to click. This is cosmetic but creates a confusing UX.

Output: A single targeted padding change to `puppeteer/dashboard/src/views/Jobs.tsx` that pushes the Copy button left of the X button.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-output-capture/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Key context the executor needs — no codebase exploration required. -->

From puppeteer/dashboard/src/components/ui/dialog.tsx:
The built-in close button is rendered at the end of DialogContent, absolutely positioned:
```tsx
<DialogPrimitive.Close className="absolute right-4 top-4 rounded-sm opacity-70 ...">
  <X className="h-4 w-4" />
</DialogPrimitive.Close>
```

The modal header in Jobs.tsx (ExecutionLogModal) currently:
```tsx
<DialogContent className="bg-zinc-950 border-zinc-800 text-white w-[95vw] max-w-6xl h-[90vh] flex flex-col p-0">
    <DialogHeader className="px-4 pt-4 pb-3 border-b border-zinc-800 shrink-0">
        <div className="flex items-center justify-between">
            <DialogTitle ...>...</DialogTitle>
            <div className="flex items-center gap-2">
                {/* attempt selector */}
                <button onClick={handleCopy} ...>Copy</button>
            </div>
        </div>
    </DialogHeader>
    ...
</DialogContent>
```

The action div `<div className="flex items-center gap-2">` has no right padding, so it sits flush at x = DialogContent.right, directly behind the X button.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add right padding to ExecutionLogModal action row to clear built-in X button</name>
  <files>puppeteer/dashboard/src/views/Jobs.tsx</files>
  <action>
In the `ExecutionLogModal` component in `Jobs.tsx`, find the inner action row div that wraps the attempt selector and Copy button:

```tsx
<div className="flex items-center gap-2">
```

Change it to:

```tsx
<div className="flex items-center gap-2 pr-8">
```

`pr-8` (2rem) gives clearance past the X button which sits at `right-4` (1rem). This is the minimal targeted change — do not alter any other part of the component.

Note: Do not suppress the built-in DialogPrimitive.Close — it provides keyboard accessibility (Escape key). Just add spacing so the Copy button does not overlap it.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. The action div containing Copy/attempt-selector has `pr-8` in its className, visually separating it from the X button.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 in `puppeteer/dashboard/`
- `grep -n "pr-8" puppeteer/dashboard/src/views/Jobs.tsx` confirms the class is present in the ExecutionLogModal action row
</verification>

<success_criteria>
- Copy button and attempt selector are not visually overlapped by the DialogPrimitive.Close X button
- Build is clean — no TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-output-capture/01-04-SUMMARY.md`
</output>
