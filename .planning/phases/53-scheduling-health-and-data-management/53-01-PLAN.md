---
phase: 53-scheduling-health-and-data-management
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - puppeteer/tests/test_scheduling_health.py
  - puppeteer/tests/test_job_templates.py
  - puppeteer/tests/test_retention.py
  - puppeteer/tests/test_execution_export.py
autonomous: true
requirements:
  - VIS-05
  - VIS-06
  - SRCH-06
  - SRCH-07
  - SRCH-08
  - SRCH-09
  - SRCH-10

must_haves:
  truths:
    - "All 4 test files exist with failing stubs (pytest.fail as first body line)"
    - "Running the test suite reports FAILED not ERROR or skip for each stub"
    - "Test names match the automated commands in 53-VALIDATION.md exactly"
  artifacts:
    - path: "puppeteer/tests/test_scheduling_health.py"
      provides: "Stubs for VIS-05, VIS-06"
      contains: "test_health_aggregate, test_missed_fire_detection"
    - path: "puppeteer/tests/test_job_templates.py"
      provides: "Stubs for SRCH-06, SRCH-07"
      contains: "test_create_template, test_template_visibility"
    - path: "puppeteer/tests/test_retention.py"
      provides: "Stubs for SRCH-08, SRCH-09"
      contains: "test_pruner_respects_pinned, test_pin_unpin"
    - path: "puppeteer/tests/test_execution_export.py"
      provides: "Stub for SRCH-10"
      contains: "test_csv_export"
  key_links:
    - from: "53-01-PLAN.md"
      to: "53-02/03/04-PLAN.md"
      via: "test file names"
      pattern: "test_scheduling_health|test_job_templates|test_retention|test_execution_export"
---

<objective>
Create four failing test scaffolds covering all Phase 53 backend requirements. Wave 0 exists so that Wave 2 backend plans can drop in real assertions and pass them cleanly.

Purpose: Enforce RED→GREEN discipline. Executors of Plans 03 and 04 pick up these stubs and make them pass.
Output: Four test files, all stubs failing with pytest.fail('not implemented').
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-scheduling-health-and-data-management/53-CONTEXT.md
@.planning/phases/53-scheduling-health-and-data-management/53-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test stubs — scheduling health and retention</name>
  <files>puppeteer/tests/test_scheduling_health.py, puppeteer/tests/test_retention.py</files>
  <behavior>
    - test_health_aggregate: stub fails with pytest.fail('not implemented') — future shape: given scheduled fire logs for 3 definitions across a 24h window, health endpoint returns aggregate {fired, skipped, failed} totals + per-definition rows
    - test_missed_fire_detection: stub fails — future shape: given a ScheduledFireLog row with no matching ExecutionRecord within 5-min grace, the health endpoint classifies it as LATE then MISSED after next scheduled fire time
    - test_pruner_respects_pinned: stub fails — future shape: given 2 expired ExecutionRecords (one pinned, one not), pruner deletes only the unpinned one
    - test_pin_unpin: stub fails — future shape: PATCH /executions/{id}/pin sets pinned=True and writes an audit log entry; PATCH /executions/{id}/unpin sets pinned=False and writes audit log
  </behavior>
  <action>
    Create puppeteer/tests/test_scheduling_health.py with exactly two test functions:
    - async def test_health_aggregate(): — first body line: pytest.fail('not implemented'); docstring describes future shape
    - async def test_missed_fire_detection(): — first body line: pytest.fail('not implemented'); docstring describes future shape

    Create puppeteer/tests/test_retention.py with exactly two test functions:
    - async def test_pruner_respects_pinned(): — first body line: pytest.fail('not implemented')
    - async def test_pin_unpin(): — first body line: pytest.fail('not implemented')

    Use the same stub convention as Phase 49 and 52:
    import pytest
    def test_xxx():
        pytest.fail('not implemented')

    (No async needed for stubs — plain sync stubs with pytest.fail are consistent with Phase 49 Wave 0 pattern.)
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_scheduling_health.py tests/test_retention.py -v 2>&1 | grep -E "FAILED|ERROR|PASSED" | head -20</automated>
  </verify>
  <done>All 4 stubs report FAILED (not ERROR, not PASSED). No import errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test stubs — job templates and execution export</name>
  <files>puppeteer/tests/test_job_templates.py, puppeteer/tests/test_execution_export.py</files>
  <behavior>
    - test_create_template: stub fails — future shape: POST /job-templates creates a template with name, payload (sans signature fields), visibility=private; returns id + payload
    - test_template_visibility: stub fails — future shape: private templates are returned only to their creator; shared templates appear for all users
    - test_csv_export: stub fails — future shape: GET /jobs/{guid}/executions/export returns CSV with headers job_guid, node_id, status, exit_code, started_at, completed_at, duration_s, attempt_number, pinned
  </behavior>
  <action>
    Create puppeteer/tests/test_job_templates.py with exactly two test functions:
    - def test_create_template(): — first body line: pytest.fail('not implemented')
    - def test_template_visibility(): — first body line: pytest.fail('not implemented')

    Create puppeteer/tests/test_execution_export.py with exactly one test function:
    - def test_csv_export(): — first body line: pytest.fail('not implemented')

    Same stub pattern: no imports beyond pytest.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_job_templates.py tests/test_execution_export.py -v 2>&1 | grep -E "FAILED|ERROR|PASSED" | head -20</automated>
  </verify>
  <done>All 3 stubs report FAILED. No import errors.</done>
</task>

</tasks>

<verification>
cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_scheduling_health.py tests/test_job_templates.py tests/test_retention.py tests/test_execution_export.py -v 2>&1 | tail -15
Expected: 7 FAILED, 0 PASSED, 0 ERROR
</verification>

<success_criteria>
All 7 stub test functions exist and fail with pytest.fail('not implemented'). Zero import errors. Test names match the exact names in 53-VALIDATION.md.
</success_criteria>

<output>
After completion, create `.planning/phases/53-scheduling-health-and-data-management/53-01-SUMMARY.md`
</output>
