---
phase: 29-backend-completeness-output-capture-retry-wiring
plan: 03
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - puppets/environment_service/runtime.py
  - puppets/environment_service/node.py
  - /home/thomas/Development/mop_validation/local_nodes/node_alpha/node-compose.yaml
  - /home/thomas/Development/mop_validation/local_nodes/node_beta/node-compose.yaml
  - /home/thomas/Development/mop_validation/local_nodes/node_gamma/node-compose.yaml
  - puppeteer/tests/test_direct_mode_removal.py
  - puppeteer/tests/test_output_capture.py
autonomous: true
requirements:
  - OUTPUT-01

must_haves:
  truths:
    - "runtime.py has no direct execution mode — only auto, docker, podman are valid"
    - "node.py fails at startup with RuntimeError if EXECUTION_MODE=direct before any network calls"
    - "node.py execute_task() computes script_hash = SHA-256 of script before execution and sends it in ResultReport"
    - "node.py respects WorkResponse.timeout_minutes for execution timeout (falls back to 30s if None)"
    - "All three mop_validation local node compose files use EXECUTION_MODE=docker"
    - "test_direct_mode_raises_on_startup passes"
    - "test_node_computes_script_hash passes"
  artifacts:
    - path: "puppets/environment_service/runtime.py"
      provides: "Container runtime without direct mode"
    - path: "puppets/environment_service/node.py"
      provides: "Startup guard + script_hash computation + timeout_minutes usage"
    - path: "puppeteer/tests/test_direct_mode_removal.py"
      provides: "Startup guard test"
    - path: "puppeteer/tests/test_output_capture.py"
      provides: "test_node_computes_script_hash implemented"
  key_links:
    - from: "node.py module level"
      to: "RuntimeError on direct mode"
      via: "os.environ.get('EXECUTION_MODE') check before class definitions"
      pattern: "EXECUTION_MODE.*direct.*RuntimeError"
    - from: "node.py execute_task()"
      to: "ResultReport script_hash field"
      via: "hashlib.sha256(script.encode('utf-8')).hexdigest() before execution"
      pattern: "hashlib\\.sha256.*script.*hexdigest"
---

<objective>
Remove the direct execution mode from runtime.py, add the startup guard to node.py, wire script_hash computation into execute_task(), respect timeout_minutes, and update mop_validation compose files from EXECUTION_MODE=direct to docker.

Purpose: Closes the remaining node-side gap — nodes now send a script_hash so the orchestrator can verify it, and the unsupported direct mode is completely gone rather than silently available. This makes Output-01 fully satisfied end-to-end.
Output: Cleaned runtime.py; hardened node.py with startup guard, script_hash, timeout wiring; three updated mop_validation compose files; completed test assertions.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-backend-completeness-output-capture-retry-wiring/29-CONTEXT.md
@.planning/phases/29-backend-completeness-output-capture-retry-wiring/29-RESEARCH.md
@.planning/phases/29-backend-completeness-output-capture-retry-wiring/29-01-SUMMARY.md

<interfaces>
<!-- runtime.py — direct mode to remove -->
<!-- detect_runtime() handles EXECUTION_MODE=direct — delete that branch -->
<!-- RuntimeEngine.__init__() or run() has if self.runtime == "direct": block — delete it -->
<!-- Valid runtimes after removal: auto, docker, podman only -->

<!-- node.py — startup guard location -->
<!-- Add module-level guard AFTER load_dotenv() and NODE_ID assignment, BEFORE class definitions -->
```python
_exec_mode = os.environ.get("EXECUTION_MODE", "auto").lower()
if _exec_mode == "direct":
    raise RuntimeError(
        "EXECUTION_MODE=direct is no longer supported. "
        "Use EXECUTION_MODE=docker, podman, or auto."
    )
```

<!-- node.py execute_task() — script_hash computation -->
<!-- Add before the call to runtime_engine.run() -->
```python
import hashlib
script_hash = hashlib.sha256(script.encode('utf-8')).hexdigest()
```
<!-- Then include script_hash in the report_result() call payload -->

<!-- node.py — timeout_minutes -->
<!-- WorkResponse now carries timeout_minutes (set by orchestrator in Plan 02) -->
<!-- node.py execute_task() should use work.timeout_minutes (convert to seconds) as subprocess/container timeout -->
<!-- Fall back to 30s if None -->
<!-- Look for the existing timeout value in execute_task() or runtime_engine.run() call -->

<!-- node.py report_result() — currently sends payload without script_hash -->
<!-- The JSON payload sent to POST /work/result must include script_hash -->
<!-- ResultReport model (from models.py in Plan 01) now has script_hash: Optional[str] = None -->

<!-- mop_validation compose files — change EXECUTION_MODE=direct to EXECUTION_MODE=docker -->
<!-- All three files mount /var/run/docker.sock already (confirmed in RESEARCH.md) -->
<!-- Only the env var value changes -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove direct mode from runtime.py and add node.py startup guard</name>
  <files>puppets/environment_service/runtime.py, puppets/environment_service/node.py, puppeteer/tests/test_direct_mode_removal.py</files>
  <behavior>
    - runtime.py detect_runtime() no longer handles 'direct' — raises ValueError or falls through to error if invalid mode given
    - runtime.py RuntimeEngine (or equivalent class/function) has no 'direct' branch in run()
    - node.py raises RuntimeError at module level if EXECUTION_MODE=direct, before any class/function definitions
    - Error message: "EXECUTION_MODE=direct is no longer supported. Use EXECUTION_MODE=docker, podman, or auto."
    - test_direct_mode_raises_on_startup: set os.environ["EXECUTION_MODE"]="direct", import node module (or reimport), assert RuntimeError raised
  </behavior>
  <action>
    Read runtime.py in full first to locate the exact direct-mode code. Delete:
    - The `if ... == "direct":` branch in detect_runtime() (or equivalent function)
    - The `if self.runtime == "direct":` block in the run() method (the entire branch, not just the body)

    Do NOT add a deprecation warning or fallback — hard delete, no trace.

    Read node.py to find the correct module-level insertion point (after load_dotenv/env var reads, before class definitions). Insert the startup guard block exactly as shown in the interfaces section.

    Update test_direct_mode_removal.py: implement test_direct_mode_raises_on_startup. The test must use importlib or subprocess to verify the RuntimeError fires — direct import of node.py may cause side effects. Safest approach: use subprocess to run a minimal Python script that sets EXECUTION_MODE=direct and imports the node module, then check for RuntimeError in stderr. Alternatively, use monkeypatching to patch os.environ before import if the test framework supports it.

    If module-level execution makes unit testing difficult, an alternative is to wrap the guard in an `if __name__ != 'pytest'` check — but this is wrong. The correct approach: move the guard into a function `_check_execution_mode()` called at module level AND directly testable by calling it with a patched env. Use pytest monkeypatch: `monkeypatch.setenv("EXECUTION_MODE", "direct"); with pytest.raises(RuntimeError): _check_execution_mode()`. Export `_check_execution_mode` from node.py.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/pytest tests/test_direct_mode_removal.py::test_direct_mode_raises_on_startup -v 2>&1 | tail -10</automated>
  </verify>
  <done>test_direct_mode_raises_on_startup passes. runtime.py has no 'direct' string anywhere in execution paths. node.py has startup guard that raises RuntimeError for EXECUTION_MODE=direct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire script_hash and timeout_minutes into node execute_task()</name>
  <files>puppets/environment_service/node.py, /home/thomas/Development/mop_validation/local_nodes/node_alpha/node-compose.yaml, /home/thomas/Development/mop_validation/local_nodes/node_beta/node-compose.yaml, /home/thomas/Development/mop_validation/local_nodes/node_gamma/node-compose.yaml, puppeteer/tests/test_output_capture.py</files>
  <behavior>
    - node.py execute_task() computes SHA-256 of script string before execution: hashlib.sha256(script.encode('utf-8')).hexdigest()
    - script_hash is included in the JSON body sent to POST /work/result as the script_hash field
    - timeout_minutes from WorkResponse is used as execution timeout (converted to seconds); falls back to 30s if None/zero
    - mop_validation node_alpha, node_beta, node_gamma compose files: EXECUTION_MODE value changed from 'direct' to 'docker'
    - test_node_computes_script_hash and test_result_report_has_script_hash tests pass
  </behavior>
  <action>
    Read node.py execute_task() (line 481) and report_result() (line 598) to understand the current flow.

    In execute_task(): before the runtime_engine.run() call, add:
    ```python
    import hashlib  # at top of file if not already imported
    script_hash = hashlib.sha256(script.encode('utf-8')).hexdigest()
    ```

    For timeout: locate where the execution timeout is currently set (look for a hardcoded 30 or similar). Replace with:
    ```python
    timeout_secs = (work.timeout_minutes * 60) if work.timeout_minutes else 30
    ```
    Pass timeout_secs to the runtime run call. If the WorkResponse object is not directly accessible in execute_task, trace how `work` flows through and pass timeout_minutes as a parameter.

    In report_result() (the node-side function that POSTs to /work/result): include script_hash in the JSON body. The ResultReport model now accepts script_hash (added in Plan 01). Update the dict/JSON payload to include `"script_hash": script_hash`.

    Update mop_validation compose files: open each of the three files and change `EXECUTION_MODE=direct` to `EXECUTION_MODE=docker`. Only change this one env var value — do not modify anything else in those files.

    Update test_output_capture.py: implement test_node_computes_script_hash by inspecting the source of node.py's execute_task function (source inspection via ast or grep on the file) and asserting that hashlib.sha256 is called with script.encode before the runtime run call.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/pytest tests/test_output_capture.py tests/test_retry_wiring.py tests/test_direct_mode_removal.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>All test_output_capture.py, test_retry_wiring.py, and test_direct_mode_removal.py tests pass. node.py execute_task() computes script_hash and passes it to report_result(). Timeout respects timeout_minutes from WorkResponse. All three mop_validation compose files have EXECUTION_MODE=docker. Full backend test suite green.</done>
</task>

</tasks>

<verification>
Confirm direct mode fully removed from runtime.py:
`grep -n "direct" /home/thomas/Development/master_of_puppets/puppets/environment_service/runtime.py`
(expect: zero matches in execution code)

Confirm script_hash in node.py:
`grep -n "script_hash\|sha256" /home/thomas/Development/master_of_puppets/puppets/environment_service/node.py`

Confirm mop_validation compose files updated:
`grep -r "EXECUTION_MODE" /home/thomas/Development/mop_validation/local_nodes/`

Run full backend test suite:
`cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/pytest -q 2>&1 | tail -10`
</verification>

<success_criteria>
- runtime.py contains no 'direct' execution path — any reference to 'direct' in run logic is deleted
- node.py has startup guard raising RuntimeError for EXECUTION_MODE=direct
- node.py execute_task() computes script_hash before execution and includes it in result report
- node.py respects WorkResponse.timeout_minutes (converts to seconds, falls back to 30s)
- All three mop_validation local node compose files show EXECUTION_MODE=docker
- All three new test files pass completely (no remaining stub failures)
- Full puppeteer test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/29-backend-completeness-output-capture-retry-wiring/29-03-SUMMARY.md`
</output>
