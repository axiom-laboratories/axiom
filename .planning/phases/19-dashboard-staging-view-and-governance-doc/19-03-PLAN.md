# Plan 19-03: Wave 3 — Governance Documentation

**Objective**: Formalize the security and governance model for job staging and OIDC integration.

## Context
Clear documentation is required to explain how the system maintains integrity during the staging flow and how it will evolve to support OIDC.

## Tasks
1. **Create `docs/architecture/OIDC_INTEGRATION.md`**:
   - Explain the current OAuth Device Flow implementation.
   - Define the API contract for a future OIDC provider (Endpoints, JWT claims).
   - Detail how local Ed25519 signing provides dual-factor integrity (Identity + Payload proof).
2. **Update User Documentation**:
   - Add a section to `docs/UserGuide.md` explaining the Staging flow: how to push from CLI, review in Dashboard, and publish to Active.
   - Explain the meaning of each lifecycle status (DRAFT, ACTIVE, DEPRECATED, REVOKED).

## Success Criteria
- Architecture document is complete and technically accurate.
- User guide provides clear instructions for the new staging workflow.

## Output
Create `.planning/phases/19-dashboard-staging-view-and-governance-doc/19-03-SUMMARY.md` after completion.
