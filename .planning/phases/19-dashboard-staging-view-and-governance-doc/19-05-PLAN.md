---
phase: 19-dashboard-staging-view-and-governance-doc
plan: 05
type: execute
wave: 5
depends_on: ["19-04"]
files_modified:
  - puppeteer/agent_service/services/scheduler_service.py
autonomous: true
requirements:
  - DASH-04
gap_closure: true

must_haves:
  truths:
    - "PATCH /api/jobs/definitions/{id} with {status: 'ACTIVE'} updates job.status in the database"
    - "A DRAFT job becomes ACTIVE after the operator clicks Publish in the dashboard"
  artifacts:
    - path: "puppeteer/agent_service/services/scheduler_service.py"
      provides: "update_job_definition() applies status field to job.status"
      contains: "if update_req.status is not None"
  key_links:
    - from: "scheduler_service.update_job_definition()"
      to: "job.status"
      via: "if update_req.status is not None: job.status = update_req.status"
      pattern: "update_req\\.status is not None"
---

<objective>
Close the DASH-04 gap: `scheduler_service.update_job_definition()` parses and validates `update_req.status` but never writes it to `job.status`. A one-line guard is missing, causing the Publish action to return HTTP 200 while leaving the job permanently as DRAFT in the database.

Purpose: Make the Publish button functional end-to-end so operators can promote DRAFT jobs to ACTIVE.
Output: One-line addition to `scheduler_service.py` that assigns `update_req.status` to `job.status` when provided.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dashboard-staging-view-and-governance-doc/19-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply status field assignment in update_job_definition</name>
  <files>puppeteer/agent_service/services/scheduler_service.py</files>
  <action>
    In `update_job_definition()`, after the `timeout_minutes` block (line 280) and before `job.updated_at = datetime.utcnow()` (line 282), insert:

    ```python
        if update_req.status is not None:
            job.status = update_req.status
    ```

    This follows the exact same guard pattern used for every other optional field in lines 265-280. The `status` field is already validated upstream (the REVOKED gate in `main.py` ensures invalid transitions are rejected before reaching the service), so the service layer simply needs to apply it.

    Do NOT alter any other lines. The indentation must match the surrounding `if` blocks (8 spaces inside the method).
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && grep -n "update_req.status is not None" agent_service/services/scheduler_service.py</automated>
  </verify>
  <done>
    `grep` returns exactly one match on a line between 281 and 283 containing `if update_req.status is not None: job.status = update_req.status` (or the two-line form). The method now applies all optional fields including status.
  </done>
</task>

</tasks>

<verification>
After the fix, confirm the complete update_job_definition() block (lines ~264-286) contains all optional field assignments including status:

```bash
cd /home/thomas/Development/master_of_puppets/puppeteer && grep -n "update_req\." agent_service/services/scheduler_service.py | tail -20
```

Expected output includes a line with `update_req.status`.

Also confirm backend syntax is valid:

```bash
cd /home/thomas/Development/master_of_puppets/puppeteer && python -c "import agent_service.services.scheduler_service" && echo "OK"
```
</verification>

<success_criteria>
- `scheduler_service.py` contains `if update_req.status is not None: job.status = update_req.status` inside `update_job_definition()`
- Python import check passes (no syntax errors)
- DASH-04 gap is closed: a PATCH with `{status: 'ACTIVE'}` will now update the database row
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-staging-view-and-governance-doc/19-05-SUMMARY.md`
</output>
