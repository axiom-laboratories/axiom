---
phase: 88-dispatch-diagnosis-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/services/job_service.py
  - puppeteer/agent_service/models.py
  - puppeteer/agent_service/main.py
autonomous: true
requirements: [DIAG-02]

must_haves:
  truths:
    - "POST /jobs/dispatch-diagnosis/bulk accepts a list of GUIDs and returns a results dict keyed by GUID"
    - "An ASSIGNED job whose started_at + (timeout_minutes * 1.2) is in the past returns reason=stuck_assigned from get_dispatch_diagnosis"
    - "An ASSIGNED job within its timeout threshold returns reason=not_pending (unchanged)"
    - "The bulk endpoint calls get_dispatch_diagnosis for each GUID and aggregates results"
  artifacts:
    - path: "puppeteer/agent_service/services/job_service.py"
      provides: "Extended get_dispatch_diagnosis with stuck-ASSIGNED branch"
      contains: "stuck_assigned"
    - path: "puppeteer/agent_service/models.py"
      provides: "BulkDiagnosisRequest model"
      contains: "BulkDiagnosisRequest"
    - path: "puppeteer/agent_service/main.py"
      provides: "POST /jobs/dispatch-diagnosis/bulk route"
      contains: "/jobs/dispatch-diagnosis/bulk"
  key_links:
    - from: "puppeteer/agent_service/main.py"
      to: "puppeteer/agent_service/services/job_service.py"
      via: "bulk endpoint calls JobService.get_dispatch_diagnosis for each guid"
      pattern: "JobService\\.get_dispatch_diagnosis"
---

<objective>
Extend the backend dispatch diagnosis system with two additions:
1. Stuck-ASSIGNED detection — ASSIGNED jobs past their timeout threshold now return a `stuck_assigned` reason from `get_dispatch_diagnosis`
2. Bulk endpoint — `POST /jobs/dispatch-diagnosis/bulk` accepts a list of GUIDs and returns all diagnoses in one response

Purpose: Provides the data contract that the Phase 88 frontend (Plan 02) requires. Operators need to see why ASSIGNED jobs are unresponsive, not just PENDING ones.
Output: Extended service method, new Pydantic request model, new FastAPI route.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-dispatch-diagnosis-ui/88-CONTEXT.md
@.planning/phases/88-dispatch-diagnosis-ui/88-RESEARCH.md
@puppeteer/agent_service/services/job_service.py
@puppeteer/agent_service/models.py
@puppeteer/agent_service/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend get_dispatch_diagnosis for stuck-ASSIGNED jobs</name>
  <files>puppeteer/agent_service/services/job_service.py</files>
  <action>
In `JobService.get_dispatch_diagnosis` (around line 1230), add a stuck-ASSIGNED check BEFORE the existing `if job.status != "PENDING"` early return.

The new branch must:
- Only fire when `job.status == "ASSIGNED"` AND `job.started_at` is not None
- Compute threshold: `threshold_minutes = (job.timeout_minutes or 30) * 1.2`
- Compute elapsed: `elapsed_minutes = (datetime.utcnow() - job.started_at).total_seconds() / 60`
- If `elapsed_minutes > threshold_minutes`, return:
  ```python
  {
      "reason": "stuck_assigned",
      "message": f"Assigned to {job.node_id} — no response in {int(elapsed_minutes)} min",
      "queue_position": None,
  }
  ```
- If elapsed is within threshold, fall through to existing `not_pending` return (do NOT change that return)

Do NOT modify any other branch of the method. The existing PENDING logic (steps 2–5) remains unchanged.

Also add `datetime` import check — it's already imported in job_service.py (used in heartbeat logic), so no new import is needed.
  </action>
  <verify>cd puppeteer && python -c "from agent_service.services.job_service import JobService; print('import ok')"</verify>
  <done>Import succeeds; `stuck_assigned` string present in job_service.py; existing PENDING logic unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Add BulkDiagnosisRequest model</name>
  <files>puppeteer/agent_service/models.py</files>
  <action>
Add a new Pydantic model `BulkDiagnosisRequest` to `models.py`, after the existing `BulkActionResponse` class (around line 86).

```python
class BulkDiagnosisRequest(BaseModel):
    """Request body for bulk dispatch diagnosis (Phase 88 — DIAG-01)."""
    guids: List[str]
```

No other changes to models.py. Do NOT create a response model — the bulk endpoint returns a plain dict.
  </action>
  <verify>cd puppeteer && python -c "from agent_service.models import BulkDiagnosisRequest; print(BulkDiagnosisRequest(guids=['a','b']).guids)"</verify>
  <done>BulkDiagnosisRequest importable; `.guids` field is a list of strings</done>
</task>

<task type="auto">
  <name>Task 3: Add POST /jobs/dispatch-diagnosis/bulk endpoint</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
Add a new route `POST /jobs/dispatch-diagnosis/bulk` in main.py.

Placement: Insert the new endpoint IMMEDIATELY AFTER the existing `GET /jobs/{guid}/dispatch-diagnosis` route (around line 1069, before the `@app.post("/jobs/{guid}/retry")` route). This keeps the dispatch-diagnosis group together and avoids any path-parameter routing confusion.

Import: Add `BulkDiagnosisRequest` to the imports from `agent_service.models` at the top of main.py (it's already importing from models — just add it to the existing import statement).

Route implementation:
```python
@app.post("/jobs/dispatch-diagnosis/bulk", tags=["Jobs"])
async def bulk_dispatch_diagnosis(
    req: BulkDiagnosisRequest,
    current_user: User = Depends(require_auth),
    db: AsyncSession = Depends(get_db),
):
    """Returns dispatch diagnosis for multiple jobs in one call (Phase 88 — DIAG-01)."""
    results = {}
    for guid in req.guids:
        results[guid] = await JobService.get_dispatch_diagnosis(guid, db)
    return {"results": results}
```

Notes:
- Use `require_auth` (not `require_permission`) — same as the single-job endpoint at line 1064
- Empty guids list is valid — returns `{"results": {}}`
- No limit on guids count — the frontend only sends PENDING/ASSIGNED job guids (typically a small set)
  </action>
  <verify>cd puppeteer && python -c "from agent_service.main import app; routes = [r.path for r in app.routes]; assert any('bulk' in r and 'diagnosis' in r for r in routes), f'bulk diagnosis route missing from: {routes}'; print('route registered')"</verify>
  <done>Route `/jobs/dispatch-diagnosis/bulk` registered in FastAPI app; import of BulkDiagnosisRequest succeeds</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd puppeteer && python -c "from agent_service.main import app; print('app loads')"` — no import errors
- [ ] `cd puppeteer && python -c "from agent_service.services.job_service import JobService; import inspect; src = inspect.getsource(JobService.get_dispatch_diagnosis); assert 'stuck_assigned' in src; print('stuck_assigned present')"` — confirms stuck-ASSIGNED branch
- [ ] `cd puppeteer && python -c "from agent_service.models import BulkDiagnosisRequest; print('model ok')"` — model importable
- [ ] `cd puppeteer && python -c "from agent_service.main import app; routes = [r.path for r in app.routes]; print([r for r in routes if 'diagnosis' in r])"` — both diagnosis routes listed
- [ ] `cd puppeteer && pytest tests/ -x -q` — full test suite passes (no regressions)
</verification>

<success_criteria>
- All three tasks completed
- `get_dispatch_diagnosis` returns `stuck_assigned` reason for ASSIGNED jobs past threshold
- `POST /jobs/dispatch-diagnosis/bulk` endpoint registered and functional
- `BulkDiagnosisRequest` model added to models.py
- All existing tests pass
- No changes to existing PENDING diagnosis logic
</success_criteria>

<output>
After completion, create `.planning/phases/88-dispatch-diagnosis-ui/88-01-SUMMARY.md`
</output>
