---
phase: 58-research-organisational-sso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/58-research-organisational-sso/58-RESEARCH.md
autonomous: false
requirements:
  - SSO-01
  - SSO-02
  - SSO-03
  - SSO-04
  - SSO-05
  - SSO-06

must_haves:
  truths:
    - "The design document contains a protocol recommendation that names OIDC, provides rationale citing Axiom's existing OAuth 2.0 device flow, and explicitly defers SAML as a future extension point"
    - "The design document specifies the full JWT bridge exchange flow — OIDC callback to Axiom JWT issuance — including how SSO logout triggers a token_version increment to invalidate all existing sessions"
    - "The design document covers RBAC group mapping: re-sync on every login, highest-role-wins priority, default viewer role on first SSO login, and JSON config format"
    - "The design document covers all five IdPs with equal depth (Cloudflare Access, Okta, Azure AD/Entra ID, Google Workspace, Authentik/Keycloak), including a CF Access risk/mitigation sub-section for the Cf-Access-Jwt-Assertion header"
    - "The design document specifies the EE plugin extension points SSO uses (entry_points, stub router, sso_router.py), confirming CE installs see zero SSO code"
    - "The design document defines the 2FA interaction policy: Mode A (SSO satisfies 2FA via amr claim) and Mode B (always require Axiom TOTP), with the TOTP enrolment prompt path for unenrolled SSO users"
    - "A reader with no prior SSO context can understand the proposal and why OIDC was chosen"
  artifacts:
    - path: ".planning/phases/58-research-organisational-sso/58-RESEARCH.md"
      provides: "Complete SSO design document covering all 8 sections across 6 requirements"
      contains: "GET /auth/sso/login"
  key_links:
    - from: "58-RESEARCH.md Protocol Recommendation"
      to: "SSO-01 requirement"
      via: "OIDC recommendation with device flow rationale, SAML deferred"
      pattern: "OIDC|SAML|device.flow|Authlib"
    - from: "58-RESEARCH.md JWT Bridge Design"
      to: "SSO-02 requirement"
      via: "token_version increment on logout, exchange flow pseudocode"
      pattern: "token_version|sso_callback|sso/callback"
    - from: "58-RESEARCH.md RBAC Group Mapping"
      to: "SSO-03 requirement"
      via: "re-sync on login, default viewer, JSON mapping config"
      pattern: "map_groups_to_role|sso_group_mapping|viewer"
    - from: "58-RESEARCH.md IdP Coverage — CF Access"
      to: "SSO-04 requirement"
      via: "Cf-Access-Jwt-Assertion risk + mitigation sub-section"
      pattern: "Cf-Access-Jwt-Assertion|CF_CERTS_URL|spoofed"
    - from: "58-RESEARCH.md Air-Gap Isolation"
      to: "SSO-05 requirement"
      via: "EE plugin entry_points, stub router, CE code path"
      pattern: "sso_stub_router|axiom\\.ee|entry.point"
    - from: "58-RESEARCH.md 2FA Interaction Policy"
      to: "SSO-06 requirement"
      via: "Mode A/B config, amr claim, TOTP enrolment prompt"
      pattern: "sso_2fa_mode|amr|Mode A|Mode B"
---

<objective>
Validate and finalise the organisational SSO design document so a future developer can implement SSO without re-doing protocol or architecture choices.

Purpose: Phase 58 is a research-only phase. The output is a single design document (`58-RESEARCH.md`) that answers: "What SSO architecture should Axiom use, and what must be decided before implementation begins?" No implementation. This plan verifies the document satisfies all six requirements and all CONTEXT.md success criteria, fills any gaps, and obtains human sign-off.

Output: A complete, validated `.planning/phases/58-research-organisational-sso/58-RESEARCH.md` covering (1) use case analysis, (2) JWT bridge design + RBAC mapping + air-gap isolation + dashboard routing, (3) protocol recommendation, (4) full IdP coverage, (5) 2FA interaction policy, (6) CLI SSO complexity assessment, (7) prior art, and (8) complexity/value recommendation with draft implementation sketch.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-research-organisational-sso/58-CONTEXT.md
@.planning/phases/58-research-organisational-sso/58-RESEARCH.md
@.planning/phases/58-research-organisational-sso/58-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and complete the research document</name>
  <files>.planning/phases/58-research-organisational-sso/58-RESEARCH.md</files>
  <action>
Read `58-RESEARCH.md` and `58-CONTEXT.md` in full. Check every locked decision from CONTEXT.md against the document's content using this checklist:

**SSO-01 — Protocol recommendation (OIDC vs SAML):**
- [ ] OIDC is named as the recommendation with a clear rationale
- [ ] Rationale explicitly notes Axiom already ships RFC 8628 OAuth 2.0 device flow — OIDC is the natural extension
- [ ] SAML is acknowledged as an enterprise requirement but explicitly not recommended (XML complexity, no browser-native support, harder in FastAPI)
- [ ] SAML is noted as a future extension point, not v1 scope
- [ ] Authlib 1.6.x is named as the library recommendation with the required dependencies listed (`authlib>=1.3.0`, `httpx>=0.27.0`, `itsdangerous>=2.1.0`)

**SSO-02 — JWT bridge design:**
- [ ] The exchange flow is documented (OIDC authorization code flow, `GET /auth/sso/login` → IdP → `GET /auth/sso/callback` → Axiom JWT → localStorage)
- [ ] The flow sequence diagram or pseudocode is concrete enough for an implementer to start from
- [ ] SSO logout triggers `token_version` increment — same mechanism as password change
- [ ] Both front-channel and back-channel logout are covered
- [ ] The user upsert pseudocode (`sso_callback` handler) is present
- [ ] New DB columns (`sso_provider`, `sso_subject`) and migration SQL are specified
- [ ] `state` + `nonce` CSRF/replay protections are called out

**SSO-03 — RBAC group mapping:**
- [ ] Re-sync on every login is the stated policy
- [ ] Default role on first SSO login is `viewer`
- [ ] `map_groups_to_role()` function or equivalent logic is documented (highest role wins)
- [ ] Group mapping stored as JSON in `Config` table, key `sso_group_mapping`
- [ ] Multiple group membership handling: highest wins
- [ ] `admin` break-glass account remains local-auth; SSO cannot produce admin without explicit config

**SSO-04 — CF Access and IdP coverage:**
- [ ] All five IdPs have dedicated sections: Cloudflare Access, Okta, Azure AD / Entra ID, Google Workspace, Authentik / Keycloak
- [ ] Each IdP section includes: OIDC discovery URL, required scopes, groups claim name, provider-specific quirks
- [ ] CF Access has a risk + mitigation sub-section for `Cf-Access-Jwt-Assertion` header
- [ ] CF Access risk: header can be spoofed if origin is accessible without the tunnel
- [ ] CF Access mitigation: validate JWT signature using CF public key endpoint, verify `aud` + `iss`
- [ ] CF Access key rotation (6-week cycle) is addressed (dynamic key fetch or short-TTL cache)
- [ ] Azure AD GUID groups quirk is documented
- [ ] Google Workspace no-native-groups quirk is documented with workaround

**SSO-05 — Air-gap isolation:**
- [ ] SSO is an EE plugin using the `axiom.ee` entry_points architecture
- [ ] CE code path: stub router returns 402 for `/auth/sso/login` and `/auth/sso/callback`
- [ ] EE code path: `sso_router.py` registered via `register(ctx)` method
- [ ] `authlib` and `httpx` are EE-only dependencies (not in main `requirements.txt`)
- [ ] `EEContext.sso: bool = False` extension point is specified
- [ ] New stub and router file paths are named: `ee/interfaces/sso.py`, `ee/routers/sso_router.py`

**SSO-06 — 2FA interaction policy:**
- [ ] Two configurable modes defined: Mode A (`"idp"`) and Mode B (`"always"`)
- [ ] Mode A: SSO satisfies 2FA if IdP `amr` claim contains `"mfa"`, `"otp"`, or `"swk"`
- [ ] Mode B: Axiom TOTP always required regardless of IdP MFA
- [ ] SSO user without TOTP enrolled hits step-up action → prompted to enrol TOTP (graceful degradation)
- [ ] No silent bypass, no lock-out
- [ ] Policy stored in `Config` table, key `sso_2fa_mode`
- [ ] `amr` claim reference table (RFC 8176 values) is present
- [ ] CLI SSO is covered with honest complexity assessment and deferral recommendation

**General requirements from CONTEXT.md:**
- [ ] Document mirrors Phase 57's four-part structure (use case analysis, architecture, protocol recommendation, complexity/value recommendation)
- [ ] Draft login flow and token exchange sketch is concrete enough for an implementer to start
- [ ] Prior art section covers Grafana, Gitea, Vault (minimum three tools)
- [ ] Document is readable by someone with no prior SSO context (verify Summary opens with accessible prose)
- [ ] Draft implementation sketch includes new routes, CE stubs, and Admin config keys table

For each checklist item:
- If the section exists and satisfies the criterion → mark checked, no changes needed
- If the section exists but is incomplete → add the missing content in place
- If a section is missing entirely → add it

After completing the checklist, if any changes were made to the document, write the updated file. If the document already satisfies all criteria, make no changes.

Do NOT add implementation code. This is a design/research document — prose, tables, pseudocode sketches, and sequence diagrams are appropriate. Full working code is not.
  </action>
  <verify>
    <automated>
grep -c "GET /auth/sso/login\|GET /auth/sso/callback" .planning/phases/58-research-organisational-sso/58-RESEARCH.md && grep -c "token_version" .planning/phases/58-research-organisational-sso/58-RESEARCH.md && grep -c "Cf-Access-Jwt-Assertion" .planning/phases/58-research-organisational-sso/58-RESEARCH.md && grep -c "sso_2fa_mode\|Mode A\|Mode B" .planning/phases/58-research-organisational-sso/58-RESEARCH.md && grep -c "sso_stub_router\|axiom\.ee\|entry.point\|entry_point" .planning/phases/58-research-organisational-sso/58-RESEARCH.md && grep -c "map_groups_to_role\|sso_group_mapping" .planning/phases/58-research-organisational-sso/58-RESEARCH.md
All six commands must return a non-zero count.
    </automated>
  </verify>
  <done>
58-RESEARCH.md exists and contains all eight required sections. Every locked decision from CONTEXT.md is satisfied. Key content markers present: "GET /auth/sso/login", "token_version", "Cf-Access-Jwt-Assertion", "sso_2fa_mode", "axiom.ee", "map_groups_to_role". All six SSO requirements are addressed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
A complete organisational SSO design document at `.planning/phases/58-research-organisational-sso/58-RESEARCH.md`. The document covers: use case analysis (who needs SSO, what deployment scenarios it targets), JWT bridge design (full OIDC exchange flow, token_version invalidation on logout, user upsert pseudocode, DB migration SQL), RBAC group mapping (re-sync on login, default viewer role, priority logic, JSON config), protocol recommendation (OIDC with Authlib, SAML deferred), full IdP coverage (CF Access with risk/mitigation, Okta, Azure AD/Entra ID, Google Workspace, Authentik/Keycloak), 2FA interaction policy (Mode A/B, TOTP enrolment prompt), CLI SSO complexity assessment with deferral recommendation, prior art (Grafana, Gitea, Vault, Argo CD), and a complexity/value recommendation with draft implementation sketch.
  </what-built>
  <how-to-verify>
Read `.planning/phases/58-research-organisational-sso/58-RESEARCH.md` and verify against each success criterion:

1. **Protocol recommendation (SSO-01):** The document names OIDC as the recommendation. The rationale explicitly mentions Axiom's existing RFC 8628 device flow as the reason OIDC is a natural extension. SAML is acknowledged but explicitly not recommended for v1; it is noted as a future extension point. Authlib 1.6.x is named as the client library.

2. **JWT bridge (SSO-02):** The document contains a sequence diagram or equivalent showing the full OIDC authorization code flow ending with an Axiom JWT in localStorage. The document specifies that SSO logout triggers a `token_version` increment (same mechanism as password change), invalidating all existing Axiom sessions. Both front-channel and back-channel logout are covered. User upsert pseudocode is present. New `sso_provider` and `sso_subject` DB columns and migration SQL are specified.

3. **RBAC mapping (SSO-03):** The document specifies re-sync on every login. The default role for first SSO login is `viewer`. The group mapping function (highest role wins across `admin` > `operator` > `viewer`) is documented. The `sso_group_mapping` JSON config key is specified. The `admin` break-glass account is preserved as local-auth only.

4. **CF Access and IdP coverage (SSO-04):** All five IdPs have dedicated sections with OIDC discovery URL, required scopes, groups claim name, and provider-specific quirks. CF Access has a risk + mitigation sub-section that names the `Cf-Access-Jwt-Assertion` header spoofing risk and mandates signature validation via CF's public key endpoint. Azure AD's GUID groups quirk and Google Workspace's no-native-groups quirk are both addressed.

5. **Air-gap isolation (SSO-05):** The document specifies SSO as an EE plugin using the `axiom.ee` entry_points architecture. CE installs get stub routes returning 402. `authlib` and `httpx` are EE-only dependencies. The file paths `ee/interfaces/sso.py` and `ee/routers/sso_router.py` are named.

6. **2FA interaction policy (SSO-06):** The document defines Mode A (`"idp"` — SSO satisfies 2FA if `amr` claim contains MFA evidence) and Mode B (`"always"` — Axiom TOTP always required). The TOTP enrolment prompt for unenrolled SSO users hitting step-up actions is described. No silent bypass and no lock-out.

7. **Readability:** The Summary section is accessible to a reader with no prior SSO context. The proposal and recommendation are clear without reading the full document.

Expected result: All seven checks pass. The document gives a future developer everything needed to implement SSO without re-doing protocol or architecture choices.
  </how-to-verify>
  <resume-signal>Type "approved" if the document satisfies all criteria, or describe any gaps to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 58 verification is document-review only (no code). The following must be true:

1. `58-RESEARCH.md` exists at `.planning/phases/58-research-organisational-sso/58-RESEARCH.md`
2. All six SSO requirements are addressed in named sections
3. Six content markers confirmed by grep: "GET /auth/sso/login", "token_version", "Cf-Access-Jwt-Assertion", "sso_2fa_mode", "axiom.ee", "map_groups_to_role"
4. Human sign-off received on the checkpoint task
5. Phase SUMMARY written to `.planning/phases/58-research-organisational-sso/58-01-SUMMARY.md`
</verification>

<success_criteria>
- The design document satisfies SSO-01: OIDC recommended with rationale tied to existing device flow; SAML deferred
- The design document satisfies SSO-02: full JWT bridge exchange flow documented with token_version invalidation hook
- The design document satisfies SSO-03: RBAC group mapping designed with re-sync policy, default viewer role, and JSON config format
- The design document satisfies SSO-04: all five IdPs covered; CF Access has dedicated risk/mitigation sub-section
- The design document satisfies SSO-05: EE plugin extension points specified; zero CE footprint confirmed
- The design document satisfies SSO-06: Mode A/B 2FA policy defined; TOTP enrolment prompt path for unenrolled SSO users
- Human reviewer confirms the document is complete and a developer could implement SSO from it
- No implementation code was written (this is a research-only phase)
</success_criteria>

<output>
After completion, create `.planning/phases/58-research-organisational-sso/58-01-SUMMARY.md` following the summary template.
</output>
