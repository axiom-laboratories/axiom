---
phase: 142-wheel-signing-tool-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - axiom-licenses/tests/tools/test_sign_wheels.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "test_sign_wheels.py has 12 working tests verifying wheel discovery, hashing, signing, manifests, and CLI flags"
    - "All 12 tests pass with pytest exit code 0"
    - "Tests validate both success paths and error cases (no wheels, SHA256 mismatch)"
  artifacts:
    - path: axiom-licenses/tests/tools/test_sign_wheels.py
      provides: "12 executable test implementations"
      min_lines: 150
    - path: axiom-licenses/tests/conftest.py
      provides: "All required fixtures (temp_wheel_dir, test_keypair, sample_wheel, sample_manifest)"
      contains: "temp_wheel_dir, test_keypair, sample_wheel, sample_manifest"
  key_links:
    - from: "test_sign_wheels.py"
      to: "sign_wheels module"
      via: "direct function import"
      pattern: "from sign_wheels import"
---

<objective>
Implement 12 test stubs in test_sign_wheels.py to validate the wheel signing tool's core functionality: wheel discovery, hashing, Ed25519 signature generation, manifest creation, and verification workflows.

Purpose: Complete Wave 1 of test gap closure for Phase 140's wheel signing infrastructure (sign_wheels.py and gen_wheel_key.py). These tests validate release-time wheel manifest signing and verification.

Output: 12 passing unit tests covering:
- Wheel discovery in directory
- SHA256 hashing with 64KB chunking
- Ed25519 signature format (base64 encoded)
- Manifest file naming and structure
- `--deploy-name` flag behavior
- Error case: no wheels found
- Verification mode: successful verification
- Verification mode: exit codes
- Key resolution from `--key` arg
- Key resolution from `AXIOM_WHEEL_SIGNING_KEY` env var
- `--quiet` flag output suppression
- Error case: SHA256 mismatch detection
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/142-wheel-signing-tool-tests/142-CONTEXT.md
@.planning/phases/142-wheel-signing-tool-tests/142-RESEARCH.md

# Fixtures (already defined, reuse directly)
@axiom-licenses/tests/conftest.py

# Source code (understand the APIs)
@axiom-licenses/tools/sign_wheels.py
@axiom-licenses/tools/gen_wheel_key.py

# Reference: existing test pattern
@axiom-licenses/tests/test_issue_licence.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement test_sign_wheels.py (12 tests)</name>
  <files>axiom-licenses/tests/tools/test_sign_wheels.py</files>
  <behavior>
    - test_wheel_discovery: verify sign_wheels discovers all .whl files in a directory
    - test_wheel_hash_chunked: verify SHA256 computation uses 64KB chunks (matches conftest.sample_manifest pattern)
    - test_signature_format: verify Ed25519 signature is computed on SHA256 hex (UTF-8 encoded) and base64-encoded in output
    - test_manifest_naming: verify manifest JSON files are written with correct naming (wheel_name.manifest.json)
    - test_deploy_name_flag: verify --deploy-name flag also writes axiom_ee.manifest.json
    - test_no_wheels_error: verify sys.exit(1) when no wheels found in directory
    - test_verify_mode: verify --verify loads public key and validates all manifest signatures
    - test_verify_exit_codes: verify --verify returns appropriate exit codes (0 success, 1 fail)
    - test_key_resolution_arg: verify --key argument takes priority for key resolution
    - test_key_resolution_env: verify fallback to AXIOM_WHEEL_SIGNING_KEY env var when no --key arg
    - test_quiet_flag: verify --quiet suppresses per-wheel summary output to stderr
    - test_verify_sha256_mismatch: verify verify_manifests detects SHA256 mismatches and reports FAIL
  </behavior>
  <action>
Import directly from sign_wheels module:
```python
from sign_wheels import sign_wheels, hash_wheel, resolve_key, verify_manifests
```

Use argparse.Namespace to simulate CLI args (Pattern 4 from RESEARCH.md):
```python
args = argparse.Namespace(key=str(path))
```

Key implementation patterns (from RESEARCH.md):

**Pattern 1: Direct function import (no subprocess)**
- Import functions directly: `from sign_wheels import sign_wheels, hash_wheel, resolve_key, verify_manifests`
- Call functions with loaded keys (deserialized from test_keypair fixture)
- Fixtures (temp_wheel_dir, sample_wheel, test_keypair, sample_manifest) are pre-defined in conftest.py

**Pattern 2: SystemExit assertions**
- For tests expecting errors: `with pytest.raises(SystemExit) as exc_info:`
- For test_no_wheels_error: call sign_wheels on empty directory, expect SystemExit
- For key resolution errors: expect SystemExit with "no signing key" or "not found" messages

**Pattern 3: Ed25519 sign/verify handshake**
- When testing, deserialize keys from test_keypair first:
```python
private_pem, public_pem = test_keypair
private_key = serialization.load_pem_private_key(private_pem, password=None)
```
- Sign the UTF-8 hex string (not raw wheel bytes): `message = sha256_hex.encode('utf-8')`
- Verify with 2-arg form: `public_key.verify(signature_bytes, message)`

**Pattern 4: Key resolution via argparse.Namespace**
- For resolve_key tests: construct minimal namespace with .key attribute
- For env var tests: use monkeypatch.setenv("AXIOM_WHEEL_SIGNING_KEY", str(path))

**Test-specific guidance:**

test_wheel_discovery: sample_wheel fixture creates one .whl; expect glob("*.whl") returns 1 file

test_wheel_hash_chunked: use hash_wheel() function directly, compare output to conftest.sample_manifest hash pattern

test_signature_format: 
- Call sign_wheels(temp_wheel_dir, private_key) with sample_wheel already placed
- Verify manifest JSON contains "sha256" (hex string) and "signature" (base64 string)
- Decode signature and verify it matches expected format

test_manifest_naming: 
- After sign_wheels() returns, glob for *.manifest.json
- Verify count matches wheel count (1 in this case)
- Verify manifest path = wheel_path.with_suffix(".manifest.json")

test_deploy_name_flag:
- Call sign_wheels(..., deploy_name=True)
- Expect both versioned manifest (axiom_ee-2.0-py3-none-any.manifest.json) AND fixed name (axiom_ee.manifest.json) to exist

test_no_wheels_error:
- Create empty temp_wheel_dir (no wheels)
- Deserialize private_key from test_keypair
- Call sign_wheels(empty_dir, private_key)
- Expect pytest.raises(SystemExit)

test_verify_mode:
- Place sample_wheel and sample_manifest in temp_wheel_dir
- Deserialize public_key from test_keypair[1]
- Call verify_manifests(temp_wheel_dir, public_key)
- Expect return value True

test_verify_exit_codes:
- Create a valid wheel+manifest pair
- Call verify_manifests and check return value (True = 0, False = 1)
- May need wrapper in resolve_key context; for now assert on return value

test_key_resolution_arg:
- Write private_pem to temp file
- Create args = Namespace(key=str(temp_path))
- Call resolve_key(args, mode="private")
- Expect returned key object is not None

test_key_resolution_env:
- Write private_pem to temp file
- Use monkeypatch.setenv("AXIOM_WHEEL_SIGNING_KEY", str(temp_path))
- Create args = Namespace(key=None)
- Call resolve_key(args, mode="private")
- Expect returned key is not None

test_quiet_flag:
- Use capsys fixture to capture stderr
- Call sign_wheels(..., quiet=True)
- Call capsys.readouterr()
- Expect "Signed:" NOT in captured.err

test_verify_sha256_mismatch:
- Place sample_wheel in temp_wheel_dir
- Write sample_manifest to disk, but corrupt the "sha256" field
- Call verify_manifests(temp_wheel_dir, public_key)
- Expect return value False

Do NOT use subprocess invocation (CONTEXT.md explicitly prohibits). All tests import and call functions directly. No CLI argument parsing in tests.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/axiom-licenses && python -m pytest tests/tools/test_sign_wheels.py -v</automated>
  </verify>
  <done>
All 12 tests in test_sign_wheels.py pass (pytest exit code 0).
Tests cover wheel discovery, hashing, signing, manifest creation, verification, key resolution, flags, and error cases.
No "TODO" comments remain in test_sign_wheels.py.
  </done>
</task>

</tasks>

<verification>
After completing this plan:
1. Run `cd axiom-licenses && python -m pytest tests/tools/test_sign_wheels.py -v` and verify all 12 tests pass
2. Verify no "TODO:" comments remain in test_sign_wheels.py
3. Verify sample_wheel fixture is properly used (creating temporary .whl files)
4. Verify all imports (sign_wheels, hash_wheel, resolve_key, verify_manifests, serialization, argparse, etc.) are present
5. Verify monkeypatch.setenv is used for env var tests (not direct os.environ manipulation)
</verification>

<success_criteria>
- All 12 tests have concrete implementations (no `assert False, "TODO..."`)
- pytest exit code 0 when running test_sign_wheels.py
- Tests import and call functions directly (no subprocess)
- Tests use established patterns: SystemExit for errors, monkeypatch for env vars, Namespace for args
- Key deserialization follows conftest.py pattern (load_pem_private_key + load_pem_public_key)
- Sample fixtures are properly utilized in all tests
</success_criteria>

<output>
After completion, create `.planning/phases/142-wheel-signing-tool-tests/142-01-SUMMARY.md`
</output>
