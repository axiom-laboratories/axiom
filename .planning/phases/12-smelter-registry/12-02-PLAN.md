# Plan 12-02: Database Migrations & Model Updates

**Objective**: Implement the database schema changes for the Smelter Registry (SMLT-01) and compliance tracking (SMLT-05).

## Context
Phase 12 requires a new table for vetted ingredients and a compliance flag on existing templates.

## Tasks
1. **Create `puppeteer/migration_v28.sql`**:
    - Define `approved_ingredients` table (id, name, version_constraint, sha256, os_family, is_vulnerable, vulnerability_report, created_at, updated_at).
    - Add `is_compliant` (Boolean, default True) to `puppet_templates` table.
2. **Update `puppeteer/agent_service/db.py`**:
    - Add `ApprovedIngredient` SQLAlchemy model.
    - Add `is_compliant` field to `PuppetTemplate` model.
3. **Update `puppeteer/agent_service/models.py`**:
    - Add `ApprovedIngredientCreate`, `ApprovedIngredientUpdate`, and `ApprovedIngredientResponse` Pydantic models.
4. **Verification**:
    - Run `python3 -m pytest puppeteer/tests/test_smelter.py::test_template_compliance_badging_stub` and confirm it now PASSES.

## Success Criteria
- SQL migration file exists.
- `db.py` and `models.py` accurately reflect the new schema.
- Wave 0 test for compliance badging passes.

## Output
Create `.planning/phases/12-smelter-registry/12-02-SUMMARY.md` after completion.
Update `STATE.md` with progress.
