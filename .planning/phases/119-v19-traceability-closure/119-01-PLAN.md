---
phase: 119-v19-traceability-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/108-transitive-dependency-resolution/108-01-SUMMARY.md
  - .planning/phases/110-cve-transitive-scan-dependency-tree-ui/110-01-SUMMARY.md
  - .planning/phases/111-npm-nuget-oci-mirrors/111-01-SUMMARY.md
  - .planning/phases/111-npm-nuget-oci-mirrors/111-02-SUMMARY.md
  - .planning/phases/112-conda-mirror-mirror-admin-ui/112-02-SUMMARY.md
  - .planning/phases/112-conda-mirror-mirror-admin-ui/112-02b-SUMMARY.md
  - .planning/phases/113-script-analyzer/113-01-SUMMARY.md
  - .planning/phases/114-curated-bundles-starter-templates/114-02-SUMMARY.md
autonomous: true
requirements:
  - MIRR-03
  - MIRR-04
  - MIRR-05
  - MIRR-09
  - UX-01
  - UX-02
  - UX-03
  - DEP-01
  - DEP-02
  - DEP-03
  - DEP-04
  - MIRR-08

must_haves:
  truths:
    - All 7 unchecked requirements have working code implementations verified via grep
    - REQUIREMENTS.md checkboxes are updated for all 7 unsatisfied requirements
    - All 12 gap-closure SUMMARY.md files have `requirements_completed` frontmatter added
    - Traceability table in REQUIREMENTS.md shows all verified items as "Complete"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: Updated checkbox status for MIRR-03, MIRR-04, MIRR-05, MIRR-09, UX-01, UX-02, UX-03
      must_contain: "[x]" for all 7 previously unchecked requirements
    - path: .planning/phases/*/\*-SUMMARY.md (12 files)
      provides: `requirements_completed` field in frontmatter
      must_contain: YAML list of completed requirement IDs
  key_links:
    - from: v19.0-MILESTONE-AUDIT.md (gap map)
      to: code implementations in puppeteer/
      via: grep verification of function names and endpoints
      pattern: Function/endpoint names from audit citations exist in source
    - from: unchecked REQUIREMENTS.md boxes
      to: updated checkboxes + SUMMARY frontmatter
      via: transitive traceability
      pattern: Each requirement verified → checkbox checked → frontmatter added
---

<objective>
Close Wave 1 traceability gaps: verify 7 unchecked requirements have working code, update REQUIREMENTS.md checkboxes, and add `requirements_completed` frontmatter to 12 gap-closure SUMMARY.md files.

Purpose: Establish code-to-documentation traceability for all 12 gap requirements before Wave 2 creates verification reports.
Output: Updated REQUIREMENTS.md with 7 new checkmarks; 12 SUMMARY.md files with `requirements_completed` frontmatter added; all changes committed.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v19.0-MILESTONE-AUDIT.md
@.planning/phases/119-v19-traceability-closure/119-CONTEXT.md
@.planning/phases/119-v19-traceability-closure/119-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify 7 unchecked requirements have working code via grep</name>
  <files>.planning/v19.0-MILESTONE-AUDIT.md (read-only for verification checklist)</files>
  <action>
For each of the 7 unsatisfied requirements from the audit (MIRR-03, MIRR-04, MIRR-05, MIRR-09, UX-01, UX-02, UX-03), use grep to verify the cited code exists and is not a stub/commented out:

1. MIRR-03 (npm mirror): Grep for `_mirror_npm` in puppeteer/agent_service/services/mirror_service.py — confirm function body exists and is not "NotImplementedError" or commented
2. MIRR-04 (NuGet mirror): Grep for `_mirror_nuget` in puppeteer/agent_service/services/mirror_service.py — confirm function body exists
3. MIRR-05 (OCI pull-through): Grep for "registry:2" or "oci" in puppeteer/agent_service/services/foundry_service.py — confirm OCI logic is implemented
4. MIRR-09 (Mirror provisioning): Grep for `/api/admin/mirrors/provision` or `provision` endpoint in puppeteer/agent_service/main.py — confirm endpoint handler exists
5. UX-01 (Script analyzer): Grep for `ScriptAnalyzerPanel` in puppeteer/dashboard/src/views/ and `/api/analyzer/analyze-script` in main.py — confirm both exist
6. UX-02 (Bundle selection): Grep for `BundleAdminPanel` in puppeteer/dashboard/src/views/Admin.tsx and bundle CRUD endpoints in main.py — confirm both exist
7. UX-03 (Starter templates): Grep for `seed_starter_templates` in puppeteer/agent_service/services/foundry_service.py and `UseTemplateDialog` in dashboard — confirm both exist

If any requirement fails the grep check (code not found or is a stub), document as FAIL and note in action output — do NOT fix the code in this phase. If code verification fails for any requirement, that requirement stays unchecked and is noted as a "needs implementation" gap.

All 7 must verify as PASS to proceed with checkbox updates.
  </action>
  <verify>
For each of the 7 requirements, run the grep commands:
- `grep -n "_mirror_npm" puppeteer/agent_service/services/mirror_service.py` (MIRR-03)
- `grep -n "_mirror_nuget" puppeteer/agent_service/services/mirror_service.py` (MIRR-04)
- `grep -n "registry:2\|oci_base_images" puppeteer/agent_service/services/foundry_service.py` (MIRR-05)
- `grep -n "provision" puppeteer/agent_service/main.py | grep api` (MIRR-09)
- `grep -n "ScriptAnalyzerPanel" puppeteer/dashboard/src/views/Templates.tsx` (UX-01)
- `grep -n "BundleAdminPanel" puppeteer/dashboard/src/views/Admin.tsx` (UX-02)
- `grep -n "seed_starter_templates" puppeteer/agent_service/services/foundry_service.py` (UX-03)

All 7 greps must return at least one non-commented line (function exists and is implemented).
  </verify>
  <done>All 7 requirements verified as implemented. Grep results confirm code exists in cited files and is not stub/commented. If any fails, note failure and do not proceed with checkbox update for that requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes for verified requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read REQUIREMENTS.md and update 7 checkboxes:

1. Line ~28: Change `[ ] **MIRR-03**` to `[x] **MIRR-03**` (npm mirror)
2. Line ~29: Change `[ ] **MIRR-04**` to `[x] **MIRR-04**` (NuGet mirror)
3. Line ~30: Change `[ ] **MIRR-05**` to `[x] **MIRR-05**` (OCI pull-through)
4. Line ~34: Change `[ ] **MIRR-09**` to `[x] **MIRR-09**` (Mirror provisioning)
5. Line ~39: Change `[ ] **UX-01**` to `[x] **UX-01**` (Script analyzer)
6. Line ~40: Change `[ ] **UX-02**` to `[x] **UX-02**` (Curated bundles)
7. Line ~41: Change `[ ] **UX-03**` to `[x] **UX-03**` (Starter templates)

Also update the traceability table at the bottom (lines 88–98): Change all 7 entries from `Pending` to `Complete` in the Status column:
- MIRR-03 | Phase 119 | Pending → MIRR-03 | Phase 111 | Complete
- MIRR-04 | Phase 119 | Pending → MIRR-04 | Phase 111 | Complete
- MIRR-05 | Phase 119 | Pending → MIRR-05 | Phase 111 | Complete
- MIRR-09 | Phase 119 | Pending → MIRR-09 | Phase 112 | Complete
- UX-01 | Phase 119 | Pending → UX-01 | Phase 113 | Complete
- UX-02 | Phase 119 | Pending → UX-02 | Phase 114 | Complete
- UX-03 | Phase 119 | Pending → UX-03 | Phase 114 | Complete

(Note: Use the actual phase number from the audit, not Phase 119 — the requirement was completed in the implementing phase, and Phase 119 is just documenting it.)

Preserve all description text unchanged. Only change checkboxes and status values.
  </action>
  <verify>
After updating, grep for the 7 requirement IDs in the modified file and confirm all have `[x]` (not `[ ]`):
`grep -E "^\- \[x\] \*\*(MIRR-03|MIRR-04|MIRR-05|MIRR-09|UX-01|UX-02|UX-03)" .planning/REQUIREMENTS.md`

Should return 7 lines, all with `[x]`.
  </verify>
  <done>REQUIREMENTS.md checkbox section updated with 7 new `[x]` marks. Traceability table updated with Phase numbers and Complete status for all 7 requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Add requirements_completed frontmatter to 12 gap-closure SUMMARY.md files</name>
  <files>
    - .planning/phases/108-transitive-dependency-resolution/108-01-SUMMARY.md
    - .planning/phases/110-cve-transitive-scan-dependency-tree-ui/110-01-SUMMARY.md
    - .planning/phases/111-npm-nuget-oci-mirrors/111-01-SUMMARY.md
    - .planning/phases/111-npm-nuget-oci-mirrors/111-02-SUMMARY.md
    - .planning/phases/112-conda-mirror-mirror-admin-ui/112-02-SUMMARY.md
    - .planning/phases/112-conda-mirror-mirror-admin-ui/112-02b-SUMMARY.md
    - .planning/phases/113-script-analyzer/113-01-SUMMARY.md
    - .planning/phases/114-curated-bundles-starter-templates/114-02-SUMMARY.md
  </files>
  <action>
Add `requirements_completed` field to the YAML frontmatter of 8 SUMMARY.md files per the audit's `completed_by_plans` field. Only add to the completing plan's file, not to all plans claiming a requirement.

Follow the pattern from 107-01-SUMMARY.md (existing working example): `requirements_completed: [REQ-ID1, REQ-ID2, ...]` as a simple YAML list in alphabetical order.

**Unsatisfied (7 requirements, add to 8 files total because UX-01 spans 2 plans):**
1. `108-01-SUMMARY.md` → Add `requirements_completed: [DEP-01]` (partial requirement)
2. `110-01-SUMMARY.md` → Add `requirements_completed: [DEP-02, DEP-03, DEP-04]` (3 partial requirements completed by this plan)
3. `111-01-SUMMARY.md` → Add `requirements_completed: [MIRR-03]` (unsatisfied)
4. `111-02-SUMMARY.md` → Add `requirements_completed: [MIRR-04, MIRR-05]` (unsatisfied)
5. `112-02-SUMMARY.md` → Add `requirements_completed: [MIRR-08]` (partial)
6. `112-02b-SUMMARY.md` → Add `requirements_completed: [MIRR-09]` (unsatisfied)
7. `113-01-SUMMARY.md` → Add `requirements_completed: [UX-01]` (unsatisfied, note: 113-02 also claims UX-01 but 113-01 is the completing plan per audit)
8. `114-02-SUMMARY.md` → Add `requirements_completed: [UX-02, UX-03]` (unsatisfied, note: 114-03 also claims these but 114-02 is the completing plan per audit)

DO NOT add to 108-02, 110-02, 111-03, 113-02, 114-01, 114-03 even though they're listed in audit's `claimed_by_plans` — only the completing plan gets the field per audit's `completed_by_plans`.

Place the new field in the frontmatter after other existing fields (phase, plan, subsystem, tags, requires, provides, affects) or at the end before the closing `---`.

For each file:
1. Read the file to find the frontmatter block (between `---` lines)
2. Add the field in correct YAML list format
3. Write the modified file back

Example format (from 107-01-SUMMARY.md):
```yaml
---
phase: 107-schema-foundation-crud-completeness
plan: 01
subsystem: foundry
tags: [crud, schema, backend, frontend]
requires: []
provides: [CRUD endpoints, EditBlueprint dialog, ApprovedOS tab]
affects: [all downstream foundry phases]
requirements_completed: [MIRR-10, CRUD-01, CRUD-03]
---
```
  </action>
  <verify>
After updating all 8 files, grep for `requirements_completed` in each:
- `grep -n "requirements_completed" .planning/phases/108-transitive-dependency-resolution/108-01-SUMMARY.md` (should find 1)
- `grep -n "requirements_completed" .planning/phases/110-cve-transitive-scan-dependency-tree-ui/110-01-SUMMARY.md` (should find 1)
- `grep -n "requirements_completed" .planning/phases/111-npm-nuget-oci-mirrors/111-01-SUMMARY.md` (should find 1)
- `grep -n "requirements_completed" .planning/phases/111-npm-nuget-oci-mirrors/111-02-SUMMARY.md` (should find 1)
- `grep -n "requirements_completed" .planning/phases/112-conda-mirror-mirror-admin-ui/112-02-SUMMARY.md` (should find 1)
- `grep -n "requirements_completed" .planning/phases/112-conda-mirror-mirror-admin-ui/112-02b-SUMMARY.md` (should find 1)
- `grep -n "requirements_completed" .planning/phases/113-script-analyzer/113-01-SUMMARY.md` (should find 1)
- `grep -n "requirements_completed" .planning/phases/114-curated-bundles-starter-templates/114-02-SUMMARY.md` (should find 1)

All 8 greps should return 1 line each (total 8 occurrences). Verify that requirement IDs match the list above.
  </verify>
  <done>All 12 gap-closure SUMMARY.md files have `requirements_completed` frontmatter added in correct YAML list format with requirement IDs per audit's completing plan assignment.</done>
</task>

</tasks>

<verification>
Wave 1 completion verification:

1. All 7 unchecked requirements verified as implemented via grep (Task 1)
2. REQUIREMENTS.md has 7 new `[x]` checkmarks and traceability table updated to "Complete" (Task 2)
3. All 12 gap-closure SUMMARY.md files have `requirements_completed` frontmatter with correct requirement IDs (Task 3)

This Wave 1 commit establishes the code-to-documentation link. Wave 2 will create VERIFICATION.md files with detailed evidence.
</verification>

<success_criteria>
- All 7 unsatisfied requirements verified as having working code implementations
- REQUIREMENTS.md checkboxes updated from `[ ]` to `[x]` for MIRR-03, MIRR-04, MIRR-05, MIRR-09, UX-01, UX-02, UX-03
- Traceability table updated to show all 7 as "Complete" with correct phase numbers
- 12 SUMMARY.md files have `requirements_completed` field with correct requirement IDs
- All changes committed to git with commit message indicating Wave 1 traceability closure
</success_criteria>

<output>
After completion, create `.planning/phases/119-v19-traceability-closure/119-01-SUMMARY.md` with frontmatter recording:
- phase: 119-v19-traceability-closure
- plan: 01
- requirements_completed: [MIRR-03, MIRR-04, MIRR-05, MIRR-09, UX-01, UX-02, UX-03, DEP-01, DEP-02, DEP-03, DEP-04, MIRR-08]
</output>
