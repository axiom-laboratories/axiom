---
phase: 129-response-model-auto-serialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [puppeteer/agent_service/models.py, puppeteer/tests/test_models_core.py]
autonomous: true
requirements: []
must_haves:
  truths:
    - "ActionResponse model accepts all 8 action statuses without validation error"
    - "PaginatedResponse[T] generic serializes correctly with any model T"
    - "ErrorResponse validates JSON with detail and status_code fields"
    - "Core models pass all unit tests"
  artifacts:
    - path: "puppeteer/agent_service/models.py"
      provides: "ActionResponse, PaginatedResponse[T], ErrorResponse core models"
      exports: ["ActionResponse", "PaginatedResponse", "ErrorResponse"]
    - path: "puppeteer/tests/test_models_core.py"
      provides: "Unit tests for core model serialization"
      contains: "test_action_response_all_statuses, test_paginated_response_generic"
  key_links:
    - from: "ActionResponse"
      to: "~11+ action endpoints"
      via: "response_model decorator"
      pattern: "response_model=ActionResponse"
    - from: "PaginatedResponse[T]"
      to: "~20+ list endpoints"
      via: "response_model decorator"
      pattern: "response_model=PaginatedResponse\\[.*\\]"
---

<objective>
Create core response models (ActionResponse, PaginatedResponse[T], ErrorResponse) as foundational building blocks for Phase 129 standardization.

Purpose: Establish reusable contract models with full Pydantic v2 generic support and documentation. All subsequent domain plans will reference these core models.

Output: Three new models in models.py, comprehensive unit tests validating serialization and edge cases.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-response-model-auto-serialization/129-CONTEXT.md
@.planning/phases/129-response-model-auto-serialization/129-RESEARCH.md
@puppeteer/agent_service/models.py (existing patterns: JobResponse, PaginatedJobResponse, RegisterResponse)
@puppeteer/tests/conftest.py (test fixtures for auth, DB, async client)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core response models (ActionResponse, PaginatedResponse[T], ErrorResponse)</name>
  <files>puppeteer/agent_service/models.py</files>
  <action>
Add three new models to models.py after existing imports. Use Pydantic v2 patterns.

1. **ActionResponse model** (~10 lines):
   - `status: Literal["acknowledged", "cancelled", "revoked", "approved", "deleted", "updated", "created", "enabled", "disabled"]`
   - `resource_type: str` — e.g., "job", "node", "signature"
   - `resource_id: str | int` — the actioned resource ID
   - `message: Optional[str] = None` — optional detail
   - Include `ConfigDict(from_attributes=True)` for ORM compatibility
   - Add Field descriptions: status ("Action status, Literal union catches typos"), resource_type, resource_id, message
   - Ensure Literal generates proper OpenAPI enum

2. **PaginatedResponse[T] generic model** (~15 lines):
   - Imports at top: `from typing import TypeVar, Generic, List`
   - Define `T = TypeVar("T")` above the model
   - `class PaginatedResponse(BaseModel, Generic[T])`
   - Fields: `items: List[T]`, `total: int`, `page: int`, `page_size: int`
   - Each field with `Field(description=...)` and json_schema_extra with examples
   - Example: `items: List[T] = Field(description="Array of items in this page", json_schema_extra={"examples": [[]]})`
   - Include `ConfigDict(from_attributes=True)`

3. **ErrorResponse model** (~5 lines):
   - `detail: str` with Field(description="Error message")
   - `status_code: int` with Field(description="HTTP status code")
   - Include `ConfigDict(from_attributes=True)`

Place all three at the top of models.py (after imports, before JobResponse) so they can be referenced by downstream models and routers.

Verify imports exist: `from typing import TypeVar, Generic, List, Literal, Optional` — add if missing.

No breaking changes to existing models. These are additions only.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets && python -c "from puppeteer.agent_service.models import ActionResponse, PaginatedResponse, ErrorResponse; print('Models imported successfully'); ar = ActionResponse(status='created', resource_type='job', resource_id='123'); print(f'ActionResponse: {ar.model_dump()}'); from pydantic import ValidationError; try: ActionResponse(status='invalid_status', resource_type='job', resource_id='123'); except ValidationError as e: print('Literal validation works')"</automated>
  </verify>
  <done>
    - ActionResponse, PaginatedResponse[T], ErrorResponse models defined in models.py
    - All models use ConfigDict(from_attributes=True)
    - Field descriptions and examples added
    - Pydantic v2 Generic[T] pattern verified
    - Literal status field generates OpenAPI enum
    - Models can be imported without circular dependencies
  </done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for core models</name>
  <files>puppeteer/tests/test_models_core.py</files>
  <action>
Create new test file test_models_core.py to validate core model behavior.

Test structure (use pytest + pydantic's ValidationError):

1. **test_action_response_all_statuses** — Verify all 8 Literal values accepted:
   - Loop through ["acknowledged", "cancelled", "revoked", "approved", "deleted", "updated", "created", "enabled", "disabled"]
   - Create ActionResponse with each status
   - Assert no ValidationError

2. **test_action_response_invalid_status** — Verify Literal validation rejects typos:
   - Create ActionResponse with status="cancelledd" (typo)
   - Expect ValidationError

3. **test_action_response_serialization** — Verify JSON roundtrip:
   - Create ActionResponse, call .model_dump_json(), parse back
   - Assert resource_id preserved (str | int union)

4. **test_action_response_optional_message** — Verify message defaults to None:
   - Create ActionResponse without message field
   - Assert message is None

5. **test_paginated_response_generic_with_string** — Test Generic[T] with simple type:
   - Import List, create PaginatedResponse[str] inline
   - Populate with items=["a", "b"], total=2, page=1, page_size=10
   - Validate no errors

6. **test_paginated_response_generic_with_dict** — Test Generic[T] with dict:
   - Create PaginatedResponse with items=[{"id": 1}, {"id": 2}], total=2, page=1, page_size=10
   - Validate serialization

7. **test_paginated_response_json_roundtrip** — Verify JSON serialization:
   - Create PaginatedResponse[str], .model_dump_json(), parse back
   - Assert all fields match

8. **test_paginated_response_schema_generated** — Verify OpenAPI schema:
   - Call `PaginatedResponse[str].model_json_schema()`
   - Assert schema has items, total, page, page_size keys

9. **test_error_response_creation** — Verify ErrorResponse basic usage:
   - Create ErrorResponse(detail="Not found", status_code=404)
   - Assert fields match

10. **test_error_response_serialization** — Verify JSON roundtrip:
    - Create ErrorResponse, .model_dump_json(), parse back
    - Assert both fields preserved

11. **test_core_models_from_attributes_config** — Verify ORM compatibility:
    - Check that all three models have ConfigDict(from_attributes=True)
    - Import models, assert model_config has from_attributes=True

Use parametrize decorator for status testing to reduce duplication:
```python
@pytest.mark.parametrize("status", ["acknowledged", "cancelled", "revoked", ...])
def test_action_response_statuses(status):
    ar = ActionResponse(status=status, resource_type="job", resource_id="123")
    assert ar.status == status
```

All tests should be < 100 lines total. No async needed (models are sync).
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets && pytest puppeteer/tests/test_models_core.py -v</automated>
  </verify>
  <done>
    - test_models_core.py created with 11+ tests
    - All tests pass (RED state → GREEN state → REFACTOR not needed)
    - ActionResponse Literal validation verified
    - PaginatedResponse[T] generic serialization verified
    - ErrorResponse basic usage verified
    - Models confirmed ORM-compatible (from_attributes=True)
  </done>
</task>

</tasks>

<verification>
1. Models import without errors: `python -c "from puppeteer.agent_service.models import ActionResponse, PaginatedResponse, ErrorResponse"`
2. Test suite passes: `cd puppeteer && pytest tests/test_models_core.py -v` (all 11+ tests green)
3. Literal validation works: Typo status="cancelledd" raises ValidationError
4. Generic[T] works with multiple types: PaginatedResponse[str], PaginatedResponse[dict], PaginatedResponse[JobResponse]
5. ORM compatibility confirmed: ConfigDict(from_attributes=True) on all models
6. Field descriptions populated: `ActionResponse.model_json_schema()` includes descriptions
</verification>

<success_criteria>
- ActionResponse, PaginatedResponse[T], ErrorResponse models implemented in models.py
- Unit tests validate all model behaviors (serialization, validation, generics, ORM compat)
- Zero circular import issues
- Pydantic v2 Generic[T] pattern working as expected
- Models ready for downstream domain plans to reference
- All tests passing in pytest
</success_criteria>

<output>
After completion, create `.planning/phases/129-response-model-auto-serialization/129-01-SUMMARY.md` with:
- Core models defined and tested
- Test results summary
- Status of circular import investigation
- Links to models.py additions and test_models_core.py
</output>
