---
phase: 170
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/deps.py
  - puppeteer/ee/services/vault_service.py
  - puppeteer/agent_service/routers/admin_router.py
  - puppeteer/agent_service/routers/jobs_router.py
  - puppeteer/agent_service/routers/system_router.py
  - puppeteer/agent_service/ee/routers/vault_router.py
  - puppeteer/agent_service/main.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "No deprecation warnings from asyncio.get_event_loop() in async context"
    - "VaultService exposes renewal_failures as a readable property"
    - "VaultService config snapshot is frozen dataclass, preventing DetachedInstanceError"
    - "Residual routes (retention, verification-key, docs, job-definitions alias) removed from main.py"
    - "Residual routes added to appropriate routers (admin, jobs, system)"
    - "All routes continue to function with zero behavior change"
  artifacts:
    - path: "puppeteer/agent_service/deps.py"
      provides: "Async event loop access with get_running_loop()"
      min_lines: 5
      pattern: "asyncio.get_running_loop()"
    - path: "puppeteer/ee/services/vault_service.py"
      provides: "VaultConfigSnapshot frozen dataclass; renewal_failures property"
      min_lines: 40
      contains: "class VaultConfigSnapshot"
    - path: "puppeteer/agent_service/routers/admin_router.py"
      provides: "Retention GET/PATCH routes"
      contains: "@router.get(\"/admin/retention\""
    - path: "puppeteer/agent_service/routers/jobs_router.py"
      provides: "Job-definitions alias route"
      contains: "@router.get(\"/job-definitions\""
    - path: "puppeteer/agent_service/routers/system_router.py"
      provides: "Verification-key and docs routes"
      contains: "@router.get(\"/verification-key\""
    - path: "puppeteer/agent_service/ee/routers/vault_router.py"
      provides: "Updated config snapshot assignment"
      pattern: "VaultConfigSnapshot.from_orm"
    - path: "puppeteer/agent_service/main.py"
      provides: "Cleaned main.py with no residual routes"
      no_pattern: "get_event_loop|@app.get.*retention|@app.get.*job-definitions|@app.get.*verification-key|@app.get.*docs"
  key_links:
    - from: "deps.py"
      to: "audit() async context"
      via: "asyncio.get_running_loop() call"
      pattern: "loop = asyncio.get_running_loop()"
    - from: "vault_service.py __init__"
      to: "VaultConfigSnapshot"
      via: "snapshot at init time"
      pattern: "self.config.*VaultConfigSnapshot"
    - from: "vault_router.py reinit"
      to: "VaultConfigSnapshot"
      via: "from_orm conversion"
      pattern: "VaultConfigSnapshot.from_orm\\(vault_config\\)"
    - from: "main.py"
      to: "admin_router, jobs_router, system_router"
      via: "app.include_router() already registered"
      pattern: "include_router already in effect"
---

<objective>
Fix four LOW-severity code quality and resource safety issues from PR #24 review:
1. Replace deprecated `asyncio.get_event_loop()` with `get_running_loop()` in deps.py
2. Expose `renewal_failures` property on VaultService (currently private)
3. Move residual routes (retention, verification-key, docs, job-definitions) from main.py to appropriate routers
4. Snapshot VaultService config into frozen dataclass to prevent DetachedInstanceError

All fixes maintain zero behavior change. No UI changes, no schema changes, no new features. Pure correctness and hygiene.

Purpose: Improve code quality (deprecation fix), expose necessary internal state (property), reduce main.py sprawl (route consolidation), prevent runtime ORM errors (config snapshot).
Output: Clean, maintainable codebase with all residual technical debt items resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/170-pr-review-fix-code-hygiene-and-resource-safety/170-CONTEXT.md
@.planning/phases/170-pr-review-fix-code-hygiene-and-resource-safety/170-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deprecated asyncio.get_event_loop() in deps.py (D-01)</name>
  <files>puppeteer/agent_service/deps.py</files>
  <read_first>
    - puppeteer/agent_service/deps.py (lines 165-180, the audit() function where the fix applies)
    - 170-PATTERNS.md section "Async loop pattern" for exact replacement
  </read_first>
  <action>
Replace deprecated `asyncio.get_event_loop()` with `asyncio.get_running_loop()` in the audit() function at line ~171.

Current code (lines 170-175):
```python
try:
    loop = asyncio.get_event_loop()
    if loop.is_running():
        loop.create_task(_insert())
except Exception:
    pass
```

Replacement code:
```python
try:
    loop = asyncio.get_running_loop()
    loop.create_task(_insert())
except RuntimeError:
    # Called outside async context
    pass
```

Key points:
- `asyncio.get_running_loop()` raises `RuntimeError` if called outside async context (correct failure mode)
- `asyncio.get_event_loop()` is deprecated in Python 3.10+ and emits DeprecationWarning
- The `if loop.is_running()` check is unnecessary because `get_running_loop()` only returns if running
- Exception catch type changes from `Exception` to `RuntimeError` for specificity (the only exception raised by `get_running_loop()`)
  </action>
  <verify>
    <automated>grep -n "asyncio.get_running_loop()" puppeteer/agent_service/deps.py | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Line 171 contains `asyncio.get_running_loop()` (not `get_event_loop()`)
    - The exception handler catches `RuntimeError` (or remains `Exception` for safety)
    - The `if loop.is_running()` check is removed
    - No other occurrences of `get_event_loop()` in non-test code remain (verified by grep)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add renewal_failures property and VaultConfigSnapshot to vault_service.py (D-02, D-08, D-09, D-10)</name>
  <files>puppeteer/ee/services/vault_service.py</files>
  <read_first>
    - puppeteer/ee/services/vault_service.py (lines 1-50, the imports and __init__)
    - puppeteer/ee/services/siem_service.py (lines 1-50, the frozen dataclass pattern)
    - puppeteer/agent_service/db.py (lines 114-132, VaultConfig ORM model)
    - 170-PATTERNS.md section "VaultConfigSnapshot definition" and "VaultService.__init__ pattern"
  </read_first>
  <action>
Add three changes to `vault_service.py`:

**Change 1: Add frozen VaultConfigSnapshot dataclass (after imports, before VaultError)**
```python
from dataclasses import dataclass

@dataclass(frozen=True)
class VaultConfigSnapshot:
    """Immutable snapshot of VaultConfig values taken at service construction time.

    Avoids DetachedInstanceError when the ORM session that loaded the config
    is closed or committed while the long-lived singleton still holds a reference.
    """
    enabled: bool
    vault_address: str
    role_id: str
    secret_id: str
    mount_path: str
    namespace: Optional[str]
    provider_type: str

    @classmethod
    def from_orm(cls, vc: "VaultConfig") -> "VaultConfigSnapshot":
        """Convert VaultConfig ORM object to snapshot."""
        return cls(
            enabled=vc.enabled,
            vault_address=vc.vault_address,
            role_id=vc.role_id,
            secret_id=vc.secret_id,
            mount_path=vc.mount_path,
            namespace=vc.namespace,
            provider_type=vc.provider_type,
        )
```

**Change 2: Update VaultService.__init__ to snapshot config (lines 35-43)**
Replace:
```python
def __init__(self, config: Optional[VaultConfig], db: AsyncSession):
    self.config = config
    self.db = db
```

With:
```python
def __init__(self, config: Optional[VaultConfig], db: AsyncSession):
    self.config: Optional[VaultConfigSnapshot] = (
        VaultConfigSnapshot.from_orm(config) if config else None
    )
    self.db = db
```

**Change 3: Add renewal_failures @property (after the __init__ method, before startup())**
```python
@property
def renewal_failures(self) -> int:
    """Return count of consecutive lease renewal failures."""
    return self._consecutive_renewal_failures
```

Ensure imports include:
- `from dataclasses import dataclass` (add if not present)
- `Optional` from typing (already present)
  </action>
  <verify>
    <automated>grep -A 8 "class VaultConfigSnapshot" puppeteer/ee/services/vault_service.py</automated>
  </verify>
  <acceptance_criteria>
    - VaultConfigSnapshot class defined with @dataclass(frozen=True)
    - VaultConfigSnapshot has 7 fields: enabled, vault_address, role_id, secret_id, mount_path, namespace, provider_type
    - VaultConfigSnapshot.from_orm() classmethod exists and converts VaultConfig to snapshot
    - VaultService.__init__ snapshots config immediately: `self.config = VaultConfigSnapshot.from_orm(config) if config else None`
    - VaultService has @property renewal_failures that returns `self._consecutive_renewal_failures`
    - Type annotation on self.config is `Optional[VaultConfigSnapshot]`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Migrate residual routes from main.py to appropriate routers (D-03 through D-07)</name>
  <files>
    - puppeteer/agent_service/main.py
    - puppeteer/agent_service/routers/admin_router.py
    - puppeteer/agent_service/routers/jobs_router.py
    - puppeteer/agent_service/routers/system_router.py
  </files>
  <read_first>
    - puppeteer/agent_service/main.py (full file, identify 4 route groups to remove)
    - puppeteer/agent_service/routers/admin_router.py (structure for where to add retention routes)
    - puppeteer/agent_service/routers/jobs_router.py (structure for where to add job-definitions alias)
    - puppeteer/agent_service/routers/system_router.py (structure for where to add verification-key and docs routes)
    - 170-PATTERNS.md sections for each router (admin_router.py, jobs_router.py, system_router.py)
  </read_first>
  <action>
Migrate 4 residual route groups from main.py to appropriate routers. Each router is already registered via app.include_router() in main.py, so no wiring changes needed — only adding handlers and removing from main.py.

**Task 3a: Add retention routes to admin_router.py**
- Source: main.py lines ~1079-1124 (GET /api/admin/retention, PATCH /api/admin/retention)
- Destination: admin_router.py (append at end before EOF)
- Pattern: Use @router.get and @router.patch (not @app.get/@app.patch)
- Auth: Keep require_permission("users:write") guard on both routes
- No path prefix needed (router is registered with prefix="/api", routes use "/admin/retention")

**Task 3b: Add job-definitions alias to jobs_router.py**
- Source: main.py lines ~975-984 (GET /job-definitions)
- Destination: jobs_router.py (append at end before EOF)
- Pattern: Use @router.get (not @app.get)
- Path: "/job-definitions" (no /api prefix)
- Auth: Keep require_auth (no special permission)
- Handler: calls scheduler_service.list_job_definitions(db)
- Response model: List[JobDefinitionResponse]

**Task 3c: Add verification-key and docs routes to system_router.py**
- Source: main.py lines ~709-723 (GET /verification-key) and lines ~1008-1059 (GET /api/docs, GET /api/docs/{filename})
- Destination: system_router.py (append at end before EOF)
- Pattern: Use @router.get (not @app.get)
- Path adjustments:
  - /verification-key → no /api prefix needed (used as-is)
  - /api/docs and /api/docs/{filename} → use full path with /api prefix
- Auth: /verification-key is unauthenticated; /api/docs/* require require_auth
- **Path calculation fix (D-07)**: In system_router.py, use 3 dirname() calls instead of 2:
  ```python
  base_dir = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
  ```
  This reaches the same puppeteer/ root when called from agent_service/routers/system_router.py

**Task 3d: Remove 4 route groups from main.py**
- Delete lines containing the @app.get/@app.post decorators and handlers for:
  - Retention routes (GET /api/admin/retention, PATCH /api/admin/retention)
  - Job-definitions alias (GET /job-definitions)
  - Verification-key route (GET /verification-key)
  - Docs routes (GET /api/docs, GET /api/docs/{filename})
- Verify that no duplicate route definitions remain in main.py
- Do NOT remove the route registration calls to include_router() — those are already in place

Ensure imports are correct in each router:
- admin_router.py: Config, ExecutionRecord, require_permission, timedelta (add as needed)
- jobs_router.py: scheduler_service (verify already imported)
- system_router.py: os, Path, validate_path_within (verify and add as needed)
  </action>
  <verify>
    <automated>grep -c "@router.get.*retention" puppeteer/agent_service/routers/admin_router.py</automated>
  </verify>
  <acceptance_criteria>
    - admin_router.py contains @router.get("/admin/retention") and @router.patch("/admin/retention")
    - jobs_router.py contains @router.get("/job-definitions") with require_auth
    - system_router.py contains @router.get("/verification-key") and @router.get("/api/docs")
    - system_router.py docs routes use 3-level dirname() for path calculation
    - main.py has NO @app.get/@app.post decorators for retention, verification-key, docs, or job-definitions
    - All 4 route groups are removed from main.py
    - No duplicate route definitions anywhere
    - app.include_router() calls still present for all routers
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update vault_router.py to use VaultConfigSnapshot (D-11)</name>
  <files>puppeteer/agent_service/ee/routers/vault_router.py</files>
  <read_first>
    - puppeteer/agent_service/ee/routers/vault_router.py (lines 82-90, the reinit line that needs updating)
    - puppeteer/ee/services/vault_service.py (the new VaultConfigSnapshot class)
    - 170-PATTERNS.md section "vault_router.py" for the exact pattern
  </read_first>
  <action>
Update the vault_router.py config reinit line to use VaultConfigSnapshot.

**Change 1: Add import at top of vault_router.py**
Add `VaultConfigSnapshot` to the existing VaultService import:
```python
from ...ee.services.vault_service import VaultService, VaultConfigSnapshot
```
(Adjust relative import path to match existing imports in the file)

**Change 2: Update reinit line (around line 86 in the PATCH /admin/vault/config handler)**
Replace:
```python
vault_service.config = vault_config
```

With:
```python
vault_service.config = VaultConfigSnapshot.from_orm(vault_config)
```

This ensures the config assignment follows the same snapshot pattern as VaultService.__init__.

Verify the reinit happens after the PATCH handler updates the VaultConfig ORM object in the database and before vault_service.startup() is called.
  </action>
  <verify>
    <automated>grep -n "VaultConfigSnapshot" puppeteer/agent_service/ee/routers/vault_router.py | head -2</automated>
  </verify>
  <acceptance_criteria>
    - vault_router.py imports VaultConfigSnapshot alongside VaultService
    - Line ~86 contains `vault_service.config = VaultConfigSnapshot.from_orm(vault_config)` (or similar snapshot assignment)
    - No direct ORM object assignment to vault_service.config (no `vault_service.config = vault_config`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Async task creation | File path access during async event loop operation; task is created fire-and-forget |
| ORM object lifecycle | VaultConfig ORM object created in one session may be accessed after session closes; snapshot ensures isolation |
| Route re-registration | Moving routes between modules could inadvertently change auth/behavior; verification ensures zero change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-01 | Information Disclosure | async loop context fallback | accept | RuntimeError on out-of-context call is correct; fire-and-forget audit is non-critical |
| T-170-02 | Tampering (ORM) | VaultConfig snapshot at init | mitigate | Frozen dataclass prevents accidental mutation; snapshot taken at service init when session is fresh |
| T-170-03 | Privilege Escalation (routing) | Route migration from main.py | mitigate | All routes moved with auth guards intact; zero behavior change verified by grep patterns |

</threat_model>

<verification>
All four fixes are LOW-severity code quality and resource safety improvements with zero behavior change.

Post-execution verification:
1. Run `pytest puppeteer/tests/` — ensure zero new test failures (existing test suite validates route behavior)
2. Run `docker compose -f puppeteer/compose.server.yaml up -d` and verify stack health (all services start cleanly)
3. Verify no deprecation warnings: `python -W all -m pytest puppeteer/tests/ 2>&1 | grep -i deprecat` should return nothing
4. Verify route functionality: POST /api/admin/retention, GET /job-definitions, GET /verification-key, GET /api/docs all return expected status codes
5. Verify Vault config snapshot: access vault_service.renewal_failures property and confirm it returns an integer

All acceptance criteria from each task must be met before marking phase complete.
</verification>

<success_criteria>
- [ ] All 4 tasks completed
- [ ] deps.py: asyncio.get_running_loop() in place of get_event_loop()
- [ ] vault_service.py: VaultConfigSnapshot defined and used in __init__; renewal_failures property exposed
- [ ] Residual routes removed from main.py and added to correct routers
- [ ] vault_router.py: config snapshot assignment updated
- [ ] Zero behavior changes verified (all routes function identically)
- [ ] Zero deprecation warnings in async context
- [ ] Full pytest suite passes with no regressions
- [ ] Docker stack starts cleanly with all services healthy
</success_criteria>

<output>
After completion, create `.planning/phases/170-pr-review-fix-code-hygiene-and-resource-safety/170-01-SUMMARY.md`

Summary should verify:
- All 4 fixes completed
- Code quality improvements (deprecation resolved, property exposed, main.py cleaned, ORM safety improved)
- Zero behavior change confirmed via pytest and route HTTP verification
- No regressions or new failures
</output>
