---
phase: 60-quick-reference
plan: 02
type: execute
wave: 2
depends_on:
  - 60-01
files_modified:
  - docs/docs/quick-ref/course.html
autonomous: true
requirements:
  - QREF-02
  - QREF-04

must_haves:
  truths:
    - "course.html contains no occurrences of 'Master of Puppets' or 'MoP'"
    - "course.html title tag and hero section identify the content as 'Axiom'"
    - "course.html contains no references to the deprecated python_script task type"
    - "course.html base64 content and CSS are byte-identical to the original (no accidental corruption)"
  artifacts:
    - path: "docs/docs/quick-ref/course.html"
      provides: "Rebranded course file with 6 targeted text replacements"
      contains: "Axiom — How It Works Under the Hood"
  key_links:
    - from: "docs/docs/quick-ref/course.html"
      to: "Axiom branding"
      via: "<title> tag and nav-title span and 4 body text occurrences"
---

<objective>
Rebrand `docs/docs/quick-ref/course.html` by replacing all 6 occurrences of "Master of Puppets" with "Axiom", and perform a full accuracy review to confirm no deprecated terminology (e.g. `python_script`) is present.

Purpose: QREF-02 and QREF-04 — course file must be branded as Axiom and must not reference removed or deprecated concepts.
Output: Updated `docs/docs/quick-ref/course.html` with 6 targeted replacements; all other content byte-identical.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-quick-reference/60-CONTEXT.md
@.planning/phases/60-quick-reference/60-RESEARCH.md
@.planning/phases/60-quick-reference/60-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact replacement targets — extracted from line-level audit in RESEARCH.md -->
<!-- Make ONLY these 6 replacements. Do not run global find-replace. -->

Line 6:
  FROM: <title>Master of Puppets — How It Works Under the Hood</title>
  TO:   <title>Axiom — How It Works Under the Hood</title>

Line 613:
  FROM: <span class="nav-title">Master of Puppets</span>
  TO:   <span class="nav-title">Axiom</span>

Line 756:
  FROM: Master of Puppets is not one program
  TO:   Axiom is not one program

Line 885:
  FROM: Master of Puppets uses something much stronger
  TO:   Axiom uses something much stronger

Line 891:
  FROM: Master of Puppets runs its own private CA
  TO:   Axiom runs its own private CA

Line 1681:
  FROM: Master of Puppets solves it with token_version
  TO:   Axiom solves it with token_version
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Apply targeted rebranding replacements to course.html</name>
  <files>docs/docs/quick-ref/course.html</files>
  <action>
    Apply exactly the 6 line-specific replacements listed in the `<interfaces>` block above. Use targeted per-line replacements — NOT a global find-replace — to avoid hitting "master" inside base64-encoded image data or CSS class names.

    Implementation approach: read the file, locate each target string at the expected line, replace only that occurrence. Verify the file size remains approximately the same as before (base64 content is not affected).

    After the 6 replacements, also add a subtitle to the course hero section if one is not already present. The exact wording is at your discretion — something like a tagline underneath the main hero heading that identifies this as Axiom content (e.g. the hero could read "How Axiom Works" as the main h1 and retain the sub-description already present). Do NOT restructure the hero — only update text that says "Master of Puppets".

    Do NOT change any CSS class names, data-* attributes, or base64 content. The HTML structure and all styling must be preserved.

    Then run a full accuracy review of the entire file:
    - Confirm no occurrences of `python_script` task type (the server rejects this; if found, replace with `script` + `runtime: "python"` in any code examples)
    - Confirm all file path references (`node.py`, `runtime.py`, `admin_signer.py`, function names `bootstrap_trust`, `fetch_verification_key`, `execute_task`, `poll_for_work`, `runtime_engine`) are still accurate — the RESEARCH.md audit confirmed these are correct, but re-verify on the actual file
    - Confirm quiz questions and answers reference current architecture — update any that reference removed concepts
  </action>
  <verify>
    <automated>grep -c "Master of Puppets\|\" MoP\"" /home/thomas/Development/master_of_puppets/docs/docs/quick-ref/course.html; echo "expected: 0"</automated>
  </verify>
  <done>
    - `grep "Master of Puppets" docs/docs/quick-ref/course.html` returns no matches
    - `grep "python_script" docs/docs/quick-ref/course.html` returns no matches (or zero count)
    - `<title>` tag contains "Axiom"
    - File renders correctly (no broken HTML structure)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify mkdocs build still passes after course.html changes</name>
  <files></files>
  <action>
    Run `cd /home/thomas/Development/master_of_puppets/docs && mkdocs build --strict` to confirm the rebranded course.html does not break the docs build.

    MkDocs serves HTML files as-is without parsing them, so this is primarily confirming no nav reference is broken and the build pipeline is healthy.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/docs && mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <done>
    `mkdocs build --strict` exits 0. course.html is served at /quick-ref/course.html with Axiom branding. `grep -c "Master of Puppets" docs/docs/quick-ref/course.html` returns 0.
  </done>
</task>

</tasks>

<verification>
- `grep "Master of Puppets" docs/docs/quick-ref/course.html` → no output
- `grep "MoP" docs/docs/quick-ref/course.html` → no output (excluding base64 context)
- `grep "python_script" docs/docs/quick-ref/course.html` → no output
- `grep "<title>" docs/docs/quick-ref/course.html` → line contains "Axiom"
- `cd docs && mkdocs build --strict` exits 0
</verification>

<success_criteria>
QREF-02 satisfied: all "Master of Puppets" and "MoP" occurrences replaced with "Axiom" throughout course.html. QREF-04 satisfied: no deprecated terminology present. MkDocs build remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/60-quick-reference/60-02-SUMMARY.md`
</output>
