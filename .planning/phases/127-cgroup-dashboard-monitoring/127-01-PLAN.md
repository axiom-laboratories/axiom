---
phase: 127-cgroup-dashboard-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [puppeteer/dashboard/src/views/Nodes.tsx, puppeteer/dashboard/src/views/__tests__/Nodes.test.tsx]
autonomous: true
requirements: [CGRP-03, CGRP-04]
must_haves:
  truths:
    - "Each node row displays a cgroup version badge (v2, v1, unsupported, or unknown)"
    - "Badge color matches version status: green (v2), amber (v1), red (unsupported), gray (unknown)"
    - "Hovering over badge shows version-specific tooltip text"
    - "When any online node is not v2, a fleet-wide degradation banner appears at top of Nodes page"
    - "Banner shows count of degraded nodes and breakdown by version"
    - "Banner is not dismissible and persists until issue resolved"
    - "Banner is hidden when all online nodes are v2"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Nodes.tsx"
      provides: "Cgroup badge rendering, degradation banner, v2/v1/unsupported/unknown detection"
      min_lines: 50
    - path: "puppeteer/dashboard/src/views/__tests__/Nodes.test.tsx"
      provides: "Unit tests for badge color mapping, tooltip content, banner visibility logic"
      exports: ["getCgroupBadgeClass", "getCgroupTooltip", "degradation banner filtering"]
  key_links:
    - from: "Nodes.tsx NodeResponse interface"
      to: "detected_cgroup_version field"
      via: "TypeScript optional property"
      pattern: "detected_cgroup_version?: string | null"
    - from: "Node row rendering"
      to: "getCgroupBadgeClass() + getCgroupTooltip()"
      via: "inline badge span with title attribute"
      pattern: "node.detected_cgroup_version && <span title={...}>"
    - from: "Degradation banner"
      to: "node filtering logic"
      via: "filter(n => n.status === 'ONLINE' && n.detected_cgroup_version !== 'v2')"
      pattern: "degradedNodes.length > 0"
---

<objective>
Implement cgroup version badges and degradation warnings on the Nodes dashboard page.

Purpose: Operators see at a glance which nodes have degraded cgroup support (v1 or unsupported), enabling proactive remediation to ensure memory and CPU limits are fully enforced across the cluster.

Output:
- Cgroup version badge per node in Nodes.tsx row header (inline with env_tag)
- Non-dismissible fleet-wide degradation banner (only when degraded nodes exist)
- Unit tests covering badge color mapping, tooltip text, banner visibility
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-cgroup-dashboard-monitoring/127-CONTEXT.md
@.planning/phases/127-cgroup-dashboard-monitoring/127-RESEARCH.md
@.planning/phases/127-cgroup-dashboard-monitoring/127-VALIDATION.md

## Existing Code References

From Nodes.tsx (verified in 127-RESEARCH.md):
- Node interface at ~line 69 (existing fields: node_id, hostname, status, env_tag, capabilities, etc.)
- Existing badge color function pattern (lines 122-144: getEnvTagBadgeClass, getEnvBadgeColor)
- Node row header rendering (lines 390-396: env_tag badge placement)
- Existing warning pattern (lines 412-419: tamper alert banner)

From Phase 123 (Cgroup Detection Backend) SUMMARY:
- `detected_cgroup_version` field added to NodeResponse in agent_service/models.py
- Node heartbeat now includes cgroup version detection (v2, v1, unsupported, null)
- No further backend changes needed for Phase 127
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for cgroup badge and degradation banner logic</name>
  <files>puppeteer/dashboard/src/views/__tests__/Nodes.test.tsx</files>
  <behavior>
    - Test 1: getCgroupBadgeClass('v2') returns emerald badge classes (bg-emerald-500/10 text-emerald-500 border-emerald-500/20)
    - Test 2: getCgroupBadgeClass('v1') returns amber badge classes (bg-amber-500/10 text-amber-500 border-amber-500/20)
    - Test 3: getCgroupBadgeClass('unsupported') returns red badge classes (bg-red-500/10 text-red-500 border-red-500/20)
    - Test 4: getCgroupBadgeClass(null) or (undefined) returns muted classes (bg-muted text-muted-foreground border-muted)
    - Test 5: getCgroupTooltip('v2') returns "Cgroup v2 — Full resource isolation..."
    - Test 6: getCgroupTooltip('v1') returns "Cgroup v1 (Degraded) — Memory limits supported..."
    - Test 7: getCgroupTooltip('unsupported') returns "No cgroup support detected..."
    - Test 8: getCgroupTooltip(null) returns "Cgroup status not reported..."
    - Test 9: Degradation banner shows when nodes array includes ONLINE node with detected_cgroup_version !== 'v2'
    - Test 10: Degradation banner hidden when all ONLINE nodes have detected_cgroup_version === 'v2'
    - Test 11: Degradation banner excludes OFFLINE/REVOKED nodes from count
    - Test 12: Banner breakdown counts v1 and unsupported nodes separately
  </behavior>
  <action>
    Create `puppeteer/dashboard/src/views/__tests__/Nodes.test.tsx` with unit tests for helper functions and degradation banner logic.

    **Helper Functions to Test:**
    - `getCgroupBadgeClass(version)` — maps version string to Tailwind color classes
    - `getCgroupTooltip(version)` — maps version string to tooltip text per CONTEXT.md
    - `getCgroupDisplayText(version)` — returns version or 'unknown'
    - Degradation banner visibility logic — filter online nodes, count degraded, compute breakdown

    **Pattern (from RESEARCH.md code examples §):**
    - Test framework: Vitest (existing)
    - Import helpers from Nodes.tsx (will be exported after implementation)
    - Mock nodes array with various status/cgroup_version combinations
    - Assert exact class strings and tooltip text per CONTEXT.md

    **Do NOT:**
    - Use recharts or UI components in unit tests — focus on logic only
    - Test component rendering (that's integration test) — test helper functions
    - Assume functions are exported yet (they'll be added in Task 2)

    Write tests in RED state. They will fail until Task 2 implements the helpers.
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run test -- src/views/__tests__/Nodes.test.tsx -x 2>&1 | head -50</automated>
  </verify>
  <done>Test file exists with 12 test cases covering badge colors, tooltip text, and degradation banner logic. All tests fail (RED state) because helpers not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cgroup badge rendering and degradation banner in Nodes.tsx</name>
  <files>puppeteer/dashboard/src/views/Nodes.tsx</files>
  <action>
    Implement cgroup version badges and degradation warning banner in Nodes.tsx.

    **Step 1: Update Node interface**
    - Add `detected_cgroup_version?: string | null;` to Node interface at top of file (around line 69 alongside env_tag and other optional fields)
    - TypeScript will now recognize the field from NodeResponse

    **Step 2: Add helper functions (after existing getEnvTagBadgeClass, before render)**
    - Export `getCgroupBadgeClass(version)` — switch statement returning badge classes per version:
      - 'v2' → 'bg-emerald-500/10 text-emerald-500 border-emerald-500/20'
      - 'v1' → 'bg-amber-500/10 text-amber-500 border-amber-500/20'
      - 'unsupported' → 'bg-red-500/10 text-red-500 border-red-500/20'
      - default (null, undefined) → 'bg-muted text-muted-foreground border-muted'
    - Export `getCgroupTooltip(version)` — switch statement returning tooltip text per CONTEXT.md exactly:
      - 'v2': "Cgroup v2 — Full resource isolation. Memory and CPU limits fully enforced."
      - 'v1': "Cgroup v1 (Degraded) — Memory limits supported. CPU enforcement may be limited. Upgrade to v2 recommended."
      - 'unsupported': "No cgroup support detected. Resource limits cannot be enforced. Jobs run without isolation."
      - default: "Cgroup status not reported. Node may be running an older version."
    - Export `getCgroupDisplayText(version)` — return version or 'unknown'

    **Step 3: Add degradation banner (above node list in render)**
    - Calculate: `const onlineNodes = nodes.filter(n => n.status === 'ONLINE');`
    - Calculate: `const degradedNodes = onlineNodes.filter(n => n.detected_cgroup_version && n.detected_cgroup_version !== 'v2');`
    - Count by type: `v1Count` and `unsupportedCount` from degradedNodes
    - Render conditionally: `{degradedNodes.length > 0 && (...)}`
    - Banner styling: `bg-amber-500/10 border border-amber-500/20` (matches existing warning pattern at line 412)
    - Banner layout: AlertTriangle icon + text showing degraded count and breakdown
    - Banner text format: "{N} of {total} nodes have degraded cgroup support" followed by bullet counts for v1 and unsupported
    - Non-dismissible: NO close button, persists until resolved

    **Step 4: Add cgroup badge to node row**
    - In NodeCard component where env_tag badge is rendered (around line 396)
    - After env_tag badge: add inline cgroup badge span
    - Styling: `className={`text-[10px] font-bold px-1.5 py-0.5 rounded border ${getCgroupBadgeClass(...)}`}`
    - Use native `title` attribute for tooltip: `title={getCgroupTooltip(...)}`
    - Display text: `{getCgroupDisplayText(...)}`
    - Render only if version exists: `{node.detected_cgroup_version && (...)}` to avoid extra badge for null

    **Reference Code (from 127-RESEARCH.md):**
    - Helper functions: §"Code Examples" → "Cgroup Badge Color & Tooltip Functions"
    - Degradation banner: §"Degradation Banner Component (Nodes.tsx)"
    - Badge in node row: §"Cgroup Badge in Node Row Header (Nodes.tsx)"

    **Do NOT:**
    - Use custom tooltip component — native `title` attribute is simpler and approved by CONTEXT.md
    - Add recharts or chart libraries
    - Include cgroup_raw debug data in tooltip
    - Count offline/revoked nodes in degradation banner

    **Use Exact Color Classes from CONTEXT.md:**
    - bg-emerald-500/10, text-emerald-500, border-emerald-500/20 (v2)
    - bg-amber-500/10, text-amber-500, border-amber-500/20 (v1)
    - bg-red-500/10, text-red-500, border-red-500/20 (unsupported)
    - bg-muted, text-muted-foreground, border-muted (unknown)
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run test -- src/views/__tests__/Nodes.test.tsx -x 2>&1 | grep -E "(PASS|FAIL|✓|✗)" | head -20</automated>
  </verify>
  <done>
    - Node interface includes `detected_cgroup_version?: string | null`
    - getCgroupBadgeClass(), getCgroupTooltip(), getCgroupDisplayText() helper functions implemented and exported
    - Cgroup version badge appears inline with env_tag in node row header
    - Badge color matches version status (emerald v2, amber v1, red unsupported, gray unknown)
    - Hover tooltip shows version-specific help text
    - Degradation banner renders at top of Nodes page when any online node is not v2
    - Banner shows count and breakdown (v1 count, unsupported count)
    - Banner is non-dismissible (no close button)
    - Banner hidden when all online nodes are v2 or when offline nodes only
    - Unit tests pass (GREEN state): all 12 tests covering badge classes, tooltip text, degradation visibility
  </done>
</task>

</tasks>

<verification>
After Task 2 completes, verify:

1. **Unit tests pass:** `cd puppeteer/dashboard && npm run test -- src/views/__tests__/Nodes.test.tsx -x` returns all GREEN (0 failures)

2. **Badge rendering (manual check in Docker stack):**
   - Start local Docker stack: `cd puppeteer && docker compose -f compose.server.yaml up -d`
   - Navigate to https://localhost:5173/nodes (dashboard Nodes page)
   - Verify each node row shows cgroup badge next to env_tag
   - Badges should be: green (v2), amber (v1), red (unsupported), gray (unknown/null)

3. **Tooltip content:** Hover over cgroup badge, verify tooltip text matches CONTEXT.md exactly

4. **Degradation banner:**
   - If any online node is v1 or unsupported → banner appears at top
   - Banner shows "{N} of {total} online nodes have degraded cgroup support"
   - Banner breakdown shows v1 count and unsupported count separately
   - If all online nodes are v2 → banner is hidden
   - Banner cannot be dismissed (no X button)

5. **Linting:** `cd puppeteer/dashboard && npm run lint` passes with no TypeScript errors
</verification>

<success_criteria>
Phase 127 Plan 01 complete when:
- ✅ Unit tests in Nodes.test.tsx all PASS (12/12 tests green)
- ✅ Cgroup badges render in Nodes.tsx node row (colors per CONTEXT.md)
- ✅ Tooltips show version-specific help text on hover
- ✅ Degradation banner appears when degraded nodes exist
- ✅ Banner breakdown counts v1 and unsupported separately
- ✅ Banner is non-dismissible and persists
- ✅ Banner hidden when all online nodes are v2
- ✅ No TypeScript build errors, lint passes
- ✅ REQUIREMENTS CGRP-03 (badges per node) satisfied
- ✅ REQUIREMENTS CGRP-04 (operator warning on degradation) partially satisfied (Nodes page warning complete; Admin panel in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/127-cgroup-dashboard-monitoring/127-01-SUMMARY.md`

Include:
- Tasks completed (2/2)
- Test status (12 tests passing)
- Code changes: Nodes.tsx (helper functions + interface update + badge rendering + degradation banner)
- Files modified: Nodes.tsx, Nodes.test.tsx
- Commits created
- Next steps: Plan 02 (Admin System Health tab)
</output>
