---
phase: 150-dashboard-read-only-views
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - puppeteer/dashboard/package.json
  - puppeteer/dashboard/src/utils/workflowStatusUtils.ts
  - puppeteer/dashboard/src/views/__tests__/Workflows.test.tsx
  - puppeteer/dashboard/src/views/__tests__/WorkflowDetail.test.tsx
  - puppeteer/dashboard/src/views/__tests__/WorkflowRunDetail.test.tsx
  - puppeteer/dashboard/src/components/__tests__/DAGCanvas.test.tsx
  - puppeteer/dashboard/src/components/__tests__/WorkflowStepNode.test.tsx
  - puppeteer/dashboard/src/components/__tests__/WorkflowStepDrawer.test.tsx
  - puppeteer/dashboard/src/utils/__tests__/workflowStatusUtils.test.ts
  - puppeteer/dashboard/src/hooks/__tests__/useWorkflowQuery.test.ts
  - puppeteer/dashboard/src/hooks/__tests__/useLayoutedElements.test.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "ReactFlow and dagre libraries are installed and importable"
    - "Status utility functions map workflow step statuses to UI colors/variants uniformly"
    - "All test scaffolds exist and pass framework validation (imports, mocks, fixtures)"
    - "Codebase is ready for implementation of DAG rendering, layout, and view components"
  artifacts:
    - path: "puppeteer/dashboard/package.json"
      provides: "ReactFlow + dagre dependencies installed"
      exports: ["@xyflow/react", "@dagrejs/dagre"]
    - path: "puppeteer/dashboard/src/utils/workflowStatusUtils.ts"
      provides: "Status color mapping and variant mapping functions"
      exports: ["getStatusVariant()", "getStatusColor()"]
    - path: "puppeteer/dashboard/src/views/__tests__/*.test.tsx"
      provides: "View test scaffolds (Workflows, WorkflowDetail, WorkflowRunDetail)"
      contains: "import { render, screen }"
    - path: "puppeteer/dashboard/src/components/__tests__/*.test.tsx"
      provides: "Component test scaffolds (DAGCanvas, WorkflowStepNode, WorkflowStepDrawer)"
      contains: "import { render }"
    - path: "puppeteer/dashboard/src/utils/__tests__/workflowStatusUtils.test.ts"
      provides: "Utility function tests (getStatusVariant, getStatusColor)"
      contains: "describe('workflowStatusUtils'"
    - path: "puppeteer/dashboard/src/hooks/__tests__/*.test.ts"
      provides: "Hook test scaffolds (useWorkflowQuery, useLayoutedElements)"
      contains: "import { renderHook }"
  key_links:
    - from: "package.json"
      to: "src/components/DAGCanvas.tsx"
      via: "import { ReactFlow, Node, Edge }"
      pattern: "@xyflow/react"
    - from: "workflowStatusUtils.ts"
      to: "src/components/WorkflowStepNode.tsx"
      via: "import { getStatusVariant, getStatusColor }"
      pattern: "getStatusVariant"
    - from: "test scaffolds"
      to: "implemented components"
      via: "wave 1+ tasks implement against these test files"
      pattern: "describe.*it"
---

<objective>
Establish Wave 0 foundations: install ReactFlow + dagre libraries, create status utility functions, and scaffold all test files for Phase 150. This prepares the codebase for DAG rendering, component implementation, and integration testing.

Purpose: Unblock all downstream implementation tasks by ensuring libraries are available and test infrastructure is in place.

Output:
- ReactFlow + dagre installed and ready to import
- workflowStatusUtils module with getStatusVariant() and getStatusColor() functions matching Jobs/History pattern
- 9 test scaffold files created with proper imports, describe blocks, fixtures, and placeholder test cases
- All tests pass framework validation (no import errors, all mocks in place)
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/150-dashboard-read-only-views/150-CONTEXT.md
@.planning/phases/150-dashboard-read-only-views/150-RESEARCH.md
@puppeteer/dashboard/package.json
@puppeteer/dashboard/src/views/History.tsx
@puppeteer/dashboard/src/views/Jobs.tsx
</context>

<interfaces>
<!-- Key types and exports for test scaffolds -->

From puppeteer/agent_service/models.py (via RESEARCH.md):
```typescript
export interface WorkflowRunResponse {
  id: string;
  workflow_id: string;
  status: 'RUNNING' | 'COMPLETED' | 'PARTIAL' | 'FAILED' | 'CANCELLED';
  started_at: string;
  completed_at?: string;
  step_runs: WorkflowStepRunResponse[];
}

export interface WorkflowStepRunResponse {
  id: string;
  workflow_step_id: string;
  status: 'PENDING' | 'RUNNING' | 'COMPLETED' | 'FAILED' | 'SKIPPED' | 'CANCELLED';
  started_at?: string;
  completed_at?: string;
  job_guid?: string;
}

export interface WorkflowStepResponse {
  id: string;
  node_type: 'SCRIPT' | 'IF_GATE' | 'AND_JOIN' | 'OR_GATE' | 'PARALLEL' | 'SIGNAL_WAIT';
  scheduled_job_id?: string;
}

export interface WorkflowEdgeResponse {
  from_step_id: string;
  to_step_id: string;
}

export interface WorkflowResponse {
  id: string;
  name: string;
  steps: WorkflowStepResponse[];
  edges: WorkflowEdgeResponse[];
  schedule_cron?: string;
  last_run?: WorkflowRunResponse;
}
```

From puppeteer/dashboard/src/views/History.tsx (existing pattern):
```typescript
function getStatusVariant(status?: string) {
  // Returns: 'default' | 'destructive' | 'outline' | 'secondary'
  // Used with <Badge variant={getStatusVariant(status)}>
}
```

From puppeteer/dashboard/src/hooks/useWebSocket.ts (existing pattern):
```typescript
export function useWebSocket(
  onMessage: (event: string, data: any) => void
): void;
```

From puppeteer/dashboard/vite.config.ts (test config):
```typescript
// vitest 3.0.5 configured with @testing-library/react 16.2.0
// Test files match pattern: src/**/__tests__/*.test.{ts,tsx}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install ReactFlow and dagre libraries</name>
  <files>puppeteer/dashboard/package.json</files>
  <action>
    1. Navigate to puppeteer/dashboard directory
    2. Run: npm install @xyflow/react @dagrejs/dagre
    3. Verify installation: npm list @xyflow/react @dagrejs/dagre
    4. Expected versions: @xyflow/react ^12.0.0+, @dagrejs/dagre ^1.0.0+
    5. Do NOT update any existing dependencies; only add ReactFlow and dagre
    6. Commit package.json and package-lock.json with message "deps(dashboard): add ReactFlow + dagre for workflow DAG visualization"
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm list @xyflow/react @dagrejs/dagre | grep -E "@xyflow/react|@dagrejs/dagre"</automated>
  </verify>
  <done>package.json updated, node_modules installed, both libraries importable with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Create workflowStatusUtils with color mapping and variant function</name>
  <files>puppeteer/dashboard/src/utils/workflowStatusUtils.ts</files>
  <action>
    Create src/utils/workflowStatusUtils.ts with the following:

    1. getStatusVariant(status: string | undefined): 'default' | 'destructive' | 'outline' | 'secondary'
       - Maps: PENDING → 'outline' (grey/muted), RUNNING → 'default' (blue), COMPLETED → 'secondary' (green), FAILED → 'destructive' (red), SKIPPED → 'outline', CANCELLED → 'outline', undefined → 'outline'
       - Use the EXACT same mapping as seen in puppeteer/dashboard/src/views/History.tsx (inspect that file first to extract the pattern)
       - Rationale per CONTEXT.md: "status colors: match Jobs screen pattern"

    2. getStatusColor(status: string | undefined): string
       - Returns hex/RGB color code for node borders and fills
       - Maps: PENDING='#888', RUNNING='#3b82f6' (blue), COMPLETED='#10b981' (green), FAILED='#ef4444' (red), SKIPPED='#888', CANCELLED='#888'
       - Used by WorkflowStepNode to set node border color

    3. statusColorMap: Record<string, string>
       - Exported constant with all status → color mappings for easy reference in components

    4. statusVariantMap: Record<string, string>
       - Exported constant with all status → variant mappings

    5. Ensure all three statuses (WorkflowRun statuses: RUNNING, COMPLETED, PARTIAL, FAILED, CANCELLED) and WorkflowStepRun statuses (PENDING, RUNNING, COMPLETED, FAILED, SKIPPED, CANCELLED) are covered with sensible defaults

    Include JSDoc comments on each function. Do NOT import any external UI libraries in this file (keep it pure utilities).
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run test -- --run src/utils/__tests__/workflowStatusUtils.test.ts 2>&1 | head -20</automated>
  </verify>
  <done>File created, all exports defined, test scaffold passes import validation (test task will verify behavior)</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold view test files (Workflows, WorkflowDetail, WorkflowRunDetail)</name>
  <files>
    - puppeteer/dashboard/src/views/__tests__/Workflows.test.tsx
    - puppeteer/dashboard/src/views/__tests__/WorkflowDetail.test.tsx
    - puppeteer/dashboard/src/views/__tests__/WorkflowRunDetail.test.tsx
  </files>
  <action>
    Create three test scaffold files in puppeteer/dashboard/src/views/__tests__/ following the existing pattern from History.test.tsx or Jobs.test.tsx:

    **Workflows.test.tsx** (List view test scaffold):
    - Import: render, screen, vi, @testing-library/react, ReactQueryTestUtils (or mock queryClient)
    - Mock: authenticatedFetch, useQuery hook
    - Test suite: describe('Workflows List View')
    - Placeholder tests:
      - "renders list of workflows with name, step count, last run status, trigger type" (UI-03)
      - "displays empty state when no workflows exist" (UX)
      - "clicking a workflow navigates to detail view" (routing — may be deferred to routing task)
      - "displays last run time and trigger type (MANUAL/CRON/WEBHOOK) correctly" (UI-03)
    - Setup: provide mock queryClient, mock authenticatedFetch to return sample WorkflowResponse[]

    **WorkflowDetail.test.tsx** (Detail view test scaffold):
    - Import: render, screen, vi, useParams (from react-router), @testing-library/react
    - Mock: authenticatedFetch, useQuery, useParams
    - Test suite: describe('WorkflowDetail View')
    - Placeholder tests:
      - "renders DAG canvas with nodes for each step" (UI-01)
      - "displays run history list below DAG canvas" (UI-03)
      - "clicking a run in the history list navigates to run detail" (routing — may be deferred)
      - "DAG shows correct node count matching workflow steps" (UI-01)
    - Setup: mock useParams to return workflowId, mock useQuery to return sample workflow with 3–4 steps

    **WorkflowRunDetail.test.tsx** (Run detail view test scaffold):
    - Import: render, screen, vi, useParams, @testing-library/react
    - Mock: authenticatedFetch, useQuery, useParams
    - Test suite: describe('WorkflowRunDetail View')
    - Placeholder tests:
      - "renders DAG canvas with status colors overlaid for current run" (UI-02)
      - "displays step status list beside or below canvas" (UI-04)
      - "clicking a step node opens right-side drawer" (UI-04)
      - "drawer shows logs for RUNNING/COMPLETED/FAILED steps" (UI-04)
      - "drawer shows 'unrun' message for PENDING/SKIPPED/CANCELLED steps" (UI-04)
    - Setup: mock useParams to return workflowId + runId, mock useQuery to return sample WorkflowRunResponse with mixed statuses

    All three files should:
    - Use vitest syntax (describe, it, expect, vi.mock)
    - Include imports for React, testing-library, mock utilities
    - Include mock fixtures at bottom (sampleWorkflow, sampleRun, etc.) that can be imported by later tests
    - Include queryClient mock setup (following pattern from existing dashboard tests)
    - Have no failing tests initially — all tests marked as pending or skipped if implementation doesn't exist yet
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run test -- --run src/views/__tests__/Workflows.test.tsx src/views/__tests__/WorkflowDetail.test.tsx src/views/__tests__/WorkflowRunDetail.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>Three test files created with proper imports, describe blocks, placeholder test cases, and fixtures. All pass vitest validation (no syntax or import errors).</done>
</task>

<task type="auto">
  <name>Task 4: Scaffold component test files (DAGCanvas, WorkflowStepNode, WorkflowStepDrawer)</name>
  <files>
    - puppeteer/dashboard/src/components/__tests__/DAGCanvas.test.tsx
    - puppeteer/dashboard/src/components/__tests__/WorkflowStepNode.test.tsx
    - puppeteer/dashboard/src/components/__tests__/WorkflowStepDrawer.test.tsx
  </files>
  <action>
    Create three component test scaffold files in puppeteer/dashboard/src/components/__tests__/:

    **DAGCanvas.test.tsx**:
    - Import: render, screen, vi, ReactFlow (or mock it), dagre (or mock), @testing-library/react
    - Mock: ReactFlow component (since actual ReactFlow can be hard to test; mock or render with minimal config)
    - Test suite: describe('DAGCanvas Component')
    - Placeholder tests:
      - "renders ReactFlow component with correct dimensions" (DOM structure)
      - "converts workflow steps to ReactFlow nodes" (data mapping — UI-01)
      - "converts workflow edges to ReactFlow edges" (data mapping)
      - "applies status colors to nodes based on stepRunStatus prop" (UI-02)
      - "calls onNodeClick callback when a node is clicked" (interaction)
      - "node shape props are passed correctly (editable=false for read-only)" (UI-01)
    - Setup: provide sample steps, edges, stepRunStatus object; mock useLayoutedElements hook

    **WorkflowStepNode.test.tsx**:
    - Import: render, screen, vi, Handle (from @xyflow/react, or mock), Badge, @testing-library/react
    - Mock: @xyflow/react/Handle component
    - Test suite: describe('WorkflowStepNode Component')
    - Placeholder tests:
      - "renders node label correctly" (DOM)
      - "renders node shape per type (SCRIPT=rect, IF_GATE=diamond, AND_JOIN=hexagon, OR_GATE=circle, PARALLEL=fork, SIGNAL_WAIT=clock)" (UI-01)
      - "applies status color to node border and background" (UI-02)
      - "displays status badge when status is provided" (UI-02)
      - "applies pulse animation class when status is RUNNING" (UI-02 — animation feedback)
    - Setup: mock node data with various nodeType and status values

    **WorkflowStepDrawer.test.tsx**:
    - Import: render, screen, vi, @testing-library/react, Sheet, @testing-library/user-event
    - Mock: Sheet component (or use actual shadcn Sheet if available in test env), authenticatedFetch, ExecutionLogModal
    - Test suite: describe('WorkflowStepDrawer Component')
    - Placeholder tests:
      - "opens drawer when open prop is true" (DOM)
      - "displays step name and node type in header" (UI-04)
      - "displays status badge matching getStatusVariant()" (UI-04)
      - "shows logs for RUNNING/COMPLETED/FAILED steps (calls ExecutionLogModal)" (UI-04)
      - "shows 'unrun' message for PENDING/SKIPPED/CANCELLED steps (no log fetch)" (UI-04)
      - "calls onOpenChange callback when close button is clicked" (interaction)
    - Setup: provide sample stepRun with various status values

    All three files should:
    - Use vitest syntax and @testing-library/react patterns (render, screen, userEvent)
    - Mock external libraries minimally (keep mocks focused on what's needed for test isolation)
    - Include prop interface documentation in comments
    - Have no failing tests initially — all marked as pending if implementation missing
    - Include sample data fixtures at bottom
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run test -- --run src/components/__tests__/DAGCanvas.test.tsx src/components/__tests__/WorkflowStepNode.test.tsx src/components/__tests__/WorkflowStepDrawer.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>Three component test files created with proper structure, mocks, and placeholder test cases. All pass vitest validation.</done>
</task>

<task type="auto">
  <name>Task 5: Scaffold hook test files (useWorkflowQuery, useLayoutedElements) and utility tests</name>
  <files>
    - puppeteer/dashboard/src/hooks/__tests__/useWorkflowQuery.test.ts
    - puppeteer/dashboard/src/hooks/__tests__/useLayoutedElements.test.ts
    - puppeteer/dashboard/src/utils/__tests__/workflowStatusUtils.test.ts
  </files>
  <action>
    Create three test scaffold files:

    **useWorkflowQuery.test.ts**:
    - Import: renderHook, waitFor, vi, @testing-library/react, @tanstack/react-query, authenticatedFetch
    - Mock: authenticatedFetch, queryClient
    - Test suite: describe('useWorkflowQuery Hook')
    - Placeholder tests:
      - "fetches workflow details from /api/workflows/{id}" (async data fetch)
      - "returns workflow data with steps and edges" (response shape)
      - "handles loading state correctly" (query state)
      - "handles error state when API fails" (error handling)
      - "refetches on interval when refetchInterval set" (polling fallback — UI-02)
      - "cache is properly updated on WebSocket workflow_step_updated event" (WS integration — UI-02)
    - Setup: mock authenticatedFetch, mock queryClient with useQuery hook wrapper

    **useLayoutedElements.test.ts**:
    - Import: renderHook, vi, @dagrejs/dagre, @xyflow/react
    - Mock: @dagrejs/dagre layout function (or use real dagre; it's fast)
    - Test suite: describe('useLayoutedElements Hook')
    - Placeholder tests:
      - "computes node positions using dagre layout algorithm" (UI-01)
      - "respects direction prop (LR = left-to-right)" (UI-01)
      - "memoizes layout result to avoid recomputation on prop change" (performance)
      - "returns nodes with updated position coordinates" (data structure)
      - "returns edges unchanged" (passthrough)
    - Setup: mock sample nodes/edges, call hook with various direction values

    **workflowStatusUtils.test.ts**:
    - Import: describe, it, expect, getStatusVariant, getStatusColor, statusColorMap, statusVariantMap
    - Test suite: describe('workflowStatusUtils')
    - Placeholder tests:
      - "getStatusVariant(PENDING) returns 'outline'" (mapping)
      - "getStatusVariant(RUNNING) returns 'default'" (mapping)
      - "getStatusVariant(COMPLETED) returns 'secondary'" (mapping)
      - "getStatusVariant(FAILED) returns 'destructive'" (mapping)
      - "getStatusVariant(undefined) returns 'outline'" (fallback)
      - "getStatusColor(RUNNING) returns blue hex code" (color mapping)
      - "getStatusColor(COMPLETED) returns green hex code" (color mapping)
      - "getStatusColor(FAILED) returns red hex code" (color mapping)
      - "statusColorMap contains all status keys" (export validation)
      - "statusVariantMap contains all status keys" (export validation)
    - Setup: no external mocks needed; pure function tests

    All three files should:
    - Use vitest syntax (describe, it, expect, vi.mock, renderHook, waitFor)
    - Include proper mock setup for async hooks
    - Have no failing tests initially — tests should pass or be explicitly skipped
    - Include JSDoc comments on test descriptions
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run test -- --run src/hooks/__tests__/useWorkflowQuery.test.ts src/hooks/__tests__/useLayoutedElements.test.ts src/utils/__tests__/workflowStatusUtils.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>Three test files created. All imports valid, test suites structured, placeholder tests ready for implementation. Full test suite passes vitest validation.</done>
</task>

<task type="auto">
  <name>Task 6: Verify all test scaffolds pass validation and full suite runs without errors</name>
  <files>puppeteer/dashboard/src</files>
  <action>
    Run the complete test suite to ensure all scaffolds are properly structured:

    1. cd puppeteer/dashboard
    2. npm run test -- --run 2>&1 | tee test-output.log
    3. Verify no import errors, no syntax errors, no missing mock setup issues
    4. Expected: All 9 test files should appear in output with "pending" or "skip" status if tests are not yet implemented, OR green if tests are passing
    5. Check that @xyflow/react and @dagrejs/dagre are importable (no "cannot find module" errors)
    6. Check that existing tests (History, Jobs, etc.) still pass (no regressions)
    7. Document any import path corrections needed and apply them
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run test -- --run 2>&1 | grep -E "PASS|FAIL|passed|failed" | head -20</automated>
  </verify>
  <done>All test scaffolds pass vitest validation; no import, syntax, or mock setup errors; full suite runs without crashing.</done>
</task>

</tasks>

<verification>
After Plan 01 completion:
- ReactFlow (@xyflow/react ^12.0+) and dagre (@dagrejs/dagre ^1.0+) installed in node_modules and importable
- workflowStatusUtils.ts exported with getStatusVariant(), getStatusColor(), statusColorMap, statusVariantMap
- 9 test scaffold files exist in correct directories with proper vitest structure and mocks
- Full test suite (`npm run test -- --run`) passes without syntax/import errors
- All downstream implementation tasks (Plans 02+) can begin without blocking on dependencies
</verification>

<success_criteria>
- [x] ReactFlow and dagre npm install succeeds without version conflicts
- [x] workflowStatusUtils.ts created with all required exports and JSDoc comments
- [x] 9 test scaffold files created (3 views + 3 components + 3 hooks/utils)
- [x] All test files have proper imports, describe blocks, placeholder tests, and fixtures
- [x] Full test suite runs with `npm run test -- --run` and produces no import/syntax errors
- [x] Existing tests (History, Jobs, etc.) still pass (no regressions)
- [x] Wave 0 is complete and unblocks Wave 1–6 implementation tasks
</success_criteria>

<output>
After completion, create `.planning/phases/150-dashboard-read-only-views/150-01-SUMMARY.md`
</output>
