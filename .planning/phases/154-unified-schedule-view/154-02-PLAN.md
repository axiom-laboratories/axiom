---
phase: 154-unified-schedule-view
plan: 02
type: execute
wave: 2
depends_on: [154-01]
files_modified:
  - puppeteer/tests/test_scheduler.py
  - puppeteer/dashboard/src/__tests__/Schedule.test.tsx
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "GET /api/schedule returns merged, sorted list of ScheduledJob and Workflow entries"
    - "GET /api/schedule filters out inactive jobs and paused workflows"
    - "GET /api/schedule handles invalid cron expressions gracefully without crashing"
    - "GET /api/schedule respects jobs:read permission and rejects unauthorized requests"
    - "Schedule.tsx table renders with correct columns and data from API"
    - "Schedule.tsx row click navigation works: JOB → /job-definitions, FLOW → /workflows/:id"
    - "Schedule.tsx auto-refetch fires every 30 seconds"
    - "Schedule.tsx empty state displays correctly when no entries exist"
  artifacts:
    - path: "puppeteer/tests/test_scheduler.py"
      provides: "Backend integration tests for GET /api/schedule"
      pattern: "test_get_unified_schedule"
    - path: "puppeteer/dashboard/src/__tests__/Schedule.test.tsx"
      provides: "Frontend component tests for Schedule.tsx"
      pattern: "test_schedule\|describe.*Schedule"
  key_links:
    - from: "test_scheduler.py"
      to: "main.py"
      via: "HTTP GET /api/schedule endpoint tests"
      pattern: "client.get.*schedule"
    - from: "Schedule.test.tsx"
      to: "Schedule.tsx"
      via: "render component and verify table output"
      pattern: "render.*Schedule"
    - from: "Schedule.test.tsx"
      to: "useQuery hook"
      via: "mock queryClient and verify refetchInterval"
      pattern: "useQuery"
---

<objective>
Integration testing for Phase 154: Verify backend service method, API endpoint, frontend component, and full-stack flow. This closes UI-05 requirement verification.

Purpose: Confirm unified schedule view works end-to-end, handles edge cases, and integrates with existing permission system.

Output:
- Backend pytest tests (3-5 tests covering merge, filtering, invalid cron, permissions)
- Frontend vitest tests (5-7 tests covering render, navigation, refetch, empty state)
- All tests passing
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Backend Test Patterns (from Phase 147-150)
- AsyncClient client fixture for testing FastAPI routes
- Database session setup with get_db dependency
- mock User with permission fixtures
- test_scheduler.py already exists with scheduler tests

# Frontend Test Patterns (from Phase 150)
- vitest + React Testing Library for component tests
- useQuery mocking with QueryClientProvider
- render() + userEvent for interaction tests
- Navigation mocking with MemoryRouter
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — Pytest integration tests for GET /api/schedule endpoint</name>
  <files>puppeteer/tests/test_scheduler.py</files>
  <action>
Add test functions to puppeteer/tests/test_scheduler.py. If the file doesn't exist or lacks test structure, create/extend with following tests:

**Test 1: test_get_unified_schedule_merges_jobs_workflows**
- Setup: Create 2 active ScheduledJob records with schedule_cron (e.g., "0 9 * * *")
- Setup: Create 2 active Workflow records with schedule_cron
- Action: GET /api/schedule
- Verify: Response has 4 entries (2 jobs + 2 workflows)
- Verify: Each entry has {id, type ("JOB"|"FLOW"), name, next_run_time (datetime), last_run_status (null or status)}

**Test 2: test_get_unified_schedule_filters_inactive**
- Setup: Create 1 active job, 1 inactive (is_active=false) job, 1 active workflow, 1 paused (is_paused=true) workflow, all with cron
- Action: GET /api/schedule
- Verify: Response has exactly 2 entries (1 job + 1 workflow); inactive/paused excluded

**Test 3: test_get_unified_schedule_filters_no_cron**
- Setup: Create jobs and workflows without schedule_cron (None or empty string)
- Action: GET /api/schedule
- Verify: Response is empty or excludes non-cron items

**Test 4: test_get_unified_schedule_invalid_cron_skipped**
- Setup: Create 1 valid ScheduledJob with "0 9 * * *" and 1 invalid ("99 * * * *")
- Action: GET /api/schedule
- Verify: Response has 1 entry (valid one); invalid one skipped gracefully (no 500 error)
- Verify: Log contains warning about invalid cron

**Test 5: test_get_unified_schedule_sorted_by_next_run**
- Setup: Create 3 jobs with different cron times (e.g., 9am, 3pm, 6am tomorrow)
- Action: GET /api/schedule
- Verify: Response entries are sorted by next_run_time ascending (earliest first)

**Test 6: test_get_unified_schedule_requires_permission**
- Setup: Create authenticated user with NO jobs:read permission
- Action: GET /api/schedule
- Verify: Response is 403 Forbidden
- Setup: Same user WITH jobs:read permission
- Action: GET /api/schedule
- Verify: Response is 200 OK

**Test 7: test_get_unified_schedule_includes_last_run_status**
- Setup: Create job with prior Job record (status=COMPLETED)
- Action: GET /api/schedule
- Verify: Response entry.last_run_status == "COMPLETED"
- Setup: Create new job with no prior runs
- Action: GET /api/schedule
- Verify: Response entry.last_run_status == null

**Pattern reference:**
```python
@pytest.mark.asyncio
async def test_get_unified_schedule_merges_jobs_workflows(client, db_session, admin_user):
    # Setup: Use fixtures from conftest.py to create jobs, workflows
    # Action: response = await client.get("/api/schedule", headers=auth_headers)
    # Assert: assert response.status_code == 200
    # Assert: data = response.json(); assert len(data['entries']) == 4
```

All tests should use:
- AsyncClient fixture (client) from test suite
- Database session (db_session) with async transaction
- Authenticated user with appropriate permissions (admin_user or viewer_user)
- Assert response.status_code == 200 for success cases
- Assert response.json() structure matches ScheduleListResponse schema

Minimum 5 tests required; 7 is ideal.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_scheduler.py::test_get_unified_schedule -xvs 2>&1 | head -30</automated>
  </verify>
  <done>At least 5 pytest tests added to test_scheduler.py covering: merge, filtering, invalid cron, permissions, sorting, last-run status. All tests use AsyncClient, proper fixtures, and assert response schema.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — Vitest component tests for Schedule.tsx</name>
  <files>puppeteer/dashboard/src/__tests__/Schedule.test.tsx</files>
  <action>
Create puppeteer/dashboard/src/__tests__/Schedule.test.tsx with following test suite using vitest + React Testing Library:

**Test 1: test_schedule_renders_table_with_columns**
- Setup: Render <Schedule /> with QueryClientProvider and mocked useQuery returning sample data
- Verify: Table header contains "Type", "Name", "Next Run", "Last Run Status"
- Verify: 2 sample rows render with correct data

**Test 2: test_schedule_displays_job_and_flow_badges**
- Setup: Render with data containing 1 JOB and 1 FLOW entry
- Verify: JOB badge visible with text "JOB"
- Verify: FLOW badge visible with text "FLOW"
- Verify: Badges have correct variant styling (e.g., secondary vs default)

**Test 3: test_schedule_formats_next_run_time**
- Setup: Mock entry with next_run_time = "2026-04-20T14:30:00Z" (assume now is 2026-04-20T10:00:00Z)
- Verify: "Next Run" column shows human-readable format like "in 4 hours" (or similar per formatDistanceToNow)

**Test 4: test_schedule_row_click_navigates_to_job_definitions**
- Setup: Render with JOB entry
- Action: userEvent.click(job row)
- Verify: navigate() called with /job-definitions?edit={id}
- Mock navigate with vi.mock('react-router-dom')

**Test 5: test_schedule_row_click_navigates_to_workflows**
- Setup: Render with FLOW entry
- Action: userEvent.click(flow row)
- Verify: navigate() called with /workflows/{id}

**Test 6: test_schedule_uses_refetch_interval**
- Setup: Render with mocked useQuery
- Verify: useQuery called with { queryKey: ['schedule'], ..., refetchInterval: 30000 }
- Use vi.spyOn to verify hook configuration

**Test 7: test_schedule_empty_state**
- Setup: Mock useQuery returning empty entries array
- Verify: "No active schedules" message displayed

**Test 8: test_schedule_loading_state**
- Setup: Mock useQuery with isLoading: true
- Verify: Skeleton loaders or loading indicators visible

**Test 9: test_schedule_error_state**
- Setup: Mock useQuery with error: Error("Failed to fetch")
- Verify: Error message displayed
- Verify: "Retry" button visible and clickable

**Test 10: test_schedule_handles_null_last_run_status**
- Setup: Entry with last_run_status: null
- Verify: "Never" text displayed instead of badge

**Pattern reference:**
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/react';
import { QueryClientProvider } from '@tanstack/react-query';
import { BrowserRouter } from 'react-router-dom';
import Schedule from '@/views/Schedule';

describe('Schedule', () => {
  it('renders table with columns', () => {
    // Setup mock useQuery
    // Render component
    // Verify table headers
  });
});
```

Use vitest + React Testing Library patterns established in Phase 150 (Workflows.test.tsx, WorkflowDetail.test.tsx).

Minimum 5 tests required; 10 is ideal for comprehensive coverage.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run test -- src/__tests__/Schedule.test.tsx --run 2>&1 | grep -E "PASS|FAIL|test|✓|✗" | head -20</automated>
  </verify>
  <done>At least 5 vitest tests created in Schedule.test.tsx covering: table render, badges, time formatting, row navigation, refetch interval, empty/loading/error states, null handling. All tests use QueryClientProvider, mocked useQuery, and React Testing Library patterns.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite validation and verification</name>
  <files>puppeteer, puppeteer/dashboard</files>
  <action>
Run full backend and frontend test suites to ensure no regressions from Phase 154 changes:

**Backend:**
```bash
cd /home/thomas/Development/master_of_puppets/puppeteer
pytest tests/test_scheduler.py::test_get_unified_schedule -xvs 2>&1 | tail -5
```
Verify: All new tests pass (✓)

Then run full test suite to check for regressions:
```bash
cd /home/thomas/Development/master_of_puppets/puppeteer
pytest 2>&1 | tail -10
```
Verify: No new failures, same or better pass count as before Phase 154

**Frontend:**
```bash
cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard
npm run test -- src/__tests__/Schedule.test.tsx --run 2>&1 | tail -5
```
Verify: All Schedule tests pass (✓)

Then run full test suite:
```bash
cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard
npm run test --run 2>&1 | tail -10
```
Verify: No new failures, same or better pass count

**Final verification:**
- Backend: `pytest -q` shows all tests passing
- Frontend: `npm run test -- --run` shows all tests passing
- No console errors or warnings related to Schedule component
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_scheduler.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>All pytest tests pass for scheduler. Backend tests cover GET /api/schedule endpoint. Frontend tests cover Schedule.tsx component. No regressions in existing test suites.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Backend service method tested: merging, filtering, invalid cron, permissions
2. API endpoint tested: correct response schema, permission gating, error handling
3. Frontend component tested: table render, navigation, refetch, empty/loading/error states
4. Full test suites passing: no regressions in existing tests
5. Ready for Phase 154 completion and Playwright E2E verification
</verification>

<success_criteria>
- 5+ pytest tests added for GET /api/schedule (merge, filter, invalid cron, permissions, sorting, last-run status)
- 5+ vitest tests added for Schedule.tsx (render, badges, navigation, refetch, empty/loading/error)
- All new tests passing (green status)
- No regressions in existing test suites
- Backend test coverage: SchedulerService.get_unified_schedule(), main.py endpoint, permission gating
- Frontend test coverage: table rendering, row navigation, useQuery hook configuration, edge cases
- Tests follow established patterns from Phase 150 (Workflows) and existing test infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/154-unified-schedule-view/154-02-SUMMARY.md`
</output>
