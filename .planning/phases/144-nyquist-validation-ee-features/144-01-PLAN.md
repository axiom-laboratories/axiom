---
phase: 144-nyquist-validation-ee-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/tests/test_licence_service.py
  - .planning/phases/137-signed-ee-wheel-manifest/137-VALIDATION.md
  - .planning/phases/138-hmac-keyed-boot-log/138-VALIDATION.md
  - .planning/phases/139-entry-point-whitelist-encryption-key/139-VALIDATION.md
  - .planning/phases/140-wheel-signing-tool-tests/140-VALIDATION.md
autonomous: true
requirements: [EE-01, EE-02, EE-03, EE-04, EE-05, EE-06]
user_setup: []

must_haves:
  truths:
    - "All 18 Phase 137 wheel manifest verification tests pass"
    - "All 26 Phase 138 HMAC boot log tests pass (including 2 fixed tests)"
    - "All 8 Phase 139 entry point + encryption key tests pass"
    - "All 23 Phase 140 wheel signing tool tests pass"
    - "Full puppeteer test suite passes (`pytest -x -q`)"
    - "Full axiom-licenses test suite passes (`pytest tests/ -q`)"
  artifacts:
    - path: puppeteer/tests/test_licence_service.py
      provides: "Fixed test expectations for Phase 138"
      contains: "test_reload_licence_with_invalid_key (fixed regex), test_licence_expiry_guard_ee_prefixes (updated tuple)"
    - path: ".planning/phases/137-signed-ee-wheel-manifest/137-VALIDATION.md"
      provides: "Phase 137 validation sign-off"
      contains: "nyquist_compliant: true, wave_0_complete: true"
    - path: ".planning/phases/138-hmac-keyed-boot-log/138-VALIDATION.md"
      provides: "Phase 138 validation sign-off"
      contains: "nyquist_compliant: true, wave_0_complete: true"
    - path: ".planning/phases/139-entry-point-whitelist-encryption-key/139-VALIDATION.md"
      provides: "Phase 139 validation sign-off"
      contains: "nyquist_compliant: true, wave_0_complete: true"
    - path: ".planning/phases/140-wheel-signing-tool-tests/140-VALIDATION.md"
      provides: "Phase 140 validation sign-off"
      contains: "nyquist_compliant: true, wave_0_complete: true"
  key_links:
    - from: "puppeteer/tests/test_licence_service.py::test_reload_licence_with_invalid_key"
      to: "agent_service/services/licence_service.py::reload_licence()"
      via: "JWT parse error handling"
      pattern: "LicenceError.*parse error"
    - from: "puppeteer/tests/test_licence_service.py::test_licence_expiry_guard_ee_prefixes"
      to: "agent_service/main.py::LicenceExpiryGuard.EE_PREFIXES"
      via: "Tuple assertion matching production middleware"
      pattern: "/api/admin/bundles"
---

<objective>
Run Nyquist validation for all 4 EE licence protection phases (137–140) and fill any test coverage gaps found. This phase ensures all requirements have passing automated tests and marks each phase's VALIDATION.md as `nyquist_compliant: true`.

**What's being validated:**
- Phase 137: Signed EE wheel manifest verification (Ed25519 signatures)
- Phase 138: HMAC-keyed boot log with backward-compatible SHA256 support (2 failing tests to fix)
- Phase 139: Entry point whitelist + ENCRYPTION_KEY enforcement
- Phase 140: Wheel signing release infrastructure (gen_wheel_key.py + sign_wheels.py)

**Why it matters:**
Validation ensures all EE licence protection mechanisms have automated test coverage and can be relied on for production deployments.

**Output:**
All 4 phase VALIDATION.md files updated to `nyquist_compliant: true` + `wave_0_complete: true`; all test suites green.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-nyquist-validation-ee-features/144-CONTEXT.md
@.planning/phases/144-nyquist-validation-ee-features/144-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Phase 138 test expectations in test_licence_service.py</name>
  <files>puppeteer/tests/test_licence_service.py</files>
  <action>
**Fix Failure 1** (line ~331 in `test_reload_licence_with_invalid_key`):
- Current regex: `match="signature invalid"`
- Issue: JWT parse error occurs before signature verification; "invalid.token.here" fails during header parsing
- Fix: Change regex to `match="parse error|signature invalid"` to accept either JWT parse error OR signature verification failure
- Rationale: Both errors are valid; reject only actual regressions, not expectation drift

**Fix Failure 2** (line ~443 in `test_licence_expiry_guard_ee_prefixes`):
- Current expected tuple: `("/api/foundry", "/api/audit", "/api/webhooks", "/api/triggers", "/api/auth-ext", "/api/smelter", "/api/executions")`
- Missing prefix: `/api/admin/bundles` (added to production middleware in a post-implementation change)
- Fix: Add `"/api/admin/bundles"` to the `expected_prefixes` tuple so it matches production's `LicenceExpiryGuard.EE_PREFIXES`
- Rationale: New EE endpoint was registered; test must reflect current production state

After fixes, run: `cd puppeteer && pytest tests/test_licence_service.py -x -q` and confirm all 26 tests pass.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_licence_service.py -x -q 2>&1 | tee /tmp/phase138_verify.log && grep "26 passed" /tmp/phase138_verify.log</automated>
  </verify>
  <done>All 26 Phase 138 tests passing; test_reload_licence_with_invalid_key accepts parse error; test_licence_expiry_guard_ee_prefixes includes /api/admin/bundles</done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 137 wheel manifest tests all passing</name>
  <files>puppeteer/tests/test_ee_manifest.py</files>
  <action>
Run Phase 137 test subset to confirm manifest verification tests are all green. This is a smoke test; Phase 137 was fully implemented in a prior phase and should have no failures.

Command: `cd puppeteer && pytest tests/test_ee_manifest.py::TestVerifyWheelManifest* tests/test_ee_manifest.py::TestInstallEEWheelIntegration tests/test_ee_manifest.py::TestActivateEELiveErrorHandling tests/test_ee_manifest.py::TestLicenceEndpointEEActivationError -x -q`

Expected: All manifest-related tests (18 total across multiple test classes) pass without modification.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_ee_manifest.py::TestVerifyWheelManifest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Phase 137 manifest verification tests confirmed passing; no fixes needed</done>
</task>

<task type="auto">
  <name>Task 3: Verify Phase 139 encryption key and entry point tests all passing</name>
  <files>puppeteer/tests/test_encryption_key_enforcement.py puppeteer/tests/test_ee_manifest.py</files>
  <action>
Run Phase 139 test subset to confirm both ENCRYPTION_KEY enforcement and entry point whitelist tests are green.

ENCRYPTION_KEY tests: `cd puppeteer && pytest tests/test_encryption_key_enforcement.py -x -q`
Entry point tests: `cd puppeteer && pytest tests/test_ee_manifest.py::TestEntryPointWhitelist -x -q`

Expected: All 8 tests (4 encryption + 4 entry point) pass without modification.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_encryption_key_enforcement.py tests/test_ee_manifest.py::TestEntryPointWhitelist -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Phase 139 encryption key and entry point tests confirmed passing; no fixes needed</done>
</task>

<task type="auto">
  <name>Task 4: Verify Phase 140 wheel signing tool tests all passing</name>
  <files>axiom-licenses/tests/tools/test_gen_wheel_key.py axiom-licenses/tests/tools/test_sign_wheels.py axiom-licenses/tests/tools/test_key_resolution.py</files>
  <action>
Run Phase 140 test subset to confirm wheel signing tool tests are all green.

Command: `cd axiom-licenses && pytest tests/tools/ -x -q`

Expected: All 23 tests (5 gen_wheel_key + 12 sign_wheels + 6 key_resolution) pass without modification.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/axiom-licenses && pytest tests/tools/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Phase 140 wheel signing tests confirmed passing; no fixes needed</done>
</task>

<task type="auto">
  <name>Task 5: Update Phase 137 VALIDATION.md to mark nyquist_compliant: true</name>
  <files>.planning/phases/137-signed-ee-wheel-manifest/137-VALIDATION.md</files>
  <action>
Edit Phase 137 VALIDATION.md frontmatter:
- Set `nyquist_compliant: true`
- Set `wave_0_complete: true`
- Ensure status line is updated to reflect compliance

Keep the rest of the document unchanged (test maps, sampling rate, etc.) — only update the frontmatter boolean flags.

This signals that Phase 137's EE-01 requirement has full automated test coverage and all tests are passing.
  </action>
  <verify>
    <automated>grep -A 3 "^---$" /home/thomas/Development/master_of_puppets/.planning/phases/137-signed-ee-wheel-manifest/137-VALIDATION.md | head -10</automated>
  </verify>
  <done>Phase 137 VALIDATION.md frontmatter updated: nyquist_compliant: true, wave_0_complete: true</done>
</task>

<task type="auto">
  <name>Task 6: Update Phase 138 VALIDATION.md to mark nyquist_compliant: true</name>
  <files>.planning/phases/138-hmac-keyed-boot-log/138-VALIDATION.md</files>
  <action>
Edit Phase 138 VALIDATION.md frontmatter:
- Set `nyquist_compliant: true`
- Set `wave_0_complete: true`
- Ensure status line is updated to reflect compliance

Keep the rest of the document unchanged. This signals that Phase 138's EE-02 and EE-03 requirements have full automated test coverage (after fixing the 2 test expectations in Task 1).
  </action>
  <verify>
    <automated>grep -A 3 "^---$" /home/thomas/Development/master_of_puppets/.planning/phases/138-hmac-keyed-boot-log/138-VALIDATION.md | head -10</automated>
  </verify>
  <done>Phase 138 VALIDATION.md frontmatter updated: nyquist_compliant: true, wave_0_complete: true</done>
</task>

<task type="auto">
  <name>Task 7: Update Phase 139 VALIDATION.md to mark nyquist_compliant: true</name>
  <files>.planning/phases/139-entry-point-whitelist-encryption-key/139-VALIDATION.md</files>
  <action>
Edit Phase 139 VALIDATION.md frontmatter:
- Set `nyquist_compliant: true`
- Set `wave_0_complete: true`
- Ensure status line is updated to reflect compliance

Keep the rest of the document unchanged. This signals that Phase 139's EE-04 and EE-06 requirements have full automated test coverage and all tests are passing.
  </action>
  <verify>
    <automated>grep -A 3 "^---$" /home/thomas/Development/master_of_puppets/.planning/phases/139-entry-point-whitelist-encryption-key/139-VALIDATION.md | head -10</automated>
  </verify>
  <done>Phase 139 VALIDATION.md frontmatter updated: nyquist_compliant: true, wave_0_complete: true</done>
</task>

<task type="auto">
  <name>Task 8: Update Phase 140 VALIDATION.md to mark nyquist_compliant: true</name>
  <files>.planning/phases/140-wheel-signing-tool-tests/140-VALIDATION.md</files>
  <action>
Edit Phase 140 VALIDATION.md frontmatter:
- Set `nyquist_compliant: true`
- Set `wave_0_complete: true`
- Ensure status line is updated to reflect compliance

Keep the rest of the document unchanged. This signals that Phase 140's EE-05 requirement has full automated test coverage and all tests are passing.
  </action>
  <verify>
    <automated>grep -A 3 "^---$" /home/thomas/Development/master_of_puppets/.planning/phases/140-wheel-signing-tool-tests/140-VALIDATION.md | head -10</automated>
  </verify>
  <done>Phase 140 VALIDATION.md frontmatter updated: nyquist_compliant: true, wave_0_complete: true</done>
</task>

<task type="auto">
  <name>Task 9: Run full puppeteer test suite regression check</name>
  <files>puppeteer</files>
  <action>
Run the complete puppeteer test suite to ensure no collateral damage from Phase 138 fixes or other changes.

Command: `cd puppeteer && pytest -x -q --tb=short`

Expected: All tests pass (34+ tests covering all phases 137–139, plus existing test suite).

If any test fails, diagnose and fix before proceeding to Task 10. Focus first on Phase 138 / licence_service.py failures as those are the validation target.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Full puppeteer test suite passing; no regressions introduced by Phase 138 fixes</done>
</task>

<task type="auto">
  <name>Task 10: Run full axiom-licenses test suite regression check</name>
  <files>axiom-licenses</files>
  <action>
Run the complete axiom-licenses test suite to ensure all Phase 140 tests remain passing.

Command: `cd axiom-licenses && pytest tests/ -q --tb=short`

Expected: All tests pass (23+ tests for Phase 140 + any other tests in axiom-licenses).

This is the final regression check before marking Phase 144 complete.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/axiom-licenses && pytest tests/ -q 2>&1 | tail -20</automated>
  </verify>
  <done>Full axiom-licenses test suite passing; Phase 140 validation complete</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All Phase 138 test fixes applied and verified (26/26 passing)
2. Phase 137 tests confirmed passing (18/18)
3. Phase 139 tests confirmed passing (8/8)
4. Phase 140 tests confirmed passing (23/23)
5. All 4 VALIDATION.md files marked `nyquist_compliant: true` and `wave_0_complete: true`
6. Full puppeteer regression suite passes (34+ tests)
7. Full axiom-licenses regression suite passes (23+ tests)

Total test coverage: 79 tests passing (18 + 26 + 8 + 23 + regression coverage).
</verification>

<success_criteria>
- [ ] Phase 137 VALIDATION.md: `nyquist_compliant: true`
- [ ] Phase 138 VALIDATION.md: `nyquist_compliant: true` (with 2 test fixes in test_licence_service.py)
- [ ] Phase 139 VALIDATION.md: `nyquist_compliant: true`
- [ ] Phase 140 VALIDATION.md: `nyquist_compliant: true`
- [ ] `cd puppeteer && pytest -x -q` passes without failures
- [ ] `cd axiom-licenses && pytest tests/ -q` passes without failures
- [ ] No manual verification steps required
</success_criteria>

<output>
After completion, create `.planning/phases/144-nyquist-validation-ee-features/144-01-SUMMARY.md`
</output>
