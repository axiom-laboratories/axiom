---
phase: 71-deploy-docs-to-github-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - docs/docs/.nojekyll
  - docs/mkdocs.yml
  - docs/Dockerfile
autonomous: true
requirements:
  - HOUSE-01
  - HOUSE-02
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03

must_haves:
  truths:
    - "git ls-files docs/site/ returns 0 lines — no site build output is tracked"
    - "docs/docs/.nojekyll exists — Jekyll will not mangle MkDocs underscore-prefixed assets"
    - "mkdocs.yml site_url points to https://axiom-laboratories.github.io/axiom/"
    - "mkdocs build --strict passes locally without OFFLINE_BUILD set"
    - "Dockerfile mkdocs build step sets OFFLINE_BUILD=true so offline plugin activates in container"
  artifacts:
    - path: "docs/docs/.nojekyll"
      provides: "Jekyll bypass marker"
    - path: "docs/mkdocs.yml"
      provides: "Updated site_url and conditional offline plugin"
      contains: "axiom-laboratories.github.io/axiom/"
    - path: "docs/Dockerfile"
      provides: "OFFLINE_BUILD=true in mkdocs build step"
      contains: "OFFLINE_BUILD=true"
  key_links:
    - from: "docs/mkdocs.yml"
      to: "offline plugin"
      via: "enabled: !ENV [OFFLINE_BUILD, false]"
      pattern: "OFFLINE_BUILD"
    - from: "docs/Dockerfile"
      to: "offline plugin activation"
      via: "OFFLINE_BUILD=true mkdocs build --strict"
      pattern: "OFFLINE_BUILD=true"
---

<objective>
Prepare the docs tree for GitHub Pages deployment by removing tracked build output,
adding the Jekyll bypass marker, and updating mkdocs.yml and the Dockerfile so the
site builds correctly both locally (no OFFLINE_BUILD) and in the container
(OFFLINE_BUILD=true).

Purpose: Without these changes the deploy workflow will push stale site files and
the offline plugin will force page.html URLs that break GitHub Pages navigation.

Output: Clean git tracking, .nojekyll marker, corrected mkdocs.yml, patched Dockerfile.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-deploy-docs-to-github-pages/71-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Untrack docs/site/ and add .gitignore entry</name>
  <files>.gitignore</files>
  <action>
    Two steps:

    1. Run `git rm -r --cached docs/site/` from the repo root to remove all 166 tracked
       files under docs/site/ from git's index without deleting them from disk.

    2. Add the following block to the bottom of `.gitignore`:

    ```
    # MkDocs generated site (build output — not tracked)
    docs/site/
    ```

    Do NOT delete the docs/site/ directory from disk — only untrack it.
    After the git rm step, verify with: `git ls-files docs/site/ | wc -l` — must return 0.
    After adding the .gitignore entry, verify with: `grep 'docs/site/' .gitignore` — must match.
  </action>
  <verify>
    <automated>git ls-files docs/site/ | wc -l</automated>
  </verify>
  <done>git ls-files docs/site/ returns 0. docs/site/ entry present in .gitignore.</done>
</task>

<task type="auto">
  <name>Task 2: Add .nojekyll and update mkdocs.yml (site_url + offline plugin)</name>
  <files>docs/docs/.nojekyll, docs/mkdocs.yml</files>
  <action>
    Two file changes:

    **A. Create docs/docs/.nojekyll**
    Create an empty file at `docs/docs/.nojekyll`. This prevents GitHub Pages from
    running Jekyll on the deployed site, which would strip MkDocs-generated assets
    whose names begin with underscores (e.g. `_static/`).

    **B. Update docs/mkdocs.yml**
    Make exactly two changes:

    1. Change `site_url` from:
       `site_url: https://dev.master-of-puppets.work/docs/`
       to:
       `site_url: https://axiom-laboratories.github.io/axiom/`
       (trailing slash required for correct relative URL generation on the subpath)

    2. Change the `offline` plugin entry from:
       `  - offline`
       to:
       ```yaml
         - offline:
             enabled: !ENV [OFFLINE_BUILD, false]
       ```
       Keep the surrounding `search`, `privacy`, and `swagger-ui-tag` plugin entries
       unchanged. Preserve the comment above the privacy/offline block:
       `# privacy + offline: required for air-gap / offline operation (INFRA-06) — do not remove`

    After both changes, run `cd docs && mkdocs build --strict` (without setting
    OFFLINE_BUILD). It must complete with zero errors and zero warnings.
  </action>
  <verify>
    <automated>test -f /home/thomas/Development/master_of_puppets/docs/docs/.nojekyll && grep 'axiom-laboratories.github.io/axiom/' /home/thomas/Development/master_of_puppets/docs/mkdocs.yml && grep 'OFFLINE_BUILD' /home/thomas/Development/master_of_puppets/docs/mkdocs.yml && cd /home/thomas/Development/master_of_puppets/docs && mkdocs build --strict</automated>
  </verify>
  <done>.nojekyll exists. mkdocs.yml has correct site_url and OFFLINE_BUILD conditional. mkdocs build --strict passes with zero warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Add OFFLINE_BUILD=true to Dockerfile mkdocs build step</name>
  <files>docs/Dockerfile</files>
  <action>
    In `docs/Dockerfile`, find the line:
    ```
    RUN mkdocs build --strict
    ```

    Change it to:
    ```
    RUN OFFLINE_BUILD=true mkdocs build --strict
    ```

    This ensures the `offline` plugin activates during Docker image builds (preserving
    air-gap container behaviour) while it stays disabled for the GitHub Pages build
    where file:// access is not needed.

    Do not change any other line in the Dockerfile.

    After the edit, verify the exact string is present:
    `grep 'OFFLINE_BUILD=true' docs/Dockerfile`
  </action>
  <verify>
    <automated>grep 'OFFLINE_BUILD=true' /home/thomas/Development/master_of_puppets/docs/Dockerfile</automated>
  </verify>
  <done>Dockerfile mkdocs build step reads `RUN OFFLINE_BUILD=true mkdocs build --strict`.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

```bash
# HOUSE-01: no tracked files under docs/site/
git ls-files docs/site/ | wc -l   # expect: 0

# HOUSE-02: .nojekyll exists
test -f docs/docs/.nojekyll && echo "OK"

# CONFIG-01: site_url updated
grep 'axiom-laboratories.github.io/axiom/' docs/mkdocs.yml

# CONFIG-02: offline plugin is conditional
grep 'OFFLINE_BUILD' docs/mkdocs.yml

# CONFIG-03: Dockerfile sets OFFLINE_BUILD
grep 'OFFLINE_BUILD=true' docs/Dockerfile

# Integration: strict build passes (without OFFLINE_BUILD — tests CONFIG-02 is conditional not always-off)
cd docs && mkdocs build --strict
```
</verification>

<success_criteria>
- `git ls-files docs/site/ | wc -l` returns `0`
- `docs/docs/.nojekyll` file exists (may be empty)
- `docs/mkdocs.yml` site_url is `https://axiom-laboratories.github.io/axiom/`
- `docs/mkdocs.yml` contains `enabled: !ENV [OFFLINE_BUILD, false]` under `offline:`
- `docs/Dockerfile` mkdocs build line reads `RUN OFFLINE_BUILD=true mkdocs build --strict`
- `cd docs && mkdocs build --strict` exits 0 with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/71-deploy-docs-to-github-pages/71-01-SUMMARY.md`
</output>
