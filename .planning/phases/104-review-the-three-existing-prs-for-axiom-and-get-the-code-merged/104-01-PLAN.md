---
phase: 104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/dashboard/src/hooks/useWebSocket.ts
  - puppeteer/agent_service/deps.py
  - puppeteer/agent_service/main.py
  - puppeteer/compose.cold-start.yaml
  - docs/docs/getting-started/enroll-node.md
  - docs/docs/getting-started/first-job.md
  - docs/docs/getting-started/install.md
  - .planning/phases/102-linux-e2e-validation/
autonomous: true
requirements:
  - PR17-MERGE
  - PR19-MERGE

must_haves:
  truths:
    - "PR #17 is squash-merged to main with only the useWebSocket.ts fix (no .planning/ files)"
    - "PR #19 is squash-merged to main with all code + docs changes"
    - "Both PRs show as merged on GitHub"
  artifacts:
    - path: "puppeteer/dashboard/src/hooks/useWebSocket.ts"
      provides: "WebSocket double-retry fix and ping interval tracking"
    - path: "puppeteer/agent_service/deps.py"
      provides: "Extracted dependency injection (from PR #19)"
    - path: "puppeteer/agent_service/main.py"
      provides: "Updated main with deps.py extraction"
  key_links:
    - from: "PR #17 (fix/ws-memory-leak)"
      to: "main"
      via: "squash merge after cherry-pick cleanup"
      pattern: "merged"
    - from: "PR #19 (phase/102-linux-e2e-validation)"
      to: "main"
      via: "squash merge after rebase on post-#17 main"
      pattern: "merged"
---

<objective>
Review, clean, and squash-merge PR #17 (WebSocket fix) and PR #19 (Linux E2E) into main.

Purpose: Land the first two PRs in the correct merge order. PR #17 is a self-contained bug fix that must go first (smallest blast radius). PR #19 has actual code changes (deps.py, main.py, compose.cold-start.yaml) that need code review before merge.
Output: Both PRs merged to main, branches intact until Plan 03 cleanup.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged/104-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean and merge PR #17 (WebSocket fix)</name>
  <files>puppeteer/dashboard/src/hooks/useWebSocket.ts</files>
  <action>
PR #17 (fix/ws-memory-leak) contains .planning/ files that don't belong in a bug fix PR. Per user decision: cherry-pick only the useWebSocket.ts change onto a clean branch, then squash merge.

Steps:
1. Review the useWebSocket.ts diff in the fix/ws-memory-leak worktree at `.worktrees/fix/ws-memory-leak/`. Confirm:
   - `onerror` is a no-op (prevents double-retry from onerror+onclose)
   - `pingRef` stores the setInterval ID and is cleared in both `onclose` and cleanup
   - No other behavioral changes snuck in
2. Create a clean branch from main: `git branch fix/ws-memory-leak-clean main`
3. Cherry-pick ONLY the useWebSocket.ts commit(s) from fix/ws-memory-leak onto fix/ws-memory-leak-clean. Use `git log --oneline fix/ws-memory-leak` to find the relevant commit(s). If the useWebSocket.ts change is mixed with .planning/ files in a single commit, use `git checkout fix/ws-memory-leak -- puppeteer/dashboard/src/hooks/useWebSocket.ts` on the clean branch and commit it.
4. Force-push the clean branch to origin: `git push origin fix/ws-memory-leak-clean:fix/ws-memory-leak --force`
5. Squash merge via CLI: `gh pr merge 17 --squash --delete-branch`

If the force-push approach is too risky, alternative: close PR #17, open a new PR from the clean branch, and merge that instead.
  </action>
  <verify>
    <automated>gh pr view 17 --json state | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['state']=='MERGED', f'PR not merged: {d[\"state\"]}';"</automated>
  </verify>
  <done>PR #17 is merged to main containing only the useWebSocket.ts fix. No .planning/ files in the merge commit.</done>
</task>

<task type="auto">
  <name>Task 2: Rebase, review, and merge PR #19 (Linux E2E)</name>
  <files>puppeteer/agent_service/deps.py, puppeteer/agent_service/main.py, puppeteer/compose.cold-start.yaml, docs/docs/getting-started/enroll-node.md, docs/docs/getting-started/first-job.md, docs/docs/getting-started/install.md</files>
  <action>
PR #19 (phase/102-linux-e2e-validation) has actual code changes that need review. This is our current branch.

Steps:
1. Update main: `git checkout main && git pull origin main` (now includes PR #17's merge)
2. Rebase PR #19's branch onto updated main:
   ```
   cd /home/thomas/Development/master_of_puppets/.worktrees/phase-102
   git fetch origin main
   git rebase origin/main
   ```
   For .planning/ conflicts: accept incoming (the branch version). For code file conflicts: manually resolve, preserving both changes.
3. Force-push the rebased branch: `git push --force-with-lease`
4. Code review the following files (read the diffs, assess risk):
   - `puppeteer/agent_service/deps.py` — new file extracting dependency injection. Verify it imports correctly, no circular deps.
   - `puppeteer/agent_service/main.py` — verify the deps.py extraction didn't break route signatures or imports.
   - `puppeteer/compose.cold-start.yaml` — verify changes are safe (no secrets exposed, correct service config).
   - `docs/` changes — skim for correctness (already reviewed during phase execution).
5. Based on risk assessment (per user decision: Claude decides if Docker stack test needed):
   - If deps.py/main.py changes are straightforward extraction: code review suffices.
   - If changes affect runtime behavior: rebuild and test Docker stack.
6. Squash merge: `gh pr merge 19 --squash --delete-branch`
  </action>
  <verify>
    <automated>gh pr view 19 --json state | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['state']=='MERGED', f'PR not merged: {d[\"state\"]}';"</automated>
  </verify>
  <done>PR #19 is merged to main. Code changes (deps.py, main.py, compose.cold-start.yaml) reviewed and confirmed safe.</done>
</task>

</tasks>

<verification>
- `gh pr view 17 --json state` returns MERGED
- `gh pr view 19 --json state` returns MERGED
- `git log --oneline main -5` shows two new squash merge commits
</verification>

<success_criteria>
- PR #17 merged with only the WebSocket fix (no .planning/ contamination)
- PR #19 merged after rebase, code changes reviewed
- main branch has both merges and is in a clean state
</success_criteria>

<output>
After completion, create `.planning/phases/104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged/104-01-SUMMARY.md`
</output>
