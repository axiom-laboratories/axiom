---
phase: 104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged
plan: 03
type: execute
wave: 3
depends_on: ["104-02"]
files_modified:
  - puppeteer/dashboard/src/views/__tests__/History.test.tsx
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TEST-FIX
  - CLEANUP
  - MILESTONE-CLOSE

must_haves:
  truths:
    - "History.test.tsx passes (4 pre-existing failures fixed)"
    - "All three remote branches deleted (fix/ws-memory-leak, worktree-phase-103, phase/102-linux-e2e-validation)"
    - "All local worktrees removed"
    - "STATE.md and ROADMAP.md reflect Phase 104 complete and v18.0 milestone finished"
  artifacts:
    - path: "puppeteer/dashboard/src/views/__tests__/History.test.tsx"
      provides: "Fixed test file with all tests passing"
    - path: ".planning/STATE.md"
      provides: "Updated project state reflecting milestone completion"
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap with Phase 104 marked complete"
  key_links:
    - from: "History.test.tsx"
      to: "vitest"
      via: "npm test passes"
      pattern: "Tests.*passed"
---

<objective>
Fix pre-existing History.test.tsx failures, clean up branches/worktrees, and close milestone v18.0.

Purpose: Complete the post-merge housekeeping. The test fix ensures main is green. Branch/worktree cleanup removes stale references. STATE.md and ROADMAP.md updates close out the milestone formally.
Output: Clean main branch with all tests passing, milestone v18.0 marked complete.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged/104-CONTEXT.md
@.planning/phases/104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged/104-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix History.test.tsx failures</name>
  <files>puppeteer/dashboard/src/views/__tests__/History.test.tsx</files>
  <action>
There are 4 pre-existing test failures in History.test.tsx on main. Per user decision: fix these after all PRs merge as a separate commit on main.

Steps:
1. Ensure you're on main: `git checkout main && git pull origin main`
2. Run the failing tests to see current errors:
   ```
   cd puppeteer/dashboard && npx vitest run src/views/__tests__/History.test.tsx 2>&1 | tail -60
   ```
3. Read the test file and the History.tsx component it tests. Diagnose the 4 failures:
   - Common causes: API response shape changed, component props changed, missing mock data fields, renamed exports
4. Fix the test file to match the current component/API contract. Do NOT change production code to match tests — fix the tests.
5. Re-run: `cd puppeteer/dashboard && npx vitest run src/views/__tests__/History.test.tsx`
6. Run full test suite to confirm no regressions: `cd puppeteer/dashboard && npx vitest run`
7. Commit on main: `git add puppeteer/dashboard/src/views/__tests__/History.test.tsx && git commit -m "fix(tests): resolve 4 pre-existing History.test.tsx failures"`
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npx vitest run src/views/__tests__/History.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>All History.test.tsx tests pass. Full vitest suite passes with no regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up branches, worktrees, and close milestone</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Post-merge cleanup per user decisions.

Steps:
1. Delete remote branches (if not already deleted by --delete-branch during merge):
   ```
   git push origin --delete fix/ws-memory-leak 2>/dev/null || true
   git push origin --delete worktree-phase-103 2>/dev/null || true
   git push origin --delete phase/102-linux-e2e-validation 2>/dev/null || true
   ```

2. Remove local worktrees:
   ```
   git worktree remove /home/thomas/Development/master_of_puppets/.claude/worktrees/phase-103 --force 2>/dev/null || true
   git worktree remove /home/thomas/Development/master_of_puppets/.worktrees/fix/ws-memory-leak --force 2>/dev/null || true
   git worktree remove /home/thomas/Development/master_of_puppets/.worktrees/phase-102 --force 2>/dev/null || true
   ```

3. Delete local tracking branches:
   ```
   git branch -d fix/ws-memory-leak 2>/dev/null || true
   git branch -d worktree-phase-103 2>/dev/null || true
   git branch -D phase/102-linux-e2e-validation 2>/dev/null || true
   ```

4. Prune remote references: `git remote prune origin`

5. Update ROADMAP.md:
   - Mark Phase 102, 103, 104 as complete with today's date
   - Add Phase 104 entry to Phase Details section:
     `- [x] **Phase 104: PR Review and Merge** — Review, clean, and squash-merge PRs #17, #18, #19; fix History.test.tsx; close milestone (completed 2026-04-01)`
   - Change v18.0 milestone status from "in progress" to completed/shipped

6. Update STATE.md:
   - Set status to completed
   - Update current position to Phase 104 complete
   - Update progress to 100%
   - Note milestone v18.0 is complete
   - Add key decision about PR merge order and cleanup

7. Commit: `git add .planning/STATE.md .planning/ROADMAP.md && git commit -m "docs(104): close phase 104 and milestone v18.0"`
  </action>
  <verify>
    <automated>git worktree list | wc -l && git branch -a | grep -c "fix/ws-memory-leak\|worktree-phase-103\|phase/102" || echo "0 stale branches"</automated>
  </verify>
  <done>All worktrees removed. All stale branches deleted (local and remote). STATE.md and ROADMAP.md updated to reflect Phase 104 complete and v18.0 milestone shipped.</done>
</task>

</tasks>

<verification>
- `cd puppeteer/dashboard && npx vitest run` — all tests pass
- `git worktree list` — only main worktree remains
- `git branch -a` — no stale branches from #17/#18/#19
- STATE.md shows milestone v18.0 complete
- ROADMAP.md shows phases 102-104 complete
</verification>

<success_criteria>
- History.test.tsx 4 failures fixed, all tests green
- Three remote branches deleted
- Three local worktrees removed
- STATE.md and ROADMAP.md updated for milestone completion
- Ready for `/gsd:complete-milestone` to archive v18.0
</success_criteria>

<output>
After completion, create `.planning/phases/104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged/104-03-SUMMARY.md`
</output>
