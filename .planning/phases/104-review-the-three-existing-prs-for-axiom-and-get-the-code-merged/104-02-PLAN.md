---
phase: 104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged
plan: 02
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - .github/workflows/release.yml
  - puppeteer/agent_service/main.py
  - puppeteer/agent_service/services/signature_service.py
  - puppeteer/compose.cold-start.yaml
  - puppets/Containerfile.node
  - puppets/environment_service/node.py
  - docs/docs/getting-started/enroll-node.md
  - docs/docs/getting-started/first-job.md
  - docs/docs/getting-started/install.md
autonomous: true
requirements:
  - PR18-MERGE

must_haves:
  truths:
    - "PR #18 is squash-merged to main with all Phase 103 changes"
    - "release.yml workflow syntax is valid"
    - "All three PRs are now merged"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Updated CI workflow from Phase 103"
    - path: "puppets/environment_service/node.py"
      provides: "Node changes from Windows E2E validation"
  key_links:
    - from: "PR #18 (worktree-phase-103)"
      to: "main"
      via: "squash merge after rebase on post-#19 main"
      pattern: "merged"
---

<objective>
Rebase, review, and squash-merge PR #18 (Windows E2E / Phase 103) into main.

Purpose: Land the largest PR last, after #17 and #19 are already on main. PR #18 has code changes (main.py, signature_service.py, node.py, Containerfile.node, compose.cold-start.yaml) plus a release.yml CI change that needs syntax verification.
Output: PR #18 merged. All three PRs landed on main.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged/104-CONTEXT.md
@.planning/phases/104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged/104-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebase, review, and merge PR #18 (Windows E2E)</name>
  <files>.github/workflows/release.yml, puppeteer/agent_service/main.py, puppeteer/agent_service/services/signature_service.py, puppeteer/compose.cold-start.yaml, puppets/Containerfile.node, puppets/environment_service/node.py, docs/docs/getting-started/enroll-node.md, docs/docs/getting-started/first-job.md, docs/docs/getting-started/install.md</files>
  <action>
PR #18 (worktree-phase-103) is the largest PR with the most file changes. It must be rebased onto main (which now has #17 and #19 merged).

Steps:
1. Update main: `git checkout main && git pull origin main`
2. Rebase PR #18's branch onto updated main:
   ```
   cd /home/thomas/Development/master_of_puppets/.claude/worktrees/phase-103
   git fetch origin main
   git rebase origin/main
   ```
   For .planning/ conflicts: accept incoming (the branch version has most up-to-date state).
   For code conflicts (main.py, compose.cold-start.yaml, docs/): manually resolve. These files were modified in both #19 and #18 — combine changes from both.
   For enroll-node.md, first-job.md, install.md: #19 added Linux-specific content, #18 added Windows/PowerShell tabs — both sets of changes must survive.
3. Force-push the rebased branch: `git push --force-with-lease`
4. Code review these files (read diffs):
   - `.github/workflows/release.yml` — verify YAML syntax is valid (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"`). Check the change doesn't break CI triggers or release logic.
   - `puppeteer/agent_service/main.py` — verify changes are compatible with the deps.py extraction from #19. No duplicate imports, no broken route signatures.
   - `puppeteer/agent_service/services/signature_service.py` — review the change for correctness.
   - `puppeteer/compose.cold-start.yaml` — verify no conflicts with #19's changes. Ensure service config is correct.
   - `puppets/Containerfile.node` — verify Dockerfile syntax, no security issues.
   - `puppets/environment_service/node.py` — verify changes are safe.
5. Risk assessment for Docker stack test (per user decision):
   - If main.py changes conflict with deps.py extraction from #19: Docker stack test required.
   - If changes are additive/non-conflicting: code review suffices.
6. Squash merge: `gh pr merge 18 --squash --delete-branch`
  </action>
  <verify>
    <automated>gh pr view 18 --json state | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['state']=='MERGED', f'PR not merged: {d[\"state\"]}';"</automated>
  </verify>
  <done>PR #18 is merged to main. release.yml verified. Code changes reviewed. All three PRs now merged.</done>
</task>

</tasks>

<verification>
- `gh pr view 18 --json state` returns MERGED
- `gh pr list --state open` shows no remaining PRs from this set
- `git log --oneline main -5` shows all three squash merge commits
</verification>

<success_criteria>
- PR #18 merged after clean rebase onto post-#17/#19 main
- release.yml syntax validated
- Code changes (main.py, signature_service.py, node.py) reviewed
- No open PRs remain from the #17/#18/#19 set
</success_criteria>

<output>
After completion, create `.planning/phases/104-review-the-three-existing-prs-for-axiom-and-get-the-code-merged/104-02-SUMMARY.md`
</output>
