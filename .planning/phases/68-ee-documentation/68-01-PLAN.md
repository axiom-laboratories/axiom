---
phase: 68-ee-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docs/getting-started/install.md
  - docs/docs/licensing.md
autonomous: true
requirements:
  - EEDOC-01
  - EEDOC-02

must_haves:
  truths:
    - "install.md Enterprise Edition section lists the EE features enabled by a valid key and includes a Dashboard/CLI tab pair showing how to verify EE is active"
    - "install.md uses GET /api/features exclusively — no reference to /api/admin/features anywhere in docs source"
    - "licensing.md Checking your licence section shows both GET /api/licence and GET /api/features with the full 9-key success JSON"
    - "AXIOM_LICENCE_KEY is the only env var name used — AXIOM_EE_LICENCE_KEY does not appear anywhere in docs source"
    - "mkdocs build --strict passes after both edits with no warnings or errors"
  artifacts:
    - path: "docs/docs/getting-started/install.md"
      provides: "Expanded EE section with feature list and verification tab pair"
      contains: "api/features"
    - path: "docs/docs/licensing.md"
      provides: "GET /api/features example in Checking your licence section"
      contains: "api/features"
  key_links:
    - from: "install.md EE section"
      to: "GET /api/features endpoint"
      via: "curl command in CLI tab"
      pattern: "api/features"
    - from: "licensing.md Checking your licence"
      to: "GET /api/features full JSON response"
      via: "appended subsection after existing GET /api/licence block"
      pattern: "api/features"
---

<objective>
Expand the Enterprise Edition section in `install.md` to document which features a valid licence key enables and how to verify EE is active. Append a `GET /api/features` example to the "Checking your licence" section in `licensing.md`.

Purpose: Phase 68 success criteria require no `/api/admin/features` references in docs (EEDOC-01) and consistent `AXIOM_LICENCE_KEY` naming (EEDOC-02). Both are met by authoring the EE verification content around the correct endpoint and env var from the start.
Output: Two edited markdown files; mkdocs build --strict passes.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-ee-documentation/68-CONTEXT.md
@.planning/phases/68-ee-documentation/68-RESEARCH.md

<interfaces>
<!-- Key facts about the target files. No codebase exploration needed. -->

docs/docs/getting-started/install.md — current EE section (lines 116-128):
```markdown
## Enterprise Edition

To enable EE features, add your licence key to `secrets.env`:

```bash
AXIOM_LICENCE_KEY=<your-licence-key>
```

The stack reads this at startup — no plugin install required. See [Licensing →](../licensing.md) for validation behaviour, expiry handling, and how to check your licence status.
```

docs/docs/licensing.md — "Checking your licence" section (lines 56-71):
```markdown
## Checking your licence

The `GET /api/licence` endpoint (authenticated) returns your current licence state:

```json
{
  "edition": "enterprise",
  "customer_id": "acme-corp",
  "expires": "2027-03-20T00:00:00+00:00",
  "features": ["foundry", "rbac", "webhooks", "triggers", "audit"]
}
```

In CE mode it returns `{"edition": "community"}`.

The dashboard sidebar also shows a **CE** or **EE** badge for at-a-glance visibility.
```

GET /api/features full response (from main.py lines 764-774, unauthenticated endpoint):
```json
{
  "audit": true,
  "foundry": true,
  "webhooks": true,
  "triggers": true,
  "rbac": true,
  "resource_limits": true,
  "service_principals": true,
  "api_keys": true,
  "executions": true
}
```

Tab pair syntax (pymdownx.tabbed, confirmed working from Phase 67):
=== "Dashboard"

    [4-space indented content]

=== "CLI"

    [4-space indented content]

CLI curl pattern from enroll-node.md (self-contained with inline token acquisition):
```bash
TOKEN=$(curl -sk -X POST https://<your-orchestrator>:8001/auth/login \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  -d 'username=admin&password=<your-password>' | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand install.md Enterprise Edition section</name>
  <files>docs/docs/getting-started/install.md</files>
  <action>
Replace the existing Enterprise Edition section (lines 116-128) with an expanded version that:

1. Keeps the existing `AXIOM_LICENCE_KEY` env var block unchanged.
2. After the env var block, adds a brief prose sentence listing what a valid key enables, then a bullet list of the 5 prominent EE features using exact API key names: `foundry`, `rbac`, `webhooks`, `triggers`, `audit`.
3. Adds a "Verify EE is active" subheading followed by a Dashboard/CLI tab pair:
   - **Dashboard tab**: One sentence pointing the operator to the CE/EE badge in the dashboard sidebar — it switches from "CE" to "EE" when the key is valid.
   - **CLI tab**: Two bash blocks:
     - First block: inline token acquisition via `TOKEN=$(curl -sk -X POST https://<your-orchestrator>:8001/auth/login -H 'Content-Type: application/x-www-form-urlencoded' -d 'username=admin&password=<your-password>' | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")`
     - Second block: `curl -sk https://<your-orchestrator>:8001/api/features -H "Authorization: Bearer $TOKEN"`
     - After the second block, a `!!! note` admonition showing the full 9-key success JSON (all values `true`). Include a brief note that if the key is missing or expired, all values return `false`.
4. Retains the existing closing link to `[Licensing →](../licensing.md)` after the tab pair.
5. The `---` separator and `**Next:** [Enroll a Node →](enroll-node.md)` footer remain unchanged.

NEVER use `/api/admin/features` — the only correct endpoint is `/api/features`.
NEVER use `AXIOM_EE_LICENCE_KEY` — the only correct env var is `AXIOM_LICENCE_KEY`.
All tab content must be indented exactly 4 spaces. Admonitions inside tabs must also be indented 4 spaces.
The full 9-key JSON response must appear verbatim — do not abbreviate with `...`.
  </action>
  <verify>
    <automated>grep "api/features" /home/thomas/Development/master_of_puppets/docs/docs/getting-started/install.md && grep -r "api/admin/features" /home/thomas/Development/master_of_puppets/docs/docs/ && echo FAIL || echo PASS</automated>
  </verify>
  <done>install.md Enterprise Edition section contains the feature list, a Dashboard/CLI tab pair with GET /api/features verification, and the full 9-key JSON success response. No /api/admin/features reference exists anywhere in docs/docs/.</done>
</task>

<task type="auto">
  <name>Task 2: Append GET /api/features to licensing.md Checking your licence section</name>
  <files>docs/docs/licensing.md</files>
  <action>
Append content to the "Checking your licence" section in `licensing.md` (currently ends at line 71 with the sidebar badge sentence).

After the existing closing sentence ("The dashboard sidebar also shows a **CE** or **EE** badge for at-a-glance visibility."), add:

1. A blank line, then a `### Feature flags` subheading (or equivalent prose lead-in — use `GET /api/features` as a natural subsection title if preferred, e.g. `### Checking active feature flags`).
2. A brief sentence: "To see which EE features are currently active, call `GET /api/features` (no authentication required):"
3. A JSON code block showing the full 9-key success response (all values `true`).
4. A brief note that in CE mode (or with an expired/missing key), all values return `false`.

NEVER use `/api/admin/features`.
NEVER use `AXIOM_EE_LICENCE_KEY`.
Do not rename or restructure any existing headings — only append after the last line of the section.
  </action>
  <verify>
    <automated>grep "api/features" /home/thomas/Development/master_of_puppets/docs/docs/licensing.md && grep -r "AXIOM_EE_LICENCE_KEY" /home/thomas/Development/master_of_puppets/docs/docs/ && echo FAIL || echo PASS</automated>
  </verify>
  <done>licensing.md "Checking your licence" section contains GET /api/features with the full 9-key JSON response. No AXIOM_EE_LICENCE_KEY reference exists anywhere in docs/docs/.</done>
</task>

<task type="auto">
  <name>Task 3: mkdocs build --strict verification</name>
  <files></files>
  <action>
Run `mkdocs build --strict` from the docs directory to confirm both edited files render without warnings or errors.

Command: `cd /home/thomas/Development/master_of_puppets/docs && mkdocs build --strict 2>&1 | tail -20`

If the build fails:
- Check for tab indentation errors (content inside `=== "..."` blocks must be indented 4 spaces)
- Check for broken anchor references caused by any accidental heading renames
- Fix the issue in the relevant file and re-run until the build is clean
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/docs && mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <done>mkdocs build --strict exits 0 with no warnings or errors. Both install.md and licensing.md edits render correctly.</done>
</task>

</tasks>

<verification>
After all three tasks complete, confirm:

1. `grep "api/features" docs/docs/getting-started/install.md` — matches found
2. `grep -r "api/admin/features" docs/docs/` — no matches (echo PASS)
3. `grep "api/features" docs/docs/licensing.md` — matches found
4. `grep -r "AXIOM_EE_LICENCE_KEY" docs/docs/` — no matches (echo PASS)
5. `cd docs && mkdocs build --strict` — exits 0
</verification>

<success_criteria>
1. No reference to `/api/admin/features` appears anywhere under `docs/docs/` (EEDOC-01)
2. `install.md` Enterprise Edition section contains: feature list with exact API key names, a Dashboard/CLI tab pair, and the full 9-key GET /api/features JSON response (EEDOC-01)
3. `licensing.md` "Checking your licence" section contains `GET /api/features` with the full 9-key JSON (EEDOC-01)
4. `AXIOM_EE_LICENCE_KEY` does not appear anywhere under `docs/docs/` (EEDOC-02)
5. `mkdocs build --strict` exits 0 after both edits
</success_criteria>

<output>
After completion, create `.planning/phases/68-ee-documentation/68-01-SUMMARY.md` with:
- What was changed in each file
- The exact new content added to install.md (EE section expansion)
- The exact new content added to licensing.md (features subsection)
- mkdocs build --strict result
- Requirements closed: EEDOC-01, EEDOC-02
</output>
