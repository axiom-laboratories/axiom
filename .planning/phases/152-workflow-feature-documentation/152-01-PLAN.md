---
phase: 152-workflow-feature-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docs/workflows/index.md
  - docs/mkdocs.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Workflow documentation structure is registered in MkDocs nav"
    - "MkDocs site builds without errors or broken nav links"
    - "Overview page exists and links to all sub-pages"
  artifacts:
    - path: "docs/docs/workflows/"
      provides: "Documentation directory for all 6 workflow pages"
    - path: "docs/docs/workflows/index.md"
      provides: "Overview and navigation hub for workflow docs"
    - path: "docs/mkdocs.yml"
      provides: "Nav registration for workflow pages"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/docs/workflows/"
      via: "nav entries"
      pattern: "workflows.*index.md"
---

<objective>
Set up the documentation directory structure and register workflow pages in MkDocs navigation. This Wave 0 foundation enables all downstream content tasks to write documentation files that will build cleanly.

Purpose: Establish the doc foundation so MkDocs build validation works throughout implementation
Output: Directory created, nav registered, index.md written, build passes
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-workflow-feature-documentation/152-CONTEXT.md
@.planning/phases/152-workflow-feature-documentation/152-RESEARCH.md
@docs/mkdocs.yml (to inspect nav structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflows directory structure and mkdocs.yml entries</name>
  <files>docs/docs/workflows/index.md, docs/mkdocs.yml</files>
  <action>
1. Create `docs/docs/workflows/` directory (if not exists)
2. Read `docs/mkdocs.yml` to understand current nav structure (look for Feature Guides and Runbooks sections)
3. Add the following nav entries under the Feature Guides section (find where other feature guides like Jobs/Foundry are listed):
   ```yaml
   - Workflows:
     - Overview: workflows/index.md
     - Concepts: workflows/concepts.md
     - User Guide: workflows/user-guide.md
     - Operator Guide: workflows/operator-guide.md
     - Developer Guide: workflows/developer-guide.md
   ```
4. Also add to Runbooks section:
   ```yaml
   - Workflows: runbooks/workflows.md
   ```
5. Create stub files with headers only (no content) for all 6 workflow docs:
   - `docs/docs/workflows/index.md` — "# Workflow Documentation" + "This section documents workflow features..."
   - `docs/docs/workflows/concepts.md` — "# Concepts" (stub)
   - `docs/docs/workflows/user-guide.md` — "# User Guide" (stub)
   - `docs/docs/workflows/operator-guide.md` — "# Operator Guide" (stub)
   - `docs/docs/workflows/developer-guide.md` — "# Developer Guide" (stub)
6. Create stub for runbook:
   - `docs/docs/runbooks/workflows.md` — "# Workflow Operations Runbook" (stub)
7. Verify nav is properly indented (2-space YAML)
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/docs && mkdocs build --strict 2>&1 | tail -10</automated>
  </verify>
  <done>
MkDocs build passes with no nav errors; all 6 workflow docs and runbook files exist; nav entries registered under Feature Guides and Runbooks
  </done>
</task>

<task type="auto">
  <name>Task 2: Update API reference nav entry to include workflow content placeholder</name>
  <files>docs/mkdocs.yml, docs/docs/api-reference/index.md</files>
  <action>
1. Read `docs/mkdocs.yml` and locate the API Reference section in the nav
2. Verify the nav entry points to `api-reference/index.md` (it should already exist from prior phases)
3. If the file doesn't exist, create it with stub header: "# API Reference"
4. If it exists, verify it has content or is a valid stub
5. The index.md is a shared file; workflow API content will be appended to it later in Plan 04, so just ensure the file exists and builds cleanly
6. Run build to confirm no nav errors
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/docs && mkdocs build --strict 2>&1 | grep -i "api-reference\|error" | head -5</automated>
  </verify>
  <done>
API reference nav entry exists and points to valid file; build passes
  </done>
</task>

</tasks>

<verification>
Run MkDocs build to confirm:
1. No nav errors or broken references
2. All 6 workflow doc files are discoverable (check build output for warnings)
3. All runbook and API reference entries parse correctly
</verification>

<success_criteria>
- `mkdocs build --strict` completes with exit code 0
- No missing file errors in build output
- MkDocs nav structure is valid (indentation correct, all paths exist)
- All 7 files exist (`workflows/{index,concepts,user-guide,operator-guide,developer-guide}.md`, `runbooks/workflows.md`, `api-reference/index.md`)
</success_criteria>

<output>
After completion, create `.planning/phases/152-workflow-feature-documentation/152-01-SUMMARY.md`

Document:
- Files created/modified (7 total)
- MkDocs nav registration (6 workflow pages + 1 runbook + 1 API ref update)
- Build validation result
</output>
