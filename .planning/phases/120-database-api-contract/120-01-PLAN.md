---
phase: 120-database-api-contract
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - puppeteer/tests/test_job_limits.py
  - puppeteer/tests/test_migration_v49.py
autonomous: true
requirements: [ENFC-03]
user_setup: []

must_haves:
  truths:
    - "Unit tests for Job model limit columns exist and are runnable"
    - "Migration idempotency tests exist for migration_v49.sql"
    - "Test fixtures include both jobs with and without limits"
    - "All test files follow existing pytest pattern in codebase"
  artifacts:
    - path: "puppeteer/tests/test_job_limits.py"
      provides: "Unit test scaffold for JobCreate, JobResponse, WorkResponse, nullable limit handling"
      min_lines: 80
    - path: "puppeteer/tests/test_migration_v49.py"
      provides: "Integration test for migration_v49.sql idempotency and schema verification"
      min_lines: 60
  key_links:
    - from: "tests/test_job_limits.py"
      to: "agent_service/models.py"
      via: "imports JobCreate, JobResponse, WorkResponse"
      pattern: "from agent_service.models import"
    - from: "tests/test_migration_v49.py"
      to: "puppeteer/migration_v49.sql"
      via: "executes migration, verifies columns exist"
      pattern: "CREATE TABLE|ALTER TABLE"

---

<objective>
Create test infrastructure (Wave 0) for Phase 120 database & API contract work.

Purpose: Establish TDD foundation before implementation. Tests define expected behavior for limit persistence, serialization, validation, and migration safety.
Output: Two test files with RED test cases (failing until Wave 1 implementation completes)
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-database-api-contract/120-CONTEXT.md
@.planning/phases/120-database-api-contract/120-RESEARCH.md

# Existing test patterns
@puppeteer/tests/conftest.py
@puppeteer/tests/test_tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_job_limits.py with RED unit tests</name>
  <files>puppeteer/tests/test_job_limits.py</files>
  <behavior>
    - Test 1: `test_memory_limit_in_job_create` — JobCreate with memory_limit="512m" accepted, field preserved
    - Test 2: `test_cpu_limit_in_job_create` — JobCreate with cpu_limit="2" accepted, field preserved
    - Test 3: `test_memory_limit_nullable_in_job_create` — JobCreate with memory_limit=None accepted, defaults to None
    - Test 4: `test_memory_format_validation_rejects_invalid` — JobCreate with memory_limit="xyz" raises ValueError
    - Test 5: `test_cpu_format_validation_rejects_invalid` — JobCreate with cpu_limit="abc" raises ValueError
    - Test 6: `test_memory_limit_in_job_response` — JobResponse serializes memory_limit correctly
    - Test 7: `test_cpu_limit_in_job_response` — JobResponse serializes cpu_limit correctly
    - Test 8: `test_work_response_has_limits` — WorkResponse includes memory_limit and cpu_limit fields
    - Test 9: `test_job_limits_persist_to_db` — Job created with limits, retrieved from DB, limits intact
    - Test 10: `test_backward_compatibility_old_jobs_no_limits` — Jobs without limits (legacy) still queryable, limits are None
  </behavior>
  <action>
    Create `/home/thomas/Development/master_of_puppets/puppeteer/tests/test_job_limits.py` following existing test patterns in `puppeteer/tests/test_tools.py`.

    Import structure:
    - `pytest`, `pytest.mark.asyncio` for async test support
    - `from agent_service.models import JobCreate, JobResponse, WorkResponse`
    - `from agent_service.db import Job, AsyncSessionLocal` for DB tests
    - Use existing `async_client` fixture from conftest.py for route-based tests

    Test coverage:
    1. **Format validation tests** (3 tests): Valid and invalid memory/CPU formats. Test that JobCreate field_validator accepts "512m", "1g", "1Gi", "2", "0.5" and rejects "xyz", "512", "1gb". Test both positive (passes) and negative (raises ValueError).
    2. **Nullable field tests** (2 tests): Confirm that JobCreate, JobResponse, WorkResponse all accept None for memory_limit and cpu_limit. Test JSON serialization includes/omits None fields as per Pydantic defaults.
    3. **Model serialization tests** (2 tests): Create JobResponse and WorkResponse with limits, verify fields appear in `.model_dump()` output and JSON serialization.
    4. **DB persistence test** (1 test): Create a Job object with memory_limit and cpu_limit via SQLAlchemy, commit to test DB (from conftest fixture), query it back, verify fields are preserved (MEDIUM confidence — requires conftest DB fixture, may need to add).
    5. **Backward compatibility test** (1 test): Create a legacy Job object without setting limits (simulating pre-Phase-120 data), query it, verify limits are None and don't cause errors when accessed.

    Test execution:
    - All 10 tests should FAIL (RED) until Phase 120 Wave 1 adds models and DB columns
    - Use `@pytest.mark.asyncio` for DB tests that require async session
    - Group related tests with descriptive test names
    - Include both positive and negative cases (e.g., valid format passes, invalid format raises)

    Fixtures:
    - Reuse `async_client` from conftest.py
    - Create helper fixture `sample_job_create()` returning JobCreate instance with limits
    - For DB tests, use `AsyncSessionLocal` fixture from existing conftest or create one if missing
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_job_limits.py -v 2>&1 | head -50</automated>
  </verify>
  <done>test_job_limits.py exists with 10 RED (failing) unit tests covering format validation, nullable fields, serialization, and backward compatibility. All tests runnable via pytest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_migration_v49.py with RED integration tests</name>
  <files>puppeteer/tests/test_migration_v49.py</files>
  <behavior>
    - Test 1: `test_migration_v49_adds_memory_limit_column` — After migration, jobs table has memory_limit column
    - Test 2: `test_migration_v49_adds_cpu_limit_column` — After migration, jobs table has cpu_limit column
    - Test 3: `test_migration_v49_idempotent` — Running migration twice succeeds (IF NOT EXISTS handles second run)
    - Test 4: `test_migration_v49_columns_nullable` — Column constraints allow NULL values
    - Test 5: `test_migration_v49_fresh_sqlite_includes_columns` — Fresh SQLite DB (via create_all) includes limit columns
  </behavior>
  <action>
    Create `/home/thomas/Development/master_of_puppets/puppeteer/tests/test_migration_v49.py` following integration test patterns in codebase.

    Migration test approach:
    - Depends on a test DB that can be manipulated (Postgres or SQLite in-memory)
    - Cannot rely on conftest.py DB fixture (it's production-like); create isolated test DB for this test file
    - Use SQLAlchemy's `inspect()` to query table columns after migration

    Test coverage:
    1. **Column existence tests** (2 tests): Run migration_v49.sql, inspect jobs table, verify both memory_limit and cpu_limit columns exist with correct type (VARCHAR/TEXT).
    2. **Idempotency test** (1 test): Run migration twice in same test DB, second run should not fail (key test for Postgres IF NOT EXISTS safety).
    3. **Column constraints test** (1 test): Insert a job without specifying limits, verify NULL allowed; insert a job with limit values, verify both scenarios work.
    4. **Fresh deployment test** (1 test): Create fresh DB using `Base.metadata.create_all()`, verify SQLite schema includes limit columns (proves create_all pattern works).

    Migration file reference:
    - migration_v49.sql must be created in puppeteer/ root (Task 3 in Wave 1 will do this)
    - For now, test assumes migration_v49.sql exists; test will FAIL until file created
    - Use subprocess or SQL Alchemy `text()` to execute raw migration SQL

    Test execution:
    - All 5 tests FAIL (RED) until migration_v49.sql exists and is correct
    - Use `@pytest.mark.asyncio` if interacting with async engine
    - May need to modify conftest.py to expose a writable test DB fixture (or create separate fixture in this test file)

    Fixtures:
    - Create `test_db_url` fixture that points to isolated test Postgres or SQLite:memory: DB
    - Create `test_engine` fixture with SQLAlchemy async engine bound to test_db_url
    - Create `run_migration` helper that executes migration_v49.sql against test DB
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_migration_v49.py -v 2>&1 | head -50</automated>
  </verify>
  <done>test_migration_v49.py exists with 5 RED (failing) integration tests covering migration idempotency, column existence, nullability, and fresh schema verification. All tests runnable via pytest.</done>
</task>

</tasks>

<verification>
Phase 0 (test scaffold) success criteria:
- Both test files exist and are importable: `pytest --collect-only puppeteer/tests/test_job_limits.py puppeteer/tests/test_migration_v49.py` returns all test names without errors
- All 15 tests visible and executable (tests may FAIL, but must RUN)
- No import errors or syntax errors in test files
- Test names match requirement traceability (ENFC-03 covered by tests)
</verification>

<success_criteria>
Wave 0 test scaffold complete when:
1. `puppeteer/tests/test_job_limits.py` exists with 10 RED unit tests
2. `puppeteer/tests/test_migration_v49.py` exists with 5 RED integration tests
3. Both files follow existing pytest conventions (imports, fixtures, async patterns)
4. Tests are executable: `pytest tests/test_job_limits.py -x` and `pytest tests/test_migration_v49.py -x` run without import/syntax errors
5. Tests are RED (failing) until Wave 1 implementation adds models and migration file
6. All 15 tests are traced to ENFC-03 requirement
</success_criteria>

<output>
After completion, create `.planning/phases/120-database-api-contract/120-01-SUMMARY.md`
</output>
