---
phase: 40-lxc-node-provisioning
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/thomas/Development/mop_validation/scripts/provision_lxc_nodes.py
autonomous: false
requirements: [NODE-01, NODE-02, NODE-03, NODE-04, NODE-05]
gap_closure: true

must_haves:
  truths:
    - "provision_lxc_nodes.py authenticates successfully against POST /auth/login"
    - "get_jwt() receives a 200 response and returns an access_token"
    - "Token generation proceeds past the auth step, enabling container provisioning"
  artifacts:
    - path: "/home/thomas/Development/mop_validation/scripts/provision_lxc_nodes.py"
      provides: "Fixed auth call using form-encoded body"
      contains: "data={\"username\": \"admin\", \"password\":"
  key_links:
    - from: "provision_lxc_nodes.py get_jwt()"
      to: "POST /auth/login"
      via: "requests.post with data= (form-encoded)"
      pattern: "data=\\{\"username\""
---

<objective>
Fix the one-line auth bug in the LXC provisioner that prevents it from authenticating against the orchestrator.

Purpose: The provisioner sends credentials as JSON to `/auth/login`, but the backend endpoint uses `OAuth2PasswordRequestForm` which only reads form-encoded request bodies (`Content-Type: application/x-www-form-urlencoded`). Sending `json=` produces a 422 response, halting all token generation and container provisioning before any LXC work begins.

Output: `provision_lxc_nodes.py` with line 93 changed from `json=` to `data=`.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-lxc-node-provisioning/40-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix json= to data= in get_jwt()</name>
  <files>/home/thomas/Development/mop_validation/scripts/provision_lxc_nodes.py</files>
  <action>
    In `get_jwt()` at line 93, change the `requests.post` keyword argument from `json=` to `data=`.

    Before:
    ```python
    resp = requests.post(
        f"{base_url}/auth/login",
        json={"username": "admin", "password": admin_password},
        verify=False,
        timeout=15,
    )
    ```

    After:
    ```python
    resp = requests.post(
        f"{base_url}/auth/login",
        data={"username": "admin", "password": admin_password},
        verify=False,
        timeout=15,
    )
    ```

    No other changes. The `verify_lxc_nodes.py` script already uses `data=` for the same login call and is correct — this brings the provisioner in line with the same pattern.
  </action>
  <verify>
    <automated>grep -n "data={\"username\"" /home/thomas/Development/mop_validation/scripts/provision_lxc_nodes.py</automated>
  </verify>
  <done>Line 93 reads `data={"username": "admin", "password": admin_password}` and grep confirms the pattern. No `json={"username"` remains in get_jwt().</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify provisioner runs end-to-end</name>
  <files>/home/thomas/Development/mop_validation/scripts/provision_lxc_nodes.py</files>
  <action>Human runs the provisioner against the live stack to confirm the auth fix works end-to-end and all 4 LXC nodes are provisioned successfully.</action>
  <what-built>One-line auth fix in provision_lxc_nodes.py — `json=` changed to `data=` at line 93 in get_jwt().</what-built>
  <how-to-verify>
    With the puppeteer stack running at https://localhost:8001, run:

    ```
    cd /home/thomas/Development/mop_validation
    python3 scripts/provision_lxc_nodes.py
    ```

    Expected:
    1. Script authenticates (no 422 error, no raise_for_status exception at the auth step)
    2. Four Incus containers (axiom-node-dev/test/prod/staging) are created and running
    3. Each container has a unique JOIN_TOKEN written to secrets/nodes/&lt;name&gt;.env
    4. All 4 nodes appear as HEALTHY in the dashboard (GET /api/nodes) within 3 minutes

    After provisioner succeeds, optionally run the full verification suite:
    ```
    python3 scripts/verify_lxc_nodes.py
    ```
    Expected: 5/5 sections print [PASS], exit code 0.

    Note: Requires Incus running on host, Docker registry at localhost:5000, and localhost/master-of-puppets-node:latest image built.
  </how-to-verify>
  <verify>
    <automated>MISSING — requires live Incus environment; human-only verification</automated>
  </verify>
  <done>Provisioner completes without auth errors, 4 nodes appear HEALTHY in GET /api/nodes, secrets/nodes/ contains 4 per-node .env files with unique JOIN_TOKENs.</done>
  <resume-signal>Type "approved" if provisioner succeeds, or describe any remaining failures</resume-signal>
</task>

</tasks>

<verification>
After the fix:
- `grep -n "data=" /home/thomas/Development/mop_validation/scripts/provision_lxc_nodes.py | grep username` returns the corrected line
- No `json={"username"` pattern remains in get_jwt()
- The script is syntactically valid: `python3 -c "import ast; ast.parse(open('/home/thomas/Development/mop_validation/scripts/provision_lxc_nodes.py').read()); print('OK')"`
</verification>

<success_criteria>
- Line 93 of provision_lxc_nodes.py uses `data=` (form-encoded) instead of `json=`
- Provisioner authenticates successfully against the orchestrator and proceeds to provision all 4 LXC nodes
- All 5 NODE-xx requirements unblocked (the auth bug was the sole blocker)
</success_criteria>

<output>
After completion, create `.planning/phases/40-lxc-node-provisioning/40-03-SUMMARY.md`
</output>
