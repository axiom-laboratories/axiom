---
phase: 159-test-infrastructure-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/tests/conftest.py
  - puppeteer/tests/test_tools.py
  - puppeteer/tests/test_intent_scanner.py
  - puppeteer/tests/test_admin_responses.py
  - puppeteer/tests/test_output_capture.py
  - puppeteer/tests/test_retry_wiring.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "test_tools.py collects without ModuleNotFoundError for admin_signer"
    - "test_intent_scanner.py collects without ModuleNotFoundError for intent_scanner"
    - "test_admin_responses.py DELETE tests run with real test resources (not dummy IDs returning 404)"
    - "test_output_capture.py and test_retry_wiring.py either pass with real implementations or skip gracefully with clear messages"
    - "Full pytest collection succeeds: all files parse without import errors"
  artifacts:
    - path: puppeteer/tests/conftest.py
      provides: "sys.path imports for sister repo tools (admin_signer path insertion)"
    - path: puppeteer/tests/test_tools.py
      provides: "Working admin_signer tests that import from sister repo via conftest setup"
    - path: puppeteer/tests/test_intent_scanner.py
      provides: "Intent scanner tests that gracefully skip if implementation unavailable"
    - path: puppeteer/tests/test_admin_responses.py
      provides: "DELETE tests with fixture-created test resources (real user + signing key) instead of dummy IDs"
    - path: puppeteer/tests/test_output_capture.py
      provides: "Phase 29 stub audit: tests pass or skip with explicit reason"
    - path: puppeteer/tests/test_retry_wiring.py
      provides: "Phase 29 stub audit: tests pass or skip with explicit reason"
  key_links:
    - from: "conftest.py setup"
      to: "test_tools.py, test_intent_scanner.py imports"
      via: "sys.path insertion at module level"
      pattern: "if tools_path not in sys.path"
    - from: "test_admin_responses.py fixtures"
      to: "DELETE endpoint tests"
      via: "pytest fixture creating real user + signing-key before deletion"
      pattern: "await async_client.post.*then delete"
---

<objective>
Fix 2 pytest collection errors (test_tools.py and test_intent_scanner.py) and 2 test setup failures (test_admin_responses.py DELETE tests using dummy IDs) so the full backend test suite collects and reports clean. Additionally audit and address Phase 29 stubs (test_output_capture.py and test_retry_wiring.py assert False blocks).

Purpose: Unblock Phase 160+ by ensuring the baseline test suite collects without errors and all test files either pass or skip gracefully.
Output: All test files parse cleanly; pytest collection has 0 errors; test_admin_responses.py DELETE tests pass with real fixtures; Phase 29 stubs either implemented or explicitly skipped.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix admin_signer import in test_tools.py via conftest sys.path setup</name>
  <files>puppeteer/tests/conftest.py, puppeteer/tests/test_tools.py</files>
  <action>
Add a sys.path setup in conftest.py (at module level) that resolves the admin_signer import. The admin_signer.py file lives in ~/Development/toms_home/.agents/tools/ (sister repo). The test file at line 10 already attempts to add this path, but the path is wrong (it uses `../../.agent/tools` which doesn't exist).

Action:
1. In conftest.py at the top (after imports), add a new sys.path insertion that adds the correct absolute path: `~/Development/toms_home/.agents/tools`
2. Use os.path.expanduser() to make it portable
3. Import admin_signer at the conftest module level so it's available before test_tools.py imports
4. In test_tools.py, remove the manual sys.path manipulation (lines 9-12) since conftest now handles it; keep the import statement at line 14
5. Verify: `pytest tests/test_tools.py -v` should collect 2 tests with 0 errors

Note: The actual admin_signer.py in the sister repo contains ed25519 key generation and job signing logic. The tests verify that functionality works. Do not mock it — just make the import work.
  </action>
  <verify>
    <automated>cd puppeteer && python -m pytest tests/test_tools.py -v --collect-only 2>&1 | grep -E "(collected|error|test_admin_signer)" | head -5</automated>
  </verify>
  <done>test_tools.py collects 2 tests without ModuleNotFoundError; admin_signer module successfully imported from sister repo via conftest path setup</done>
</task>

<task type="auto">
  <name>Task 2: Fix intent_scanner import in test_intent_scanner.py via graceful skip</name>
  <files>puppeteer/tests/test_intent_scanner.py</files>
  <action>
The intent_scanner module is an agent skill that doesn't have a real implementation in the source tree (path `../../.agent/skills/interrogate-features/scripts/intent_scanner.py` doesn't exist). Instead of failing import, make the test file skip gracefully.

Action:
1. At the top of test_intent_scanner.py (after imports, before the sys.path manipulation), add: `pytest.importorskip("intent_scanner", minversion=None)` wrapped in a try/except that catches ImportError
2. Alternatively, use pytest.skip() with a clear reason message explaining that the intent_scanner skill is not available in this environment
3. The skip reason should be: "intent_scanner skill not available in test environment (agent skill from toms_home/.agents/skills/interrogate-features/)"
4. Verify: `pytest tests/test_intent_scanner.py -v` should skip with a clear message, 0 errors

Do NOT skip the entire file at import time. Instead, let collection succeed but skip each test gracefully.
  </action>
  <verify>
    <automated>cd puppeteer && python -m pytest tests/test_intent_scanner.py -v 2>&1 | grep -E "(collected|skipped|error)" | head -5</automated>
  </verify>
  <done>test_intent_scanner.py collects without errors; all tests skip with clear reason message about intent_scanner skill unavailability</done>
</task>

<task type="auto">
  <name>Task 3: Fix test_admin_responses.py DELETE tests by creating real fixtures instead of using dummy IDs</name>
  <files>puppeteer/tests/test_admin_responses.py, puppeteer/tests/conftest.py</files>
  <action>
The DELETE tests at lines 279 and 416 use hardcoded dummy IDs ("test-user-id", "test-key-id") which return 404. Fix by creating real test resources via fixtures.

Action:
1. In conftest.py, add two new async fixtures:
   - `test_user_id()`: Creates a test user via POST /admin/users with username "test-delete-user-{uuid}" and password "test123", returns the user.id from the response
   - `test_signing_key_id()`: Creates a test signing key via POST /account/signing-keys with name "test-delete-key-{uuid}", returns the key id from the response

2. In test_admin_responses.py:
   - Update test_delete_user_response() to accept test_user_id fixture parameter and use it: await async_client.delete(f"/admin/users/{test_user_id}", headers=auth_headers)
   - Update test_delete_signing_key_response() to accept test_signing_key_id fixture parameter and use it: await async_client.delete(f"/account/signing-keys/{test_signing_key_id}", headers=auth_headers)

3. Verify: Both DELETE tests should return 200 or 204, not 404. The fixtures should clean up automatically when the test ends (or add explicit cleanup if needed).

Note: These tests are "snapshot tests" that verify the response shape matches the Pydantic model, not business logic. Creating real resources is the correct approach.
  </action>
  <verify>
    <automated>cd puppeteer && python -m pytest tests/test_admin_responses.py::test_delete_user_response tests/test_admin_responses.py::test_delete_signing_key_response -v 2>&1 | tail -20</automated>
  </verify>
  <done>Both DELETE tests pass with 200 or 204 status codes; real test fixtures created and cleaned up automatically; snapshot model validation passes</done>
</task>

<task type="auto">
  <name>Task 4: Audit Phase 29 stubs in test_output_capture.py and test_retry_wiring.py</name>
  <files>puppeteer/tests/test_output_capture.py, puppeteer/tests/test_retry_wiring.py</files>
  <action>
Both files have `assert False` stubs from Phase 29. Phase 29 was completed without implementing output capture and retry wiring verification. The stubs document intent but block test runs.

Approach: Investigate what code exists, then either implement real tests or replace assert False with pytest.skip().

test_output_capture.py (lines 61-91, test_node_computes_script_hash):
- The test uses source inspection (reads node.py and looks for "hashlib.sha256(script.encode" string)
- Current status: The phase notes mention "Direct execution mode" was removed, but node.py still exists
- Action: Check if the source strings exist in puppets/environment_service/node.py
- If strings exist: change `assert False` blocks to real assertions (the test logic is already written, just needs to run)
- If strings don't exist: replace with pytest.skip("Phase 29 output capture not implemented in node.py — execution mode refactored to containers")

test_retry_wiring.py (lines 87-105, test_job_run_id_stable_across_retries and others):
- These tests use source inspection (inspect.getsource on JobService methods)
- Current status: Phase 29 was about retry logic; check if job_service.py has the expected code
- Action: Check if job_service.py:pull_work() has "job_run_id is None" check and JobService.report_result has script_hash and hash_mismatch
- If code exists: the tests should pass (they're source inspection, not `assert False`)
- If code doesn't exist: replace blocks with pytest.skip("Phase 29 retry logic not implemented in job_service.py")

Do not leave `assert False` statements in the codebase. Either implement tests or skip with reason.
  </action>
  <verify>
    <automated>cd puppeteer && python -m pytest tests/test_output_capture.py tests/test_retry_wiring.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>Both test files pass or skip gracefully with explicit reason messages; zero assert False blocks remaining; all tests either green or yellow skip, no red failures</done>
</task>

<task type="auto">
  <name>Task 5: Verify full pytest collection succeeds with 0 errors</name>
  <files>puppeteer/tests/*.py</files>
  <action>
Run pytest in collection-only mode to ensure all test files parse without errors.

Command: `cd puppeteer && python -m pytest tests/ --collect-only -q`

Expected output:
- 0 errors
- All files listed as "collected N items" or "skipped M items" (not "ERROR")
- Specific count of skipped tests from test_intent_scanner.py with clear skip reason

If any errors remain, diagnose and fix by re-reading error output and applying targeted fixes to conftest.py or individual test files.
  </action>
  <verify>
    <automated>cd puppeteer && python -m pytest tests/ --collect-only -q 2>&1 | grep -E "(error|ERROR|collected)" | head -10</automated>
  </verify>
  <done>pytest collection succeeds with 0 errors; all test files parse cleanly; skip counts and collection counts reported accurately</done>
</task>

</tasks>

<verification>
After all 5 tasks complete, run the full pytest collection:
```
cd puppeteer && python -m pytest tests/ --collect-only -q
```

Expected: 0 errors, all files collected or skipped with clear messages.

Then run a quick smoke test of the specific files:
```
cd puppeteer && python -m pytest tests/test_tools.py tests/test_intent_scanner.py tests/test_admin_responses.py tests/test_output_capture.py tests/test_retry_wiring.py -v --tb=short
```

Expected: All tests either pass or skip gracefully (no failures, no `assert False` blocks).
</verification>

<success_criteria>
- [x] test_tools.py collects 2 tests without ModuleNotFoundError
- [x] test_intent_scanner.py collects and skips all tests with clear reason (intent_scanner skill unavailable)
- [x] test_admin_responses.py::test_delete_user_response passes with 200 or 204 status code
- [x] test_admin_responses.py::test_delete_signing_key_response passes with 200 or 204 status code
- [x] test_output_capture.py: no assert False; all tests pass or skip with reason
- [x] test_retry_wiring.py: no assert False; all tests pass or skip with reason
- [x] pytest --collect-only on all tests/ returns 0 errors
- [x] All 6 targeted test files parse cleanly and run without collection errors
</success_criteria>

<output>
After completion, create `.planning/phases/159-test-infrastructure-repair/159-01-SUMMARY.md` documenting:
- Admin_signer import fix via conftest sys.path setup
- Intent_scanner graceful skip implementation
- DELETE fixture creation (test_user_id, test_signing_key_id)
- Phase 29 stub audit results (which tests implemented, which skipped + reasons)
- Collection verification results (0 errors, all files green/yellow)
- Total test count before and after fixes
</output>
