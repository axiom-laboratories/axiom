---
phase: 153-verify-gate-node-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/tests/conftest.py
  - puppeteer/tests/test_gate_evaluation.py
autonomous: true
requirements:
  - GATE-01
  - GATE-02
user_setup: []

must_haves:
  truths:
    - "All gate evaluation unit tests pass (TestEvaluateCondition, TestEvaluateIfGate)"
    - "Condition evaluation correctly handles eq, neq, gt, lt, contains, exists operators"
    - "IF_GATE correctly evaluates conditions and routes to matching branches"
    - "Unmatched IF_GATE conditions return error signal for cascade handling"
  artifacts:
    - path: puppeteer/tests/test_gate_evaluation.py
      provides: "22 unit tests for GateEvaluationService (GATE-01/02 coverage)"
      min_lines: 200
    - path: puppeteer/agent_service/services/gate_evaluation_service.py
      provides: "Condition resolution and evaluation logic"
      exports: ["GateEvaluationService.resolve_field", "GateEvaluationService.evaluate_condition", "GateEvaluationService.evaluate_if_gate"]
  key_links:
    - from: puppeteer/tests/test_gate_evaluation.py
      to: puppeteer/agent_service/services/gate_evaluation_service.py
      via: "import GateEvaluationService"
      pattern: "from agent_service.services.gate_evaluation_service import"
---

<objective>
Verify and fix gate condition evaluation (GATE-01, GATE-02) by running unit tests and ensuring GateEvaluationService correctly handles all condition operators and IF gate routing logic.

Purpose: Foundation for subsequent gate dispatch tests; GATE-01/02 are prerequisites for IF_GATE integration in workflow dispatch.

Output: Passing test_gate_evaluation.py unit tests; evidence artifacts for VERIFICATION.md.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md

Phase 153 is verify-and-fix: if a requirement is not fully implemented, fix it during this phase before marking as closed.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/153-verify-gate-node-types/153-CONTEXT.md
@.planning/phases/153-verify-gate-node-types/153-RESEARCH.md

## Existing Gate Implementation

From Phase 148 (4 plans completed):
- `puppeteer/agent_service/services/gate_evaluation_service.py` — GateEvaluationService with resolve_field() and evaluate_condition()
- `puppeteer/agent_service/services/workflow_service.py` — workflow dispatch with gate node handling (_evaluate_if_gates, dispatch_next_wave)
- `puppeteer/tests/test_gate_evaluation.py` — 22 unit tests (currently failing due to schema issues)

## Testing Setup

Working directory: `/home/thomas/Development/master_of_puppets/`

Quick test command:
```bash
cd puppeteer && pytest tests/test_gate_evaluation.py -xvs
```

All gate tests currently fail due to missing `workflow_step_run_id` column on jobs table (SQLite test DB). This plan handles unit-level gate logic verification only; Wave 2 will verify dispatch integration.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SQLite test database schema for gate tests</name>
  <files>puppeteer/tests/conftest.py</files>
  <action>
In conftest.py, update the async session fixture that initializes the test database to apply migration_v54 SQL schema to the in-memory SQLite database before tests run.

Specifically:
1. In the `@pytest.fixture(scope="session")` or `@pytest.fixture(scope="function")` that sets up async_client or async_session, add a setup step after creating tables with `Base.metadata.create_all()`.
2. Execute migration_v54.sql schema: Create workflow_step_runs table, add workflow_step_run_id/depth columns to jobs table.
3. Use `db.execute(text("..."))` with Postgres-compatible (SQLite-compatible) DDL.
4. Ensure the migration runs for every test run (idempotent with IF NOT EXISTS and ADD COLUMN IF NOT EXISTS).

Do not use raw SQL file reading; inline the migration DDL in the fixture setup code for portability.

Reference: migration_v54.sql (lines 5-33) contains the exact DDL needed.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_gate_evaluation.py::TestEvaluateCondition -xvs 2>&1 | grep -E "passed|FAILED" | head -5</automated>
  </verify>
  <done>
Condition evaluation tests run without schema errors. At least TestEvaluateCondition::test_eq passes (schema is valid for gate tests to proceed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Run and verify GATE-01 unit tests (condition evaluation)</name>
  <files>puppeteer/tests/test_gate_evaluation.py</files>
  <action>
Run the TestEvaluateCondition test class to verify GATE-01 implementation:

```bash
cd puppeteer && pytest tests/test_gate_evaluation.py::TestEvaluateCondition -xvs
```

Expected assertions (from GATE-01 requirement):
- eq operator: actual == value (equality comparison)
- neq operator: actual != value (inequality)
- gt operator: actual > value (greater than)
- lt operator: actual < value (less than)
- contains operator: value in str(actual) (substring search)
- exists operator: field found in result (existence check)

All 9 test cases should pass. If any fail, the failure is a regression in gate_evaluation_service.py. Do NOT fix the tests; fix the implementation.

Reference: RESEARCH.md lines 75-80 (GATE-01 requirement definition and test map).
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_gate_evaluation.py::TestEvaluateCondition -xvs 2>&1 | tail -3</automated>
  </verify>
  <done>
All 9 condition evaluation tests pass. GATE-01 behavior verified: all 6 operators (eq, neq, gt, lt, contains, exists) correctly evaluate against result.json fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run and verify GATE-02 unit tests (IF_GATE routing)</name>
  <files>puppeteer/tests/test_gate_evaluation.py</files>
  <action>
Run the TestEvaluateIfGate test class to verify GATE-02 implementation:

```bash
cd puppeteer && pytest tests/test_gate_evaluation.py::TestEvaluateIfGate -xvs
```

Expected assertions (from GATE-02 requirement):
- evaluate_if_gate() returns (branch_name, error_msg) tuple
- Evaluates conditions in order: "true" branch first, then "false"
- Returns matching branch name when conditions match
- Returns (None, error_msg) when no branch matches (unmatched gate signal)

All 4 test cases should pass. If any fail, fix the implementation (do not modify test expectations).

Integration note: GATE-02 also requires that workflow_service._evaluate_if_gates() uses this function to:
1. Evaluate the IF_GATE condition
2. On no-match (error_msg is not None), mark the step FAILED and call _cascade_cancel()

This task verifies the unit-level condition evaluation. Integration verification happens in Plan 02 (GATE-03..05) and Plan 03 (GATE-06).

Reference: RESEARCH.md lines 366-393 (GATE-02 code example and integration flow).
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_gate_evaluation.py::TestEvaluateIfGate -xvs 2>&1 | tail -3</automated>
  </verify>
  <done>
All 4 IF_GATE routing tests pass. GATE-02 behavior verified: IF_GATE evaluates conditions, routes to matching branch (true/false), and signals error on no-match for cascade handling.
  </done>
</task>

</tasks>

<verification>
**Layer 1 — Automated Tests:**

Run full gate evaluation test suite:
```bash
cd puppeteer && pytest tests/test_gate_evaluation.py -xvs
```

Must pass: 22 tests total (TestEvaluateCondition 9 + TestEvaluateIfGate 4 + others 9).

Confirm no schema errors (workflow_step_run_id column exists on jobs table in test DB).

**Coverage check:**
- GATE-01 requirement (condition operators): 9 test assertions covering eq, neq, gt, lt, contains, exists, type mismatches, missing fields
- GATE-02 requirement (IF_GATE routing): 4 test assertions covering matching branches, no-match error signals, config validation

If any test fails, investigate and fix the implementation (do not modify test expectations).
</verification>

<success_criteria>
- TestEvaluateCondition: all 9 tests passing ✓
- TestEvaluateIfGate: all 4 tests passing ✓
- No schema errors (workflow_step_run_id column accessible in test DB) ✓
- GATE-01 condition evaluation verified for all 6 operators ✓
- GATE-02 IF_GATE routing verified for branch selection and no-match handling ✓
</success_criteria>

<output>
After completion, create `.planning/phases/153-verify-gate-node-types/153-01-SUMMARY.md` with:
- Test execution results (test counts, pass/fail)
- Evidence: sample output from pytest showing passing tests
- Artifacts: paths to test file with line counts
- Next steps: Plan 02 to verify GATE-03/04/05 (dispatch integration)
</output>
