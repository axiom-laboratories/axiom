---
phase: 46-tech-debt-security-branding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/dashboard/src/views/Templates.tsx
  - puppeteer/dashboard/src/components/CreateBlueprintDialog.tsx
  - puppeteer/dashboard/src/components/CreateTemplateDialog.tsx
  - puppeteer/dashboard/src/components/BlueprintWizard.tsx
  - puppeteer/dashboard/src/views/Admin.tsx
  - puppeteer/dashboard/src/views/__tests__/Templates.test.tsx
autonomous: true
requirements:
  - BRAND-01

must_haves:
  truths:
    - "The Foundry section of the dashboard shows 'Image Recipe' everywhere 'Blueprint' was shown"
    - "The Foundry section shows 'Node Image' everywhere 'Puppet Template' or 'Template' (in Foundry context) was shown"
    - "The Admin page Tools tab shows 'Tool' everywhere 'Capability Matrix' was shown"
    - "No legacy labels ('Blueprint', 'Puppet Template', 'Capability Matrix') remain in any visible UI string in the Foundry or Admin areas"
    - "Nav sidebar 'Foundry' label is unchanged"
    - "No TypeScript compile errors are introduced — only string literals changed, not interface names or component names"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Templates.tsx"
      provides: "Renamed tab labels, button text, empty state strings"
      contains: "Image Recipe"
    - path: "puppeteer/dashboard/src/components/CreateBlueprintDialog.tsx"
      provides: "Renamed dialog title, field label, button text"
      contains: "Image Recipe"
    - path: "puppeteer/dashboard/src/components/CreateTemplateDialog.tsx"
      provides: "Renamed dialog title, field labels, button text"
      contains: "Node Image"
    - path: "puppeteer/dashboard/src/components/BlueprintWizard.tsx"
      provides: "Renamed step labels, button text, toast messages"
      contains: "Image Recipe"
    - path: "puppeteer/dashboard/src/views/Admin.tsx"
      provides: "Renamed Capability Matrix tab label to Tools"
      contains: "Tools"
    - path: "puppeteer/dashboard/src/views/__tests__/Templates.test.tsx"
      provides: "BRAND-01 smoke test"
  key_links:
    - from: "Templates.test.tsx"
      to: "Templates.tsx rendered output"
      via: "vitest render assertions"
      pattern: "Image Recipe|Node Image"
---

<objective>
Rename legacy labels throughout the Foundry UI section: "Blueprint" becomes "Image Recipe", "Puppet Template"/"Template" (in Foundry context) becomes "Node Image", and "Capability Matrix" references become "Tool".

Purpose: Aligns visible dashboard labels with the product's current branding before the next wave of operator-facing features land. Zero API or DB changes — frontend string literals only.
Output: Updated TSX files with renamed labels, a new smoke test confirming no legacy strings remain in the rendered Foundry UI.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-tech-debt-security-branding/46-CONTEXT.md
@.planning/phases/46-tech-debt-security-branding/46-RESEARCH.md

<interfaces>
<!-- Rename map from CONTEXT.md — locked decisions. -->

Rename map (visible UI strings only — DO NOT rename TypeScript identifiers, interface names, or component names):
  "Blueprint"              → "Image Recipe"         (tab labels, buttons, card headers, dialog titles, toast messages)
  "Runtime Blueprint"      → "Runtime Image Recipe"
  "Network Blueprint"      → "Network Image Recipe"
  "New Runtime Blueprint"  → "New Runtime Image Recipe"
  "New Network Blueprint"  → "New Network Image Recipe"
  "Create New Blueprint"   → "Create New Image Recipe"
  "Blueprint Name"         → "Image Recipe Name"
  "Creating..."            — keep (generic)
  "Create Blueprint"       → "Create Image Recipe"
  "Save Blueprint"         → "Save Image Recipe"
  "Blueprint created successfully" → "Image Recipe created successfully"

  "Compose Puppet Template" → "Compose Node Image"
  "Template Friendly Name"  → "Node Image Name"
  "Runtime Blueprint" (in CreateTemplateDialog) → "Runtime Image Recipe"
  "Create Template"         → "Create Node Image"

  "Capability Matrix"       → "Tools"               (Admin tab trigger label)
  "Add Capability Matrix Entry" → "Add Tool"         (dialog title/button in Admin)
  "Capability Matrix Entry"  → "Tool"               (any card/row labels)

DO NOT change:
  - TypeScript interface names: Blueprint, ToolMatrix, PuppetTemplate
  - React component names: BlueprintItem, BlueprintWizard, CreateBlueprintDialog, etc.
  - API endpoint paths: /api/blueprints, /api/templates
  - Nav sidebar "Foundry" label
  - The string value "matrix" used as a tab value (internal to Radix Tabs) — only its visible label changes

Files confirmed affected (from research):
  - puppeteer/dashboard/src/views/Templates.tsx
  - puppeteer/dashboard/src/components/CreateBlueprintDialog.tsx
  - puppeteer/dashboard/src/components/CreateTemplateDialog.tsx
  - puppeteer/dashboard/src/components/BlueprintWizard.tsx
  - puppeteer/dashboard/src/views/Admin.tsx

No Docs.tsx exists in the codebase — skip the docs sweep mentioned in CONTEXT.md.
Grep for any remaining "Blueprint" or "Capability Matrix" occurrences in other .tsx files before committing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Templates.test.tsx smoke test scaffold for BRAND-01</name>
  <files>puppeteer/dashboard/src/views/__tests__/Templates.test.tsx</files>
  <behavior>
    - Renders the Templates view (or a subset of its components) and asserts "Image Recipe" is present in the output
    - Asserts "Node Image" is present in the output
    - Asserts "Blueprint" (as a standalone visible word, not part of "Image Recipe") is NOT present in rendered output
    - Asserts "Puppet Template" and "Capability Matrix" are NOT present
    - Test must fail RED before the rename is done (current code still has "Blueprint")
  </behavior>
  <action>
    Create puppeteer/dashboard/src/views/__tests__/Templates.test.tsx following the pattern in the existing test files (JobDefinitions.test.tsx, Nodes.test.tsx for reference on mock structure).

    The test should:
    1. Mock the API calls that Templates.tsx makes (use vi.mock for fetch or the authenticatedFetch pattern)
    2. Render the Templates component with mock data that includes a blueprint item and a template item
    3. Assert: screen.getByText(/Image Recipe/i) — should be present
    4. Assert: screen.getByText(/Node Image/i) — should be present
    5. Assert: screen.queryByText(/^Blueprint$/i) is null (legacy label gone)

    Keep mocks minimal — just enough to render without crashing. The test verifies label strings exist or are absent, not full interactivity.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npx vitest run src/views/__tests__/Templates.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Templates.test.tsx exists. Test runs and fails RED (legacy "Blueprint" label still present, "Image Recipe" not yet in code). This confirms the test is correctly sensitive to the rename.</done>
</task>

<task type="auto">
  <name>Task 2: Apply BRAND-01 label rename across all five affected files</name>
  <files>puppeteer/dashboard/src/views/Templates.tsx, puppeteer/dashboard/src/components/CreateBlueprintDialog.tsx, puppeteer/dashboard/src/components/CreateTemplateDialog.tsx, puppeteer/dashboard/src/components/BlueprintWizard.tsx, puppeteer/dashboard/src/views/Admin.tsx</files>
  <action>
    Read each file fully before editing. Apply the rename map below to string literals in JSX only — do NOT rename TypeScript interface names, component function names, or internal identifiers.

    Templates.tsx — rename these visible strings:
    - Tab trigger labels: "Runtime Blueprints" → "Runtime Image Recipes", "Network Blueprints" → "Network Image Recipes"
    - "New Runtime Blueprint" button → "New Runtime Image Recipe"
    - "New Network Blueprint" button → "New Network Image Recipe"
    - Any "Blueprint" in card headers, empty state text, tooltip strings
    - "Add Capability Matrix Entry" button/dialog trigger → "Add Tool"

    CreateBlueprintDialog.tsx — rename these:
    - Dialog title "Create New Blueprint" → "Create New Image Recipe"
    - Label "Blueprint Name" → "Image Recipe Name"
    - Button "Create Blueprint" → "Create Image Recipe"
    - Any "Creating..." text → keep as-is (generic)

    CreateTemplateDialog.tsx — rename these:
    - Dialog title "Compose Puppet Template" → "Compose Node Image"
    - Label "Template Friendly Name" → "Node Image Name"
    - Label "Runtime Blueprint" (the dropdown label) → "Runtime Image Recipe"
    - Button "Create Template" → "Create Node Image"

    BlueprintWizard.tsx — rename these:
    - Any "Blueprint Name" label → "Image Recipe Name"
    - Button "Create Blueprint" → "Create Image Recipe"
    - Button "Save Blueprint" → "Save Image Recipe"
    - Toast "Blueprint created successfully" → "Image Recipe created successfully"

    Admin.tsx — rename these:
    - Tab trigger visible label for capability matrix tab: "Capability Matrix" → "Tools"
    - Any "Add Capability Matrix Entry" text → "Add Tool"
    - Any "Capability Matrix Entry" label in rows/cards → "Tool"

    After all edits, grep the five files for any remaining occurrences of "Blueprint" (outside of JSX string literals where it's part of "Image Recipe"), "Puppet Template", "Capability Matrix" — fix any missed instances.

    Also run: grep -rn '"Blueprint"\|>Blueprint<\|Blueprint Name\|Capability Matrix\|Puppet Template' puppeteer/dashboard/src/ --include="*.tsx" to catch any missed occurrences in other files.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npx vitest run src/views/__tests__/Templates.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Templates.test.tsx passes GREEN. npm run build in puppeteer/dashboard exits 0 (no TypeScript errors). Grep confirms no visible legacy labels remain in the five affected files. Nav "Foundry" label unchanged.</done>
</task>

</tasks>

<verification>
Run full frontend suite after both tasks complete:
```
cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run test
```
All tests pass, including the new Templates.test.tsx.

Also verify TypeScript compile:
```
cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build
```
Build exits 0 with no type errors.
</verification>

<success_criteria>
1. `npx vitest run src/views/__tests__/Templates.test.tsx` passes GREEN
2. `npm run build` exits 0 in puppeteer/dashboard — no TypeScript errors
3. `npm run test` full suite passes with no regressions
4. Grep confirms: no bare "Blueprint", "Puppet Template", "Capability Matrix" visible labels in the five affected files
5. Nav sidebar "Foundry" label is unchanged
6. No TypeScript interface or component names were renamed (only string literals)
</success_criteria>

<output>
After completion, create `.planning/phases/46-tech-debt-security-branding/46-03-SUMMARY.md`
</output>
