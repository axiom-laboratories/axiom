# Plan 18-04: Wave 4 — Final Verification & E2E

**Objective**: Comprehensive verification of the `mop-push` CLI against a live backend.

## Context
Phase 18 is complete when all CLI requirements are verified in a realistic environment.

## Tasks
1. **E2E Manual Run**:
   - Install SDK in a fresh venv.
   - Run `mop-push login`.
   - Push a DRAFT job.
   - Create an ACTIVE job.
   - Verify all appear correctly in the backend.
2. **Integration Suite**:
   - Create a final integration test script that automates the above using a test backend.
3. **Documentation**:
   - Update `docs/SDK_GUIDE.md` or similar with CLI usage instructions.

## Success Criteria
- All 5 manual E2E scenarios pass.
- `mop-push` is fully functional and documented.

## Output
Create `.planning/phases/18-cli-mop-push-implementation/18-04-SUMMARY.md` after completion.
Update `ROADMAP.md` to mark Phase 18 complete.
