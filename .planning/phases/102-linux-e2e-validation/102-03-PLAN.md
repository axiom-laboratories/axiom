---
phase: 102-linux-e2e-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 102-02
files_modified:
  - /home/thomas/Development/mop_validation/reports/FRICTION-LNX-102.md
  - /home/thomas/Development/mop_validation/reports/linux_e2e_synthesis.md
autonomous: false
requirements:
  - LNX-06

must_haves:
  truths:
    - "Every BLOCKER friction point in FRICTION-LNX-102.md has a non-empty Fix applied field"
    - "The golden path re-runs end-to-end with zero BLOCKER friction points in a fresh LXC"
    - "linux_e2e_synthesis.md exists with verdict READY and zero open product BLOCKERs"
  artifacts:
    - path: "/home/thomas/Development/mop_validation/reports/FRICTION-LNX-102.md"
      provides: "Completed friction catalogue with all BLOCKERs fixed"
      contains: "Fix applied:"
    - path: "/home/thomas/Development/mop_validation/reports/linux_e2e_synthesis.md"
      provides: "Phase 102 sign-off report generated by patched synthesise_friction.py"
      contains: "READY"
  key_links:
    - from: "FRICTION-LNX-102.md"
      to: "linux_e2e_synthesis.md"
      via: "synthesise_friction.py --files FRICTION-LNX-102.md"
      pattern: "synthesise_friction.*--files"
---

<objective>
Fix every BLOCKER found during the Plan 02 validation run, iterate the LXC golden path until it completes cleanly, then produce the phase sign-off synthesis report.

Purpose: LNX-06 — all friction catalogued and fixed. The phase is not complete until the golden path runs end-to-end with zero BLOCKERs in a fresh LXC.
Output: FRICTION-LNX-102.md with all BLOCKERs resolved, linux_e2e_synthesis.md with READY verdict.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-linux-e2e-validation/102-CONTEXT.md
@.planning/phases/102-linux-e2e-validation/102-02-SUMMARY.md

<interfaces>
<!-- Fix target patterns (from synthesise_friction.py _derive_fix_target heuristics) -->
<!-- Common fix files: -->
<!--   docs/docs/getting-started/install.md       → deploy/startup friction -->
<!--   docs/docs/getting-started/enroll-node.md   → enrollment friction -->
<!--   docs/docs/getting-started/first-job.md     → job dispatch friction -->
<!--   puppeteer/compose.cold-start.yaml           → compose networking/volume friction -->
<!--   puppets/Containerfile.node                 → node image friction -->
<!--   puppeteer/agent_service/main.py            → API behaviour friction -->

<!-- After any code fix: rebuild and push to GHCR before re-running LXC -->
<!-- After any docs fix: re-push docs files into workspace before re-running subagent -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all BLOCKER friction points</name>
  <files>/home/thomas/Development/mop_validation/reports/FRICTION-LNX-102.md</files>
  <action>
Read FRICTION-LNX-102.md and for each finding classified as BLOCKER, apply the fix.

**Fix strategy:**

1. Read each BLOCKER block: `### [Category] Title` + `**Why it happened:**` field.

2. Determine fix target using the category and title:
   - `[Setup]` or `[Install Step N]` → check `compose.cold-start.yaml` and `install.md`
   - `[Login]` → check `main.py` forced password change logic (`must_change_password` handling)
   - `[Enroll-Node Step N]` → check `enroll-node.md` and `compose.cold-start.yaml` node service definitions
   - `[Job Dispatch]` → check `first-job.md` and signing path in `main.py` / `job_service.py`
   - `[Dashboard]` → check the relevant React view in `puppeteer/dashboard/src/views/`

3. Apply the fix to the source file(s). For docs fixes, edit the markdown directly. For code fixes, edit the relevant Python or TypeScript file.

4. After each fix, update the FRICTION entry's `**Fix applied:**` field to describe what was changed.

5. **After code fixes that affect the Docker images:** Rebuild and push the relevant GHCR image:
   ```bash
   cd /home/thomas/Development/master_of_puppets/puppeteer
   docker build -t ghcr.io/axiom-laboratories/axiom:latest -f Dockerfile .
   docker push ghcr.io/axiom-laboratories/axiom:latest
   ```
   For dashboard changes: rebuild and push `ghcr.io/axiom-laboratories/axiom-dashboard:latest`.
   For docs changes: rebuild and push `ghcr.io/axiom-laboratories/axiom-docs:latest`.

6. Run the backend test suite to confirm no regressions from code fixes:
   ```bash
   cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/ -x -q
   ```
   For frontend fixes: `cd puppeteer/dashboard && npm run test`

**Note:** If the FRICTION file shows "Verdict: PASS — no blockers" from Plan 02, this task is a no-op (skip to Task 2).
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Every BLOCKER entry in FRICTION-LNX-102.md has a non-empty "Fix applied:" field. Backend test suite passes. All changed Docker images rebuilt and pushed to GHCR.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run golden path and iterate until clean</name>
  <files>/home/thomas/Development/mop_validation/reports/FRICTION-LNX-102.md</files>
  <action>
Re-run the full LXC golden path using the Phase 102 orchestrator. This verifies that the fixes from Task 1 resolve all blockers.

```bash
cd /home/thomas/Development/mop_validation/scripts
python3 run_linux_e2e.py
```

The orchestrator re-provisions a fresh LXC (no cached state) and runs the full golden path again from scratch. This is intentional — the LXC must be fresh to simulate a true new-user experience.

**After the re-run:**

Check for remaining blockers:
```bash
grep "Classification.*BLOCKER" /home/thomas/Development/mop_validation/reports/FRICTION-LNX-102.md
```

**If new BLOCKERs are found:** Append them to FRICTION-LNX-102.md (do not overwrite previous entries) and return to Task 1 to fix them. Update the "Fix applied:" fields and re-run again.

**Loop until:** The run completes with zero BLOCKER findings. The FRICTION file will end with "Verdict: PASS — golden path completed end-to-end with no blockers."

**Keep all findings** across iterations — add new findings as additional `### [Category]` blocks below the existing ones. Do not delete previous entries even if they are now fixed. This maintains a complete history of what was found and fixed.

When the golden path is clean (zero BLOCKERs), print:
```
PHASE 102 GOLDEN PATH: CLEAN
All BLOCKERs resolved. Ready for sign-off.
```
  </action>
  <verify>
    <automated>grep -c "Classification.*BLOCKER" /home/thomas/Development/mop_validation/reports/FRICTION-LNX-102.md 2>/dev/null | xargs -I{} sh -c 'test {} -eq 0 && echo "PASS: zero BLOCKERs" || echo "FAIL: {} BLOCKERs remain"'</automated>
  </verify>
  <done>Zero BLOCKER findings in FRICTION-LNX-102.md. The golden path ran end-to-end in a fresh LXC with no documentation deviations or error stops.</done>
</task>

<task type="auto">
  <name>Task 3: Generate synthesis sign-off report</name>
  <files>/home/thomas/Development/mop_validation/reports/linux_e2e_synthesis.md</files>
  <action>
Produce the phase sign-off synthesis report using the patched synthesise_friction.py.

```bash
cd /home/thomas/Development/mop_validation/scripts
python3 synthesise_friction.py \
  --files FRICTION-LNX-102.md \
  --output /home/thomas/Development/mop_validation/reports/linux_e2e_synthesis.md
```

After running, verify the report contains a READY verdict:
```bash
grep "Verdict:" /home/thomas/Development/mop_validation/reports/linux_e2e_synthesis.md
```

If the verdict is NOT READY (open BLOCKERs remain), this means Task 2 above has not yet confirmed a clean run. Return to Task 1 and fix remaining blockers before running this step.

If the verdict is READY:
1. Print the executive summary section to the console.
2. Confirm the report was written to the correct path.
3. Run the full test suite one final time as the phase gate:
   ```bash
   cd /home/thomas/Development/master_of_puppets/puppeteer && pytest && cd dashboard && npm run test
   ```

The synthesis report is the phase deliverable and gate artifact for `/gsd:verify-work`.
  </action>
  <verify>
    <automated>grep "Verdict:" /home/thomas/Development/mop_validation/reports/linux_e2e_synthesis.md 2>/dev/null | grep -q "READY" && echo "PASS: READY verdict" || echo "FAIL: verdict not READY or file missing"</automated>
  </verify>
  <done>linux_e2e_synthesis.md exists with "Verdict: READY". Backend and frontend test suites pass. Phase 102 gate conditions are met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase 102 sign-off</name>
  <files>mop_validation/reports/linux_e2e_synthesis.md</files>
  <action>Human reviews synthesis report READY verdict and confirms all BLOCKERs are resolved.</action>
  <verify>
    <automated>grep -q 'READY' /home/thomas/Development/mop_validation/reports/linux_e2e_synthesis.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>User types "approved" — phase 102 sign-off confirmed.</done>
  <what-built>
    Complete Linux E2E validation: golden path ran clean in a fresh LXC, all friction fixed, synthesis report produced with READY verdict.
  </what-built>
  <how-to-verify>
    1. Check the synthesis report verdict: `grep "Verdict:" /home/thomas/Development/mop_validation/reports/linux_e2e_synthesis.md`
    2. Review fixed friction: `cat /home/thomas/Development/mop_validation/reports/FRICTION-LNX-102.md`
    3. Confirm all BLOCKER entries have a "Fix applied:" section with real content (not "None").
    4. Optionally skim linux_e2e_synthesis.md for the executive summary counts.
    5. Run full test suite if not done: `cd /home/thomas/Development/master_of_puppets/puppeteer && pytest -q && cd dashboard && npm run test`
  </how-to-verify>
  <resume-signal>Type "approved" if the synthesis shows READY and all blockers are resolved, or describe any remaining concerns.</resume-signal>
</task>

</tasks>

<verification>
Phase 102 complete when:
1. `grep "Verdict:" /home/thomas/Development/mop_validation/reports/linux_e2e_synthesis.md` → "READY"
2. `grep -c "Classification.*BLOCKER" /home/thomas/Development/mop_validation/reports/FRICTION-LNX-102.md` → all have "Fix applied:" (no "None — requires fix")
3. `cd /home/thomas/Development/master_of_puppets/puppeteer && pytest -q` → all tests pass
4. `cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run test` → all tests pass
</verification>

<success_criteria>
- FRICTION-LNX-102.md catalogues all friction found across all iterations
- Every BLOCKER has been fixed and the fix is documented in the FRICTION file
- linux_e2e_synthesis.md has "Verdict: READY"
- Backend and frontend test suites pass with no regressions from fixes
- Golden path ran clean (zero BLOCKERs) in a fresh LXC as the final verification
</success_criteria>

<output>
After completion, create `/home/thomas/Development/master_of_puppets/.planning/phases/102-linux-e2e-validation/102-03-SUMMARY.md`
</output>
