---
phase: 174-mop-validation-repo-migration
plan: 02
type: execute
wave: 2
depends_on: [174-01]
files_modified: [CLAUDE.md, GEMINI.md, .planning/ROADMAP.md]
autonomous: true
requirements: [MIG-04, MIG-01]
user_setup: []

must_haves:
  truths:
    - "CLAUDE.md references axiom-laboratories org for mop_validation"
    - "GEMINI.md references axiom-laboratories org for mop_validation"
    - "ROADMAP.md Phase 174 goal and success criteria use axiom-laboratories, not axiom"
    - "No lingering axiom/ references to mop_validation repo remain in master_of_puppets"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Sister Repositories section with mop_validation GitHub URL"
      pattern: "github.com/axiom-laboratories/mop_validation"
    - path: "GEMINI.md"
      provides: "Sister Repositories section with mop_validation GitHub URL"
      pattern: "github.com/axiom-laboratories/mop_validation"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 174 goal and success criteria with axiom-laboratories"
      pattern: "axiom-laboratories/mop_validation"
  key_links:
    - from: "CLAUDE.md"
      to: "mop_validation repo docs"
      via: "GitHub URL in Sister Repositories"
      pattern: "axiom-laboratories"
    - from: "GEMINI.md"
      to: "mop_validation repo docs"
      via: "GitHub URL in Sister Repositories"
      pattern: "axiom-laboratories"
---

<objective>
Update documentation files in `master_of_puppets` to reflect the mop_validation repo's new home in the `axiom-laboratories` GitHub organization. Add the new GitHub URL to Sister Repositories sections in CLAUDE.md and GEMINI.md; fix ROADMAP.md naming error (`axiom` → `axiom-laboratories`).

Purpose: Ensure documentation is consistent with the migrated repo; fix the ROADMAP naming error identified in D-05; maintain single source of truth for tooling and repo references.

Output: Three files updated with correct `axiom-laboratories` org references; no ambiguity about repo location; team/future maintainers can find the repo immediately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Key References:**
- D-04: Add GitHub URL to Sister Repositories in both CLAUDE.md and GEMINI.md
- D-05: Fix ROADMAP.md `axiom` → `axiom-laboratories` in Phase 174 goal and success criteria
- CLAUDE.md Sister Repositories is at line 232+ (search for "### `~/Development/mop_validation`")
- GEMINI.md Sister Repositories mention is at line 22 (search for "Validation Repo")
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

**File Status (pre-update):**
- CLAUDE.md: Sister Repositories section exists (line 232–261); describes mop_validation but NO GitHub URL yet
- GEMINI.md: Sister Repositories section exists (line 12–27); mentions mop_validation with local path only
- ROADMAP.md: Phase 174 goal text has `axiom` (lines 288, 292, 294, 298) — should be `axiom-laboratories`

**New URL to add:** `https://github.com/axiom-laboratories/mop_validation` (private repo)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md Sister Repositories section — add GitHub URL for mop_validation</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md (lines 232–261, the entire mop_validation sister repo section)</read_first>
  <action>
In CLAUDE.md, locate the mop_validation Sister Repositories section (search for "### \`~/Development/mop_validation\`" or line ~232).

Add a new line immediately after the section heading or first description line:
```
GitHub: `https://github.com/axiom-laboratories/mop_validation` (private)
```

Example placement:
```markdown
### `~/Development/mop_validation`
GitHub: `https://github.com/axiom-laboratories/mop_validation` (private)
Validation, diagnostics, and development tooling...
```

This documents the public GitHub location of the repo per D-04.
  </action>
  <verify>
Run `grep -n "axiom-laboratories/mop_validation" CLAUDE.md` — must return at least 1 match.
Also verify the line is in the mop_validation section (not elsewhere).
  </verify>
  <acceptance_criteria>
`grep "axiom-laboratories/mop_validation" CLAUDE.md` returns exactly 1 match in the mop_validation Sister Repositories section. The line contains the full GitHub URL. No typos in "axiom-laboratories".
  </acceptance_criteria>
  <done>CLAUDE.md updated with mop_validation GitHub URL.</done>
</task>

<task type="auto">
  <name>Task 2: Update GEMINI.md Sister Repositories section — add GitHub URL for mop_validation</name>
  <files>GEMINI.md</files>
  <read_first>GEMINI.md (lines 12–27, the entire Sister Repositories section with mop_validation subsection)</read_first>
  <action>
In GEMINI.md, locate the "2. **Validation Repo (`~/Development/mop_validation`)**:" section (around line 22).

Within that subsection, add the GitHub URL. Two placement options:
1. After the "Purpose" line: add `GitHub: https://github.com/axiom-laboratories/mop_validation (private)`
2. At the end of the subsection, before the next repo section

Example:
```markdown
2.  **Validation Repo (`~/Development/mop_validation`)**:
    - **GitHub**: https://github.com/axiom-laboratories/mop_validation (private)
    - **Purpose**: E2E tests, diagnostics, and separate test infrastructure.
    - `scripts/`: ...
```

This mirrors the CLAUDE.md update and follows D-04.
  </action>
  <verify>
Run `grep -n "axiom-laboratories/mop_validation" GEMINI.md` — must return at least 1 match in the Validation Repo section.
  </verify>
  <acceptance_criteria>
`grep "axiom-laboratories/mop_validation" GEMINI.md` returns exactly 1 match. URL is complete and spelled correctly ("axiom-laboratories", not "axiom").
  </acceptance_criteria>
  <done>GEMINI.md updated with mop_validation GitHub URL.</done>
</task>

<task type="auto">
  <name>Task 3: Fix ROADMAP.md Phase 174 — replace axiom with axiom-laboratories</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md (lines 287–300, Phase 174 section including goal, depends_on, requirements, and success criteria)</read_first>
  <action>
In ROADMAP.md Phase 174 section, replace all occurrences of `axiom/mop_validation` with `axiom-laboratories/mop_validation`.

Specific lines to update (confirm via grep first):
- Line 288: `**Goal**: Transfer \`mop_validation\` to the \`axiom\` GitHub organisation...` → replace `axiom` with `axiom-laboratories`
- Line 292: `1. \`mop_validation\` is accessible at \`github.com/axiom/mop_validation\`...` → replace with `axiom-laboratories`
- Line 294: `3. Local git remote \`origin\` points to \`github.com/axiom/mop_validation\`` → replace with `axiom-laboratories`
- Line 298: `- [ ] 174-01: GitHub org transfer — initiate repo transfer to \`axiom\` org...` → replace `axiom` with `axiom-laboratories`

Use a search-replace approach (e.g., sed or direct file edit) to change all four instances from `axiom` to `axiom-laboratories` within the Phase 174 section only.

Command (safe, uses -i.bak to create backup):
```bash
cd ~/.planning && sed -i.bak \
  's|github.com/axiom/mop_validation|github.com/axiom-laboratories/mop_validation|g' ROADMAP.md && \
  sed -i '' \
  's|to the \`axiom\` GitHub|to the \`axiom-laboratories\` GitHub|g' ROADMAP.md && \
  sed -i '' \
  's|transfer to \`axiom\` org|transfer to \`axiom-laboratories\` org|g' ROADMAP.md
```

Or use the Edit tool to manually replace each line.
  </action>
  <verify>
Run `grep "axiom/mop_validation" .planning/ROADMAP.md` — should return 0 matches (the old "axiom" org name is gone).
Run `grep "axiom-laboratories/mop_validation" .planning/ROADMAP.md` — should return at least 3 matches (lines 292, 294).
Run `grep "axiom-laboratories" .planning/ROADMAP.md` — should return matches for org name corrections in Phase 174.
  </verify>
  <acceptance_criteria>
- `grep "axiom/mop_validation" .planning/ROADMAP.md` returns 0 matches (all old references removed)
- `grep "axiom-laboratories/mop_validation" .planning/ROADMAP.md` returns at least 3 matches
- `grep "axiom-laboratories\` GitHub" .planning/ROADMAP.md` returns at least 1 match (the goal text)
- No other instances of bare `axiom` (without `-laboratories`) in Phase 174 section regarding mop_validation
  </acceptance_criteria>
  <done>ROADMAP.md Phase 174 naming corrected from axiom to axiom-laboratories throughout.</done>
</task>

<task type="auto">
  <name>Task 4: Final verification — no stale axiom references for mop_validation in master_of_puppets</name>
  <files></files>
  <read_first></read_first>
  <action>
Do a final sweep to confirm no remaining `axiom/mop_validation` references exist in the three updated files:

```bash
cd /home/thomas/Development/master_of_puppets

# Check for stale axiom/ (non-laboratories) references to mop_validation
grep "axiom/mop_validation" CLAUDE.md GEMINI.md .planning/ROADMAP.md 2>/dev/null || echo "✓ No stale axiom/ references found"

# Confirm axiom-laboratories references exist in all three
echo "Checking axiom-laboratories references..."
grep -l "axiom-laboratories" CLAUDE.md GEMINI.md .planning/ROADMAP.md
```

This is a final guard to catch any missed updates before commit.
  </action>
  <verify>
`grep "axiom/mop_validation" [files]` returns 0 matches (exit code 1 with no output, or explicit "No matches").
`grep -l "axiom-laboratories" CLAUDE.md GEMINI.md .planning/ROADMAP.md` returns all three filenames.
  </verify>
  <acceptance_criteria>
All three files are listed in the output of the second grep command. First grep (stale axiom/) finds no matches. No file contains both `axiom/` and `mop_validation` on the same line.
  </acceptance_criteria>
  <done>All stale references removed; all new references present and consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source files ↔ Updated docs | Docs must reflect actual repo location after migration |
| Multiple files | All three files must reference same URL (consistency) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-04 | Tampering | ROADMAP.md org name | Mitigate | Grep for stale "axiom/" references; verify only "axiom-laboratories" appears; double-check Phase 174 section |
| T-174-05 | Tampering | CLAUDE.md/GEMINI.md URLs | Mitigate | Grep for "axiom-laboratories" in both files post-update; verify exact spelling (hyphenated, no spaces) |
| T-174-06 | Omission | Partial updates (one file updated, others missed) | Mitigate | Task 4 verifies all three files have correct reference; grep sweep confirms consistency |

</threat_model>

<verification>
## Phase 174-02 Verification

1. **CLAUDE.md updated**: Contains GitHub URL for axiom-laboratories/mop_validation in Sister Repositories section
2. **GEMINI.md updated**: Contains GitHub URL for axiom-laboratories/mop_validation in Validation Repo section
3. **ROADMAP.md fixed**: All "axiom/mop_validation" references changed to "axiom-laboratories/mop_validation"; Phase 174 goal/criteria use correct org name
4. **Consistency check**: All three files reference the same URL (axiom-laboratories, not axiom)
5. **No stale references**: grep for "axiom/mop_validation" returns 0 matches across all three files

</verification>

<success_criteria>
- [x] MIG-04: `CLAUDE.md` and `GEMINI.md` updated with new GitHub URL for mop_validation
- [x] MIG-01 (carried forward): axiom-laboratories org reference is correct throughout
- [x] ROADMAP.md Phase 174 naming corrected (axiom → axiom-laboratories)
- [x] All references consistent (same spelling, same URL)
- [x] No incomplete updates (all three files updated, or none are incomplete)

</success_criteria>

<output>
After completion, create `.planning/phases/174-mop-validation-repo-migration/174-02-SUMMARY.md` documenting:
- Files updated (CLAUDE.md, GEMINI.md, ROADMAP.md)
- Line numbers where updates were made
- grep verification results (stale refs found: 0, correct refs found: count)
- Consistency check passed (all three files reference axiom-laboratories)
</output>
