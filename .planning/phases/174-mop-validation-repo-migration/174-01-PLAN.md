---
phase: 174-mop-validation-repo-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [MIG-01, MIG-02, MIG-03]
user_setup: []

must_haves:
  truths:
    - "mop_validation repo exists at axiom-laboratories org on GitHub"
    - "Local git remote origin points to axiom-laboratories URL"
    - "git fetch origin succeeds from new remote"
  artifacts:
    - path: "github.com/axiom-laboratories/mop_validation"
      provides: "Private repo housing"
      status: "exists on GitHub"
  key_links:
    - from: "~/Development/mop_validation/.git/config"
      to: "github.com/axiom-laboratories/mop_validation"
      via: "git remote origin"
      pattern: "axiom-laboratories/mop_validation"
---

<objective>
Transfer the `mop_validation` repository from `github.com/Bambibanners/mop_validation` to `github.com/axiom-laboratories/mop_validation` (private); verify the transfer succeeds; update the local git remote to point to the new location.

Purpose: Consolidate Axiom tooling under a single GitHub organization (per D-01); maintain all script and CI functionality post-migration (per D-02/D-03).

Output: mop_validation accessible at new axiom-laboratories org; local clone's git remote updated; git fetch and push commands work without authentication issues.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Key Reference (per CONTEXT.md):**
- D-01: Target org is `axiom-laboratories`, NOT `axiom` — this is the locked decision
- D-02: Org already exists; user has admin rights; skip org creation
- D-03: Verification = git operations only (fetch/push); no script smoke tests needed
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

**Phase Context (174-CONTEXT.md):**
- Current remote: `https://github.com/Bambibanners/mop_validation.git`
- Target remote: `https://github.com/axiom-laboratories/mop_validation.git` (private)
- No hardcoded GitHub URLs in scripts — all use local paths
- No CI workflows (no `.github/` directory)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify current git remote state</name>
  <files></files>
  <read_first>~/Development/mop_validation/.git/config</read_first>
  <action>
In `~/Development/mop_validation`, run `git remote -v` to confirm the current origin is `https://github.com/Bambibanners/mop_validation.git`. Document the exact output. This baseline ensures we know what we're transferring.
  </action>
  <verify>
Command output confirms:
```
origin	https://github.com/Bambibanners/mop_validation.git (fetch)
origin	https://github.com/Bambibanners/mop_validation.git (push)
```
  </verify>
  <acceptance_criteria>
`git remote -v` output contains exactly `Bambibanners/mop_validation` (both fetch and push lines).
  </acceptance_criteria>
  <done>Baseline remote state documented; no changes made yet.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Initiate GitHub repo transfer (MANUAL — requires user)</name>
  <read_first></read_first>
  <what-built>N/A — this is a manual GitHub UI step. Claude cannot automate it without org-level API token (which requires GitHub personal access token stored in environment).</what-built>
  <how-to-verify>
**Option A (GitHub Web UI):**
1. Go to https://github.com/Bambibanners/mop_validation
2. Settings → Danger Zone → Transfer Ownership
3. Type `axiom-laboratories` in the "New owner" field
4. Confirm the transfer

**Option B (gh CLI — if you have `gh` installed and authenticated):**
Run this command from any directory:
```bash
gh api repos/Bambibanners/mop_validation/transfer \
  --method POST \
  -f new_owner=axiom-laboratories
```

Either method will trigger the transfer. GitHub will send a notification email.
  </how-to-verify>
  <resume-signal>
Type "transferred" once you confirm the repo is visible at `https://github.com/axiom-laboratories/mop_validation`. Do NOT proceed until the transfer is complete.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify repo transfer completed on GitHub</name>
  <files></files>
  <read_first></read_first>
  <action>
After the user confirms the transfer, verify it succeeded by running:
```bash
curl -I https://github.com/axiom-laboratories/mop_validation 2>/dev/null | head -1
```

This checks if the new URL is accessible. If you get a 200/301 response (not 404), the transfer completed successfully.

Alternatively, use:
```bash
gh repo view axiom-laboratories/mop_validation --json nameWithOwner
```
  </action>
  <verify>
HTTP response is 200 or 301 (repo found), not 404 (not found).
Or gh CLI output shows `nameWithOwner: "axiom-laboratories/mop_validation"`.
  </verify>
  <acceptance_criteria>
The new GitHub URL `https://github.com/axiom-laboratories/mop_validation` is publicly accessible (HTTP 200/301).
  </acceptance_criteria>
  <done>Transfer confirmed on GitHub side.</done>
</task>

<task type="auto">
  <name>Task 4: Update local git remote to axiom-laboratories URL</name>
  <files></files>
  <read_first>~/Development/mop_validation/.git/config</read_first>
  <action>
In the `~/Development/mop_validation` directory, update the git remote URL (per D-03, this is the only local change needed):

```bash
cd ~/Development/mop_validation
git remote set-url origin https://github.com/axiom-laboratories/mop_validation.git
```

This changes the remote URL from the old `Bambibanners` org to the new `axiom-laboratories` org. The local clone stays in `~/Development/mop_validation` — only the remote reference changes.
  </action>
  <verify>
Run `git remote -v` and confirm output shows:
```
origin	https://github.com/axiom-laboratories/mop_validation.git (fetch)
origin	https://github.com/axiom-laboratories/mop_validation.git (push)
```
Both fetch and push lines must show `axiom-laboratories/mop_validation`.
  </verify>
  <acceptance_criteria>
`git remote -v` shows both fetch and push origins pointing to `https://github.com/axiom-laboratories/mop_validation.git`.
  </acceptance_criteria>
  <done>Local git remote updated to point to axiom-laboratories org.</done>
</task>

<task type="auto">
  <name>Task 5: Verify git fetch succeeds from new remote (MIG-03 satisfaction)</name>
  <files></files>
  <read_first></read_first>
  <action>
Test that git can successfully fetch from the new remote, proving the local clone can communicate with the migrated repo:

```bash
cd ~/Development/mop_validation
git fetch origin
```

This command should complete without errors. If it fails with "fatal: could not read Username", the user may need to authenticate (see below).
  </action>
  <verify>
Command exits with code 0 (success). Output shows no errors, optionally includes "Already up to date" or similar.

If auth fails: User can authenticate via `git config --global user.email` + `git config --global user.name` or `gh auth login`.
  </verify>
  <acceptance_criteria>
`git fetch origin` completes with exit code 0. No authentication errors (unless user is not authenticated to GitHub, which is expected in that case — the executor should document the auth issue and let the user resolve it).
  </acceptance_criteria>
  <done>Git operations to the new remote verified working (or auth issue documented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local ↔ GitHub | Transfer target must be `axiom-laboratories`, not typo `axiom` or wrong org |
| Git remote config | Local `.git/config` must point to correct new URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01 | Tampering | GitHub repo transfer | Mitigate | Verify HTTP 200/301 on new URL before updating local remote; use `gh` CLI if available (authenticated API) instead of curl |
| T-174-02 | Tampering | Local `.git/config` | Mitigate | Document baseline state (Task 1); verify after update (Task 4) with grep on exact URL string |
| T-174-03 | Spoofing | axiom org name | Mitigate | Decision D-01 locks org name as `axiom-laboratories` (not `axiom`); all tasks reference this explicitly |

</threat_model>

<verification>
## Phase 174-01 Verification

1. **GitHub transfer succeeded**: `https://github.com/axiom-laboratories/mop_validation` returns HTTP 200/301
2. **Local remote updated**: `git remote -v` in `~/Development/mop_validation` shows `axiom-laboratories/mop_validation` on both lines
3. **Git fetch works**: `git fetch origin` exits 0 from `~/Development/mop_validation`
4. **No local file changes**: Only `.git/config` modified; no source code or script files touched

</verification>

<success_criteria>
- [x] MIG-01: `mop_validation` accessible at `github.com/axiom-laboratories/mop_validation` (verified by HTTP check or gh CLI)
- [x] MIG-02: Git operations work post-migration (`git fetch origin` succeeds)
- [x] MIG-03: Local remote `origin` updated to new URL (verified by `git remote -v`)
- [x] No authentication blockers (user may need to authenticate, but command runs without internal errors)

</success_criteria>

<output>
After completion, create `.planning/phases/174-mop-validation-repo-migration/174-01-SUMMARY.md` documenting:
- Baseline remote (Bambibanners URL)
- Transfer completion (axiom-laboratories URL reachable)
- Local remote update (git config updated)
- Git fetch verification (success or auth requirement)
</output>
