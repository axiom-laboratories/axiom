---
phase: 136-user-propagation-generated-images
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/services/foundry_service.py
autonomous: true
requirements:
  - CONT-08
must_haves:
  truths:
    - "Generated Dockerfile for DEBIAN base includes `RUN useradd --no-create-home appuser`"
    - "Generated Dockerfile for ALPINE base includes `RUN adduser -D appuser`"
    - "Generated Dockerfile includes `RUN chown -R appuser:appuser /app` before `USER appuser`"
    - "Generated Dockerfile includes `USER appuser` before `CMD`"
    - "WINDOWS OS family templates do NOT receive user injection"
  artifacts:
    - path: "puppeteer/agent_service/services/foundry_service.py"
      provides: "User injection in build_template() for DEBIAN/ALPINE; skip WINDOWS"
      key_lines: "User creation after FROM; chown + USER before CMD"
    - path: "tests/test_foundry.py"
      provides: "Unit tests for user injection logic (Wave 0 prerequisite)"
      contains: "test_debian_user_injection, test_alpine_user_injection, test_windows_skip_user_injection, test_chown_user_placement"
  key_links:
    - from: "foundry_service.py build_template()"
      to: "dockerfile list"
      via: "User creation append after FROM (line ~207); chown+USER append before CMD (line ~295)"
      pattern: "if os_family in \\(\"DEBIAN\", \"ALPINE\"\\)"
---

<objective>
Extend non-root user execution from Phase 132 base images to Foundry-generated Dockerfiles. Generated node images must run as appuser (UID 1000), with correct file ownership and security isolation.

Purpose: Close security gap for custom images built by Foundry; align all nodes (base + generated) on non-root execution.

Output: `foundry_service.py` with OS-family-aware user injection; test suite validating injection logic.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v22.0-ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase Context
@.planning/phases/136-user-propagation-generated-images/136-CONTEXT.md
@.planning/phases/136-user-propagation-generated-images/136-RESEARCH.md

# Relevant Source
@puppeteer/agent_service/services/foundry_service.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Wave 0: Create test stub file for user injection tests</name>
  <files>tests/test_foundry.py</files>
  <action>
Create test file `tests/test_foundry.py` with pytest stubs (all tests marked `@pytest.mark.skip("Pending implementation")`) covering:
- `test_debian_user_injection()` — verify Dockerfile includes `RUN useradd --no-create-home appuser` when os_family == "DEBIAN"
- `test_alpine_user_injection()` — verify Dockerfile includes `RUN adduser -D appuser` when os_family == "ALPINE"
- `test_windows_skip_user_injection()` — verify Dockerfile does NOT include user creation lines when os_family == "WINDOWS"
- `test_chown_user_placement()` — verify `RUN chown -R appuser:appuser /app` appears before `USER appuser`
- `test_user_directive_placement()` — verify `USER appuser` appears immediately before `CMD`

Each test should accept a mock `PuppetTemplate`, `RuntimeBlueprint`, and `NetworkBlueprint` (from conftest.py if available, else inline). Tests will call `foundry_service.build_template()` and inspect the returned `dockerfile` list for expected lines.

Use pytest.raises and assertions; import `pytest` and conftest fixtures if they exist.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_foundry.py -v --collect-only 2>&1 | grep -c "test_.*_user_injection\|test_windows_skip\|test_chown_user_placement\|test_user_directive_placement"</automated>
  </verify>
  <done>tests/test_foundry.py exists with 5 skipped test stubs (all 5 collected by pytest); no test is marked as error or missing</done>
</task>

<task type="auto">
  <name>Task 1: Inject user creation in foundry_service.py build_template()</name>
  <files>puppeteer/agent_service/services/foundry_service.py</files>
  <action>
In `foundry_service.py` `build_template()` method (line 59–435):

**Step 1: Inject user creation immediately after FROM (line ~206)**
After the line `dockerfile = [f"FROM {base_image}"]`, add conditional user creation:
```python
if os_family in ("DEBIAN", "ALPINE"):
    if os_family == "ALPINE":
        dockerfile.append("RUN adduser -D appuser")
    elif os_family == "DEBIAN":
        dockerfile.append("RUN useradd --no-create-home appuser")
```

The user creation runs as root (first RUN step), does not interfere with package installs or mirror configuration that follow.

**Step 2: Verify os_family variable exists**
Confirm that `os_family` is already computed at line 89 from `rt_bp.os_family` or inferred from `base_os` string. Use this exact variable for the guard.

**Step 3: Inject chown + USER before CMD (line ~295)**
Just before the line that appends `CMD [...]`, add:
```python
if os_family in ("DEBIAN", "ALPINE"):
    dockerfile.append("RUN chown -R appuser:appuser /app")
    dockerfile.append("USER appuser")
```

This happens AFTER all COPY, RUN (install), and WORKDIR commands, as root, then switches execution context for CMD.

**Step 4: No changes needed elsewhere**
- Alpine post-processing block (lines 301–305 replacing `apk add` with `apk add --allow-untrusted`) is unaffected
- Smelt-check (line 411) already exists and will run inside the built image as appuser after USER directive
- OCI cache FROM rewrite (lines 307–318) is unaffected (no FROM rewriting of user creation lines)

**Implementation note:** Both the user creation guard and the chown+USER guard check `if os_family in ("DEBIAN", "ALPINE")` to ensure WINDOWS OS family is skipped entirely (per locked decision).
  </action>
  <verify>
    <automated>cd puppeteer && grep -n "RUN adduser -D appuser\|RUN useradd --no-create-home appuser\|RUN chown -R appuser:appuser /app\|USER appuser" agent_service/services/foundry_service.py | head -10</automated>
  </verify>
  <done>foundry_service.py contains user creation inject guards (adduser/useradd) and chown+USER guards; both guarded by `if os_family in ("DEBIAN", "ALPINE")`; Windows OS family is skipped</done>
</task>

<task type="auto">
  <name>Task 2: Implement user injection tests</name>
  <files>tests/test_foundry.py</files>
  <action>
Replace the `@pytest.mark.skip` markers with actual test implementations:

**test_debian_user_injection():**
- Mock a PuppetTemplate with os_family="DEBIAN"
- Call foundry_service.build_template(template, ...)
- Assert "RUN useradd --no-create-home appuser" in returned dockerfile list
- Assert "RUN adduser -D appuser" NOT in dockerfile

**test_alpine_user_injection():**
- Mock a PuppetTemplate with os_family="ALPINE"
- Call foundry_service.build_template(template, ...)
- Assert "RUN adduser -D appuser" in returned dockerfile list
- Assert "RUN useradd --no-create-home appuser" NOT in dockerfile

**test_windows_skip_user_injection():**
- Mock a PuppetTemplate with os_family="WINDOWS"
- Call foundry_service.build_template(template, ...)
- Assert "RUN useradd" NOT in dockerfile
- Assert "RUN adduser" NOT in dockerfile
- Assert "USER appuser" NOT in dockerfile

**test_chown_user_placement():**
- Mock a PuppetTemplate with os_family="DEBIAN"
- Call foundry_service.build_template(template, ...)
- Find indices of "RUN chown -R appuser:appuser /app" and "USER appuser" in dockerfile
- Assert chown line comes before USER line
- Assert both appear before the CMD line

**test_user_directive_placement():**
- Mock a PuppetTemplate with os_family="ALPINE"
- Call foundry_service.build_template(template, ...)
- Find index of "USER appuser" and "CMD [" in dockerfile
- Assert USER immediately precedes CMD (index difference == 1)

Use conftest.py fixtures if available for mock templates, else create inline fixtures. All tests should be independent and run in < 1 second each.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_foundry.py::test_debian_user_injection tests/test_foundry.py::test_alpine_user_injection tests/test_foundry.py::test_windows_skip_user_injection tests/test_foundry.py::test_chown_user_placement tests/test_foundry.py::test_user_directive_placement -v</automated>
  </verify>
  <done>All 5 tests pass; each asserts correct user injection (DEBIAN/ALPINE) or skipping (WINDOWS); chown+USER placement verified</done>
</task>

<task type="auto">
  <name>Task 3: Run full foundry test suite to verify no regressions</name>
  <files></files>
  <action>
Run full test suite scoped to foundry-related tests:
```bash
cd puppeteer && pytest tests/test_foundry.py -v --tb=short
```

If `tests/test_foundry.py` is a new file, also run:
```bash
cd puppeteer && pytest tests/ -v --tb=short -k "foundry or template"
```

Ensure all tests pass (new user injection tests + any existing foundry tests). If tests fail, inspect the error and adjust implementation or test fixtures.

This verifies no integration issues with existing smelt-check, Docker build logic, or OCI caching.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_foundry.py -v --tb=line 2>&1 | tail -5 | grep -q "passed\|PASSED"</automated>
  </verify>
  <done>Full foundry test suite passes; no failures or skips related to user injection; regression tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Verify user injection in actual Foundry workflow (manual Docker build)</name>
  <files></files>
  <action>
Run a manual Foundry template build to verify the generated Dockerfile contains user creation and USER directive:

1. Start the full Docker stack (if not running):
```bash
cd puppeteer && docker compose -f compose.server.yaml up -d
```

2. Create a test template via API or admin UI with:
   - OS family: DEBIAN (or ALPINE)
   - A simple runtime (Python)
   - A simple network blueprint

3. Trigger a build and inspect the generated Dockerfile:
```bash
docker compose -f compose.server.yaml logs agent 2>&1 | grep -A 10 "FROM.*python"
```
Or, inspect the temp build directory if it's mounted:
```bash
docker exec puppeteer-agent-1 find /tmp/puppet_build_* -name Dockerfile -exec cat {} \;
```

4. Verify the Dockerfile contains:
   - `RUN adduser -D appuser` (if ALPINE) or `RUN useradd --no-create-home appuser` (if DEBIAN) near the top
   - `RUN chown -R appuser:appuser /app` near the bottom
   - `USER appuser` just before `CMD`

5. (Optional) Build the image manually and verify it runs as appuser:
```bash
docker run --rm <image> id
```
Should output: `uid=1000(appuser) gid=1000(appuser) groups=1000(appuser)`
  </action>
  <verify>
    <automated>MISSING — Requires live Docker stack and manual template creation. Skip automated check; proceed to checkpoint:human-verify after Task 3 passes.</automated>
  </verify>
  <done>Generated Dockerfile inspected; contains correct user creation syntax (DEBIAN/ALPINE), chown, and USER directives in correct order; no errors in build logs</done>
</task>

</tasks>

<verification>
**Phase gate:** All tasks complete, unit tests green, integration verified.

1. ✅ Wave 0: test_foundry.py stubs created (5 tests collected)
2. ✅ Task 1: User creation inject logic added to foundry_service.py
3. ✅ Task 2: Unit tests implemented and pass
4. ✅ Task 3: Full foundry test suite passes (no regressions)
5. ✅ Task 4: Manual Docker build verification (optional but recommended)

**Requirement coverage:**
- CONT-08: Foundry-generated Dockerfiles append USER appuser → satisfied by Task 1 + Tasks 2–3 verification
</verification>

<success_criteria>
- [ ] tests/test_foundry.py exists with 5 passing tests covering DEBIAN, ALPINE, WINDOWS, and chown+USER placement
- [ ] foundry_service.py build_template() injects user creation (adduser/useradd) immediately after FROM
- [ ] foundry_service.py build_template() injects chown + USER before CMD
- [ ] Both injections guarded by `if os_family in ("DEBIAN", "ALPINE")` (WINDOWS skipped)
- [ ] Full pytest suite passes with no regressions
- [ ] Manual Docker build test shows generated Dockerfile with correct user directives
- [ ] Requirement CONT-08 satisfied: Custom node images built by Foundry run as uid 1000
</success_criteria>

<output>
After completion, create `.planning/phases/136-user-propagation-generated-images/136-01-SUMMARY.md`

Summary should include:
- User injection pattern: `adduser -D appuser` (Alpine) / `useradd --no-create-home appuser` (Debian)
- Placement: user creation after FROM; chown+USER before CMD
- WINDOWS guarded skip (no injection)
- Test coverage: 5 unit tests (DEBIAN, ALPINE, WINDOWS, chown placement, USER placement)
- Integration verified: full foundry test suite passes
- CONT-08 satisfied
</output>
