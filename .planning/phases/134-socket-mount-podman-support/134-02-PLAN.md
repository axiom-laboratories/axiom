---
phase: 134-socket-mount-podman-support
plan: 02
type: execute
wave: 2
depends_on: [134-01]
files_modified: [puppets/node-compose.yaml, puppets/node-compose.podman.yaml, puppets/environment_service/node.py, puppeteer/tests/test_node_compose.py]
autonomous: true
requirements: [CONT-02, CONT-09]
user_setup: []

must_haves:
  truths:
    - "node-compose.yaml removes privileged: true and mounts Docker socket with group_add"
    - "node-compose.yaml defines jobs_network bridge and joins node to both networks"
    - "node-compose.podman.yaml is new variant for rootless Podman with userns_mode: keep-id"
    - "Both compose files apply cap_drop: ALL + security_opt: no-new-privileges consistent with Phase 133"
    - "DOCKER_HOST and EXECUTION_MODE env vars are set explicitly in compose"
    - "node.py passes 'jobs_network' to runtime.py run() method as network_ref"
  artifacts:
    - path: "puppets/node-compose.yaml"
      provides: "Updated Docker node compose with socket mount, cap_drop, jobs_network"
      contains: "group_add:" and "/var/run/docker.sock" and "jobs_network:" and "cap_drop: ALL"
      min_lines: 40
    - path: "puppets/node-compose.podman.yaml"
      provides: "New Podman variant with userns_mode: keep-id and rootless socket"
      contains: "userns_mode: keep-id" and "PODMAN_SOCK" and "EXECUTION_MODE=podman" and "jobs_network:"
      min_lines: 40
    - path: "puppets/environment_service/node.py"
      provides: "Pass jobs_network to runtime.py when executing jobs"
      contains: "network_ref.*jobs_network" or "jobs_network"
    - path: "puppeteer/tests/test_node_compose.py"
      provides: "Compose file validation tests"
      exports: ["test_docker_compose_no_privileged", "test_podman_compose_valid", "test_both_have_jobs_network"]
  key_links:
    - from: "node-compose.yaml"
      to: "/var/run/docker.sock volume mount"
      via: "volumes: - /var/run/docker.sock:/var/run/docker.sock:rw"
      pattern: "/var/run/docker\\.sock:/var/run/docker\\.sock"
    - from: "node-compose.yaml"
      to: "jobs_network definition"
      via: "networks: section + node joins both puppeteer_default and jobs_network"
      pattern: "jobs_network:" and "- puppeteer_default" and "- jobs_network"
    - from: "node.py job execution"
      to: "runtime.py run() method"
      via: "pass network_ref='jobs_network' argument"
      pattern: "await.*runtime.*run\\(.*network_ref.*=.*jobs_network"
    - from: "node-compose.podman.yaml"
      to: "userns_mode: keep-id"
      via: "rootless Podman UID mapping"
      pattern: "userns_mode: keep-id"

---

<objective>
Update both Docker and Podman compose files to remove privileged mode, mount sockets, apply capability restrictions, and wire jobs_network. Update node.py to pass jobs_network to runtime. Add validation tests.

**Purpose:** Enable non-privileged job execution via socket mounts. Ship dual compose variants for Docker and rootless Podman deployments. Isolate jobs to dedicated network bridge.

**Output:** Updated node-compose.yaml, new node-compose.podman.yaml, updated node.py, and compose validation tests.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-socket-mount-podman-support/134-CONTEXT.md
@.planning/phases/134-socket-mount-podman-support/134-RESEARCH.md

**Locked decisions from CONTEXT.md:**
- Socket access: use `group_add: [999]` with inline comment about GID verification
- Podman variant: `userns_mode: keep-id` + `${PODMAN_SOCK:-/run/user/1000/podman/podman.sock}`
- Both files: `cap_drop: ALL` + `security_opt: no-new-privileges` (Phase 133 pattern)
- Node joins: both `puppeteer_default` (for orchestrator) and `jobs_network` (for job isolation)
- Sidecar: keep `network_mode: service:node` (shares node's namespace, unchanged)
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create compose validation test stubs</name>
  <files>puppeteer/tests/test_node_compose.py</files>
  <action>
    Create a new test file for compose file validation. This is Wave 0 — tests fail until compose files are updated.

    **puppeteer/tests/test_node_compose.py** tests:
    - Test 1: `node-compose.yaml` is valid YAML, passes `docker compose config` validation
    - Test 2: `node-compose.yaml` does NOT contain `privileged: true`
    - Test 3: `node-compose.yaml` contains socket mount: `/var/run/docker.sock:/var/run/docker.sock:rw`
    - Test 4: `node-compose.yaml` contains `cap_drop: ALL` on node service
    - Test 5: `node-compose.yaml` contains `security_opt: no-new-privileges:true`
    - Test 6: `node-compose.yaml` defines `jobs_network` bridge in networks section
    - Test 7: `node-compose.yaml` node service joins both `puppeteer_default` and `jobs_network`
    - Test 8: `node-compose.yaml` contains `group_add: [999]`
    - Test 9: `node-compose.yaml` contains `DOCKER_HOST=unix:///var/run/docker.sock` env var
    - Test 10: `node-compose.podman.yaml` is valid YAML and passes `docker compose config` validation
    - Test 11: `node-compose.podman.yaml` contains `userns_mode: keep-id`
    - Test 12: `node-compose.podman.yaml` contains Podman socket mount with `${PODMAN_SOCK:-...}` default
    - Test 13: `node-compose.podman.yaml` contains `EXECUTION_MODE=podman` env var
    - Test 14: `node-compose.podman.yaml` does NOT contain `group_add` (userns_mode handles it)
    - Test 15: `node-compose.podman.yaml` defines `jobs_network` bridge

    Use `yaml.safe_load()` to parse both files. Use subprocess to run `docker compose -f <file> config --quiet` for validation. Use string/regex checks for specific config lines.

    All tests should fail initially (files don't exist or lack these configurations).
  </action>
  <verify>
    <automated>cd puppeteer && python -m pytest tests/test_node_compose.py --collect-only -q</automated>
  </verify>
  <done>Test file exists with 15 failing tests. All tests are discoverable and runnable.</done>
</task>

<task type="auto">
  <name>Task 1: Update node-compose.yaml (Docker variant)</name>
  <files>puppets/node-compose.yaml</files>
  <action>
    Replace the entire `node-compose.yaml` file following the exact structure from RESEARCH.md. Key changes from current version:

    1. Remove `privileged: true` (line 19 in current file)
    2. Add `cap_drop` and `security_opt` (Phase 133 pattern):
       ```yaml
       cap_drop:
         - ALL
       security_opt:
         - no-new-privileges:true
       ```
    3. Add `group_add` with inline comment:
       ```yaml
       group_add:
         - "999"  # GID 999 is the docker group default — verify on your host with: getent group docker
       ```
    4. Mount Docker socket in volumes:
       ```yaml
       volumes:
         - ./secrets:/app/secrets
         - /var/run/docker.sock:/var/run/docker.sock:rw
       ```
    5. Add `DOCKER_HOST` env var to environment section:
       ```yaml
       - DOCKER_HOST=unix:///var/run/docker.sock
       ```
    6. Update `networks` section to include both `puppeteer_default` and `jobs_network`:
       ```yaml
       networks:
         puppeteer_default:
           external: true
         jobs_network:
           driver: bridge
       ```
    7. Update node service to join both networks:
       ```yaml
       networks:
         - puppeteer_default
         - jobs_network
       ```
    8. Keep sidecar unchanged: `network_mode: service:node`

    Preserve all existing environment variables (NODE_TAGS, JOB_IMAGE, AGENT_URL, JOIN_TOKEN, ROOT_CA_PATH, PYTHONUNBUFFERED). Preserve build context and image.

    Final file should be ~50 lines, valid YAML, with clear organization: networks, node service (with cap_drop/security_opt early), sidecar service.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_node_compose.py::test_docker_compose_config_valid -x -v</automated>
  </verify>
  <done>node-compose.yaml is valid, no privileged mode, socket mount present, cap_drop and security_opt applied, jobs_network defined, node joins both networks.</done>
</task>

<task type="auto">
  <name>Task 2: Create node-compose.podman.yaml (Podman variant)</name>
  <files>puppets/node-compose.podman.yaml</files>
  <action>
    Create a new file `puppets/node-compose.podman.yaml` for rootless Podman deployments. Use RESEARCH.md example as the template. Key differences from Docker variant:

    1. Use `userns_mode: keep-id` instead of `group_add` (rootless Podman UID mapping)
    2. Mount Podman socket with env var default:
       ```yaml
       volumes:
         - ./secrets:/app/secrets
         - ${PODMAN_SOCK:-/run/user/1000/podman/podman.sock}:/run/podman/podman.sock:rw
       ```
    3. Set `EXECUTION_MODE=podman` in environment (explicit override, not auto):
       ```yaml
       - EXECUTION_MODE=podman
       ```
    4. Do NOT set `DOCKER_HOST` (Podman uses different env var if needed)
    5. Apply same `cap_drop: ALL` + `security_opt: no-new-privileges:true`
    6. Define same `jobs_network` bridge
    7. Node joins both `puppeteer_default` and `jobs_network`
    8. Sidecar unchanged: `network_mode: service:node`

    Add inline comment before userns_mode explaining rootless mapping:
    ```yaml
    # Rootless Podman: keep-id maps UID 1000 inside container to host user running Podman
    userns_mode: keep-id
    ```

    Add comment in environment section:
    ```yaml
    # Podman socket path — override via PODMAN_SOCK env var if using non-standard location
    # Requires: systemctl --user enable podman.socket on host before deployment
    ```

    Final file should mirror Docker variant in structure, ~50 lines, valid YAML.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_node_compose.py -k "podman" -x -v</automated>
  </verify>
  <done>node-compose.podman.yaml exists, is valid, has userns_mode, Podman socket mount with env var default, EXECUTION_MODE=podman, jobs_network defined, node joins both networks.</done>
</task>

<task type="auto">
  <name>Task 3: Update node.py to pass jobs_network to runtime.py</name>
  <files>puppets/environment_service/node.py</files>
  <action>
    Locate the code in `node.py` that calls `runtime.py`'s `run()` method. This is typically in a job execution handler or task runner.

    Find the line that looks like:
    ```python
    result = await runtime.run(image=..., command=..., ...)
    ```

    Add `network_ref="jobs_network"` parameter:
    ```python
    result = await runtime.run(image=..., command=..., network_ref="jobs_network", ...)
    ```

    If there are multiple call sites (multiple places where jobs are executed), add the parameter to all of them. If the parameter is already present in the signature but not being passed, pass it explicitly.

    This wires the jobs_network bridge name from compose → node.py → runtime.py.

    Do NOT hardcode the network name in runtime.py (it should default to jobs_network but allow override). Pass it explicitly from node.py for clarity.
  </action>
  <verify>
    <automated>grep -n "network_ref.*jobs_network" puppets/environment_service/node.py</automated>
  </verify>
  <done>node.py passes network_ref="jobs_network" to all runtime.run() calls. Wiring complete.</done>
</task>

<task type="auto">
  <name>Task 4: Validate both compose files and run all tests</name>
  <files>puppeteer/tests/test_node_compose.py</files>
  <action>
    Run full compose validation suite:

    1. Validate Docker variant:
       ```bash
       docker compose -f puppets/node-compose.yaml config --quiet
       ```

    2. Validate Podman variant:
       ```bash
       docker compose -f puppets/node-compose.podman.yaml config --quiet
       ```

    3. Run all compose tests:
       ```bash
       cd puppeteer && pytest tests/test_node_compose.py -x -v
       ```

    All 15 tests should pass. No privileged mode, socket mounts present, cap_drop/security_opt applied, jobs_network defined, env vars set correctly.

    If any test fails, the task action above was incomplete. Debug and fix the relevant compose file.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_node_compose.py -x -v && docker compose -f puppets/node-compose.yaml config --quiet && docker compose -f puppets/node-compose.podman.yaml config --quiet</automated>
  </verify>
  <done>All 15 compose validation tests pass. Both files are valid YAML. docker compose config succeeds on both. No regressions.</done>
</task>

</tasks>

<verification>
Run full validation:
```
cd puppeteer && pytest tests/test_node_compose.py -x -v
docker compose -f puppets/node-compose.yaml config --quiet
docker compose -f puppets/node-compose.podman.yaml config --quiet
```

Verify network isolation:
- Check that job containers will spawn with `--network=jobs_network` (from Plan 01 wiring)
- Check that node container joins both `puppeteer_default` (for orchestrator) and `jobs_network` (for jobs)
- Check that sidecar uses `network_mode: service:node` (unchanged)
</verification>

<success_criteria>
- node-compose.yaml: valid, no privileged, socket mount, cap_drop, jobs_network, DOCKER_HOST set
- node-compose.podman.yaml: valid, userns_mode: keep-id, Podman socket mount, EXECUTION_MODE=podman, jobs_network
- node.py: passes network_ref="jobs_network" to runtime.py
- All 15 compose validation tests pass
- Both files pass `docker compose config` validation
- No regressions in node behavior
</success_criteria>

<output>
After completion, create `.planning/phases/134-socket-mount-podman-support/134-02-SUMMARY.md` with:
- Compose validation test results (15/15 passing)
- Differences between node-compose.yaml and node-compose.podman.yaml
- node.py changes (grep output showing network_ref wiring)
- Deployment instructions for operators (which compose file to use)
- Any blockers for Phase 135 (none expected)
</output>
