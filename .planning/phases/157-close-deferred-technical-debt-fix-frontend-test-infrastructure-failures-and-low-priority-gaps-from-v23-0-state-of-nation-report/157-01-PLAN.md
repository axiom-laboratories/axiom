---
phase: 157-close-deferred-technical-debt-fix-frontend-test-infrastructure-failures-and-low-priority-gaps-from-v23-0-state-of-nation-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [puppeteer/dashboard/src/views/__tests__/Workflows.test.tsx, puppeteer/dashboard/src/views/__tests__/WorkflowRunDetail.test.tsx, puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Workflows.test.tsx renders async workflow data without act() warnings"
    - "WorkflowRunDetail.test.tsx renders run details with DAG and status without act() warnings"
    - "Jobs.test.tsx checkbox column, select, and header-checkbox behaviors are tested (3 todos converted)"
    - "All 30 failures in Workflows + WorkflowRunDetail are resolved"
    - "All 3 todos in Jobs become real passing tests"
  artifacts:
    - path: "puppeteer/dashboard/src/views/__tests__/Workflows.test.tsx"
      provides: "Test suite for Workflows list view (async data, routing, pagination)"
      min_lines: 150
    - path: "puppeteer/dashboard/src/views/__tests__/WorkflowRunDetail.test.tsx"
      provides: "Test suite for WorkflowRunDetail view (DAG, step list, drawer, status updates)"
      min_lines: 200
    - path: "puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx"
      provides: "Test suite for Jobs list view (includes 3 todo-to-real conversions)"
      min_lines: 300
  key_links:
    - from: "Workflows.test.tsx"
      to: "src/views/Workflows.tsx"
      via: "render() component; mock authenticatedFetch for API"
      pattern: "waitFor.*getByRole.*toBeInTheDocument"
    - from: "WorkflowRunDetail.test.tsx"
      to: "src/views/WorkflowRunDetail.tsx"
      via: "render() component; mock useQuery, useWebSocket, DAGCanvas"
      pattern: "waitFor.*getByRole.*toBeInTheDocument"
    - from: "Jobs.test.tsx"
      to: "src/views/Jobs.tsx"
      via: "render() component; mock checkbox interactions"
      pattern: "getAllByRole.*checkbox"
---

<objective>
Rewrite three frontend test files to fix 30 failing tests and convert 3 todos to real passing tests using React Testing Library best practices (waitFor, scoped selectors, proper async patterns).

Purpose: Eliminate act() warnings and test infrastructure debt from Phase 155. Establish modern test patterns (waitFor, getByRole, within scoping) that prevent race conditions and selector collisions.

Output: Three rewritten test files with all tests passing, zero warnings, zero todos.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-close-deferred-technical-debt-fix-frontend-test-infrastructure-failures-and-low-priority-gaps-from-v23-0-state-of-nation-report/157-CONTEXT.md
@.planning/phases/157-close-deferred-technical-debt-fix-frontend-test-infrastructure-failures-and-low-priority-gaps-from-v23-0-state-of-nation-report/157-RESEARCH.md

# Test Infrastructure (already passing — use as reference)
@puppeteer/dashboard/src/views/__tests__/Schedule.test.tsx
@puppeteer/dashboard/src/views/__tests__/WorkflowDetail.test.tsx

# Existing failing test files (to be rewritten)
@puppeteer/dashboard/src/views/__tests__/Workflows.test.tsx
@puppeteer/dashboard/src/views/__tests__/WorkflowRunDetail.test.tsx
@puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx

# Vitest setup (already configured)
@puppeteer/dashboard/vitest.config.ts
@puppeteer/dashboard/src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Workflows.test.tsx with proper async patterns</name>
  <files>puppeteer/dashboard/src/views/__tests__/Workflows.test.tsx</files>
  <action>
Complete rewrite of Workflows.test.tsx using React Testing Library best practices:

1. **Replace getByText() with getByRole() / scoped queries:** The root cause of "multiple elements found" errors is that "Workflows" appears in sidebar + page heading. Use `screen.getByRole('heading', { name: 'Workflows' })` or scope with `within(document.querySelector('main')).getByText()`.

2. **Replace setTimeout(100) with waitFor():** All async state updates (data loading, navigation) must use `await waitFor(() => expect(...).toBeInTheDocument())` instead of blind sleep.

3. **Maintain existing test cases:** Do not add new tests or change the test intentions. Examples from current file:
   - renders workflow list with columns (name, steps, trigger, status)
   - displays empty state when no workflows
   - navigates to workflow detail on row click
   - handles loading and error states
   - pagination works
   - any other existing cases

4. **Auth mock pattern (from Phase 154):**
   ```typescript
   vi.spyOn(authModule, 'authenticatedFetch').mockResolvedValueOnce({
     ok: true,
     json: async () => ({ workflows: mockWorkflows })
   } as any);
   ```

5. **QueryClient setup:**
   ```typescript
   const queryClient = new QueryClient({
     defaultOptions: { queries: { retry: false } }
   });
   ```

6. **Render wrapper (with BrowserRouter, QueryClientProvider)** — wrap component in both providers for routing + data fetching.

7. **User interactions:** Use `userEvent` for realistic interactions (click, type), avoid `fireEvent` for state-dependent actions.

Expected result: ~150 lines of test code, ~15 test cases, all passing, zero act() warnings. Verify with `npm test -- Workflows.test.tsx --run`.
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm test -- Workflows.test.tsx --run 2>&1 | grep -E "PASS|FAIL|act\(\) warning" | head -20</automated>
  </verify>
  <done>Workflows.test.tsx passes all tests without act() warnings; test file follows React Testing Library best practices (waitFor, getByRole, scoped selectors); same test cases as original, just better patterns</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite WorkflowRunDetail.test.tsx with proper async patterns</name>
  <files>puppeteer/dashboard/src/views/__tests__/WorkflowRunDetail.test.tsx</files>
  <action>
Complete rewrite of WorkflowRunDetail.test.tsx using React Testing Library best practices:

1. **Replace setTimeout() with waitFor():** Same pattern as Task 1 — all async state (data load, step drawer open/close, WebSocket updates) must use `await waitFor(() => expect(...).toBeInTheDocument())`.

2. **Mock component dependencies:** WorkflowRunDetail depends on:
   - DAGCanvas (mocked as a simple container)
   - useWebSocket (mocked hook that returns event bus)
   - useQuery / React Query (mocked fetch responses)
   - useStepLogs (mocked hook)
   These can remain mocked IF the test's goal is to test WorkflowRunDetail's integration, not the child components.

3. **Scoped selectors:** Use `getByRole()` for headings, buttons, links. Use `within()` to scope away from duplicates (sidebar + main).

4. **Test cases to maintain (from current file):**
   - renders run header with status badge
   - displays run started/completed time/duration
   - shows DAG canvas
   - step list renders with status colors
   - clicking step opens drawer
   - drawer displays step logs
   - navigation breadcrumb works
   - WebSocket updates change status in real-time
   - loading and error states

5. **Auth mock pattern:** Same as Workflows (authenticatedFetch spy).

6. **QueryClient:** Same retry:false setup.

Expected result: ~200 lines of test code, ~20 test cases, all passing, zero act() warnings. Verify with `npm test -- WorkflowRunDetail.test.tsx --run`.
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm test -- WorkflowRunDetail.test.tsx --run 2>&1 | grep -E "PASS|FAIL|act\(\) warning" | head -20</automated>
  </verify>
  <done>WorkflowRunDetail.test.tsx passes all tests without act() warnings; test file follows React Testing Library best practices (waitFor, scoped selectors, proper async patterns); same test cases as original, just better patterns; DAG, step list, drawer, status updates verified</done>
</task>

<task type="auto">
  <name>Task 3: Convert 3 Jobs.test.tsx todos to real passing tests</name>
  <files>puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx</files>
  <action>
Convert the 3 `it.todo()` stubs in Jobs.test.tsx (lines 280-282 per CONTEXT.md) to real passing tests. These cover:
- Checkbox column renders in job table first column
- Clicking row checkbox activates bulk action bar
- Header checkbox selects all rows

Implementation:
1. **Inspect Jobs.tsx** to see how checkboxes are actually rendered:
   - Are they `<input type="checkbox">` with roles?
   - Are they in a specific column position?
   - What DOM structure?

2. **Test 1: Checkbox column renders**
   ```
   Mock jobs data with 2+ items
   Render Jobs component
   Use getAllByRole('checkbox') or getAllByLabelText(/select/)
   Expect count >= (jobs.length + 1) [rows + header checkbox]
   ```

3. **Test 2: Click row checkbox activates bulk action bar**
   ```
   Mock jobs data
   Render Jobs component
   Click one row's checkbox using userEvent
   Wait for bulk action bar to appear (getByText('Delete selected') or similar)
   Verify bar is visible
   ```

4. **Test 3: Header checkbox selects all**
   ```
   Mock jobs data with 3+ items
   Render Jobs component
   Click header checkbox using userEvent
   Wait for all row checkboxes to be checked
   Verify count of checked == jobs.length
   ```

Use same patterns as Tasks 1-2:
- Mock authenticatedFetch for initial data
- Use waitFor() for state updates (checkbox check state, bar appearance)
- Use getByRole('checkbox') or within() scoped queries
- Use QueryClient with retry:false

Expected result: ~100 lines of new test code, 3 tests total, all passing, no warnings. Total file size ~350-400 lines. Verify with `npm test -- Jobs.test.tsx --run`.
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm test -- Jobs.test.tsx --run 2>&1 | tail -5</automated>
  </verify>
  <done>3 it.todo() markers in Jobs.test.tsx converted to real passing tests; checkbox column renders, row select activates bulk bar, header select-all all verified; no remaining todos in file</done>
</task>

</tasks>

<verification>
**After all tasks complete:**

1. **Frontend test suite green:** `cd puppeteer/dashboard && npm test -- --run 2>&1 | tail -10`
   - Should show `PASS: all tests` or similar
   - Should show 0 failures, 0 todos
   - Should show no act() warnings

2. **Targeted verification for fixed files:**
   - `npm test -- Workflows.test.tsx --run` → ✓ PASS
   - `npm test -- WorkflowRunDetail.test.tsx --run` → ✓ PASS
   - `npm test -- Jobs.test.tsx --run` → ✓ PASS

3. **Build check:** `npm run build` and `npm run lint` pass with no TypeScript errors.

4. **Specific test counts:** Phase 157 Plan 01 should close exactly 30 failures (Workflows + WorkflowRunDetail) + 3 todos (Jobs) = 33 test fixes.

**Success gate:** `npm test -- --run` shows 461/461 passing (or close to it, with Plan 02 regressions completing the count).
</verification>

<success_criteria>
- [ ] Workflows.test.tsx passes all tests with zero act() warnings
- [ ] WorkflowRunDetail.test.tsx passes all tests with zero act() warnings
- [ ] Jobs.test.tsx has 0 remaining `it.todo()` markers; all 3 converted to real tests
- [ ] No `getByText()` used on shared strings ("Workflows", "Jobs", etc.) — replaced with scoped queries or getByRole()
- [ ] No `setTimeout()` in any test — all async patterns use `await waitFor()`
- [ ] TypeScript build passes: `npm run build`
- [ ] Lint passes: `npm run lint`
- [ ] Total passing: 461/461 frontend tests (after this plan + Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/157-close-deferred-technical-debt-fix-frontend-test-infrastructure-failures-and-low-priority-gaps-from-v23-0-state-of-nation-report/157-01-SUMMARY.md`
</output>
