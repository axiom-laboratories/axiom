---
phase: 138-hmac-keyed-boot-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/services/licence_service.py
  - puppeteer/tests/test_licence_service.py
  - puppeteer/agent_service/security.py
autonomous: true
requirements: [EE-02, EE-03]
must_haves:
  truths:
    - "New boot log entries are written with `hmac:` prefix and HMAC-SHA256 digest keyed on ENCRYPTION_KEY"
    - "Legacy SHA256 entries (no prefix) are accepted on read without re-verification"
    - "HMAC entries are verified on read using constant-time comparison"
    - "HMAC mismatch raises RuntimeError in EE licences (VALID, GRACE, EXPIRED)"
    - "HMAC mismatch logs warning in CE mode and continues (not blocking)"
    - "Boot log chain continuity is maintained across legacy→HMAC transition"
  artifacts:
    - path: "puppeteer/agent_service/services/licence_service.py"
      provides: "HMAC computation, verification, and entry type detection"
      exports: ["_compute_boot_hmac()", "_verify_boot_hmac()", "_parse_boot_log_entry()", "check_and_record_boot()"]
    - path: "puppeteer/tests/test_licence_service.py"
      provides: "6 unit tests for HMAC write, verify, mixed coexistence, and error handling"
      contains: ["test_hmac_entry_write", "test_hmac_verify_on_read", "test_legacy_sha256_silent_accept", "test_mixed_format_coexist", "test_hmac_mismatch_strict_ee", "test_hmac_mismatch_ce_lax"]
  key_links:
    - from: "licence_service.py"
      to: "security.py"
      via: "import ENCRYPTION_KEY"
      pattern: "from.*security import.*ENCRYPTION_KEY"
    - from: "licence_service.py"
      to: "licence_service.py"
      via: "check_and_record_boot() uses _parse_boot_log_entry() + _verify_boot_hmac()"
      pattern: "_parse_boot_log_entry|_verify_boot_hmac"
    - from: "test_licence_service.py"
      to: "licence_service.py"
      via: "test fixtures mock BOOT_LOG_PATH and patch ENCRYPTION_KEY"
      pattern: "patch.*BOOT_LOG_PATH|mock.*ENCRYPTION_KEY"
---

<objective>
Upgrade the boot log in `licence_service.py` from plain SHA256 hash chain to HMAC-SHA256 keyed on `ENCRYPTION_KEY`, while maintaining backward compatibility with existing SHA256 entries.

Purpose: Strengthen EE licence protection by making the boot log cryptographically bound to the encryption key. Legacy SHA256 entries coexist indefinitely with no forced migration.

Output: Updated licence_service.py with HMAC write path, entry type detection, verification on read, and mixed-format support. Unit tests covering all scenarios (HMAC write, legacy read, mixed coexistence, error handling in EE/CE modes).
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/PROJECT.md
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/STATE.md
@/home/thomas/Development/master_of_puppets/.planning/phases/138-hmac-keyed-boot-log/138-CONTEXT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/138-hmac-keyed-boot-log/138-RESEARCH.md

## Key Interfaces

From `puppeteer/agent_service/services/licence_service.py` (existing):
```python
class LicenceStatus(Enum):
    VALID = "valid"
    GRACE = "grace"
    EXPIRED = "expired"
    CE = "ce"

BOOT_LOG_PATH = Path("secrets/boot.log")

def _compute_hash(prev_hash_hex: str, iso_ts: str) -> str:
    """SHA256 chain: SHA256(prev_hash + iso_ts)"""

def check_and_record_boot(licence_status: LicenceStatus) -> bool:
    """Read last boot.log line, detect rollback, append new entry with timestamp."""
```

From `puppeteer/agent_service/security.py` (existing patterns):
```python
import hmac as _hmac
import hashlib

def compute_signature_hmac(key_bytes: bytes, signature_payload: str, ...) -> str:
    """HMAC-SHA256 with hexdigest()"""
    return _hmac.new(key_bytes, message, hashlib.sha256).hexdigest()

def verify_signature_hmac(key_bytes: bytes, stored_hmac: str, ...) -> bool:
    """Constant-time comparison with hmac.compare_digest()"""
    return _hmac.compare_digest(stored_hmac, expected)

ENCRYPTION_KEY = bytes  # Loaded from env via _load_or_generate_encryption_key()
```

Entry format (locked decision):
- New HMAC: `hmac:<64-hex-digest> <ISO8601-timestamp>`
- Legacy SHA256: `<64-hex-digest> <ISO8601-timestamp>` (no prefix)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write HMAC unit test infrastructure (Wave 0 test scaffolding)</name>
  <files>puppeteer/tests/test_licence_service.py</files>
  <behavior>
    - Test 1: `test_hmac_entry_write()` — new boot entries are written with `hmac:` prefix and HMAC-SHA256 digest
    - Test 2: `test_hmac_verify_on_read()` — HMAC entry on last line is verified on read; mismatch raises in EE mode (VALID, GRACE, EXPIRED)
    - Test 3: `test_hmac_mismatch_ce_lax()` — HMAC mismatch logs warning in CE mode (no raise)
    - Test 4: `test_legacy_sha256_silent_accept()` — legacy SHA256 entries (no `hmac:` prefix) are read silently without verification
    - Test 5: `test_legacy_warning_on_read()` — warning is logged once when last entry is legacy SHA256
    - Test 6: `test_mixed_format_coexist()` — boot log with both legacy SHA256 and new HMAC entries reads correctly; chain maintained
  </behavior>
  <action>
Create 6 test function stubs in `puppeteer/tests/test_licence_service.py` that follow the existing test structure (use tempfile.TemporaryDirectory, mock BOOT_LOG_PATH via patch, import ENCRYPTION_KEY from security.py). Each stub should:
1. Create a temporary boot.log
2. Construct test data (legacy SHA256 or HMAC entries as appropriate)
3. Call check_and_record_boot() with the test licence_status
4. Assert expected behavior (success, RuntimeError, warning logged, etc.)

Follow the pattern from existing tests (e.g., `test_clock_rollback_detection()` at line ~154) and use pytest.raises() for exception checking. Use mock.patch to mock BOOT_LOG_PATH and potentially caplog for warning assertions.

Placeholder implementations (just `pass`) are sufficient for Wave 0; implementation comes in Task 2.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_licence_service.py::test_hmac_entry_write tests/test_licence_service.py::test_hmac_verify_on_read tests/test_licence_service.py::test_hmac_mismatch_ce_lax tests/test_licence_service.py::test_legacy_sha256_silent_accept tests/test_licence_service.py::test_legacy_warning_on_read tests/test_licence_service.py::test_mixed_format_coexist -v 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | wc -l</automated>
  </verify>
  <done>All 6 test functions exist in test_licence_service.py with stubs (pass statement); can be imported and discovered by pytest without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HMAC boot log helpers and entry parsing</name>
  <files>puppeteer/agent_service/services/licence_service.py</files>
  <action>
Add three new private helper functions to licence_service.py (after _compute_hash and before check_and_record_boot):

1. `_compute_boot_hmac(key_bytes: bytes, iso_ts: str) -> str`
   - HMAC-SHA256 computation of ISO8601 timestamp only
   - Use: `hmac.new(key_bytes, iso_ts.encode("utf-8"), hashlib.sha256).hexdigest()`
   - Return: 64-character hex string
   - Follow the pattern from security.py:36–39

2. `_verify_boot_hmac(key_bytes: bytes, stored_hmac: str, iso_ts: str) -> bool`
   - Constant-time HMAC verification
   - Compute expected HMAC via _compute_boot_hmac()
   - Compare using `hmac.compare_digest(stored_hmac, expected)`
   - Return: bool (True if match)
   - Follow the pattern from security.py:42–45

3. `_parse_boot_log_entry(line: str) -> tuple[str, str, str]`
   - Parse a single boot log line and detect entry type
   - Return: (entry_type, digest_or_hmac, iso_ts)
   - entry_type: "hmac" or "sha256"
   - digest_or_hmac: the hex string (stripped of `hmac:` prefix if present)
   - iso_ts: the ISO8601 timestamp string
   - Handles both formats:
     * New: `hmac:<64-hex> <ISO8601>` → ("hmac", "<64-hex>", "<ISO8601>")
     * Legacy: `<64-hex> <ISO8601>` → ("sha256", "<64-hex>", "<ISO8601>")
   - Use simple `line.split(" ", 1)` + prefix check (no regex)

Ensure imports are present: `import hmac as _hmac` at module level (same as security.py pattern).

Import ENCRYPTION_KEY from security.py: `from .security import ENCRYPTION_KEY` at the top of licence_service.py.

All three functions are testable in isolation; Task 2 TDD tests exercise these helpers.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_licence_service.py::test_hmac_entry_write tests/test_licence_service.py::test_hmac_verify_on_read tests/test_licence_service.py::test_legacy_sha256_silent_accept -x -q</automated>
  </verify>
  <done>Three helper functions (_compute_boot_hmac, _verify_boot_hmac, _parse_boot_log_entry) are implemented in licence_service.py and importable. Unit tests for HMAC write, verify, and legacy read pass.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor check_and_record_boot() for mixed HMAC+legacy support</name>
  <files>puppeteer/agent_service/services/licence_service.py</files>
  <action>
Refactor the `check_and_record_boot(licence_status)` function to:

1. **Entry type detection on read:**
   - After reading the last line from boot.log, call `_parse_boot_log_entry(last_line)` to detect entry type
   - Extract: entry_type, stored_digest, iso_ts

2. **HMAC verification path (when last entry is "hmac"):**
   - Call `_verify_boot_hmac(ENCRYPTION_KEY, stored_digest, iso_ts)`
   - If verification fails:
     * Determine strict mode: `strict_hmac = licence_status != LicenceStatus.CE`
     * If strict (EE): raise RuntimeError("Boot log HMAC verification failed — possible tampering")
     * If lax (CE): log warning via logger.warning() and continue (don't block)

3. **Legacy entry read path (when last entry is "sha256"):**
   - Log warning once (use logger.warning) when last entry read is legacy SHA256
   - Continue without verification (no raise, no block)
   - Read the hex value as `prev_hash` for SHA256 chain continuity

4. **Write path (unchanged from current, but clarified):**
   - Compute new SHA256 chain hash: `SHA256(prev_hash + now_ts)` (use existing `_compute_hash()`)
   - Compute HMAC: `_compute_boot_hmac(ENCRYPTION_KEY, now_ts)`
   - Write line as: `f"hmac:{hmac_hex} {now_ts}"`
   - The stored line is the HMAC digest (self-contained); chain is computed separately for continuity

5. **Rollback detection:** Unchanged (if `last_ts > now_ts`, raise RuntimeError)

The rollback detection, chain computation, and file operations remain the same. Only the read and write paths change to support HMAC while preserving legacy entries.

Reference Task 2 test output and research doc for implementation patterns; follow existing error handling style (strict vs. lax based on licence_status).
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_licence_service.py::test_mixed_format_coexist tests/test_licence_service.py::test_hmac_mismatch_strict_ee tests/test_licence_service.py::test_hmac_mismatch_ce_lax tests/test_licence_service.py::test_legacy_warning_on_read -x -q</automated>
  </verify>
  <done>check_and_record_boot() now detects entry type, verifies HMAC on read, logs legacy warning, and writes new HMAC entries. All 4 integration tests pass: mixed format, HMAC mismatch (strict EE mode), HMAC mismatch (lax CE mode), legacy warning.</done>
</task>

<task type="auto">
  <name>Task 4: Verify existing test regression (backward compatibility)</name>
  <files>puppeteer/tests/test_licence_service.py</files>
  <action>
Run the full test_licence_service.py suite to confirm all existing tests still pass after the HMAC changes. This verifies:
- Rollback detection (test_clock_rollback_detection) still works
- Existing boot log operations (non-HMAC) still work
- No breakage in clock handling or licence status logic
- No import or initialization errors

The existing test suite should pass without modification; only the 6 new HMAC tests were added.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_licence_service.py -x -q</automated>
  </verify>
  <done>All existing tests in test_licence_service.py pass (no regression). Full suite (existing + 6 new HMAC tests) is green.</done>
</task>

</tasks>

<verification>
All tasks complete when:
- [ ] Task 1: 6 test stubs exist in test_licence_service.py (discoverable by pytest)
- [ ] Task 2: _compute_boot_hmac(), _verify_boot_hmac(), _parse_boot_log_entry() implemented; HMAC write/verify/legacy tests pass
- [ ] Task 3: check_and_record_boot() refactored for mixed format; integration tests pass (mixed coexistence, HMAC mismatch strict/lax, legacy warning)
- [ ] Task 4: Full test_licence_service.py suite passes (existing + new tests green)
- [ ] ENCRYPTION_KEY imported in licence_service.py (no import errors)
- [ ] Entry format validation: sample boot log written shows `hmac:<64-hex> <ISO8601>` for new entries
</verification>

<success_criteria>
- All 6 new HMAC unit tests pass
- All existing boot log tests pass (no regression)
- Full `cd puppeteer && pytest tests/test_licence_service.py -q` succeeds
- Requirements EE-02 (HMAC-SHA256 boot log) and EE-03 (backward-compatible SHA256 read) satisfied
- Mixed-format boot log (legacy + HMAC entries) is readable and verifiable
- Chain continuity maintained across legacy→HMAC transition
</success_criteria>

<output>
After completion, create `.planning/phases/138-hmac-keyed-boot-log/138-01-SUMMARY.md` with:
- Which 6 HMAC tests were added
- Implementation details (helper functions, entry format detection)
- Test results (full suite green)
- Any deferred work or known limitations
</output>
