---
phase: 103-windows-e2e-validation
plan: 04
type: execute
wave: 3
depends_on:
  - 103-03
files_modified:
  - /home/thomas/Development/mop_validation/reports/FRICTION-WIN-103.md
  - /home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md
autonomous: false
requirements:
  - WIN-06

must_haves:
  truths:
    - "Every BLOCKER friction point in FRICTION-WIN-103.md has a non-empty Fix applied field"
    - "The Windows golden path re-runs end-to-end on Dwight with zero BLOCKER findings"
    - "windows_e2e_synthesis.md exists with a READY verdict"
  artifacts:
    - path: "/home/thomas/Development/mop_validation/reports/FRICTION-WIN-103.md"
      provides: "Completed friction catalogue with all BLOCKERs fixed and documented"
      contains: "Fix applied:"
    - path: "/home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md"
      provides: "Phase 103 sign-off report from synthesise_friction.py"
      contains: "READY"
  key_links:
    - from: "FRICTION-WIN-103.md"
      to: "windows_e2e_synthesis.md"
      via: "synthesise_friction.py --files FRICTION-WIN-103.md"
      pattern: "synthesise_friction.*--files.*FRICTION-WIN-103"
---

<objective>
Fix every BLOCKER found during the Plan 03 validation run, iterate the Dwight golden path until it completes cleanly with zero BLOCKERs, then produce the phase sign-off synthesis report.

Purpose: WIN-06 — all friction catalogued and fixed. The phase is not complete until the Windows golden path runs end-to-end with zero BLOCKERs on Dwight.
Output: FRICTION-WIN-103.md with all BLOCKERs resolved, windows_e2e_synthesis.md with READY verdict.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-windows-e2e-validation/103-CONTEXT.md
@.planning/phases/103-windows-e2e-validation/103-03-SUMMARY.md

<interfaces>
<!-- Fix target patterns for Windows-specific blockers: -->
<!-- [Setup] or [Install Step N]  → docs/getting-started/install.md or compose.cold-start.yaml -->
<!-- [Login]                       → main.py must_change_password handling or windows docs -->
<!-- [Enroll-Node Step N]          → docs/getting-started/enroll-node.md Windows tab -->
<!-- [Job Dispatch]                → docs/getting-started/first-job.md Windows tab -->
<!-- [Signing]                     → first-job.md Windows signing tab -->
<!-- [Dashboard]                   → relevant React view in puppeteer/dashboard/src/views/ -->

<!-- After docs fixes: re-push docs to Dwight before re-running (run_windows_e2e.py does this) -->
<!-- After code fixes that affect Docker images: rebuild + push GHCR image before re-run -->

<!-- synthesise_friction.py --files usage (already patched in Phase 102): -->
<!-- python3 synthesise_friction.py --files FRICTION-WIN-103.md --output windows_e2e_synthesis.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all BLOCKER friction points</name>
  <files>/home/thomas/Development/mop_validation/reports/FRICTION-WIN-103.md</files>
  <action>
Read FRICTION-WIN-103.md and for each finding classified as BLOCKER, apply the fix.

**If Plan 03 produced "Verdict: PASS — no blockers":** Skip Task 1 entirely, go to Task 2.

**Fix strategy:**

1. Read each BLOCKER block: `### [Category] Title` + `**Why it happened:**`.

2. Determine fix target from category:
   - `[Setup]` or `[Install Step N]` → check `compose.cold-start.yaml` and `install.md`
   - `[Login]` → check `main.py` forced password change logic; if docs gap, fix `enroll-node.md` or `install.md`
   - `[Enroll-Node Step N]` → fix `enroll-node.md` Windows (PowerShell) tab
   - `[Job Dispatch]` or `[Signing]` → fix `first-job.md` Windows (PowerShell) tab
   - `[Dashboard]` → check the relevant React view

3. Apply the fix. For docs fixes, edit the markdown file. For code fixes, edit Python/TypeScript.

4. Update the FRICTION entry's `**Fix applied:**` field — describe exactly what changed.

5. **After code fixes that affect Docker images:** Rebuild and push the relevant GHCR image:
   ```bash
   cd /home/thomas/Development/master_of_puppets/puppeteer
   docker build -t ghcr.io/axiom-laboratories/axiom:latest -f Dockerfile .
   docker push ghcr.io/axiom-laboratories/axiom:latest
   ```

6. **After all fixes:** Run the backend test suite to confirm no regressions:
   ```bash
   cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/ -x -q
   ```
   For frontend fixes: `cd puppeteer/dashboard && npm run test`
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Every BLOCKER entry in FRICTION-WIN-103.md has a non-empty "Fix applied:" field (not "None — requires fix"). Backend test suite passes. All changed Docker images rebuilt and pushed to GHCR.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run Windows golden path and iterate until clean</name>
  <files>/home/thomas/Development/mop_validation/reports/FRICTION-WIN-103.md</files>
  <action>
Re-run the full Windows golden path using the Phase 103 orchestrator. This verifies the Task 1 fixes resolve all BLOCKERs.

```bash
cd /home/thomas/Development/mop_validation/scripts
python3 run_windows_e2e.py
```

The orchestrator re-pushes updated docs and artifacts to Dwight, resets the stack, and runs the full golden path again from scratch. This is intentional — Dwight must be in a clean state to simulate a true new-user experience.

**After the re-run:**
```bash
grep "Classification.*BLOCKER" /home/thomas/Development/mop_validation/reports/FRICTION-WIN-103.md
```

**If new BLOCKERs found:** Append new findings to FRICTION-WIN-103.md (do not overwrite — preserve history) and return to Task 1. Fix and re-run again.

**Loop until:** Zero BLOCKER findings. The FRICTION file will end with "Verdict: PASS — golden path completed end-to-end with no blockers."

**Keep all findings across iterations** — add new `### [Category]` blocks below existing ones to maintain a complete history of what was found and fixed.

When the golden path is clean, print:
```
PHASE 103 WINDOWS GOLDEN PATH: CLEAN
All BLOCKERs resolved. Ready for sign-off.
```
  </action>
  <verify>
    <automated>grep -c "Classification.*BLOCKER" /home/thomas/Development/mop_validation/reports/FRICTION-WIN-103.md 2>/dev/null | xargs -I{} sh -c 'test {} -eq 0 && echo "PASS: zero BLOCKERs" || echo "FAIL: {} BLOCKERs remain"'</automated>
  </verify>
  <done>Zero BLOCKER findings in FRICTION-WIN-103.md. The golden path ran end-to-end on Dwight with no documentation deviations or error stops.</done>
</task>

<task type="auto">
  <name>Task 3: Generate synthesis sign-off report</name>
  <files>/home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md</files>
  <action>
Produce the phase sign-off synthesis report using the patched synthesise_friction.py.

```bash
cd /home/thomas/Development/mop_validation/scripts
python3 synthesise_friction.py \
  --files FRICTION-WIN-103.md \
  --output /home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md
```

Verify the READY verdict:
```bash
grep "Verdict:" /home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md
```

If verdict is NOT READY: BLOCKERs remain in FRICTION-WIN-103.md. Return to Task 1.

If verdict is READY:
1. Print the executive summary to console.
2. Run the full test suite as the phase gate:
   ```bash
   cd /home/thomas/Development/master_of_puppets/puppeteer && pytest && cd dashboard && npm run test
   ```

The synthesis report is the phase deliverable and gate artifact for `/gsd:verify-work`.
  </action>
  <verify>
    <automated>grep "Verdict:" /home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md 2>/dev/null | grep -q "READY" && echo "PASS: READY verdict" || echo "FAIL: verdict not READY or file missing"</automated>
  </verify>
  <done>windows_e2e_synthesis.md exists with "Verdict: READY". Backend and frontend test suites pass. Phase 103 gate conditions met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase 103 sign-off</name>
  <files>/home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md</files>
  <action>Human reviews synthesis report READY verdict and confirms all Windows BLOCKERs are resolved.</action>
  <verify>
    <automated>grep -q 'READY' /home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>User types "approved" — Phase 103 sign-off confirmed.</done>
  <what-built>
    Complete Windows E2E validation: golden path ran clean on Dwight, all friction fixed, synthesis report produced with READY verdict. v18.0 milestone is now complete.
  </what-built>
  <how-to-verify>
    1. Check the synthesis verdict: `grep "Verdict:" /home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md`
    2. Review fixed friction: `cat /home/thomas/Development/mop_validation/reports/FRICTION-WIN-103.md`
    3. Confirm all BLOCKER entries have a "Fix applied:" section with real content (not "None").
    4. Run full test suite if not done: `cd /home/thomas/Development/master_of_puppets/puppeteer && pytest -q && cd dashboard && npm run test`
  </how-to-verify>
  <resume-signal>Type "approved" if the synthesis shows READY and all blockers are resolved, or describe any remaining concerns.</resume-signal>
</task>

</tasks>

<verification>
Phase 103 complete when:
1. `grep "Verdict:" /home/thomas/Development/mop_validation/reports/windows_e2e_synthesis.md` → "READY"
2. `grep -c "Classification.*BLOCKER" /home/thomas/Development/mop_validation/reports/FRICTION-WIN-103.md` → all have non-empty "Fix applied:"
3. `cd /home/thomas/Development/master_of_puppets/puppeteer && pytest -q` → all tests pass
4. `cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run test` → all tests pass
</verification>

<success_criteria>
- FRICTION-WIN-103.md catalogues all friction found across all Dwight iterations
- Every BLOCKER has been fixed and the fix is documented in the FRICTION file
- windows_e2e_synthesis.md has "Verdict: READY"
- Backend and frontend test suites pass with no regressions from any fixes applied
- Windows golden path ran clean (zero BLOCKERs) on a fresh Dwight stack as the final verification
- v18.0 milestone (Phase 101 + 102 + 103) can be marked complete
</success_criteria>

<output>
After completion, create `/home/thomas/Development/master_of_puppets/.planning/phases/103-windows-e2e-validation/103-04-SUMMARY.md`
</output>
