---
phase: 123-cgroup-detection-backend
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/main.py
autonomous: true
gap_closure: true
requirements: [CGRP-02]

must_haves:
  truths:
    - "NodeResponse exposes detected_cgroup_version for Phase 127 dashboard"
  artifacts:
    - path: "puppeteer/agent_service/main.py"
      provides: "list_nodes() endpoint returns detected_cgroup_version in response dict"
      pattern: '"detected_cgroup_version": n.detected_cgroup_version'
  key_links:
    - from: "puppeteer/agent_service/db.py Node table"
      to: "puppeteer/agent_service/main.py list_nodes() endpoint"
      via: "response dict construction at line 1748"
      pattern: '"detected_cgroup_version": n.detected_cgroup_version'
---

<objective>
Close the Phase 123 verification gap: expose detected_cgroup_version field in the /nodes API endpoint response so Phase 127 dashboard can consume cgroup version data.

Purpose: Phase 123 implementation stored detected_cgroup_version in the Node DB table and HeartbeatPayload, but the list_nodes() endpoint does not return it. This breaks the chain from database → API → dashboard consumer.

Output: Single-line addition to list_nodes() response dict to include detected_cgroup_version field. Phase 127 dashboard can now access cgroup version data via GET /nodes.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/123-cgroup-detection-backend/123-VERIFICATION.md

# Verification gap source
The VERIFICATION.md (lines 116-143) identifies the gap: NodeResponse model has detected_cgroup_version field (line 215 in models.py), Node DB table persists the field (verified), but list_nodes() endpoint at lines 1686-1756 in main.py does not return it in the response dict.

# Code context
File: puppeteer/agent_service/main.py
- list_nodes() endpoint: lines 1686-1756
- Response dict construction: lines 1730-1749
- Current last field: line 1748 "env_tag": n.env_tag
- Fix location: Insert "detected_cgroup_version": n.detected_cgroup_version after env_tag

# Existing pattern from Phase 123-01
NodeResponse model (models.py line 215) already includes detected_cgroup_version: Optional[str] = None
Node DB model (db.py line 151) already maps detected_cgroup_version: Mapped[Optional[str]]
HeartbeatPayload (models.py line 171) already includes detected_cgroup_version: Optional[str] = None
JobService.receive_heartbeat (job_service.py lines 947-948) already updates both node.detected_cgroup_version and node.cgroup_raw from heartbeat payload

All infrastructure is in place; only the endpoint response dict is missing the field.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add detected_cgroup_version to list_nodes() response dict</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
In the list_nodes() endpoint (lines 1686-1756), find the response dict construction around line 1748 where env_tag is added. Add a new line immediately after "env_tag": n.env_tag to include:

    "detected_cgroup_version": n.detected_cgroup_version,

This exposes the cgroup version field that is already stored in the Node DB table and included in the NodeResponse model. The field is Optional[str] (defaults to None for nodes that haven't reported yet), so it's safe to add without migration concerns.

Why this location: The list_nodes() endpoint builds a custom dict response with all relevant Node fields. The detected_cgroup_version field is part of the Node model and must be included in the response dict for consumers (specifically Phase 127 dashboard) to access it via the API.

Why this approach: Phase 123-01 already populated the database with detected_cgroup_version values from heartbeat payloads. The NodeResponse model already declares the field. The list_nodes() endpoint is the final link in the chain that delivers this data to consumers. Adding the field to the response dict completes the wiring.

Optional enhancement (not required for Phase 127): Also add "cgroup_raw": n.cgroup_raw if debugging raw detection info is desired. This is available in the Node model and can be useful for operator diagnostics.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets && grep -n '"detected_cgroup_version": n.detected_cgroup_version' puppeteer/agent_service/main.py && echo "Field added to response dict"</automated>
  </verify>
  <done>
The list_nodes() endpoint response dict includes "detected_cgroup_version": n.detected_cgroup_version. Verification grep confirms the field is present in the response construction. The API now exposes detected_cgroup_version to consumers (Dashboard Phase 127).
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Single-line fix to list_nodes() endpoint response dict to expose detected_cgroup_version field.</what-built>
  <how-to-verify>
1. Rebuild the Docker stack: `cd /home/thomas/Development/master_of_puppets/puppeteer && docker compose -f compose.server.yaml build agent && docker compose -f compose.server.yaml up -d`
2. Wait for the agent to be healthy: `docker compose -f compose.server.yaml ps` (agent should show "healthy" status)
3. Run a test API call: `curl -s https://localhost:8001/nodes -H "Authorization: Bearer <JWT_TOKEN>" | jq '.items[0] | keys' | grep -q detected_cgroup_version && echo "PASS: detected_cgroup_version in response" || echo "FAIL: field missing"`
   - If you don't have a JWT token, first login: `curl -s -X POST https://localhost:8001/auth/login -d "username=admin&password=<admin_password>" | jq -r '.access_token'`
   - Use that token in the Authorization header
4. Expected outcome: The /nodes endpoint response includes detected_cgroup_version field in each node object (value may be null for nodes that haven't reported yet, but field must be present)
5. Type "approved" if the field is present and the fix is complete, or describe any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
After Task 1 and checkpoint approval:
- Grep confirms "detected_cgroup_version": n.detected_cgroup_version is in main.py at the response dict location
- Docker rebuild confirms no syntax errors in main.py
- API test confirms /nodes endpoint returns the field in response
- The verification gap from VERIFICATION.md (truth: "NodeResponse exposes detected_cgroup_version for Phase 127 dashboard") is now satisfied
</verification>

<success_criteria>
- The /nodes API endpoint returns detected_cgroup_version in every node object in the response (value may be null for new nodes, but field must be present)
- Grep verification confirms the line is in main.py at the correct location (~line 1748 in response dict)
- No syntax errors or test failures introduced by the change
- Phase 127 dashboard can now consume detected_cgroup_version from GET /nodes response
</success_criteria>

<output>
After completion, create `.planning/phases/123-cgroup-detection-backend/123-04-SUMMARY.md` with:
- Gap addressed: "NodeResponse exposes detected_cgroup_version for Phase 127 dashboard"
- Change made: Single-line addition to list_nodes() response dict
- Result: Phase 123 verification gap closed; all 7/7 must-haves now satisfied
- Files modified: puppeteer/agent_service/main.py (1 line added at ~1748)
- Tests: No new tests required; existing Phase 123-01 tests remain passing
- Phase status: Ready for Phase 127 dashboard integration
</output>
