---
phase: 112-conda-mirror-mirror-admin-ui
plan: 02
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - puppeteer/agent_service/models.py
  - puppeteer/agent_service/routers/smelter_router.py
  - puppeteer/agent_service/services/mirror_service.py
  - puppeteer/agent_service/db.py
  - puppeteer/dashboard/src/views/Admin.tsx
  - puppeteer/dashboard/src/components/MirrorConfigCard.tsx
  - puppeteer/tests/test_smelter.py
  - puppeteer/dashboard/src/views/__tests__/Admin.test.tsx
autonomous: true
requirements: [MIRR-08]
user_setup: []

must_haves:
  truths:
    - "Admin can see a 'Mirrors' tab in the Admin page with 8 cards (one per ecosystem)"
    - "Each mirror card shows: URL field, live health badge (green/amber/red), and edit/save controls"
    - "Admin can edit mirror URLs and save changes via PUT /api/admin/mirror-config"
    - "Health badges update in real-time based on HTTP health checks to mirror URLs"
    - "Non-admin users can view mirror URLs but cannot edit them"
  artifacts:
    - path: "puppeteer/agent_service/models.py"
      provides: "Expanded MirrorConfigUpdate with all 8 ecosystem URL fields"
      exports: ["MirrorConfigUpdate", "MirrorConfigResponse"]
    - path: "puppeteer/agent_service/routers/smelter_router.py"
      provides: "GET/PUT /api/admin/mirror-config endpoints returning all ecosystem URLs"
      exports: ["GET /api/admin/mirror-config", "PUT /api/admin/mirror-config"]
    - path: "puppeteer/agent_service/db.py"
      provides: "Config table entries for all 8 mirror URLs with defaults"
      exports: ["Config model with mirror URL keys"]
    - path: "puppeteer/dashboard/src/views/Admin.tsx"
      provides: "New 'Mirrors' tab with 8 MirrorConfigCard components"
      exports: ["Admin view with Mirrors tab"]
    - path: "puppeteer/dashboard/src/components/MirrorConfigCard.tsx"
      provides: "Per-ecosystem mirror config card with URL field and health badge"
      exports: ["MirrorConfigCard component"]
  key_links:
    - from: "Admin.tsx Mirrors tab"
      to: "GET /api/admin/mirror-config"
      via: "useEffect fetch on mount"
      pattern: "fetch.*admin/mirror-config"
    - from: "MirrorConfigCard"
      to: "PUT /api/admin/mirror-config"
      via: "onBlur handler on URL input"
      pattern: "mutation.*saveConfig"
    - from: "Health badge"
      to: "HTTP health check"
      via: "useSystemHealth hook (existing)"
      pattern: "useSystemHealth.*mirrors_available"
---

<objective>
Implement unified Admin mirror configuration UI with 8 ecosystem cards and real-time health checks.

Purpose: Admin can view/edit mirror URLs for all ecosystems (PyPI, APT, apk, npm, NuGet, OCI Hub, OCI GHCR, Conda) and see live health status.

Output: Expanded MirrorConfigUpdate model, GET/PUT mirror-config endpoints, Admin Mirrors tab with 8 cards, MirrorConfigCard component, unit tests.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

## Existing Admin Pattern

From Phase 111 and prior:
- `Admin.tsx` uses Tabs component from shadcn/ui
- Existing tabs: Users, Roles, Licence, Audit Log, Certificate Management
- Each tab is a self-contained React component
- GET/PUT endpoints in smelter_router.py for admin configuration

## Mirror Config Model (Current)

From models.py:
```python
class MirrorConfigUpdate(BaseModel):
    pypi_mirror_url: Optional[str] = None
    apt_mirror_url: Optional[str] = None
```

Needs expansion to all 8 ecosystems (apk, npm, nuget, oci_hub, oci_ghcr, conda).

## Health Check Pattern

From Phase 109:
- `useSystemHealth` hook polls /api/system/health every 30s
- Returns `{ mirrors_available: bool }`
- Can be extended to support per-ecosystem health status
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test stub file for mirror config tests</name>
  <files>puppeteer/tests/test_smelter.py</files>
  <action>
Add test stubs to puppeteer/tests/test_smelter.py for Wave 0 (RED state TDD). Create empty test functions with docstrings:

- `test_get_mirror_config_all_ecosystems()` — verify GET returns all 8 URLs
- `test_put_mirror_config_updates_database()` — verify PUT updates Config DB
- `test_mirror_config_permission_check()` — verify non-admin cannot PUT
- `test_mirror_config_health_status()` — verify health_status dict in response

Do NOT implement yet — just define test signatures with pass stubs.

Also add stub to puppeteer/dashboard/src/views/__tests__/Admin.test.tsx:
- `test_admin_mirrors_tab_renders()` — Mirrors tab visible in Admin UI
- `test_mirror_card_shows_health_badge()` — verify MirrorConfigCard displays health status

Pattern: Use existing test structure (pytest for backend, vitest for frontend).
  </action>
  <verify>
    <automated>grep -n "def test_get_mirror_config_all_ecosystems\|def test_admin_mirrors_tab_renders" puppeteer/tests/test_smelter.py puppeteer/dashboard/src/views/__tests__/Admin.test.tsx</automated>
  </verify>
  <done>Test stub files exist with Wave 0 placeholder tests (RED state, not yet passing)</done>
</task>

<task type="auto">
  <name>Task 1: Expand MirrorConfigUpdate model with all 8 ecosystem URLs</name>
  <files>puppeteer/agent_service/models.py</files>
  <action>
Update MirrorConfigUpdate Pydantic model to include all 8 mirror URLs:

```python
class MirrorConfigUpdate(BaseModel):
    pypi_mirror_url: Optional[str] = None
    apt_mirror_url: Optional[str] = None
    apk_mirror_url: Optional[str] = None
    npm_mirror_url: Optional[str] = None
    nuget_mirror_url: Optional[str] = None
    oci_hub_mirror_url: Optional[str] = None
    oci_ghcr_mirror_url: Optional[str] = None
    conda_mirror_url: Optional[str] = None
```

Also add `MirrorConfigResponse` model (return type for GET):
```python
class MirrorConfigResponse(BaseModel):
    pypi_mirror_url: str
    apt_mirror_url: str
    apk_mirror_url: str
    npm_mirror_url: str
    nuget_mirror_url: str
    oci_hub_mirror_url: str
    oci_ghcr_mirror_url: str
    conda_mirror_url: str
    health_status: dict[str, str]  # { "pypi": "ok", "apt": "ok", ..., "conda": "ok" }
```

Validation: Each URL field (if provided in PUT) must be valid HTTP/HTTPS URL (use Pydantic HttpUrl or field validator).
  </action>
  <verify>
    <automated>cd puppeteer && python -c "from agent_service.models import MirrorConfigUpdate, MirrorConfigResponse; m = MirrorConfigUpdate(conda_mirror_url='http://mirror:8081/conda'); r = MirrorConfigResponse(pypi_mirror_url='http://mirror:8080', apt_mirror_url='http://mirror:8000', apk_mirror_url='http://mirror:8002', npm_mirror_url='http://mirror:8003', nuget_mirror_url='http://mirror:8004', oci_hub_mirror_url='http://mirror:8005', oci_ghcr_mirror_url='http://mirror:8006', conda_mirror_url='http://mirror:8081/conda', health_status={}); print('✓ Models expanded')"</automated>
  </verify>
  <done>MirrorConfigUpdate includes all 8 ecosystem URLs; MirrorConfigResponse includes all 8 URLs + health_status dict</done>
</task>

<task type="auto">
  <name>Task 2: Implement GET/PUT /api/admin/mirror-config endpoints</name>
  <files>puppeteer/agent_service/routers/smelter_router.py</files>
  <action>
Expand existing smelter_router.py GET/PUT mirror-config endpoints to handle all 8 ecosystems:

1. **GET /api/admin/mirror-config** (admin-only, requires users:write permission):
   - Query Config DB for each mirror URL key (PYPI_MIRROR_URL, APT_MIRROR_URL, ..., CONDA_MIRROR_URL)
   - Fall back to env vars if Config DB doesn't have entry (same pattern as existing get_mirror_config())
   - Fetch current health status from `app.state.mirror_health` dict (dict of ecosystem -> "ok" | "warn" | "error", updated by background health check)
   - Return MirrorConfigResponse with all URLs + health_status dict

2. **PUT /api/admin/mirror-config** (admin-only, requires admin role):
   - Accept MirrorConfigUpdate
   - For each non-None URL field, update Config DB table with key-value pair
   - Trigger health check immediately (don't wait for next scheduled check)
   - Return 200 with updated MirrorConfigResponse
   - On validation error, return 422 Unprocessable Entity with field error details

3. Permission pattern: Same admin-only check as existing endpoints (require admin role, not just users:write).

Pattern reference: Existing mirror-config endpoints in smelter_router.py — same structure, Config DB usage.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_smelter.py::test_get_mirror_config_all_ecosystems tests/test_smelter.py::test_put_mirror_config_updates_database tests/test_smelter.py::test_mirror_config_permission_check -xvs</automated>
  </verify>
  <done>GET/PUT /api/admin/mirror-config endpoints implemented with all 8 ecosystem URLs; health_status included in response; permission checks enforce admin-only access</done>
</task>

<task type="auto">
  <name>Task 3: Ensure Config DB entries for all 8 mirror URLs</name>
  <files>puppeteer/agent_service/db.py</files>
  <action>
Verify Config table has entries for all 8 mirror URL keys. If not present, add seed data or defaults:

1. In init_db() startup function, ensure these Config entries exist (create if not present):
   - PYPI_MIRROR_URL=http://mirror:8080/simple
   - APT_MIRROR_URL=http://mirror:8000
   - APK_MIRROR_URL=http://mirror:8002
   - NPM_MIRROR_URL=http://mirror:8003
   - NUGET_MIRROR_URL=http://mirror:8004
   - OCI_HUB_MIRROR_URL=http://mirror:8005
   - OCI_GHCR_MIRROR_URL=http://mirror:8006
   - CONDA_MIRROR_URL=http://mirror:8081/conda

2. Use ON CONFLICT DO NOTHING pattern (Postgres) or conditional create (SQLite) to avoid duplicates on restart.

3. Ensure Config model supports arbitrary key-value storage (already exists).

Pattern: Same as existing Config seeding for API_KEY, ENCRYPTION_KEY, etc.
  </action>
  <verify>
    <automated>cd puppeteer && python -c "from agent_service.db import init_db, Config; db = init_db(); configs = db.query(Config).filter(Config.key.like('%%MIRROR_URL')).all(); print(f'✓ Found {len(configs)} mirror URL configs'); assert len(configs) >= 8, f'Expected 8+ mirror configs, got {len(configs)}'"</automated>
  </verify>
  <done>Config table has seed entries for all 8 mirror URLs with reasonable defaults</done>
</task>

<task type="auto">
  <name>Task 4: Create Admin Mirrors tab with 8 ecosystem cards</name>
  <files>puppeteer/dashboard/src/views/Admin.tsx, puppeteer/dashboard/src/components/MirrorConfigCard.tsx</files>
  <action>
1. Update Admin.tsx to add new "Mirrors" tab:
   - Add import for MirrorConfigCard component
   - Extend TabsContent to include `value="mirrors"` section
   - Fetch mirror config on mount via GET /api/admin/mirror-config
   - Render 8 MirrorConfigCard components (one per ecosystem: pypi, apt, apk, npm, nuget, oci_hub, oci_ghcr, conda)
   - Load state: show spinner while fetching
   - Error state: show toast if GET fails

2. Create new MirrorConfigCard.tsx component (reusable per ecosystem):
   - Props: `{ ecosystem: string, url: string, healthStatus: "ok" | "warn" | "error", onUpdate: (newUrl: string) => void, canEdit: boolean }`
   - Layout: ecosystem name (h3), URL input field, health badge (green/amber/red circle + status text)
   - Input disabled if canEdit=false (for non-admins)
   - On URL input blur, call onUpdate() which triggers PUT /api/admin/mirror-config with the changed field
   - Show toast notification on success/error

3. Card styling:
   - Use existing shadcn/ui components: Card, Input, Button, Badge
   - Health badge: green dot "Healthy", amber dot "Unreachable", red dot "Error"
   - Use lucide-react icons (CheckCircle2 for green, AlertCircle for amber, XCircle for red)

4. Determine canEdit based on: is current user admin AND admin permission granted
   - Query from GET /api/admin/mirror-config response or separate endpoint check

Pattern: Follow existing Admin tab components (Users, Roles) — same styling, spacing, error handling.
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run build 2>&1 | grep -i "error" | head -20</automated>
  </verify>
  <done>Admin.tsx includes new Mirrors tab; MirrorConfigCard component renders 8 ecosystem cards with URL fields and health badges; non-admins see read-only UI</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Write comprehensive tests for Admin mirror config UI</name>
  <files>puppeteer/tests/test_smelter.py, puppeteer/dashboard/src/views/__tests__/Admin.test.tsx</files>
  <behavior>
    - Backend Test 1: GET /api/admin/mirror-config returns all 8 ecosystem URLs
    - Backend Test 2: GET /api/admin/mirror-config includes health_status dict with 8 keys
    - Backend Test 3: PUT /api/admin/mirror-config updates Config DB entries
    - Backend Test 4: Non-admin users cannot PUT /api/admin/mirror-config (return 403)
    - Frontend Test 1: Admin.tsx renders Mirrors tab with 8 MirrorConfigCards
    - Frontend Test 2: Each card shows ecosystem name, URL, and health badge
    - Frontend Test 3: URL input on blur triggers saveConfig mutation with PUT request
    - Frontend Test 4: Health badges show correct colors (green/amber/red) based on health_status
    - Frontend Test 5: Non-admin users see read-only URL fields (input disabled)
    - Frontend Test 6: Toast notification shows on successful config save
    - Frontend Test 7: Error toast shows if GET or PUT fails
  </behavior>
  <action>
Implement TDD RED→GREEN cycle:

RED phase: Write all test stubs (assertions only, function bodies are empty or use pass).

GREEN phase: Implement _mirror_conda(), endpoints, and components to pass all tests.

Backend tests: Mock Config DB queries, health check dict. Test permission checks (403 for non-admin).

Frontend tests: Render Admin tab, verify 8 cards render, simulate URL input blur, verify mutation calls, check input disabled state based on user role, verify toast notifications.

Use existing test infrastructure (pytest for backend with mocking, vitest for frontend with React Testing Library).
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_smelter.py -k mirror -xvs && cd dashboard && npm run test 2>&1 | grep -E "Admin|Mirror" | head -20</automated>
  </verify>
  <done>All 11 backend + frontend tests passing; permission checks, health badge rendering, and URL editing verified</done>
</task>

</tasks>

<verification>
Manual verification after all tasks complete:

1. **Admin access:** Log in as admin, navigate to Admin page, verify "Mirrors" tab appears
2. **Mirror cards:** Verify 8 ecosystem cards render (PyPI, APT, apk, npm, NuGet, OCI Hub, OCI GHCR, Conda)
3. **URL editing:** Click on a URL field, change value, blur — verify PUT request sent and toast shows success
4. **Health badges:** Verify health badges show correct colors (green if mirror healthy, amber if unreachable, red if error)
5. **Non-admin access:** Log in as operator, navigate to Admin page — verify Mirrors tab visible but URL fields are read-only
6. **Permission check:** Verify non-admins cannot PUT /api/admin/mirror-config (403 error)
</verification>

<success_criteria>
✅ Requirement MIRR-08 satisfied:
- Admin mirror configuration UI includes URL fields for all 8 ecosystems
- Health badges updated in real-time via HTTP health checks
- Non-admin users see read-only UI

✅ All backend + frontend tests passing (11/11)
✅ No TypeScript/linting errors in Admin.tsx and new components
✅ Permission checks enforced (admin-only)
✅ Error handling for validation and network issues
✅ UI responsive and accessible (proper input states, color contrast)
</success_criteria>

<output>
After completion, create `.planning/phases/112-conda-mirror-mirror-admin-ui/112-02-SUMMARY.md` with:
- Implementation summary (endpoints added, components created, files modified)
- Test results (passing count, coverage)
- Manual verification steps completed
- Key decisions made (Config DB for mirror URLs, health_status dict structure)
- Any deferred work
</output>
