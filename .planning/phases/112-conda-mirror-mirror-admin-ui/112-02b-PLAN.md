---
phase: 112-conda-mirror-mirror-admin-ui
plan: 02b
type: execute
wave: 2
depends_on: ["112-01", "112-02"]
files_modified:
  - puppeteer/agent_service/services/mirror_service.py
  - puppeteer/agent_service/routers/smelter_router.py
  - puppeteer/dashboard/src/components/MirrorProvisioner.tsx
  - puppeteer/dashboard/src/hooks/useDockerApi.ts
  - puppeteer/compose.server.yaml
  - puppeteer/compose.ee.yaml
  - puppeteer/.env.example
  - puppeteer/requirements.txt
  - puppeteer/tests/test_provisioning.py
autonomous: true
requirements: [MIRR-09]
user_setup:
  - service: docker_socket
    why: "Docker socket access for one-click provisioning (optional, gated by ALLOW_CONTAINER_MANAGEMENT)"
    env_vars:
      - name: ALLOW_CONTAINER_MANAGEMENT
        source: "Set to 'true' to enable Docker-based service provisioning (defaults false)"
    dashboard_config:
      - task: "Enable provisioning: set ALLOW_CONTAINER_MANAGEMENT=true in .env or secrets.env"
        location: "Agent environment startup"

must_haves:
  truths:
    - "Admin can toggle mirror services on/off when ALLOW_CONTAINER_MANAGEMENT is enabled"
    - "When ALLOW_CONTAINER_MANAGEMENT is false, toggles show read-only status with docker compose command hint"
    - "Docker socket API calls are only made when ALLOW_CONTAINER_MANAGEMENT is explicitly true"
    - "Provisioning toggles appear in mirror cards for PyPI, APT, apk, npm, NuGet, OCI Hub, OCI GHCR, Conda"
  artifacts:
    - path: "puppeteer/agent_service/services/mirror_service.py"
      provides: "ProvisioningService class for Docker container lifecycle management"
      exports: ["ProvisioningService with start_service, stop_service, get_service_status"]
    - path: "puppeteer/agent_service/routers/smelter_router.py"
      provides: "POST /api/admin/mirror-provision/{service} and GET /api/admin/mirror-provision/status endpoints"
      exports: ["POST /api/admin/mirror-provision", "GET /api/admin/mirror-provision/status"]
    - path: "puppeteer/dashboard/src/components/MirrorProvisioner.tsx"
      provides: "UI component for Docker-based service management (toggles or read-only status)"
      exports: ["MirrorProvisioner component"]
    - path: "puppeteer/dashboard/src/hooks/useDockerApi.ts"
      provides: "Hook for Docker API calls (start/stop mirror containers)"
      exports: ["useDockerApi"]
  key_links:
    - from: "MirrorProvisioner toggle"
      to: "POST /api/admin/mirror-provision/{service}"
      via: "useDockerApi hook"
      pattern: "POST.*provision.*mirror.*service"
    - from: "Provisioning UI"
      to: "ALLOW_CONTAINER_MANAGEMENT env var"
      via: "feature flag check in GET /api/admin/mirror-config response"
      pattern: "provisioning_enabled"
    - from: "Docker socket"
      to: "Mirror container lifecycle"
      via: "ProvisioningService docker-py client"
      pattern: "docker\\..*\\.(start|stop|create)"
---

<objective>
Implement Docker socket-based one-click service provisioning for mirror services, gated by ALLOW_CONTAINER_MANAGEMENT env var.

Purpose: Admin can optionally enable one-click provisioning to start/stop mirror services without manual Docker commands (when running in sandbox/VM).

Output: ProvisioningService class, POST/GET provisioning endpoints, MirrorProvisioner component, useDockerApi hook, unit tests.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

## Docker Provisioning Pattern

From Phase 111:
- Docker socket mounted at `/var/run/docker.sock`
- Foundry builds already use Docker socket via docker-py (version 7.1.0+)
- Same trust model applies: provisioning API requires admin role + ALLOW_CONTAINER_MANAGEMENT env var

## Service Provisioning Specifics

**Gating mechanism:** If ALLOW_CONTAINER_MANAGEMENT != "true", provisioning toggles show read-only status with manual docker compose command.

**Container configs (hardcoded):** For each ecosystem, store image name, ports, volumes, network. Examples:
- PyPI: `pypiserver:latest` (port 8080, volume: `/mirror-data/packages`)
- Conda: `mirror-sidecar:latest` (port 8081, volume: `/mirror-data/conda`)
- npm: `verdaccio:latest` (port 4873, volume: `/verdaccio/storage`)
- NuGet: `bagetter:latest` (port 5555, volume: `/data`)

**Auto-pull:** If container image not available locally, docker-py auto-pulls before create.

**Status indicators:** Running (green), Stopped (gray), Error (red) — fetched from docker.container.get().status.

## Existing Mirror Config Response

From Plan 02:
- GET /api/admin/mirror-config returns MirrorConfigResponse with all 8 URLs + health_status dict
- Need to add `provisioning_enabled: bool` field based on ALLOW_CONTAINER_MANAGEMENT env var
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test stub file for provisioning</name>
  <files>puppeteer/tests/test_provisioning.py</files>
  <action>
Create puppeteer/tests/test_provisioning.py test stubs (RED state TDD):

- `test_start_mirror_service()` — start Docker container for PyPI mirror
- `test_stop_mirror_service()` — stop mirror container
- `test_get_service_status()` — fetch container status
- `test_provisioning_auth_check()` — verify 403 if ALLOW_CONTAINER_MANAGEMENT != "true"
- `test_provisioning_admin_only()` — verify non-admin cannot call provisioning endpoint
- `test_service_image_auto_pull()` — verify docker-py pulls image if not available

Do NOT implement yet — just define test signatures with pass stubs.

Pattern: Use existing pytest structure (mocking Docker API calls).
  </action>
  <verify>
    <automated>grep -n "def test_start_mirror_service\|def test_provisioning_auth_check" puppeteer/tests/test_provisioning.py</automated>
  </verify>
  <done>Test stub file created with Wave 0 provisioning tests (RED state)</done>
</task>

<task type="auto">
  <name>Task 1: Implement ProvisioningService class in mirror_service.py</name>
  <files>puppeteer/agent_service/services/mirror_service.py, puppeteer/requirements.txt</files>
  <action>
Add new ProvisioningService class to mirror_service.py for Docker container lifecycle:

```python
class ProvisioningService:
    def __init__(self, docker_socket_path="/var/run/docker.sock"):
        self.client = docker.from_socket(docker_socket_path)  # docker-py
        self.services = {
            "pypi": { "image": "pypiserver:latest", "port": 8080, "volume": "/mirror-data/packages" },
            "apt": { "image": "nginx:latest", "port": 8000, "volume": "/mirror-data/apt" },
            "apk": { "image": "nginx:latest", "port": 8002, "volume": "/mirror-data/apk" },
            "npm": { "image": "verdaccio:latest", "port": 4873, "volume": "/verdaccio/storage" },
            "nuget": { "image": "bagetter:latest", "port": 5555, "volume": "/data" },
            "oci_hub": { "image": "nginx:latest", "port": 8005, "volume": "/mirror-data/oci" },
            "oci_ghcr": { "image": "nginx:latest", "port": 8006, "volume": "/mirror-data/ghcr" },
            "conda": { "image": "mirror-sidecar:latest", "port": 8081, "volume": "/mirror-data/conda" },
        }

    async def start_service(self, service_name: str) -> dict:
        # Pull image if not available
        # Create container if not exists
        # Start container if stopped
        # Return { "status": "running", "message": "Service started" }

    async def stop_service(self, service_name: str) -> dict:
        # Stop and remove container
        # Return { "status": "stopped", "message": "Service stopped" }

    async def get_service_status(self, service_name: str) -> str:
        # Return "running" | "stopped" | "error"

    async def get_all_statuses(self) -> dict:
        # Return { "pypi": "running", "conda": "stopped", ... }
```

Error handling:
- Docker socket unavailable → raise 500 Internal Server Error
- Container image not found → auto-pull via docker-py
- Invalid service name → raise 404 Not Found

Note: Ensure docker-py is in requirements.txt (already present for Foundry builds). If not, add `docker>=7.0.0`.
  </action>
  <verify>
    <automated>cd puppeteer && python -c "from agent_service.services.mirror_service import ProvisioningService; p = ProvisioningService(); assert 'pypi' in p.services; print('✓ ProvisioningService imported')" 2>&1 | head -20</automated>
  </verify>
  <done>ProvisioningService class implements Docker container start/stop/status with error handling; hardcoded service configs for all 8 ecosystems</done>
</task>

<task type="auto">
  <name>Task 2: Implement POST/GET provisioning endpoints in smelter_router.py</name>
  <files>puppeteer/agent_service/routers/smelter_router.py</files>
  <action>
Add new provisioning endpoints to smelter_router.py:

1. **POST /api/admin/mirror-provision/{service}** (admin-only, gated by ALLOW_CONTAINER_MANAGEMENT):
   - Accept { "action": "start" | "stop" }
   - Check if ALLOW_CONTAINER_MANAGEMENT env var == "true" — if not, return 403 with message "Provisioning disabled"
   - Call ProvisioningService.start_service() or stop_service()
   - Return 200 with { "status": "running|stopped", "message": "..." }
   - Return 404 if service_name not in supported list
   - Return 500 on Docker API error with helpful message

2. **GET /api/admin/mirror-provision/status** (admin-only):
   - Return status of all mirror containers: { "pypi": "running", "conda": "stopped", ... }
   - Fetch from Docker API; cache for 5s to avoid socket thrashing
   - Return 200 with all statuses

3. **Update GET /api/admin/mirror-config response** to include:
   - `provisioning_enabled: bool` field (True if ALLOW_CONTAINER_MANAGEMENT == "true", else False)
   - Frontend uses this to show/hide provisioning toggles

Pattern: Same admin-only check as existing endpoints (require admin role). Use try/except for Docker API calls.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_provisioning.py::test_provisioning_auth_check tests/test_provisioning.py::test_provisioning_admin_only -xvs</automated>
  </verify>
  <done>POST /api/admin/mirror-provision/{service} endpoint gated by env var and admin role; GET /api/admin/mirror-provision/status returns all service statuses; GET /api/admin/mirror-config includes provisioning_enabled flag</done>
</task>

<task type="auto">
  <name>Task 3: Add ALLOW_CONTAINER_MANAGEMENT env var to compose files</name>
  <files>puppeteer/compose.server.yaml, puppeteer/compose.ee.yaml, puppeteer/.env.example</files>
  <action>
1. Add ALLOW_CONTAINER_MANAGEMENT env var to both compose files:
   - compose.server.yaml: `ALLOW_CONTAINER_MANAGEMENT=false` (default, CE doesn't use provisioning)
   - compose.ee.yaml: `ALLOW_CONTAINER_MANAGEMENT=false` (default, EE admin can opt-in by overriding)

2. Update .env.example with comment:
```
# Docker socket-based service provisioning (gated by security setting)
# Set to 'true' only if running Axiom in a sandbox/VM with dedicated Docker socket
# When false, Admin dashboard shows manual docker compose commands instead of toggles
ALLOW_CONTAINER_MANAGEMENT=false
```

3. Add to agent service environment in both compose files:
   ```yaml
   environment:
     ALLOW_CONTAINER_MANAGEMENT: ${ALLOW_CONTAINER_MANAGEMENT:-false}
   ```

Note: Docker socket already mounted at `/var/run/docker.sock` in compose.server.yaml for Foundry builds. Provisioning reuses same mount.
  </action>
  <verify>
    <automated>grep -n "ALLOW_CONTAINER_MANAGEMENT" puppeteer/compose.server.yaml puppeteer/compose.ee.yaml puppeteer/.env.example | wc -l</automated>
  </verify>
  <done>ALLOW_CONTAINER_MANAGEMENT env var added to compose files (default false) and .env.example with documentation; added to agent service environment</done>
</task>

<task type="auto">
  <name>Task 4: Create MirrorProvisioner component and useDockerApi hook</name>
  <files>puppeteer/dashboard/src/components/MirrorProvisioner.tsx, puppeteer/dashboard/src/hooks/useDockerApi.ts</files>
  <action>
1. Create useDockerApi.ts custom hook:
   - Props: (serviceName: string)
   - Methods: startService(), stopService(), getStatus()
   - All call POST /api/admin/mirror-provision/{service} with { action: "start" | "stop" }
   - Handle 403 error → show banner: "Docker service provisioning disabled. Set ALLOW_CONTAINER_MANAGEMENT=true to use this feature."
   - Handle 500 error → show toast with error message
   - Auto-poll GET /api/admin/mirror-provision/status every 5s to update status
   - Return { startService, stopService, getStatus, status: "running"|"stopped"|"error" }

2. Create MirrorProvisioner.tsx component:
   - Props: `{ ecosystem: string, provisioning_enabled: bool }`
   - If provisioning_enabled is false:
     - Show read-only gray toggle (disabled) + gray text banner with docker compose command:
       ```
       docker compose -f compose.ee.yaml up -d {ecosystem}
       ```
   - If provisioning_enabled is true:
     - Show enabled toggle switch (Switch component from shadcn/ui)
     - On toggle, call useDockerApi.startService() or stopService()
     - Show loading spinner (Loader icon from lucide-react) while request in flight
     - Update status badge (running/stopped/error)

3. Error handling:
   - Network error → show toast "Failed to manage service"
   - 403 Forbidden → show banner "Provisioning disabled"
   - 500 Internal Server Error → show toast with error message from backend

Pattern: Follow existing components (Button, Switch from shadcn/ui). Use useQuery or useMutation pattern (react-query if available, or useState + effect).
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run build 2>&1 | grep -E "MirrorProvisioner|useDockerApi" | head -5</automated>
  </verify>
  <done>MirrorProvisioner component renders read-only mode (ALLOW_CONTAINER_MANAGEMENT=false) with docker compose hints, or interactive mode (enabled) with start/stop toggles; useDockerApi hook manages Docker API calls with error handling</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Write comprehensive tests for Docker provisioning</name>
  <files>puppeteer/tests/test_provisioning.py</files>
  <behavior>
    - Test 1: POST /api/admin/mirror-provision/pypi with action=start creates and starts container
    - Test 2: POST /api/admin/mirror-provision/pypi with action=stop stops container
    - Test 3: GET /api/admin/mirror-provision/status returns all 8 service statuses
    - Test 4: POST /api/admin/mirror-provision/{service} returns 403 if ALLOW_CONTAINER_MANAGEMENT != "true"
    - Test 5: POST /api/admin/mirror-provision/{service} returns 403 if user is not admin
    - Test 6: POST /api/admin/mirror-provision/{service} returns 404 if service_name invalid
    - Test 7: ProvisioningService auto-pulls Docker image if not available locally
    - Test 8: ProvisioningService returns helpful error message on Docker socket unavailable
    - Test 9: Status caching works (5s cache prevents socket thrashing)
    - Test 10: MirrorProvisioner renders read-only when provisioning_enabled=false
    - Test 11: MirrorProvisioner renders interactive toggle when provisioning_enabled=true
  </behavior>
  <action>
Implement TDD RED→GREEN cycle:

RED phase: Write all test stubs (assertions only, function bodies are empty or use pass).

GREEN phase: Implement ProvisioningService, endpoints, and components to pass all tests.

Backend tests: Mock docker-py client calls (create, start, stop, get). Test permission checks (403 for non-admin). Test env var gating.

Frontend tests: Render MirrorProvisioner component, verify toggle visibility based on provisioning_enabled flag, simulate toggle click, verify mutation calls.

Use existing test infrastructure (pytest with mocking, vitest for frontend).
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_provisioning.py -xvs && cd dashboard && npm run test 2>&1 | grep -E "MirrorProvisioner" | head -20</automated>
  </verify>
  <done>All 11 provisioning tests passing; env var gating, permission checks, Docker API mocking, and UI rendering verified</done>
</task>

</tasks>

<verification>
Manual verification after all tasks complete:

1. **Provisioning disabled (default):** Set ALLOW_CONTAINER_MANAGEMENT=false, navigate to Admin > Mirrors, verify provisioning toggles are read-only gray with docker compose command hint
2. **Provisioning enabled:** Set ALLOW_CONTAINER_MANAGEMENT=true, restart stack, verify provisioning toggles are interactive
3. **Start service:** Click toggle to start PyPI mirror, verify Docker container is created and started (check `docker ps`)
4. **Stop service:** Click toggle to stop service, verify container is stopped
5. **Status indicator:** Verify status badge updates (running/stopped/error)
6. **Non-admin access:** Log in as operator, verify provisioning toggles appear but are disabled
7. **Permission check:** Verify non-admins cannot call POST /api/admin/mirror-provision/* (403 error)
</verification>

<success_criteria>
✅ Requirement MIRR-09 satisfied:
- Docker socket-based provisioning available when ALLOW_CONTAINER_MANAGEMENT=true
- Provisioning safely disabled by default with helpful manual command hints
- Admin can start/stop mirror services via toggles (if enabled)

✅ All tests passing (11/11)
✅ No TypeScript/linting errors in components
✅ Permission checks enforced (admin-only, feature-gated by env var)
✅ Error handling for Docker API failures and network issues
✅ Status caching prevents socket thrashing
</success_criteria>

<output>
After completion, create `.planning/phases/112-conda-mirror-mirror-admin-ui/112-02b-SUMMARY.md` with:
- Implementation summary (ProvisioningService added, endpoints created, components built)
- Test results (passing count, coverage)
- Manual verification steps completed
- Key decisions made (env var gating, service configs, docker-py for API)
- Any deferred work
</output>
