---
phase: 112-conda-mirror-mirror-admin-ui
plan: 03
type: execute
wave: 3
depends_on: ["112-01", "112-02"]
files_modified:
  - puppeteer/dashboard/src/components/SmelterIngredientSelector.tsx
  - puppeteer/dashboard/src/components/CondaDefaultsToSModal.tsx
  - puppeteer/agent_service/routers/smelter_router.py
  - puppeteer/dashboard/src/views/__tests__/Smelter.test.tsx
autonomous: true
requirements: [MIRR-06]
user_setup: []

must_haves:
  truths:
    - "When operator selects Anaconda 'defaults' channel in Smelter, a blocking modal appears explaining the commercial ToS"
    - "Modal recommends conda-forge as the free alternative"
    - "Operator must acknowledge the ToS before proceeding with ingredient approval"
    - "Modal persists per-user — once acknowledged, operator won't see it again (until next session or cache clear)"
    - "Other users still see the modal on first encounter with defaults channel"
  artifacts:
    - path: "puppeteer/dashboard/src/components/CondaDefaultsToSModal.tsx"
      provides: "Blocking modal dialog for Conda defaults ToS acknowledgment"
      exports: ["CondaDefaultsToSModal component"]
    - path: "puppeteer/dashboard/src/components/SmelterIngredientSelector.tsx"
      provides: "Updated ingredient selector that triggers modal when defaults channel selected"
      exports: ["SmelterIngredientSelector component"]
    - path: "puppeteer/agent_service/routers/smelter_router.py"
      provides: "POST /api/admin/conda-defaults-acknowledge endpoint for ToS acknowledgment"
      exports: ["POST /api/admin/conda-defaults-acknowledge"]
  key_links:
    - from: "SmelterIngredientSelector (Conda channel dropdown)"
      to: "CondaDefaultsToSModal"
      via: "conditional rendering on channel selection"
      pattern: "if.*channel.*==.*defaults.*<CondaDefaultsToSModal"
    - from: "CondaDefaultsToSModal (Acknowledge button)"
      to: "POST /api/admin/conda-defaults-acknowledge"
      via: "mutation on button click"
      pattern: "mutation.*acknowledge.*POST.*conda-defaults"
    - from: "POST /api/admin/conda-defaults-acknowledge"
      to: "Config DB CONDA_DEFAULTS_TOS_ACKNOWLEDGED_BY_{user_id}"
      via: "server-side Config entry creation"
      pattern: "Config.create.*CONDA_DEFAULTS_TOS.*user_id"
    - from: "GET /api/admin/mirror-config"
      to: "CondaDefaultsToSModal visibility"
      via: "conda_defaults_acknowledged_by_current_user flag in response"
      pattern: "conda_defaults_acknowledged.*current_user"
---

<objective>
Implement blocking Conda defaults ToS modal in Smelter ingredient selector UI, preventing approval until operator acknowledges commercial terms.

Purpose: Ensure operators are aware of Anaconda's commercial Terms of Service for the defaults channel and encouraged to use the free conda-forge alternative.

Output: CondaDefaultsToSModal component, updated SmelterIngredientSelector, POST acknowledgment endpoint, unit tests.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-conda-mirror-mirror-admin-ui/112-CONTEXT.md

## Conda ToS Decision (from CONTEXT.md)

- Selecting the Anaconda `defaults` channel triggers a blocking modal dialog explaining Anaconda's commercial ToS
- Dialog recommends conda-forge as the free alternative; operator must acknowledge before proceeding
- Default channel for new Conda ingredients is conda-forge (pre-selected in UI)
- ToS acknowledgment persisted per-user — once acknowledged, that user won't see it again; other users still see it on first encounter

## Smelter UI Pattern

From existing Smelter.tsx:
- Ingredient selector with ecosystem dropdown, channel dropdown, version constraint field
- "Approve Ingredient" button submits the selection
- Modal/dialog pattern already established in codebase

## Config DB Pattern

From Plan 02:
- Config table stores per-user ToS acknowledgment: `CONDA_DEFAULTS_TOS_ACKNOWLEDGED_BY_{user_id}`
- Endpoint: POST /api/admin/conda-defaults-acknowledge accepts { "channel": "defaults" } (locked)
- GET /api/admin/mirror-config returns `conda_defaults_acknowledged_by_current_user: bool`
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test stub file for Conda ToS modal</name>
  <files>puppeteer/dashboard/src/views/__tests__/Smelter.test.tsx</files>
  <action>
Add test stubs to puppeteer/dashboard/src/views/__tests__/Smelter.test.tsx (RED state TDD):

- `test_conda_defaults_modal_appears_on_channel_selection()` — selecting "defaults" shows modal
- `test_conda_defaults_modal_blocks_approval()` — approval button disabled until acknowledged
- `test_conda_defaults_modal_hidden_after_acknowledgment()` — modal closes after "Acknowledge" button clicked
- `test_conda_defaults_acknowledged_per_user()` — different users see modal independently
- `test_conda_defaults_acknowledges_via_api()` — "Acknowledge" button calls POST /api/admin/conda-defaults-acknowledge

Do NOT implement yet — just define test signatures with pass stubs.

Pattern: Use existing test structure (vitest + React Testing Library).
  </action>
  <verify>
    <automated>grep -n "test_conda_defaults_modal_appears_on_channel_selection\|test_conda_defaults_modal_blocks_approval" puppeteer/dashboard/src/views/__tests__/Smelter.test.tsx</automated>
  </verify>
  <done>Test stub file created with Wave 0 Conda ToS modal tests (RED state)</done>
</task>

<task type="auto">
  <name>Task 1: Create CondaDefaultsToSModal component</name>
  <files>puppeteer/dashboard/src/components/CondaDefaultsToSModal.tsx</files>
  <action>
Create new CondaDefaultsToSModal.tsx component:

1. Component props:
   - `isOpen: bool` — whether modal is visible
   - `onAcknowledge: () => void` — callback when user clicks Acknowledge
   - `onCancel: () => void` — callback when user clicks Cancel/Close

2. UI layout:
   - Title: "Anaconda defaults Channel — Commercial Terms"
   - Body text explaining:
     - Anaconda's `defaults` channel requires a commercial license for organizations with 200+ employees
     - conda-forge is a free, community-maintained alternative
     - Operator can proceed with defaults only after acknowledging understanding
   - Recommendation: "We recommend conda-forge for most use cases"
   - Icon: warning icon (AlertTriangle from lucide-react)
   - Buttons:
     - "I Acknowledge" (primary, triggers onAcknowledge)
     - "Cancel" (secondary, triggers onCancel)

3. Styling:
   - Use Dialog component from shadcn/ui
   - Amber/yellow warning color for the icon (matches warning state)
   - Read-only text (not editable)

Pattern: Follow existing modal components in Admin.tsx (Dialog, Button from shadcn/ui).
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run build 2>&1 | grep -i "error.*CondaDefaultsToSModal\|CondaDefaultsToSModal.*error" | head -5</automated>
  </verify>
  <done>CondaDefaultsToSModal component renders Dialog with warning text, Acknowledge/Cancel buttons, and proper styling</done>
</task>

<task type="auto">
  <name>Task 2: Update SmelterIngredientSelector to trigger modal on defaults selection</name>
  <files>puppeteer/dashboard/src/components/SmelterIngredientSelector.tsx</files>
  <action>
Update SmelterIngredientSelector component to manage Conda defaults ToS modal:

1. Add state:
   - `selectedChannel: string` (track which channel is selected)
   - `showCondaDefaultsModal: bool` (show/hide modal)
   - `condaDefaultsAcknowledged: bool` (persisted per-user, from backend GET /api/admin/mirror-config)

2. On channel selection change:
   - If ecosystem == CONDA and channel == "defaults" and NOT condaDefaultsAcknowledged:
     - Set showCondaDefaultsModal = true
     - Prevent ingredient approval until acknowledged
   - If ecosystem == CONDA and channel == "defaults" and condaDefaultsAcknowledged:
     - Allow ingredient approval without modal (user already acknowledged)
   - If ecosystem != CONDA or channel != "defaults":
     - Hide modal, allow approval normally

3. Approval button:
   - Disabled if showCondaDefaultsModal is true
   - Enabled after user clicks "Acknowledge" in modal

4. Modal handlers:
   - onAcknowledge: call POST /api/admin/conda-defaults-acknowledge, update condaDefaultsAcknowledged state, hide modal, enable approval button
   - onCancel: hide modal, reset channel selection

5. Fetch condaDefaultsAcknowledged on component mount:
   - From GET /api/admin/mirror-config response field `conda_defaults_acknowledged_by_current_user`

Pattern: Follow existing ingredient selector UI logic. Use useMutation for API call.
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run build 2>&1 | grep -i "error" | grep -i "SmelterIngredientSelector" | head -5</automated>
  </verify>
  <done>SmelterIngredientSelector triggers modal on defaults channel selection; approval button disabled until acknowledged; modal hidden after acknowledgment</done>
</task>

<task type="auto">
  <name>Task 3: Implement POST /api/admin/conda-defaults-acknowledge endpoint</name>
  <files>puppeteer/agent_service/routers/smelter_router.py</files>
  <action>
Add POST /api/admin/conda-defaults-acknowledge endpoint to smelter_router.py (if not already in Plan 02):

1. Endpoint signature:
   - Route: POST /api/admin/conda-defaults-acknowledge
   - Auth: Requires any authenticated user (not admin-only — any operator can acknowledge)
   - Request body: { "channel": "defaults" } (locked value, validated on server)

2. Implementation:
   - Extract current user ID from JWT/session
   - Check if Config DB has entry `CONDA_DEFAULTS_TOS_ACKNOWLEDGED_BY_{user_id}`
   - If exists, return 200 { "acknowledged": true, "message": "Already acknowledged" }
   - If not exists, create Config entry with value="true"
   - Return 200 { "acknowledged": true, "message": "ToS acknowledged" }

3. Response type:
   - `{ "acknowledged": bool, "message": str }`

4. Error handling:
   - Invalid channel value → return 422 with "channel must be 'defaults'"
   - Database error → return 500 with helpful message

Pattern: Same Config DB pattern as Plan 02 mirror-config endpoints.
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_smelter.py::test_conda_defaults_acknowledge_endpoint -xvs</automated>
  </verify>
  <done>POST /api/admin/conda-defaults-acknowledge endpoint creates Config entry for current user; returns 200 with success message</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write comprehensive tests for Conda ToS modal UI and backend</name>
  <files>puppeteer/dashboard/src/views/__tests__/Smelter.test.tsx, puppeteer/tests/test_smelter.py</files>
  <behavior>
    - Frontend Test 1: Selecting Conda defaults channel shows CondaDefaultsToSModal (first time)
    - Frontend Test 2: Approval button disabled while modal is open
    - Frontend Test 3: Clicking "Acknowledge" button calls POST /api/admin/conda-defaults-acknowledge
    - Frontend Test 4: Modal closes after successful acknowledgment
    - Frontend Test 5: Approval button enabled after modal closes
    - Frontend Test 6: Selecting Conda defaults channel does NOT show modal on second encounter (after acknowledgment)
    - Frontend Test 7: Selecting different channel (conda-forge or other) does not show modal
    - Frontend Test 8: Different users see modal independently (mocked API responses)
    - Backend Test 1: POST /api/admin/conda-defaults-acknowledge creates Config entry for user
    - Backend Test 2: Subsequent calls to POST return 200 (idempotent)
    - Backend Test 3: Invalid channel value returns 422
    - Backend Test 4: GET /api/admin/mirror-config includes conda_defaults_acknowledged_by_current_user flag
  </behavior>
  <action>
Implement TDD RED→GREEN cycle:

RED phase: Write all test stubs (assertions only).

GREEN phase: Implement CondaDefaultsToSModal, SmelterIngredientSelector logic, and endpoint to pass all tests.

Frontend tests: Render SmelterIngredientSelector, select Conda defaults channel, verify modal appears, click Acknowledge, verify API call, verify modal closes.

Backend tests: Mock Config DB, verify entry created, test idempotency, test GET response includes flag.

Use existing test infrastructure (vitest for frontend, pytest for backend).
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm run test 2>&1 | grep -E "CondaDefaults|conda_defaults" | head -20</automated>
  </verify>
  <done>All 12 frontend + backend tests passing; modal rendering, acknowledgment flow, per-user persistence, and API integration verified</done>
</task>

</tasks>

<verification>
Manual verification after all tasks complete:

1. **Modal appears on defaults selection:** In Smelter UI, ecosystem = Conda, select channel = "defaults" — verify modal appears with warning text
2. **Approval button blocked:** With modal open, verify "Approve Ingredient" button is disabled
3. **Modal closes after acknowledge:** Click "I Acknowledge" button in modal — verify modal closes and "Approve Ingredient" button becomes enabled
4. **Next time no modal:** Select Conda defaults channel again — verify modal does NOT appear (user already acknowledged)
5. **Different user sees modal:** Log in as different operator, select defaults channel — verify modal appears (separate acknowledgment)
6. **Other channels work normally:** Select Conda conda-forge channel or any other ecosystem — verify no modal appears
7. **Cancel button works:** Open modal, click "Cancel" — verify modal closes without acknowledgment (approval still disabled)
</verification>

<success_criteria>
✅ Requirement MIRR-06 satisfied:
- Selecting Anaconda `defaults` channel shows blocking ToS modal (UI implemented)
- Modal explains commercial terms and recommends conda-forge
- Operator must acknowledge before proceeding
- Per-user persistence: once acknowledged, user won't see again (other users still see)

✅ All tests passing (12/12)
✅ No TypeScript/linting errors in Smelter and modal components
✅ Backend endpoint properly gates ToS acknowledgment
✅ GET /api/admin/mirror-config returns current user's acknowledgment status
✅ Error handling for invalid channel values and DB errors
</success_criteria>

<output>
After completion, create `.planning/phases/112-conda-mirror-mirror-admin-ui/112-03-SUMMARY.md` with:
- Implementation summary (CondaDefaultsToSModal component, SmelterIngredientSelector updates, endpoint added)
- Test results (passing count, coverage)
- Manual verification steps completed
- Key decisions made (Config DB for per-user tracking, modal blocking behavior)
- Screenshots of modal (if applicable)
- Any deferred work
</output>
