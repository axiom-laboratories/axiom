---
phase: 66-backend-code-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/ee/interfaces/executions.py
  - puppeteer/agent_service/ee/routers/executions_router.py
  - puppeteer/agent_service/ee/__init__.py
  - puppeteer/agent_service/main.py
  - puppeteer/agent_service/tests/test_ce_smoke.py
autonomous: true
requirements:
  - CODE-04
must_haves:
  truths:
    - "GET /api/executions returns HTTP 402 in CE mode (no EE plugin installed)"
    - "All 7 execution routes are CE-gated — none reachable without EE"
    - "test_ce_stub_routers_return_402 asserts all 7 execution stub handlers return 402"
    - "test_ce_features_all_false includes executions in the flags list"
    - "test_ce_table_count passes with the correct assertion of 15 CE tables"
    - "EE plugin can load executions_router.py to restore the real implementation"
  artifacts:
    - path: "puppeteer/agent_service/ee/interfaces/executions.py"
      provides: "execution_stub_router with 7 stub handlers returning 402"
      exports: ["execution_stub_router"]
    - path: "puppeteer/agent_service/ee/routers/executions_router.py"
      provides: "Real executions router (moved from main.py) for EE plugin use"
      exports: ["executions_router"]
    - path: "puppeteer/agent_service/ee/__init__.py"
      provides: "EEContext with executions flag; _mount_ce_stubs registers execution_stub_router"
      contains: "executions: bool = False"
    - path: "puppeteer/agent_service/main.py"
      provides: "All 7 execution routes removed"
    - path: "puppeteer/agent_service/tests/test_ce_smoke.py"
      provides: "Updated smoke tests covering executions CE-gating and correct table count (15)"
  key_links:
    - from: "puppeteer/agent_service/ee/__init__.py"
      to: "puppeteer/agent_service/ee/interfaces/executions.py"
      via: "_mount_ce_stubs() include_router call"
      pattern: "from .interfaces.executions import execution_stub_router"
    - from: "puppeteer/agent_service/tests/test_ce_smoke.py"
      to: "puppeteer/agent_service/ee/interfaces/executions.py"
      via: "direct handler import in test"
      pattern: "from agent_service.ee.interfaces.executions import"
---

<objective>
CE-gate all 7 execution-history API routes: remove them from main.py, create a CE stub router
that returns 402, move the real implementation to an EE router file, wire the stub into the CE
loader, and update test_ce_smoke.py to assert CE-gating. Also fix the pre-existing
test_ce_table_count failure (was asserting 13; actual count is 15).

Purpose: Closes CODE-04. Execution History is an EE feature; CE deployments must receive 402.
Output: ee/interfaces/executions.py, ee/routers/executions_router.py, updated ee/__init__.py,
main.py, and test_ce_smoke.py.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-backend-code-fixes/66-CONTEXT.md
@.planning/phases/66-backend-code-fixes/66-RESEARCH.md

<interfaces>
<!-- Pattern source: puppeteer/agent_service/ee/interfaces/foundry.py -->
<!--
from fastapi import APIRouter
from fastapi.responses import JSONResponse

foundry_stub_router = APIRouter(tags=["Foundry"])
_EE_RESPONSE = JSONResponse(
    status_code=402,
    content={"detail": "This feature requires Axiom Enterprise Edition. See https://axiom.run/enterprise"}
)

@foundry_stub_router.get("/api/blueprints")
async def blueprints_get(): return _EE_RESPONSE
# one handler per route, each with a unique function name
-->

<!-- Current EEContext in ee/__init__.py: -->
<!--
@dataclass
class EEContext:
    foundry: bool = False
    audit: bool = False
    webhooks: bool = False
    triggers: bool = False
    rbac: bool = False
    resource_limits: bool = False
    service_principals: bool = False
    api_keys: bool = False
-->

<!-- Current _mount_ce_stubs in ee/__init__.py: -->
<!--
def _mount_ce_stubs(app: Any) -> None:
    from .interfaces.foundry import foundry_stub_router
    from .interfaces.audit import audit_stub_router
    from .interfaces.webhooks import webhooks_stub_router
    from .interfaces.triggers import triggers_stub_router
    from .interfaces.auth_ext import auth_ext_stub_router
    from .interfaces.smelter import smelter_stub_router
    app.include_router(foundry_stub_router)
    ... (6 routers total)
    logger.info("CE mode: mounted 6 stub routers (402 for all EE routes)")
-->

<!-- 7 routes to remove from main.py (confirmed line numbers): -->
<!--
# --- Execution History --- block (lines 229-374):
  @app.get("/api/executions")               list_executions     (line 231)
  @app.get("/api/executions/{id}")          get_execution       (line 296)
  @app.get("/api/executions/{id}/attestation") get_execution_attestation (line 339)

# Per-job executions (line 1369-1432):
  @app.get("/jobs/{guid}/executions")       list_executions [DUPLICATE NAME] (line 1369)

# --- Execution Pin/Unpin --- block (lines 2272-2305):
  @app.patch("/api/executions/{exec_id}/pin")   pin_execution   (line 2274)
  @app.patch("/api/executions/{exec_id}/unpin") unpin_execution (line 2291)

# --- Per-job Execution CSV Export --- block (lines 2355-2397):
  @app.get("/api/jobs/{guid}/executions/export") export_job_executions (line 2357)
-->

<!-- EE router import pattern from ee/routers/foundry_router.py: -->
<!--
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.future import select
from ...db import get_db, AsyncSession, ExecutionRecord, Job
from ...deps import require_permission, require_auth, audit
from ...models import ExecutionRecordResponse, AttestationExportResponse
-->

<!-- CE table count reality (15 tables): -->
<!--
alerts, config, execution_records, job_templates, jobs, node_stats,
nodes, pings, revoked_certs, scheduled_fire_log, scheduled_jobs,
signals, signatures, tokens, users  ==> 15 total
test_ce_table_count currently asserts 13 (pre-existing failure)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CE stub and EE router for execution routes</name>
  <files>
    puppeteer/agent_service/ee/interfaces/executions.py,
    puppeteer/agent_service/ee/routers/executions_router.py
  </files>
  <behavior>
    - execution_stub_router is an APIRouter(tags=["Execution Records"])
    - list_executions_stub() returns JSONResponse(status_code=402)
    - get_execution_stub(id: int) returns JSONResponse(status_code=402)
    - get_execution_attestation_stub(id: int) returns JSONResponse(status_code=402)
    - list_job_executions_stub(guid: str) returns JSONResponse(status_code=402)
    - pin_execution_stub(exec_id: int) returns JSONResponse(status_code=402)
    - unpin_execution_stub(exec_id: int) returns JSONResponse(status_code=402)
    - export_job_executions_stub(guid: str) returns JSONResponse(status_code=402)
    - executions_router is an APIRouter() (no tags needed — real implementation)
  </behavior>
  <action>
Create two files.

FILE 1: `puppeteer/agent_service/ee/interfaces/executions.py`
Follow the exact pattern of ee/interfaces/foundry.py. Each handler must have a UNIQUE
function name (use _stub suffix). The _EE_RESPONSE is a module-level constant:

```python
from fastapi import APIRouter
from fastapi.responses import JSONResponse

execution_stub_router = APIRouter(tags=["Execution Records"])

_EE_RESPONSE = JSONResponse(
    status_code=402,
    content={"detail": "This feature requires Axiom Enterprise Edition. See https://axiom.run/enterprise"}
)


@execution_stub_router.get("/api/executions")
async def list_executions_stub(): return _EE_RESPONSE

@execution_stub_router.get("/api/executions/{id}")
async def get_execution_stub(id: int): return _EE_RESPONSE

@execution_stub_router.get("/api/executions/{id}/attestation")
async def get_execution_attestation_stub(id: int): return _EE_RESPONSE

@execution_stub_router.get("/jobs/{guid}/executions")
async def list_job_executions_stub(guid: str): return _EE_RESPONSE

@execution_stub_router.patch("/api/executions/{exec_id}/pin")
async def pin_execution_stub(exec_id: int): return _EE_RESPONSE

@execution_stub_router.patch("/api/executions/{exec_id}/unpin")
async def unpin_execution_stub(exec_id: int): return _EE_RESPONSE

@execution_stub_router.get("/api/jobs/{guid}/executions/export")
async def export_job_executions_stub(guid: str): return _EE_RESPONSE
```

FILE 2: `puppeteer/agent_service/ee/routers/executions_router.py`
Move the real implementations from main.py into this file. Use THREE-LEVEL relative imports
(ee/routers/ is 3 levels below the package root) — same pattern as ee/routers/foundry_router.py.
Use `from ...db import ...`, `from ...deps import ...`, `from ...models import ...`.
The router must import StreamingResponse, io, csv (needed by export_job_executions).
Name the main router object `executions_router = APIRouter()`.
Give the per-job executions handler a unique name (e.g., `list_job_executions` instead of
`list_executions`) to avoid the duplicate-name issue from main.py.

Key imports needed in executions_router.py:
- `from fastapi import APIRouter, Depends, Query`
- `from fastapi.responses import StreamingResponse`
- `import io, csv, json`
- `from typing import Optional, List`
- `from sqlalchemy.future import select`
- `from sqlalchemy import desc`
- `from ...db import get_db, AsyncSession, ExecutionRecord, Job, User`
- `from ...deps import require_auth`
- `from ...models import ExecutionRecordResponse, AttestationExportResponse`

The 7 handler implementations come directly from main.py (lines 231-374, 1369-1432,
2274-2291, 2291-2305, 2357-2397) — copy the function bodies verbatim, only changing:
  1. The decorator from `@app.get` to `@executions_router.get` etc.
  2. Absolute imports to three-level relative imports
  3. Duplicate function name `list_executions` (line 1369 version) to `list_job_executions`
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.ee.interfaces.executions import execution_stub_router, list_executions_stub, get_execution_stub, get_execution_attestation_stub, list_job_executions_stub, pin_execution_stub, unpin_execution_stub, export_job_executions_stub; print('stub imports OK')" && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.ee.routers.executions_router import executions_router; print('router imports OK')"</automated>
  </verify>
  <done>
    - ee/interfaces/executions.py exists with 7 stub handlers and execution_stub_router
    - ee/routers/executions_router.py exists with executions_router and 7 real handlers
    - Both files import cleanly with no errors
    - No duplicate function names in either file
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire stub into CE loader, remove routes from main.py, update smoke tests</name>
  <files>
    puppeteer/agent_service/ee/__init__.py,
    puppeteer/agent_service/main.py,
    puppeteer/agent_service/tests/test_ce_smoke.py
  </files>
  <behavior>
    - EEContext dataclass has executions: bool = False as a new field
    - _mount_ce_stubs() includes execution_stub_router (7 total, log message updated)
    - main.py has no @app.get("/api/executions") or any of the 7 execution routes
    - test_ce_features_all_false ee_flags list includes "executions"
    - test_ce_stub_routers_return_402 imports and asserts all 7 execution stub handlers
    - test_ce_table_count asserts len(ce_tables) == 15 (not 13)
  </behavior>
  <action>
Three targeted edits:

EDIT 1: `puppeteer/agent_service/ee/__init__.py`
a) Add `executions: bool = False` to EEContext dataclass after `api_keys: bool = False`.
b) In `_mount_ce_stubs()`:
   - Add import: `from .interfaces.executions import execution_stub_router`
   - Add: `app.include_router(execution_stub_router)`
   - Update logger.info string: "CE mode: mounted 7 stub routers (402 for all EE routes)"

EDIT 2: `puppeteer/agent_service/main.py`
Remove all 7 execution route handlers and their surrounding comment blocks. Search by these
markers and remove the handler plus all decorators/docstrings for each:

Block 1 — remove the entire "# --- Execution History ---" comment block and all three handlers
  under it (list_executions at line 231, get_execution at line 296,
  get_execution_attestation at line 339). Remove from the comment line through the closing
  line of get_execution_attestation's return statement (~line 374).

Block 2 — remove @app.get("/jobs/{guid}/executions") handler at line 1369 through ~line 1432.
  Remove only this single handler and its decorator/docstring.

Block 3 — remove the "# --- Execution Pin/Unpin ---" comment block and both handlers
  (pin_execution at line 2274, unpin_execution at line 2291) through ~line 2305.

Block 4 — remove the "# --- Per-job Execution CSV Export ---" comment block and handler
  (export_job_executions at line 2357) through ~line 2397.

CRITICAL: Search main.py for each route path to confirm all 7 are removed before done:
  /api/executions, /api/executions/{id}, /api/executions/{id}/attestation,
  /jobs/{guid}/executions, /api/executions/{exec_id}/pin,
  /api/executions/{exec_id}/unpin, /api/jobs/{guid}/executions/export

EDIT 3: `puppeteer/agent_service/tests/test_ce_smoke.py`
a) In test_ce_features_all_false: add "executions" to the ee_flags list.
b) In test_ce_stub_routers_return_402: extend the handler list to test all 7 execution
   stubs. Add imports and include all 7 in the for loop:
   ```python
   from agent_service.ee.interfaces.executions import (
       list_executions_stub, get_execution_stub, get_execution_attestation_stub,
       list_job_executions_stub, pin_execution_stub, unpin_execution_stub,
       export_job_executions_stub
   )
   ```
c) In test_ce_table_count: change `assert len(ce_tables) == 13` to `assert len(ce_tables) == 15`.
   Update the docstring from "13 CE tables" to "15 CE tables".
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -m pytest agent_service/tests/test_ce_smoke.py -v</automated>
  </verify>
  <done>
    - All 3 tests in test_ce_smoke.py pass: test_ce_features_all_false, test_ce_stub_routers_return_402, test_ce_table_count
    - grep for "@app.get(\"/api/executions\")" in main.py returns no output
    - grep for "@app.patch(\"/api/executions" in main.py returns no output
    - EEContext has executions field
    - _mount_ce_stubs mounts 7 routers
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd puppeteer && python -m pytest agent_service/tests/test_ce_smoke.py -v` — all 3 tests pass
2. `grep -c "@app.get(\"/api/executions" puppeteer/agent_service/main.py` returns 0
3. `grep -c "executions: bool" puppeteer/agent_service/ee/__init__.py` returns 1
4. `grep -c "execution_stub_router" puppeteer/agent_service/ee/__init__.py` returns 2 (import + include_router)
5. Full suite: `cd puppeteer && python -m pytest agent_service/tests/ -v` passes
</verification>

<success_criteria>
CODE-04 complete:
- All 7 execution routes removed from main.py
- CE stub router (executions.py) returns 402 for all 7 routes
- EE router (executions_router.py) holds the real implementations for EE plugin to mount
- EEContext has executions: bool = False
- test_ce_smoke.py all 3 tests pass (including pre-existing table-count fix: 13 -> 15)
</success_criteria>

<output>
After completion, create `.planning/phases/66-backend-code-fixes/66-02-SUMMARY.md`
</output>
