---
phase: 66-backend-code-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 66-01
  - 66-02
files_modified: []
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
autonomous: true
must_haves:
  truths:
    - "docker --version succeeds inside the built node image (CODE-01 confirmed)"
    - "pwsh --version succeeds inside the built node image (CODE-03 confirmed)"
    - "/tmp:/tmp appears exactly twice in compose.cold-start.yaml (CODE-02 confirmed)"
    - "The full test suite passes after all Phase 66 changes"
  artifacts:
    - path: "puppets/Containerfile.node"
      provides: "Built image with working Docker CLI and PowerShell binaries"
    - path: "puppeteer/compose.cold-start.yaml"
      provides: "/tmp:/tmp mount present for both puppet-node-1 and puppet-node-2"
  key_links:
    - from: "puppets/Containerfile.node"
      to: "built Docker image"
      via: "docker build"
      pattern: "docker build.*Containerfile.node"
---

<objective>
Verify all four Phase 66 requirements are satisfied in the built artifacts. Build the node
image from the updated Containerfile.node and confirm Docker CLI and PowerShell work inside
it. Confirm the /tmp bind mount is present in compose.cold-start.yaml. Run the full test
suite to confirm CODE-04 changes leave no regressions.

Purpose: Phase gate — all four CODE requirements confirmed before Phase 67 begins.
Output: No new source files. All verification commands exit 0.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-backend-code-fixes/66-CONTEXT.md
@.planning/phases/66-backend-code-fixes/66-01-SUMMARY.md
@.planning/phases/66-backend-code-fixes/66-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CODE-02 (static) and build node image for CODE-01 and CODE-03</name>
  <files></files>
  <action>
Step 1 — Verify CODE-02 statically (no build needed):
Run: `grep -c '/tmp:/tmp' puppeteer/compose.cold-start.yaml`
Expected output: 2 (one for puppet-node-1, one for puppet-node-2).
If output is not 2, the file was changed unexpectedly — report as a failure.

Step 2 — Build the node image:
Run from the repo root:
  docker build -t axiom-node-verify:phase66 -f puppets/Containerfile.node puppets/
This must complete without error. The --platform flag is NOT needed for this local build
(amd64 host builds amd64 by default; the TARGETARCH guard will be exercised during arm64
cross-builds or explicit --platform linux/arm64 builds in CI).

Step 3 — Verify CODE-01 inside the built image:
  docker run --rm axiom-node-verify:phase66 docker --version
Expected: prints "Docker version ..." with exit code 0.

Step 4 — Verify CODE-03 inside the built image:
  docker run --rm axiom-node-verify:phase66 pwsh --version
Expected: prints "PowerShell 7.6.0" with exit code 0.

Step 5 — Clean up the verification image:
  docker rmi axiom-node-verify:phase66

If any step fails, report the exact output and exit code. Do not mark this task done until
all four checks pass.
  </action>
  <verify>
    <automated>grep -c '/tmp:/tmp' /home/thomas/Development/master_of_puppets/puppeteer/compose.cold-start.yaml</automated>
  </verify>
  <done>
    - grep returns 2 for /tmp:/tmp in compose.cold-start.yaml
    - docker build exits 0
    - docker run ... docker --version exits 0 and prints a version string
    - docker run ... pwsh --version exits 0 and prints "PowerShell 7.6.0"
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm no regressions</name>
  <files></files>
  <action>
Run the full backend test suite from the puppeteer directory:
  cd /home/thomas/Development/master_of_puppets/puppeteer
  /home/thomas/Development/master_of_puppets/.venv/bin/python -m pytest agent_service/tests/ -v

All tests must pass. The previously-failing test_ce_table_count must now pass (assertion
updated to 15 in Plan 02). The new test_ce_stub_routers_return_402 execution stubs must all
return 402.

If any test fails that is NOT in the execution or CE smoke test files, report it as a
potential regression from the main.py route removal and investigate before proceeding.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -m pytest agent_service/tests/ -v --tb=short</automated>
  </verify>
  <done>
    - All tests pass (0 failures, 0 errors)
    - test_ce_smoke.py shows 3 PASSED
    - No unexpected failures in any other test module
  </done>
</task>

</tasks>

<verification>
Phase 66 gate — all four requirements confirmed:
- CODE-01: docker --version runs inside built node image
- CODE-02: /tmp:/tmp present in compose.cold-start.yaml (count == 2)
- CODE-03: pwsh --version runs inside built node image (PowerShell 7.6.0)
- CODE-04: test_ce_smoke.py 3/3 pass; full suite green
</verification>

<success_criteria>
All four CODE requirements verified:
1. docker --version exits 0 inside the built node image
2. /tmp:/tmp appears exactly twice in compose.cold-start.yaml
3. pwsh --version exits 0 inside the built node image (7.6.0)
4. Full pytest suite passes — zero failures, test_ce_table_count passes at 15
</success_criteria>

<output>
After completion, create `.planning/phases/66-backend-code-fixes/66-03-SUMMARY.md`
</output>
