---
phase: 146-workflow-data-model
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - puppeteer/requirements.txt
  - puppeteer/tests/test_workflow.py
  - puppeteer/tests/conftest.py
  - puppeteer/migration_v53.sql
autonomous: true
requirements: [WORKFLOW-01, WORKFLOW-02, WORKFLOW-03, WORKFLOW-04, WORKFLOW-05]

must_haves:
  truths:
    - "Tests run and provide baseline stubs for all WORKFLOW-01..05 behaviors"
    - "Database schema exists with 5 normalized workflow tables (no JSON blobs)"
    - "networkx library available for cycle detection and depth calculation"
    - "Async DB session fixtures support transactional test isolation"
  artifacts:
    - path: "puppeteer/requirements.txt"
      provides: "networkx>=3.6,<4.0 dependency"
      pattern: "^networkx"
    - path: "puppeteer/tests/test_workflow.py"
      provides: "Baseline test cases covering WORKFLOW-01..05 (stubs, no implementation)"
      min_lines: 80
    - path: "puppeteer/tests/conftest.py"
      provides: "workflow_fixture + async session fixtures"
      pattern: "workflow_fixture|async_db_session"
    - path: "puppeteer/migration_v53.sql"
      provides: "CREATE TABLE statements for workflows, workflow_steps, workflow_edges, workflow_parameters"
      contains: "CREATE TABLE workflow"
  key_links:
    - from: "puppeteer/requirements.txt"
      to: "puppeteer/agent_service/services/workflow_service.py"
      via: "import networkx"
      pattern: "networkx"
    - from: "puppeteer/migration_v53.sql"
      to: "puppeteer/agent_service/db.py"
      via: "ORM models inherit from Base"
      pattern: "CREATE TABLE workflow"

---

<objective>
Establish the test and database foundation for Phase 146. All test stubs exist, database schema is defined (5 normalized tables, no JSON blobs), and networkx is added to the dependency chain. No API routes or ORM models yet — those come in Plans 02–03.

Purpose: Validate DAG structure early and enable test-driven development. Wave 0 must complete before any service/route code is written.

Output: 
- Test file with all test cases (stubbed, ready to be implemented)
- Migration SQL for all 5 workflow tables
- networkx in requirements.txt
- conftest fixtures for async DB session and workflow test data
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-workflow-data-model/146-CONTEXT.md
@.planning/phases/146-workflow-data-model/146-RESEARCH.md
@.planning/phases/146-workflow-data-model/146-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add networkx to requirements.txt</name>
  <files>puppeteer/requirements.txt</files>
  <action>
    Add `networkx>=3.6,<4.0` to puppeteer/requirements.txt.
    Append to the end of the file.
    No version conflicts expected — this is a lightweight, pure-Python DAG library with no dependencies.
  </action>
  <verify>
    <automated>grep "^networkx>=" /home/thomas/Development/master_of_puppets/puppeteer/requirements.txt</automated>
  </verify>
  <done>networkx>=3.6,<4.0 appears in requirements.txt</done>
</task>

<task type="auto">
  <name>Task 2: Create migration_v53.sql with all 5 workflow tables</name>
  <files>puppeteer/migration_v53.sql</files>
  <action>
    Create migration_v53.sql with CREATE TABLE statements for:
    1. `workflows` (id UUID PK, name VARCHAR, created_by UUID FK to users, created_at, updated_at, is_paused BOOLEAN DEFAULT false)
    2. `workflow_steps` (id UUID PK, workflow_id UUID FK, scheduled_job_id UUID FK, node_type VARCHAR, config_json TEXT)
    3. `workflow_edges` (id UUID PK, workflow_id UUID FK, from_step_id UUID FK to workflow_steps, to_step_id UUID FK to workflow_steps, branch_name VARCHAR nullable)
    4. `workflow_parameters` (id UUID PK, workflow_id UUID FK, name VARCHAR, type VARCHAR, default_value VARCHAR nullable)
    5. Unique constraints: workflows(name) with scoping by created_by context if needed; workflow_edges(from_step_id, to_step_id, workflow_id) to prevent duplicate edges

    Use IF NOT EXISTS pattern for idempotency (already-deployed databases).
    Add indexes on foreign keys and lookups (workflow_id on workflow_steps, workflow_edges, workflow_parameters).
    Use standard Postgres types: UUID/CHAR, VARCHAR, TEXT, BOOLEAN, TIMESTAMP.
    Do NOT reference workflow_runs yet (Phase 147).

    Follow existing migration_v52.sql pattern in structure and style.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE" /home/thomas/Development/master_of_puppets/puppeteer/migration_v53.sql | grep -E "^5$"</automated>
  </verify>
  <done>migration_v53.sql exists with 5 CREATE TABLE statements, all tables include workflow_id foreign key except workflows itself, IF NOT EXISTS pattern used</done>
</task>

<task type="auto">
  <name>Task 3: Create test_workflow.py with all WORKFLOW-01..05 test stubs</name>
  <files>puppeteer/tests/test_workflow.py</files>
  <action>
    Create puppeteer/tests/test_workflow.py with pytest test stubs (all `assert False` or `pytest.skip`) covering:

    WORKFLOW-01 (Create):
    - test_create_workflow_success — valid workflow with steps/edges
    - test_create_workflow_invalid_edges — edge references non-existent step
    - test_create_workflow_cycle_detected — cyclic edges rejected

    WORKFLOW-02 (List):
    - test_list_workflows — returns list with step_count and last_run_status

    WORKFLOW-03 (Update):
    - test_update_workflow_success — update steps/edges
    - test_update_workflow_cycle_detected — update creates cycle, rejected
    - test_update_workflow_depth_exceeded — update violates 30-level limit

    WORKFLOW-04 (Delete):
    - test_delete_workflow_success
    - test_delete_workflow_blocked_by_active_runs — HTTP 409

    WORKFLOW-05 (Fork):
    - test_fork_workflow_success — clone into new workflow
    - test_fork_pauses_source — source workflow.is_paused set to true

    Each test is marked with the requirement ID in a docstring: `"""WORKFLOW-01: ..."""`

    Use pytest fixtures (async_client, workflow_fixture from conftest) — don't import yet (they'll be in conftest).
    Use `@pytest.mark.asyncio` for async tests.
    Include docstrings explaining what's being tested.
    Do NOT implement any logic — just assert False or pytest.skip() placeholders.
  </action>
  <verify>
    <automated>grep -c "def test_" /home/thomas/Development/master_of_puppets/puppeteer/tests/test_workflow.py | grep -E "^[0-9]{2}$"</automated>
  </verify>
  <done>test_workflow.py exists with all test stubs (12+ test functions), WORKFLOW-01..05 coverage, docstrings with requirement IDs, no implementation</done>
</task>

<task type="auto">
  <name>Task 4: Add fixtures to conftest.py</name>
  <files>puppeteer/tests/conftest.py</files>
  <action>
    Add to existing puppeteer/tests/conftest.py (or create if missing):

    1. `async_db_session` fixture — yields an async SQLAlchemy session that:
       - Connects to test DB (SQLite or test Postgres)
       - Wraps each test in a transaction that rolls back after test completes
       - Allows parallel test isolation

    2. `workflow_fixture` — depends on async_db_session, creates a valid Workflow with:
       - 3 ScheduledJob steps (pre-created with valid IDs)
       - 2 edges forming a simple chain (step1 → step2 → step3)
       - Returns the full Workflow entity with nested steps[], edges[], parameters[]
       - Cleans up after test (via transaction rollback)

    Structure:
    ```python
    @pytest_asyncio.fixture
    async def async_db_session():
        """Transactional DB session for test isolation."""
        # Reuse existing session pattern if present in conftest
        # Otherwise: create engine, yield session, rollback
        pass

    @pytest_asyncio.fixture
    async def workflow_fixture(async_db_session):
        """Pre-created workflow with 3 steps and 2 edges."""
        # Create Workflow, WorkflowStep (3x), WorkflowEdge (2x)
        # Return as dict with steps[], edges[], parameters[] arrays
        pass
    ```

    Reference existing conftest patterns (if any) to stay consistent.
  </action>
  <verify>
    <automated>grep -E "def (async_db_session|workflow_fixture)" /home/thomas/Development/master_of_puppets/puppeteer/tests/conftest.py</automated>
  </verify>
  <done>conftest.py has async_db_session and workflow_fixture, both decorated with @pytest_asyncio.fixture</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `puppeteer/requirements.txt` has networkx>=3.6,<4.0
2. `puppeteer/tests/test_workflow.py` exists with 12+ test stubs (no implementation)
3. `puppeteer/tests/conftest.py` has async_db_session and workflow_fixture
4. `puppeteer/migration_v53.sql` exists with 5 CREATE TABLE statements
5. All files are committed to git
</verification>

<success_criteria>
- [x] networkx in requirements.txt
- [x] migration_v53.sql with 5 tables (workflows, workflow_steps, workflow_edges, workflow_parameters, no definition_json blob)
- [x] test_workflow.py with all WORKFLOW-01..05 stubs (no implementation)
- [x] conftest.py with async_db_session and workflow_fixture
- [x] Test stubs use @pytest.mark.asyncio and async_client fixture
- [x] Wave 0 complete — no ORM models yet, no API routes yet
</success_criteria>

<output>
After completion, create `.planning/phases/146-workflow-data-model/146-01-SUMMARY.md`

Content:
```
# Phase 146, Plan 01 — Test & Schema Foundation

## Completed

- Added networkx>=3.6,<4.0 to requirements.txt
- Created migration_v53.sql with 5 workflow tables (no JSON blobs)
- Created test_workflow.py with 12+ test stubs for WORKFLOW-01..05
- Enhanced conftest.py with async_db_session and workflow_fixture

## Files Changed

- puppeteer/requirements.txt — added networkx
- puppeteer/migration_v53.sql — new file, 5 CREATE TABLE
- puppeteer/tests/test_workflow.py — new file, 12+ test stubs
- puppeteer/tests/conftest.py — added fixtures

## Test Baseline

Tests ready but stubbed (assert False). Plan 02 implements ORM + service validation. Plan 03 wires API routes.

## Next Wave

Wave 1 (Plan 02): ORM models (Workflow, WorkflowStep, WorkflowEdge, WorkflowParameter) + workflow_service with validation logic (cycle detection, depth calculation).
```
</output>
