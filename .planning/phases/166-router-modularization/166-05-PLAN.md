---
phase: 166
plan: 05
type: execute
wave: 2
depends_on: ["166-01", "166-02", "166-03", "166-04"]
files_modified: []
autonomous: true
requirements:
  - ARCH-04
user_setup: []

must_haves:
  truths:
    - "Full pytest suite passes with no new failures"
    - "All 89+ routes accessible and returning correct status codes"
    - "No regressions in behavior or coverage"
    - "Test count stable compared to pre-refactoring baseline"
  artifacts:
    - path: "puppeteer/pytest.ini"
      provides: "Pytest configuration"
      contains: "testpaths, python_files, asyncio_mode"
    - path: "puppeteer/tests/"
      provides: "82 test files covering all routes"
      contains: "test_*.py with 737+ test cases"
    - path: "/tmp/pytest_full.log"
      provides: "Full pytest execution log with coverage"
      contains: "passed, failed, error counts"
  key_links:
    - from: "puppeteer/agent_service/routers/"
      to: "puppeteer/tests/test_*.py"
      via: "pytest discovery"
      pattern: "test_auth.py, test_jobs.py, test_nodes.py, etc."
    - from: "puppeteer/agent_service/main.py"
      to: "puppeteer/conftest.py"
      via: "app fixture in conftest"
      pattern: "from agent_service.main import app"
---

<objective>
Final regression validation of the complete Phase 166 refactoring. Ensures all 89+ routes are accessible, functional, and unchanged in behavior. Confirms ARCH-04 (full test suite passes).

Purpose: Provide definitive proof that the router modularization is correct and complete. No broken routes, no lost functionality, no coverage gaps.

Output: Detailed test execution report showing 737+ tests passing with no new failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-router-modularization/166-CONTEXT.md
@.planning/phases/166-router-modularization/166-RESEARCH.md
@.planning/phases/166-router-modularization/166-VALIDATION.md

## Key Reference: ARCH-04 Requirement

From ROADMAP.md:
> ARCH-04: Full pytest suite passes with no new failures. All 737+ tests passing. Coverage unchanged.

## Dependencies

This plan depends on successful completion of:
- Plan 01: 2 routers created and wired (auth, jobs)
- Plan 02: 2 more routers created and wired (nodes, workflows)
- Plan 03: 2 more routers created and wired (admin, system); all routes removed from main.py
- Plan 04: OpenAPI schema verified (zero behavior change)

## Test Infrastructure

From VALIDATION.md and CLAUDE.md:

**Framework:** pytest 7.x (Python testing)
**Config:** puppeteer/pytest.ini
**Quick test:** `cd puppeteer && pytest tests/ -x -q --tb=short`
**Full test:** `cd puppeteer && pytest tests/` (30–60 seconds, ~737 tests)

**Test files:** 82 files in puppeteer/tests/ covering:
- Authentication (auth.py, JWT, password change, session security)
- Jobs (CRUD, dispatch, templates, definitions, scheduling)
- Nodes (enrollment, heartbeat, node selection, capability matching)
- Workflows (CRUD, runs, webhooks)
- Admin (users, roles, signatures, audit log, alerts, config)
- System (health, CRL, features, license, WebSocket)
- Smelter (code execution, validation, testing)
- Integration (multi-endpoint flows)

## Test Client Pattern (conftest.py)

All tests use the FastAPI TestClient:
```python
from fastapi.testclient import TestClient
from agent_service.main import app

@pytest.fixture
def client():
    return TestClient(app)
```

</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full pytest suite and capture results</name>
  <files></files>
  <read_first>
    - puppeteer/pytest.ini (to understand test configuration)
  </read_first>
  <action>
Run the full pytest suite to verify all routes are accessible and functional after the complete router refactoring:

```bash
cd puppeteer && pytest tests/ -v --tb=short 2>&1 | tee /tmp/pytest_full.log
```

This will:
1. Discover all test files in puppeteer/tests/
2. Run all 737+ test cases
3. Report pass/fail status for each test
4. Print a final summary showing total passed, failed, skipped, errors
5. Save full output to /tmp/pytest_full.log

**Expected output:**
- All tests pass (or at minimum, no NEW failures compared to baseline)
- Summary line: `XXX passed in YYs`
- No `FAILED` or `ERROR` lines

**If tests fail:**
1. Check the error message — identify which routes are broken
2. Likely causes:
   - Missing route handler in a router (a route was not extracted)
   - Circular import (a router is importing main at module level when it shouldn't)
   - Missing `ws_manager` export from main.py
   - Missing import statement in a router
   - Test assumes routes are still in main.py (unlikely, test client imports app)

3. Fix the issue and re-run:
   ```bash
   cd puppeteer && pytest tests/ -x -q --tb=short
   ```

4. Once tests pass, run the full suite again to capture final results.

**Capture results:**
After tests pass, extract the summary:
```bash
tail -30 /tmp/pytest_full.log | grep -E "passed|failed|error"
```

Record the final test count (e.g., "737 passed in 45.23s").
  </action>
  <verify>
    <automated>
      cd puppeteer && pytest tests/ -q --tb=line 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    Full pytest suite executed successfully. All 737+ tests pass with no new failures. Test results captured in /tmp/pytest_full.log. Summary shows all routes accessible and functional via wired routers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → App | TestClient uses app fixture from conftest.py; tests import app from agent_service.main |
| Tests → Database | Tests use in-memory SQLite or test fixtures; no production data accessed |
| Tests → External services | Tests mock external APIs (if any); no real API calls made |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-14 | Tampering | Test data isolation | accept | Each test uses a fresh database; test fixtures clean up after themselves. No cross-test contamination. |

**Security assessment:** Tests verify that refactored routes preserve original behavior. No new security concerns introduced. Test suite itself is development-only and not deployed to production.

</threat_model>

<verification>
## Plan 166-05 Success Criteria

1. ✅ Full pytest suite runs without errors
2. ✅ All 737+ tests pass (or baseline test count maintained)
3. ✅ No new test failures introduced by router refactoring
4. ✅ All routes accessible via TestClient (no 404s or 500s)
5. ✅ Authentication routes work correctly
6. ✅ Job routes work correctly
7. ✅ Node routes work correctly
8. ✅ Workflow routes work correctly
9. ✅ Admin routes work correctly
10. ✅ System routes work correctly
11. ✅ Smelter routes work correctly
12. ✅ Integration tests pass (multi-endpoint flows)
</verification>

<success_criteria>
After this plan completes:

- [ ] Full pytest suite passes with no new failures
- [ ] Test count matches baseline (737+ tests)
- [ ] All routes return expected status codes (200, 201, 400, 401, 403, 404, 500, etc.)
- [ ] Permission checks enforced correctly
- [ ] Authentication flows work correctly
- [ ] Database operations work correctly
- [ ] WebSocket connections work correctly
- [ ] File output: `/tmp/pytest_full.log` contains full test results

**Wave 2 complete:** All refactoring plans done. Routes extracted, wired, removed from main.py, verified via OpenAPI and pytest. Ready for cleanup verification (Plan 06).
</success_criteria>

<output>
After successful completion, create `.planning/phases/166-router-modularization/166-05-SUMMARY.md` with:

- **Status:** COMPLETE
- **Duration:** (actual time taken)
- **Test execution:** Command run: `pytest tests/ -v --tb=short`
- **Results:** XXX passed in YYs
- **Tests by domain:**
  - Authentication: X passed
  - Jobs: X passed
  - Nodes: X passed
  - Workflows: X passed
  - Admin: X passed
  - System: X passed
  - Smelter: X passed
  - Integration: X passed
- **Failures:** None (or list any expected/known failures)
- **Coverage:** Unchanged from baseline
- **Next steps:** Phase 166 complete; ready for deployment
</output>
