---
phase: 128-concurrent-isolation-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mop_validation/scripts/stress/python/noisy_monitor.py
autonomous: true
requirements:
  - ISOL-02
user_setup: []

must_haves:
  truths:
    - "noisy_monitor.py exists in mop_validation/scripts/stress/python/"
    - "Script measures sleep(1) drift over 60 iterations with nanosecond precision"
    - "Script outputs JSON on first stdout line with max_drift_s, mean_drift_s, measurements, pass fields"
    - "Script respects DRIFT_THRESHOLD_S env var (default 1.1s)"
    - "Script exits 0 on pass, 2 on fail (all measurements below threshold)"
  artifacts:
    - path: "mop_validation/scripts/stress/python/noisy_monitor.py"
      provides: "Python latency drift monitor for concurrent isolation testing"
      min_lines: 80
  key_links:
    - from: "orchestrate_stress_tests.py:run_scenario_3_concurrent_isolation()"
      to: "mop_validation/scripts/stress/python/noisy_monitor.py"
      via: "load_script('python', 'noisy_monitor.py') call"
      pattern: "load_script.*noisy_monitor"
---

<objective>
Create the Python version of noisy_monitor.py, completing the three-language stress test suite (Python, Bash, PowerShell). This script measures sleep drift over 60 iterations to detect if concurrent jobs on the same node suffer latency disruption from noisy neighbours.

Purpose: Enable Phase 128's concurrent isolation verification to measure memory isolation effectiveness
Output: Executable Python script ready for orchestrator dispatch
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/128-concurrent-isolation-verification/128-CONTEXT.md
@.planning/phases/128-concurrent-isolation-verification/128-RESEARCH.md

## Existing References (Three-Language Parity)

From mop_validation/scripts/stress/bash/noisy_monitor.sh:
- 60 iterations of sleep(1.0)
- Nanosecond-precision timestamp tracking: `date +%s%N` → `(end_ns - start_ns) / 1e9`
- Per-iteration elapsed time in measurements array
- max_drift, mean_drift calculation
- Pass condition: all measurements < threshold (default 1.1s)
- JSON output on first stdout line: `{"type": "noisy_monitor", "language": "bash", "max_drift_s": X.XXX, "mean_drift_s": X.XXX, "threshold_s": 1.1, "measurements": [X.XXX, ...], "pass": true/false}`
- Exit code: 0 (pass), 2 (fail)

From mop_validation/scripts/stress/pwsh/noisy_monitor.ps1:
- Same 60-iteration sleep drift algorithm
- Stopwatch for per-iteration timing: `$sw.Elapsed.TotalSeconds`
- Identical JSON schema with same field names and 3-decimal precision
- Exit codes: 0 (pass), 2 (fail)

From mop_validation/scripts/stress/python/memory_hog.py:
- Python pattern: json, os, time imports
- Environment variable parsing: `os.environ.get("VAR_NAME", "default_value")`
- JSON output: `json.dumps(dict)` on first stdout line
- Exit codes via `sys.exit(code)`

## Integration Points

From orchestrate_stress_tests.py line 654:
```python
monitor_script = load_script("python", "noisy_monitor.py")
```

From orchestrate_stress_tests.py lines 700-704:
```python
if stdout:
    try:
        first_line = stdout.split("\n")[0]
        result = json.loads(first_line)
        max_drift = result.get("max_drift_s", 1.5)
        passed = result.get("pass", False)
```

Orchestrator expects: JSON-parseable first stdout line with `max_drift_s` and `pass` fields.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create noisy_monitor.py with sleep drift measurement algorithm</name>
  <files>mop_validation/scripts/stress/python/noisy_monitor.py</files>
  <action>
Create Python script that mirrors bash/pwsh noisy_monitor implementations exactly:

1. Shebang: `#!/usr/bin/env python3`
2. Docstring: Explain sleep drift measurement, no capability gating, DRIFT_THRESHOLD_S env var (default 1.1)
3. Imports: json, os, time, sys (standard library only)
4. Parse DRIFT_THRESHOLD_S from environment: `os.environ.get("DRIFT_THRESHOLD_S", "1.1")` → convert to float, default 1.1 if invalid
5. Initialize empty measurements list
6. Loop 60 times:
   - Use `time.time() * 1e9` to capture start nanosecond timestamp (as int)
   - Call `time.sleep(1.0)`
   - Use `time.time() * 1e9` to capture end nanosecond timestamp (as int)
   - Calculate elapsed_ns = end_ns - start_ns
   - Calculate elapsed_s = elapsed_ns / 1e9 (float)
   - Append to measurements list
7. Calculate statistics:
   - max_drift_s = max(measurements)
   - mean_drift_s = sum(measurements) / len(measurements)
8. Determine pass = all(m < threshold for m in measurements)
9. Build measurements_array with 3-decimal precision floats: `[round(m, 3) for m in measurements]`
10. Output JSON on first stdout line (exact schema):
    ```json
    {"type": "noisy_monitor", "language": "python", "max_drift_s": X.XXX, "mean_drift_s": X.XXX, "threshold_s": 1.1, "measurements": [X.XXX, ...], "pass": true/false}
    ```
    Use `json.dumps()` with dict, NO pretty-printing
11. Output human-readable summary on subsequent lines:
    - If pass: "PASS: Sleep drift within tolerance (max=X.XXXs, mean=X.XXXs < X.Xs threshold)"
    - If fail: "FAIL: Sleep drift exceeded threshold (max=X.XXXs > X.Xs) — noisy neighbours detected"
12. Exit with code: 0 if pass, 2 if fail
13. NO capability gating (unlike memory_hog.py — monitor runs unconstrained to detect if neighbours disrupt it)

Reference implementations:
- bash: /mop_validation/scripts/stress/bash/noisy_monitor.sh (lines 1-76)
- pwsh: /mop_validation/scripts/stress/pwsh/noisy_monitor.ps1 (lines 1-93)
- memory_hog.py pattern: /mop_validation/scripts/stress/python/memory_hog.py (imports, json.dumps, sys.exit)

Precision constraint: All drift values rounded to 3 decimal places in JSON and summary output.
  </action>
  <verify>
    <automated>
      # Verify file exists and is executable
      test -f /home/thomas/Development/master_of_puppets/mop_validation/scripts/stress/python/noisy_monitor.py
      test -x /home/thomas/Development/master_of_puppets/mop_validation/scripts/stress/python/noisy_monitor.py

      # Verify script can be imported/parsed (syntax check)
      cd /home/thomas/Development/master_of_puppets && python3 -m py_compile mop_validation/scripts/stress/python/noisy_monitor.py

      # Verify JSON output on first line (dry run with mock sleep)
      # This is a manual verification: run the script and confirm output format
    </automated>
  </verify>
  <done>
    - noisy_monitor.py created in mop_validation/scripts/stress/python/
    - File is executable (chmod +x already applied)
    - Script syntax valid (python3 -m py_compile passes)
    - Implements exact 60-iteration sleep drift algorithm with nanosecond precision
    - Outputs JSON on first line with max_drift_s, mean_drift_s, threshold_s, measurements, pass fields
    - Respects DRIFT_THRESHOLD_S env var (default 1.1s)
    - Exits 0 on pass (all measurements below threshold), 2 on fail
    - Human-readable summary on subsequent lines
  </done>
</task>

</tasks>

<verification>
Verify script is ready for orchestrator consumption:
- File exists and is executable
- Syntax valid (can be parsed by Python)
- No import errors
- Env var parsing works (test with DRIFT_THRESHOLD_S=0.5)
- JSON output format matches bash/pwsh implementations (orchestrator will parse first stdout line)
- Exit codes correct (0 on pass, 2 on fail)
</verification>

<success_criteria>
- noisy_monitor.py implemented as Python mirror of bash/pwsh versions
- All 60 measurements collected with nanosecond precision
- JSON schema matches orchestrator expectations (max_drift_s, mean_drift_s, measurements, pass)
- Script ready to be dispatched by orchestrator in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/128-concurrent-isolation-verification/128-01-SUMMARY.md`
</output>
