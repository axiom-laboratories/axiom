---
phase: 107-schema-foundation-crud-completeness
plan: 03
type: execute
wave: 2
depends_on: ["107-01"]
files_modified:
  - puppeteer/dashboard/src/views/Templates.tsx
autonomous: false
requirements:
  - CRUD-02
  - CRUD-03

must_haves:
  truths:
    - "Operator can click Edit on a tool recipe row, modify fields, and save via PATCH endpoint"
    - "Admin can see an 'Approved OS' tab in the Foundry page"
    - "Admin can add a new Approved OS entry from the tab"
    - "Admin can inline-edit an existing Approved OS entry (name, image_uri, os_family)"
    - "Admin can delete an Approved OS entry; blocked with 409 if referenced by a blueprint"
    - "OS Family dropdown in Approved OS shows only DEBIAN and ALPINE"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Templates.tsx"
      provides: "Tool recipe edit dialog + Approved OS tab with full inline CRUD"
      min_lines: 800
  key_links:
    - from: "Templates.tsx tool edit button"
      to: "PATCH /api/capability-matrix/{id}"
      via: "authenticatedFetch in edit handler"
      pattern: "authenticatedFetch.*capability-matrix.*PATCH"
    - from: "Templates.tsx Approved OS tab"
      to: "GET /api/approved-os"
      via: "useQuery fetch"
      pattern: "useQuery.*approved-os"
    - from: "Templates.tsx OS delete handler"
      to: "DELETE /api/approved-os/{id}"
      via: "authenticatedFetch with 409 handling"
      pattern: "409.*referenced.*blueprint"
---

<objective>
Add tool recipe edit UI (pencil icon on tool rows, pre-populated edit dialog using existing PATCH endpoint) and a full Approved OS management tab in the Foundry page (list, add, inline edit, delete with referential integrity error handling).

Purpose: Operators can edit tool recipes without recreating them. Admins can manage the Approved OS list entirely through the dashboard.
Output: Updated Templates.tsx with tool edit dialog and new Approved OS tab.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-schema-foundation-crud-completeness/107-CONTEXT.md
@.planning/phases/107-schema-foundation-crud-completeness/107-RESEARCH.md
@.planning/phases/107-schema-foundation-crud-completeness/107-01-SUMMARY.md

<interfaces>
<!-- Backend endpoints the executor needs -->

Existing endpoints:
```
GET    /api/capability-matrix       -> CapabilityMatrixEntry[]
PATCH  /api/capability-matrix/{id}  -> CapabilityMatrixEntry
  Body: { name?, injection_recipe?, validation_cmd?, runtime_dependencies?, os_family? }
DELETE /api/capability-matrix/{id}  -> 204

GET    /api/approved-os             -> ApprovedOSResponse[]
POST   /api/approved-os             -> ApprovedOSResponse
  Body: { name, image_uri, os_family }
DELETE /api/approved-os/{id}        -> 200 or 409 (if referenced by blueprint, from Plan 01)
```

New from Plan 01:
```
PATCH  /api/approved-os/{id}        -> ApprovedOSResponse
  Body: { name?, image_uri?, os_family? }
```

CapabilityMatrixEntry type:
```typescript
{
  id: string;
  name: string;
  injection_recipe: string;   // multi-line text
  validation_cmd: string;
  runtime_dependencies: string[];  // JSON array
  os_family: string;           // DEBIAN, ALPINE
  created_at: string;
}
```

ApprovedOSResponse type:
```typescript
{
  id: string;
  name: string;
  image_uri: string;
  os_family: string;
  is_active: boolean;
  created_at: string;
}
```

Existing tab pattern in Templates.tsx:
```typescript
// Uses @/components/ui/tabs: Tabs, TabsList, TabsTrigger, TabsContent
// Current tabs: "recipes" (Image Recipes), "templates" (Templates), "tools" (Tools)
// Add: "approved-os" tab
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tool recipe edit dialog</name>
  <files>puppeteer/dashboard/src/views/Templates.tsx</files>
  <action>
1. Add state for tool editing: `editingTool` (CapabilityMatrixEntry | null), `toolEditOpen` (boolean).

2. Add a pencil icon (Lucide Pencil) to each tool row in the Tools tab. OnClick: `setEditingTool(tool); setToolEditOpen(true)`.

3. Create a tool edit dialog (Radix Dialog) that pre-populates:
   - Name (text input)
   - Injection Recipe (textarea -- multi-line)
   - Validation Command (text input)
   - Runtime Dependencies (comma-separated text input, parsed to/from string array)
   - OS Family (select: DEBIAN, ALPINE only -- no FEDORA)

4. Save button sends PATCH to `/api/capability-matrix/${editingTool.id}` with changed fields only. On success: toast "Tool updated", invalidate capability-matrix query, close dialog. On error: toast error message.

5. The existing "Add Tool" dialog pattern in Templates.tsx is the reference for dialog structure and form layout. Follow the same styling conventions.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build</automated>
  </verify>
  <done>Pencil icon on each tool row opens pre-populated edit dialog. Save sends PATCH to existing endpoint. Toast on success/error. OS family restricted to DEBIAN/ALPINE.</done>
</task>

<task type="auto">
  <name>Task 2: Approved OS tab with full CRUD</name>
  <files>puppeteer/dashboard/src/views/Templates.tsx</files>
  <action>
1. Add a new "Approved OS" tab (TabsTrigger value="approved-os") to the existing Tabs component. Position it after the Tools tab.

2. In the TabsContent for "approved-os", render a table with columns: Name, Image URI, OS Family, Actions.

3. Fetch data with useQuery: `GET /api/approved-os`.

4. "Add OS" button above the table opens an inline form row (or a simple dialog -- Claude's discretion per CONTEXT.md recommendation of inline edit). Fields: name (text), image_uri (text), os_family (select: DEBIAN/ALPINE only). Save sends POST /api/approved-os. On success: toast, invalidate query.

5. Inline edit: pencil icon in Actions column toggles that row into edit mode:
   - Name, Image URI become Input fields pre-filled with current values
   - OS Family becomes a Select pre-selected with current value
   - Show Save (check icon) and Cancel (X icon) buttons
   - Save sends PATCH /api/approved-os/{id} with changed fields
   - On success: toast "OS entry updated", exit edit mode, invalidate query
   - On error: toast error

6. Delete: trash icon in Actions column. Sends DELETE /api/approved-os/{id}.
   - On 409: toast.error with the detail message (e.g., "Cannot delete: referenced by blueprint 'my-blueprint'")
   - On success: toast "OS entry removed", invalidate query

7. Restrict OS Family options to DEBIAN and ALPINE in both add and edit forms. Do not show FEDORA.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build</automated>
  </verify>
  <done>Approved OS tab visible in Foundry page. Table lists all OS entries. Add creates new entry. Inline edit updates entry via PATCH. Delete removes entry or shows 409 toast if referenced. OS family restricted to DEBIAN/ALPINE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all CRUD flows in Docker stack</name>
  <action>
    Human verifies that all Phase 107 frontend CRUD flows (Plans 02 and 03) work correctly in the Docker stack.
  </action>
  <what-built>
    Plan 02: Blueprint edit mode with optimistic locking, dependency confirmation dialog, FEDORA removed.
    Plan 03: Tool recipe edit dialog, Approved OS tab with inline CRUD.
    All changes to the Foundry page (Templates.tsx) and BlueprintWizard.
  </what-built>
  <how-to-verify>
    1. Rebuild the Docker stack: `cd /home/thomas/Development/master_of_puppets/puppeteer && docker compose -f compose.server.yaml build agent && docker compose -f compose.server.yaml up -d`
    2. Open the Foundry page in the dashboard
    3. Verify "Approved OS" tab exists alongside Image Recipes, Templates, Tools
    4. In Approved OS tab: add a new entry, edit it inline, verify delete shows 409 if referenced
    5. In Image Recipes tab: verify pencil icon on blueprint cards, click to open wizard in edit mode with pre-populated fields
    6. Save an edit -- verify version increments and toast appears
    7. In Tools tab: verify pencil icon on tool rows, click to open edit dialog with pre-populated fields
    8. Verify OS family dropdowns show only DEBIAN and ALPINE (no FEDORA) everywhere
  </how-to-verify>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build</automated>
  </verify>
  <done>Human confirms all CRUD flows work in Docker stack.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Build check: `cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build`
2. Lint check: `cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run lint`
3. Frontend tests: `cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run test`
4. Full stack rebuild + manual verification via checkpoint
</verification>

<success_criteria>
- Tool recipe edit dialog: pencil icon, pre-populated fields, PATCH save, toast feedback
- Approved OS tab: list, add, inline edit, delete with 409 handling
- OS family restricted to DEBIAN/ALPINE everywhere (no FEDORA)
- Dashboard builds with no TS errors
- Human verification confirms all CRUD flows work in Docker stack
</success_criteria>

<output>
After completion, create `.planning/phases/107-schema-foundation-crud-completeness/107-03-SUMMARY.md`
</output>
