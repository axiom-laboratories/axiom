---
phase: 139-entry-point-whitelist-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/security.py
  - puppeteer/agent_service/ee/__init__.py
  - puppeteer/tests/test_encryption_key_enforcement.py
  - puppeteer/tests/test_ee_manifest.py
autonomous: true
requirements:
  - EE-04
  - EE-06

must_haves:
  truths:
    - "ENCRYPTION_KEY environment variable is required at module load time"
    - "Missing ENCRYPTION_KEY raises RuntimeError with actionable error message"
    - "Entry points with untrusted values raise RuntimeError at startup"
    - "Entry points with untrusted values raise RuntimeError at live-reload"
    - "Trusted entry points load successfully in both startup and live-reload paths"
  artifacts:
    - path: "puppeteer/agent_service/security.py"
      provides: "ENCRYPTION_KEY hard requirement enforcement"
      key_function: "_load_or_generate_encryption_key"
    - path: "puppeteer/agent_service/ee/__init__.py"
      provides: "Entry point whitelist validation"
      key_functions: ["load_ee_plugins", "activate_ee_live"]
    - path: "puppeteer/tests/test_encryption_key_enforcement.py"
      provides: "Unit tests for ENCRYPTION_KEY hard requirement (EE-06)"
      test_count: 3
    - path: "puppeteer/tests/test_ee_manifest.py"
      provides: "Entry point whitelist validation tests (EE-04)"
      test_additions: 4
  key_links:
    - from: "puppeteer/agent_service/main.py"
      to: "puppeteer/agent_service/security.py"
      via: "import ENCRYPTION_KEY"
      pattern: "from .security import ENCRYPTION_KEY"
      criticality: "Module load fails at import if ENCRYPTION_KEY unset — earliest possible failure point"
    - from: "puppeteer/agent_service/ee/__init__.py:load_ee_plugins()"
      to: "importlib.metadata.entry_points()"
      via: "entry point discovery and whitelist validation"
      pattern: "if ep.value != \"ee.plugin:EEPlugin\": raise RuntimeError"
      criticality: "Untrusted entry points rejected at startup"
    - from: "puppeteer/agent_service/ee/__init__.py:activate_ee_live()"
      to: "importlib.metadata.entry_points()"
      via: "live-reload entry point validation"
      pattern: "if ep.value != \"ee.plugin:EEPlugin\": raise RuntimeError"
      criticality: "Hot-reload must validate identically to startup"
---

<objective>
Harden the EE plugin loader and encryption key bootstrapping by enforcing a whitelist for entry point values and requiring ENCRYPTION_KEY presence at module load time, with no fallbacks or silent degradation.

Purpose: Close two security gaps in the EE licence protection system:
1. Entry point whitelist prevents untrusted plugin code from loading
2. ENCRYPTION_KEY hard requirement ensures encrypted data is managed explicitly

Output: Updated security.py and ee/__init__.py with enforcement checks; comprehensive unit tests covering both positive and negative cases across startup and live-reload paths.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/139-entry-point-whitelist-enforcement/139-CONTEXT.md
@.planning/phases/139-entry-point-whitelist-enforcement/139-RESEARCH.md
@.planning/phases/139-entry-point-whitelist-enforcement/139-VALIDATION.md

Key existing codebase patterns:
- **RuntimeError for security violations** — ee/__init__.py uses RuntimeError for wheel manifest verification (lines 96–173); same exception type for entry point whitelist
- **Module-level imports** — main.py imports security.py at line 50, ensuring module-level RuntimeError aborts startup before routes register
- **Entry point structure** — importlib.metadata.entry_points() returns EntryPoint objects with .value (module:class), .name, and .load() methods
- **Logger usage** — Existing pattern: logger.error() before raising RuntimeError in error handlers
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ENCRYPTION_KEY hard requirement in security.py</name>
  <files>puppeteer/agent_service/security.py</files>
  <action>
Refactor `_load_or_generate_encryption_key()` function to enforce ENCRYPTION_KEY environment variable as required with no fallbacks:

1. Remove the file-based fallback (`Path("/app/secrets/encryption.key")`)
2. Remove auto-generate fallback (Fernet.generate_key())
3. Keep the env var check: if `ENCRYPTION_KEY` env var is set, use it
4. If env var is absent, raise RuntimeError immediately with actionable message

Error message must include:
- "ENCRYPTION_KEY environment variable is required but not set."
- Generation command: "python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'"

Optional: Add logger.critical() line before raising RuntimeError (Claude's discretion per CONTEXT.md).

The function is called at module level (after function definition):
```python
ENCRYPTION_KEY = _load_or_generate_encryption_key()
cipher_suite = Fernet(ENCRYPTION_KEY)
```

This ensures the check executes when security.py is imported by main.py, failing at the earliest possible point if ENCRYPTION_KEY is absent. Any RuntimeError here aborts startup before FastAPI routes are registered.

Preserve all other functionality in security.py (encrypt_secret, decrypt_secret, get_node_secret_hash, etc.) — only modify _load_or_generate_encryption_key().
  </action>
  <verify>
    <automated>cd puppeteer && python -c "import os; os.environ.pop('ENCRYPTION_KEY', None); from agent_service.security import ENCRYPTION_KEY" 2>&1 | grep -q "ENCRYPTION_KEY environment variable is required" && echo "PASS: RuntimeError raised when ENCRYPTION_KEY absent" || echo "FAIL: RuntimeError not raised"</automated>
  </verify>
  <done>
ENCRYPTION_KEY enforcement working:
- _load_or_generate_encryption_key() raises RuntimeError if env var absent
- Error message includes "ENCRYPTION_KEY environment variable is required"
- Error message includes Fernet key generation one-liner
- Module still initializes correctly when ENCRYPTION_KEY is set
- cipher_suite is initialized after successful ENCRYPTION_KEY load
  </done>
</task>

<task type="auto">
  <name>Task 2: Add entry point whitelist validation to load_ee_plugins()</name>
  <files>puppeteer/agent_service/ee/__init__.py</files>
  <action>
Locate the `load_ee_plugins()` async function (around lines 290–315 based on RESEARCH.md) and add entry point whitelist validation:

1. In the `for ep in plugins:` loop, immediately before `ep.load()` call, add:
```python
# EE-04: Entry point whitelist validation — exact value match only
if ep.value != "ee.plugin:EEPlugin":
    raise RuntimeError(
        f"Untrusted axiom.ee entry point: '{ep.value}' — expected 'ee.plugin:EEPlugin'"
    )
```

2. Keep existing logger.info() calls for trusted entry points — they remain unchanged.

3. Existing try/except around the entire block catches any RuntimeError and logs it as warning, then mounts CE stubs — this is correct behavior per RESEARCH.md. The try/except pattern ensures a single bad entry point doesn't prevent startup entirely (but it will cause CE fallback, which is the intended consequence of a security violation).

4. Do not add whitelist check to _mount_ce_stubs() or other functions — only in load_ee_plugins().

5. Add comment linking to EE-04 requirement for clarity.

Expected outcome: Untrusted entry points raise RuntimeError within the try block → caught by except clause → CE stubs mounted, but the security violation is logged.
  </action>
  <verify>
    <automated>cd puppeteer && grep -A 2 "EE-04: Entry point whitelist" agent_service/ee/__init__.py | grep -q "ep.value != \"ee.plugin:EEPlugin\"" && echo "PASS: Entry point whitelist check in load_ee_plugins()" || echo "FAIL: Check not found"</automated>
  </verify>
  <done>
Entry point whitelist validation in load_ee_plugins():
- Whitelist check added before ep.load()
- Correct error message format: "Untrusted axiom.ee entry point: '{ep.value}' — expected 'ee.plugin:EEPlugin'"
- try/except wrapper preserves existing error handling
- CE stubs mounted on whitelist failure (expected fallback)
- Comment references EE-04 requirement
  </done>
</task>

<task type="auto">
  <name>Task 3: Add entry point whitelist validation to activate_ee_live()</name>
  <files>puppeteer/agent_service/ee/__init__.py</files>
  <action>
Locate the `activate_ee_live()` async function (around lines 274–298 based on RESEARCH.md) and add the same entry point whitelist validation:

1. In the section where plugins are loaded (after `plugins = list(entry_points(group="axiom.ee"))`), in the `for ep in plugins:` loop, add:
```python
# EE-04: Entry point whitelist validation — same check as load_ee_plugins()
if ep.value != "ee.plugin:EEPlugin":
    raise RuntimeError(
        f"Untrusted axiom.ee entry point: '{ep.value}' — expected 'ee.plugin:EEPlugin'"
    )
```

2. Place check immediately before `ep.load()` call, exactly matching the check in load_ee_plugins().

3. Existing error handling in activate_ee_live() (logger.error before returning None or remounting stubs) must be preserved.

4. Ensure both code paths have identical error messages and behavior.

Important: Both load_ee_plugins() and activate_ee_live() must have this check. Forgetting one creates a security gap (RESEARCH.md Pitfall #3).
  </action>
  <verify>
    <automated>cd puppeteer && grep -c "EE-04: Entry point whitelist" agent_service/ee/__init__.py | grep -q "^2$" && echo "PASS: Entry point whitelist checks in both paths" || echo "FAIL: Not in both paths"</automated>
  </verify>
  <done>
Entry point whitelist validation in activate_ee_live():
- Whitelist check added before ep.load()
- Error message matches load_ee_plugins()
- Check appears in both load_ee_plugins() and activate_ee_live() (verified by count)
- Live-reload path now enforces whitelist identically to startup
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write test infrastructure for ENCRYPTION_KEY hard requirement (Wave 0)</name>
  <files>puppeteer/tests/test_encryption_key_enforcement.py</files>
  <behavior>
Tests for EE-06 (ENCRYPTION_KEY hard requirement):

1. **Test: ENCRYPTION_KEY required at module load**
   - Input: os.environ["ENCRYPTION_KEY"] is absent
   - Expected: Importing security module raises RuntimeError
   - Error message must contain "ENCRYPTION_KEY environment variable is required"

2. **Test: ENCRYPTION_KEY absent raises RuntimeError**
   - Input: Call _load_or_generate_encryption_key() with empty env var
   - Expected: RuntimeError raised immediately
   - Error message contains exact required phrases

3. **Test: ENCRYPTION_KEY error message includes generation command**
   - Input: env var absent
   - Expected: Error message includes "python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'"
   - Actionability: Operator can copy-paste command to generate key

Additional checks (not separate tests but part of above):
- When ENCRYPTION_KEY is set, module loads successfully
- cipher_suite is initialized correctly when ENCRYPTION_KEY valid
  </behavior>
  <action>
Create `/home/thomas/Development/master_of_puppets/puppeteer/tests/test_encryption_key_enforcement.py` with pytest-based unit tests:

1. Use `unittest.mock.patch` to mock `os.getenv()` and simulate missing ENCRYPTION_KEY
2. Use `importlib.reload()` to reload security module with modified environment
3. Test structure pattern (existing in codebase):
   - Import pytest, unittest.mock, and os at top
   - Define test functions with `def test_*()` names
   - Use `pytest.raises(RuntimeError)` to assert exception type
   - Check exception message with `.match()` regex or exception str

4. Create 3 test functions covering:
   - test_encryption_key_required() — verifies module import fails when env var absent
   - test_encryption_key_absent_raises() — verifies _load_or_generate_encryption_key() raises RuntimeError
   - test_encryption_key_error_message() — verifies error message content (includes generation command)

5. Add fixtures if needed (conftest.py patterns already exist in codebase — can reference):
   - Fixture to save/restore ENCRYPTION_KEY env var across tests
   - Fixture to reload security module cleanly

6. Follow existing test file style (see test_ee_manifest.py, test_tools.py patterns):
   - Docstrings on functions
   - Clear assert statements
   - No extraneous dependencies

7. Tests should pass when security.py is correctly implemented (Task 1).
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_encryption_key_enforcement.py -xvs 2>&1 | tail -5</automated>
  </verify>
  <done>
Test infrastructure for ENCRYPTION_KEY enforcement:
- test_encryption_key_enforcement.py created with 3 unit tests
- All tests focused on EE-06 requirement
- Tests verify RuntimeError raised when ENCRYPTION_KEY absent
- Tests verify error message content (generation command included)
- Tests can be run independently: `pytest tests/test_encryption_key_enforcement.py -xvs`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Write test infrastructure for entry point whitelist validation (Wave 0)</name>
  <files>puppeteer/tests/test_ee_manifest.py</files>
  <behavior>
Extend existing test_ee_manifest.py with tests for EE-04 (entry point whitelist):

1. **Test: Entry point whitelist at startup**
   - Input: load_ee_plugins() with mocked entry_points() returning trusted value
   - Expected: ep.value == "ee.plugin:EEPlugin" → plugin loads successfully, no RuntimeError

2. **Test: Untrusted entry point raises RuntimeError at startup**
   - Input: load_ee_plugins() with mocked entry_points() returning ep.value = "evil.module:Malware"
   - Expected: RuntimeError raised during plugin load, message includes actual ep.value

3. **Test: Entry point whitelist at live-reload**
   - Input: activate_ee_live() with mocked entry_points() returning trusted value
   - Expected: Plugin loads in live-reload path, no RuntimeError

4. **Test: Untrusted entry point raises RuntimeError at live-reload**
   - Input: activate_ee_live() with mocked entry_points() returning ep.value = "evil.module:BadCode"
   - Expected: RuntimeError raised, message includes actual ep.value

Additional coverage (not separate tests):
- Verify error message format: "Untrusted axiom.ee entry point: '{ep.value}' — expected 'ee.plugin:EEPlugin'"
- Verify both startup and live-reload paths have identical behavior
  </behavior>
  <action>
Update existing `/home/thomas/Development/master_of_puppets/puppeteer/tests/test_ee_manifest.py`:

1. Add a new test class for entry point validation (e.g., `TestEntryPointWhitelist`):
   ```python
   class TestEntryPointWhitelist:
       """Tests for EE-04: Entry point whitelist validation"""
   ```

2. Create fixtures to mock entry_points() with controllable return values:
   - Mock `importlib.metadata.entry_points()` using `unittest.mock.patch`
   - Create EntryPoint-like objects with configurable `.value` and `.name` attributes
   - Can use `Mock` objects with `value` and `name` set as properties

3. Create 4 test functions:
   - `test_entry_point_whitelist_startup()` — trusted value, load_ee_plugins() succeeds
   - `test_untrusted_entry_point_startup()` — untrusted value, RuntimeError raised
   - `test_entry_point_whitelist_live_reload()` — trusted value, activate_ee_live() succeeds
   - `test_untrusted_entry_point_live_reload()` — untrusted value, RuntimeError raised

4. For the async functions (load_ee_plugins and activate_ee_live), use `pytest.mark.asyncio` decorator (or `pytest-asyncio` if not already present):
   ```python
   @pytest.mark.asyncio
   async def test_entry_point_whitelist_startup():
   ```

5. Use `pytest.raises(RuntimeError)` to assert exception:
   ```python
   with pytest.raises(RuntimeError, match="Untrusted axiom.ee entry point"):
       await load_ee_plugins(mock_app, mock_engine)
   ```

6. Preserve existing tests in test_ee_manifest.py (for EE-01 manifest verification) — only add new class for entry point tests.

7. Tests should pass when ee/__init__.py is correctly implemented (Tasks 2 & 3).
  </action>
  <verify>
    <automated>cd puppeteer && pytest tests/test_ee_manifest.py::TestEntryPointWhitelist -xvs 2>&1 | tail -10</automated>
  </verify>
  <done>
Test infrastructure for entry point whitelist validation:
- TestEntryPointWhitelist test class added to test_ee_manifest.py
- 4 unit tests covering startup + live-reload paths
- Both trusted and untrusted entry points tested
- Mocks for entry_points() correctly return controllable EntryPoint objects
- Tests verify RuntimeError raised with correct message format
- Tests can be run: `pytest tests/test_ee_manifest.py::TestEntryPointWhitelist -xvs`
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run comprehensive validation:

1. **Module import test** — Verify security.py enforcement at import time:
   ```bash
   cd puppeteer && ENCRYPTION_KEY= python -c "from agent_service.security import ENCRYPTION_KEY" 2>&1 | grep -q "required" && echo "PASS" || echo "FAIL"
   ```

2. **Code inspection** — Verify whitelist checks in both code paths:
   ```bash
   cd puppeteer && grep -c "EE-04" agent_service/ee/__init__.py
   # Expected: 2 (one in load_ee_plugins, one in activate_ee_live)
   ```

3. **Test suite** — Run all new and modified tests:
   ```bash
   cd puppeteer && pytest tests/test_encryption_key_enforcement.py tests/test_ee_manifest.py::TestEntryPointWhitelist -xvs
   ```

4. **Existing test compatibility** — Ensure no regression in existing tests:
   ```bash
   cd puppeteer && pytest tests/test_ee_manifest.py -x
   ```

5. **Full test suite** — Verify no side effects on other modules:
   ```bash
   cd puppeteer && pytest tests/ -x --tb=short -q
   ```

6. **Functional test** — Verify server still starts with valid ENCRYPTION_KEY:
   ```bash
   cd puppeteer && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") python -m agent_service.main &
   sleep 3 && curl http://localhost:8001/admin/health && kill %1
   ```
</verification>

<success_criteria>
Phase 139 Plan 01 is complete when:

1. **ENCRYPTION_KEY Enforcement (EE-06)**
   - `_load_or_generate_encryption_key()` raises RuntimeError if env var absent
   - Error message includes exact phrases: "required but not set" and Fernet generation command
   - Module import fails at earliest point (before routes register)
   - 3 unit tests in test_encryption_key_enforcement.py all pass

2. **Entry Point Whitelist (EE-04)**
   - `load_ee_plugins()` has whitelist check with RuntimeError on mismatch
   - `activate_ee_live()` has identical whitelist check
   - Error message format: "Untrusted axiom.ee entry point: '{ep.value}' — expected 'ee.plugin:EEPlugin'"
   - 4 unit tests in TestEntryPointWhitelist all pass (2 startup, 2 live-reload)

3. **Test Infrastructure**
   - test_encryption_key_enforcement.py created and passes
   - test_ee_manifest.py updated with TestEntryPointWhitelist class and passes
   - No regression in existing tests
   - All tests automated (no manual verification needed)

4. **Requirements Satisfaction**
   - EE-04: Entry point validation implemented in both code paths
   - EE-06: ENCRYPTION_KEY hard requirement with no fallbacks

5. **Code Quality**
   - Error messages actionable and include debugging information
   - Comments link changes to requirement IDs (EE-04, EE-06)
   - No breaking changes to existing security.py functions (encrypt_secret, decrypt_secret, etc.)
   - Consistent use of RuntimeError for security violations (matches existing ee/__init__.py patterns)
</success_criteria>

<output>
After completion, create `.planning/phases/139-entry-point-whitelist-enforcement/139-01-SUMMARY.md`

Summary should include:
- Tasks completed: 5 (Tasks 1-5)
- ENCRYPTION_KEY enforcement: ✓ implemented with hard RuntimeError
- Entry point whitelist: ✓ implemented in load_ee_plugins() and activate_ee_live()
- Test coverage: ✓ 3 ENCRYPTION_KEY tests + 4 entry point tests (7 total)
- Requirements satisfied: ✓ EE-04, EE-06
- Verification: Full test suite passes
</output>
