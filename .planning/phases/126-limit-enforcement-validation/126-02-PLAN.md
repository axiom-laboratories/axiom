---
phase: 126-limit-enforcement-validation
plan: 02
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - mop_validation/reports/stress_test_docker_{timestamp}.json
  - mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md
autonomous: true
requirements: [ENFC-01, ENFC-02]
user_setup: []

must_haves:
  truths:
    - "Memory limit enforcement test (exit 137 on OOM) passes on Docker runtime"
    - "CPU limit enforcement test (ratio < 0.8) passes on Docker runtime"
    - "Validation report documents Docker runtime results per scenario per language"
    - "Core enforcement requirements (ENFC-01, ENFC-02) verified on Docker"
  artifacts:
    - path: "mop_validation/reports/stress_test_docker_{timestamp}.json"
      provides: "Raw JSON test results from orchestrator running against Docker nodes"
      min_lines: 50
    - path: "mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md"
      provides: "Human-readable Docker validation report with per-scenario pass/fail tables"
      min_lines: 40
  key_links:
    - from: "orchestrate_stress_tests.py --runtime docker"
      to: "/nodes endpoint"
      via: "Filter nodes by execution_mode='docker' and cgroup_version='v2'"
      pattern: "nodes filtered before dispatch"
    - from: "stress test results"
      to: "ENFC-01 verification"
      via: "memory_hog exit_code == 137"
      pattern: "exit_code in job.stdout JSON"
    - from: "stress test results"
      to: "ENFC-02 verification"
      via: "cpu_burn ratio < 0.8"
      pattern: "ratio field in job.stdout JSON"
---

<objective>
Run the enhanced Phase 125 orchestrator against Docker nodes and document memory + CPU limit enforcement validation results.

Purpose: Establish baseline proof that resource limits are enforced correctly on the standard Docker runtime. This unblocks Phase 127 progression and provides Docker validation data independently of Podman enrollment status.

Output:
- mop_validation/reports/stress_test_docker_{timestamp}.json — raw orchestrator output (4 scenarios, 3 languages each)
- mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md — human-readable summary with per-scenario tables and ENFC-01/ENFC-02 status
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-limit-enforcement-validation/126-CONTEXT.md
@.planning/phases/126-limit-enforcement-validation/126-RESEARCH.md
@.planning/phases/125-stress-test-corpus/125-04-SUMMARY.md

# Enhanced orchestrator from Plan 01
@mop_validation/scripts/stress/orchestrate_stress_tests.py (with --runtime flag)
@mop_validation/scripts/stress/preflight_check.py
@mop_validation/scripts/stress/python/
@mop_validation/scripts/stress/bash/
@mop_validation/scripts/stress/pwsh/

# API endpoints
@puppeteer/agent_service/main.py (/dispatch, /nodes, /jobs/{id})
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Docker-only orchestrator validation</name>
  <files>mop_validation/reports/stress_test_docker_{timestamp}.json</files>
  <action>
Execute the enhanced orchestrator against Docker nodes, filtering by execution_mode='docker' and cgroup_version='v2'.

**Preconditions:**
- Docker nodes already running and healthy (assumed from prior phases)
- Enhanced orchestrator from Plan 01 deployed with --runtime flag
- Stress test scripts (Phase 125) available at mop_validation/scripts/stress/

**Execution:**

1. **Verify Docker nodes are available and healthy:**
```bash
# Check that at least one Docker node exists and reports execution_mode='docker'
curl -s -k -H "Authorization: Bearer $(cat ~/.mop_auth_token)" \
  https://localhost:8001/nodes | jq '.[] | select(.execution_mode == "docker") | .id'
# Expected output: at least one node ID
```

2. **Run preflight check on Docker nodes:**
```bash
# Validates cgroup v2 is present and controllers are enabled
python3 mop_validation/scripts/stress/preflight_check.py \
  --runtime docker \
  --server https://localhost:8001
# Expected: All checks pass for Docker nodes
```

3. **Run full orchestrator against Docker nodes:**
```bash
cd mop_validation/scripts/stress

python3 orchestrate_stress_tests.py \
  --runtime docker \
  --server https://localhost:8001 \
  --token-file ~/.mop_auth_token \
  --timeout 120

# Output: mop_validation/reports/stress_test_docker_{timestamp}.json
# Console output: Progress table showing 4 scenarios × 3 languages = 12 tests per scenario
```

**Expected JSON report structure:**
```json
{
  "timestamp": "2026-04-09T21:30:00.000Z",
  "runtime": "docker",
  "server": "https://localhost:8001",
  "total_nodes": 1,
  "preflight": {
    "total_nodes": 1,
    "passed": 1,
    "failed": 0,
    "skipped": 0,
    "skipped_details": []
  },
  "scenarios": [
    {
      "name": "single_cpu_burn",
      "results": [
        {"language": "python", "pass": true, "details": "ratio=0.50"},
        {"language": "bash", "pass": true, "details": "ratio=0.48"},
        {"language": "powershell", "pass": true, "details": "ratio=0.52"}
      ]
    },
    {
      "name": "single_memory_oom",
      "results": [
        {"language": "python", "pass": true, "details": "exit_code=137"},
        {"language": "bash", "pass": true, "details": "exit_code=137"},
        {"language": "powershell", "pass": true, "details": "exit_code=137"}
      ]
    },
    {
      "name": "concurrent_isolation",
      "results": [
        {"language": "python", "pass": true, "details": "max_drift=0.15s"},
        {"language": "bash", "pass": true, "details": "max_drift=0.12s"},
        {"language": "powershell", "pass": true, "details": "max_drift=0.18s"}
      ]
    },
    {
      "name": "all_language_sweep",
      "results": [
        {"language": "python", "pass": true, "details": "cpu_burn + memory_hog pass"},
        {"language": "bash", "pass": true, "details": "cpu_burn + memory_hog pass"},
        {"language": "powershell", "pass": true, "details": "cpu_burn + memory_hog pass"}
      ]
    }
  ],
  "summary": {
    "total_tests": 48,
    "passed": 48,
    "failed": 0,
    "notes": "All core enforcement tests (ENFC-01: memory OOMKill, ENFC-02: CPU throttle) passed on Docker runtime"
  }
}
```

**Success criteria for this task:**
- JSON report exists at correct path
- Runtime field = 'docker'
- Preflight section shows 1 passed, 0 failed
- All 4 scenarios present
- Scenario 1 (cpu_burn): all 3 languages with ratio < 0.8 (ENFC-02)
- Scenario 2 (memory_oom): all 3 languages with exit_code == 137 (ENFC-01)
- Scenario 3 (concurrent): all 3 languages with max_drift passed
- Scenario 4 (sweep): all 3 languages passed
- Total_tests and passed counts match

**Troubleshooting:**

If orchestrator timeout or node unavailable:
- Verify Puppeteer service is running: `docker compose -f puppeteer/compose.server.yaml ps`
- Verify nodes are reporting: `GET /nodes` returns non-empty list
- Verify node execution_mode field: `curl https://localhost:8001/nodes | jq '.[] | .execution_mode'` should show 'docker'
- If field missing, node.py heartbeat not reporting execution_mode — this is a framework gap from Plan 01, not test failure
  </action>
  <verify>
    <automated>
      # Verify JSON report exists and contains expected fields
      if [ -f "$(ls -t mop_validation/reports/stress_test_docker_*.json 2>/dev/null | head -1)" ]; then
        REPORT=$(ls -t mop_validation/reports/stress_test_docker_*.json | head -1)
        python3 << 'EOF'
import json
with open('$REPORT') as f:
    data = json.load(f)
    assert data.get('runtime') == 'docker', f"runtime field: {data.get('runtime')}"
    assert 'preflight' in data, "preflight section missing"
    assert len(data.get('scenarios', [])) >= 2, "need at least 2 scenarios"
    # Check ENFC-01: memory OOMKill (scenario 2 should have exit_code=137)
    mem_scenario = [s for s in data['scenarios'] if 'memory' in s.get('name', '')][0]
    mem_results = mem_scenario.get('results', [])
    for result in mem_results:
        assert result.get('pass'), f"Memory OOMKill failed for {result.get('language')}"
    # Check ENFC-02: CPU throttle (scenario 1 should have ratio < 0.8)
    cpu_scenario = [s for s in data['scenarios'] if 'cpu' in s.get('name', '')][0]
    cpu_results = cpu_scenario.get('results', [])
    for result in cpu_results:
        assert result.get('pass'), f"CPU throttle failed for {result.get('language')}"
    print('✓ Docker validation report valid: ENFC-01 (memory) and ENFC-02 (CPU) both pass')
EOF
      else
        echo "✗ No Docker stress_test JSON report found"
        exit 1
      fi
    </automated>
  </verify>
  <done>
- Docker orchestrator validation executed against Docker nodes filtered by execution_mode='docker' + cgroup_version='v2'
- JSON report generated at mop_validation/reports/stress_test_docker_{timestamp}.json
- Report includes preflight section, 4 scenarios, all test results
- ENFC-01 (memory OOMKill): all languages exit with code 137 on Docker
- ENFC-02 (CPU throttle): all languages show ratio < 0.8 on Docker
- Report committed to repo
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate Docker-only validation report</name>
  <files>mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md</files>
  <action>
Parse the Docker JSON report and create a human-readable markdown summary documenting memory and CPU limit enforcement validation results.

**Input:** mop_validation/reports/stress_test_docker_*.json (latest file from Task 1)

**Create:** mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md

**Report structure:**

```markdown
# Limit Enforcement Validation — Docker Runtime

**Generated:** {timestamp from JSON}
**Runtime:** Docker
**Scope:** Memory and CPU limit enforcement validation (cgroup v2, Docker nodes only)
**Phase:** 126

## Summary

- **Total scenarios tested:** 4
- **Total test cases:** 48 (4 scenarios × 3 languages + preflight)
- **Docker nodes tested:** {count from preflight.total_nodes}
- **Preflight checks passed:** {preflight.passed}/{preflight.total_nodes}
- **Skipped nodes:** {preflight.skipped} (reason: {v1/unsupported})

## Core Enforcement Requirements

| Requirement | Status | Evidence |
|-------------|--------|----------|
| ENFC-01: Memory limit triggers OOMKill (exit 137) | ✅ PASS | All memory_hog tests on Docker return exit_code=137 |
| ENFC-02: CPU limit caps cores (ratio < 0.8) | ✅ PASS | All cpu_burn tests on Docker show ratio < 0.8 |

**Conclusion:** Core enforcement requirements verified on Docker runtime. Ready for production validation.

## Docker Runtime — Scenario Results

### Scenario 1: Single CPU Burn (cpu_limit=0.5)

| Language | Result | Ratio | Notes |
|----------|--------|-------|-------|
| Python | ✅ PASS | {ratio} | CPU throttled as expected |
| Bash | ✅ PASS | {ratio} | CPU throttled as expected |
| PowerShell | ✅ PASS | {ratio} | CPU throttled as expected |

**ENFC-02 Status:** ✅ **PASS** — All languages demonstrate CPU throttling under 0.5 core limit.

### Scenario 2: Single Memory OOM (memory_limit=128M)

| Language | Result | Exit Code | Notes |
|----------|--------|-----------|-------|
| Python | ✅ PASS | 137 | OOMKill triggered |
| Bash | ✅ PASS | 137 | OOMKill triggered |
| PowerShell | ✅ PASS | 137 | OOMKill triggered |

**ENFC-01 Status:** ✅ **PASS** — All languages trigger OOMKill at 128M memory limit.

### Scenario 3: Concurrent Isolation

| Language | Result | Max Sleep Drift | Status |
|----------|--------|-----------------|--------|
| Python | ✅ PASS | {max_drift}s | No interference between concurrent scripts |
| Bash | ✅ PASS | {max_drift}s | No interference between concurrent scripts |
| PowerShell | ✅ PASS | {max_drift}s | No interference between concurrent scripts |

**Isolation Status:** ✅ **PASS** — Concurrent jobs do not interfere with each other on Docker.

### Scenario 4: All-Language Sweep

| Language | CPU Burn | Memory OOM | Result |
|----------|----------|-----------|--------|
| Python | ✅ PASS | ✅ PASS | ✅ PASS |
| Bash | ✅ PASS | ✅ PASS | ✅ PASS |
| PowerShell | ✅ PASS | ✅ PASS | ✅ PASS |

**Sweep Status:** ✅ **PASS** — All scripting languages pass both CPU and memory enforcement tests.

## Preflight Check Details

**Cgroup Detection:**
- Version detected: v2
- Controllers enabled: memory, cpu
- Own limit readback: ✅ Present

**Nodes Tested:**
- Total: {total_nodes}
- Passed preflight: {passed}
- Failed preflight: {failed}
- Skipped: {skipped}

Skipped nodes (if any):
- {node_id}: {reason}

## Key Findings

1. **Docker enforcement is solid:** All core requirements (OOMKill, CPU throttling) work as designed across all scripting languages.
2. **No unexpected failures:** No enforcement gaps or runtime-specific issues discovered on Docker.
3. **Cgroup v2 fully supported:** Controllers are enabled and enforced correctly.
4. **Language parity:** Python, Bash, and PowerShell all behave identically under resource constraints.

## Conclusion

**Phase 126 Docker validation: COMPLETE**

Memory and CPU limit enforcement is verified and working correctly on Docker runtime. All core requirements (ENFC-01, ENFC-02) are met. This establishes Docker as a baseline for limit enforcement validation.

---

**Raw data:** stress_test_docker_{timestamp}.json
**Generated:** {timestamp}
**Verifier:** gsd-executor
```

**Implementation notes:**

1. Parse the JSON report from Task 1
2. Extract: timestamp, runtime, preflight counts, scenario results
3. For each scenario, extract: language, pass/fail, key metric (ratio or exit_code or drift)
4. Fill tables with actual data from JSON
5. Summarize core requirements (ENFC-01 and ENFC-02) with pass/fail status
6. Document findings (no issues expected; note if any unexpected results)
7. Write file to mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md
  </action>
  <verify>
    <automated>
      # Verify markdown report exists and contains required sections
      if [ -f mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md ]; then
        REPORT="mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md"
        grep -q "ENFC-01" "$REPORT" || { echo "ENFC-01 missing"; exit 1; }
        grep -q "ENFC-02" "$REPORT" || { echo "ENFC-02 missing"; exit 1; }
        grep -q "Scenario 1: Single CPU Burn" "$REPORT" || { echo "CPU scenario missing"; exit 1; }
        grep -q "Scenario 2: Single Memory OOM" "$REPORT" || { echo "Memory scenario missing"; exit 1; }
        grep -q "PASS" "$REPORT" || { echo "No PASS status found"; exit 1; }
        echo "✓ Docker validation report complete with ENFC-01/02 status"
      else
        echo "✗ LIMIT_ENFORCEMENT_DOCKER.md not found"
        exit 1
      fi
    </automated>
  </verify>
  <done>
- mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md created with human-readable validation results
- Report includes per-scenario tables (CPU, memory, isolation, sweep) with results per language
- Core requirements section shows ENFC-01 (memory) and ENFC-02 (CPU) status
- Preflight check details documented (cgroup v2 detection, controllers, node count)
- Key findings summarized (no enforcement gaps on Docker, language parity verified)
- Conclusion confirms phase completion for Docker runtime
- File committed to repo
  </done>
</task>

</tasks>

<verification>
After completion of all tasks:
1. Docker orchestrator validation executed: `ls -lh mop_validation/reports/stress_test_docker_*.json`
2. JSON report valid and parseable: `jq '.runtime' mop_validation/reports/stress_test_docker_*.json` returns 'docker'
3. ENFC-01 verified (memory): `jq '.scenarios[] | select(.name | contains("memory")) | .results[] | .pass' mop_validation/reports/stress_test_docker_*.json` all true
4. ENFC-02 verified (CPU): `jq '.scenarios[] | select(.name | contains("cpu")) | .results[] | .pass' mop_validation/reports/stress_test_docker_*.json` all true
5. Markdown report exists: `cat mop_validation/reports/LIMIT_ENFORCEMENT_DOCKER.md` shows ENFC-01 PASS, ENFC-02 PASS
6. Both files committed: `git log --oneline -2 | grep "Docker\|enforcement"`
</verification>

<success_criteria>
Plan 02 complete when:
- [ ] Docker orchestrator validation executed against Docker nodes (filtered by execution_mode='docker')
- [ ] stress_test_docker_{timestamp}.json created with valid JSON structure
- [ ] Preflight check passed on all Docker nodes
- [ ] ENFC-01 (memory OOMKill) verified: all memory_hog tests show exit_code=137
- [ ] ENFC-02 (CPU throttle) verified: all cpu_burn tests show ratio < 0.8
- [ ] LIMIT_ENFORCEMENT_DOCKER.md created with per-scenario tables and core requirement status
- [ ] Both JSON and markdown reports committed to repo
- [ ] Plan 03 can proceed (Podman enrollment troubleshooting + final dual-runtime report)
</success_criteria>

<output>
After completion, update `.planning/STATE.md` with:
- Plan 02 status: COMPLETE
- Docker validation results: ENFC-01 PASS, ENFC-02 PASS
- Remaining work: Plan 03 (Podman enrollment + dual-runtime report)
</output>
