---
phase: 124-ephemeral-execution-guarantee
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/db.py
  - puppeteer/agent_service/models.py
  - puppeteer/agent_service/services/job_service.py
  - puppeteer/migration_v14.sql
autonomous: true
requirements:
  - EPHR-01
  - EPHR-02

must_haves:
  truths:
    - "Node table persists execution_mode reported from heartbeat"
    - "HeartbeatPayload accepts optional execution_mode field (backward compatible)"
    - "NodeResponse exposes execution_mode to API consumers"
    - "Heartbeat handler updates node.execution_mode on every heartbeat"
  artifacts:
    - path: "puppeteer/agent_service/db.py"
      provides: "Node model with execution_mode column"
      min_lines: 5
    - path: "puppeteer/agent_service/models.py"
      provides: "HeartbeatPayload + NodeResponse with execution_mode field"
      min_lines: 3
    - path: "puppeteer/agent_service/services/job_service.py"
      provides: "handle_heartbeat() updates node.execution_mode"
      min_lines: 2
    - path: "puppeteer/migration_v14.sql"
      provides: "ALTER TABLE nodes ADD execution_mode TEXT"
  key_links:
    - from: "puppets/environment_service/node.py"
      to: "puppeteer/agent_service/db.py"
      via: "heartbeat POST with execution_mode → DB column"
      pattern: "node.execution_mode = .*"
    - from: "puppeteer/agent_service/services/job_service.py"
      to: "puppeteer/agent_service/db.py"
      via: "handle_heartbeat() persists execution_mode"
      pattern: "hb.execution_mode"
    - from: "puppeteer/agent_service/main.py"
      to: "puppeteer/agent_service/models.py"
      via: "get_nodes() endpoint constructs NodeResponse with execution_mode"
      pattern: "execution_mode.*n.execution_mode"
---

<objective>
Implement server-side persistence of node execution mode (docker/podman detection).

Purpose: Nodes report their detected runtime in every heartbeat; orchestrator stores this for dashboard visibility and later validation logic.

Output: Database schema + API models + heartbeat handler all extended with execution_mode field, following the Phase 123 cgroup detection pattern.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/124-ephemeral-execution-guarantee/124-CONTEXT.md
@.planning/phases/124-ephemeral-execution-guarantee/124-RESEARCH.md
@.planning/phases/123-cgroup-detection-backend/123-03-SUMMARY.md

## Integration Points

**Node heartbeat (puppets/environment_service/node.py):**
- Line 480: `self.runtime_engine = runtime.ContainerRuntime()` — already instantiated
- Line 424-433: heartbeat payload dict — will add `"execution_mode": self.runtime_engine.runtime`

**Backend models (puppeteer/agent_service/models.py):**
- HeartbeatPayload (line 162): add execution_mode field
- NodeResponse (line 201): add execution_mode field

**DB schema (puppeteer/agent_service/db.py):**
- Node model (line 127): add execution_mode column

**Heartbeat handler (puppeteer/agent_service/services/job_service.py):**
- handle_heartbeat() (line 944-950): update node.execution_mode

## Key Patterns (From Phase 123)

```
Node startup detects runtime → heartbeat includes detection → orchestrator persists to DB → API exposes
```

HeartbeatPayload already extends for cgroup detection (Phase 123):
```python
detected_cgroup_version: Optional[str] = None
cgroup_raw: Optional[str] = None
```

This plan adds:
```python
execution_mode: Optional[str] = None
```

Same pattern: optional string, backward compatible, added to DB column, updated in heartbeat handler.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add execution_mode column to Node DB model and migration</name>
  <files>puppeteer/agent_service/db.py, puppeteer/migration_v14.sql</files>
  <action>
1. Edit `puppeteer/agent_service/db.py` Node model (around line 127 where detected_cgroup_version exists)
   - Add column: `execution_mode: Mapped[Optional[str]] = mapped_column(String, nullable=True)`
   - Place right after cgroup-related columns for consistency

2. Create migration file `puppeteer/migration_v14.sql` with:
   ```sql
   -- Phase 124: Server-side execution_mode visibility
   ALTER TABLE nodes ADD COLUMN IF NOT EXISTS execution_mode TEXT;
   ```

   Use IF NOT EXISTS for idempotency (follows Phase 123 migration pattern)

**Why this order:** DB schema must exist before heartbeat handler can write to it.
  </action>
  <verify>
    <automated>grep -n "execution_mode.*Mapped.*Optional" puppeteer/agent_service/db.py && grep -n "ALTER TABLE nodes ADD COLUMN IF NOT EXISTS execution_mode" puppeteer/migration_v14.sql</automated>
  </verify>
  <done>
    - Node model includes execution_mode column (optional, String type)
    - Migration file exists with idempotent ALTER TABLE statement
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend HeartbeatPayload and NodeResponse models with execution_mode field</name>
  <files>puppeteer/agent_service/models.py</files>
  <action>
1. Find `class HeartbeatPayload` (around line 162)
   - Add after detected_cgroup_version and cgroup_raw fields:
     `execution_mode: Optional[str] = None  # NEW: Phase 124`
   - Comment matches style used for Phase 123 additions

2. Find `class NodeResponse` (around line 201)
   - Add after detected_cgroup_version field:
     `execution_mode: Optional[str] = None  # NEW: Phase 124`

Both additions are `Optional[str] = None` for backward compatibility with nodes that haven't upgraded.

**Why optional:** Old nodes running before this upgrade won't send execution_mode in heartbeat. API consumers (dashboard) may not have it for those nodes yet (will show null/undefined, handled by frontend).
  </action>
  <verify>
    <automated>grep -A 3 "class HeartbeatPayload" puppeteer/agent_service/models.py | grep "execution_mode" && grep -A 3 "class NodeResponse" puppeteer/agent_service/models.py | grep "execution_mode"</automated>
  </verify>
  <done>
    - HeartbeatPayload has execution_mode field
    - NodeResponse has execution_mode field
    - Both are Optional[str] with default None
  </done>
</task>

<task type="auto">
  <name>Task 3: Update heartbeat handler to persist execution_mode from payload to DB</name>
  <files>puppeteer/agent_service/services/job_service.py</files>
  <action>
1. Find `handle_heartbeat()` function (around line 944-950)
   - Locate where detected_cgroup_version is updated: `node.detected_cgroup_version = hb.detected_cgroup_version`
   - Add right after those updates:
     `node.execution_mode = hb.execution_mode  # Phase 124`

2. Verify the function commits the transaction (it should already, from Phase 123)
   - Look for `await db.commit()` at end of function

**Why this location:** Heartbeat handler already batches all node updates and commits once. Adding one more field is additive, no transaction changes needed.

**Backward compatibility:** If hb.execution_mode is None (old node), node.execution_mode will be set to None (no error). If hb.execution_mode is "docker" or "podman" (new node), it persists correctly.
  </action>
  <verify>
    <automated>grep -n "node.execution_mode = hb.execution_mode" puppeteer/agent_service/services/job_service.py</automated>
  </verify>
  <done>
    - handle_heartbeat() updates node.execution_mode from heartbeat payload
    - Update placed alongside other heartbeat field assignments
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend get_nodes() endpoint to include execution_mode in NodeResponse</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
1. Find `get_nodes()` endpoint (around line 1649-1751)
   - Locate where the response dict is built, specifically where detected_cgroup_version is added:
     ```python
     "detected_cgroup_version": n.detected_cgroup_version,
     ```
   - Add right after:
     ```python
     "execution_mode": n.execution_mode,  # Phase 124
     ```

2. Confirm the endpoint already returns a list of NodeResponse dicts (it should, from Phase 123 cgroup addition)

**Why:** get_nodes() constructs response dicts from Node ORM objects. Adding one more field is a one-liner.

**API contract:** Dashboard can now consume execution_mode from NodeResponse and render Docker/Podman badges (done in Phase 127).
  </action>
  <verify>
    <automated>grep -n '"execution_mode": n.execution_mode' puppeteer/agent_service/main.py</automated>
  </verify>
  <done>
    - get_nodes() endpoint includes execution_mode in response dict
    - Field exposed as "execution_mode" (snake_case) in JSON API
  </done>
</task>

</tasks>

<verification>
**Wave 1 Verification (Backend Models):**

1. DB schema change:
   ```bash
   cd puppeteer && grep -n "execution_mode.*Mapped.*Optional" agent_service/db.py
   ```
   Expected: One line matching Node.execution_mode column definition

2. Models extended:
   ```bash
   cd puppeteer && grep -n "execution_mode.*Optional\[str\]" agent_service/models.py
   ```
   Expected: Two matches (HeartbeatPayload + NodeResponse)

3. Heartbeat handler updated:
   ```bash
   cd puppeteer && grep -n "node.execution_mode = hb.execution_mode" agent_service/services/job_service.py
   ```
   Expected: One match in handle_heartbeat()

4. API endpoint updated:
   ```bash
   cd puppeteer && grep -n '"execution_mode": n.execution_mode' agent_service/main.py
   ```
   Expected: One match in get_nodes()

5. Migration file exists:
   ```bash
   ls -la puppeteer/migration_v14.sql
   ```
   Expected: File exists with IF NOT EXISTS clause
</verification>

<success_criteria>
- [x] Node table has execution_mode column (nullable String)
- [x] HeartbeatPayload accepts execution_mode field (Optional[str])
- [x] NodeResponse exposes execution_mode field (Optional[str])
- [x] Heartbeat handler persists execution_mode to database
- [x] get_nodes() API endpoint returns execution_mode in response dict
- [x] All changes are backward compatible (Optional fields, nullable column)
- [x] Migration file created with idempotent ALTER TABLE
</success_criteria>

<output>
After completion, create `.planning/phases/124-ephemeral-execution-guarantee/124-01-SUMMARY.md`
</output>
