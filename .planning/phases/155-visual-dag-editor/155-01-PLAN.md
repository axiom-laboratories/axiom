---
phase: 155-visual-dag-editor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [src/utils/dagValidation.ts, src/utils/__tests__/dagValidation.test.tsx, src/components/WorkflowNodePalette.tsx, src/components/__tests__/WorkflowNodePalette.test.tsx, src/components/ScriptNodeJobSelector.tsx, src/components/__tests__/ScriptNodeJobSelector.test.tsx, src/components/IfGateConfigDrawer.tsx, src/components/__tests__/IfGateConfigDrawer.test.tsx, src/hooks/useDAGValidation.ts, src/hooks/__tests__/useDAGValidation.test.tsx, src/hooks/useWorkflowEdit.ts, src/hooks/__tests__/useWorkflowEdit.test.tsx]
autonomous: true
requirements: [UI-06, UI-07]
user_setup: []

must_haves:
  truths:
    - "User can drag node types from a side palette onto the DAG canvas"
    - "Dropped SCRIPT nodes show an unlinked indicator until a job is assigned"
    - "IF gate nodes can be clicked to open a configuration drawer"
    - "Cycle detection runs in real-time as edges are added/removed"
    - "Depth warnings and limits are calculated as the DAG grows"
  artifacts:
    - path: "src/utils/dagValidation.ts"
      provides: "validateDAG() function with DFS cycle detection and depth calculation"
      exports: ["validateDAG", "ValidationResult"]
    - path: "src/components/WorkflowNodePalette.tsx"
      provides: "Draggable node type palette component"
      exports: ["WorkflowNodePalette"]
    - path: "src/components/ScriptNodeJobSelector.tsx"
      provides: "Job search and selection popover for SCRIPT nodes"
      exports: ["ScriptNodeJobSelector"]
    - path: "src/components/IfGateConfigDrawer.tsx"
      provides: "IF gate structured config form in a Sheet drawer"
      exports: ["IfGateConfigDrawer"]
    - path: "src/hooks/useDAGValidation.ts"
      provides: "Reactive validation state management hook"
      exports: ["useDAGValidation"]
    - path: "src/hooks/useWorkflowEdit.ts"
      provides: "Edit mode and node/edge change state management"
      exports: ["useWorkflowEdit"]
  key_links:
    - from: "src/utils/dagValidation.ts"
      to: "src/hooks/useDAGValidation.ts"
      via: "validateDAG() called on every node/edge change"
      pattern: "setValidation.*validateDAG"
    - from: "src/components/WorkflowNodePalette.tsx"
      to: "src/hooks/useWorkflowEdit.ts"
      via: "onNodeAdd callback called when node is dropped"
      pattern: "handleDrop.*nodeAdd"
    - from: "src/components/IfGateConfigDrawer.tsx"
      to: "src/components/DAGCanvas.tsx"
      via: "Integrated via click handler on IF_GATE nodes"
      pattern: "onNodeClick.*IF_GATE"
---

<objective>
Build the foundation for Phase 155 visual DAG editor: create utilities for DAG validation, hooks for edit state management, and reusable components for the node palette, SCRIPT job selector, and IF gate configuration drawer. All functionality is test-first; implementation code is minimal (stubs/skeletons) to unblock Wave 1 integration.

Purpose: Establish test infrastructure and component contracts before wiring them into WorkflowDetail in Wave 1.

Output: 12 new files (6 utilities/hooks + 6 test suites) with comprehensive test coverage. All tests passing, TypeScript compilation clean.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/155-visual-dag-editor/155-CONTEXT.md
@.planning/phases/155-visual-dag-editor/155-RESEARCH.md
@.planning/phases/155-visual-dag-editor/155-VALIDATION.md
@puppeteer/dashboard/src/components/DAGCanvas.tsx
@puppeteer/dashboard/src/views/WorkflowDetail.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DAG Validation Utility — DFS Cycle Detection & Depth Calculation</name>
  <files>src/utils/dagValidation.ts, src/utils/__tests__/dagValidation.test.tsx</files>
  <behavior>
    Test 1: validateDAG() returns isValid=true, hasCycle=false for acyclic DAG (linear chain A→B→C)
    Test 2: validateDAG() detects cycle A→B→C→A; returns hasCycle=true, cycleNodes=["A","B","C"]
    Test 3: validateDAG() returns maxDepth=3 for chain A→B→C (3 nodes deep)
    Test 4: validateDAG() returns depthExceeded=true for depth 31 when maxDepth=30
    Test 5: validateDAG() returns depthExceeded=false for depth 25 (warning level, not error)
    Test 6: validateDAG() handles diamond graph (multiple paths to same node) without false cycles
    Test 7: validateDAG() ignores edges referencing non-existent nodes (orphaned edges)
    Test 8: validateDAG() returns isValid=false if hasCycle=true OR depthExceeded=true
    Test 9: validateDAG() returns isValid=true if hasCycle=false AND depthExceeded=false
    Test 10: calculateMaxDepth() returns 0 for single node (no edges)
    Test 11: calculateMaxDepth() returns 0 for disconnected components
    Test 12: validateDAG() can handle >100 nodes without performance regression
  </behavior>
  <action>
Create src/utils/dagValidation.ts with:
- ValidationResult interface: {isValid, hasCycle, cycleNodes?, maxDepth, depthExceeded}
- validateDAG(nodes: Node[], edges: Edge[], maxDepth: number = 30): ValidationResult
  - Build adjacency list from edges (nodes → targets)
  - DFS cycle detection: track visited + recStack for back edge detection
  - Extract cycle path if found
  - Calculate max depth via memoized DFS from each node
  - Return combined validation result

Create src/utils/__tests__/dagValidation.test.tsx with all 12 test cases using vitest:
- Test utilities: createMockNode(), createMockEdge() for test setup
- Import from src/utils/dagValidation (not yet implemented)
- All tests should FAIL initially (red phase of TDD)
- Verify no imports are broken; keep tests compilable

Implementation code: stub only (return { isValid: false } placeholder)
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm test -- --run src/utils/__tests__/dagValidation.test.tsx 2>&1 | grep -E "(FAIL|PASS|✓|✗)" | head -20</automated>
  </verify>
  <done>All 12 test cases compile and run (currently failing); ValidationResult type and validateDAG() function signatures are exported; no TypeScript errors in test file</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Node Palette Component — Draggable Chips for 6 Node Types</name>
  <files>src/components/WorkflowNodePalette.tsx, src/components/__tests__/WorkflowNodePalette.test.tsx</files>
  <behavior>
    Test 1: WorkflowNodePalette renders all 6 node types (SCRIPT, IF_GATE, AND_JOIN, OR_GATE, PARALLEL, SIGNAL_WAIT) as draggable items
    Test 2: Each draggable item has correct label and icon
    Test 3: Drag start event sets dataTransfer data to 'application/reactflow' with node type string
    Test 4: onNodeAdd callback is called with node type when drag starts
    Test 5: Component renders in a narrow left panel with correct styling (overflow-y auto if needed)
    Test 6: Hovering over a draggable item shows cursor: move
    Test 7: Each node type badge shows a distinct icon (ChevronRight for SCRIPT, GitBranch for IF_GATE, etc.)
    Test 8: onNodeAdd is not called on non-drag interactions (e.g. scroll, click without drag)
  </behavior>
  <action>
Create src/components/WorkflowNodePalette.tsx with:
- Props: {onNodeAdd: (type: string) => void}
- Render narrow left panel (w-32 or similar, scrollable)
- Map over 6 node types: SCRIPT, IF_GATE, AND_JOIN, OR_GATE, PARALLEL, SIGNAL_WAIT
- Each item: draggable={true}, draggable event handlers for drag start/end
- Set dataTransfer.effectAllowed = 'move'
- Call onNodeAdd(type) on drag start
- Use lucide-react icons for visual distinction

Create src/components/__tests__/WorkflowNodePalette.test.tsx with all 8 test cases using vitest + @testing-library/react:
- Render with mock onNodeAdd callback
- Simulate drag events using userEvent.pointer() or mock dataTransfer
- Verify all 6 types render
- Verify drag data is set correctly
- Verify callback is called

Implementation code: stub; render text labels only (no icons yet)
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm test -- --run src/components/__tests__/WorkflowNodePalette.test.tsx 2>&1 | grep -E "(FAIL|PASS|✓|✗)" | head -20</automated>
  </verify>
  <done>All 8 test cases compile and run (currently failing); WorkflowNodePalette component and onNodeAdd prop are exported; no TypeScript errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SCRIPT Job Selector Popover — Job Search & Selection</name>
  <files>src/components/ScriptNodeJobSelector.tsx, src/components/__tests__/ScriptNodeJobSelector.test.tsx</files>
  <behavior>
    Test 1: ScriptNodeJobSelector renders a "Select job" button or trigger when node.scheduled_job_id is null
    Test 2: Clicking trigger opens a popover/drawer with job search input
    Test 3: Typing in search input filters job list (mock jobs: "build", "deploy", "test")
    Test 4: Clicking a job in list calls onSelectJob(nodeId, jobId) with correct IDs
    Test 5: Selecting a job closes the popover
    Test 6: If node already has scheduled_job_id, shows current job name + "Change" link
    Test 7: Clicking "Change" re-opens selector to pick a different job
    Test 8: Escape key closes the popover without selecting
  </behavior>
  <action>
Create src/components/ScriptNodeJobSelector.tsx with:
- Props: {nodeId: string, currentJobId?: string, onSelectJob: (nodeId, jobId) => void}
- If no currentJobId: render a button "Select job"
- If currentJobId: render job name + "Change" link
- Trigger opens Popover or Sheet (check if src/components/ui/popover.tsx exists; if not, create wrapper)
- Inside popover: search input + filtered job list (hardcode or fetch /api/jobs)
- On job click: call onSelectJob, close popover

Create src/components/__tests__/ScriptNodeJobSelector.test.tsx with all 8 test cases:
- Render with no jobId; verify button appears
- Click button; verify popover/drawer opens
- Type in search; verify list filters
- Click job; verify callback called with correct arguments
- Verify popover closes after selection
- Render with jobId; verify job name shows
- Click "Change"; verify selector re-opens

Implementation code: stub; use inline job list mock (no API call)
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm test -- --run src/components/__tests__/ScriptNodeJobSelector.test.tsx 2>&1 | grep -E "(FAIL|PASS|✓|✗)" | head -20</automated>
  </verify>
  <done>All 8 test cases compile and run (currently failing); ScriptNodeJobSelector component, onSelectJob prop, and trigger logic exported; no TypeScript errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: IF Gate Config Drawer — Structured Condition Form</name>
  <files>src/components/IfGateConfigDrawer.tsx, src/components/__tests__/IfGateConfigDrawer.test.tsx</files>
  <behavior>
    Test 1: IfGateConfigDrawer renders as a right-side Sheet with title "Configure IF Gate"
    Test 2: Form has 5 fields: Field (text), Operator (select), Value (text), True Branch, False Branch
    Test 3: Operator select shows options: eq, neq, gt, lt, contains, exists (6 options)
    Test 4: Value field is hidden when operator is 'exists'
    Test 5: Form pre-populates with currentConfig if provided
    Test 6: [Save condition] button calls onSave with form data as IfGateConfig object
    Test 7: [Clear] button resets form to default (all empty, operator='eq')
    Test 8: onSave data includes: {field, op, value, true_branch, false_branch}
    Test 9: Closing the drawer calls onClose callback
    Test 10: Form validation: Field and branch names required (cannot save if empty)
  </behavior>
  <action>
Create src/components/IfGateConfigDrawer.tsx with:
- Props: {stepId: string, currentConfig?: IfGateConfig, onSave: (config) => void, onClose: () => void}
- IfGateConfig interface: {field: string, op: 'eq'|'neq'|'gt'|'lt'|'contains'|'exists', value?: string, true_branch: string, false_branch: string}
- Render using shadcn Sheet component (already available in src/components/ui/sheet.tsx)
- Form fields: Input (Field), Select (Operator), Input (Value, conditional), Input (True/False branches)
- [Save condition] button with onSave handler
- [Clear] button with reset logic
- onClose on Sheet close

Create src/components/__tests__/IfGateConfigDrawer.test.tsx with all 10 test cases:
- Render with open={true}; verify Sheet visible
- Verify all 5 form fields render
- Verify operator options (6 items)
- Type in fields; verify state updates
- Hide Value field when operator='exists'
- Pre-populate form from currentConfig
- Click [Save condition]; verify onSave called with correct shape
- Click [Clear]; verify reset
- Test branch name validation

Implementation code: stub; render form skeleton only (no validation logic yet)
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm test -- --run src/components/__tests__/IfGateConfigDrawer.test.tsx 2>&1 | grep -E "(FAIL|PASS|✓|✗)" | head -20</automated>
  </verify>
  <done>All 10 test cases compile and run (currently failing); IfGateConfigDrawer component, IfGateConfig interface, and form structure exported; no TypeScript errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: useDAGValidation Hook — Reactive Validation State</name>
  <files>src/hooks/useDAGValidation.ts, src/hooks/__tests__/useDAGValidation.test.tsx</files>
  <behavior>
    Test 1: useDAGValidation(nodes, edges) returns {validation, isValid, hasCycle, maxDepth}
    Test 2: On nodes/edges change, validation is recomputed automatically
    Test 3: If hasCycle, isValid=false; banner should show error
    Test 4: If maxDepth >= 25 and <= 30, isValid=true but warning banner should show
    Test 5: If maxDepth > 30, isValid=false; error banner shows and Save is disabled
    Test 6: Adding an edge that creates a cycle: validation updates immediately (hasCycle=true)
    Test 7: Removing that cycle-creating edge: hasCycle becomes false again
    Test 8: Multiple rapid node/edge changes: validation batches/debounces (no React errors)
  </behavior>
  <action>
Create src/hooks/useDAGValidation.ts with:
- Hook: useDAGValidation(nodes: Node[], edges: Edge[]): {validation: ValidationResult, isValid: boolean, hasCycle: boolean, maxDepth: number}
- Import validateDAG from src/utils/dagValidation
- useEffect dependency: [nodes, edges]
- Call validateDAG on every change
- useState for validation result
- Return structured object with flags for convenience (isValid, hasCycle extracted)
- No debouncing (real-time validation per spec)

Create src/hooks/__tests__/useDAGValidation.test.tsx with all 8 test cases:
- Render component with useDAGValidation hook
- Verify initial validation
- Simulate node/edge additions
- Verify validation updates
- Test cycle detection and removal
- Test depth warning vs. error states

Implementation code: stub hook; return hardcoded validation object
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm test -- --run src/hooks/__tests__/useDAGValidation.test.tsx 2>&1 | grep -E "(FAIL|PASS|✓|✗)" | head -20</automated>
  </verify>
  <done>All 8 test cases compile and run (currently failing); useDAGValidation hook exported with correct signature; no TypeScript errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: useWorkflowEdit Hook — Edit Mode State Management</name>
  <files>src/hooks/useWorkflowEdit.ts, src/hooks/__tests__/useWorkflowEdit.test.tsx</files>
  <behavior>
    Test 1: useWorkflowEdit(initialSteps, initialEdges) returns {nodes, edges, isEditing, setIsEditing, handleNodesChange, handleEdgesChange, handleConnect, handleDrop, ...}
    Test 2: Initial state: nodes/edges match input; isEditing=false
    Test 3: setIsEditing(true) enables edit mode
    Test 4: handleNodesChange applies ReactFlow node mutations (position, selection, delete)
    Test 5: handleEdgesChange applies ReactFlow edge mutations (delete, reorder)
    Test 6: handleConnect connects two nodes with a new edge
    Test 7: handleDrop creates new node at position from drag event (screenToFlowPosition required)
    Test 8: Unlinked SCRIPT nodes (no scheduled_job_id) have unlinked=true flag
    Test 9: getUnlinkedScriptNodes() returns all SCRIPT nodes without job ID
    Test 10: All SCRIPT nodes must be linked before Save is allowed (validation check)
  </behavior>
  <action>
Create src/hooks/useWorkflowEdit.ts with:
- Hook: useWorkflowEdit(initialSteps, initialEdges): {
    nodes, edges, isEditing, setIsEditing,
    handleNodesChange, handleEdgesChange, handleConnect, handleDrop,
    getUnlinkedScriptNodes, canSave
  }
- State: nodes (Node[]), edges (Edge[]), isEditing (boolean)
- Handlers: standard ReactFlow handlers (nodes/edges apply mutations via setNodes/setEdges)
- handleDrop: convert screen → flow coords (requires reactFlowInstance ref, pass separately or via context)
- getUnlinkedScriptNodes: filter nodes where nodeType='SCRIPT' && !scheduled_job_id
- canSave: no unlinked SCRIPT nodes AND no cycles AND depth <= 30 (check via validation hook later)
- Return all handlers + state accessors

Create src/hooks/__tests__/useWorkflowEdit.test.tsx with all 10 test cases:
- Render test component using hook
- Verify initial state
- Simulate edit mode toggle
- Simulate node/edge mutations
- Verify unlinked node detection
- Test Save readiness checks

Implementation code: stub handlers; return skeleton state
  </action>
  <verify>
    <automated>cd puppeteer/dashboard && npm test -- --run src/hooks/__tests__/useWorkflowEdit.test.tsx 2>&1 | grep -E "(FAIL|PASS|✓|✗)" | head -20</automated>
  </verify>
  <done>All 10 test cases compile and run (currently failing); useWorkflowEdit hook exported with full signature; no TypeScript errors; all handlers defined</done>
</task>

</tasks>

<verification>
**Wave 0 Completion Checklist:**
- [ ] All 6 test files compile without errors (vitest --run)
- [ ] All 12 test cases execute (currently failing — expected for TDD wave 0)
- [ ] All 6 implementation files are importable (stub implementations)
- [ ] No missing TypeScript types or imports
- [ ] `npm run build` succeeds in puppeteer/dashboard
- [ ] No lint errors (`npm run lint` passes)
- [ ] Verify test count: expect 12 + 8 + 8 + 10 + 8 + 10 = 56 total test cases created

**Acceptance:** Wave 0 complete when all 56 tests are defined and execute (currently in RED phase of TDD). Implementation happens in Wave 1 (Plan 02).
</verification>

<success_criteria>
1. All 12 test files created and compiling (vitest can parse them)
2. All 56 test cases defined and executable (will fail, as implementations are stubs)
3. All 6 utility/hook/component files created with stub exports
4. No TypeScript compilation errors (`npm run build` passes)
5. No lint violations (`npm run lint` passes, or minor violations documented)
6. Test infrastructure verified: can run `npm test -- --run src/utils/__tests__/dagValidation.test.tsx` and get output
7. All files committed to git with message: "test(155-01): wave 0 test scaffold — 56 failing tests ready for implementation"
</success_criteria>

<output>
After completion, create `.planning/phases/155-visual-dag-editor/155-01-SUMMARY.md` with:
- Status: Wave 0 complete
- Test count: 56 tests defined, 56 failing (RED phase)
- Artifacts created: 6 utilities/hooks/components + 6 test files
- Next: Wave 1 implementation (Plan 02)
- Commits: {list of commit hashes}
</output>
