---
phase: 57-research-parallel-job-swarming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md
autonomous: false
requirements:
  - SWRM-01
  - SWRM-02
  - SWRM-03

must_haves:
  truths:
    - "The design document contains a use case analysis distinguishing tag targeting (single-node) from fan-out / campaign dispatch (all matching nodes)"
    - "The design document covers pull-model race conditions, backpressure handling, and barrier synchronisation with a concrete state machine"
    - "The design document delivers a tiered build/defer recommendation with effort estimate and draft API endpoint signatures"
    - "A reader with no prior swarming context can understand the proposal and the reasoning behind the recommendation"
  artifacts:
    - path: ".planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md"
      provides: "Complete swarming design document covering all five required sections"
      contains: "POST /api/jobs/swarm"
  key_links:
    - from: "57-RESEARCH.md Use Case Analysis"
      to: "SWRM-01 requirement"
      via: "fan-out vs work-queue distinction + campaign definition"
      pattern: "fan-out|campaign|work.queue"
    - from: "57-RESEARCH.md Pull-Model Impact"
      to: "SWRM-02 requirement"
      via: "race condition, backpressure, barrier sync sections"
      pattern: "barrier|backpressure|race.condition|recompute_aggregate"
    - from: "57-RESEARCH.md Complexity / Value Recommendation"
      to: "SWRM-03 requirement"
      via: "tiered build path + draft API shape"
      pattern: "Tier 1|Tier 2|POST /api/jobs/swarm"
---

<objective>
Validate and finalise the parallel job swarming design document so the team can make an informed build/defer decision.

Purpose: Phase 57 is a research-only phase. The output is a single design document that answers: "What would fan-out swarming cost to build, and is it worth doing?" No implementation. This plan verifies the document satisfies all three requirements and all CONTEXT.md success criteria, fills any gaps, and obtains sign-off.

Output: A complete, validated `.planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md` covering (1) use case analysis, (2) pull-model impact, (3) data model sketch, (4) external system comparison, and (5) complexity/value recommendation with draft API shape.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-research-parallel-job-swarming/57-CONTEXT.md
@.planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md
@.planning/phases/57-research-parallel-job-swarming/57-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and complete the research document</name>
  <files>.planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md</files>
  <action>
Read `57-RESEARCH.md` and `57-CONTEXT.md` in full. Check every locked decision from CONTEXT.md against the document's content using this checklist:

**Required sections (from CONTEXT.md):**
- [ ] Section 1 — Use case analysis: fan-out vs work-queue defined; campaign concept defined; table comparing "today's tag targeting" vs "campaign / fan-out"; explicit statement of when tag targeting is sufficient vs when swarming is needed
- [ ] Section 2 — Pull-model impact: race condition analysis (double-assignment) with a recommended solution; backpressure analysis (nodes at concurrency limit + staggered start + dispatch timeout interaction); barrier synchronisation with the PENDING → RUNNING → COMPLETE/PARTIAL state machine specified
- [ ] Section 3 — Data model sketch: `swarm_records` table columns listed; `Job.swarm_id` nullable FK addition; relationship diagram; explicit list of what does NOT change
- [ ] Section 4 — External system comparison: at least three systems compared; summary table showing fan-out model, race prevention, and barrier approach for each including MoP's proposed approach
- [ ] Section 5 — Complexity/value recommendation: per-component complexity table; overall verdict; tiered build path (Tier 1 = fan-out, Tier 2 = work-queue defer); rough phase/plan count estimate; draft `POST /api/jobs/swarm` endpoint signature with request body and response; `GET /api/jobs/swarms` and `GET /api/jobs/swarms/{id}` signatures; work-queue identified as distinct pattern and explicitly deferred

**Required framing from CONTEXT.md:**
- [ ] Work-queue pattern: problem-definition only — identified, explained why harder in pull model, explicitly deferred
- [ ] Recommendation is "fan-out swarming as next milestone feature"
- [ ] Doc is readable by someone with no prior swarming context (verify the use case analysis section opens with accessible prose before technical detail)

For each checklist item:
- If the section exists and satisfies the criterion → mark checked, no changes needed
- If the section exists but is incomplete → add the missing content in place
- If a section is missing entirely → add it

After completing the checklist, if any changes were made to the document, write the updated file. If the document already satisfies all criteria, make no changes (do not touch the file).

Do NOT add implementation code. This is a design/research document — prose, tables, pseudo-code sketches, and sequence diagrams are appropriate. Full working code is not.
  </action>
  <verify>
    <automated>MISSING — this is a documentation phase. Verify by grepping for required content markers:
grep -c "POST /api/jobs/swarm" .planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md
grep -c "recompute_aggregate\|barrier" .planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md
grep -c "Tier 1\|Tier 2" .planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md
All three commands must return a non-zero count.
    </automated>
  </verify>
  <done>
57-RESEARCH.md exists and contains all five required sections. Every locked decision from CONTEXT.md is satisfied. Key content markers present: "POST /api/jobs/swarm", "recompute_aggregate", "Tier 1", "Tier 2", "fan-out", "work-queue", "PENDING → RUNNING", "PARTIAL", "FAILED".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
A complete parallel job swarming design document at `.planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md`. The document covers: use case analysis (fan-out vs work-queue vs today's tag targeting), pull-model impact (race conditions, backpressure, barrier synchronisation), data model sketch (swarm_records table + Job.swarm_id addition), external system comparison (Celery, Kubernetes Jobs, Ray), and a complexity/value recommendation with tiered build path and draft API shape.
  </what-built>
  <how-to-verify>
Read `.planning/phases/57-research-parallel-job-swarming/57-RESEARCH.md` and verify:

1. **Use case analysis (SWRM-01):** The document clearly distinguishes "today's tag targeting = single-node dispatch" from "campaign/fan-out = all matching nodes simultaneously". There is a comparison table. The campaign concept is defined. Work-queue is identified as a distinct, harder pattern.

2. **Pull-model impact (SWRM-02):** The document covers (a) the double-assignment race condition with a concrete recommended solution (pre-pin at swarm creation using target_node_id), (b) backpressure handling for nodes at concurrency limit, and (c) the barrier synchronisation state machine (PENDING → RUNNING → COMPLETE/PARTIAL/FAILED) with the recompute_aggregate trigger mechanism.

3. **Complexity/value recommendation (SWRM-03):** The document delivers (a) a per-component complexity table, (b) an explicit "build now" recommendation for fan-out swarming as a next milestone, (c) a rough phase/plan count estimate (3 phases, ~9-12 plans), and (d) concrete draft API endpoint signatures for POST /api/jobs/swarm, GET /api/jobs/swarms, and GET /api/jobs/swarms/{id}.

4. **Readability:** A developer unfamiliar with swarming can read the Summary section and understand the core proposal and recommendation without reading the full document.

Expected result: All four checks pass. The document gives the team everything needed to decide whether to implement fan-out swarming in the next milestone.
  </how-to-verify>
  <resume-signal>Type "approved" if the document satisfies all criteria, or describe any gaps to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 57 verification is document-review only (no code). The following must be true:

1. `57-RESEARCH.md` exists at the correct path
2. All five required sections are present and complete
3. Three content markers confirmed by grep: "POST /api/jobs/swarm", "recompute_aggregate", "Tier 1"
4. Human sign-off received on the checkpoint task
5. Phase SUMMARY written to `.planning/phases/57-research-parallel-job-swarming/57-01-SUMMARY.md`
</verification>

<success_criteria>
- The design document satisfies SWRM-01: fan-out vs work-queue use case analysis with campaign definition
- The design document satisfies SWRM-02: race condition, backpressure, and barrier synchronisation all addressed
- The design document satisfies SWRM-03: build recommendation with effort estimate and draft API shape
- Human reviewer confirms the document is complete and the recommendation is clear
- No implementation code was written (this is a research-only phase)
</success_criteria>

<output>
After completion, create `.planning/phases/57-research-parallel-job-swarming/57-01-SUMMARY.md` following the summary template.
</output>
