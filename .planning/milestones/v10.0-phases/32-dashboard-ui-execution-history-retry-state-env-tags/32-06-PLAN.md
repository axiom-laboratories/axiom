---
phase: 32-dashboard-ui-execution-history-retry-state-env-tags
plan: "06"
type: execute
wave: 3
depends_on:
  - "32-03"
  - "32-04"
  - "32-05"
files_modified: []
autonomous: false
requirements:
  - OUTPUT-03
  - OUTPUT-04
  - RETRY-03
  - ENVTAG-03

must_haves:
  truths:
    - "All automated tests pass GREEN"
    - "Visual appearance confirmed by user for all 4 requirement surfaces"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm all Phase 32 UI changes work correctly in the browser before phase close.

The automated tests verify structure and data flow, but correct badge colours, terminal output rendering, and responsive layout require visual confirmation.

Purpose: Gate phase completion on visual/functional correctness that automated tests cannot cover.
Output: User approval or bug report list for gap closure.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dashboard-ui-execution-history-retry-state-env-tags/32-03-SUMMARY.md
@.planning/phases/32-dashboard-ui-execution-history-retry-state-env-tags/32-04-SUMMARY.md
@.planning/phases/32-dashboard-ui-execution-history-retry-state-env-tags/32-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build check</name>
  <files></files>
  <action>
Run the full frontend test suite and confirm all tests pass GREEN:

```bash
cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run test
```

Also verify the production build has no TypeScript errors:

```bash
cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build 2>&1 | tail -20
```

Report: total tests passing, any failures, any build errors.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run test 2>&1 | tail -10</automated>
  </verify>
  <done>All tests GREEN; `npm run build` exits 0 with no TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 32 UI changes</name>
  <action>User verifies all 4 requirement surfaces in the browser — attestation badges, attempt tabs, history panel, env tag badges and filter.</action>
  <what-built>
Four UI features completed across 5 plans:

1. **ExecutionLogModal** (Plan 03): Attestation badge in header (VERIFIED/ATTEST FAILED/NO ATTESTATION), attempt tabs moved to top of modal sorted oldest-first with "(final)" on last tab, `jobRunId` prop for loading all attempts of a run.

2. **JobDefinitions history panel** (Plan 04): Click a definition name in the table to reveal an execution history panel below. Rows grouped by job_run_id. Retry badge shows "Attempt N of M" for multi-attempt runs. Clicking a row opens the ExecutionLogModal.

3. **History.tsx definition selector** (Plan 04): 4th filter column — "Scheduled Job" dropdown populated from `/jobs/definitions`. Selecting a definition filters the history table to that definition's runs only.

4. **Nodes env tag** (Plan 05): Colour-coded env tag badge in each node card header (DEV=blue, TEST=amber, PROD=rose, custom=zinc). Env filter dropdown above node cards — dynamically derived from live node data.
  </what-built>
  <how-to-verify>
Start the dev server:
```bash
cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run dev
```

**Check 1 — Nodes env tag (ENVTAG-03):**
- Navigate to `/nodes`
- Confirm nodes with `env_tag` set show a coloured badge near hostname (PROD=rose, TEST=amber, DEV=blue, custom=zinc)
- Confirm nodes without `env_tag` show no badge (not "UNTAGGED")
- If any nodes have env tags, confirm the filter dropdown appears above the node cards
- Select an env tag in the dropdown; confirm only matching nodes appear
- Select "All"; confirm all nodes return

**Check 2 — JobDefinitions history panel (OUTPUT-04):**
- Navigate to `/job-definitions`
- Click a job definition name cell (not the chevron expand button on the right)
- Confirm a history panel appears below the definitions table with a "Execution History" heading
- Confirm the selected row gains a left border highlight
- Click the same definition again; confirm the panel collapses
- If the definition has runs: confirm rows show timestamp, node, status badge, duration
- For runs with retries: confirm amber "Attempt N of M" badge or red "Failed N/M" badge appears
- Click a run row "Logs" button; confirm the ExecutionLogModal opens

**Check 3 — ExecutionLogModal attestation + attempt tabs (OUTPUT-03, RETRY-03):**
- Open the ExecutionLogModal from any route (History, Jobs, or JobDefinitions history panel)
- For a run with attestation data: confirm the VERIFIED / ATTEST FAILED / NO ATTESTATION badge appears in the modal header alongside the status badge
- For a run with multiple attempts: confirm attempt tabs appear AT THE TOP of the modal (not at the bottom), sorted oldest-first, with "(final)" on the last tab
- Switch between attempt tabs; confirm log output updates and scroll-to-bottom fires

**Check 4 — History.tsx definition selector (OUTPUT-04):**
- Navigate to `/history`
- Confirm 4 filter columns appear in the filter bar (Job GUID, Node ID, Status, Scheduled Job)
- Select a job definition from the "Scheduled Job" dropdown
- Confirm the history table updates to show only runs for that definition
- Clear the selection ("All Definitions"); confirm all runs return
  </how-to-verify>
  <verify>
    <automated>MISSING — this task is a human visual check; automated check is in Task 1</automated>
  </verify>
  <done>User has confirmed all 4 checks pass visually</done>
  <resume-signal>Type "approved" to close the phase, or describe any visual/functional issues found for gap closure</resume-signal>
</task>

</tasks>

<verification>
User approval or issue list from the checkpoint above.
</verification>

<success_criteria>
- All automated tests GREEN (`npm run test`)
- `npm run build` exits 0
- User confirms all 4 visual checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/32-dashboard-ui-execution-history-retry-state-env-tags/32-06-SUMMARY.md`
</output>
