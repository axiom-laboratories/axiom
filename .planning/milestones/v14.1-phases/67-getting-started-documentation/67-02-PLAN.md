---
phase: 67-getting-started-documentation
plan: "02"
type: execute
wave: 2
depends_on:
  - "67-01"
files_modified:
  - docs/docs/getting-started/enroll-node.md
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06
  - DOCS-07

must_haves:
  truths:
    - "A user without dashboard access can generate a JOIN_TOKEN entirely via CLI using documented curl commands"
    - "The AGENT_URL table covers the cold-start compose scenario with https://agent:8001"
    - "EXECUTION_MODE=docker is the only execution mode documented in Option B (no direct references)"
    - "Option B uses the correct Axiom node image (localhost/master-of-puppets-node:latest)"
    - "Option B includes a Docker socket volume mount note"
    - "mkdocs build --strict passes after all edits"
  artifacts:
    - path: "docs/docs/getting-started/enroll-node.md"
      provides: "CLI token path as primary tab; restructured AGENT_URL table; Step 3 as tab pair"
      contains: "agent:8001"
  key_links:
    - from: "docs/docs/getting-started/enroll-node.md"
      to: "docs/mkdocs.yml"
      via: "=== tab syntax requires pymdownx.tabbed (added in plan 67-01)"
      pattern: "=== \"Dashboard\""
---

<objective>
Restructure enroll-node.md to close five requirements: promote the CLI token path to a primary tab (DOCS-03), verify DOCS-04 and DOCS-05 are not regressed, fix the AGENT_URL table to cover the cold-start compose scenario (DOCS-06), and preserve the Docker socket mount note (DOCS-07).

Purpose: A user who cannot or prefers not to use the dashboard must have a complete, documented CLI path for the entire enrollment flow from token generation through node startup.
Output: enroll-node.md with Step 1 as a Dashboard/CLI tab pair, a restructured AGENT_URL table, and Step 3 as an Option A/Option B tab pair — all passing mkdocs build --strict.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-getting-started-documentation/67-CONTEXT.md
@.planning/phases/67-getting-started-documentation/67-RESEARCH.md
@.planning/phases/67-getting-started-documentation/67-01-SUMMARY.md

<interfaces>
<!-- Current enroll-node.md structure (read 2026-03-26): -->
<!-- Step 1: Generate an enrollment token — single dashboard path with !!! note CLI callout at bottom -->
<!-- Step 2: Configure node connectivity — AGENT_URL table (3 rows; 172.17.0.1 as Linux primary) -->
<!-- Step 3: Install the node — ### Option A heading + ### Option B heading (sub-headings, not tabs) -->
<!-- Step 4: Verify enrollment -->

<!-- DOCS-04 and DOCS-05 are already correct in the current file: -->
<!-- Option B uses: image: localhost/master-of-puppets-node:latest  (DOCS-04 satisfied) -->
<!-- Option B uses: EXECUTION_MODE: docker  (DOCS-05 satisfied) -->
<!-- Verify these values are preserved — do not overwrite with wrong values -->

<!-- Docker socket volume already present as !!! tip block in Option B (DOCS-07 satisfied) -->
<!-- Must be preserved in the new tab structure -->

<!-- Current AGENT_URL table (DOCS-06 needs fixing): -->
<!-- | Docker Desktop (Mac or Windows) | https://host.docker.internal:8001 | -->
<!-- | Linux Docker (bridge network)   | https://172.17.0.1:8001           | -->
<!-- | Same Docker Compose network     | https://puppeteer-agent-1:8001    | -->
<!-- Missing: cold-start compose scenario (https://agent:8001) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote CLI token path to primary tab and restructure AGENT_URL table</name>
  <files>docs/docs/getting-started/enroll-node.md</files>
  <action>
Read docs/docs/getting-started/enroll-node.md in full first. Confirm that Option B currently has `localhost/master-of-puppets-node:latest` and `EXECUTION_MODE: docker` — do not change these values.

Make two targeted edits:

**Edit 1 — Step 1: Replace the single dashboard path + !!! note CLI callout with a Dashboard/CLI tab pair.**

The existing content is: numbered dashboard steps followed by a `!!! warning "Use the dashboard Copy button"` block, then a `!!! note "CLI / headless alternative"` block with the curl commands, then a `!!! warning "Admin password (cold-start installs)"` block.

Replace this with a tab pair. The Dashboard tab gets the existing numbered steps plus the "Use the dashboard Copy button" warning. The CLI tab gets the curl commands promoted to first-class content. The cold-start admin password warning moves outside the tabs (applies to both paths).

```markdown
## Step 1: Generate an enrollment token

=== "Dashboard"

    1. In the dashboard, go to **Nodes**
    2. Click **Generate Token**
    3. Click **Copy JOIN_TOKEN** — this copies the enhanced token (base64-encoded JSON with the Root CA embedded)

    !!! warning "Use the dashboard Copy button"
        The raw API endpoint `POST /api/enrollment-tokens` returns only the token hex string. The **enhanced JOIN_TOKEN** — which includes the Root CA for mTLS bootstrap — is only available via the dashboard **Copy JOIN_TOKEN** button.

        If you give the node the raw hex string, you will see the node failing with a TLS error rather than the expected log line:
        ```
        Detected Enhanced Token. Bootstrapping Trust...
        ```

        **Always copy the JOIN_TOKEN from the dashboard.**

=== "CLI"

    Log in to get a JWT:

    ```bash
    TOKEN=$(curl -sk -X POST https://<your-orchestrator>:8001/auth/login \
      -H 'Content-Type: application/x-www-form-urlencoded' \
      -d 'username=admin&password=<your-password>' | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")
    ```

    Then generate and retrieve the enhanced token:

    ```bash
    curl -sk -X POST https://<your-orchestrator>:8001/admin/generate-token \
      -H "Authorization: Bearer $TOKEN" \
      | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('enhanced_token', d.get('join_token', '')))"
    ```

    The `enhanced_token` field contains the full base64-encoded JOIN_TOKEN with the Root CA embedded.

!!! warning "Admin password (cold-start installs)"
    If you started Axiom using `compose.cold-start.yaml` without setting `ADMIN_PASSWORD` in your `.env` file, the admin account was created with a random password. You will not be able to log in.

    Before starting the stack, create a `.env` file in the same directory as `compose.cold-start.yaml`:

    ```bash
    ADMIN_PASSWORD=your-chosen-password
    ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
    ```

    Then restart: `docker compose -f compose.cold-start.yaml --env-file .env down -v && docker compose -f compose.cold-start.yaml --env-file .env up -d`
```

**Edit 2 — Step 2: Replace the AGENT_URL table with the restructured scenario-based version.**

Replace the existing 3-row table and the `172.17.0.1` fallback note with:

```markdown
## Step 2: Configure node connectivity

Choose the `AGENT_URL` value that matches your deployment scenario:

| Scenario | AGENT_URL |
|----------|-----------|
| Cold-start compose (node in same compose network) | `https://agent:8001` |
| Server compose, node on same host | `https://puppeteer-agent-1:8001` |
| Remote host / separate machine | `https://<hostname-or-ip>:8001` |
| Docker Desktop (Mac or Windows) | `https://host.docker.internal:8001` |

If your node is on a custom Linux bridge network, find the gateway with:

```bash
ip route | awk '/default/ {print $3}'
```
```

All tab content must be indented exactly 4 spaces. Do not change Step 3 (Option A and Option B) in this task — that is handled in Task 2.
  </action>
  <verify>
    <automated>grep "agent:8001" /home/thomas/Development/master_of_puppets/docs/docs/getting-started/enroll-node.md && grep "=== \"CLI\"" /home/thomas/Development/master_of_puppets/docs/docs/getting-started/enroll-node.md</automated>
  </verify>
  <done>enroll-node.md Step 1 is a Dashboard/CLI tab pair; AGENT_URL table has 4 rows including `https://agent:8001` for cold-start compose; `172.17.0.1` appears only in the fallback note, not as a primary table entry.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Step 3 sub-headings to tab pair; verify DOCS-04/05/07; run build gate</name>
  <files>docs/docs/getting-started/enroll-node.md</files>
  <action>
Read the current enroll-node.md (after Task 1 edits). Confirm before writing:
- Option B image is `localhost/master-of-puppets-node:latest` (DOCS-04 — must remain unchanged)
- Option B has `EXECUTION_MODE: docker` — no `direct` anywhere (DOCS-05 — must remain unchanged)
- The `!!! tip "EXECUTION_MODE=docker"` Docker socket mount block is present (DOCS-07 — must be preserved)

Then convert Step 3 from sub-heading style to a tab pair. Replace the `### Option A: curl installer (recommended)` and `### Option B: Docker Compose (power user)` sub-headings with `=== "Option A: curl installer"` and `=== "Option B: Docker Compose"` tabs under a single `## Step 3: Install the node` heading. All existing content for each option moves into its respective tab, indented 4 spaces.

```markdown
## Step 3: Install the node

=== "Option A: curl installer"

    The one-liner downloads and runs the universal installer script hosted on the orchestrator:

    ```bash
    curl -sSL https://<your-orchestrator>/installer.sh | bash -s -- --token "<JOIN_TOKEN>"
    ```

    Replace `<your-orchestrator>` with your orchestrator's hostname or IP (e.g., `10.0.0.5:8001` or `my-orchestrator.example.com`).

    The installer script:

    - Detects Docker or Podman on your system
    - Downloads a ready-to-run `node-compose.yaml` from the orchestrator
    - Starts the node container automatically

    !!! tip "Getting the compose file without running it"
        The orchestrator also serves the generated compose file directly if you want to inspect or customise it before running:
        ```bash
        curl -sSL "https://<your-orchestrator>/api/installer/compose?token=<JOIN_TOKEN>" > node-compose.yaml
        docker compose -f node-compose.yaml up -d
        ```

=== "Option B: Docker Compose"

    For full control over the configuration, create the compose file manually.

    Create `node-compose.yaml` with the following content, substituting your JOIN_TOKEN and AGENT_URL:

    ```yaml
    services:
      puppet-node:
        image: localhost/master-of-puppets-node:latest
        environment:
          NODE_TAGS: general,linux
          JOB_IMAGE: docker.io/library/python:3.12-alpine
          AGENT_URL: https://172.17.0.1:8001
          JOIN_TOKEN: <paste-your-enhanced-token-here>
          ROOT_CA_PATH: /app/secrets/root_ca.crt
          EXECUTION_MODE: docker
        volumes:
          - node-secrets:/app/secrets
          - /var/run/docker.sock:/var/run/docker.sock

    volumes:
      node-secrets:
    ```

    !!! tip "EXECUTION_MODE=docker"
        When the node container runs inside Docker, set `EXECUTION_MODE=docker`. This tells the node to spawn job containers using the host's Docker daemon via the mounted socket (`/var/run/docker.sock`).

        You must also add the Docker socket to the node's volumes:

        ```yaml
        volumes:
          - node-secrets:/app/secrets
          - /var/run/docker.sock:/var/run/docker.sock
        ```

        Then update your compose to mount the socket:

        ```yaml
        services:
          puppet-node:
            image: localhost/master-of-puppets-node:latest
            environment:
              ...
              EXECUTION_MODE: docker
            volumes:
              - node-secrets:/app/secrets
              - /var/run/docker.sock:/var/run/docker.sock
        ```

    Then start the node:

    ```bash
    docker compose -f node-compose.yaml up -d
    ```
```

After writing, run all verification checks and `mkdocs build --strict`. Fix any indentation errors before marking complete.
  </action>
  <verify>
    <automated>grep "master-of-puppets-node:latest" /home/thomas/Development/master_of_puppets/docs/docs/getting-started/enroll-node.md && grep -c "EXECUTION_MODE=direct" /home/thomas/Development/master_of_puppets/docs/docs/getting-started/enroll-node.md && grep "docker.sock" /home/thomas/Development/master_of_puppets/docs/docs/getting-started/enroll-node.md && cd /home/thomas/Development/master_of_puppets/docs && mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <done>enroll-node.md Step 3 uses === tabs not sub-headings; `grep -c "EXECUTION_MODE=direct"` returns 0; `localhost/master-of-puppets-node:latest` present; `docker.sock` present; `mkdocs build --strict` passes.</done>
</task>

</tasks>

<verification>
1. `grep "=== \"CLI\"" docs/docs/getting-started/enroll-node.md` confirms CLI tab present (DOCS-03)
2. `grep "master-of-puppets-node:latest" docs/docs/getting-started/enroll-node.md` confirms correct image (DOCS-04)
3. `grep -c "EXECUTION_MODE=direct" docs/docs/getting-started/enroll-node.md` returns 0 (DOCS-05)
4. `grep "agent:8001" docs/docs/getting-started/enroll-node.md` confirms cold-start AGENT_URL present (DOCS-06)
5. `grep "docker.sock" docs/docs/getting-started/enroll-node.md` confirms Docker socket note present (DOCS-07)
6. `cd /home/thomas/Development/master_of_puppets/docs && mkdocs build --strict` exits 0
</verification>

<success_criteria>
- DOCS-03: Step 1 is a Dashboard/CLI tab pair; CLI path is primary, not a footnote
- DOCS-04: Option B compose snippet uses `localhost/master-of-puppets-node:latest` (verified, not regressed)
- DOCS-05: No `EXECUTION_MODE=direct` in the file (verified, not regressed)
- DOCS-06: AGENT_URL table has `https://agent:8001` as the cold-start compose entry; 172.17.0.1 only in fallback note
- DOCS-07: Docker socket mount note present in Option B tab (preserved from existing !!! tip block)
- `mkdocs build --strict` passes
</success_criteria>

<output>
After completion, create `.planning/phases/67-getting-started-documentation/67-02-SUMMARY.md`
</output>
