---
phase: 81-homepage-enterprise-messaging-sso-narrative-compliance-framing-and-conversion-optimisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - homepage/style.css
  - homepage/index.html
autonomous: false
requirements: []

must_haves:
  truths:
    - "A security posture section titled 'Security that satisfies your infosec team' appears between the pain-points section and the CE/EE editions section"
    - "The EE card shows an indigo 'Early access' badge instead of the crimson 'Coming soon' badge"
    - "The EE feature list is readable (not faint/dimmed) and the SSO line reads 'SAML 2.0 / OIDC SSO integration'"
    - "The EE card shows the design-partner intro sentence above the feature list"
    - "Every 'Register your interest' / 'Enterprise' link opens the Google Form placeholder in a new tab — no self-referencing #enterprise-interest anchors remain"
    - "The dual-CTA enterprise block button reads 'Get early access' and links to the Google Form placeholder"
  artifacts:
    - path: "homepage/style.css"
      provides: "Security grid, security card, early-access badge, responsive rule for security grid"
      contains: ".security-grid"
    - path: "homepage/index.html"
      provides: "Complete updated page with security section and fixed enterprise CTA"
      contains: "SAML 2.0 / OIDC"
  key_links:
    - from: "homepage/index.html"
      to: "GOOGLE_FORM_URL_PLACEHOLDER"
      via: "All four former #enterprise-interest anchors replaced"
      pattern: "GOOGLE_FORM_URL_PLACEHOLDER"
    - from: "homepage/index.html"
      to: "homepage/style.css"
      via: ".security-grid and .badge-early-access class references"
      pattern: "security-grid|badge-early-access"
---

<objective>
Update the Axiom marketing homepage with enterprise-grade messaging: add a security posture section, fix the broken enterprise CTA conversion path, update EE card framing to "early access", and sharpen the SSO feature line.

Purpose: The current homepage has a broken conversion path (self-referencing anchor), vague SSO claim ("Organisation SSO readiness"), and EE framing that signals unavailability ("Coming soon"). This phase fixes all three and adds a new security trust section.

Output: Updated `homepage/style.css` (new security grid and early-access badge styles) and `homepage/index.html` (new section, fixed anchors, updated EE card).
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-homepage-enterprise-messaging-sso-narrative-compliance-framing-and-conversion-optimisation/81-CONTEXT.md
@.planning/phases/81-homepage-enterprise-messaging-sso-narrative-compliance-framing-and-conversion-optimisation/81-RESEARCH.md

<interfaces>
<!-- Current HTML structure — key sections the executor will modify. Read directly from homepage/index.html. -->

Nav Enterprise link (line 21 — self-referencing anchor to fix):
```html
<a href="#enterprise-interest" class="btn-nav-enterprise">Enterprise</a>
```

Hero secondary CTA (line 37 — self-referencing anchor to fix):
```html
<a href="#enterprise-interest" class="btn btn-secondary">Talk to us about Enterprise</a>
```

Pain-points section end (line 103 — new security section inserts immediately after):
```html
    </div>
  </div>
</section>

<!-- CE vs EE -->
<section class="section section-alt">
```

EE card badge (line 128 — replace):
```html
<span class="badge badge-coming-soon">Coming soon</span>
```

EE card intro (line 130 — replace with two sentences):
```html
<p class="card-intro">Everything in CE, plus:</p>
```

EE feature list (line 131 — remove --dimmed modifier):
```html
<ul class="feature-list feature-list--dimmed">
```

SSO line (line 138 — replace text):
```html
<li>Organisation SSO readiness</li>
```

EE card internal link (line 141 — self-referencing anchor to fix):
```html
<a href="#enterprise-interest">Get in touch &rarr;</a>
```

Dual-CTA enterprise block (lines 164-169 — fix href and button text):
```html
<div class="dual-cta-block">
  <p class="section-label">Enterprise edition</p>
  <h3>Interested in Enterprise?</h3>
  <p class="install-sub">Tell us what you need. We'll be in touch when Enterprise Edition launches.</p>
  <a href="#enterprise-interest" class="btn btn-primary">Register your interest &rarr;</a>
</div>
```

Current CSS badge patterns (style.css lines 212-222):
```css
.badge-free {
  background-color: rgba(46, 160, 67, 0.15);
  color: #3fb950;
  border: 1px solid rgba(46, 160, 67, 0.4);
}

.badge-ee {
  background-color: rgba(210, 53, 86, 0.15);
  color: var(--axiom-primary);
  border: 1px solid rgba(210, 53, 86, 0.4);
}
```

Current pain-grid pattern (style.css lines 485-514 — security grid mirrors this):
```css
.pain-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.25rem;
  margin-top: 2rem;
}

.pain-card {
  background-color: var(--axiom-surface);
  border: 1px solid var(--axiom-border);
  border-radius: var(--radius);
  padding: 1.25rem;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security grid and early-access badge CSS to style.css</name>
  <files>homepage/style.css</files>
  <action>
Read `homepage/style.css` first, then append the following new CSS blocks at the end of the file (before the final responsive `@media` block, or appended after it — either works as long as the responsive rule is inside the existing `@media (max-width: 640px)` block).

**New CSS to add:**

1. Security posture grid section (add after the `.pain-card p` block, before the `/* ----- Dual CTA section ----- */` block):

```css
/* ----- Security posture grid ----- */
.security-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1.25rem;
  margin-top: 2rem;
}

.security-card {
  background-color: var(--axiom-bg);
  border: 1px solid var(--axiom-border);
  border-radius: var(--radius);
  padding: 1.25rem;
}

.security-card h3 {
  font-size: 1rem;
  margin-bottom: 0.4rem;
}

.security-card p {
  font-size: 0.875rem;
  color: var(--axiom-text-muted);
  line-height: 1.55;
}
```

Note: `.security-card` uses `var(--axiom-bg)` (not `--axiom-surface`) because the section uses `.section-alt` (surface background) — cards need contrast against the section background.

2. Early-access badge (add alongside the existing `.badge-coming-soon` block):

```css
/* ----- Early access badge ----- */
.badge-early-access {
  background-color: rgba(99, 102, 241, 0.15);
  color: #818cf8;
  border: 1px solid rgba(99, 102, 241, 0.4);
}
```

3. Add `.security-grid` responsive rule inside the existing `@media (max-width: 640px)` block (after the `.dual-cta-divider` rule inside that block):

```css
  .security-grid {
    grid-template-columns: 1fr;
  }
```

The `.pain-icon` class is already defined in the existing CSS and can be reused by security cards — do not duplicate it.
  </action>
  <verify>
    <automated>grep -n "security-grid\|badge-early-access" /home/thomas/Development/master_of_puppets/homepage/style.css</automated>
  </verify>
  <done>
    - `homepage/style.css` contains `.security-grid`, `.security-card`, and `.badge-early-access` class definitions
    - `.security-grid` responsive rule exists inside the `@media (max-width: 640px)` block
    - No existing styles were modified — only new blocks added
  </done>
</task>

<task type="auto">
  <name>Task 2: Update index.html — security section, EE card, and CTA anchor fixes</name>
  <files>homepage/index.html</files>
  <action>
Read `homepage/index.html` first, then make ALL of the following changes in a single write:

**Change 1 — Nav Enterprise link (fix broken anchor + open in new tab):**
Replace line 21:
```html
        <a href="#enterprise-interest" class="btn-nav-enterprise">Enterprise</a>
```
With:
```html
        <!-- TODO: replace GOOGLE_FORM_URL_PLACEHOLDER with real Google Form URL before launch -->
        <a href="GOOGLE_FORM_URL_PLACEHOLDER" target="_blank" rel="noopener noreferrer" class="btn-nav-enterprise">Enterprise</a>
```

**Change 2 — Hero secondary CTA (fix broken anchor + open in new tab):**
Replace line 37:
```html
        <a href="#enterprise-interest" class="btn btn-secondary">Talk to us about Enterprise</a>
```
With:
```html
        <a href="GOOGLE_FORM_URL_PLACEHOLDER" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">Talk to us about Enterprise</a>
```

**Change 3 — Insert security posture section between pain-points section and CE/EE editions section.**
The pain-points section closes with `</section>` before the comment `<!-- CE vs EE -->`. Insert the following block between them:

```html
  <!-- Security posture -->
  <section class="section section-alt">
    <div class="container">
      <p class="section-label">Enterprise-grade security</p>
      <h2>Security that satisfies your infosec team</h2>
      <div class="security-grid">
        <div class="security-card">
          <span class="pain-icon">🔏</span>
          <h3>Cryptographic audit trail</h3>
          <p>Every job execution is Ed25519-signed before it runs and logged with a full audit record. You know who ran what — and you can prove it.</p>
        </div>
        <div class="security-card">
          <span class="pain-icon">🛡️</span>
          <h3>Least-privilege RBAC</h3>
          <p>Three built-in roles: admin, operator, viewer. No shared accounts. Permissions are scoped and auditable from day one.</p>
        </div>
        <div class="security-card">
          <span class="pain-icon">🏢</span>
          <h3>Air-gapped deployment</h3>
          <p>Fully self-hosted. No telemetry, no cloud dependency, no data leaves your network. Runs on your infrastructure, under your control.</p>
        </div>
        <div class="security-card">
          <span class="pain-icon">🔐</span>
          <h3>Certificate-based node identity</h3>
          <p>Each node holds a unique mTLS client certificate. Revocation is cryptographic — not a password reset.</p>
        </div>
      </div>
    </div>
  </section>
```

**Change 4 — EE card badge (replace "Coming soon" with "Early access"):**
Replace:
```html
            <span class="badge badge-coming-soon">Coming soon</span>
```
With:
```html
            <span class="badge badge-early-access">Early access</span>
```

**Change 5 — EE card intro (add design-partner sentence, keep "Everything in CE, plus:" line):**
Replace:
```html
          <p class="card-intro">Everything in CE, plus:</p>
```
With:
```html
          <p class="card-intro">Built with early design partners — shaped by real enterprise deployments.</p>
          <p class="card-intro">Everything in CE, plus:</p>
```

**Change 6 — EE feature list (remove --dimmed modifier, update SSO line):**
Replace:
```html
          <ul class="feature-list feature-list--dimmed">
```
With:
```html
          <ul class="feature-list">
```

And replace the SSO line:
```html
<li>Organisation SSO readiness</li>
```
With:
```html
<li>SAML 2.0 / OIDC SSO integration</li>
```

**Change 7 — EE card internal link (fix broken anchor + open in new tab):**
Replace:
```html
            Interested in Enterprise? <a href="#enterprise-interest">Get in touch &rarr;</a>
```
With:
```html
            Interested in Enterprise? <a href="GOOGLE_FORM_URL_PLACEHOLDER" target="_blank" rel="noopener noreferrer">Get in touch &rarr;</a>
```

**Change 8 — Dual-CTA enterprise block (fix broken anchor, update copy and button text):**
Replace the entire enterprise dual-cta-block:
```html
        <div class="dual-cta-block">
          <p class="section-label">Enterprise edition</p>
          <h3>Interested in Enterprise?</h3>
          <p class="install-sub">Tell us what you need. We'll be in touch when Enterprise Edition launches.</p>
          <a href="#enterprise-interest" class="btn btn-primary">Register your interest &rarr;</a>
        </div>
```
With:
```html
        <div class="dual-cta-block">
          <p class="section-label">Enterprise edition</p>
          <h3>Get early access</h3>
          <p class="install-sub">We're working with design partners now. Tell us about your environment.</p>
          <a href="GOOGLE_FORM_URL_PLACEHOLDER" target="_blank" rel="noopener noreferrer" class="btn btn-primary">Get early access &rarr;</a>
        </div>
```

After all changes, verify zero `#enterprise-interest` self-references remain by checking the file content.
  </action>
  <verify>
    <automated>grep -c 'href="#enterprise-interest"' /home/thomas/Development/master_of_puppets/homepage/index.html && echo "FAIL: self-references remain" || echo "PASS: no self-references"</automated>
  </verify>
  <done>
    - `grep -c 'href="#enterprise-interest"' homepage/index.html` returns 0
    - `grep -n 'SAML' homepage/index.html` returns exactly 1 line with "SAML 2.0 / OIDC SSO integration"
    - `grep -n 'security-grid' homepage/index.html` returns the new section
    - `grep -n 'badge-early-access' homepage/index.html` returns the EE card badge line
    - `grep -n 'GOOGLE_FORM_URL_PLACEHOLDER' homepage/index.html` returns 4 occurrences (nav, hero CTA, EE card link, dual-CTA button)
    - `grep -n 'TODO.*GOOGLE_FORM' homepage/index.html` returns 1 comment (above the nav link)
    - `grep -n 'feature-list--dimmed' homepage/index.html` returns 0 (dimming removed)
    - `grep -n 'Built with early design partners' homepage/index.html` returns 1 line
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of all homepage changes</name>
  <action>Open homepage/index.html in a browser and verify all changes per the checklist below. Run the grep sanity check to confirm zero self-referencing anchors remain.</action>
  <what-built>
    - New "Security that satisfies your infosec team" section with 4 capability cards (2×2 grid) inserted between pain-points and CE/EE editions
    - EE card updated: indigo "Early access" badge, design-partner intro sentence, readable (non-dimmed) feature list, "SAML 2.0 / OIDC SSO integration" line
    - All enterprise CTAs (nav, hero, EE card, dual-CTA) link to GOOGLE_FORM_URL_PLACEHOLDER in a new tab
    - Dual-CTA enterprise block reads "Get early access" with updated supporting copy
    - TODO comment above nav link for easy URL swap
  </what-built>
  <how-to-verify>
    1. Open `homepage/index.html` directly in a browser (file:// URL — no server needed)
    2. Check page section order: nav → hero → mockup → pain-points → security posture → editions → dual-CTA → footer
    3. Confirm the security section shows 4 cards in a 2-column grid with emoji icons and factual copy (no certification claims)
    4. Confirm the EE card shows an indigo "Early access" badge (distinct from the green "Free" badge)
    5. Confirm the EE feature list text is readable (muted, not near-invisible faint)
    6. Confirm the SSO line reads "SAML 2.0 / OIDC SSO integration" (not "Organisation SSO readiness")
    7. Click the nav "Enterprise" button — should attempt to open GOOGLE_FORM_URL_PLACEHOLDER in a new tab
    8. Click the "Get early access" button in the dual-CTA section — same behaviour
    9. Resize to mobile width (~375px) — confirm security cards stack to single column
    10. Run: `grep -c 'href="#enterprise-interest"' homepage/index.html` — must return 0
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Post-implementation checks:
- `grep -c 'href="#enterprise-interest"' homepage/index.html` returns 0 (all self-references fixed)
- `grep -n 'SAML' homepage/index.html` returns 1 line
- `grep -n 'security-grid' homepage/style.css` returns at least 2 lines (definition + responsive rule)
- `grep -n 'badge-early-access' homepage/style.css` returns at least 1 line
- `grep -n 'TODO.*GOOGLE_FORM' homepage/index.html` returns 1 line
- `grep -n 'feature-list--dimmed' homepage/index.html` returns 0
- Page structure order in index.html: pain-points section → security section → editions section
</verification>

<success_criteria>
Phase 81 complete when:
1. Security posture section exists between pain-points and editions with 4 factual capability cards
2. EE badge shows "Early access" in indigo (not "Coming soon" in crimson)
3. EE feature list is readable, SSO line specifies "SAML 2.0 / OIDC"
4. Design-partner intro sentence appears above EE feature list
5. Zero `href="#enterprise-interest"` self-references remain in index.html
6. All enterprise CTAs point to GOOGLE_FORM_URL_PLACEHOLDER with `target="_blank"`
7. One TODO comment marks the placeholder for easy URL replacement
8. Human-verify checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/81-homepage-enterprise-messaging-sso-narrative-compliance-framing-and-conversion-optimisation/81-01-SUMMARY.md`
</output>
