---
phase: 79-install-docs-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/compose.cold-start.yaml
  - docs/docs/getting-started/install.md
autonomous: true
requirements:
  - INST-01
  - INST-02

must_haves:
  truths:
    - "Running docker compose -f compose.cold-start.yaml up -d starts only the five Axiom services (db, cert-manager, agent, dashboard, docs) — no puppet-node-1 or puppet-node-2"
    - "install.md tab labels read 'Quick Start' (not 'Cold-Start Install') across all three tabbed sections (Steps 2, 3, 4)"
    - "The Step 3 Quick Start prose mentions only the three Axiom services — no reference to bundled puppet nodes or JOIN tokens"
    - "The compose.cold-start.yaml header comment 'Quick start:' has only two numbered steps — no JOIN token generation or JOIN_TOKEN_1/JOIN_TOKEN_2 references"
  artifacts:
    - path: "puppeteer/compose.cold-start.yaml"
      provides: "Clean cold-start compose with no node services or orphan volumes"
      contains: "services: db, cert-manager, agent, dashboard, docs"
    - path: "docs/docs/getting-started/install.md"
      provides: "Updated install doc with Quick Start tab labels and clean Step 3 prose"
  key_links:
    - from: "puppeteer/compose.cold-start.yaml"
      to: "docs/docs/getting-started/install.md"
      via: "filename reference in install.md (compose.cold-start.yaml unchanged)"
      pattern: "compose\\.cold-start\\.yaml"
---

<objective>
Remove the two bundled test node services (puppet-node-1, puppet-node-2) from compose.cold-start.yaml and update install.md atomically so that a new user following the install guide encounters a clean Axiom stack with no phantom node services, stale JOIN_TOKEN references, or misleading tab labels.

Purpose: A new user running the Quick Start compose command should see exactly the services they expect and no confusing extra containers that require separate JOIN token setup. The install doc should match what the compose file actually starts.
Output: A lean compose file (5 services, no node secrets volumes, no JOIN token header steps) and an updated install doc (Quick Start tab labels, clean Step 3 prose, zero JOIN_TOKEN mentions).
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean compose.cold-start.yaml — remove node services and volumes</name>
  <files>puppeteer/compose.cold-start.yaml</files>
  <action>
Make the following edits to puppeteer/compose.cold-start.yaml:

1. DELETE the `puppet-node-1` service block entirely (lines 105–126 in the current file — the full block from `puppet-node-1:` through `condition: service_started` at the end of the block).

2. DELETE the `puppet-node-2` service block entirely (lines 127–147 in the current file — same pattern).

3. In the `volumes:` block at the bottom of the file, DELETE the two lines:
   ```
     node1-secrets:
     node2-secrets:
   ```
   Leave `pgdata:`, `certs-volume:`, `caddy_data:`, `caddy_config:`, and `secrets-data:` untouched. Keep the existing `secrets-data:` inline comment as-is: `# Agent secrets persistence (boot.log, licence.key) across restarts` (no change needed).

4. In the header comment block (lines 8–16), REMOVE steps 3 and 4:
   ```
   #   3. Generate JOIN tokens from the dashboard (Admin → Join Tokens) or via API:
   #        curl -sk -X POST https://localhost:8001/admin/generate-token \
   #          -H "Authorization: Bearer <admin-jwt>" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['token'])"
   #   4. Set JOIN_TOKEN_1 and JOIN_TOKEN_2 in your .env file and restart the node services.
   ```
   After deletion the header's "Quick start:" block should have exactly two numbered steps:
   ```
   # Quick start:
   #   1. Create a .env file with at minimum:
   #        ADMIN_PASSWORD=<choose-a-password>
   #        ENCRYPTION_KEY=<32-char-fernet-key>  # generate: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
   #   2. docker compose -f compose.cold-start.yaml --env-file .env up -d
   ```
   The Usage block immediately below (CE run / EE run lines) is unaffected — those lines contain no JOIN_TOKEN references.

The resulting file must be valid Docker Compose YAML with exactly five services: db, cert-manager, agent, dashboard, docs.
  </action>
  <verify>
    <automated>grep -c "puppet-node-1\|puppet-node-2\|node1-secrets\|node2-secrets\|JOIN_TOKEN" /home/thomas/Development/master_of_puppets/puppeteer/compose.cold-start.yaml; echo "count above must be 0"; docker compose -f /home/thomas/Development/master_of_puppets/puppeteer/compose.cold-start.yaml config --quiet && echo "YAML_VALID"</automated>
  </verify>
  <done>grep count is 0 for all prohibited strings; docker compose config --quiet exits 0; file has exactly 5 services (db, cert-manager, agent, dashboard, docs)</done>
</task>

<task type="auto">
  <name>Task 2: Update install.md — rename tabs and fix Step 3 prose</name>
  <files>docs/docs/getting-started/install.md</files>
  <action>
Make the following edits to docs/docs/getting-started/install.md:

1. RENAME all three tab labels from "Cold-Start Install" to "Quick Start". These appear at three locations:
   - Line 59: `=== "Cold-Start Install"` (Step 2: Configure environment variables)
   - Line 91: `=== "Cold-Start Install"` (Step 3: Start the stack)
   - Line 113: `=== "Cold-Start Install"` (Step 4: Verify)

   Replace all three with `=== "Quick Start"`. A global find-and-replace on the string `Cold-Start Install` covers all three instances in one pass.

2. REPLACE the Step 3 prose at line 97. Current text (within the Quick Start tab under Step 3):
   ```
       This starts: Caddy (reverse proxy + TLS, port 8443), the Agent Service (port 8001), and PostgreSQL. The two built-in puppet nodes start automatically but require JOIN_TOKEN_1 and JOIN_TOKEN_2 to be set in your `.env` before they can enroll.
   ```
   Replace with:
   ```
       This starts: Caddy (reverse proxy + TLS, port 8443), the Agent Service (port 8001), and PostgreSQL.
   ```
   The four-space indent before "This starts" must be preserved (MkDocs Material requires consistent indentation inside tab blocks). No forward pointer needed — the "Next: Enroll a Node" link at the bottom of the page already handles that.

No other changes to install.md. The filename, all other tab labels ("Server Install", "Git Clone", "GHCR Pull (no git required)", "Dashboard", "CLI"), all other prose, and all links remain unchanged.
  </action>
  <verify>
    <automated>grep -c "Cold-Start Install\|JOIN_TOKEN_1\|JOIN_TOKEN_2\|built-in puppet" /home/thomas/Development/master_of_puppets/docs/docs/getting-started/install.md; echo "count above must be 0"</automated>
  </verify>
  <done>grep count is 0 for all four prohibited strings; file contains exactly three occurrences of '=== "Quick Start"'; Step 3 Quick Start tab prose ends with "...and PostgreSQL." and contains no JOIN_TOKEN reference</done>
</task>

</tasks>

<verification>
After both tasks complete, run the combined smoke check:

```bash
# All counts must be 0
grep -c "Cold-Start Install\|JOIN_TOKEN_1\|JOIN_TOKEN_2\|built-in puppet\|puppet-node-1\|puppet-node-2\|node1-secrets\|node2-secrets" \
  /home/thomas/Development/master_of_puppets/docs/docs/getting-started/install.md \
  /home/thomas/Development/master_of_puppets/puppeteer/compose.cold-start.yaml

# YAML must parse cleanly
docker compose -f /home/thomas/Development/master_of_puppets/puppeteer/compose.cold-start.yaml config --quiet && echo "YAML_OK"
```
</verification>

<success_criteria>
1. Running `docker compose -f compose.cold-start.yaml up -d` starts exactly 5 services: db, cert-manager, agent, dashboard, docs — no puppet-node-1, puppet-node-2, or node secrets volumes.
2. `install.md` contains zero occurrences of "Cold-Start Install", "JOIN_TOKEN_1", "JOIN_TOKEN_2", or "built-in puppet".
3. All three tabbed sections in install.md (Steps 2, 3, 4) show the "Quick Start" tab label.
4. Step 3 Quick Start prose matches: "This starts: Caddy (reverse proxy + TLS, port 8443), the Agent Service (port 8001), and PostgreSQL."
5. `docker compose -f puppeteer/compose.cold-start.yaml config --quiet` exits 0 (valid YAML).
</success_criteria>

<output>
After completion, create `.planning/phases/79-install-docs-cleanup/79-01-SUMMARY.md` using the summary template.
</output>
