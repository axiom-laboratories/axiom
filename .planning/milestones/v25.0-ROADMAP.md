# Roadmap Archive: v25.0 EE Validation & Infrastructure

**Archived:** 2026-05-05
**Status:** SHIPPED
**Phases:** 173–175 (3 phases, 7 plans)
**Requirements:** 21/21 satisfied

---

## Milestones

- ✅ **v25.0 — EE Validation & Infrastructure** — Phases 173–175 (shipped 2026-05-05)

---

## Phases

- [x] **Phase 173: EE Behavioural Validation Test Suite** — Build comprehensive automated pytest suite in `mop_validation` covering all 14 CE/EE behavioural scenarios (VAL-01 through VAL-14) (completed 2026-05-05)
  - [x] Plan 01: Test fixtures + CE validation (VAL-01, VAL-02, VAL-03) — shared pytest fixtures for CE-only and EE installs; table count assertion; feature flag endpoint; stub route sweep
  - [x] Plan 02: Licence state machine tests (VAL-04 through VAL-09) — valid, GRACE, EXPIRED, absent, tampered-signature scenarios
  - [x] Plan 03: Wheel + boot log security tests (VAL-10, VAL-11, VAL-13) — bad SHA256 manifest, non-whitelisted entry point, clock-rollback HMAC
  - [x] Plan 04: Node limit test + coverage assertion (VAL-12, VAL-14) — enrollment 402 at capacity; assert all VAL scenarios covered

- [x] **Phase 174: mop_validation Repo Migration** — Transfer `mop_validation` to `axiom-laboratories` GitHub organisation as private repo; update all references (completed 2026-04-21)
  - [x] Plan 01: GitHub org transfer — initiate repo transfer to `axiom-laboratories` org; verify repo is accessible; update local git remote
  - [x] Plan 02: Reference updates — update `CLAUDE.md`, `GEMINI.md`, and `ROADMAP.md` with `axiom-laboratories` org references

- [x] **Phase 175: Licence Architecture Analysis** — Structured comparison of three issued-licence storage approaches with concrete recommendation (completed 2026-04-21)
  - [x] Plan 01: Research + comparison + recommendation — investigate `axiom-licences` repo structure; survey three options; write `LIC-ANALYSIS.md` with comparison table, rationale, and migration path

---

## Phase Goals

### Phase 173 Goal
Build a comprehensive automated test suite in `mop_validation` covering all 14 CE/EE behavioural scenarios (VAL-01 through VAL-14) — zero manual-only steps.

**Success Criteria (all met):**
1. CE-only fixture: exactly 15 tables present, no EE schema, all feature flags false, 7 stub routes return 402 ✓
2. EE fixture: 41 tables (15 CE + 26 EE) with valid licence; all EE flags true; `GET /api/licence` returns `status=VALID` ✓
3. Licence state machine: GRACE banner visible, EXPIRED triggers DEGRADED_CE (pull_work empty, not 402), absent/tampered licence → CE mode + no crash ✓
4. Wheel security chain: tampered SHA256 manifest raises `RuntimeError`; non-whitelisted entry point raises `RuntimeError`; EE does not load in either case ✓
5. Boot log HMAC: clock-rollback raises `RuntimeError` on EE; CE emits warning only ✓
6. Node limit: enrollment returns 402 when `active_nodes ≥ node_limit`; existing enrolled nodes continue ✓
7. All 14 VAL scenarios covered by automated pytest tests; `pytest mop_validation/tests/` passes with zero skips ✓

### Phase 174 Goal
Transfer `mop_validation` to the `axiom-laboratories` GitHub organisation as a private repo; update all references so tooling, scripts, and CI continue to work without modification.

**Success Criteria (all met):**
1. `mop_validation` accessible at `github.com/axiom-laboratories/mop_validation` (private repo) ✓
2. All scripts in `mop_validation/scripts/` execute correctly from the new remote ✓
3. Local git remote `origin` points to `github.com/axiom-laboratories/mop_validation` ✓
4. `CLAUDE.md` and `GEMINI.md` in `master_of_puppets` updated to reference the new org URL ✓

### Phase 175 Goal
Produce a structured, evidence-based comparison of three issued-licence storage approaches and deliver a concrete recommendation with rationale.

**Success Criteria (all met):**
1. Comparison covers all three options across all six dimensions ✓
2. "Why this over the others" rationale section present with single concrete recommendation ✓
3. Migration path documented with effort estimate (two-phase: Git now → VPS at scale) ✓
4. Delivered as `.planning/LIC-ANALYSIS.md` ✓

---

## Key Outputs

- `mop_validation/tests/conftest.py` — module-scoped CE/EE LXC fixtures, `ee_licence_fixtures` session fixture generating 5 JWT keys (valid, expired, grace, tampered, node_limit)
- `mop_validation/tests/helpers.py` — `incus_exec`, `wait_for_api_ready`, `inject_licence_and_restart`, `patch_agent_test_pubkey`, `patch_ee_plugin_for_testing`
- `mop_validation/tests/test_173_01_ce_validation.py` — VAL-01, VAL-02, VAL-03
- `mop_validation/tests/test_173_02_ee_licence_states.py` — VAL-04, VAL-05, VAL-06, VAL-07, VAL-08, VAL-09
- `mop_validation/tests/test_173_03_wheel_security.py` — VAL-10, VAL-11, VAL-13 (offline, no live stack)
- `mop_validation/tests/test_173_04_node_limit.py` — VAL-12, VAL-14
- `.planning/LIC-ANALYSIS.md` — 655-line document with comparison table, two-phase recommendation, VPS architecture wireframe (Hetzner CX11, FastAPI+SQLite/Postgres, Caddy TLS, 4 endpoints, online/airgapped JWT claim)
