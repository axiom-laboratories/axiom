---
phase: 55-verification-docs-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/thomas/Development/mop_validation/scripts/test_sched03_modal.py
  - .planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04

must_haves:
  truths:
    - "48-VERIFICATION.md exists in .planning/phases/48-scheduled-job-signing-safety/ with status: passed"
    - "SCHED-01 evidence recorded: code pointer to scheduler_service.py DRAFT transition + 4 unit tests green"
    - "SCHED-02 evidence recorded: code pointer to SKIP_STATUSES guard + verbatim log message + test_draft_skip_log_message green"
    - "SCHED-03 evidence recorded: Playwright test runs and confirms modal appears before DRAFT save"
    - "SCHED-04 evidence recorded: code pointer to AlertService.create_alert calls + test_draft_transition_creates_alert green"
  artifacts:
    - path: "/home/thomas/Development/mop_validation/scripts/test_sched03_modal.py"
      provides: "Playwright SCHED-03 modal evidence test"
    - path: ".planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md"
      provides: "Goal-backward verification report for Phase 48"
      contains: "status: passed"
  key_links:
    - from: "48-VERIFICATION.md"
      to: "puppeteer/agent_service/services/scheduler_service.py"
      via: "code pointer with line numbers for DRAFT transition"
    - from: "48-VERIFICATION.md"
      to: "puppeteer/agent_service/tests/test_scheduler_service.py"
      via: "pytest run output block (6 tests)"
    - from: "48-VERIFICATION.md"
      to: "test_sched03_modal.py"
      via: "Playwright run output confirming modal visible"
---

<objective>
Retroactively produce the VERIFICATION.md that was skipped when Phase 48 completed. This involves writing a Playwright test for SCHED-03 (the one requirement that was manually-verified only), running all automated evidence (pytest + Playwright), and documenting the goal-backward analysis in the standard verification format.

Purpose: Phase 48 implementation is fully present in the codebase but has no VERIFICATION.md. The v12.0 milestone audit flagged this as a process gap. This plan closes it.
Output: `48-VERIFICATION.md` in the Phase 48 directory; `test_sched03_modal.py` in mop_validation/scripts/.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-verification-docs-cleanup/55-CONTEXT.md
@.planning/phases/55-verification-docs-cleanup/55-RESEARCH.md
@.planning/phases/48-scheduled-job-signing-safety/48-VALIDATION.md

<interfaces>
<!-- Key implementation locations confirmed by RESEARCH.md code inspection -->

From puppeteer/agent_service/services/scheduler_service.py (approximate lines):
- SCHED-01: DRAFT transition logic in update_job_definition() — "case (d): script changed, no signature → soft DRAFT transition"; job.status set to "DRAFT"; AlertService.create_alert called
- SCHED-02: SKIP_STATUSES = {"DRAFT", "REVOKED", "DEPRECATED"} guard at top of execute_scheduled_job(); verbatim reason string: "Skipped: job in DRAFT state, pending re-signing"
- SCHED-04: AlertService.create_alert(db_session, ...) calls at ~lines 491-497 and 526-530

From puppeteer/agent_service/tests/test_scheduler_service.py (6 tests):
- test_update_script_without_sig_transitions_to_draft (SCHED-01)
- test_update_script_with_sig_stays_active (SCHED-01)
- test_draft_reedits_no_duplicate_alert (SCHED-01)
- test_resign_without_script_change_reactivates (SCHED-01)
- test_draft_skip_log_message (SCHED-02)
- test_draft_transition_creates_alert (SCHED-04)

From puppeteer/dashboard/src/views/JobDefinitions.tsx:
- SCHED-03: "Save & Go to DRAFT" button confirmed present at ~lines 355-476

SCHED-03 Playwright pattern (from CLAUDE.md + RESEARCH.md):
- Auth: POST /api/auth/token with data={} form-encoded (NOT json={}); localStorage key: mop_auth_token
- Browser: chromium.launch(args=['--no-sandbox'], headless=True)
- Stack URL: https://localhost:8443 (Caddy frontend); API: https://localhost:8001
- Test fixture: create ACTIVE scheduled job via API before navigating UI
- Verify: modal dialog contains "DRAFT" text after editing script without changing signature
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Playwright SCHED-03 modal test</name>
  <files>/home/thomas/Development/mop_validation/scripts/test_sched03_modal.py</files>
  <action>
Create a Python Playwright test at `/home/thomas/Development/mop_validation/scripts/test_sched03_modal.py` that confirms the DRAFT confirmation modal appears when an operator edits a scheduled job's script without providing a new signature.

Test structure:
1. Read admin credentials from `/home/thomas/Development/mop_validation/secrets.env` (same pattern as other mop_validation scripts — load file, parse KEY=VALUE lines).
2. POST to `https://localhost:8001/api/auth/token` with `data={"username": "admin", "password": "<password>"}` (form-encoded, NOT json=) and `verify=False`.
3. Via API, create a test fixture ACTIVE scheduled job using `POST /api/jobs/definitions` — minimal payload: name, cron_expression (`"0 3 * * *"`), script_content, task_type ("script"), runtime ("python"). Store its `id` for cleanup.
4. With `sync_playwright()` launch `chromium.launch(args=['--no-sandbox'], headless=True)`.
5. `new_context(ignore_https_errors=True)`, new page, navigate to `https://localhost:8443`, evaluate `localStorage.setItem('mop_auth_token', token)`.
6. Navigate to `https://localhost:8443/job-definitions`, wait for networkidle.
7. Find the test fixture job row (match by name). Click the edit (pencil) button.
8. In the edit modal, find the script textarea (the one holding script content, not signature). Clear it and type new script content without touching signature fields.
9. Click the save/submit button. Assert that a dialog containing the text "DRAFT" is visible on the page (do NOT click "Save & Go to DRAFT" — just assert the dialog appeared).
10. Close browser. DELETE `/api/jobs/definitions/{id}` via API to clean up the fixture.
11. Print "SCHED-03 PASSED: DRAFT confirmation modal appeared before save" on success. Exit code 0. On any assertion failure, print details and exit code 1.

Use `requests` for API calls. Wrap the entire test in try/finally to ensure fixture cleanup even on failure.

Do NOT use the login form (React controlled inputs unreliable with fill()). Always inject JWT via localStorage.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets && docker compose -f puppeteer/compose.server.yaml ps --services --filter "status=running" | grep -c agent</automated>
  </verify>
  <done>File `/home/thomas/Development/mop_validation/scripts/test_sched03_modal.py` exists and is runnable. File contains sync_playwright, --no-sandbox, localStorage injection, and a dialog assertion for "DRAFT" text.</done>
</task>

<task type="auto">
  <name>Task 2: Run evidence and produce VERIFICATION.md</name>
  <files>.planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md</files>
  <action>
Run all verification evidence, then write the VERIFICATION.md for Phase 48.

Step 1 — Ensure Docker stack is up:
```bash
cd /home/thomas/Development/master_of_puppets && docker compose -f puppeteer/compose.server.yaml up -d
```
Wait ~15s for agent to be healthy before running tests.

Step 2 — Run pytest evidence inside the agent container:
```bash
docker compose -f puppeteer/compose.server.yaml exec agent python -m pytest \
  agent_service/tests/test_scheduler_service.py -v \
  2>&1
```
Capture the full -v output. All 6 tests must pass. If any fail, investigate and document the failure in the VERIFICATION.md under "Test Failures" — do NOT block the plan on pre-existing unrelated failures in other test files.

Step 3 — Run the SCHED-03 Playwright test:
```bash
python /home/thomas/Development/mop_validation/scripts/test_sched03_modal.py \
  2>&1
```
Capture the output. If the stack is not reachable, document that explicitly in the VERIFICATION.md.

Step 4 — Write `.planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md` following the exact format used in Phase 52 (52-VERIFICATION.md):

Frontmatter:
```yaml
---
phase: 48-scheduled-job-signing-safety
verified: <ISO timestamp of this run>
status: passed
score: 4/4 must-haves verified
re_verification: true
---
```
(Use re_verification: true since Phase 48 already completed — this is retroactive.)

Sections:
1. **Phase Goal** (copy from ROADMAP.md Phase 48 goal)
2. **Goal Achievement** — Observable Truths table (4 rows, one per SCHED requirement):
   - SCHED-01: code pointer to scheduler_service.py DRAFT transition + test names
   - SCHED-02: code pointer to SKIP_STATUSES guard + verbatim message + test name
   - SCHED-03: reference to Playwright test run output
   - SCHED-04: code pointer to AlertService.create_alert calls + test name
3. **Required Artifacts** table — list scheduler_service.py (DRAFT transition + skip guard + alert calls), test_scheduler_service.py (6 tests), JobDefinitions.tsx / JobDefinitionList.tsx (SCHED-03 modal and re-sign dialog), JobDefinitionModal.tsx (save intercept)
4. **Key Link Verification** — document: update_job_definition → DRAFT status change, execute_scheduled_job → SKIP_STATUSES guard, DRAFT transition → AlertService.create_alert, edit modal → DRAFT confirmation dialog
5. **Requirements Coverage** table (SCHED-01 through SCHED-04, each SATISFIED)
6. **Automated Test Evidence** block — paste the full pytest -v output from Step 2
7. **Playwright Evidence** block — paste the output from Step 3
8. **Gaps Summary** — "No gaps. All four SCHED requirements implemented and verified."
9. **Re-verification Note** — "Phase 48 completed 2026-03-22. VERIFICATION.md not produced at that time (process omission). This report is produced retroactively in Phase 55 based on code inspection and automated test runs."

If the Playwright test fails because no suitable ACTIVE job exists or the stack is not accessible, document as: "SCHED-03 Playwright: Stack not accessible during verification — manual verification record from Phase 48 VALIDATION.md substituted (approved 2026-03-22)."
  </action>
  <verify>
    <automated>test -f /home/thomas/Development/master_of_puppets/.planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md && grep -q "status: passed" /home/thomas/Development/master_of_puppets/.planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md && echo "VERIFICATION.md exists and shows passed"</automated>
  </verify>
  <done>48-VERIFICATION.md exists in the Phase 48 directory with status: passed, score: 4/4, sections for all four SCHED requirements, actual pytest output embedded, and a re_verification: true note.</done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md` — file exists
2. `grep -q "status: passed" .planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md` — status is passed
3. `grep -c "SCHED-0" .planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md` — returns 4+ (all requirements covered)
4. `test -f /home/thomas/Development/mop_validation/scripts/test_sched03_modal.py` — Playwright test file exists
</verification>

<success_criteria>
- 48-VERIFICATION.md exists at `.planning/phases/48-scheduled-job-signing-safety/48-VERIFICATION.md`
- Status is `passed`, score is `4/4`
- All four SCHED-01 through SCHED-04 requirements appear as SATISFIED with code evidence
- Pytest output from Docker exec embedded in the document (all 6 tests green)
- SCHED-03 either has Playwright evidence or has a documented substitute referencing the Phase 48 manual verification
- test_sched03_modal.py committed to mop_validation/scripts/
</success_criteria>

<output>
After completion, create `.planning/phases/55-verification-docs-cleanup/55-01-SUMMARY.md`
</output>
