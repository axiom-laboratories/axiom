---
phase: 55-verification-docs-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - RT-06

must_haves:
  truths:
    - "RT-06 checkbox shows [x] with Dropped status, Phase 47/55 in traceability table"
    - "SCHED-01 through SCHED-04 all show [x] with Complete status, Phase 48 in traceability table"
    - "Coverage count Pending (gap closure) reads 0"
    - "Traceability table Phase 54 rows (VIS-02, SRCH-10, JOB-01, RT-01, RT-02, JOB-04, JOB-05) all show Complete"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirements state for all v12.0 requirements"
      contains: "Pending (gap closure): 0"
  key_links:
    - from: "REQUIREMENTS.md RT-06 line"
      to: "Phase 47 decision record in STATE.md"
      via: "inline annotation: Decision recorded: Phase 55"
    - from: "REQUIREMENTS.md traceability table"
      to: "Phase 48 implementation"
      via: "SCHED-01 through SCHED-04 rows Phase 48 / Complete"
---

<objective>
Bring REQUIREMENTS.md fully up to date: tick RT-06 as closed-by-design (Dropped), tick SCHED-01–04 as Complete, backfill Phase 54 traceability rows, and recount coverage totals from scratch.

Purpose: REQUIREMENTS.md has been stale since Phase 54 completed — 12 requirements were listed as Pending but most had already been delivered. This plan makes the document accurate.
Output: Updated `.planning/REQUIREMENTS.md` with zero stale Pending items.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/55-verification-docs-cleanup/55-CONTEXT.md

<interfaces>
<!-- Locked decisions from CONTEXT.md — implement exactly as specified -->

RT-06 requirements list line — FINAL FORM (locked):
  - [x] **RT-06**: ~~Existing `python_script` task type is retained as an alias~~ — **Dropped by design** (Phase 47 planning decision: `python_script` returns HTTP 422; operators use `script` + `runtime: python`). Decision recorded: Phase 55.

RT-06 traceability row — FINAL FORM (locked):
  | RT-06 | Phase 47/55 | Dropped |

SCHED-01 through SCHED-04 traceability rows — FINAL FORM (locked):
  | SCHED-01 | Phase 48 | Complete |
  | SCHED-02 | Phase 48 | Complete |
  | SCHED-03 | Phase 48 | Complete |
  | SCHED-04 | Phase 48 | Complete |

Phase 54 traceability rows to verify and confirm Complete (these may already show Complete — check and confirm, do not change if already correct):
  VIS-02, SRCH-10, JOB-01, RT-01, RT-02, JOB-04, JOB-05 — all should be Phase 54 / Complete

Coverage count format (keep "Pending (gap closure): 0" line even when zero):
  - v12.0 requirements: 44 total
  - Mapped to phases: 44 (recount per-phase numbers)
  - Pending (gap closure): 0
  - Unmapped: 0 ✓
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md — checkboxes, traceability, coverage count</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md` in full. Make all of the following edits in a single write:

**Edit 1 — RT-06 checkbox (requirements list section):**
Change the RT-06 line from its current `[ ]` form to exactly:
`- [x] **RT-06**: ~~Existing \`python_script\` task type is retained as an alias~~ — **Dropped by design** (Phase 47 planning decision: \`python_script\` returns HTTP 422; operators use \`script\` + \`runtime: python\`). Decision recorded: Phase 55.`

**Edit 2 — SCHED-01 through SCHED-04 checkboxes (requirements list section):**
Change all four `[ ]` to `[x]`. The requirement text stays unchanged.

**Edit 3 — RT-06 traceability row:**
Change the RT-06 row from `| RT-06 | Phase 55 | Pending |` to `| RT-06 | Phase 47/55 | Dropped |`

**Edit 4 — SCHED-01 through SCHED-04 traceability rows:**
Change all four rows from `| SCHED-0N | Phase 55 | Pending |` to `| SCHED-0N | Phase 48 | Complete |`

**Edit 5 — Phase 54 traceability rows:**
Verify these seven rows already show `Complete`: VIS-02, SRCH-10, JOB-01, RT-01, RT-02, JOB-04, JOB-05. If any show `Pending`, change to `| <ID> | Phase 54 | Complete |`.

**Edit 6 — Coverage count block:**
Recount from scratch: count every `- [x]` and `- [ ]` line in the v12.0 requirements list section (Runtime Expansion, Job Submission UX, Bulk Job Operations, Queue & Visibility, Scheduled Job Signing Safety, Search Scale & Data Management, Tech Debt, Security, Branding). Do NOT count lines in Future Requirements, Out of Scope, or the Traceability section.

After counting:
- Total = sum of all `[x]` and `[ ]` entries (should be 44 — RT-06 counts as 1 even though Dropped)
- Pending = count of `[ ]` entries (should be 0 after this edit)
- Update per-phase breakdown: count which requirements map to which phase from the traceability table

Replace the entire Coverage block with the accurate counts. Keep the format:
```
**Coverage:**
- v12.0 requirements: 44 total
- Mapped to phases: 44 (Phase 46: N, Phase 47: N, Phase 48: N, ...)
- Pending (gap closure): 0
- Unmapped: 0 ✓
```
Keep the "Pending (gap closure): 0" line (do not remove it — shows the count is accurate, not omitted per CONTEXT.md).

**Edit 7 — Last updated line:**
Update the footer timestamp: `*Last updated: 2026-03-23 — Phase 55: RT-06 closed as Dropped; SCHED-01–04 marked Complete; Phase 54 traceability backfilled; coverage count recalculated*`

Write the complete updated file back to `.planning/REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>grep -c "\- \[x\]" /home/thomas/Development/master_of_puppets/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
- RT-06 line shows `[x]` with "Dropped by design" and "Decision recorded: Phase 55"
- SCHED-01 through SCHED-04 all show `[x]`
- Traceability table: RT-06 = Dropped/Phase 47/55; SCHED-01–04 = Complete/Phase 48; Phase 54 rows = Complete
- Coverage block shows "Pending (gap closure): 0"
- `grep "Pending (gap closure): 0" .planning/REQUIREMENTS.md` returns a match
  </done>
</task>

</tasks>

<verification>
1. `grep "\- \[x\] \*\*RT-06\*\*" .planning/REQUIREMENTS.md` — RT-06 ticked
2. `grep "Dropped" .planning/REQUIREMENTS.md` — "Dropped" appears in both the requirement line and traceability row
3. `grep "47/55" .planning/REQUIREMENTS.md` — Phase 47/55 appears in RT-06 traceability row
4. `grep -c "\- \[ \]" .planning/REQUIREMENTS.md` — returns 0 (no unchecked requirements in v12.0)
5. `grep "Pending (gap closure): 0" .planning/REQUIREMENTS.md` — coverage count shows zero pending
</verification>

<success_criteria>
- All v12.0 requirements in REQUIREMENTS.md show `[x]`
- RT-06 traceability row: Phase 47/55, Status Dropped
- SCHED-01–04 traceability rows: Phase 48, Status Complete
- Phase 54 rows (VIS-02, SRCH-10, JOB-01, RT-01, RT-02, JOB-04, JOB-05): Phase 54, Status Complete
- Coverage count block shows Pending (gap closure): 0
- `grep -c "- [ ]" .planning/REQUIREMENTS.md` returns 0
</success_criteria>

<output>
After completion, create `.planning/phases/55-verification-docs-cleanup/55-02-SUMMARY.md`
</output>
