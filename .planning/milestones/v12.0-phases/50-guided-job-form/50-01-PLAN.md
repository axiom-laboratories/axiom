---
phase: 50-guided-job-form
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx
autonomous: true
requirements:
  - JOB-01
  - JOB-02
  - JOB-03

must_haves:
  truths:
    - "Test file exists with 11 failing stubs covering guided form render, JSON preview, and advanced mode"
    - "Each stub fails with a meaningful description of the expected behaviour"
    - "Running the test command exits non-zero (red phase confirmed)"
  artifacts:
    - path: "puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx"
      provides: "Wave 0 test scaffold for GuidedDispatchCard behaviours"
      contains: "GuidedDispatchCard"
  key_links:
    - from: "puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx"
      to: "puppeteer/dashboard/src/components/GuidedDispatchCard.tsx"
      via: "import in test file (component does not exist yet — import will fail or be mocked)"
      pattern: "GuidedDispatchCard"
---

<objective>
Create the Wave 0 test scaffold: 11 failing stubs that define the expected behaviours for JOB-01, JOB-02, and JOB-03. These stubs must fail (red) before implementation begins.

Purpose: Establish the Nyquist contract — every behaviour has a test expectation before code is written. Plans 02 and 03 turn these stubs green.
Output: `puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx` with 11 failing describe/it blocks.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-guided-job-form/50-CONTEXT.md
@.planning/phases/50-guided-job-form/50-RESEARCH.md
@.planning/phases/50-guided-job-form/50-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 11 failing test stubs for GuidedDispatchCard</name>
  <files>puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx</files>
  <action>
Create `puppeteer/dashboard/src/views/__tests__/Jobs.test.tsx` with 11 failing test stubs. The file must:

1. Import vitest (`describe`, `it`, `expect`, `vi`) and `@testing-library/react` (`render`, `screen`, `fireEvent`).
2. Import `GuidedDispatchCard` from `'../../components/GuidedDispatchCard'` — this import will fail or resolve to nothing until Plan 02 creates the component; that is intentional for Wave 0.
3. Mock `../../auth` to provide `authenticatedFetch` that returns a resolved empty response (prevents network calls).

Write exactly these 9 test stubs as `it.fails` or `it('...', () => { expect.fail('not implemented') })` pattern — use `expect.fail` approach so tests fail with a clear message:

**JOB-01 stubs (guided form):**
- "renders runtime selector, script textarea, node dropdown, target tag chip input, and capability chip input"
- "Dispatch button is disabled when no targeting field is provided"
- "Dispatch button is enabled when a target tag is added and both signature fields are non-empty"
- "calls POST /jobs with correctly structured payload on dispatch"
- "shows amber warning and clears signature fields when script content changes after signature is entered"

**JOB-02 stubs (JSON preview):**
- "JSON preview accordion is collapsed by default"
- "JSON preview shows generated payload that updates live as form fields change"

**JOB-03 stubs (advanced mode):**
- "ADV button shows confirmation dialog before switching to Advanced mode"
- "confirming Advanced mode pre-fills JSON editor with serialised guided form values"
- "Dispatch button is disabled in Advanced mode when JSON is invalid"
- "Reset button in Advanced mode shows confirmation dialog and returns to blank guided form"

That is 11 stubs covering all 3 requirements. Use `it('description', () => { throw new Error('not implemented') })` for each stub so the test runner reports failures, not skips.

Add a top-level `describe('GuidedDispatchCard', () => { ... })` wrapper.

Do NOT write any implementation or assertions beyond the `throw new Error('not implemented')` body.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npx vitest run src/views/__tests__/Jobs.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists; running the test command reports 11 failing tests (red). Zero passing. Zero skipped.</done>
</task>

</tasks>

<verification>
`cd puppeteer/dashboard && npx vitest run src/views/__tests__/Jobs.test.tsx` exits non-zero with all 11 tests failing.
</verification>

<success_criteria>
- Jobs.test.tsx exists with 11 stubs grouped under `describe('GuidedDispatchCard')`
- All 11 tests fail (throw Error or use expect.fail) — none pass, none skip
- File imports GuidedDispatchCard (component does not exist yet — Wave 0 confirms red before green)
</success_criteria>

<output>
After completion, create `.planning/phases/50-guided-job-form/50-01-SUMMARY.md`
</output>
