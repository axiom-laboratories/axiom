---
phase: 73-ee-licence-system
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - puppeteer/tests/test_licence_service.py
autonomous: true
requirements:
  - LIC-01
  - LIC-02
  - LIC-03
  - LIC-04
  - LIC-05
  - LIC-06
  - LIC-07

must_haves:
  truths:
    - "7 RED failing tests cover every LIC requirement before any implementation exists"
    - "test_generate_licence_jwt verifies that a valid EdDSA JWT payload round-trips correctly"
    - "test_invalid_signature_falls_to_ce verifies tampered token returns CE state"
    - "test_grace_period_active verifies is_ee_active=True when within grace window"
    - "test_degraded_ce_state verifies is_ee_active=False when grace has elapsed"
    - "test_clock_rollback_detection verifies rollback returns False and strict mode raises"
    - "test_licence_status_endpoint verifies /api/licence JSON shape for each state"
    - "test_enroll_node_limit_enforced verifies 402 raised when active_count >= node_limit"
  artifacts:
    - path: "puppeteer/tests/test_licence_service.py"
      provides: "7 failing unit tests for all LIC requirements"
      exports: []
  key_links:
    - from: "puppeteer/tests/test_licence_service.py"
      to: "puppeteer/agent_service/services/licence_service.py"
      via: "import after Wave 1 creates the module"
      pattern: "from puppeteer.agent_service.services.licence_service import"
---

<objective>
Write 7 failing unit tests that define the exact contract for the licence system before any implementation exists.

Purpose: RED phase of TDD — tests document the required behaviour for all 7 LIC requirements.
Output: `puppeteer/tests/test_licence_service.py` with 7 failing tests that turn GREEN after plan 02/03.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-ee-licence-system/73-CONTEXT.md
@.planning/phases/73-ee-licence-system/73-RESEARCH.md
@.planning/phases/73-ee-licence-system/73-VALIDATION.md
</context>

<feature>
  <name>Licence system RED tests — Wave 0</name>
  <files>puppeteer/tests/test_licence_service.py</files>
  <behavior>
    - test_generate_licence_jwt: generate an Ed25519 keypair + sign a JWT payload containing all required fields (customer_id, tier, node_limit, features, exp, grace_days, iat, issued_to, contact_email, licence_id, version); decode and assert all fields round-trip correctly
    - test_invalid_signature_falls_to_ce: call load_licence() with a JWT whose signature has been tampered (flip last char); assert returned LicenceState.status == "ce" and is_ee_active == False
    - test_grace_period_active: call _compute_state() with exp = time.time() - 60 (1 minute ago) and grace_days = 30; assert status == "grace" and is_ee_active == True
    - test_degraded_ce_state: call _compute_state() with exp = time.time() - 31*86400 (31 days ago) and grace_days = 30; assert status == "expired" and is_ee_active == False
    - test_clock_rollback_detection: write a boot.log with a future timestamp, call check_and_record_boot(); assert return value is False (rollback detected); test strict mode by patching os.getenv("AXIOM_STRICT_CLOCK") = "true" and assert RuntimeError raised
    - test_licence_status_endpoint: create a mock FastAPI Request with app.state.licence_state set to a LicenceState with status="grace"; call the get_licence_status route handler; assert JSON response contains status, days_until_expiry, node_limit, tier, customer_id, grace_days
    - test_enroll_node_limit_enforced: call enroll_node() with a mock db that returns active_count == node_limit (e.g. 5 == 5) and app.state.licence_state.node_limit == 5; assert HTTPException status_code == 402 raised
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED test file for all 7 LIC requirements</name>
  <files>puppeteer/tests/test_licence_service.py</files>
  <behavior>
    - test_generate_licence_jwt: imports from licence_service (NameError expected) — RED
    - test_invalid_signature_falls_to_ce: imports load_licence (NameError expected) — RED
    - test_grace_period_active: imports _compute_state (NameError expected) — RED
    - test_degraded_ce_state: imports _compute_state (NameError expected) — RED
    - test_clock_rollback_detection: imports check_and_record_boot (NameError expected) — RED
    - test_licence_status_endpoint: imports get_licence_status handler (NameError expected) — RED
    - test_enroll_node_limit_enforced: patches enroll_node with licence_state mock (NameError expected) — RED
  </behavior>
  <action>
    Create `puppeteer/tests/test_licence_service.py` with exactly 7 test functions.

    CRITICAL imports (use these exact paths — they must match where plan 02/03 create the modules):
    ```python
    import pytest
    import time
    import tempfile
    from pathlib import Path
    from unittest.mock import patch, AsyncMock, MagicMock
    from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey
    from cryptography.hazmat.primitives import serialization
    import jwt as _jwt
    ```

    For tests that call licence_service functions, import at function scope so the NameError is the failure, not import-time crash:
    ```python
    def test_generate_licence_jwt():
        from puppeteer.agent_service.services.licence_service import _decode_licence_jwt, LicenceState
        ...
    ```

    Tests must cover these exact function/class names (plan 02 will implement them):
    - `from puppeteer.agent_service.services.licence_service import _decode_licence_jwt, _compute_state, load_licence, check_and_record_boot, LicenceState, LicenceStatus`
    - `from puppeteer.agent_service.main import enroll_node` (already importable)
    - `/api/licence` route: test via `from puppeteer.agent_service.main import app` + TestClient or direct route handler

    test_generate_licence_jwt:
    - Generate a test keypair inline (Ed25519PrivateKey.generate())
    - Sign a payload with all required fields using `jwt.encode(payload, private_key, algorithm="EdDSA")`
    - Call `_decode_licence_jwt(token, pub_key)` — but wait, _decode_licence_jwt uses the hardcoded key; for this test, test the encoding roundtrip directly via jwt.decode with the test public key
    - Assert all expected fields present in decoded payload

    test_invalid_signature_falls_to_ce:
    - Generate valid JWT with a test key
    - Tamper last 4 chars of token
    - Patch `puppeteer.agent_service.services.licence_service._pub_key` with test public key
    - Call `load_licence()` with env var patched to the tampered token
    - Assert result.status.value == "ce" and result.is_ee_active == False

    test_grace_period_active:
    - Import _compute_state
    - Call with payload = {"exp": int(time.time()) - 60, "grace_days": 30, "tier": "ee", "node_limit": 5, "features": [], "customer_id": "test", "iat": 0}
    - Assert result.status.value == "grace" and result.is_ee_active == True

    test_degraded_ce_state:
    - Call _compute_state with exp = int(time.time()) - 31*86400, grace_days = 30
    - Assert result.status.value == "expired" and result.is_ee_active == False

    test_clock_rollback_detection:
    - Use tempfile to create a boot.log with a future timestamp (2 hours ahead)
    - Patch BOOT_LOG_PATH to point at the temp file
    - Call check_and_record_boot() — assert returns False
    - For strict mode: patch os.environ["AXIOM_STRICT_CLOCK"] = "true" and assert RuntimeError raised

    test_licence_status_endpoint:
    - Use FastAPI TestClient from `from fastapi.testclient import TestClient`
    - Create a minimal test app or use the main app
    - Patch app.state.licence_state with a LicenceState(status=LicenceStatus.GRACE, ...)
    - GET /api/licence with a valid auth token (mock require_auth via override_dependency)
    - Assert response.json() has keys: status, days_until_expiry, node_limit, tier, customer_id, grace_days

    test_enroll_node_limit_enforced:
    - Mock db.execute to return active_count = 5
    - Create mock request with request.app.state.licence_state.node_limit = 5
    - Call enroll_node(req, mock_request, mock_db)
    - Assert HTTPException raised with status_code == 402
    - Follow exact mock pattern from test_lifecycle_enforcement.py (mock_db.execute side_effect)

    Run after writing: `cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_licence_service.py -x -q 2>&1 | head -30`
    All 7 tests must fail (ImportError or similar) — if any PASS that means the test is not testing anything yet.
    Commit: `test(73-01): add RED tests for LIC-01 through LIC-07`
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_licence_service.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 tests exist and fail (ImportError/AttributeError/ModuleNotFoundError) — RED phase confirmed. File committed.</done>
</task>

</tasks>

<verification>
`cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_licence_service.py -q 2>&1 | tail -10`

Expected: 7 errors (FAILED or ERROR status), 0 passing. The test file exercises all 7 LIC requirement behaviours.
</verification>

<success_criteria>
- `puppeteer/tests/test_licence_service.py` exists with exactly 7 test functions
- All 7 tests fail due to missing implementation (ImportError or AttributeError)
- Each test maps to one LIC requirement (LIC-01 through LIC-07)
- Commit `test(73-01): add RED tests for LIC-01 through LIC-07` exists
</success_criteria>

<output>
After completion, create `.planning/phases/73-ee-licence-system/73-01-SUMMARY.md`
</output>
