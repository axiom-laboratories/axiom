---
phase: 76-v14.3-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/tests/test_licence.py
  - puppeteer/compose.cold-start.yaml
autonomous: true
requirements: []

must_haves:
  truths:
    - "pytest puppeteer/agent_service/tests/test_licence.py passes with zero failures"
    - "compose.cold-start.yaml contains no API_KEY line in the agent environment block"
    - "vault_service.cpython-312.pyc does not exist on disk"
  artifacts:
    - path: "puppeteer/agent_service/tests/test_licence.py"
      provides: "Fixed ASGI endpoint tests asserting current response shape"
      contains: "licence_state"
    - path: "puppeteer/compose.cold-start.yaml"
      provides: "Clean compose file with no dead API_KEY env var"
  key_links:
    - from: "test_licence_endpoint_community"
      to: "app.state.licence_state"
      via: "deletes licence_state attr, not licence"
      pattern: "licence_state"
    - from: "test_licence_endpoint_enterprise"
      to: "LicenceState dataclass"
      via: "imports and constructs real LicenceState instance"
      pattern: "LicenceState\\("
---

<objective>
Fix three tech debt items identified by the v14.3 milestone audit:
1. Update two stale endpoint tests in test_licence.py to assert the current response shape and reference the correct app.state key
2. Remove the dead API_KEY env var line from compose.cold-start.yaml
3. Delete the orphaned vault_service bytecode file

Purpose: CI will pass against the current API shape; compose files contain no misleading env vars; no orphaned bytecode lingers from deleted source files.
Output: Green test_licence.py tests, clean compose.cold-start.yaml, no vault_service .pyc on disk.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-v14.3-tech-debt-cleanup/76-RESEARCH.md
@.planning/phases/76-v14.3-tech-debt-cleanup/76-VALIDATION.md

<interfaces>
<!-- Current /api/licence endpoint — puppeteer/agent_service/main.py lines 773–794 -->
```python
@app.get("/api/licence", tags=["System"])
async def get_licence_status(request: Request, current_user: User = Depends(require_auth)):
    ls: Optional[LicenceState] = getattr(request.app.state, "licence_state", None)
    if ls is None:
        return {
            "status": "ce",
            "days_until_expiry": 0,
            "node_limit": 0,
            "tier": "ce",
            "customer_id": None,
            "grace_days": 0,
        }
    return {
        "status": ls.status.value if hasattr(ls.status, "value") else str(ls.status),
        "days_until_expiry": ls.days_until_expiry,
        "node_limit": ls.node_limit,
        "tier": ls.tier,
        "customer_id": ls.customer_id,
        "grace_days": ls.grace_days,
    }
```

<!-- LicenceState dataclass — puppeteer/agent_service/services/licence_service.py -->
```python
@dataclass
class LicenceState:
    status: LicenceStatus   # Enum: "valid", "grace", "expired", "ce"
    tier: str               # "ce" or "enterprise"
    customer_id: Optional[str]
    node_limit: int
    grace_days: int
    days_until_expiry: int
    features: List[str]
    is_ee_active: bool
```

<!-- compose.cold-start.yaml line 70 — the dead API_KEY line to remove -->
<!-- Keep line 77: AXIOM_LICENCE_KEY — that one is valid and consumed -->
```yaml
- API_KEY=${API_KEY:-master-secret-key}   # DELETE THIS LINE
- AXIOM_LICENCE_KEY=${AXIOM_LICENCE_KEY:-}  # KEEP THIS
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix stale endpoint tests in test_licence.py</name>
  <files>puppeteer/agent_service/tests/test_licence.py</files>
  <behavior>
    - test_licence_endpoint_community: clears app.state.licence_state (not app.state.licence), asserts {"status": "ce", "tier": "ce", "days_until_expiry": 0, "node_limit": 0, "customer_id": None, "grace_days": 0}
    - test_licence_endpoint_enterprise: sets app.state.licence_state = LicenceState(...) using the real imported dataclass (not a raw dict), asserts {"status": "valid", "tier": "enterprise", "customer_id": "test-co", "node_limit": 10, "grace_days": 30, "days_until_expiry": 365}
    - teardown in enterprise test cleans up app.state.licence_state (not app.state.licence)
  </behavior>
  <action>
Edit puppeteer/agent_service/tests/test_licence.py. Update ONLY the two functions test_licence_endpoint_community and test_licence_endpoint_enterprise. Leave all other tests untouched.

For test_licence_endpoint_community:
- Change `if hasattr(app.state, "licence"): del app.state.licence` to `if hasattr(app.state, "licence_state"): del app.state.licence_state`
- Change assertion from `assert data == {"edition": "community"}` to assert all six fields: `data["status"] == "ce"`, `data["tier"] == "ce"`, `data["days_until_expiry"] == 0`, `data["node_limit"] == 0`, `data["customer_id"] is None`, `data["grace_days"] == 0`
- Update docstring to reflect new assertion

For test_licence_endpoint_enterprise:
- Add import at top of function (or at file-level imports): `from agent_service.services.licence_service import LicenceState, LicenceStatus`
- Remove the `exp_time = int(time.time()) + 3600` line (no longer needed)
- Replace `app.state.licence = {"customer_id": "test-co", "exp": exp_time, "features": [...]}` with `app.state.licence_state = LicenceState(status=LicenceStatus.VALID, tier="enterprise", customer_id="test-co", node_limit=10, grace_days=30, days_until_expiry=365, features=["foundry", "audit"], is_ee_active=True)`
- Change assertions from `data["edition"] == "enterprise"`, `data["features"]`, `data["expires"]` to: `data["status"] == "valid"`, `data["tier"] == "enterprise"`, `data["customer_id"] == "test-co"`, `data["node_limit"] == 10`, `data["grace_days"] == 30`, `data["days_until_expiry"] == 365`
- Update teardown to clean up `app.state.licence_state` (not `app.state.licence`)
- Update docstring to reflect new assertion

Note: `time` import at top of file can be removed if it is only used by these two tests — check before removing.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest agent_service/tests/test_licence.py::test_licence_endpoint_community agent_service/tests/test_licence.py::test_licence_endpoint_enterprise -v</automated>
  </verify>
  <done>Both test_licence_endpoint_community and test_licence_endpoint_enterprise pass. No KeyError or AttributeError in output.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead API_KEY from compose.cold-start.yaml and delete vault .pyc</name>
  <files>puppeteer/compose.cold-start.yaml</files>
  <action>
Two changes in this task:

1. Edit puppeteer/compose.cold-start.yaml: Remove the line `- API_KEY=${API_KEY:-master-secret-key}` from the agent service environment block (currently line 70). Do NOT remove the AXIOM_LICENCE_KEY line (line 77) — that is valid and consumed by the EE licence system.

2. Delete the orphaned bytecode file at `/home/thomas/Development/master_of_puppets/puppeteer/agent_service/services/__pycache__/vault_service.cpython-312.pyc` using `rm -f`. This file is not git-tracked (covered by .gitignore __pycache__ exclusion) so no `git rm` is needed — a filesystem delete is sufficient. Verify deletion with: `test ! -f .../vault_service.cpython-312.pyc && echo PASS`.
  </action>
  <verify>
    <automated>grep 'API_KEY' /home/thomas/Development/master_of_puppets/puppeteer/compose.cold-start.yaml; test ! -f /home/thomas/Development/master_of_puppets/puppeteer/agent_service/services/__pycache__/vault_service.cpython-312.pyc && echo "pyc GONE" || echo "pyc EXISTS"</automated>
  </verify>
  <done>grep API_KEY in compose.cold-start.yaml returns no output (or only lines containing AXIOM_LICENCE_KEY, which is unrelated). Shell check confirms vault_service .pyc does not exist on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite green gate</name>
  <files></files>
  <action>
Run the full pytest suite from the puppeteer directory to confirm the two fixed tests pass and no regressions were introduced by the edits.

Command: `cd /home/thomas/Development/master_of_puppets/puppeteer && pytest`

If any tests fail that are NOT in test_licence.py, investigate whether they are pre-existing failures (check git blame / recent commits) or regressions introduced in this plan. Only failures in test_licence_endpoint_community or test_licence_endpoint_enterprise are this plan's responsibility.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest --tb=short -q</automated>
  </verify>
  <done>Full pytest suite exits with 0 failures. test_licence_endpoint_community and test_licence_endpoint_enterprise both show PASSED in output.</done>
</task>

</tasks>

<verification>
All three tech debt items from the v14.3 audit are closed:

1. `cd puppeteer && pytest agent_service/tests/test_licence.py -v` — both endpoint tests PASS
2. `grep API_KEY puppeteer/compose.cold-start.yaml` — returns no output (line removed)
3. `test ! -f puppeteer/agent_service/services/__pycache__/vault_service.cpython-312.pyc && echo PASS` — returns PASS
4. `cd puppeteer && pytest --tb=short -q` — full suite green, no regressions
</verification>

<success_criteria>
- pytest agent_service/tests/test_licence.py exits with 0 failures
- test_licence_endpoint_community asserts the 6-field CE response shape
- test_licence_endpoint_enterprise uses LicenceState dataclass and asserts the 6-field EE response shape
- compose.cold-start.yaml has no API_KEY line; AXIOM_LICENCE_KEY line is preserved
- vault_service.cpython-312.pyc does not exist on disk
- Full pytest suite exits green with no new failures
</success_criteria>

<output>
After completion, create `.planning/phases/76-v14.3-tech-debt-cleanup/76-01-SUMMARY.md` following the summary template.
</output>