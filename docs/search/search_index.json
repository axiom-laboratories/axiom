{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Axiom","text":"<p>Axiom is a self-hosted orchestration platform for managing distributed agent nodes \u2014 deploy jobs, schedule tasks, and monitor your fleet from a central control plane.</p>"},{"location":"#what-it-does","title":"What it does","text":"<ul> <li>Node management \u2014 Axiom Nodes self-enroll via mTLS and poll for work. No inbound firewall rules needed.</li> <li>Job dispatch \u2014 Submit signed Python scripts to nodes matched by capability tags and resource limits.</li> <li>Scheduled jobs \u2014 Cron-based job definitions with APScheduler, scoped to node capability sets.</li> <li>Foundry \u2014 Build custom node images from runtime and network blueprints via the dashboard.</li> <li>RBAC \u2014 Three roles (<code>admin</code>, <code>operator</code>, <code>viewer</code>) with per-permission grants, service principals, and API keys.</li> <li>Audit log \u2014 All security-relevant actions logged and queryable from the dashboard.</li> </ul>"},{"location":"#architecture-overview","title":"Architecture overview","text":"<pre><code>Axiom Orchestrator (Control Plane)  \u2190\u2500\u2500 mTLS \u2500\u2500\u2192  Axiom Nodes\n  \u251c\u2500\u2500 Agent Service (8001)                           \u2514\u2500\u2500 Polls /work/pull every N sec\n  \u251c\u2500\u2500 Model Service (8000)                               Executes jobs, reports results\n  \u251c\u2500\u2500 PostgreSQL\n  \u2514\u2500\u2500 React Dashboard\n</code></pre> <p>Nodes initiate all connections to the control plane \u2014 the pull model means no inbound ports need to be open on your nodes.</p>"},{"location":"#getting-started","title":"Getting started","text":"Goal Where to go Check your environment before installing Prerequisites Install and run the stack Install Connect your first node Enroll a Node Dispatch your first job First Job Build custom node images Foundry Guide Push jobs from the CLI axiom-push CLI Explore the REST API API Reference"},{"location":"licensing/","title":"Licensing","text":"<p>Axiom is distributed under an open-core model.</p>"},{"location":"licensing/#editions","title":"Editions","text":"Edition Distribution EE Features Community Edition (CE) Open source \u2014 MIT licence No Enterprise Edition (EE) Commercial \u2014 private wheel Yes <p>CE and EE share the same agent service binary. EE features are enabled at runtime by a valid <code>AXIOM_LICENCE_KEY</code> environment variable.</p>"},{"location":"licensing/#setting-your-licence-key","title":"Setting your licence key","text":"<p>Add the licence key to your environment before starting the agent service:</p> <pre><code>AXIOM_LICENCE_KEY=&lt;your-key&gt;\n</code></pre> <p>With Docker Compose, add it to your <code>secrets.env</code> or pass it via the <code>environment:</code> block:</p> <pre><code>environment:\n  AXIOM_LICENCE_KEY: \"${AXIOM_LICENCE_KEY}\"\n</code></pre>"},{"location":"licensing/#validation","title":"Validation","text":"<p>Licence validation is offline \u2014 no network call is made. The key is an Ed25519-signed payload containing your <code>customer_id</code>, an expiry timestamp (<code>exp</code>), and the list of enabled <code>features</code>. The public key used to verify the signature is compiled into the EE binary.</p> <p>Validation runs once at startup. A restart is required to pick up a new or renewed key.</p>"},{"location":"licensing/#expiry-behaviour","title":"Expiry behaviour","text":"Scenario Result Valid key, not expired EE features enabled Valid key, expired CE mode \u2014 <code>GET /api/features</code> returns all false Invalid or tampered key CE mode \u2014 warning logged at startup No key set CE mode \u2014 info logged at startup <p>The server always starts regardless of licence state \u2014 EE features are simply absent when the key is missing or invalid.</p>"},{"location":"licensing/#key-rotation","title":"Key rotation","text":"<p>Licence key rotation requires a new EE wheel release containing the updated public key, plus a rolling deployment. Contact Axiom Labs for key renewal.</p>"},{"location":"licensing/#checking-your-licence","title":"Checking your licence","text":"<p>The <code>GET /api/licence</code> endpoint (authenticated) returns your current licence state:</p> <pre><code>{\n  \"edition\": \"enterprise\",\n  \"customer_id\": \"acme-corp\",\n  \"expires\": \"2027-03-20T00:00:00+00:00\",\n  \"features\": [\"foundry\", \"rbac\", \"webhooks\", \"triggers\", \"audit\"]\n}\n</code></pre> <p>In CE mode it returns <code>{\"edition\": \"community\"}</code>.</p> <p>The dashboard sidebar also shows a CE or EE badge for at-a-glance visibility.</p>"},{"location":"licensing/#checking-active-feature-flags","title":"Checking active feature flags","text":"<p>To see which EE features are currently active, call <code>GET /api/features</code> (no authentication required):</p> <pre><code>{\n  \"audit\": true,\n  \"foundry\": true,\n  \"webhooks\": true,\n  \"triggers\": true,\n  \"rbac\": true,\n  \"resource_limits\": true,\n  \"service_principals\": true,\n  \"api_keys\": true,\n  \"executions\": true\n}\n</code></pre> <p>In CE mode (or with an expired or missing key), all values return <code>false</code>.</p>"},{"location":"api-reference/","title":"API Reference","text":"<p>Full REST API for Axiom. All endpoints require JWT authentication except node-facing endpoints (enroll, work/pull, heartbeat) which use mTLS.</p> <p></p>"},{"location":"assets/screenshots/","title":"Index","text":"<p>Screenshots captured by <code>tools/capture_screenshots.py</code>. Run against a live stack to populate.</p>"},{"location":"developer/architecture/","title":"Architecture","text":"<p>Axiom is a distributed job orchestration platform built around a central control plane (the Axiom Orchestrator) and any number of stateless worker agents (the Axiom Nodes). This guide is a full technical deep-dive: it covers every service, the database schema, the security model, and the data flows that make jobs run reliably across a fleet of nodes \u2014 without weakening the trust model.</p> <p>The platform is designed for environments where nodes cannot accept inbound connections. A pull model means every node initiates all communication outward to the orchestrator; no inbound firewall rules are ever required on a node.</p>"},{"location":"developer/architecture/#system-overview","title":"System Overview","text":"<pre><code>graph TB\n    subgraph Puppeteer [\"Axiom Orchestrator (Control Plane)\"]\n        A[agent_service :8001]\n        M[model_service :8000]\n        DB[(PostgreSQL / SQLite)]\n        DASH[React Dashboard :5173]\n        DOCS[MkDocs Site :80/docs/]\n        A &lt;--&gt; DB\n        M &lt;--&gt; DB\n        DASH --&gt; A\n    end\n    subgraph Puppet [\"Axiom Node (Worker)\"]\n        N[environment_service/node.py]\n        R[runtime.py]\n        N --&gt; R\n    end\n    N --&gt;|\"mTLS client cert \u00b7 POLL /work/pull\"| A\n    N --&gt;|\"POST /heartbeat\"| A\n    A --&gt;|\"WorkResponse (signed script + limits)\"| N\n    N --&gt;|\"POST /work/{guid}/result\"| A\n    OPER([Operator]) --&gt; DASH\n    OPER --&gt; N</code></pre> <p>Three components run in every deployment:</p> Component Location Responsibility Axiom Orchestrator <code>puppeteer/</code> All state, API, scheduling, PKI, security enforcement Axiom Node <code>puppets/environment_service/</code> Stateless job executor; polls for work, runs scripts, reports results React Dashboard <code>puppeteer/dashboard/</code> Web UI for operators and admins"},{"location":"developer/architecture/#service-inventory","title":"Service Inventory","text":"<p>The Axiom Orchestrator is composed of two FastAPI applications and a set of service modules that handle distinct concerns.</p>"},{"location":"developer/architecture/#top-level-applications","title":"Top-Level Applications","text":"Service Port File Role <code>agent_service</code> 8001 <code>puppeteer/agent_service/main.py</code> Primary control plane \u2014 all API routes, WebSocket, PKI, job dispatch <code>model_service</code> 8000 <code>puppeteer/model_service/main.py</code> APScheduler-backed cron scheduling; fires job definitions on a cron schedule <p><code>agent_service</code> handles every interaction from the dashboard, nodes, and external integrations. <code>model_service</code> runs as a separate process and is responsible only for triggering scheduled jobs \u2014 it calls <code>agent_service</code> to dispatch them.</p>"},{"location":"developer/architecture/#service-modules-puppeteeragent_serviceservices","title":"Service Modules (<code>puppeteer/agent_service/services/</code>)","text":""},{"location":"developer/architecture/#job_service","title":"job_service","text":"<p>The core of the job dispatch pipeline. Responsibilities:</p> <ul> <li>Node selection: given a pending job, find the best available node by matching <code>target_tags</code> (JSON tag set), <code>capability_requirements</code>, and <code>memory_limit</code> against each node's <code>capabilities</code> and <code>job_memory_limit</code>. Nodes that cannot satisfy the requirements are skipped.</li> <li>Job assignment: sets <code>Job.node_id</code>, updates status to <code>ASSIGNED</code>, and the node picks it up on the next poll.</li> <li>Heartbeat processing: updates <code>Node.last_seen</code>, inserts a <code>NodeStats</code> row for the CPU/RAM snapshot, prunes old stats rows (keeps last 60 per node).</li> <li>Execution records: writes an <code>ExecutionRecord</code> for every job run (stdout, stderr, exit code, timing).</li> </ul>"},{"location":"developer/architecture/#foundry_service","title":"foundry_service","text":"<p>Builds custom Docker images for Axiom Nodes. The build pipeline:</p> <ol> <li>Reads a <code>PuppetTemplate</code> (which references a RUNTIME blueprint and a NETWORK blueprint).</li> <li>Copies <code>puppets/environment_service/</code> into a temporary directory.</li> <li>Generates a <code>Dockerfile</code> from the <code>capability_matrix</code> injection recipes for each capability declared in the RUNTIME blueprint.</li> <li>Runs <code>docker build</code> via the mounted Docker socket.</li> <li>Optionally pushes the image to the local registry (<code>registry:5000</code>).</li> <li>Updates <code>PuppetTemplate.last_built_at</code> and <code>PuppetTemplate.current_image_uri</code> on success.</li> </ol> <p>A semaphore limits concurrent builds to 2. <code>smelter_service</code> validates all blueprints before the build starts.</p>"},{"location":"developer/architecture/#scheduler_service","title":"scheduler_service","text":"<p>Wraps APScheduler (<code>AsyncIOScheduler</code>) to manage cron job definitions:</p> <ul> <li>CRUD for <code>ScheduledJob</code> records.</li> <li>Adds/removes/reschedules APScheduler jobs when definitions change.</li> <li>On cron trigger, verifies the Ed25519 signature is still valid and dispatches a new <code>Job</code> via <code>job_service</code>.</li> <li>Stats pruning: periodically removes old <code>NodeStats</code> rows.</li> </ul>"},{"location":"developer/architecture/#pki_service","title":"pki_service","text":"<p>Thin lifecycle helpers wrapping <code>pki.py</code>'s <code>CertificateAuthority</code>:</p> <ul> <li><code>get_or_create_ca()</code> \u2014 initialises or loads the Root CA (RSA 4096, stored in DB <code>config</code> table).</li> <li><code>sign_csr()</code> \u2014 signs a node's certificate signing request.</li> <li><code>revoke_node()</code> \u2014 adds the node's cert serial to <code>RevokedCert</code> and regenerates the CRL.</li> <li><code>get_crl_pem()</code> \u2014 returns the current signed CRL.</li> </ul>"},{"location":"developer/architecture/#signature_service","title":"signature_service","text":"<p>Manages Ed25519 public keys used to sign job scripts:</p> <ul> <li>Stores public keys (PEM format) in the <code>signatures</code> table.</li> <li><code>verify_signature(public_key_id, script_bytes, signature_bytes)</code> \u2014 returns <code>True</code>/<code>False</code>.</li> <li>Nodes independently verify signatures before executing any script.</li> </ul>"},{"location":"developer/architecture/#smelter_service","title":"smelter_service","text":"<p>The security gatekeeper for Foundry builds:</p> <ul> <li>Maintains the <code>approved_ingredients</code> table \u2014 an allowlist of packages, versions, and SHA-256 hashes.</li> <li>Validates every package in a blueprint's <code>definition</code> against the allowlist before a build proceeds.</li> <li>Two enforcement modes: STRICT (build fails on any violation) and WARNING (violations logged, build continues).</li> <li>Integrates with <code>mirror_service</code> to confirm packages are available from the local PyPI/APT mirror.</li> </ul>"},{"location":"developer/architecture/#vault_service","title":"vault_service","text":"<p>Binary artifact storage for Foundry capability injection files:</p> <ul> <li>Stores arbitrary binary blobs in the <code>artifacts</code> table with a SHA-256 content hash.</li> <li>Used by <code>capability_matrix</code> to reference pre-built tool binaries injected into node images during build.</li> </ul>"},{"location":"developer/architecture/#alert_service","title":"alert_service","text":"<p>Generates internal alerts for security and operational events:</p> <ul> <li>Job failure after maximum retries.</li> <li>Node offline (heartbeat timeout exceeded).</li> <li>Script tamper detection (signature mismatch at node execution time).</li> <li>Writes to the <code>alerts</code> table; acknowledged by operators via the dashboard.</li> </ul>"},{"location":"developer/architecture/#webhook_service","title":"webhook_service","text":"<p>Outbound HMAC-signed webhook delivery:</p> <ul> <li>Reads <code>webhooks</code> table entries (URL, HMAC secret, event filter list).</li> <li>Signs every outbound payload with HMAC-SHA256 using the configured secret.</li> <li>Delivers asynchronously; retries on failure.</li> </ul>"},{"location":"developer/architecture/#trigger_service","title":"trigger_service","text":"<p>Inbound webhook-triggered job dispatch:</p> <ul> <li>Each <code>Trigger</code> record has a unique <code>slug</code> and a <code>secret_token</code>.</li> <li><code>POST /api/trigger/{slug}</code> validates the token and dispatches the associated job definition.</li> <li>Enables CI/CD pipelines and external systems to fire scheduled jobs on demand.</li> </ul>"},{"location":"developer/architecture/#mirror_service","title":"mirror_service","text":"<p>Local package mirror management for air-gap builds:</p> <ul> <li>Manages the <code>pypi</code> (pypiserver) and <code>mirror</code> (APT proxy) services.</li> <li>Tracks which packages are mirrored and whether they are current.</li> <li>Smelter checks mirror availability before approving a build.</li> </ul>"},{"location":"developer/architecture/#staging_service","title":"staging_service","text":"<p>A review gate for job definitions before they are activated:</p> <ul> <li>Job definitions can be submitted to a staging queue (<code>DRAFT</code> state) rather than dispatched directly.</li> <li>An approver promotes a staged definition to active via a separate approval endpoint.</li> <li>Provides an audit trail of who approved what and when.</li> </ul>"},{"location":"developer/architecture/#database-schema","title":"Database Schema","text":"<p>The schema is managed by <code>SQLAlchemy</code>'s <code>Base.metadata.create_all</code> at startup. There is no Alembic \u2014 see the Contributing guide for the migration pattern.</p> <pre><code>erDiagram\n    jobs {\n        string guid PK\n        string task_type\n        string payload\n        string status\n        string node_id FK\n        string target_tags\n        string capability_requirements\n        string memory_limit\n        string cpu_limit\n        int max_retries\n        string depends_on\n    }\n    nodes {\n        string node_id PK\n        string hostname\n        string ip\n        datetime last_seen\n        string status\n        string capabilities\n        int concurrency_limit\n        string job_memory_limit\n        text client_cert_pem\n        string template_id FK\n    }\n    node_stats {\n        int id PK\n        string node_id FK\n        datetime recorded_at\n        float cpu\n        float ram\n    }\n    scheduled_jobs {\n        int id PK\n        string name\n        text script_content\n        int signature_id FK\n        string schedule_cron\n        string target_tags\n        string capability_requirements\n        bool is_active\n    }\n    signatures {\n        int id PK\n        string name\n        text public_key\n        string uploaded_by\n    }\n    users {\n        string username PK\n        string password_hash\n        string role\n        bool must_change_password\n        int token_version\n    }\n    role_permissions {\n        int id PK\n        string role\n        string permission\n    }\n    puppet_templates {\n        int id PK\n        string friendly_name\n        int runtime_blueprint_id FK\n        int network_blueprint_id FK\n        string canonical_id\n        string current_image_uri\n        datetime last_built_at\n        bool is_compliant\n    }\n    blueprints {\n        int id PK\n        string type\n        string name\n        text definition\n        string os_family\n    }\n    execution_records {\n        int id PK\n        string job_guid FK\n        string node_id FK\n        string status\n        int exit_code\n        datetime started_at\n        datetime completed_at\n        text output_log\n        bool truncated\n    }\n    audit_log {\n        int id PK\n        datetime timestamp\n        string username\n        string action\n        string resource_id\n        text detail\n    }\n\n    jobs ||--o{ execution_records : \"has\"\n    nodes ||--o{ node_stats : \"records\"\n    nodes ||--o{ jobs : \"executes\"\n    puppet_templates ||--|| blueprints : \"runtime_blueprint\"\n    puppet_templates ||--|| blueprints : \"network_blueprint\"\n    scheduled_jobs }o--|| signatures : \"signed_by\"\n    users ||--o{ audit_log : \"generates\"</code></pre>"},{"location":"developer/architecture/#key-table-notes","title":"Key Table Notes","text":"Table Notes <code>jobs</code> <code>status</code> values: <code>PENDING</code>, <code>ASSIGNED</code>, <code>RUNNING</code>, <code>COMPLETED</code>, <code>FAILED</code>, <code>RETRYING</code>. <code>depends_on</code> is a JSON array of guids. <code>nodes</code> <code>status</code> values: <code>ONLINE</code>, <code>OFFLINE</code>, <code>REVOKED</code>. <code>capabilities</code> is a JSON object (e.g., <code>{\"python\": true, \"docker\": true}</code>). <code>node_stats</code> Pruned to last 60 rows per node by <code>scheduler_service</code>. Used for sparkline charts in the dashboard. <code>role_permissions</code> Has a <code>UniqueConstraint(role, permission)</code>. Admin role bypasses this table entirely \u2014 it has an unconditional bypass in <code>require_permission()</code>. <code>blueprints</code> <code>type</code> is <code>RUNTIME</code> (defines packages/tools) or <code>NETWORK</code> (defines network configuration). <code>definition</code> is a JSON object; package lists use <code>{\"python\": [...]}</code> dict format. <code>audit_log</code> All security-relevant events are written here: logins, permission changes, job dispatches, node enrollments, revocations. <code>revoked_certs</code> Backing store for the CRL. Checked at every <code>/work/pull</code> and <code>/api/enroll</code> request. <code>config</code> Key-value store for system state: Root CA PEM, base image staleness markers, system settings."},{"location":"developer/architecture/#security-model","title":"Security Model","text":"<p>Axiom has five layered security mechanisms. Each addresses a different threat vector.</p>"},{"location":"developer/architecture/#mtls-mutual-tls-node-authentication","title":"mTLS \u2014 Mutual TLS Node Authentication","text":"<p>Every node-to-orchestrator connection is mutually authenticated with TLS client certificates. The orchestrator operates an internal Root CA (<code>pki.py</code>). Nodes never connect over plain HTTP.</p> <p>Enrollment flow:</p> <pre><code>sequenceDiagram\n    participant N as Axiom Node\n    participant P as Axiom Orchestrator (agent_service)\n    participant CA as Internal Root CA\n\n    N-&gt;&gt;N: Decode JOIN_TOKEN (base64 JSON \u2192 root CA PEM)\n    N-&gt;&gt;N: Write root CA to disk (trust anchor)\n    N-&gt;&gt;N: Generate RSA keypair + CSR\n    N-&gt;&gt;P: POST /api/enroll {csr_pem, hostname, machine_id, node_secret}\n    P-&gt;&gt;CA: sign_csr(csr_pem, hostname)\n    CA--&gt;&gt;P: client_cert.pem\n    P-&gt;&gt;P: Store client_cert_pem in Node DB row\n    P--&gt;&gt;N: {client_cert, node_id}\n    N-&gt;&gt;N: Write cert + key to secrets/node-{id}.crt/.key\n    Note over N,P: All subsequent requests present the client cert (mTLS)</code></pre> <p>Cert lifecycle:</p> <ul> <li><code>JOIN_TOKEN</code> is a base64-encoded JSON object containing the Root CA PEM. It is the only credential a node needs to bootstrap.</li> <li>After enrollment, the node stores its cert+key in <code>secrets/node-{id}.crt</code> and <code>secrets/node-{id}.key</code>. On restart, it scans for these files and reuses the existing node ID \u2014 preventing re-enrollment churn.</li> <li>Revoked certs are tracked in the <code>revoked_certs</code> table. Revoked nodes receive 403 on <code>/work/pull</code> and <code>/api/enroll</code>.</li> <li>The current CRL is served at <code>GET /system/crl.pem</code> (no auth required \u2014 needed by TLS clients).</li> </ul>"},{"location":"developer/architecture/#ed25519-script-signing-chain","title":"Ed25519 Script Signing Chain","text":"<p>All job scripts must be signed with an Ed25519 private key whose corresponding public key is registered in the <code>signatures</code> table. This prevents an attacker who compromises the orchestrator database from pushing arbitrary scripts to nodes.</p> <p>The signing chain:</p> <pre><code>flowchart LR\n    OP[Operator] --&gt;|generates keypair| KEY[Ed25519 keypair]\n    KEY --&gt;|uploads public key| SIG[signatures table]\n    KEY --&gt;|signs script locally| SIGN[signature bytes]\n    OP --&gt;|POST /jobs: script + signature| A[agent_service]\n    A --&gt;|verify signature| A\n    A --&gt;|store Job + signature_payload| DB[(DB)]\n    DB --&gt;|WorkResponse: script + signature| N[Axiom Node]\n    N --&gt;|verify signature locally| N\n    N --&gt;|refuse if invalid| X[Execution blocked]\n    N --&gt;|execute if valid| R[runtime.py]</code></pre> <ul> <li>Operators generate Ed25519 keypairs using <code>admin_signer.py</code> (or any standard tool).</li> <li>The public key is registered via the dashboard or <code>POST /signatures</code>.</li> <li>The operator signs each script offline before dispatching. The signature is stored in <code>Job.signature_payload</code>.</li> <li>Nodes re-verify the signature at execution time using the registered public key. If verification fails, the script is not executed and a tamper alert is generated.</li> </ul>"},{"location":"developer/architecture/#jwt-token-versioning","title":"JWT + Token Versioning","text":"<p>The dashboard and API use JWT bearer tokens for human user authentication.</p> <ul> <li>Tokens are signed with <code>SECRET_KEY</code> (HS256).</li> <li>Every token embeds a <code>tv</code> (token version) field matching <code>User.token_version</code>.</li> <li>On any password change, <code>token_version</code> is incremented. All previously issued tokens become invalid immediately \u2014 no revocation list or session store required.</li> <li>The <code>must_change_password</code> flag forces a password reset dialog on next login before any other UI is accessible.</li> <li>Token expiry is 24 hours by default.</li> </ul> <p>Production requirement</p> <p>Always set <code>SECRET_KEY</code> to a cryptographically random value in production. The default is a weak development placeholder. Use <code>openssl rand -hex 32</code>.</p>"},{"location":"developer/architecture/#rbac-role-based-access-control","title":"RBAC \u2014 Role-Based Access Control","text":"<p>Three roles exist: <code>admin</code>, <code>operator</code>, <code>viewer</code>. Permissions are stored in the <code>role_permissions</code> table and seeded at startup.</p> Role Typical Permissions Notes <code>admin</code> All Bypasses all permission checks \u2014 checked inline in <code>require_permission()</code> <code>operator</code> <code>jobs:write</code>, <code>nodes:write</code>, <code>foundry:write</code>, <code>signatures:write</code> Can dispatch jobs, manage nodes, manage Foundry <code>viewer</code> <code>jobs:read</code>, <code>nodes:read</code>, <code>audit:read</code> Read-only dashboard access <p>Route protection pattern (from <code>main.py</code>):</p> <pre><code>async def some_route(current_user = Depends(require_permission(\"jobs:write\"))):\n    ...\n</code></pre> <p><code>require_permission()</code> is a factory that returns a FastAPI dependency. Admin users skip the DB lookup. All other users are checked against <code>role_permissions</code>.</p> <p>Node-facing endpoints (<code>/api/enroll</code>, <code>/work/pull</code>, <code>/heartbeat</code>, <code>/work/{guid}/result</code>) are unauthenticated \u2014 they rely on mTLS client certificates only.</p>"},{"location":"developer/architecture/#fernet-encryption-at-rest","title":"Fernet Encryption at Rest","text":"<p>Sensitive job payloads and secrets stored in the database are encrypted using Fernet (AES-128-CBC with HMAC-SHA256).</p> <ul> <li>The <code>ENCRYPTION_KEY</code> environment variable must hold a valid Fernet key.</li> <li>Encryption and decryption are handled in <code>security.py</code> via <code>fernet_encrypt()</code> and <code>fernet_decrypt()</code>.</li> <li>If <code>ENCRYPTION_KEY</code> is not set, the service will auto-generate one at startup \u2014 but this key is lost on restart, making all previously encrypted data unreadable. Always set <code>ENCRYPTION_KEY</code> explicitly in production.</li> </ul> <p>Generating a Fernet key</p> <pre><code>python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\"\n</code></pre>"},{"location":"developer/architecture/#job-execution-data-flow","title":"Job Execution Data Flow","text":"<p>The sequence from operator action to completed job result:</p> <pre><code>sequenceDiagram\n    participant OP as Operator\n    participant D as Dashboard\n    participant A as agent_service\n    participant N as Axiom Node\n    participant R as runtime.py\n\n    OP-&gt;&gt;D: Dispatch job (script content + signature)\n    D-&gt;&gt;A: POST /jobs {payload, signature_payload, target_tags, capability_requirements}\n    A-&gt;&gt;A: Verify Ed25519 signature against registered public key\n    A-&gt;&gt;A: Insert Job (status=PENDING) into DB\n    loop Poll every N seconds\n        N-&gt;&gt;A: POST /work/pull (mTLS client cert)\n        A-&gt;&gt;A: Find PENDING job matching node tags + capabilities + memory\n        A--&gt;&gt;N: WorkResponse {script, signature, memory_limit, cpu_limit}\n        A-&gt;&gt;A: Update Job (status=ASSIGNED)\n    end\n    N-&gt;&gt;N: Verify Ed25519 signature (tamper check)\n    N-&gt;&gt;R: run(script, memory_limit, cpu_limit)\n    R-&gt;&gt;R: Execute in subprocess or container (based on EXECUTION_MODE)\n    R--&gt;&gt;N: {stdout, stderr, exit_code}\n    N-&gt;&gt;A: POST /work/{guid}/result {status, output, exit_code}\n    A-&gt;&gt;A: Update Job (status=COMPLETED or FAILED)\n    A-&gt;&gt;A: Write ExecutionRecord\n    A-&gt;&gt;D: WebSocket push (job status update)\n    D-&gt;&gt;OP: Live status update</code></pre> <p>Key decision points in the flow:</p> <ul> <li>If the node's Ed25519 verification fails, the script is not executed and a tamper alert is generated. The job is marked <code>FAILED</code>.</li> <li>If <code>max_retries</code> is set and the job fails, it re-enters <code>PENDING</code> and is reassigned on the next poll cycle.</li> <li><code>depends_on</code> (JSON array of job guids) \u2014 jobs are not dispatched until all dependencies are <code>COMPLETED</code>.</li> <li>Memory admission: if a node's <code>job_memory_limit</code> is lower than the job's <code>memory_limit</code>, the node is skipped during selection.</li> </ul>"},{"location":"developer/architecture/#foundry-smelter","title":"Foundry &amp; Smelter","text":"<p>Foundry builds custom Docker images for Axiom Nodes. Smelter is the security gatekeeper that validates every ingredient before a build proceeds.</p>"},{"location":"developer/architecture/#build-pipeline","title":"Build Pipeline","text":"<pre><code>flowchart TD\n    BPRINT_R[RUNTIME Blueprint\\ndefinition: packages + tools] --&gt; TPL[PuppetTemplate\\nruntime_bp + network_bp]\n    BPRINT_N[NETWORK Blueprint\\ndefinition: network config] --&gt; TPL\n    TPL --&gt; FOUNDRY[foundry_service\\nbuild_template]\n    FOUNDRY --&gt; SMELTER{smelter_service\\nvalidate ingredients}\n    SMELTER --&gt;|STRICT violation| FAIL[Build blocked]\n    SMELTER --&gt;|PASS or WARNING| BUILD[docker build\\ntemp dir context]\n    BUILD --&gt; PUSH[Push to registry:5000]\n    PUSH --&gt; ACTIVE[Image ACTIVE\\nTemplate last_built_at updated]\n    ACTIVE --&gt; NODE[Axiom Node\\npulls image from registry]</code></pre>"},{"location":"developer/architecture/#blueprint-model","title":"Blueprint Model","text":"<p>Blueprints are reusable build ingredients. Two types:</p> Type Purpose Definition Format <code>RUNTIME</code> Declares packages, tools, and capabilities installed in the image <code>{\"python\": [\"requests\", \"boto3\"], \"system\": [\"curl\"]}</code> <code>NETWORK</code> Declares network configuration (proxy settings, DNS, etc.) JSON network config object <p>Each blueprint has an <code>os_family</code> field (<code>DEBIAN</code> or <code>ALPINE</code>) that selects the appropriate injection recipe from <code>capability_matrix</code>.</p>"},{"location":"developer/architecture/#template-image-build","title":"Template \u2192 Image Build","text":"<p>A <code>PuppetTemplate</code> combines one RUNTIME blueprint and one NETWORK blueprint. When built, the orchestrator:</p> <ol> <li><code>foundry_service</code> copies <code>puppets/environment_service/</code> to a temporary directory.</li> <li>For each capability in the RUNTIME blueprint, the matching <code>capability_matrix</code> row provides a Dockerfile snippet (<code>injection_recipe</code>) that installs the capability.</li> <li>A complete <code>Dockerfile</code> is assembled and <code>docker build</code> is invoked.</li> <li>The resulting image is tagged and pushed to the local registry at <code>localhost:5000</code>.</li> </ol>"},{"location":"developer/architecture/#smelter-enforcement","title":"Smelter Enforcement","text":"<p>The <code>approved_ingredients</code> table is the security allowlist for Foundry builds. Before any build:</p> <ul> <li>Every package in every blueprint is checked against <code>approved_ingredients</code> (name, version constraint, SHA-256).</li> <li>In STRICT mode: any package not in the allowlist causes an immediate build failure.</li> <li>In WARNING mode: violations are logged and the build proceeds.</li> <li>The enforcement mode is set in the system <code>config</code> table.</li> </ul>"},{"location":"developer/architecture/#image-lifecycle","title":"Image Lifecycle","text":"State Meaning <code>ACTIVE</code> Currently built; nodes can use this image <code>DEPRECATED</code> Superseded by a newer build; nodes should upgrade <code>REVOKED</code> Security issue; nodes must not use this image <p>The dashboard shows a staleness warning when the base OS image has been updated but a template has not been rebuilt since the update.</p>"},{"location":"developer/architecture/#pull-model-architecture","title":"Pull Model Architecture","text":"<p>All node-to-orchestrator communication is initiated by the node. This is a deliberate architectural decision.</p> <p>Why pull, not push?</p> <p>In a push model, the orchestrator would need to open a connection to each node to deliver work. This requires:</p> <ul> <li>The orchestrator to know the node's current IP address.</li> <li>Inbound firewall rules on every node to accept connections from the orchestrator.</li> <li>Network infrastructure that routes the orchestrator's traffic to every node (VLANs, VPNs, etc.).</li> </ul> <p>In the pull model, nodes only need outbound HTTPS connectivity to the orchestrator's address. This works:</p> <ul> <li>Behind NAT, without port forwarding.</li> <li>In cloud environments where nodes have no stable public IP.</li> <li>Across network boundaries where inbound rules are locked down.</li> <li>With ephemeral nodes (containers, spot instances) that come and go.</li> </ul> <p>How the pull loop works:</p> <pre><code>sequenceDiagram\n    participant N as Axiom Node\n    participant A as agent_service\n\n    loop Every POLL_INTERVAL seconds (default 5)\n        N-&gt;&gt;A: POST /work/pull (mTLS)\n        alt Job available and node matches\n            A--&gt;&gt;N: 200 WorkResponse {job details}\n            N-&gt;&gt;N: Execute job\n            N-&gt;&gt;A: POST /work/{guid}/result\n        else No matching job\n            A--&gt;&gt;N: 204 No Content\n        end\n    end\n    N-&gt;&gt;A: POST /heartbeat (CPU%, RAM%, node metadata)</code></pre> <p>Heartbeat cadence: Nodes send heartbeats independently of the work poll. If a node's <code>last_seen</code> timestamp exceeds the offline threshold (configurable), <code>job_service</code> marks the node <code>OFFLINE</code> and generates an alert. Any jobs assigned to that node are re-queued.</p> <p>WorkResponse payload:</p> <pre><code>class WorkResponse(BaseModel):\n    guid: str           # Job identifier\n    task_type: str      # Informational job type label\n    payload: str        # Script content (plaintext Python)\n    signature_payload: str  # Base64-encoded Ed25519 signature\n    memory_limit: str | None\n    cpu_limit: str | None\n</code></pre> <p>The node receives the script content and the signature in a single response. It must verify the signature before passing the script to <code>runtime.py</code>. There is no mechanism to execute a script without a valid registered signature.</p> <p>Node identity persistence</p> <p>Node IDs are stable across container restarts. On startup, <code>node.py</code> scans the <code>secrets/</code> directory for an existing <code>node-{id}.crt</code> file. If found, that node ID is reused \u2014 preventing re-enrollment on every restart. The <code>secrets/</code> directory should be a persistent Docker volume in any production deployment.</p>"},{"location":"developer/architecture/#environment-variables-reference","title":"Environment Variables Reference","text":"Variable Service Required? Purpose <code>SECRET_KEY</code> agent_service Recommended JWT signing key \u2014 use a strong random value <code>ENCRYPTION_KEY</code> agent_service Required Fernet key for secrets at rest \u2014 must be stable <code>ADMIN_PASSWORD</code> agent_service Recommended Initial admin password (random if unset, logged) <code>API_KEY</code> agent_service Required Shared API key \u2014 <code>sys.exit(1)</code> at import if missing <code>DATABASE_URL</code> agent_service, model_service Optional Defaults to SQLite (<code>jobs.db</code>). Use <code>postgresql+asyncpg://...</code> for production <code>AGENT_URL</code> model_service, nodes Required for nodes URL nodes use to reach agent_service (e.g., <code>https://192.168.1.10:8001</code>) <code>JOIN_TOKEN</code> nodes Required Base64 JSON containing Root CA PEM; generated by the dashboard <code>EXECUTION_MODE</code> nodes Optional <code>auto</code> (default), <code>direct</code>, <code>docker</code>, or <code>podman</code>. Use <code>direct</code> inside Docker-in-Docker <code>JOB_MEMORY_LIMIT</code> nodes Optional Default memory limit for all jobs on this node (e.g., <code>512m</code>) <code>JOB_CPU_LIMIT</code> nodes Optional Default CPU limit for all jobs on this node (e.g., <code>1.0</code>) <p>API_KEY is mandatory</p> <p><code>security.py</code> calls <code>sys.exit(1)</code> at module import time if <code>API_KEY</code> is not set. This will prevent <code>agent_service</code> from starting at all. Always set it \u2014 even for local development (<code>export API_KEY=dev-key</code> is sufficient).</p>"},{"location":"developer/contributing/","title":"Contributing to Axiom","text":"<p>This guide covers how to contribute to Axiom \u2014 from running tests locally to the PR bar and database migration conventions. Read it before opening a pull request.</p>"},{"location":"developer/contributing/#overview","title":"Overview","text":"<p>The project is split into three components:</p> <ul> <li>Axiom Orchestrator (<code>puppeteer/</code>) \u2014 FastAPI control plane + React dashboard</li> <li>Axiom Nodes (<code>puppets/</code>) \u2014 stateless worker agents that poll for jobs</li> <li>Docs (<code>docs/</code>) \u2014 MkDocs Material static site</li> </ul> <p>Most contributions touch <code>puppeteer/</code>. Changes to <code>puppets/environment_service/</code> affect running nodes \u2014 test them carefully.</p>"},{"location":"developer/contributing/#code-style","title":"Code Style","text":""},{"location":"developer/contributing/#python-black-formatter","title":"Python \u2014 Black (formatter)","text":"<p>All Python code must be formatted with Black:</p> <pre><code>pip install black ruff\nblack puppeteer/\n</code></pre> <p>Configuration lives in <code>puppeteer/pyproject.toml</code>:</p> <ul> <li>Line length: 88 characters</li> <li>Target Python version: 3.12</li> </ul> <p>To check without modifying files:</p> <pre><code>black --check puppeteer/\n</code></pre>"},{"location":"developer/contributing/#python-ruff-linter","title":"Python \u2014 Ruff (linter)","text":"<p>Ruff replaces flake8, isort, and pyupgrade in a single fast tool:</p> <pre><code>ruff check puppeteer/\n</code></pre> <p>Active rule sets (from <code>puppeteer/pyproject.toml</code>):</p> Rule set Source <code>E</code> pycodestyle errors <code>F</code> pyflakes <code>I</code> isort (import ordering) <code>W</code> pycodestyle warnings <p><code>E501</code> (line-too-long) is explicitly ignored \u2014 Black handles line length.</p> <p>To auto-fix safe issues:</p> <pre><code>ruff check --fix puppeteer/\n</code></pre>"},{"location":"developer/contributing/#frontend-eslint","title":"Frontend \u2014 ESLint","text":"<p>Frontend linting uses ESLint with the existing project config:</p> <pre><code>cd puppeteer/dashboard\nnpm run lint\n</code></pre> <p>This must pass with no errors before a PR can merge. The config is already set up \u2014 do not modify the ESLint configuration as part of a feature PR.</p>"},{"location":"developer/contributing/#style-only-reformatting","title":"Style-Only Reformatting","text":"<p>Keep style changes separate</p> <p>Do NOT run <code>black .</code> or <code>ruff --fix .</code> on existing code in the same PR as your feature changes. Style-only reformatting should be a separate commit or PR to keep diffs reviewable.</p>"},{"location":"developer/contributing/#running-tests","title":"Running Tests","text":"<p>Both test suites must be green before a PR can merge. Run them with the exact commands and working directories shown below.</p>"},{"location":"developer/contributing/#backend-pytest","title":"Backend (pytest)","text":"<pre><code>cd puppeteer\npytest\n</code></pre> <p>For faster iteration during development:</p> <p>Quick run (fail-fast)</p> <p><pre><code>pytest tests/ -x -q\n</code></pre> <code>-x</code> stops on first failure. <code>-q</code> reduces output noise. Use this during active development; run the full <code>pytest</code> suite before submitting.</p>"},{"location":"developer/contributing/#frontend-vitest","title":"Frontend (vitest)","text":"<pre><code>cd puppeteer/dashboard\nnpm run test\n</code></pre> <p>For CI-style non-interactive run:</p> <pre><code>cd puppeteer/dashboard\nnpm run test -- --run\n</code></pre>"},{"location":"developer/contributing/#coverage","title":"Coverage","text":"<p>There is no numeric coverage threshold. Green = pass. The bar is: all existing tests continue to pass and new code has tests for its core behaviour. Do not submit untested business logic.</p>"},{"location":"developer/contributing/#pr-requirements","title":"PR Requirements","text":"<p>Before opening a pull request, verify each of the following:</p> <ul> <li>[ ] <code>cd puppeteer &amp;&amp; pytest</code> passes with no failures</li> <li>[ ] <code>cd puppeteer/dashboard &amp;&amp; npm run test</code> passes with no failures</li> <li>[ ] <code>cd puppeteer/dashboard &amp;&amp; npm run lint</code> passes with no errors</li> <li>[ ] New Python code follows Black formatting (<code>black --check puppeteer/</code>)</li> <li>[ ] New Python code passes Ruff lint (<code>ruff check puppeteer/</code>)</li> <li>[ ] New API routes include a <code>tags=</code> parameter (17 tag groups established \u2014 check existing routes in <code>main.py</code> for examples)</li> <li>[ ] New DB columns include a migration SQL file (see the Database Migrations section below)</li> </ul>"},{"location":"developer/contributing/#commit-style","title":"Commit style","text":"<p>Use conventional commits:</p> <pre><code>feat(component): short present-tense description\n\n- Detail 1\n- Detail 2\n</code></pre> <p>Types: <code>feat</code>, <code>fix</code>, <code>refactor</code>, <code>test</code>, <code>chore</code>, <code>docs</code>.</p>"},{"location":"developer/contributing/#database-migrations","title":"Database Migrations","text":"<p>Major contributor gotcha \u2014 read this before adding DB columns</p> <p>This project does not use Alembic. <code>Base.metadata.create_all</code> runs at startup and creates new tables automatically \u2014 but it will never alter existing tables. If you add a column to a model without a migration file, fresh deployments will have the column; existing deployments will silently miss it.</p>"},{"location":"developer/contributing/#how-it-works","title":"How it works","text":"<ul> <li>Fresh deployments: fully covered by <code>create_all</code> \u2014 no migration steps needed.</li> <li>Existing deployments: if you add a column to an existing DB model, you must also create a <code>puppeteer/migration_vN.sql</code> file with the necessary <code>ALTER TABLE</code> statement.</li> </ul>"},{"location":"developer/contributing/#migration-file-format","title":"Migration file format","text":"<pre><code>-- migration_v32.sql (increment from current highest: migration_v31.sql)\n-- NOTE: IF NOT EXISTS syntax is PostgreSQL-only. For SQLite use bare ALTER TABLE.\nALTER TABLE my_table ADD COLUMN IF NOT EXISTS new_column TEXT;\nALTER TABLE my_table ADD COLUMN IF NOT EXISTS another_column INTEGER DEFAULT 0;\n</code></pre>"},{"location":"developer/contributing/#rules","title":"Rules","text":"Rule Why Use <code>IF NOT EXISTS</code> Makes the migration idempotent \u2014 safe to apply twice SQLite caveat SQLite's <code>ALTER TABLE ... ADD COLUMN</code> does not support <code>IF NOT EXISTS</code> \u2014 add a comment in the file noting it's for Postgres Never drop columns Dropping columns is destructive \u2014 coordinate separately, outside a migration file Sequential naming <code>migration_v32.sql</code>, <code>migration_v33.sql</code> \u2014 increment from the current highest file"},{"location":"developer/contributing/#finding-the-current-migration-number","title":"Finding the current migration number","text":"<pre><code>ls puppeteer/migration_v*.sql | sort -V | tail -1\n</code></pre> <p>The next migration should be one higher than whatever this returns.</p>"},{"location":"developer/contributing/#code-structure","title":"Code Structure","text":"<p>See <code>CLAUDE.md</code> at the repo root for the full layout table. Key patterns to follow:</p>"},{"location":"developer/contributing/#backend","title":"Backend","text":"<ul> <li>All FastAPI route handlers live in <code>puppeteer/agent_service/main.py</code> \u2014 do not split routes across multiple files. The single-file convention keeps routing searchable and consistent.</li> <li>Business logic goes in <code>puppeteer/agent_service/services/</code> \u2014 one service module per domain (<code>job_service.py</code>, <code>foundry_service.py</code>, <code>scheduler_service.py</code>, etc.)</li> <li>Pydantic models for request/response in <code>puppeteer/agent_service/models.py</code></li> <li>SQLAlchemy ORM models in <code>puppeteer/agent_service/db.py</code></li> <li>Auth (<code>auth.py</code>) and security (<code>security.py</code>) are separate from business logic</li> </ul>"},{"location":"developer/contributing/#adding-a-new-api-route","title":"Adding a new API route","text":"<ol> <li>Define the Pydantic request/response models in <code>models.py</code></li> <li>Add the SQLAlchemy model to <code>db.py</code> if new storage is needed</li> <li>Write the business logic in the appropriate <code>services/</code> file</li> <li>Add the route handler in <code>main.py</code> with:</li> <li>The correct <code>tags=</code> parameter (check existing routes for the 17 established tag groups)</li> <li>A <code>Depends(require_permission(\"...\"))</code> guard</li> <li>Write a migration file if new DB columns are needed</li> </ol>"},{"location":"developer/contributing/#frontend","title":"Frontend","text":"<ul> <li>All authenticated API calls go through <code>authenticatedFetch()</code> in <code>dashboard/src/auth.ts</code> \u2014 never call <code>fetch()</code> directly for authenticated endpoints</li> <li>Views are self-contained pages in <code>dashboard/src/views/</code></li> <li>Shared components go in <code>dashboard/src/components/</code></li> <li>Custom hooks go in <code>dashboard/src/hooks/</code></li> </ul>"},{"location":"developer/contributing/#websocket-and-real-time-updates","title":"WebSocket and Real-Time Updates","text":"<p>The WebSocket endpoint is at <code>/ws?token=&lt;jwt&gt;</code> and pushes job/node status updates to the dashboard. The token is required \u2014 unauthenticated WebSocket connections are rejected.</p> <p>When adding new real-time events:</p> <ol> <li>Emit the event via the WebSocket manager in <code>main.py</code> (after the relevant DB write)</li> <li>Handle the message type in the client-side hook: <code>dashboard/src/hooks/useWebSocket.ts</code></li> </ol> <p>The hook auto-reconnects with exponential backoff \u2014 do not add custom reconnect logic in view components.</p>"},{"location":"developer/contributing/#environment-variables","title":"Environment Variables","text":"<p>When adding new configuration:</p> <ol> <li>Document the variable in <code>CLAUDE.md</code> under the Configuration table</li> <li>Set a safe default for local dev (SQLite-compatible, no external services)</li> <li>Note whether the variable is required in production</li> </ol> <p>Key variables used in local dev (set in <code>puppeteer/.env</code>):</p> Variable Dev default Purpose <code>DATABASE_URL</code> (unset, uses SQLite) Postgres connection string in production <code>SECRET_KEY</code> weak dev default JWT signing \u2014 always override in production <code>ENCRYPTION_KEY</code> (none) Fernet key for secrets at rest <code>API_KEY</code> (none) Legacy API key auth <code>ADMIN_PASSWORD</code> random Initial admin password <p>aiosqlite is not in requirements.txt</p> <p>The async SQLite driver (<code>aiosqlite</code>) is not listed in <code>requirements.txt</code> because production uses PostgreSQL. If you see <code>ModuleNotFoundError: aiosqlite</code> when running locally, install it manually: <code>pip install aiosqlite</code>. Do not add it to <code>requirements.txt</code>.</p>"},{"location":"developer/contributing/#getting-help","title":"Getting Help","text":"<ul> <li>Architecture overview: developer/architecture.md \u2014 system components, data flow, mTLS, job signing</li> <li>API reference: API Reference \u2014 full OpenAPI endpoint schemas, generated from the live server</li> <li>CLAUDE.md: at the repo root \u2014 concise reference for all commands, file paths, and architecture decisions</li> </ul>"},{"location":"developer/setup-deployment/","title":"Setup &amp; Deployment","text":"<p>This guide covers everything you need to get Axiom running \u2014 whether you want a full production stack via Docker Compose or a local development environment to hack on the code.</p>"},{"location":"developer/setup-deployment/#quick-start","title":"Quick Start","text":"<p>Get running in 60 seconds</p> <p>The fastest path to a working stack:</p> <pre><code>git clone &lt;repo-url&gt; master-of-puppets\ncd master-of-puppets/puppeteer\ncp .env.example .env   # fill in SECRET_KEY, ENCRYPTION_KEY, ADMIN_PASSWORD, API_KEY\ndocker compose -f compose.server.yaml up -d\n# Dashboard available at https://localhost (Caddy self-signed) or your CF tunnel domain\n</code></pre> <p>The stack takes approximately 15 seconds to become healthy after containers start. See the Environment Variables section for how to generate the values needed before <code>docker compose up</code>.</p>"},{"location":"developer/setup-deployment/#prerequisites","title":"Prerequisites","text":""},{"location":"developer/setup-deployment/#all-deployments","title":"All deployments","text":"<ul> <li>Docker 24+ and Docker Compose v2 (<code>docker compose</code> \u2014 not the legacy <code>docker-compose</code>)</li> </ul>"},{"location":"developer/setup-deployment/#local-development-only","title":"Local development only","text":"<ul> <li>Python 3.12 (the backend uses <code>asyncio</code> features not available in earlier versions)</li> <li>Node.js 18+ and npm (for the React dashboard)</li> </ul>"},{"location":"developer/setup-deployment/#production-deployment-docker-compose","title":"Production Deployment (Docker Compose)","text":"<p>The recommended way to run Axiom is via the provided <code>compose.server.yaml</code>. This brings up the entire stack as a set of coordinated containers.</p>"},{"location":"developer/setup-deployment/#services","title":"Services","text":"<p>The stack runs ten services:</p> Service Image Port Purpose <code>db</code> <code>postgres:15-alpine</code> 5432 (internal) PostgreSQL \u2014 primary datastore <code>cert-manager</code> Custom Caddy 80, 8443 Reverse proxy, TLS termination, static asset serving <code>agent</code> Custom Python 8001 FastAPI agent service \u2014 the control plane API <code>model</code> Custom Python 8000 (internal) Model service \u2014 job model and scoring <code>dashboard</code> Via Caddy \u2014 React dashboard (served by cert-manager) <code>docs</code> Custom nginx \u2014 MkDocs static site (served by cert-manager at <code>/docs/</code>) <code>registry</code> <code>registry:2</code> 5000 Local Docker registry for Foundry-built images <code>pypi</code> Custom 8080 (internal) Local PyPI mirror for node builds <code>mirror</code> Custom 8081 (internal) Package mirror for offline node builds <code>tunnel</code> <code>cloudflare/cloudflared</code> \u2014 Cloudflare tunnel (outbound only, if enabled) <p>docs container uses nginx, not mkdocs serve</p> <p>The <code>docs</code> container is a pre-built static site served by nginx. It does not run <code>mkdocs serve</code>. Running <code>mkdocs serve</code> in production is unsafe (see MkDocs GitHub issue #1825) and is only used during local documentation development.</p>"},{"location":"developer/setup-deployment/#bring-up-the-stack","title":"Bring up the stack","text":"<p>Run from the repository root (or adjust the path accordingly):</p> <pre><code>docker compose -f puppeteer/compose.server.yaml up -d\n</code></pre> <p>Or, if you are already inside <code>puppeteer/</code>:</p> <pre><code>docker compose -f compose.server.yaml up -d\n</code></pre>"},{"location":"developer/setup-deployment/#check-health","title":"Check health","text":"<pre><code>docker compose -f puppeteer/compose.server.yaml ps\n</code></pre> <p>All services should show <code>healthy</code> or <code>running</code> within ~30 seconds. If <code>agent</code> is slow to start, it is usually waiting for the <code>db</code> health check to pass \u2014 this is normal.</p>"},{"location":"developer/setup-deployment/#view-logs","title":"View logs","text":"<pre><code># Follow logs for the agent service\ndocker compose -f puppeteer/compose.server.yaml logs -f agent\n\n# Follow all services\ndocker compose -f puppeteer/compose.server.yaml logs -f\n</code></pre>"},{"location":"developer/setup-deployment/#rebuild-after-code-changes","title":"Rebuild after code changes","text":"<p>The <code>agent</code> service is the most frequently changed. To rebuild and hot-swap it without restarting the entire stack:</p> <pre><code>docker compose -f puppeteer/compose.server.yaml build agent\ndocker compose -f puppeteer/compose.server.yaml up -d --no-build agent\n</code></pre> <p>To rebuild all services:</p> <pre><code>docker compose -f puppeteer/compose.server.yaml build\ndocker compose -f puppeteer/compose.server.yaml up -d\n</code></pre> <p>Do not use <code>docker restart</code> for code changes</p> <p><code>docker restart</code> only restarts the container \u2014 it does not pick up new image layers. Always use <code>docker compose build</code> followed by <code>docker compose up -d</code> to apply code changes.</p>"},{"location":"developer/setup-deployment/#tear-down","title":"Tear down","text":"<pre><code># Stop containers, keep volumes (preserves database data)\ndocker compose -f puppeteer/compose.server.yaml down\n\n# Stop containers and remove volumes (destroys all data \u2014 destructive!)\ndocker compose -f puppeteer/compose.server.yaml down -v\n</code></pre>"},{"location":"developer/setup-deployment/#environment-variables","title":"Environment Variables","text":"<p>The orchestrator reads environment variables from <code>puppeteer/.env</code> (and optionally <code>puppeteer/secrets.env</code> for sensitive values like <code>ADMIN_PASSWORD</code>).</p> <p>Copy the example file before your first run:</p> <pre><code>cp puppeteer/.env.example puppeteer/.env\n</code></pre> <p>Then edit <code>puppeteer/.env</code> and fill in the required values:</p> Variable Required Purpose Example / How to Generate <code>API_KEY</code> Required Shared API key for legacy API key auth. <code>security.py</code> calls <code>sys.exit(1)</code> at import time if this is missing \u2014 the service will not start without it. <code>openssl rand -hex 32</code> <code>ENCRYPTION_KEY</code> Required (unstable if absent) Fernet key for job secrets encrypted at rest. If you change this after deployment, all stored secrets become unreadable. <code>python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\"</code> <code>SECRET_KEY</code> Recommended JWT signing key. Defaults to a weak dev value \u2014 always override in production. <code>openssl rand -hex 32</code> <code>ADMIN_PASSWORD</code> Recommended Initial admin account password. Only used on first startup if the admin user does not yet exist. The database password is the source of truth after that. Any secure password <code>DATABASE_URL</code> Optional Database connection string. Defaults to SQLite (<code>jobs.db</code>) if absent. <code>postgresql+asyncpg://user:pass@host/db</code> <code>AGENT_URL</code> Required for nodes The URL nodes use to reach the agent service from the network. Must be reachable from every node. <code>https://192.168.1.10:8001</code> <code>CLOUDFLARE_TUNNEL_TOKEN</code> CF tunnel only Cloudflare Tunnel credentials. Only needed if you are routing the dashboard through a Cloudflare Tunnel. From CF Dashboard \u2192 Tunnels <code>DUCKDNS_TOKEN</code> / <code>DUCKDNS_DOMAIN</code> DuckDNS only Dynamic DNS for Caddy cert-manager TLS. Only needed if using DuckDNS for certificate issuance. From DuckDNS account page <p>Never commit real keys to git</p> <p>The <code>.env</code> file is gitignored for good reason. Even though the docs site sits behind Cloudflare Access protection, never commit real API keys, encryption keys, or passwords to the repository. Use <code>.env</code> files, <code>secrets.env</code>, or a proper secrets manager.</p>"},{"location":"developer/setup-deployment/#local-development","title":"Local Development","text":"<p>Most production deployments run via Docker Compose (see above). This section is for contributors who want to run the backend and/or frontend directly on their machine for rapid iteration.</p>"},{"location":"developer/setup-deployment/#backend-fastapi","title":"Backend (FastAPI)","text":""},{"location":"developer/setup-deployment/#install-dependencies","title":"Install dependencies","text":"<pre><code>cd puppeteer\npip install -r requirements.txt\npip install aiosqlite  # required for SQLite local dev \u2014 NOT included in requirements.txt\n</code></pre> <p>aiosqlite is required for SQLite local dev</p> <p>The <code>requirements.txt</code> targets the Docker image, which runs against PostgreSQL. If you run locally without a <code>DATABASE_URL</code> pointing to Postgres, SQLAlchemy defaults to SQLite and requires <code>aiosqlite</code>. Install it separately as shown above.</p>"},{"location":"developer/setup-deployment/#set-required-environment-variables","title":"Set required environment variables","text":"<p><code>security.py</code> calls <code>sys.exit(1)</code> at import time if <code>API_KEY</code> is not set. This means the FastAPI process will not start \u2014 not even to print a helpful error \u2014 unless the env var is present before launch. Set these before running:</p> <pre><code>export API_KEY=dev-key\nexport ENCRYPTION_KEY=$(python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\")\nexport SECRET_KEY=dev-secret-key\n</code></pre> <p>You can also place these in a <code>.env</code> file in <code>puppeteer/</code> and load them with <code>source .env</code> or a tool like <code>direnv</code>.</p>"},{"location":"developer/setup-deployment/#start-the-backend","title":"Start the backend","text":"<pre><code>cd puppeteer\npython -m agent_service.main\n</code></pre> <p>The backend starts on https://localhost:8001 using a self-signed TLS certificate. The first startup will create <code>jobs.db</code> (SQLite) in the <code>puppeteer/</code> directory and seed the default roles and admin user.</p>"},{"location":"developer/setup-deployment/#frontend-react-vite","title":"Frontend (React / Vite)","text":""},{"location":"developer/setup-deployment/#install-and-run","title":"Install and run","text":"<pre><code>cd puppeteer/dashboard\nnpm install\nnpm run dev\n</code></pre> <p>The dev server runs at http://localhost:5173. API calls are proxied to <code>https://localhost:8001</code> per the settings in <code>.env.development</code> \u2014 the backend must be running for any API calls to succeed.</p> <p>The dev server handles hot module replacement (HMR) automatically. Changes to <code>.tsx</code> / <code>.ts</code> / <code>.css</code> files appear immediately without a manual refresh.</p>"},{"location":"developer/setup-deployment/#production-build","title":"Production build","text":"<p>To test the production bundle locally:</p> <pre><code>cd puppeteer/dashboard\nnpm run build\nnpm run preview\n</code></pre>"},{"location":"developer/setup-deployment/#lint","title":"Lint","text":"<pre><code>cd puppeteer/dashboard\nnpm run lint\n</code></pre>"},{"location":"developer/setup-deployment/#running-tests","title":"Running Tests","text":""},{"location":"developer/setup-deployment/#backend-tests-pytest","title":"Backend tests (pytest)","text":"<pre><code>cd puppeteer\npytest\n</code></pre> <p>Run a single test file:</p> <pre><code>cd puppeteer\npytest tests/test_tools.py\n</code></pre>"},{"location":"developer/setup-deployment/#frontend-tests-vitest","title":"Frontend tests (vitest)","text":"<pre><code>cd puppeteer/dashboard\nnpm run test\n</code></pre> <p>Run a single test file:</p> <pre><code>cd puppeteer/dashboard\nnpx vitest run src/views/__tests__/JobDefinitions.test.tsx\n</code></pre>"},{"location":"developer/setup-deployment/#tls-bootstrap-node-enrollment","title":"TLS Bootstrap &amp; Node Enrollment","text":"<p>After deploying the orchestrator, nodes need to know where to connect and which CA to trust. This is handled via two environment variables on each node container.</p>"},{"location":"developer/setup-deployment/#how-it-works","title":"How it works","text":"<ol> <li> <p>Get the JOIN_TOKEN \u2014 after your first login, navigate to the Admin panel in the dashboard. The <code>JOIN_TOKEN</code> is displayed there. It is a base64-encoded JSON blob containing the Root CA PEM that nodes use to bootstrap trust.</p> </li> <li> <p>Set env vars on the node \u2014 when deploying a puppet node container, provide:</p> <pre><code>JOIN_TOKEN=&lt;token-from-admin-panel&gt;\nAGENT_URL=https://192.168.1.10:8001   # puppeteer's address reachable from the node\n</code></pre> </li> <li> <p>Node auto-enrolls \u2014 on first startup, the node generates a key pair, sends a CSR to <code>AGENT_URL/api/enroll</code>, and receives a signed client certificate. The node appears in the Nodes view of the dashboard once enrollment succeeds.</p> </li> <li> <p>Subsequent restarts \u2014 the node stores its certificate in the <code>secrets/</code> volume mount and reuses it on restart. The node ID is derived from the certificate filename to prevent identity drift on container restart.</p> </li> </ol> <p>Deep cert rotation and mTLS details</p> <p>This section covers the happy-path enrollment flow. For certificate revocation, CRL management, mTLS verification, and the full PKI lifecycle, see the Security Guide (coming in a later documentation phase).</p>"},{"location":"developer/setup-deployment/#upgrading","title":"Upgrading","text":""},{"location":"developer/setup-deployment/#pull-and-rebuild","title":"Pull and rebuild","text":"<pre><code>git pull\ndocker compose -f puppeteer/compose.server.yaml build\ndocker compose -f puppeteer/compose.server.yaml up -d\n</code></pre> <p>This rebuilds all images from the updated source and replaces running containers. Volumes (including the database) are preserved.</p>"},{"location":"developer/setup-deployment/#database-migrations","title":"Database migrations","text":"<p>New deployments are handled automatically \u2014 <code>Base.metadata.create_all</code> runs at startup and creates all tables.</p> <p>Existing deployments require manual migration for any new columns added since your last upgrade. Check the <code>puppeteer/</code> directory for migration files:</p> <pre><code>ls puppeteer/migration_v*.sql\n</code></pre> <p>Apply any migration files with a version higher than your current schema version:</p> <pre><code># Example: applying a migration to a running Postgres container\ndocker exec -i puppeteer-db-1 psql -U postgres -d puppeteer &lt; puppeteer/migration_v14.sql\n</code></pre> <p>See the Contributing guide for the migration pattern used in this project.</p>"},{"location":"feature-guides/axiom-push/","title":"axiom-push CLI","text":"<p><code>axiom-push</code> is the command-line tool for pushing signed jobs to Axiom from a developer or CI/CD machine. It handles OAuth authentication, Ed25519 signing, and job submission in a single workflow \u2014 so you can submit jobs without touching the dashboard.</p> <p>Prerequisites</p> <p>This guide assumes the stack is running and at least one node is enrolled. If you haven't done that yet, start with Getting Started.</p> <p>Enterprise</p>"},{"location":"feature-guides/axiom-push/#install","title":"Install","text":"<p>Install <code>axiom-push</code> from the repository root:</p> <pre><code>pip install -e .\n</code></pre> <p>Verify the installation:</p> <pre><code>axiom-push --help\n</code></pre> <p>pipx for isolation</p> <p>If you don't want to install into your global Python environment, use <code>pipx</code> instead:</p> <pre><code>pipx install -e .\n</code></pre> <p>This keeps <code>axiom-push</code> isolated in its own virtualenv while still making the command available on your PATH.</p>"},{"location":"feature-guides/axiom-push/#login","title":"Login","text":"<p><code>axiom-push</code> uses the OAuth 2.0 Device Authorization Grant (RFC 8628) \u2014 no browser on the machine running the CLI is required. This makes it safe to use in headless environments and CI/CD pipelines.</p> <p>Set the server URL using the environment variable:</p> <pre><code>export AXIOM_URL=https://your-host\n</code></pre> <p>Then run the login command:</p> <pre><code>axiom-push login\n</code></pre> <p>You will see output like this:</p> <pre><code>Initiating login to https://your-host...\n\n========================================\nUSER CODE: ABCD-1234\n========================================\n\nPlease approve this request in your browser:\nhttps://your-host/auth/device?user_code=ABCD-1234\n\nAttempting to open browser automatically...\n\nWaiting for approval...\n\nSuccessfully authenticated and saved credentials.\n</code></pre> <p>What happens step by step:</p> <ol> <li>The CLI requests a device code from the server</li> <li>Your user code and a browser URL are displayed</li> <li>Open the URL in any browser and log in with your dashboard credentials</li> <li>Approve the request \u2014 the CLI is polling in the background</li> <li>Once approved, the CLI receives and stores your access token</li> </ol> <p>Credential store</p> <p>Credentials are saved to <code>~/.axiom/credentials.json</code> with permissions <code>0600</code>. If you are running as root or inside a container, confirm the home directory is writable and that the path resolves to where you expect. You can verify with:</p> <pre><code>cat ~/.axiom/credentials.json\n</code></pre> <p>Session lifetime: Credentials expire after the JWT lifetime configured on the server (default: 15 minutes for access tokens). Run <code>axiom-push login</code> again to re-authenticate when your session expires.</p>"},{"location":"feature-guides/axiom-push/#ed25519-key-setup","title":"Ed25519 Key Setup","text":"<p>All jobs submitted via <code>axiom-push</code> must be signed with an Ed25519 private key. The corresponding public key must be registered in the dashboard before you can push.</p>"},{"location":"feature-guides/axiom-push/#generate-a-keypair","title":"Generate a keypair","text":"<p>Option 1 \u2014 Using openssl (any machine with openssl installed):</p> <pre><code>openssl genpkey -algorithm ed25519 -out signing.key\nopenssl pkey -in signing.key -pubout -out verification.key\n</code></pre> <p>Option 2 \u2014 Using admin_signer.py (if the toms_home tooling is available):</p> <pre><code>python ~/Development/toms_home/.agents/tools/admin_signer.py --generate\n# Creates: secrets/signing.key and secrets/verification.key\n</code></pre> <p>Protect your private key</p> <p>Never commit <code>signing.key</code> to a git repository. Add it to <code>.gitignore</code> immediately:</p> <pre><code>echo \"signing.key\" &gt;&gt; .gitignore\n</code></pre> <p>In production, store private keys in a secrets manager (HashiCorp Vault, AWS Secrets Manager, or similar) and inject them at runtime via environment variable or mounted volume. A leaked signing key allows anyone to submit jobs that nodes will execute.</p>"},{"location":"feature-guides/axiom-push/#register-the-public-key-in-the-dashboard","title":"Register the public key in the dashboard","text":"<ol> <li>Go to Signatures in the dashboard sidebar</li> <li>Click Add Signature Key</li> <li>Paste the full contents of <code>verification.key</code> (including the <code>-----BEGIN PUBLIC KEY-----</code> header and footer)</li> <li>Give it a descriptive name \u2014 for example, <code>ci-deploy-key</code> or <code>my-laptop-key</code></li> <li>Click Save</li> </ol> <p>Note the Key ID shown in the list. You will pass this as <code>--key-id</code> when pushing jobs.</p>"},{"location":"feature-guides/axiom-push/#push-a-job","title":"Push a Job","text":"<p>Write a simple test script to try your first push:</p> <pre><code># hello.py\nprint(\"Hello from axiom-push!\")\nimport platform\nprint(f\"Node: {platform.node()}, OS: {platform.system()}\")\n</code></pre> <p>Push it to the server:</p> <pre><code>axiom-push job push \\\n  --script hello.py \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n</code></pre> <p>The CLI will sign the script with your private key, upload it along with the signature, and print the created job ID and its initial status.</p> <p>DRAFT jobs are not dispatched</p> <p>A newly pushed job has status DRAFT. Nodes will not receive DRAFT jobs. You must publish the job via the dashboard (or the API) before it is dispatched to any node. See Publish from Staging below.</p> <p>You can also pass <code>--url</code> directly if you haven't set <code>AXIOM_URL</code>:</p> <pre><code>axiom-push --url https://your-host job push \\\n  --script hello.py \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n</code></pre>"},{"location":"feature-guides/axiom-push/#publish-from-staging","title":"Publish from Staging","text":"<p>After pushing, complete the promotion flow in the dashboard:</p> <ol> <li>Go to Jobs in the sidebar and select the Staging tab</li> <li>Your job appears with status DRAFT and a preview of the script content</li> <li>Review the script in the staging view \u2014 this is your gate to confirm the right script and the correct signature are present before dispatching to nodes</li> <li>Click Publish to promote the job from DRAFT to ACTIVE</li> </ol> <p>Once published:</p> <ul> <li>Active jobs are dispatched to nodes matching the job's target tags, or to any available node if no tags are set</li> <li>Monitor progress in the Jobs list \u2014 the job moves through PENDING \u2192 ASSIGNED \u2192 COMPLETED</li> <li>Click the job row to view the captured output from the node</li> </ul> <p>At this point you have an active job running on a node. The complete flow is: push \u2192 DRAFT \u2192 Staging review \u2192 Publish \u2192 ACTIVE \u2192 node execution \u2192 output captured.</p>"},{"location":"feature-guides/axiom-push/#updating-a-job","title":"Updating a Job","text":"<p>If you push a new version of a script and want to replace an existing job, pass the existing job ID with <code>--id</code>:</p> <pre><code>axiom-push job push \\\n  --id &lt;existing-job-id&gt; \\\n  --script updated_script.py \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n</code></pre> <p>This creates a new DRAFT version associated with the existing job ID. Go to Staging and Publish as before to make it active.</p>"},{"location":"feature-guides/axiom-push/#environment-variable-reference","title":"Environment Variable Reference","text":"Variable Purpose Example <code>AXIOM_URL</code> Base URL for the Axiom server <code>https://your-host</code>"},{"location":"feature-guides/foundry/","title":"Foundry","text":"<p>Foundry builds custom Docker images for Axiom Nodes. Instead of using a generic base image, you define exactly what packages, tools, and network configuration each node image should contain \u2014 then Foundry assembles and builds it.</p> <p>The workflow has two stages: first you create blueprints that define runtime or network configuration, then you compose those blueprints into a template that Foundry turns into a Docker image. Nodes enroll against a template, so every node launched from the same template has an identical, reproducible environment.</p> <p>Enterprise</p>"},{"location":"feature-guides/foundry/#concepts","title":"Concepts","text":"Concept Description Blueprint Reusable definition of a runtime environment (Python packages) or network configuration Template Combines one RUNTIME blueprint and one NETWORK blueprint into a Docker image build Smelter Built-in package scanner that validates blueprint ingredients against the Smelter registry Image Lifecycle Status tracking for built templates: ACTIVE, DEPRECATED, or REVOKED"},{"location":"feature-guides/foundry/#blueprints","title":"Blueprints","text":"<p>Blueprints come in two types:</p> <ul> <li>RUNTIME \u2014 defines the Python packages and execution environment that will be available inside the node container</li> <li>NETWORK \u2014 defines network configuration and connectivity settings for the node</li> </ul>"},{"location":"feature-guides/foundry/#creating-a-blueprint","title":"Creating a blueprint","text":"<p>Navigate to Foundry \u2192 Blueprints \u2192 New Blueprint. The Blueprint Wizard opens and walks you through five steps:</p> <p>Step 1 \u2014 Identity</p> <p>Choose a name, select the blueprint type (RUNTIME or NETWORK), and choose the OS family (DEBIAN, ALPINE, or FEDORA). If you want to base the new blueprint on an existing one, use the Clone Existing option \u2014 it pre-populates all fields from the selected blueprint and lets you modify from there.</p> <p>Step 2 \u2014 Base Image</p> <p>Select the base container image for the blueprint. This is the Docker image that Foundry will build on top of.</p> <p>Step 3 \u2014 Ingredients</p> <p>Add the Python packages to include. This is where Smelter runs its validation (see Smelter below).</p> <p>Package format</p> <p>Packages must be specified as a dict with a <code>\"python\"</code> key:</p> <pre><code>{\"python\": [\"requests\", \"numpy\", \"pandas\"]}\n</code></pre> <p>A plain list like <code>[\"requests\", \"numpy\"]</code> is rejected. This is the most common source of blueprint creation failures \u2014 if your blueprint is not saving, check the package format first.</p> <p>Step 4 \u2014 Tools</p> <p>Select the capability tools to inject into the node image. These are pre-built additions (such as file transfer utilities or monitoring agents) that extend what the node can do.</p> <p>Step 5 \u2014 Review</p> <p>The wizard shows a JSON preview of the full blueprint definition. Confirm the details and click Submit.</p>"},{"location":"feature-guides/foundry/#advanced-mode","title":"Advanced mode","text":"<p>The wizard has an Advanced (JSON) toggle that bypasses the guided form and lets you paste raw JSON directly. This is useful for importing a blueprint exported from another environment or for making bulk edits without stepping through each page.</p> <p>After submission, the blueprint appears in the Blueprints list showing its OS family, type, and package count.</p>"},{"location":"feature-guides/foundry/#the-foundry-interface","title":"The Foundry interface","text":"<p>The Foundry Templates tab lists all built images with their blueprint composition and build status:</p> <p></p>"},{"location":"feature-guides/foundry/#templates","title":"Templates","text":"<p>A template combines one RUNTIME blueprint and one NETWORK blueprint into a single Docker image build.</p>"},{"location":"feature-guides/foundry/#building-a-template","title":"Building a template","text":"<p>Navigate to Foundry \u2192 Templates \u2192 New Template.</p> <ol> <li>Give the template a name</li> <li>Select the RUNTIME blueprint to use</li> <li>Select the NETWORK blueprint to use</li> <li>Click Build</li> </ol> <p>Foundry copies the <code>environment_service</code> code into a temporary build context, generates a Dockerfile from the blueprint definitions, and runs <code>docker build</code>. Build progress is visible in the dashboard; if the build fails, the Docker build log is surfaced so you can diagnose the issue.</p> <p>On success, the template shows a Last Built timestamp and is ready for node enrollment.</p>"},{"location":"feature-guides/foundry/#rebuilding-after-a-base-image-update","title":"Rebuilding after a base image update","text":"<p>If the underlying base image changes (for example, after a security patch), navigate to Foundry \u2192 Templates, find the template row, and click Rebuild. Foundry will re-run the Docker build using the current blueprint definitions and the updated base image.</p>"},{"location":"feature-guides/foundry/#smelter","title":"Smelter","text":"<p>Smelter is the built-in package validation engine. It runs during blueprint creation at Step 3 (Ingredients) and checks each package against the Smelter registry for known CVEs.</p> <p>Smelter has two enforcement modes:</p> Mode Behaviour STRICT Build fails if any package fails the CVE scan \u2014 the blueprint cannot be saved until the offending package is removed or replaced WARNING Build proceeds normally; the template shows an amber warning badge indicating that one or more packages have flagged vulnerabilities <p>The enforcement mode is configured per-template at build time.</p> <p>Deep CVE configuration</p> <p>Configuring custom enforcement policies and adding packages to the Smelter allow-list is covered in the Security section.</p>"},{"location":"feature-guides/foundry/#image-lifecycle","title":"Image Lifecycle","text":"<p>Every built template has a lifecycle status. Use Foundry \u2192 Templates to view and change the status of any template.</p> Status Meaning How to change ACTIVE Normal operation \u2014 nodes can enroll and the image is in active use Default state after a successful build DEPRECATED New node enrollments with this template are blocked; nodes that already hold a cert from this template continue to function Click Deprecate in the template row REVOKED All enrollment with this template is blocked (403 returned at <code>/api/enroll</code>) Click Revoke in the template row <p>Revoke is irreversible</p> <p>Revoking a template cannot be undone from the dashboard. If you want to phase out a template without immediately blocking all nodes, use DEPRECATED first. DEPRECATED allows existing nodes to keep running while preventing new enrollments \u2014 giving you time to migrate before revoking.</p> <p>For the enforcement mechanics that explain how REVOKED blocks the <code>/api/enroll</code> endpoint, see the Architecture guide.</p>"},{"location":"feature-guides/foundry/#quick-reference","title":"Quick Reference","text":"Action Steps Create a blueprint Foundry \u2192 Blueprints \u2192 New Blueprint \u2192 complete the wizard Build a template Foundry \u2192 Templates \u2192 New Template \u2192 select blueprints \u2192 Build Rebuild after base image update Foundry \u2192 Templates \u2192 template row \u2192 Rebuild Change lifecycle status Foundry \u2192 Templates \u2192 template row \u2192 status dropdown"},{"location":"feature-guides/job-scheduling/","title":"Job Scheduling","text":"<p>Axiom runs scripts on a schedule using APScheduler cron triggers.</p>"},{"location":"feature-guides/job-scheduling/#prerequisites","title":"Prerequisites","text":"<p>Before creating a scheduled job, you need:</p> <ul> <li>A working node deployment. See the First Job guide for the full setup walkthrough.</li> <li>A signed script. Every scheduled job must have a valid Ed25519 signature before it can be scheduled. Register your public key in the Signatures section of the dashboard before proceeding.</li> </ul>"},{"location":"feature-guides/job-scheduling/#creating-a-job-definition","title":"Creating a Job Definition","text":"<p>Open the Job Definitions view from the sidebar. Click New Job Definition to open the creation form.</p> <p>Fill in the following fields:</p> Field Description Name A human-readable label for this job definition. Script Content The Python script body to run on the target node. Signature ID The UUID of the Ed25519 public key registered in Signatures. Signature Payload The base64-encoded signature of the script content produced by your signing key. Schedule (cron) A 5-field cron expression controlling when the job fires. See Cron Syntax below. Target Node ID Optional. Pin the job to a specific node UUID. Target Tags Optional. A list of node tags (e.g. <code>[\"gpu\", \"secure\"]</code>). Capability Requirements Optional. A dict of required capabilities (e.g. <code>{\"os_family\": \"DEBIAN\"}</code>). Max Retries Number of retry attempts on failure. Default: <code>0</code> (no retries). Backoff Multiplier Multiplier applied to the retry wait interval. Default: <code>2.0</code>. Timeout (minutes) Optional. Job is marked failed if it does not complete within this time. <p>New jobs start as DRAFT</p> <p>Creating a job definition via the dashboard sets its status to <code>DRAFT</code>. The scheduler will not fire a <code>DRAFT</code> job. You must promote it to <code>ACTIVE</code> before it will run. See Job Lifecycle below.</p>"},{"location":"feature-guides/job-scheduling/#cron-syntax","title":"Cron Syntax","text":"<p>Axiom uses 5-field standard cron with the field order:</p> <pre><code>minute  hour  day  month  day_of_week\n</code></pre> <p>APScheduler supports the <code>*</code>, <code>/</code>, <code>-</code>, and <code>,</code> operators.</p> <p>Common patterns:</p> Pattern Expression Description Every 5 minutes <code>*/5 * * * *</code> Runs at :00, :05, :10, ... Daily at 2 AM <code>0 2 * * *</code> Every day at 02:00 Weekdays at noon <code>0 12 * * 1-5</code> Mon\u2013Fri at 12:00 First of month <code>0 0 1 * *</code> Midnight, 1st of each month Every hour <code>0 * * * *</code> At :00 of every hour <p>Expression builder</p> <p>Use crontab.guru to compose and verify complex cron expressions before saving a job definition.</p> <p>Do not use 6-field cron</p> <p>Do not use a 6-field cron expression (with a leading seconds field). Axiom uses 5-field standard cron; a 6-field string will fail to schedule and the job definition will not be activated.</p>"},{"location":"feature-guides/job-scheduling/#the-scheduled-jobs-view","title":"The Scheduled Jobs view","text":"<p>The Scheduled Jobs view lists all active job definitions with their next fire time and last execution status:</p> <p></p>"},{"location":"feature-guides/job-scheduling/#node-targeting","title":"Node Targeting","text":"<p>Three targeting modes are available. They can be combined, but specific targeting takes precedence:</p> Mode Field When to use Any eligible node (leave all targeting fields empty) Stateless jobs that can run anywhere Capability targeting <code>capability_requirements</code> dict Jobs requiring specific OS, runtime, or hardware Tag targeting <code>target_tags</code> list Jobs for a tagged group (e.g. <code>[\"gpu\"]</code>) Specific node <code>target_node_id</code> UUID Pinned jobs that must run on a named node <p>Capability targeting example:</p> <pre><code>{\n  \"os_family\": \"DEBIAN\",\n  \"runtime\": \"python3\"\n}\n</code></pre> <p>Only nodes whose reported capabilities satisfy every key\u2013value pair in the dict will be considered for assignment.</p> <p>Tag targeting example:</p> <pre><code>[\"gpu\", \"secure\"]\n</code></pre> <p>Only nodes with all listed tags will match.</p> <p>Capability vs tag targeting</p> <p>Use capability targeting when you need to match on specific runtime properties. Use tag targeting when you control node tags directly and want a human-readable grouping (e.g. <code>\"production\"</code>, <code>\"gpu\"</code>, <code>\"europe\"</code>).</p>"},{"location":"feature-guides/job-scheduling/#job-lifecycle","title":"Job Lifecycle","text":"<p>A job definition moves through the following statuses:</p> Status Meaning <code>DRAFT</code> Created but not yet active. The scheduler skips DRAFT jobs. Promote to ACTIVE when ready. <code>ACTIVE</code> Live \u2014 the scheduler fires the job on each scheduled cron interval. <code>DEPRECATED</code> Soft-retired. The scheduler skips firing and logs <code>job:deprecated_skip</code>. History is preserved. <code>REVOKED</code> Hard-retired. The scheduler skips firing and logs <code>job:revoked_skip</code>. Use for security-sensitive revocations. <p>Typical promotion path: <code>DRAFT</code> \u2192 <code>ACTIVE</code>. Retirement path: <code>ACTIVE</code> \u2192 <code>DEPRECATED</code> or <code>REVOKED</code>.</p> <p>Overlap guard: If a previous instance of the job is still <code>PENDING</code>, <code>ASSIGNED</code>, or <code>RETRYING</code> when the next cron fire is due, Axiom skips the new instance and logs <code>job:cron_skip</code>. This prevents queue buildup for slow or hung jobs.</p> <p>REVOKED is not reversible via the UI</p> <p>Revoking a job definition is intended for security events (e.g. a compromised signing key). Treat it as permanent.</p>"},{"location":"feature-guides/job-scheduling/#retry-configuration","title":"Retry Configuration","text":"<p>When a job fails, Axiom can retry it automatically based on the job definition's retry settings:</p> Field Default Description <code>max_retries</code> <code>0</code> Maximum number of retry attempts. <code>0</code> = no retries. <code>backoff_multiplier</code> <code>2.0</code> Each successive retry waits <code>attempt \u00d7 base_interval \u00d7 backoff_multiplier</code>. <code>timeout_minutes</code> (none) If set, a running job that exceeds this duration is marked failed. <p>Example: With <code>max_retries=3</code> and <code>backoff_multiplier=2.0</code>, the retry waits are approximately 1\u00d7, 2\u00d7, and 4\u00d7 the base interval.</p> <p>Retries and the overlap guard interact</p> <p>A job in <code>RETRYING</code> state counts as an active instance for the overlap guard. If a job is retrying when the next scheduled fire is due, the new fire will be skipped with <code>job:cron_skip</code>.</p>"},{"location":"feature-guides/job-scheduling/#staging-review","title":"Staging Review","text":"<p>Scheduled jobs use the same <code>DRAFT</code> \u2192 <code>ACTIVE</code> promotion workflow as manually pushed jobs. New jobs created via the dashboard or API start as <code>DRAFT</code> and must be reviewed before they run.</p> <p>The Staging view lets you inspect the script content, verify the signature, and promote or reject a job definition before it goes live.</p> <p>See the axiom-push guide for the full Staging view walkthrough, including how to push a script, review it, and promote it to <code>ACTIVE</code>.</p>"},{"location":"feature-guides/job-scheduling/#scheduling-health","title":"Scheduling Health","text":"<p>The Scheduling Health panel shows aggregate execution statistics for all job definitions over a configurable time window (1 hour, 6 hours, 24 hours, or 7 days).</p> <p>Access it from the Job Definitions view \u2192 Scheduling Health panel.</p>"},{"location":"feature-guides/job-scheduling/#health-metrics","title":"Health Metrics","text":"Metric Meaning <code>fired</code> Job fires that executed successfully <code>skipped</code> Fires skipped because a previous fire was still running <code>failed</code> Fires that resulted in a job failure <code>late</code> Fires that started later than their scheduled time <code>missed</code> Scheduled fires that did not occur at all (e.g. service was down) <p>Each definition shows a health status: <code>ok</code>, <code>warning</code>, or <code>error</code> based on its failure and missed-fire rates.</p>"},{"location":"feature-guides/job-scheduling/#api","title":"API","text":"<pre><code>GET /api/health/scheduling?window=24h\n</code></pre> <p>Returns aggregate counts and per-definition rows. The <code>window</code> parameter accepts <code>1h</code>, <code>6h</code>, <code>24h</code>, or <code>7d</code>.</p>"},{"location":"feature-guides/job-scheduling/#execution-retention","title":"Execution Retention","text":"<p>By default, Axiom keeps 14 days of execution history. Older job execution records are pruned automatically.</p> <p>To change the retention period:</p> <ol> <li>Go to Admin \u2192 Retention Settings</li> <li>Set <code>execution_retention_days</code> to your desired value (minimum: 1, recommended: 30\u201390 for production)</li> <li>Click Save</li> </ol> <p>Pinned jobs are not pruned</p> <p>Individual job execution records can be pinned to prevent pruning. Use pinning for post-incident forensics or compliance evidence.</p>"},{"location":"feature-guides/jobs/","title":"Jobs","text":"<p>The Jobs view is the primary interface for dispatching scripts to nodes and monitoring execution in real time.</p>"},{"location":"feature-guides/jobs/#task-types","title":"Task Types","text":"<p>All jobs use the unified <code>script</code> task type with a <code>runtime</code> selector. The three supported runtimes are <code>python</code>, <code>bash</code>, and <code>powershell</code>.</p> <p>Example job payload:</p> <pre><code>{\n  \"task_type\": \"script\",\n  \"runtime\": \"python\",\n  \"script\": \"print('hello')\",\n  \"target_node_id\": \"node-abc123\"\n}\n</code></pre> <p>Old task types are rejected</p> <p>The task type values <code>python_script</code>, <code>bash_script</code>, and <code>powershell_script</code> were removed in v12.0. Jobs submitted with these types are rejected by the API with a 422 validation error. Use <code>task_type: \"script\"</code> with a <code>runtime</code> field instead.</p>"},{"location":"feature-guides/jobs/#dispatching-a-job","title":"Dispatching a Job","text":""},{"location":"feature-guides/jobs/#guided-form","title":"Guided Form","text":"<p>The guided form is the default dispatch interface. It walks through job configuration in structured steps:</p> <ol> <li>Runtime \u2014 select Python, Bash, or PowerShell</li> <li>Script \u2014 paste or type your script content</li> <li>Target \u2014 choose a specific node, env tag, or capability requirements</li> <li>Signature \u2014 select a signing key (required for script execution)</li> </ol> <p>Click Dispatch to submit. The job appears immediately in the queue below.</p>"},{"location":"feature-guides/jobs/#advanced-mode","title":"Advanced Mode","text":"<p>Click [ADV] to switch to the raw JSON dispatch form. This exposes every field in the job payload, including <code>memory_limit</code>, <code>cpu_limit</code>, and <code>retry_after</code>. Advanced mode is intended for power users and CI/CD pipelines. Click [ADV] again to return to the guided form.</p>"},{"location":"feature-guides/jobs/#bulk-operations","title":"Bulk Operations","text":"<p>Select multiple jobs using the checkboxes in the job list to enable bulk actions:</p> Action Description Cancel Cancels all selected pending jobs Resubmit Creates new jobs identical to each selected job Delete Permanently removes selected jobs from the queue <p>Bulk actions apply only to jobs in compatible states \u2014 for example, bulk cancel only affects <code>PENDING</code> or <code>ASSIGNED</code> jobs.</p>"},{"location":"feature-guides/jobs/#queue-monitor","title":"Queue Monitor","text":"<p>The Queue Monitor shows all pending and active jobs in real time via WebSocket updates. It displays per-node job counts and highlights nodes that are receiving work.</p> <p>Click any node card in the Queue Monitor to open the node detail drawer, which shows current health metrics and a history of recently executed jobs.</p> <p>DRAINING nodes</p> <p>A node in <code>DRAINING</code> state does not receive new job assignments. It continues to complete already-assigned jobs. See Nodes for how to set and clear the DRAINING state.</p>"},{"location":"feature-guides/jobs/#draft-lifecycle","title":"DRAFT Lifecycle","text":"<p>Jobs dispatched via <code>axiom-push</code> start in <code>DRAFT</code> state and must be reviewed and published before nodes receive them. See axiom-push CLI and Job Scheduling for DRAFT workflow details.</p> <p>Jobs dispatched directly (via the dashboard or API) start in <code>PENDING</code> state and are assigned to a node immediately.</p>"},{"location":"feature-guides/nodes/","title":"Nodes","text":"<p>Nodes are the worker machines that execute jobs. Each node runs the Axiom puppet agent, which enrolls via mTLS and polls for assigned work.</p>"},{"location":"feature-guides/nodes/#node-states","title":"Node States","text":"State Meaning <code>ONLINE</code> Node is healthy and accepting new job assignments <code>OFFLINE</code> Node has missed recent heartbeats \u2014 no new jobs assigned <code>DRAINING</code> Node is being taken out of service \u2014 no new jobs assigned, but it completes already-assigned jobs <code>REVOKED</code> Node certificate revoked \u2014 blocked from enrolling or pulling work <code>TAMPERED</code> Attestation failure \u2014 node excluded until reviewed"},{"location":"feature-guides/nodes/#the-nodes-view","title":"The Nodes view","text":"<p>The Nodes page provides live status for all enrolled nodes, including capability tags and resource utilisation:</p> <p></p>"},{"location":"feature-guides/nodes/#draining-state","title":"DRAINING State","text":"<p>Use the DRAINING state to gracefully remove a node from the job pool without losing in-flight work.</p> <p>To drain a node (requires <code>nodes:write</code> permission):</p> <p>From the dashboard \u2014 open the node detail drawer and click Drain. The node status changes to <code>DRAINING</code> immediately.</p> <p>Via the API:</p> <pre><code>curl -X PATCH https://your-host/api/nodes/{node_id}/drain \\\n  -H \"Authorization: Bearer $TOKEN\"\n</code></pre> <p>What happens while draining:</p> <ul> <li>The node stops receiving new job assignments</li> <li>Jobs already assigned continue executing normally</li> <li>Heartbeats from the node do NOT revert it back to <code>ONLINE</code> \u2014 the DRAINING state persists until explicitly cleared</li> <li>Other nodes absorb new job assignments</li> </ul> <p>To undrain a node:</p> <p>From the dashboard \u2014 click Undrain in the node detail drawer.</p> <p>Via the API:</p> <pre><code>curl -X PATCH https://your-host/api/nodes/{node_id}/undrain \\\n  -H \"Authorization: Bearer $TOKEN\"\n</code></pre> <p>Safe rolling upgrades</p> <p>DRAINING is the recommended way to take a node offline for maintenance or a software upgrade. Drain the node, wait for in-flight jobs to finish, upgrade the agent, then undrain.</p>"},{"location":"feature-guides/nodes/#node-detail-drawer","title":"Node Detail Drawer","text":"<p>Click any node in the Nodes view or Queue Monitor to open its detail drawer, which shows:</p> <ul> <li>Current health metrics (CPU, RAM) with a sparkline history</li> <li>Assigned and recently completed jobs</li> <li>Enrollment timestamp and certificate serial</li> <li>Drain / Undrain controls</li> </ul>"},{"location":"feature-guides/nodes/#queue-monitor","title":"Queue Monitor","text":"<p>The Queue Monitor (accessible from the Jobs view) shows live per-node job queues via WebSocket. See Jobs for details.</p>"},{"location":"feature-guides/oauth/","title":"OAuth &amp; Authentication","text":"<p>Axiom uses JWT-based authentication with RFC 8628 device flow for CLI tools and API keys for automation.</p> <p>Enterprise</p>"},{"location":"feature-guides/oauth/#authentication-methods","title":"Authentication Methods","text":"<p>Three authentication methods are supported:</p> Method Best for Obtained via Password login Dashboard users Login page (<code>/login</code>) Device flow (OAuth 2.0) CLI tools, <code>axiom-push</code> <code>POST /auth/device</code> + browser approval API key Automation, scripts, CI pipelines My Account \u2192 API Keys \u2192 Create <p>All three methods return or carry a JWT that must be supplied as a <code>Bearer</code> token in the <code>Authorization</code> header.</p>"},{"location":"feature-guides/oauth/#device-flow-oauth-20","title":"Device Flow (OAuth 2.0)","text":"<p>The OAuth 2.0 Device Authorization Grant (RFC 8628) allows tools without a browser \u2014 like <code>axiom-push</code> \u2014 to authenticate without a redirect URI. The flow works by having the user approve the request in a browser session while the tool polls for the result.</p> <p>Why Axiom uses device flow: CLI tools cannot host a redirect URI. The device flow separates the user approval step (done in a browser) from the tool waiting for the token (done by polling). No callback server is needed on the client machine.</p>"},{"location":"feature-guides/oauth/#flow-overview","title":"Flow overview","text":"<ol> <li>The CLI requests a <code>device_code</code> and <code>user_code</code> from <code>POST /auth/device</code>.</li> <li>The CLI displays the <code>user_code</code> and the <code>verification_uri</code> to the operator.</li> <li>The operator visits the <code>verification_uri</code> in a browser and enters the <code>user_code</code>.</li> <li>An admin (or the same user if they have permission) approves the request at the approval page.</li> <li>The CLI polls <code>POST /auth/device/token</code> every 5 seconds until a JWT is returned.</li> </ol> <p>Constants:</p> Parameter Value Authorization window 300 seconds (5 minutes) Poll interval 5 seconds <p>Device codes are not persisted</p> <p>Device authorization codes are stored in memory and are not persisted across server restarts. If the server restarts during the 5-minute authorization window, the login flow must be restarted from step 1.</p> <p>For step-by-step <code>axiom-push</code> login instructions, see the axiom-push CLI guide.</p>"},{"location":"feature-guides/oauth/#token-lifecycle","title":"Token Lifecycle","text":"<p>All JWTs issued by Axiom share the same structure and expiry rules.</p> <p>Token fields:</p> Field Description <code>sub</code> Username (string) <code>role</code> User role: <code>admin</code>, <code>operator</code>, or <code>viewer</code> <code>tv</code> Token version \u2014 used for forced invalidation (see below) <code>exp</code> Expiry timestamp (Unix epoch) <code>type</code> Present for non-password tokens: <code>\"device_flow\"</code> or <code>\"service_principal\"</code> <code>sp_id</code> Present on service principal tokens: the service principal's UUID <p>Default expiry: 24 hours from the time of issuance.</p>"},{"location":"feature-guides/oauth/#forced-invalidation","title":"Forced invalidation","text":"<p>When a user's password is changed \u2014 either by the user via My Account or by an admin \u2014 all existing tokens for that user are immediately rejected.</p> <p>The JWT includes a <code>tv</code> (token version) field. When the server validates a token, it checks that the <code>tv</code> value matches the user's current <code>token_version</code> in the database. Any token with a mismatched <code>tv</code> is rejected instantly, regardless of the token's <code>exp</code> claim. There is no need to wait for token expiry.</p> <p>To invalidate all sessions for a user:</p> <pre><code>curl -X POST \\\n  -H \"Authorization: Bearer &lt;ADMIN_JWT&gt;\" \\\n  https://&lt;YOUR_HOST&gt;/admin/users/&lt;USERNAME&gt;/reset-password \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"new_password\": \"&lt;NEW_PASSWORD&gt;\"}'\n</code></pre> <p>This increments the user's <code>token_version</code> and immediately invalidates all prior tokens for that user.</p>"},{"location":"feature-guides/oauth/#api-keys","title":"API Keys","text":"<p>API keys are long-lived bearer tokens. They are created in the My Account section of the dashboard.</p> <p>Creating an API key: Navigate to My Account \u2192 API Keys \u2192 Create. Give the key a descriptive name. The full key value is shown once at creation time \u2014 copy it immediately.</p> <p>Using an API key:</p> <pre><code>curl -H \"Authorization: Bearer &lt;YOUR_API_KEY&gt;\" \\\n  https://&lt;YOUR_HOST&gt;/api/nodes\n</code></pre> <p>API key permissions</p> <p>API keys grant the same access as the owning user's role. The <code>permissions</code> field is reserved for future per-key permission scoping \u2014 per-key permission restriction is not currently enforced. If you need to restrict access, create a dedicated user account with the <code>viewer</code> or <code>operator</code> role and generate an API key for that account.</p> <p>Revoking an API key: Navigate to My Account \u2192 API Keys and click Revoke next to the key. Revocation is immediate.</p>"},{"location":"feature-guides/oauth/#service-principal-tokens","title":"Service Principal Tokens","text":"<p>Service principals are machine identities \u2014 not human users. They are documented fully in the RBAC guide. To obtain a token for a service principal:</p> <pre><code>curl -X POST \\\n  -u \"&lt;SP_CLIENT_ID&gt;:&lt;SP_CLIENT_SECRET&gt;\" \\\n  https://&lt;YOUR_HOST&gt;/auth/service-principals/&lt;SP_ID&gt;/token\n</code></pre> <p>The response contains a JWT with <code>\"type\": \"service_principal\"</code> and the <code>sp_id</code> field set to the service principal's UUID.</p> <p>Secret rotation:</p> <pre><code>curl -X POST \\\n  -H \"Authorization: Bearer &lt;ADMIN_JWT&gt;\" \\\n  https://&lt;YOUR_HOST&gt;/auth/service-principals/&lt;SP_ID&gt;/rotate\n</code></pre> <p>Secret shown once</p> <p>The new secret is returned in the rotation response and cannot be retrieved again. Store it immediately in your secrets manager. The old secret is invalidated at the moment of rotation.</p>"},{"location":"feature-guides/oauth/#cicd-integration","title":"CI/CD Integration","text":"<p>Two approaches are available for pipeline automation. Both use <code>&lt;PLACEHOLDER&gt;</code> values \u2014 replace them with your actual credentials stored as CI secrets.</p>"},{"location":"feature-guides/oauth/#approach-a-api-key-simpler","title":"Approach A: API key (simpler)","text":"<p>Suitable for single-team pipelines where a single operator-level identity is sufficient.</p> <ol> <li>Create an API key in the dashboard: My Account \u2192 API Keys \u2192 Create.</li> <li>Store the key as a CI secret in your pipeline (for example, a secret named AXIOM_API_KEY in GitHub Actions).</li> <li>Use the key directly in pipeline steps:</li> </ol> <pre><code>curl -H \"Authorization: Bearer &lt;AXIOM_API_KEY&gt;\" \\\n  https://&lt;YOUR_HOST&gt;/api/jobs/push \\\n  -H \"Content-Type: application/json\" \\\n  -d @job_payload.json\n</code></pre> <p>Or with <code>axiom-push</code>:</p> <pre><code>AXIOM_API_KEY=&lt;AXIOM_API_KEY&gt; axiom-push push --script my_job.py\n</code></pre>"},{"location":"feature-guides/oauth/#approach-b-service-principal-recommended-for-teams","title":"Approach B: Service principal (recommended for teams)","text":"<p>Suitable when multiple pipelines or teams need isolated identities with independent revocation.</p> <ol> <li>Create a service principal: Admin \u2192 Service Principals \u2192 Create.</li> <li>Set the role to <code>operator</code> (not <code>admin</code>) \u2014 follow the principle of least privilege.</li> <li>Store <code>client_id</code> and <code>client_secret</code> as CI secrets.</li> <li>Request a token at the start of each pipeline run and use it for the duration:</li> </ol> <pre><code># Obtain a token\nTOKEN=$(curl -s -X POST \\\n  -u \"&lt;SP_CLIENT_ID&gt;:&lt;SP_CLIENT_SECRET&gt;\" \\\n  https://&lt;YOUR_HOST&gt;/auth/service-principals/&lt;SP_ID&gt;/token \\\n  | jq -r '.access_token')\n\n# Use the token\ncurl -H \"Authorization: Bearer $TOKEN\" \\\n  https://&lt;YOUR_HOST&gt;/api/nodes\n</code></pre> <p>Set an expiry and rotate on schedule</p> <p>Service principals support an <code>expires_at</code> field set at creation time. Configure a rotation schedule in your CI system to refresh the secret before it expires. See the RBAC guide for the full service principal management reference.</p>"},{"location":"feature-guides/rbac-reference/","title":"RBAC Permission Reference","text":"<p>Complete reference for all permissions in Axiom, with default role assignments.</p> <p>Enterprise</p>"},{"location":"feature-guides/rbac-reference/#default-role-assignments","title":"Default Role Assignments","text":"<p>These are the default permissions assigned at first startup. Permissions can be customised per role through the dashboard \u2014 see the RBAC guide.</p> Permission Description admin operator viewer <code>jobs:read</code> View job queue and results \u2713 \u2713 \u2713 <code>jobs:write</code> Submit, cancel, and retry jobs \u2713 \u2713 \u2014 <code>nodes:read</code> View node status and stats \u2713 \u2713 \u2713 <code>nodes:write</code> Enroll, revoke, and delete nodes \u2713 \u2713 \u2014 <code>definitions:read</code> View scheduled job definitions \u2713 \u2713 \u2713 <code>definitions:write</code> Create, edit, and delete job definitions \u2713 \u2713 \u2014 <code>foundry:read</code> View templates and blueprints \u2713 \u2713 \u2713 <code>foundry:write</code> Create, build, and delete templates/blueprints \u2713 \u2713 \u2014 <code>signatures:read</code> View signing keys \u2713 \u2713 \u2713 <code>signatures:write</code> Upload and delete signing keys \u2713 \u2713 \u2014 <code>tokens:write</code> Issue device flow tokens \u2713 \u2713 \u2014 <code>alerts:read</code> View alerts \u2713 \u2713 \u2713 <code>alerts:write</code> Acknowledge and manage alerts \u2713 \u2713 \u2014 <code>webhooks:read</code> View webhook configurations \u2713 \u2713 \u2014 <code>webhooks:write</code> Create and delete webhooks \u2713 \u2713 \u2014 <code>users:write</code> Manage users, roles, and permissions \u2713 \u2014 \u2014 <p>The <code>admin</code> role bypasses all permission checks regardless of this table. <code>users:write</code> is the only permission not granted to <code>operator</code> by default; it governs user management and role configuration.</p>"},{"location":"feature-guides/rbac-reference/#admin-role","title":"Admin Role","text":"<p>Admin is not a permission set \u2014 it is an unconditional bypass. Any user with role <code>admin</code> can perform any action without a permission check. The table above does not constrain admin behaviour. Audit log entries for admin actions are recorded normally.</p>"},{"location":"feature-guides/rbac-reference/#custom-permissions","title":"Custom Permissions","text":"<p>Any permission can be granted to or revoked from any non-admin role via the dashboard (Admin \u2192 Role Permissions) or the API (<code>POST /admin/roles/{role}/permissions</code>, <code>DELETE /admin/roles/{role}/permissions/{permission}</code>). Changes take effect immediately for all new requests.</p> <p>Warning</p> <p>Granting <code>users:write</code> to the <code>operator</code> role effectively gives every operator full control over user management, including the ability to create admin accounts. Treat <code>users:write</code> as an admin-tier permission.</p> <p>For guidance on least-privilege configuration, see the RBAC Hardening guide.</p>"},{"location":"feature-guides/rbac/","title":"Role-Based Access Control","text":"<p>Axiom uses three roles \u2014 <code>admin</code>, <code>operator</code>, and <code>viewer</code> \u2014 with a permission model that can be customised through the dashboard.</p> <p>Enterprise</p>"},{"location":"feature-guides/rbac/#roles-overview","title":"Roles Overview","text":""},{"location":"feature-guides/rbac/#admin","title":"admin","text":"<p>Full, unrestricted access. The admin role bypasses all permission checks \u2014 no database lookup occurs. Use the admin account only for initial setup, user provisioning, and emergency access. Day-to-day platform work should be done with an <code>operator</code> account.</p>"},{"location":"feature-guides/rbac/#operator","title":"operator","text":"<p>The standard role for platform engineers. Operators can manage jobs, nodes, Foundry templates and blueprints, signing keys, and scheduled job definitions. They cannot manage users, roles, or system configuration.</p>"},{"location":"feature-guides/rbac/#viewer","title":"viewer","text":"<p>Read-only access across the platform. Viewers can inspect job results, node status, Foundry content, and signing keys, but cannot submit jobs or make changes.</p> <p>For the complete permission matrix showing which permissions are assigned to each role by default, see the RBAC Permission Reference.</p>"},{"location":"feature-guides/rbac/#managing-users","title":"Managing Users","text":"<p>The Users section of the dashboard lists all accounts and provides controls for creation, role assignment, and credential management. Only <code>admin</code> users can access this section.</p>"},{"location":"feature-guides/rbac/#creating-a-user","title":"Creating a user","text":"<ol> <li>Navigate to Users in the sidebar</li> <li>Click Create User and fill in username, email, and initial password</li> <li>Select the role: <code>admin</code>, <code>operator</code>, or <code>viewer</code></li> <li>Click Save \u2014 the user can now log in immediately</li> </ol>"},{"location":"feature-guides/rbac/#changing-a-users-role","title":"Changing a user's role","text":"<p>Click the role badge next to a username, select the new role from the dropdown, and confirm. The change takes effect immediately; any active sessions for that user continue with the new role on their next request.</p>"},{"location":"feature-guides/rbac/#force-password-change-on-next-login","title":"Force password change on next login","text":"<p>Toggle Force Password Change on the user row. The user is prompted to set a new password on their next login. Their existing sessions remain valid until that change is completed.</p>"},{"location":"feature-guides/rbac/#resetting-a-users-password","title":"Resetting a user's password","text":"<p>Click Reset Password on a user row. A new temporary password is generated and returned. This immediately increments the user's token version, invalidating all active sessions \u2014 the user must log in again with the new password.</p>"},{"location":"feature-guides/rbac/#deleting-a-user","title":"Deleting a user","text":"<p>Click Delete on the user row to permanently remove the account.</p> <p>Warning</p> <p>Deleting a user is permanent and cannot be undone. Any API keys or signing keys associated with that user are also removed.</p>"},{"location":"feature-guides/rbac/#customising-role-permissions","title":"Customising Role Permissions","text":"<p>Permissions can be granted or revoked on a per-role basis through Admin \u2192 Role Permissions. This allows you to tailor the operator and viewer roles to your team's access requirements without creating custom roles.</p> <p>Warning</p> <p>Permission changes affect all users with that role immediately. Adding <code>users:write</code> to the <code>operator</code> role gives every operator the ability to manage users \u2014 use with care.</p>"},{"location":"feature-guides/rbac/#updating-permissions","title":"Updating permissions","text":"<ol> <li>Navigate to Admin \u2192 Role Permissions</li> <li>Select a role tab (operator or viewer)</li> <li>Click a permission chip to grant it; click it again to revoke</li> <li>Changes take effect immediately for all new requests</li> </ol> <p>The <code>admin</code> role's permissions cannot be modified \u2014 admin always has full access regardless of the permissions table.</p>"},{"location":"feature-guides/rbac/#service-principals","title":"Service Principals","text":"<p>Service principals are machine identities \u2014 they are not human users and do not use the standard login flow. They are designed for automation, CI/CD pipelines, and any non-interactive integration that needs to authenticate against the API.</p> <p>A service principal has a <code>client_id</code> and a <code>client_secret</code>, and is assigned a role just like a user. Actions performed by a service principal are attributed in the audit log as <code>sp:&lt;name&gt;</code>, making it straightforward to trace automated actions separately from human activity.</p>"},{"location":"feature-guides/rbac/#creating-a-service-principal","title":"Creating a service principal","text":"<ol> <li>Navigate to Admin \u2192 Service Principals</li> <li>Click Create Service Principal</li> <li>Enter a name (e.g. <code>ci-pipeline</code>) and select a role</li> <li>Optionally set an expiry date \u2014 the service principal's tokens will be rejected after this date</li> <li>Click Create</li> </ol> <p>Danger</p> <p>The <code>client_secret</code> is shown only once immediately after creation. Copy it to your secrets manager before closing the dialog. It cannot be retrieved again. If lost, use the Rotate Secret button to generate a new one \u2014 this invalidates the old secret immediately.</p>"},{"location":"feature-guides/rbac/#rotating-a-service-principal-secret","title":"Rotating a service principal secret","text":"<p>Click Rotate Secret on the service principal row. The new secret is shown once. The previous secret is invalid from the moment rotation completes.</p>"},{"location":"feature-guides/rbac/#deactivating-a-service-principal","title":"Deactivating a service principal","text":"<p>Toggle Active to false on the service principal row. All tokens issued to that SP are rejected immediately. The service principal record is preserved \u2014 reactivating it does not require regenerating credentials.</p> <p>To get a JWT for a service principal and use it in CI/CD pipelines, see the OAuth &amp; Authentication guide.</p>"},{"location":"feature-guides/rbac/#api-keys","title":"API Keys","text":"<p>API keys are long-lived credentials tied to a specific user account, managed in My Account. They can be used in automation in place of short-lived JWT tokens. Unlike JWT tokens, API keys do not expire unless explicitly revoked.</p> <p>See the OAuth &amp; Authentication guide for usage patterns, including how to use API keys in scripts and how to scope them for read-only access.</p>"},{"location":"getting-started/docker-deployment/","title":"Running with Docker","text":"<p>Axiom ships as a multi-service Docker Compose stack. This guide covers everything needed to run it in production \u2014 from secret generation to upgrades.</p>"},{"location":"getting-started/docker-deployment/#prerequisites","title":"Prerequisites","text":"<ul> <li>Docker 24+ and Docker Compose v2</li> <li>A Linux host (amd64 or arm64)</li> <li>A PostgreSQL 15 database (see PostgreSQL setup below, or use a managed service)</li> </ul> <p>Copy <code>.env.example</code> to <code>.env</code> and fill in the required values before starting:</p> <pre><code>cp .env.example .env\n</code></pre>"},{"location":"getting-started/docker-deployment/#postgresql-setup","title":"PostgreSQL Setup","text":"<p>Axiom uses SQLite by default (dev only). For production, provide a PostgreSQL connection string via <code>DATABASE_URL</code>.</p> <p>If you are running the full Compose stack (<code>compose.server.yaml</code>), a PostgreSQL service named <code>db</code> is already included. Set:</p> <pre><code>DATABASE_URL=postgresql+asyncpg://puppet:password@db/puppet_db\n</code></pre> <p>SQLite is not suitable for production</p> <p>SQLite has no concurrent write protection and no backup tooling. Always use PostgreSQL for deployments with more than one operator or any scheduled jobs.</p>"},{"location":"getting-started/docker-deployment/#secret-generation","title":"Secret Generation","text":"<p>Three secrets must be set before first start:</p> <p>JWT signing key \u2014 used to sign all operator session tokens: <pre><code>python -c \"import secrets; print(secrets.token_hex(32))\"\n# \u2192 paste result into SECRET_KEY=\n</code></pre></p> <p>Fernet encryption key \u2014 encrypts node join tokens and secrets at rest: <pre><code>python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\"\n# \u2192 paste result into ENCRYPTION_KEY=\n</code></pre></p> <p>API key \u2014 required by the agent service (crashes at import if absent): <pre><code>python -c \"import secrets; print(secrets.token_urlsafe(32))\"\n# \u2192 paste result into API_KEY=\n</code></pre></p> <p>API_KEY has no fallback</p> <p>Unlike <code>SECRET_KEY</code> (which falls back to a weak dev default), <code>API_KEY</code> has no default value. The agent service will crash at startup if it is not set.</p>"},{"location":"getting-started/docker-deployment/#starting-the-stack","title":"Starting the Stack","text":"<pre><code>docker compose -f puppeteer/compose.server.yaml up -d\n</code></pre> <p>The stack starts four services: <code>agent</code> (FastAPI on 8001), <code>model</code> (port 8000), <code>db</code> (PostgreSQL), and <code>caddy</code> (reverse proxy on 443).</p> <p>Check that all services are healthy:</p> <pre><code>docker compose -f puppeteer/compose.server.yaml ps\n</code></pre>"},{"location":"getting-started/docker-deployment/#optional-service-toggles","title":"Optional Service Toggles","text":"Feature How to enable Env var Cloudflare Tunnel Set <code>CLOUDFLARE_TUNNEL_TOKEN</code> See <code>.env.example</code> Tunnel section Axiom EE Set <code>AXIOM_LICENCE_KEY</code> Paste your EE licence key Custom TLS hostname Set <code>SERVER_HOSTNAME</code> Your domain (e.g. <code>axiom.example.com</code>) <p>Leave any optional var commented-out (or blank) to skip that feature. The stack runs without them.</p>"},{"location":"getting-started/docker-deployment/#upgrade-and-re-deploy","title":"Upgrade and Re-deploy","text":"<p>To apply a code change or pull a new image:</p> <pre><code># 1. Pull the latest image (or rebuild locally)\ndocker compose -f puppeteer/compose.server.yaml pull agent\n\n# 2. Apply any pending migration SQL (check release notes)\ndocker exec puppeteer-db-1 psql -U puppet puppet_db &lt; puppeteer/migration_vNN.sql\n\n# 3. Restart only the agent (zero PostgreSQL downtime)\ndocker compose -f puppeteer/compose.server.yaml up -d --no-deps agent\n</code></pre> <p>Schema migrations</p> <p>Axiom uses <code>CREATE TABLE IF NOT EXISTS</code> (no Alembic). New tables are created automatically on restart. New columns require a manual <code>ALTER TABLE</code> \u2014 check the <code>migration_vNN.sql</code> files in the repo for each release.</p>"},{"location":"getting-started/docker-deployment/#production-checklist","title":"Production Checklist","text":"<p>Before exposing Axiom to the network:</p> <ul> <li>[ ] <code>SECRET_KEY</code> set to a randomly generated 32-byte hex string</li> <li>[ ] <code>ENCRYPTION_KEY</code> set to a Fernet-generated key</li> <li>[ ] <code>API_KEY</code> set (non-empty \u2014 service crashes without it)</li> <li>[ ] <code>ADMIN_PASSWORD</code> set to something strong (only read on first start)</li> <li>[ ] <code>DATABASE_URL</code> points to PostgreSQL, not SQLite</li> <li>[ ] TLS configured (Caddy handles this automatically via HTTPS redirect)</li> <li>[ ] Cloudflare Tunnel or reverse proxy restricts public access to port 443 only</li> </ul>"},{"location":"getting-started/docker-deployment/#next-steps","title":"Next Steps","text":"<ul> <li>Enroll your first node to start routing jobs</li> <li>Set up RBAC for operator and viewer accounts</li> <li>Configure Foundry to build custom node images</li> </ul>"},{"location":"getting-started/enroll-node/","title":"Enroll a Node","text":"<p>Nodes self-enroll over mTLS \u2014 they generate a certificate signing request and get a signed client cert from the control plane. You provide a JOIN_TOKEN that embeds the Root CA so the node can establish trust automatically.</p>"},{"location":"getting-started/enroll-node/#step-1-generate-an-enrollment-token","title":"Step 1: Generate an enrollment token","text":"DashboardCLI <ol> <li>In the dashboard, go to Nodes</li> <li>Click Generate Token</li> <li>Click Copy JOIN_TOKEN \u2014 this copies the enhanced token (base64-encoded JSON with the Root CA embedded)</li> </ol> <p>Use the dashboard Copy button</p> <p>The raw API endpoint <code>POST /api/enrollment-tokens</code> returns only the token hex string. The enhanced JOIN_TOKEN \u2014 which includes the Root CA for mTLS bootstrap \u2014 is only available via the dashboard Copy JOIN_TOKEN button.</p> <p>If you give the node the raw hex string, you will see the node failing with a TLS error rather than the expected log line: <pre><code>Detected Enhanced Token. Bootstrapping Trust...\n</code></pre></p> <p>Always copy the JOIN_TOKEN from the dashboard.</p> <p>Log in to get a JWT:</p> <pre><code>TOKEN=$(curl -sk -X POST https://&lt;your-orchestrator&gt;:8001/auth/login \\\n  -H 'Content-Type: application/x-www-form-urlencoded' \\\n  -d 'username=admin&amp;password=&lt;your-password&gt;' | python3 -c \"import sys,json; print(json.load(sys.stdin)['access_token'])\")\n</code></pre> <p>Then generate and retrieve the enhanced token:</p> <pre><code>curl -sk -X POST https://&lt;your-orchestrator&gt;:8001/admin/generate-token \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['token'])\"\n</code></pre> <p>The <code>token</code> field contains the full base64-encoded JOIN_TOKEN with the Root CA embedded.</p> <p>Admin password (cold-start installs)</p> <p>If you started Axiom using <code>compose.cold-start.yaml</code> without setting <code>ADMIN_PASSWORD</code> in your <code>.env</code> file, the admin account was created with a random password. You will not be able to log in.</p> <p>Before starting the stack, create a <code>.env</code> file in the same directory as <code>compose.cold-start.yaml</code>:</p> <pre><code>ADMIN_PASSWORD=your-chosen-password\nENCRYPTION_KEY=$(python3 -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\")\n</code></pre> <p>Then restart: <code>docker compose -f compose.cold-start.yaml --env-file .env down -v &amp;&amp; docker compose -f compose.cold-start.yaml --env-file .env up -d</code></p>"},{"location":"getting-started/enroll-node/#step-2-configure-node-connectivity","title":"Step 2: Configure node connectivity","text":"<p>Choose the <code>AGENT_URL</code> value that matches your deployment scenario:</p> Scenario AGENT_URL Cold-start compose (node in same compose network) <code>https://agent:8001</code> Server compose, node on same host <code>https://puppeteer-agent-1:8001</code> Remote host / separate machine <code>https://&lt;hostname-or-ip&gt;:8001</code> Docker Desktop (Mac or Windows) <code>https://host.docker.internal:8001</code> <p>If your node is on a custom Linux bridge network, find the gateway with:</p> <pre><code>ip route | awk '/default/ {print $3}'\n</code></pre>"},{"location":"getting-started/enroll-node/#step-3-install-the-node","title":"Step 3: Install the node","text":"Option A: curl installerOption B: Docker Compose <p>The one-liner downloads and runs the universal installer script hosted on the orchestrator:</p> <pre><code>curl -sSL https://&lt;your-orchestrator&gt;/installer.sh | bash -s -- --token \"&lt;JOIN_TOKEN&gt;\"\n</code></pre> <p>Replace <code>&lt;your-orchestrator&gt;</code> with your orchestrator's hostname or IP (e.g., <code>10.0.0.5:8001</code> or <code>my-orchestrator.example.com</code>).</p> <p>The installer script:</p> <ul> <li>Detects Docker or Podman on your system</li> <li>Downloads a ready-to-run <code>node-compose.yaml</code> from the orchestrator</li> <li>Starts the node container automatically</li> </ul> <p>Getting the compose file without running it</p> <p>The orchestrator also serves the generated compose file directly if you want to inspect or customise it before running: <pre><code>curl -sSL \"https://&lt;your-orchestrator&gt;/api/installer/compose?token=&lt;JOIN_TOKEN&gt;\" &gt; node-compose.yaml\ndocker compose -f node-compose.yaml up -d\n</code></pre></p> <p>For full control over the configuration, create the compose file manually.</p> <p>Create <code>node-compose.yaml</code> with the following content, substituting your JOIN_TOKEN and AGENT_URL:</p> <pre><code>services:\n  puppet-node:\n    image: localhost/master-of-puppets-node:latest\n    environment:\n      NODE_TAGS: general,linux\n      JOB_IMAGE: docker.io/library/python:3.12-alpine\n      AGENT_URL: https://172.17.0.1:8001\n      JOIN_TOKEN: &lt;paste-your-enhanced-token-here&gt;\n      ROOT_CA_PATH: /app/secrets/root_ca.crt\n      EXECUTION_MODE: docker\n    volumes:\n      - node-secrets:/app/secrets\n      - /var/run/docker.sock:/var/run/docker.sock\n\nvolumes:\n  node-secrets:\n</code></pre> <p>EXECUTION_MODE=docker</p> <p>When the node container runs inside Docker, set <code>EXECUTION_MODE=docker</code>. This tells the node to spawn job containers using the host's Docker daemon via the mounted socket (<code>/var/run/docker.sock</code>).</p> <p>You must also add the Docker socket to the node's volumes:</p> <pre><code>volumes:\n  - node-secrets:/app/secrets\n  - /var/run/docker.sock:/var/run/docker.sock\n</code></pre> <p>Then update your compose to mount the socket:</p> <pre><code>services:\n  puppet-node:\n    image: localhost/master-of-puppets-node:latest\n    environment:\n      ...\n      EXECUTION_MODE: docker\n    volumes:\n      - node-secrets:/app/secrets\n      - /var/run/docker.sock:/var/run/docker.sock\n</code></pre> <p>Then start the node:</p> <pre><code>docker compose -f node-compose.yaml up -d\n</code></pre>"},{"location":"getting-started/enroll-node/#step-4-verify-enrollment","title":"Step 4: Verify enrollment","text":"<p>Check the node logs:</p> <pre><code>docker compose -f node-compose.yaml logs -f\n</code></pre> <p>On successful enrollment you should see:</p> <pre><code>Detected Enhanced Token. Bootstrapping Trust...\nNode enrolled successfully\n</code></pre> <p>Then check the dashboard \u2014 go to Nodes and the node should appear with status ONLINE within 30 seconds as it begins sending heartbeats.</p>"},{"location":"getting-started/enroll-node/#what-the-nodes-view-looks-like","title":"What the Nodes view looks like","text":"<p>Once your node is enrolled and sending heartbeats, the Nodes page shows live status:</p> <p></p> <p>Click any node row to open the node detail drawer with capability information, stats history, and enrollment details:</p> <p></p> <p>Next: First Job \u2192</p>"},{"location":"getting-started/first-job/","title":"Your First Job","text":"<p>Jobs are Python scripts signed with an Ed25519 private key. The node verifies the signature before executing. This guide walks through generating a signing key, registering the public key, and dispatching your first job.</p>"},{"location":"getting-started/first-job/#step-0-generate-a-signing-keypair","title":"Step 0: Generate a signing keypair","text":"<p>Every job must be signed before dispatch. Generate a keypair once \u2014 the private key stays on your machine, the public key gets registered in Axiom.</p> Python (cryptography)openssl <pre><code>python3 - &lt;&lt;'EOF'\nfrom cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey\nfrom cryptography.hazmat.primitives import serialization\n\nkey = Ed25519PrivateKey.generate()\n\nwith open(\"signing.key\", \"wb\") as f:\n    f.write(key.private_bytes(\n        serialization.Encoding.PEM,\n        serialization.PrivateFormat.PKCS8,\n        serialization.NoEncryption()\n    ))\n\nwith open(\"verification.key\", \"wb\") as f:\n    f.write(key.public_key().public_bytes(\n        serialization.Encoding.PEM,\n        serialization.PublicFormat.SubjectPublicKeyInfo\n    ))\n\nprint(\"Done. Upload verification.key to Axiom, keep signing.key private.\")\nEOF\n</code></pre> <pre><code>openssl genpkey -algorithm ed25519 -out signing.key\nopenssl pkey -in signing.key -pubout -out verification.key\n</code></pre> <p>This produces <code>signing.key</code> (private \u2014 keep safe) and <code>verification.key</code> (public \u2014 upload to Axiom).</p> <p>Never commit signing.key</p> <p>Store it in a secrets manager in production. Anyone who holds it can forge job signatures.</p>"},{"location":"getting-started/first-job/#register-the-public-key","title":"Register the public key","text":"<ol> <li>Go to Signatures in the dashboard sidebar</li> <li>Click Register Trusted Key</li> <li>Paste the contents of <code>verification.key</code>, give it a name (e.g. <code>dev-key</code>), click Establish Trust</li> <li>Note the Key ID \u2014 you'll need it when dispatching</li> </ol> <p>Getting started banner</p> <p>The Signatures page shows a banner with these same commands when no keys are registered yet.</p>"},{"location":"getting-started/first-job/#quick-start-axiom-push-cli","title":"Quick Start (axiom-push CLI)","text":"<p>axiom-push requires EE</p> <p><code>axiom-push</code> is an Enterprise Edition feature. CE users should skip to Manual Setup.</p> <p>Step 0: Set your server URL</p> <pre><code>export AXIOM_URL=https://your-orchestrator:8001\n</code></pre> <p>Step 1: Run axiom-push init</p> <pre><code>axiom-push init\n</code></pre> <p><code>init</code> completes three steps automatically: 1. Login \u2014 opens a browser for the OAuth device flow (skipped if already logged in) 2. Key generation \u2014 creates <code>~/.axiom/signing.key</code> and <code>~/.axiom/verification.key</code> 3. Registration \u2014 uploads the public key to the server and prints your Key ID</p> <p>On success you will see:</p> <pre><code>Setup complete.\nKey ID: &lt;id&gt;\n\nPush your first job:\n  axiom-push job push --script hello.py --key ~/.axiom/signing.key --key-id &lt;id&gt;\n</code></pre> <p>Copy the printed command \u2014 your Key ID is already substituted in.</p> Generate a keypair without running init (standalone key generation) <p>Use <code>axiom-push key generate</code> if you only want to create keys locally without logging in:</p> <pre><code>axiom-push key generate\n</code></pre> <p>This writes <code>~/.axiom/signing.key</code> (0600) and <code>~/.axiom/verification.key</code> to disk and prints the public key PEM to stdout. Use <code>--force</code> to overwrite existing keys.</p> <p>After generation, register the public key in the dashboard: 1. Go to Signatures in the sidebar 2. Click Add Signature Key, paste the printed PEM, give it a name, click Save 3. Note the Key ID for use with <code>--key-id</code></p>"},{"location":"getting-started/first-job/#step-2-write-a-test-script","title":"Step 2: Write a test script","text":"<p>Create <code>hello.py</code>:</p> <pre><code>print(\"Hello from Axiom!\")\nimport platform\nprint(f\"Running on {platform.node()} ({platform.system()})\")\n</code></pre>"},{"location":"getting-started/first-job/#step-3-dispatch-the-job","title":"Step 3: Dispatch the job","text":"CLI (axiom-push)Dashboard <pre><code>axiom-push job push \\\n  --script hello.py \\\n  --key ~/.axiom/signing.key \\\n  --key-id &lt;your-key-id&gt;\n</code></pre> <ol> <li>Go to Jobs in the dashboard sidebar</li> <li>Click New Job</li> <li>Fill in the form:<ul> <li>Script: paste the contents of <code>hello.py</code></li> <li>Signature: base64-encoded signature (see Manual Setup below)</li> <li>Signature Key: select the key you registered</li> <li>Target tags: leave blank for any available node, or enter <code>general</code></li> </ul> </li> <li>Click Dispatch</li> </ol>"},{"location":"getting-started/first-job/#step-4-verify-the-result","title":"Step 4: Verify the result","text":"<p>The job appears in the Jobs list and transitions through statuses:</p> <pre><code>PENDING \u2192 ASSIGNED \u2192 COMPLETED\n</code></pre> <p>Click the job row to expand the result. The output should show:</p> <pre><code>Hello from Axiom!\nRunning on &lt;node-hostname&gt; (Linux)\n</code></pre> <p>You've completed the Getting Started guide</p> <p>Your node is enrolled, jobs are running, and results are captured in the dashboard.</p> <p>What to explore next:</p> <ul> <li>Foundry \u2014 build custom node images with pre-installed runtimes and packages</li> <li>axiom-push CLI \u2014 full CLI reference</li> </ul>"},{"location":"getting-started/first-job/#what-the-jobs-view-looks-like","title":"What the Jobs view looks like","text":"<p>The Jobs view shows all dispatched jobs with their current status:</p> <p></p> <p>Click a completed job row to open the job detail panel with output, timing, and attestation details:</p> <p></p>"},{"location":"getting-started/first-job/#manual-setup","title":"Manual Setup","text":"<p>Use this path if you do not have <code>axiom-push</code> or prefer to use <code>openssl</code> directly.</p>"},{"location":"getting-started/first-job/#generate-a-signing-keypair","title":"Generate a signing keypair","text":"<pre><code>openssl genpkey -algorithm ed25519 -out signing.key\nopenssl pkey -in signing.key -pubout -out verification.key\n</code></pre> <p>Keep your private key safe</p> <p>Never commit <code>signing.key</code> to git. In production, store it in a secrets manager.</p>"},{"location":"getting-started/first-job/#register-the-public-key-in-the-dashboard","title":"Register the public key in the dashboard","text":"<ol> <li>Go to Signatures in the dashboard sidebar</li> <li>Click Add Signature Key</li> <li>Paste the contents of <code>verification.key</code> into the field</li> <li>Give it a descriptive name (e.g., <code>dev-operator-key</code>)</li> <li>Click Save</li> <li>Note the Key ID \u2014 you will need it when submitting jobs</li> </ol> <p>Register before dispatching</p> <p>Job creation fails with a <code>422</code> signature validation error if no public key is registered.</p>"},{"location":"getting-started/first-job/#sign-and-submit-with-curl","title":"Sign and submit with curl","text":"<pre><code>SIG=$(openssl pkeyutl -sign -inkey signing.key -rawin -in hello.py | base64 -w0)\ncurl -sk -X POST https://&lt;your-orchestrator&gt;:8001/jobs \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d \"{\\\"script_content\\\": \\\"$(cat hello.py | python3 -c 'import sys,json; print(json.dumps(sys.stdin.read()))')\\\", \\\"signature\\\": \\\"$SIG\\\", \\\"signature_key_id\\\": \\\"&lt;key-id&gt;\\\"}\"\n</code></pre> <p>Set <code>$TOKEN</code> by logging in first: <pre><code>TOKEN=$(curl -sk -X POST https://&lt;your-orchestrator&gt;:8001/auth/login \\\n  -H 'Content-Type: application/x-www-form-urlencoded' \\\n  -d 'username=admin&amp;password=&lt;your-password&gt;' | python3 -c \"import sys,json; print(json.load(sys.stdin)['access_token'])\")\n</code></pre></p>"},{"location":"getting-started/install/","title":"Install","text":"<p>This guide uses Docker Compose (v2) \u2014 the recommended path for both homelab and production deployments.</p>"},{"location":"getting-started/install/#step-1-get-the-source","title":"Step 1: Get the source","text":"Git CloneGHCR Pull (no git required) <pre><code>git clone https://github.com/your-org/master-of-puppets.git\ncd master-of-puppets\n</code></pre> <p>Download the compose file directly and pull all images \u2014 no git installation needed:</p> <pre><code>curl -sSLO https://raw.githubusercontent.com/axiom-laboratories/axiom/main/puppeteer/compose.cold-start.yaml\ndocker compose -f compose.cold-start.yaml pull\n</code></pre> <p>Then continue with Step 2 to create your <code>.env</code> file before starting the stack.</p>"},{"location":"getting-started/install/#step-2-configure-environment-variables","title":"Step 2: Configure environment variables","text":"Server InstallQuick Start <p>Create <code>puppeteer/secrets.env</code> with the required variables:</p> <pre><code># puppeteer/secrets.env\n\n# JWT signing secret \u2014 generate with:\n# python -c \"import secrets; print(secrets.token_hex(32))\"\nSECRET_KEY=&lt;random-64-char-hex&gt;\n\n# Fernet key for encrypting secrets at rest \u2014 generate with:\n# python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\"\nENCRYPTION_KEY=&lt;fernet-key&gt;\n\n# Shared API key for legacy API key auth\nAPI_KEY=&lt;arbitrary-string&gt;\n\n# Initial admin password \u2014 seeds the admin user on first start only\nADMIN_PASSWORD=&lt;initial-admin-password&gt;\n</code></pre> <p>API_KEY is required</p> <p>If <code>API_KEY</code> is missing from <code>secrets.env</code>, the agent service crashes at startup with no useful log message \u2014 the process exits silently at import time. Set it to any non-empty string.</p> <p>ADMIN_PASSWORD is first-start only</p> <p><code>ADMIN_PASSWORD</code> is only read when the admin user does not yet exist in the database. After the first start, changing this value in <code>secrets.env</code> has no effect. Use the dashboard Users page to change the admin password on existing deployments.</p> <p>Create a <code>.env</code> file in the same directory as <code>compose.cold-start.yaml</code>:</p> <pre><code># .env \u2014 place in same directory as compose.cold-start.yaml\n\n# Initial admin password \u2014 seeds the admin user on first start only\nADMIN_PASSWORD=&lt;choose-a-password&gt;\n\n# Fernet key for encrypting secrets at rest\nENCRYPTION_KEY=$(python3 -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\")\n</code></pre> <p>ADMIN_PASSWORD is first-start only</p> <p><code>ADMIN_PASSWORD</code> is only read when the admin user does not yet exist in the database. After the first start, changing this value has no effect. Use the dashboard Users page to change the admin password.</p> <p>Generating ENCRYPTION_KEY</p> <p>Run the <code>python3 -c \"...\"</code> command above in your shell and paste the output as the value. The key must be a valid Fernet key (43 URL-safe base64 characters followed by <code>=</code>).</p>"},{"location":"getting-started/install/#step-3-start-the-stack","title":"Step 3: Start the stack","text":"Server InstallQuick Start <pre><code>docker compose -f puppeteer/compose.server.yaml up -d\n</code></pre> <p>This starts: Caddy (reverse proxy + TLS), the Agent Service (API on port 8001), the Model Service (port 8000), and PostgreSQL.</p> <pre><code>docker compose -f compose.cold-start.yaml --env-file .env up -d\n</code></pre> <p>This starts: Caddy (reverse proxy + TLS, port 8443), the Agent Service (port 8001), and PostgreSQL.</p>"},{"location":"getting-started/install/#step-4-verify","title":"Step 4: Verify","text":"Server InstallQuick Start <p>Check that all containers are running:</p> <pre><code>docker compose -f puppeteer/compose.server.yaml ps\n</code></pre> <p>All services should show <code>running</code> status. Then open <code>https://localhost/</code> in a browser \u2014 you should see the dashboard login page.</p> <p>Check that all containers are running:</p> <pre><code>docker compose -f compose.cold-start.yaml ps\n</code></pre> <p>All services should show <code>running</code> status. Then open <code>https://localhost:8443/</code> in a browser \u2014 you should see the dashboard login page.</p> <p>TLS certificate warning</p> <p>The Root CA is auto-generated on first start. Your browser will show a certificate warning because it does not trust the self-signed CA yet. To install the CA in your browser and system trust store:</p> <ul> <li>Linux: Visit <code>https://your-host/system/root-ca-installer</code> and run the downloaded script</li> <li>Windows: Visit <code>https://your-host/system/root-ca-installer.ps1</code> and run the downloaded script</li> </ul> <p>This installs the Root CA into your OS and browser (Chrome and Firefox included on Linux).</p> <p>Podman Compose</p> <p>The stack is tested under both Docker and Podman. Substitute <code>podman compose</code> for <code>docker compose</code> in all commands. Everything else in this guide applies unchanged.</p>"},{"location":"getting-started/install/#enterprise-edition","title":"Enterprise Edition","text":"<p>To enable EE features, add your licence key to <code>secrets.env</code>:</p> <pre><code>AXIOM_LICENCE_KEY=&lt;your-licence-key&gt;\n</code></pre> <p>The stack reads this at startup \u2014 no plugin install required. A valid key enables the following features:</p> <ul> <li><code>foundry</code> \u2014 Docker image builder for custom node environments</li> <li><code>rbac</code> \u2014 role-based access control with per-role permission management</li> <li><code>webhooks</code> \u2014 outbound webhook delivery on job events</li> <li><code>triggers</code> \u2014 event-driven job triggering</li> <li><code>audit</code> \u2014 persistent audit log for all security-relevant actions</li> <li><code>resource_limits</code> \u2014 per-job memory and CPU enforcement</li> <li><code>service_principals</code> \u2014 non-human machine identities for CI/CD</li> <li><code>api_keys</code> \u2014 long-lived API key authentication</li> <li><code>executions</code> \u2014 execution history and attestation log</li> </ul>"},{"location":"getting-started/install/#verify-ee-is-active","title":"Verify EE is active","text":"DashboardCLI <p>Open the dashboard in your browser. The sidebar shows a CE or EE badge \u2014 it switches from CE to EE once the key is loaded and valid.</p> <p>Call <code>GET /api/features</code>:</p> <pre><code>curl -sk https://&lt;your-orchestrator&gt;:8001/api/features\n</code></pre> <p>Expected response when EE is active</p> <pre><code>{\n  \"audit\": true,\n  \"foundry\": true,\n  \"webhooks\": true,\n  \"triggers\": true,\n  \"rbac\": true,\n  \"resource_limits\": true,\n  \"service_principals\": true,\n  \"api_keys\": true,\n  \"executions\": true\n}\n</code></pre> <p>If the key is missing or expired, all values return <code>false</code>.</p> <p>See Licensing \u2192 for validation behaviour, expiry handling, and how to check your licence status.</p> <p>Next: Enroll a Node \u2192</p>"},{"location":"getting-started/prerequisites/","title":"Prerequisites","text":"<p>Before installing Axiom, verify your environment meets the following requirements.</p>"},{"location":"getting-started/prerequisites/#checklist","title":"Checklist","text":"<ul> <li> <p>[ ] Docker 24+ with Docker Compose v2</p> <p>verify with: <pre><code>docker --version &amp;&amp; docker compose version\n</code></pre></p> <p>You need Docker Engine 24.0 or later and the <code>docker compose</code> v2 plugin (note: not the legacy <code>docker-compose</code> v1 binary). Check that <code>docker compose version</code> outputs <code>v2.x.x</code> or higher.</p> </li> <li> <p>[ ] 4 GB RAM available</p> <p>verify with: <pre><code># Linux\nfree -h\n</code></pre></p> <p>On Mac, check Activity Monitor \u2192 Memory. On Windows, open Task Manager \u2192 Performance \u2192 Memory. Ensure at least 4 GB is free before starting the stack.</p> </li> <li> <p>[ ] Ports 80 and 443 available</p> <p>verify with: <pre><code># Linux\nss -tlnp | grep -E ':80|:443'\n\n# macOS\nnetstat -an | grep LISTEN | grep -E ':80|:443'\n</code></pre></p> <p>No output means the ports are free. If a service is already listening on 80 or 443, stop it before starting the stack (Caddy needs both ports).</p> </li> <li> <p>[ ] Git (to clone the repository)</p> <p>verify with: <pre><code>git --version\n</code></pre></p> </li> </ul> <p>Podman Compose</p> <p>The Axiom stack runs under Podman as a drop-in alternative to Docker. To verify your Podman installation: <pre><code>podman-compose --version\n</code></pre> Substitute <code>podman compose</code> for <code>docker compose</code> in all commands throughout this guide.</p> <p>Corporate / enterprise proxy</p> <p>If deploying behind a corporate proxy, set the standard proxy environment variables before running <code>docker compose</code>: <pre><code>export HTTP_PROXY=http://proxy.example.com:8080\nexport HTTPS_PROXY=http://proxy.example.com:8080\nexport NO_PROXY=localhost,127.0.0.1\n</code></pre></p> <p>Next: Install \u2192</p>"},{"location":"quick-ref/","title":"Quick Reference","text":"<p>This section contains standalone quick-reference materials for Axiom operators and learners.</p>"},{"location":"quick-ref/#course-how-axiom-works","title":"Course \u2014 How Axiom Works","text":"<p>A visual, interactive course covering the Axiom architecture end-to-end: how nodes enroll, how jobs are dispatched, how mTLS secures the pipeline, and how the scheduler fires jobs. Suitable for new users and those who want a deeper mental model.</p> <p>Open Course</p>"},{"location":"quick-ref/#operator-guide","title":"Operator Guide","text":"<p>The Axiom Operator Guide \u2014 a modular reference covering the full lifecycle of running Axiom in production: managing nodes, dispatching and scheduling jobs, configuring RBAC, securing the platform, and interpreting scheduling health metrics.</p> <p>Open Operator Guide</p>"},{"location":"runbooks/","title":"Runbooks","text":"<p>Symptom-first troubleshooting guides for operators. Find the observable state that matches what you are seeing, not the internal component name.</p>"},{"location":"runbooks/#guides","title":"Guides","text":"Guide Use when Node Troubleshooting A node shows Offline, fails to enroll, or reports cert errors Job Execution A job is stuck in Queued, SECURITY_REJECTED, or DEAD_LETTER Foundry A template build fails, Smelt-Check rejects an image, or registry push fails FAQ You hit a known misconfiguration or have an operational how-to question Node Validation You want to verify a node handles all runtimes and constraints correctly <p>If your problem is not covered here, check the Architecture guide or open an issue on GitHub.</p>"},{"location":"runbooks/faq/","title":"FAQ","text":"<p>This is the single searchable reference for known misconfigurations and operator how-to questions. If you do not know which component owns your problem, start here \u2014 each entry links to the deeper guide once you have identified the cause.</p>"},{"location":"runbooks/faq/#quick-reference","title":"Quick Reference","text":"Question Section Blueprint packages are not installed during Foundry builds Blueprint packages not installed Node container fails to run jobs \u2014 RuntimeError: No container runtime found RuntimeError: No container runtime found Node fails to enroll \u2014 Token payload missing 't' or 'ca' JOIN_TOKEN enrollment failure Changing ADMIN_PASSWORD in .env has no effect ADMIN_PASSWORD has no effect How do I reset a node identity without re-enrolling? Reset node identity My scheduled job does not run at the expected local time Scheduled job UTC timezone Can I submit jobs without Ed25519 signing? Ed25519 signing bypass Why does my node appear multiple times in the Nodes view? Duplicate node entries Why is my Foundry template stuck in STAGING status? Template stuck in STAGING Why does my node keep showing TAMPERED status? TAMPERED status"},{"location":"runbooks/faq/#blueprint-packages-are-not-installed-during-foundry-builds","title":"Blueprint packages are not installed during Foundry builds","text":"<p>Blueprint package lists must be structured as a dict with a <code>\"python\"</code> key \u2014 not a plain list. The Foundry service calls <code>packages.get(\"python\", [])</code> internally. A plain list returns an empty result, silently skipping all package installations. The Docker build succeeds, but the node image has none of the expected packages installed.</p> <pre><code>// Correct\n{\"python\": [\"requests\", \"numpy\"]}\n\n// Wrong \u2014 silently skips all packages\n[\"requests\", \"numpy\"]\n</code></pre> <p>See Foundry \u2192 Blueprints for the full blueprint format reference.</p>"},{"location":"runbooks/faq/#node-container-fails-to-run-jobs-runtimeerror-no-container-runtime-found","title":"Node container fails to run jobs \u2014 RuntimeError: No container runtime found","text":"<p>When an Axiom Node runs inside a Docker container (for example, a Foundry-built image deployed via Docker Compose), <code>EXECUTION_MODE</code> must be set to <code>direct</code>. In <code>auto</code> mode, the node tries to spawn Podman or Docker sub-containers. Inside an existing Docker container, this fails due to cgroup v2 conflicts \u2014 neither Podman nor Docker is available in a standard Docker-in-Docker setup. <code>direct</code> mode executes job scripts as Python subprocesses within the node process, without a container wrapper.</p> <pre><code># In node-compose.yaml environment section\nEXECUTION_MODE: direct\n</code></pre> <p>If the node image was built by Foundry and the build itself is the problem, see Foundry Troubleshooting \u2192 Build Failures.</p>"},{"location":"runbooks/faq/#node-fails-to-enroll-token-payload-missing-t-or-ca","title":"Node fails to enroll \u2014 Token payload missing 't' or 'ca'","text":"<p>The <code>JOIN_TOKEN</code> is a base64-encoded JSON object \u2014 it is not a plain string or a JWT. It contains exactly two keys: <code>t</code> (the actual token) and <code>ca</code> (the Root CA PEM). A single trailing newline, a space, or a URL-encoding artefact in the value will corrupt the base64 decode and trigger the <code>Token payload missing 't' or 'ca'</code> error, aborting enrollment.</p> <pre><code>// JOIN_TOKEN decodes to this structure (never construct manually):\n{\n  \"t\": \"&lt;actual_token_string&gt;\",\n  \"ca\": \"-----BEGIN CERTIFICATE-----\\n...\\n-----END CERTIFICATE-----\\n\"\n}\n</code></pre> <p>Copy the token exactly as displayed in Admin \u2192 Nodes \u2192 Generate Join Token. Do not add quotes, newlines, or spaces. When setting it in a compose file, use a single-line value without shell quoting that would introduce escape sequences.</p> <p>See mTLS &amp; Certificates \u2192 The JOIN_TOKEN for the full enrollment flow.</p>"},{"location":"runbooks/faq/#changing-admin_password-in-env-has-no-effect","title":"Changing ADMIN_PASSWORD in .env has no effect","text":"<p>The <code>ADMIN_PASSWORD</code> environment variable is read only once \u2014 when the orchestrator starts for the first time and the <code>admin</code> user does not yet exist in the database. Once the admin account has been created, the env var is ignored on every subsequent start. The database password is the source of truth for all existing deployments.</p> <pre><code># Orchestrator startup logic (simplified):\n# Only seeds if admin user does not exist\n</code></pre> <p>To change the admin password after first run, use Admin \u2192 Users \u2192 admin \u2192 Reset Password in the dashboard.</p> <p>Warning</p> <p>Do not drop the database to reset the admin password. Dropping the database destroys all nodes, jobs, job definitions, templates, and audit history. Use the dashboard Reset Password flow instead.</p>"},{"location":"runbooks/faq/#how-do-i-reset-a-node-identity-without-re-enrolling","title":"How do I reset a node identity without re-enrolling?","text":"<p>You cannot reset a node's identity without re-enrolling. The node identity is cryptographically bound to its client certificate \u2014 the certificate serial number is what the orchestrator uses to identify the node across restarts. To assign a fresh identity: stop the node container, delete <code>secrets/node-*.crt</code> and <code>secrets/node-*.key</code> from the secrets volume, and restart with a valid <code>JOIN_TOKEN</code>. The orchestrator registers it as a new node entry. The old Offline entry can be removed from Nodes \u2192 [node] \u2192 Delete.</p> <p>See mTLS &amp; Certificates \u2192 Certificate Rotation for the full procedure.</p>"},{"location":"runbooks/faq/#my-scheduled-job-does-not-run-at-the-expected-local-time","title":"My scheduled job does not run at the expected local time","text":"<p>APScheduler (the orchestrator's scheduling engine) evaluates all cron expressions in UTC. There is no per-job timezone configuration \u2014 all jobs use the same UTC clock. A cron expression like <code>0 9 * * *</code> fires at 09:00 UTC regardless of the orchestrator server's local timezone or the operator's timezone.</p> <p>Tip</p> <p>Use a UTC cron calculator to convert your intended local time before entering it in the cron field. For example: 09:00 BST (UTC+1) = <code>0 8 * * *</code>. 09:00 AEST (UTC+10) = <code>0 23 * * *</code> (previous day UTC).</p> <p>See Job Scheduling for cron expression syntax and examples.</p>"},{"location":"runbooks/faq/#can-i-submit-jobs-without-ed25519-signing","title":"Can I submit jobs without Ed25519 signing?","text":"<p>No. Signature verification is enforced at the node before any script is executed \u2014 it is not configurable. A job submitted without a valid Ed25519 signature is immediately rejected with status <code>SECURITY_REJECTED</code>. The node must cryptographically confirm that the script was produced by an authorised operator holding a registered private key before running it.</p> <p>Danger</p> <p>There is no configuration flag, environment variable, or API option to disable signature verification. This is a security invariant \u2014 disabling it would allow any actor with API access to execute arbitrary code on nodes.</p> <p>See axiom-push CLI \u2192 Ed25519 Key Setup for how to generate a signing keypair and register the public key.</p>"},{"location":"runbooks/faq/#why-does-my-node-appear-multiple-times-in-the-nodes-view","title":"Why does my node appear multiple times in the Nodes view?","text":"<p>Duplicate entries occur when the <code>secrets/</code> volume is recreated or the <code>node-*.crt</code> and <code>node-*.key</code> files inside it are deleted. On the next restart, the node generates a new UUID, creates fresh credentials, and enrolls as a new node \u2014 creating a second database entry. The original entry remains in the dashboard with an Offline status. To clean up, delete the Offline duplicate from Nodes \u2192 [node] \u2192 Delete. No operational data is lost by removing a stale Offline entry.</p>"},{"location":"runbooks/faq/#why-is-my-foundry-template-stuck-in-staging-status","title":"Why is my Foundry template stuck in STAGING status?","text":"<p>A template in STAGING status means the Docker build completed successfully, but the post-build Smelt-Check smoke test (<code>python --version &amp;&amp; pip --version</code>) failed inside the built image. The Python environment is not functional. Check orchestrator logs for <code>\u274c Smelt-Check FAILED for &lt;template_name&gt;</code>, identify the cause \u2014 usually a mismatch between the base image OS family and the package manager (Alpine vs Debian) \u2014 fix the blueprint, and trigger a rebuild.</p> <p>See Foundry Troubleshooting \u2192 Smelt-Check Failures for full recovery steps.</p>"},{"location":"runbooks/faq/#why-does-my-node-keep-showing-tampered-status","title":"Why does my node keep showing TAMPERED status?","text":"<p>The zero-trust capability guard detected that the node reported a capability that is not present in its registered <code>expected_capabilities</code>. This happens when a tool is installed inside a running container after the template was built, when capability detection logic changes between node restarts, or when a node is redeployed from a different template than the one it was originally enrolled against. To investigate: run <code>docker exec &lt;node_container&gt; &lt;capability_check_command&gt;</code> to inspect the actual capabilities reported. To clear the flag: Nodes \u2192 [node] \u2192 Clear Tamper. If the additional capability is legitimate, rebuild the template to include it so future deployments pass the capability check without triggering TAMPERED.</p>"},{"location":"runbooks/foundry/","title":"Foundry Troubleshooting","text":"<p>Foundry builds custom Docker images for Axiom Nodes from blueprints and templates. When a build fails, the error appears in the Templates view status column and in the orchestrator logs \u2014 this runbook maps each observable symptom to its root cause and recovery steps.</p> <p>See Foundry feature guide for background on blueprints, templates, and the Smelter.</p>"},{"location":"runbooks/foundry/#quick-reference","title":"Quick Reference","text":"Symptom Section Template status shows FAILED Template build status shows FAILED Build rejected with 403 \u2014 unapproved ingredients Template build rejected with 403 \u2014 unapproved ingredients Build rejected with 403 \u2014 ingredient not yet mirrored Template build rejected with 403 \u2014 ingredient not yet mirrored Build log shows environment_service warning Build log shows warning: environment_service not found Template stuck in STAGING after build Template status is STAGING after a successful build Template shows amber warning badge Template shows amber warning badge in Templates view Template shows last built time but image unavailable Template shows a last built time but image is not available on nodes Node container fails to pull image from local registry Node container fails to pull image from the local registry at startup"},{"location":"runbooks/foundry/#build-failures","title":"Build Failures","text":""},{"location":"runbooks/foundry/#template-build-status-shows-failed","title":"Template build status shows FAILED","text":"<p>The Docker build process returned a non-zero exit code \u2014 the most common causes are a pip package that does not exist, a base image that could not be pulled, or a Dockerfile syntax error generated by the template engine. The orchestrator captures the last 250 characters of the build output in the template status field.</p> <p>Recovery:</p> <ol> <li>Check the orchestrator logs for <code>\u274c Build Failed:</code> followed by the truncated Docker output.</li> <li>Identify the failure type:<ul> <li><code>\"no such package\"</code> or <code>\"Could not find a version\"</code> \u2014 the package name in the blueprint is incorrect or does not exist on PyPI. Check the package name and version specifier.</li> <li><code>\"image not found\"</code> or <code>\"pull access denied\"</code> \u2014 the base image specified in the blueprint cannot be pulled. Verify the base image name and that your registry is reachable.</li> <li>Syntax error or unexpected content \u2014 the blueprint JSON has a malformed entry. Review the blueprint in Foundry \u2192 Blueprints and correct any invalid fields.</li> </ul> </li> <li>Fix the blueprint and navigate to Foundry \u2192 Templates \u2192 [template] \u2192 Rebuild to trigger a fresh build.</li> </ol> <p>Verify: The template status column changes from FAILED to ACTIVE.</p> <p>If the build continues to fail after fixing the blueprint, check orchestrator logs for the full Docker build output (<code>docker compose logs agent | grep -A 20 \"Build Failed\"</code>).</p> <p>See Foundry \u2192 Blueprints for the correct blueprint structure.</p>"},{"location":"runbooks/foundry/#template-build-rejected-with-403-unapproved-ingredients","title":"Template build rejected with 403 \u2014 unapproved ingredients","text":"<p>The Smelter is running in STRICT enforcement mode and the blueprint contains one or more packages that are not present in the approved ingredient list. The response body in the orchestrator logs is:</p> <pre><code>Build rejected: Blueprint contains unapproved ingredients: [&lt;pkg&gt;, ...]\n</code></pre> <p>The build is rejected before Docker is invoked \u2014 no image is produced.</p> <p>Recovery:</p> <ol> <li>Check the orchestrator logs for the rejected package names.</li> <li>Either:<ul> <li>Submit an approval request for the missing packages via Foundry \u2192 Smelter \u2192 Add Ingredient, then rebuild once approved.</li> <li>Remove the unapproved package from the blueprint if it is not required.</li> </ul> </li> <li>If you need to unblock quickly and your deployment policy permits it, switch the Smelter to WARNING mode \u2014 the build will proceed and the template will be marked non-compliant rather than rejected.</li> </ol> <p>Verify: Template builds successfully and the status column shows ACTIVE (or STAGING if Smelt-Check runs).</p> <p>Warning</p> <p>In WARNING mode, templates with unapproved ingredients are marked <code>is_compliant=False</code> and display an amber badge. They can be deployed, but they do not meet the approval policy. Switch back to STRICT mode after resolving ingredient approvals.</p> <p>See Foundry \u2192 Smelter for enforcement mode configuration.</p>"},{"location":"runbooks/foundry/#template-build-rejected-with-403-ingredient-not-yet-mirrored","title":"Template build rejected with 403 \u2014 ingredient not yet mirrored","text":"<p>The package is approved in the Smelter but has not yet been synchronised to the local PyPI or APT mirror \u2014 the <code>mirror_status</code> is not MIRRORED. In air-gapped environments where node containers cannot reach the public internet, Foundry requires all packages to be available on the local mirror before building. The response body is:</p> <pre><code>Build rejected: Ingredient '&lt;pkg_name&gt;' is approved but not yet mirrored (Status: &lt;status&gt;). Wait for mirroring to complete or upload the package manually.\n</code></pre> <p>Recovery:</p> <ol> <li>Wait for the mirror synchronisation job to complete. Mirror status is visible in Foundry \u2192 Smelter \u2192 Ingredients.</li> <li>If the sync is taking too long or has stalled, use the Smelter manual upload endpoint to push the package directly.</li> <li>Once the ingredient status changes to MIRRORED, trigger a rebuild.</li> </ol> <p>Verify: Template builds successfully with no 403 response.</p> <p>See Air-Gap Operation for mirror configuration and offline package upload procedures.</p>"},{"location":"runbooks/foundry/#build-log-shows-warning-environment_service-not-found","title":"Build log shows warning: environment_service not found","text":"<p>The Foundry service builds the Docker context by copying <code>puppets/environment_service/</code> from the orchestrator container's filesystem into a temporary build directory. If the path is not found \u2014 due to a misconfigured volume mount or an incorrect working directory \u2014 the generated Dockerfile's COPY step will fail. The warning appears in orchestrator logs before the build starts:</p> <pre><code>\u26a0\ufe0f  environment_service not found at &lt;path&gt; \u2014 COPY may fail\n</code></pre> <p>Recovery:</p> <ol> <li>Confirm the orchestrator container has access to the puppets source directory. Run <code>docker exec puppeteer-agent-1 ls /app/puppets/environment_service</code> to check whether the path exists inside the container.</li> <li>If the path is missing, verify the volume mounts in <code>compose.server.yaml</code> include the <code>puppets/</code> directory.</li> <li>In production deployments built from the standard Docker Compose setup, <code>environment_service/</code> is baked into the image \u2014 no volume mount is needed. If you are running a custom deployment, verify the path matches what the Foundry service expects.</li> </ol> <p>Verify: Run the build again and confirm the <code>\u26a0\ufe0f  environment_service not found</code> warning does not appear.</p> <p>Tip</p> <p>This warning almost always indicates a non-standard deployment path. Standard Docker Compose deployments using <code>compose.server.yaml</code> do not encounter it because <code>environment_service/</code> is included in the agent image build context.</p>"},{"location":"runbooks/foundry/#smelt-check-failures","title":"Smelt-Check Failures","text":""},{"location":"runbooks/foundry/#template-status-is-staging-after-a-successful-build","title":"Template status is STAGING after a successful build","text":"<p>The Smelt-Check smoke test (<code>python --version &amp;&amp; pip --version</code>) ran inside the newly built image and returned a non-zero exit code \u2014 the image was built successfully but the Python environment inside it is not functional. The build pipeline moves the template to STAGING status and logs:</p> <pre><code>\u274c Smelt-Check FAILED for &lt;template_name&gt;\n</code></pre> <p>STAGING templates cannot be deployed to nodes.</p> <p>Recovery:</p> <ol> <li>Check the orchestrator logs for the Smelt-Check failure line to confirm the template name.</li> <li>The most common cause is a base image and OS family mismatch. Alpine images use <code>apk</code> and have different package paths than Debian images. Verify that the <code>base_os</code> field in the blueprint matches the OS family of the base image (e.g., <code>alpine</code> in the image name \u2192 OS family should be ALPINE).</li> <li>Fix the blueprint's base image or OS family setting and trigger a rebuild from Foundry \u2192 Templates \u2192 [template] \u2192 Rebuild.</li> </ol> <p>Verify: Template status changes from STAGING to ACTIVE after the rebuild.</p> <p>If the Smelt-Check continues to fail after fixing the OS family, inspect the build output manually: <code>docker run --rm &lt;image_name&gt; python --version</code> to test the built image locally.</p> <p>See Foundry \u2192 Image Lifecycle for a description of template statuses.</p>"},{"location":"runbooks/foundry/#template-shows-amber-warning-badge-in-templates-view","title":"Template shows amber warning badge in Templates view","text":"<p>The Smelter is in WARNING mode and detected unapproved or unmirrored ingredients during the build. The build succeeded and the Smelt-Check passed, but the template is marked <code>is_compliant=False</code>. The amber badge is informational \u2014 the template can be deployed, but it does not meet the current approval policy.</p> <p>Recovery:</p> <ol> <li>This is expected behaviour in WARNING mode. No action is required to deploy the template.</li> <li>If compliance is required: navigate to Foundry \u2192 Smelter, approve the flagged ingredients, switch to STRICT mode for future builds, then rebuild the template.</li> <li>After rebuilding in STRICT mode with all ingredients approved, the amber badge is removed.</li> </ol> <p>Verify: After rebuilding with STRICT mode and approved ingredients, the amber badge is absent.</p> <p>See Foundry \u2192 Smelter for enforcement mode details.</p>"},{"location":"runbooks/foundry/#registry-issues","title":"Registry Issues","text":""},{"location":"runbooks/foundry/#template-shows-a-last-built-time-but-image-is-not-available-on-nodes","title":"Template shows a last built time but image is not available on nodes","text":"<p>The Docker build succeeded but the subsequent <code>docker push</code> to the local registry (<code>localhost:5000</code>) failed. The template's status field begins with <code>PUSH_FAILED:</code>. The orchestrator logs the push failure and continues \u2014 it does not raise an error or mark the template FAILED \u2014 so the dashboard may show a <code>last_built_at</code> timestamp even though the image was never pushed.</p> <p>Recovery:</p> <ol> <li>Check orchestrator logs for <code>PUSH_FAILED</code> following the build completion line.</li> <li>Verify the local registry container is running: <code>docker ps | grep registry</code>. If it is not listed, start it with <code>docker compose -f compose.server.yaml up -d registry</code>.</li> <li>Once the registry is confirmed running, navigate to Foundry \u2192 Templates \u2192 [template] \u2192 Rebuild to re-trigger the build and push.</li> </ol> <p>Verify: Check orchestrator logs \u2014 the push step should complete without <code>PUSH_FAILED</code>. Confirm the image is in the registry: <code>curl http://localhost:5000/v2/&lt;image_name&gt;/tags/list</code>.</p> <p>Warning</p> <p>The dashboard last built at timestamp reflects when the Docker build completed, not when the push succeeded. Always check orchestrator logs if nodes cannot pull the image, even if the dashboard shows a recent build time.</p>"},{"location":"runbooks/foundry/#node-container-fails-to-pull-image-from-the-local-registry-at-startup","title":"Node container fails to pull image from the local registry at startup","text":"<p>The node container cannot reach <code>localhost:5000</code> from inside the Docker bridge network. <code>localhost</code> inside a container resolves to the container itself \u2014 not the Docker host. The node logs a pull error at startup.</p> <p>Recovery:</p> <ol> <li>Change the image URL in the node's compose file from <code>localhost:5000/&lt;image&gt;</code> to <code>host.docker.internal:5000/&lt;image&gt;</code> or <code>172.17.0.1:5000/&lt;image&gt;</code>.</li> <li>Add <code>extra_hosts</code> to the node service definition to ensure hostname resolution:     <pre><code>extra_hosts:\n  - \"host.docker.internal:172.17.0.1\"\n</code></pre></li> <li>Restart the node container: <code>docker compose up -d &lt;node_service&gt;</code>.</li> </ol> <p>Verify: Node container pulls the image successfully and progresses past the pull step in <code>docker logs &lt;node_container&gt;</code>.</p> <p>Tip</p> <p>The validation node compose files in <code>mop_validation/local_nodes/</code> use the correct networking pattern. Copy the <code>extra_hosts</code> block and image URL format from there if setting up a new node.</p>"},{"location":"runbooks/jobs/","title":"Job Execution Troubleshooting","text":"<p>This runbook covers failures that occur from job submission through execution and result reporting. Use it when a job is stuck, rejected, or disappears from the queue \u2014 match the dashboard status badge or log line you observe to the symptom below and follow the recovery steps.</p>"},{"location":"runbooks/jobs/#quick-reference","title":"Quick Reference","text":"Symptom Section Job stuck in <code>PENDING</code> indefinitely Job stuck in PENDING indefinitely Job status is <code>BLOCKED</code> Job status is BLOCKED Job status is <code>CANCELLED</code> with \"cancelled because upstream failed\" Job status is CANCELLED \u2014 upstream dependency failed Job status is <code>DEAD_LETTER</code> Job status is DEAD_LETTER Job status is <code>ZOMBIE_REAPED</code> Job status is ZOMBIE_REAPED Job status is <code>SECURITY_REJECTED</code> \u2014 Signature Verification FAILED SECURITY_REJECTED \u2014 Signature Verification FAILED Job status is <code>SECURITY_REJECTED</code> \u2014 Verification Key missing SECURITY_REJECTED \u2014 Verification Key missing Job status is <code>SECURITY_REJECTED</code> \u2014 Missing script or signature SECURITY_REJECTED \u2014 Missing script or signature Job moves from <code>ASSIGNED</code> to <code>FAILED</code> or <code>ZOMBIE_REAPED</code> after ~30 minutes Job reaped after ~30 minutes Node never reports job result \u2014 job stays <code>ASSIGNED</code> indefinitely Job stays ASSIGNED indefinitely"},{"location":"runbooks/jobs/#dispatch-failures","title":"Dispatch Failures","text":""},{"location":"runbooks/jobs/#job-stuck-in-queued-pending-indefinitely","title":"Job stuck in Queued / PENDING indefinitely","text":"<p>The orchestrator found no node with matching tags, required capabilities, and sufficient memory budget. Jobs remain <code>PENDING</code> until a matching node checks in \u2014 they are never dropped silently.</p> <p>Recovery steps:</p> <ol> <li>Open Jobs view and note the job's <code>capability_requirements</code> and <code>target_tags</code>.</li> <li>Open Nodes view and confirm at least one node with <code>Online</code> status has the required capabilities. Select a node and check its capabilities panel.</li> <li>If no node qualifies, either reduce the job's requirements or enroll a node with the needed capabilities.</li> <li>If the job has a <code>memory_limit</code> set, check that at least one online node has sufficient free memory headroom \u2014 nodes with insufficient memory are skipped by the selection loop.</li> </ol> <p>Verify it worked:</p> <p>Watch the job status in the Jobs view. After a matching node checks in (within its next poll interval), the status should change from <code>PENDING</code> to <code>ASSIGNED</code>.</p> <p>Expected: job status badge transitions to <code>ASSIGNED</code> within 30 seconds of a matching node coming online.</p> <p>If the issue persists after a node with matching capabilities is Online, check orchestrator logs for node selection output \u2014 look for lines indicating why each candidate node was skipped.</p>"},{"location":"runbooks/jobs/#job-status-is-blocked","title":"Job status is BLOCKED","text":"<p>The job has a <code>depends_on</code> dependency that has not yet reached <code>COMPLETED</code> status. <code>BLOCKED</code> is expected behaviour \u2014 the job will unblock automatically when its dependency completes.</p> <p>Recovery steps:</p> <ol> <li>Open the Jobs view and identify the dependency job listed under <code>depends_on</code>.</li> <li>Check the dependency job's current status.</li> <li>If it is <code>RUNNING</code> or <code>ASSIGNED</code>: no action needed \u2014 wait for it to complete.</li> <li>If it is <code>FAILED</code> or <code>DEAD_LETTER</code>: the dependent job will be <code>CANCELLED</code> (see Job status is CANCELLED). Investigate the dependency failure first.</li> <li>If it is <code>PENDING</code>: the dependency itself may be blocked \u2014 repeat this process for it.</li> </ol> <p>Verify it worked:</p> <p>Observe the dependency job reaching <code>COMPLETED</code>. The dependent job should automatically move to <code>PENDING</code> within one poll cycle.</p> <p>If the dependent job remains <code>BLOCKED</code> after the dependency has <code>COMPLETED</code>, check orchestrator logs for errors in the dependency resolution loop.</p>"},{"location":"runbooks/jobs/#job-status-is-cancelled-upstream-dependency-failed","title":"Job status is CANCELLED \u2014 upstream dependency failed","text":"<p>The upstream dependency job failed, and the orchestrator propagated cancellation to all downstream dependents. The following log line appears in the orchestrator logs:</p> <pre><code>\ud83d\udeab Job &lt;guid&gt; cancelled because upstream &lt;guid&gt; failed\n</code></pre> <p>The dependent job cannot be recovered in place \u2014 it must be resubmitted once the underlying problem is fixed.</p> <p>Recovery steps:</p> <ol> <li>Identify the upstream dependency job (the second <code>&lt;guid&gt;</code> in the log line above).</li> <li>Open the upstream job's execution record and check its output for the failure reason.</li> <li>Fix the root cause of the upstream failure (script error, missing resource, or signing problem).</li> <li>Resubmit the upstream job. If it completes successfully, resubmit the originally-cancelled downstream job.</li> </ol> <p>Verify it worked:</p> <p>Resubmit both jobs and confirm the upstream job reaches <code>COMPLETED</code>. The downstream job should then reach <code>COMPLETED</code> (or <code>ASSIGNED</code>) without being <code>CANCELLED</code>.</p> <p>If the issue persists, check that the <code>depends_on</code> chain is correct and that the upstream job's fix addresses the original failure cause.</p>"},{"location":"runbooks/jobs/#job-status-is-dead_letter","title":"Job status is DEAD_LETTER","text":"<p>The job failed on every retry attempt up to its <code>max_retries</code> limit. The orchestrator marks it <code>DEAD_LETTER</code> and creates an alert. The following log line appears in the orchestrator logs:</p> <pre><code>Job &lt;guid&gt; exhausted all &lt;N&gt; retries and failed terminally.\n</code></pre> <p>DEAD_LETTER jobs cannot be retried</p> <p>A job in <code>DEAD_LETTER</code> status is permanently terminal. You cannot retry it \u2014 you must fix the underlying issue and submit a new job.</p> <p>Recovery steps:</p> <ol> <li>Open the job's execution record in the Jobs view and read the output or error message from the final attempt.</li> <li>Identify the root cause: script error, missing dependency, resource constraint, or signing problem.</li> <li>Fix the underlying issue in your script or job configuration.</li> <li>Submit a new job with the corrected script (re-signed via <code>axiom-push</code> if the script was changed).</li> </ol> <p>Verify it worked:</p> <p>The new job should progress past <code>PENDING</code> to <code>COMPLETED</code> without reaching <code>FAILED</code>.</p> <p>If the new job also fails, compare its output with the original \u2014 look for environment differences, node capability gaps, or persistent resource constraints.</p>"},{"location":"runbooks/jobs/#job-status-is-zombie_reaped","title":"Job status is ZOMBIE_REAPED","text":"<p>The job was <code>ASSIGNED</code> to a node, but the node stopped sending execution updates (crash, OOM kill, or network partition). The zombie reaper reclaims the job after <code>zombie_timeout_minutes</code> elapses (default: 30 minutes, configurable via Admin \u2192 Configuration).</p> <p>Recovery steps:</p> <ol> <li>Check whether the assigned node is still <code>Online</code> in the Nodes view.</li> <li>If the node went <code>Offline</code>, run <code>docker logs &lt;node-container&gt;</code> to check for OOM kill messages or runtime errors.</li> <li>If the node was OOM killed, increase the node's <code>job_memory_limit</code> or reduce the job's <code>memory_limit</code>.</li> <li>If retries remain, the reaped job is automatically returned to <code>PENDING</code> for reassignment. If retries are exhausted, it moves to <code>FAILED</code> \u2014 resubmit a new job.</li> </ol> <p>Verify it worked:</p> <p>Confirm the node returns to <code>Online</code> status. If retries remain, watch the job move back to <code>ASSIGNED</code> on the next healthy node.</p> <p>If the node keeps crashing on the same job, the job's memory or CPU requirements likely exceed what the node can provide \u2014 adjust limits before resubmitting.</p>"},{"location":"runbooks/jobs/#signing-errors","title":"Signing Errors","text":""},{"location":"runbooks/jobs/#security_rejected-signature-verification-failed","title":"SECURITY_REJECTED \u2014 Signature Verification FAILED","text":"<p>The node verified the job's signature against the registered Ed25519 public key and the check failed. Either the script was signed with a different key than the one registered in Signatures, or the script content was modified after signing. The following log line appears in the node's container output:</p> <pre><code>[node-abc12345] \u274c Signature Verification FAILED for Job &lt;guid&gt;: &lt;exception detail&gt;\n</code></pre> <p>Recovery steps:</p> <ol> <li>Check which Ed25519 key was used to sign the script. The <code>axiom-push</code> tool shows the key fingerprint at signing time.</li> <li>Open Signatures in the dashboard and confirm the matching public key is registered.</li> <li>If the key is missing, register it: see Ed25519 Key Setup.</li> <li>If the script was modified after signing, re-sign it with <code>axiom-push</code> and resubmit.</li> </ol> <p>Do not manually edit signed scripts</p> <p>Any modification to a script after signing \u2014 including whitespace changes \u2014 invalidates the signature. Always re-sign after any edit.</p> <p>Verify it worked:</p> <p>Resubmit the job and confirm it reaches <code>ASSIGNED</code> rather than <code>SECURITY_REJECTED</code>. Check the node logs for the absence of the <code>\u274c Signature Verification FAILED</code> line.</p> <p>If the issue persists, confirm the public key in Signatures is the exact hex or PEM of the key that signed the script \u2014 a key registered under the wrong name is a common cause of repeat failures.</p>"},{"location":"runbooks/jobs/#security_rejected-verification-key-missing","title":"SECURITY_REJECTED \u2014 Verification Key missing","text":"<p>The node could not fetch the verification public key from the orchestrator's <code>/verification-key</code> endpoint at startup \u2014 usually a transient connectivity issue or the node started before the orchestrator was ready. The following log line appears in the node's container output:</p> <pre><code>[node-abc12345] \u274c CRITICAL: Verification Key missing. Cannot verify signature.\n</code></pre> <p>Until the verification key is loaded, the node rejects every job with <code>SECURITY_REJECTED</code>.</p> <p>Recovery steps:</p> <ol> <li>Restart the node container. On restart, the node re-fetches the verification key from the orchestrator.</li> <li>If the error persists after restart, confirm the orchestrator is reachable at <code>AGENT_URL</code> from within the node's network. Run <code>docker exec &lt;node-container&gt; curl -k &lt;AGENT_URL&gt;/api/verification-key</code> to test connectivity.</li> <li>If the orchestrator is unreachable, check <code>AGENT_URL</code> in the node's environment variables and verify no firewall or DNS issue blocks the connection.</li> </ol> <p>Verify it worked:</p> <p>Check the node startup logs for <code>[node-abc12345] \ud83d\udd11 Verification Key updated.</code> \u2014 this line confirms the key was fetched successfully.</p> <p>If the verification key line is missing and the CRITICAL error persists, the orchestrator endpoint may be unhealthy \u2014 check orchestrator logs for startup errors.</p>"},{"location":"runbooks/jobs/#security_rejected-missing-script-or-signature","title":"SECURITY_REJECTED \u2014 Missing script or signature","text":"<p>The job payload reached the node without a <code>signature</code> field. This happens when a job is submitted directly via the API without using <code>axiom-push</code>, or when the signing step was skipped. The node returns the following string in the execution result:</p> <pre><code>Missing script or signature\n</code></pre> <p>Bypassing signing is not supported</p> <p>Signature verification is enforced at the node before any script executes. There is no configuration flag to disable it. Every job must be signed with a registered Ed25519 key.</p> <p>Recovery steps:</p> <ol> <li>Resubmit the job using <code>axiom-push</code>, which handles signing automatically. See Ed25519 Key Setup for setup instructions.</li> <li>If submitting via the API directly, ensure the job payload includes a valid <code>signature</code> field. The signature must cover the exact script content and be produced by a key registered in Signatures.</li> </ol> <p>Verify it worked:</p> <p>Resubmit via <code>axiom-push</code> and confirm the job reaches <code>ASSIGNED</code> (or <code>COMPLETED</code>) without <code>SECURITY_REJECTED</code> status.</p> <p>If the issue persists, confirm that <code>axiom-push</code> is configured with a key whose public half is registered in the dashboard Signatures view.</p>"},{"location":"runbooks/jobs/#timeout-patterns","title":"Timeout Patterns","text":""},{"location":"runbooks/jobs/#job-reaped-after-30-minutes","title":"Job reaped after ~30 minutes","text":"<p>The zombie reaper fires after <code>zombie_timeout_minutes</code> elapses (default: 30 minutes). This is the effective operator-visible timeout for job execution. If the executing node stopped reporting progress within this window \u2014 whether due to a crash, OOM kill, or network partition \u2014 the orchestrator reclaims the job and marks it <code>ZOMBIE_REAPED</code> or <code>FAILED</code>.</p> <p>The 30-minute limit is configurable</p> <p>To increase the timeout for long-running jobs, go to Admin \u2192 Configuration and set <code>zombie_timeout_minutes</code> to a higher value. This applies globally to all jobs.</p> <p>Recovery steps:</p> <ol> <li>If the job genuinely requires more than 30 minutes to complete, increase <code>zombie_timeout_minutes</code> via Admin \u2192 Configuration.</li> <li>If the node crashed mid-execution, run <code>docker logs &lt;node-container&gt;</code> to check for OOM messages or unhandled exceptions.</li> <li>If the node was OOM killed, increase the node's <code>job_memory_limit</code> or reduce the job's <code>memory_limit</code>.</li> <li>Once the node is stable and the timeout is adjusted, resubmit the job (if retries are exhausted) or let the queue reassign it (if retries remain).</li> </ol> <p>Verify it worked:</p> <p>Watch the resubmitted job progress through <code>ASSIGNED</code> \u2192 <code>RUNNING</code> \u2192 <code>COMPLETED</code> without being reaped again.</p> <p>If the job is still being reaped after raising the timeout, the node may be crashing for a reason unrelated to timeout \u2014 check node logs for fatal errors during script execution.</p>"},{"location":"runbooks/jobs/#job-stays-assigned-indefinitely","title":"Job stays ASSIGNED indefinitely","text":"<p>The node container was OOM killed (memory limit hit) or suffered a network partition. The heartbeat stops, but the zombie reaper has not yet fired \u2014 the job appears stuck in <code>ASSIGNED</code> with no progress.</p> <p>Recovery steps:</p> <ol> <li>Open the Nodes view and check whether the assigned node is <code>Online</code> or <code>Offline</code>. An OOM-killed node typically goes <code>Offline</code> within a few seconds of the kill.</li> <li>Run <code>docker stats &lt;node-container&gt;</code> to check current memory usage against the container's limit. If the container is gone, check <code>docker inspect</code> for the last exit code \u2014 exit code <code>137</code> indicates OOM kill.</li> <li>Increase <code>job_memory_limit</code> on the node template or reduce the job's <code>memory_limit</code>.</li> <li>The zombie reaper will reclaim the job within <code>zombie_timeout_minutes</code> (default: 30 minutes) and return it to <code>PENDING</code> if retries remain.</li> </ol> <p>Verify it worked:</p> <p>After the zombie reaper fires, confirm the job moves back to <code>PENDING</code> and is successfully reassigned to a node with adequate memory. Watch for <code>COMPLETED</code> status on the next execution.</p> <p>If the job is repeatedly stuck in <code>ASSIGNED</code> across multiple nodes, the script itself may be consuming unbounded memory \u2014 profile the script and add explicit memory limits.</p>"},{"location":"runbooks/jobs/#see-also","title":"See Also","text":"<ul> <li>axiom-push: Signing and publishing jobs</li> <li>Ed25519 Key Setup</li> <li>FAQ</li> </ul>"},{"location":"runbooks/node-validation/","title":"Node Validation Job Library","text":"<p>The node validation corpus is a set of reference jobs at <code>tools/example-jobs/</code> that operators use to verify a node handles all supported runtimes and constraint types correctly. Scripts are committed unsigned \u2014 sign each one with your registered <code>axiom-push</code> key before dispatching. The manifest at <code>tools/example-jobs/manifest.yaml</code> contains machine-readable metadata for every job.</p>"},{"location":"runbooks/node-validation/#hello-bash","title":"hello-bash","text":"<p>What it tests: Verifies that the Bash runtime is available and that a job can be dispatched, signature-verified, and executed end-to-end on the node.</p> <p>Required capabilities: None.</p> <p>Dispatch:</p> <pre><code>axiom-push job push \\\n  --script tools/example-jobs/bash/hello.sh \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n# Then publish from the Staging tab in the dashboard\n</code></pre> <p>Expected PASS output:</p> <pre><code>Hello from Axiom!\nHostname: node-a1b2c3d4\nOS: Ubuntu 22.04.4 LTS\nBash: 5.1.16(1)-release\nUTC: 2026-03-28T12:00:00Z\n</code></pre> <p>Expected FAIL output: N/A \u2014 this job has no expected failure path. If it fails, the node's Bash runtime is not available or the script was not signed correctly. Check <code>SECURITY_REJECTED</code> status in the Job Execution runbook.</p>"},{"location":"runbooks/node-validation/#hello-python","title":"hello-python","text":"<p>What it tests: Verifies that the Python runtime is available and that a Python job can be dispatched, signature-verified, and executed end-to-end on the node.</p> <p>Required capabilities: None.</p> <p>Dispatch:</p> <pre><code>axiom-push job push \\\n  --script tools/example-jobs/python/hello.py \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n# Then publish from the Staging tab in the dashboard\n</code></pre> <p>Expected PASS output:</p> <pre><code>Hello from Axiom!\nHostname: node-a1b2c3d4\nOS: Linux\nPython: 3.11.8\nUTC: 2026-03-28T12:00:00Z\n</code></pre> <p>Expected FAIL output: N/A \u2014 if the job fails, the node's Python runtime is not available or the signing step was skipped.</p>"},{"location":"runbooks/node-validation/#hello-pwsh","title":"hello-pwsh","text":"<p>What it tests: Verifies that PowerShell 7+ (<code>pwsh</code>) is available on the node and that a PowerShell job can be dispatched and executed end-to-end.</p> <p>Required capabilities: None.</p> <p>Dispatch:</p> <pre><code>axiom-push job push \\\n  --script tools/example-jobs/pwsh/hello.ps1 \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n# Then publish from the Staging tab in the dashboard\n</code></pre> <p>Expected PASS output:</p> <pre><code>Hello from Axiom!\nHostname: node-a1b2c3d4\nOS: Ubuntu 22.04.4 LTS\nPowerShell: 7.4.1\nUTC: 2026-03-28T12:00:00Z\n</code></pre> <p>Expected FAIL output: N/A \u2014 if the job fails, PowerShell 7+ is not installed on the node. Use a Foundry-built image that includes <code>pwsh</code> or install it manually on the node.</p>"},{"location":"runbooks/node-validation/#validation-volume-mapping","title":"validation-volume-mapping","text":"<p>What it tests: Confirms that a volume mount is readable and writable inside the job container at the path specified by <code>AXIOM_VOLUME_PATH</code>.</p> <p>Required capabilities: None.</p> <p>Setup: Before dispatching, the target node must have a volume mounted at the path you specify, and <code>AXIOM_VOLUME_PATH</code> must be set in the job's environment payload.</p> <p>Node compose snippet:</p> <pre><code>services:\n  puppet-node:\n    volumes:\n      - axiom_data:/mnt/axiom-data\n    environment:\n      - AXIOM_VOLUME_PATH=/mnt/axiom-data\n\nvolumes:\n  axiom_data:\n</code></pre> <p>Dispatch:</p> <pre><code>axiom-push job push \\\n  --script tools/example-jobs/validation/volume-mapping.sh \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n# Then publish from the Staging tab in the dashboard\n</code></pre> <p>Expected PASS output:</p> <pre><code>PASS: volume mount at /mnt/axiom-data is readable and writable.\n</code></pre> <p>Expected FAIL output:</p> <pre><code>FAIL: cannot write to /mnt/axiom-data \u2014 check volume mount configuration.\n</code></pre> <p>If the job fails, confirm the volume is mounted on the node (<code>docker inspect &lt;container&gt;</code> \u2192 <code>Mounts</code>) and that <code>AXIOM_VOLUME_PATH</code> matches the mount point inside the container.</p>"},{"location":"runbooks/node-validation/#validation-network-filter","title":"validation-network-filter","text":"<p>What it tests: Confirms network isolation is active on the target node. The job attempts a connection to <code>AXIOM_BLOCKED_HOST</code> (default: <code>8.8.8.8</code>) and exits 0 if the host is unreachable (isolation confirmed), or exits 1 if the host is reachable (isolation not in effect).</p> <p>Required capabilities: None.</p> <p>The script does not create isolation</p> <p>This job validates isolation that is already in place. You must configure <code>--network=none</code> (or equivalent) on the node before dispatching. The script does not manipulate <code>iptables</code> or any node-global network state.</p> <p>Setup: Configure the target node with <code>--network=none</code>:</p> <pre><code>services:\n  puppet-node:\n    network_mode: \"none\"\n</code></pre> <p>Alternatively, pass <code>--network=none</code> via the Docker run command. <code>AXIOM_BLOCKED_HOST</code> can be overridden in the job environment if your isolation blocks a different address range.</p> <p>Dispatch:</p> <pre><code>axiom-push job push \\\n  --script tools/example-jobs/validation/network-filter.py \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n# Then publish from the Staging tab in the dashboard\n</code></pre> <p>Expected PASS output:</p> <pre><code>PASS: network isolation confirmed \u2014 8.8.8.8 is unreachable.\n</code></pre> <p>Expected FAIL output:</p> <pre><code>FAIL: network isolation NOT active \u2014 8.8.8.8 is reachable. Configure --network=none on the node.\n</code></pre>"},{"location":"runbooks/node-validation/#validation-memory-hog","title":"validation-memory-hog","text":"<p>What it tests: OOM enforcement \u2014 the job allocates 256 MB (page-touched to prevent overcommit deferral) and holds it for 30 seconds. On a node with a memory limit below 256 MB, the container runtime kills the process before it completes.</p> <p>Required capabilities: <code>resource_limits_supported: \"1.0\"</code></p> <p>Expected FAILED status</p> <p>The job reaching <code>FAILED</code> status is the expected success outcome. The OOM kill is the point of the test \u2014 it confirms the runtime is enforcing memory limits. If the job reaches <code>COMPLETED</code>, the memory limit is not being enforced.</p> <p>Setup: Configure the target node with a memory limit below 256 MB. Set the <code>resource_limits_supported</code> capability in the node's capability list.</p> <p>Node compose snippet:</p> <pre><code>services:\n  puppet-node:\n    environment:\n      - JOB_MEMORY_LIMIT=128m\n</code></pre> <p>Register <code>resource_limits_supported</code> in the dashboard: open the Nodes view, select the node, open the Capabilities panel, and add <code>resource_limits_supported = 1.0</code>.</p> <p>Dispatch:</p> <pre><code>axiom-push job push \\\n  --script tools/example-jobs/validation/memory-hog.py \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n# Then publish from the Staging tab in the dashboard\n</code></pre> <p>Expected PASS output (job status = FAILED):</p> <pre><code>[node] Job &lt;guid&gt; killed by OOM \u2014 memory limit enforced correctly.\n</code></pre> <p>The job status will show <code>FAILED</code>. This is correct behaviour.</p> <p>Expected FAIL output (job status = COMPLETED):</p> <pre><code>Allocated 256 MB \u2014 sleeping 30 seconds.\nDone.\n</code></pre> <p>If the job completes, memory limits are not being enforced. Check cgroup v2 support on the host (see LXC caveat below).</p>"},{"location":"runbooks/node-validation/#validation-cpu-spin","title":"validation-cpu-spin","text":"<p>What it tests: CPU throttle enforcement \u2014 the job runs a CPU-bound loop for 5 seconds and reports wall time, CPU time, and their ratio. On a node with <code>cpu_limit=0.5</code>, the ratio will be approximately <code>0.5</code> rather than <code>1.0</code>, confirming throttling is active.</p> <p>Required capabilities: <code>resource_limits_supported: \"1.0\"</code></p> <p>The job completes normally (exit 0) and reports its measurements. The operator reads the output to verify the ratio.</p> <p>Setup: Configure the target node with a CPU limit of 0.5 and register the <code>resource_limits_supported</code> capability.</p> <p>Node compose snippet:</p> <pre><code>services:\n  puppet-node:\n    environment:\n      - JOB_CPU_LIMIT=0.5\n</code></pre> <p>Register <code>resource_limits_supported</code> in the dashboard: open the Nodes view, select the node, open the Capabilities panel, and add <code>resource_limits_supported = 1.0</code>.</p> <p>Dispatch:</p> <pre><code>axiom-push job push \\\n  --script tools/example-jobs/validation/cpu-spin.py \\\n  --key signing.key \\\n  --key-id &lt;your-key-id&gt;\n# Then publish from the Staging tab in the dashboard\n</code></pre> <p>Expected PASS output:</p> <pre><code>CPU spin complete.\nWall time : 10.02s\nCPU time  : 5.01s\nRatio     : 0.50\nPASS: ratio &lt; 0.8 \u2014 CPU throttling confirmed.\n</code></pre> <p>Expected FAIL output:</p> <pre><code>CPU spin complete.\nWall time : 5.01s\nCPU time  : 5.00s\nRatio     : 1.00\nFAIL: ratio &gt;= 0.8 \u2014 CPU throttling is NOT active. Check JOB_CPU_LIMIT on the node.\n</code></pre>"},{"location":"runbooks/node-validation/#resource-limit-node-setup","title":"Resource Limit Node Setup","text":"<p>Use this section to configure a node for the memory and CPU validation jobs.</p>"},{"location":"runbooks/node-validation/#memory-limit","title":"Memory limit","text":"<p>Set <code>JOB_MEMORY_LIMIT</code> on the node. The node agent reads this value and passes it as <code>--memory</code> to the container runtime when executing each job:</p> <pre><code>environment:\n  - JOB_MEMORY_LIMIT=128m\n</code></pre> <p>The <code>validation-memory-hog</code> job allocates 256 MB \u2014 any limit strictly below 256 MB will trigger the OOM kill. <code>128m</code> is recommended for a clear result.</p>"},{"location":"runbooks/node-validation/#cpu-limit","title":"CPU limit","text":"<p>Set <code>JOB_CPU_LIMIT</code> on the node. The node agent reads this value and passes it as <code>--cpus</code> to the container runtime:</p> <pre><code>environment:\n  - JOB_CPU_LIMIT=0.5\n</code></pre> <p>The <code>validation-cpu-spin</code> job spins on two CPUs. With <code>cpu_limit=0.5</code>, the wall/CPU ratio will be approximately <code>0.5</code>. Any value below <code>1.0</code> will show measurable throttling.</p>"},{"location":"runbooks/node-validation/#capability-registration","title":"Capability registration","text":"<p>Resource limit jobs require the <code>resource_limits_supported</code> capability to be registered on the target node. Without this capability, the job exits with code 1 and reports:</p> <pre><code>FAIL: resource limits are not supported on this node (resource_limits_supported capability missing)\n</code></pre> <p>To register the capability:</p> <ol> <li>Open the Nodes view in the dashboard.</li> <li>Select the target node.</li> <li>Open the Capabilities panel.</li> <li>Add capability name <code>resource_limits_supported</code> with value <code>1.0</code>.</li> <li>Save. The capability takes effect on the next job assignment cycle.</li> </ol>"},{"location":"runbooks/node-validation/#lxc-caveat","title":"LXC caveat","text":"<p>Cgroup v2 resource limit enforcement is unreliable on LXC-hosted nodes. When <code>EXECUTION_MODE=direct</code> (Python subprocess, used in Docker-in-Docker configurations), <code>--memory</code> and <code>--cpus</code> flags are not passed to any container runtime \u2014 the host must enforce limits at the LXC level instead. For reliable enforcement of <code>validation-memory-hog</code> and <code>validation-cpu-spin</code>, run these jobs on a native Docker node (not LXC-hosted).</p>"},{"location":"runbooks/nodes/","title":"Node Troubleshooting","text":"<p>This guide helps operators diagnose and recover from node failures. Find the symptom that matches what you observe in the dashboard or container logs, then follow the numbered recovery steps.</p> <p>A healthy startup looks like this</p> <p>If you're unsure whether a node enrolled correctly, compare your container logs against this reference sequence:</p> <pre><code>\ud83d\ude80 Environment Node Started (1)\n\ud83d\udd12 Secure Mode Active. Trust Root: /app/secrets/root_ca.crt\n[node-abc12345] \ud83d\udcdc Detected Enhanced Token. Bootstrapping Trust...\n[node-abc12345] No identity found. Enrolling with Server...\n[node-abc12345] \u2705 Enrollment Successful! Certificate Saved.\n[node-abc12345] \ud83d\udc93 Heartbeat Thread Started\n[node-abc12345] Starting Work Loop...\n</code></pre> <p>If you do not see <code>Enrollment Successful!</code> followed by <code>Heartbeat Thread Started</code>, the node did not complete enrollment.</p>"},{"location":"runbooks/nodes/#quick-reference","title":"Quick Reference","text":"Symptom Section Container exits immediately at startup Node container exits immediately at startup Enrollment Failed: Connection refused Enrollment Failed: Connection refused Enrollment Failed: SSL CERTIFICATE_VERIFY_FAILED Enrollment Failed: SSL verification failed Token payload missing 't' or 'ca' Token payload missing t or ca Node appears as duplicate entry in Nodes view Node appears as a duplicate in the dashboard Node goes Offline after container restart Node goes Offline after container restart Node stays Offline after cert rotation Node stays Offline after cert rotation Node shows TAMPERED status Node shows TAMPERED status Node gets HTTP 403 on work/pull and stops receiving jobs Node gets HTTP 403 and stops receiving jobs Node receives concurrency_limit: 0 and processes no jobs Node receives concurrency 0 and processes no jobs"},{"location":"runbooks/nodes/#enrollment-failures","title":"Enrollment Failures","text":"<p>Enrollment failures happen during node startup, before the heartbeat thread starts. Check <code>docker logs &lt;container-name&gt;</code> immediately after starting the container \u2014 the failure reason is logged on the last few lines.</p>"},{"location":"runbooks/nodes/#node-container-exits-immediately-at-startup","title":"Node container exits immediately at startup","text":"<p>The node process failed before completing enrollment. The three most common causes are a malformed <code>JOIN_TOKEN</code>, an unreachable orchestrator, or stale certificate files from a previous enrollment in the <code>secrets/</code> volume. The node logs the specific failure reason as <code>\u274c Enrollment Failed: &lt;exception&gt;</code> before exiting.</p> <p>Recovery steps:</p> <ol> <li>Run <code>docker logs &lt;container-name&gt;</code> and read the last 10 lines \u2014 the exact error will identify which cause applies.</li> <li>If the log shows <code>Connection refused</code>, see Enrollment Failed: Connection refused.</li> <li>If the log shows <code>SSL: CERTIFICATE_VERIFY_FAILED</code>, see Enrollment Failed: SSL verification failed.</li> <li>If the log shows <code>Token payload missing 't' or 'ca'</code>, see Token payload missing t or ca.</li> <li>If the <code>secrets/</code> volume contains old <code>node-*.crt</code> and <code>node-*.key</code> files from a prior deployment, stop the container, delete those files from the volume, and restart.</li> </ol> <p>Verify it worked:</p> <pre><code>docker logs &lt;container-name&gt; | tail -15\n</code></pre> <p>Expected output: <code>[node-abc12345] Starting Work Loop...</code> (with no <code>\u274c Enrollment Failed</code> line above it).</p> <p>If the issue persists after these steps, run <code>docker logs &lt;container-name&gt; --follow</code> during the next restart attempt to capture the full error sequence and open an issue with the output.</p>"},{"location":"runbooks/nodes/#enrollment-failed-connection-refused","title":"Enrollment Failed: Connection refused","text":"<p>The node cannot reach the orchestrator because <code>AGENT_URL</code> points to the wrong host or port. The enrollment request is refused at the TCP level before any TLS handshake occurs.</p> <p>Recovery steps:</p> <ol> <li>Check the <code>AGENT_URL</code> environment variable in your node's compose file or environment \u2014 it must point to the orchestrator host and port (default <code>8001</code>), e.g., <code>https://host.docker.internal:8001</code>.</li> <li>Confirm the orchestrator container is running: <code>docker ps | grep puppeteer-agent</code>.</li> <li>If running nodes inside Docker (Docker-in-Docker), verify <code>extra_hosts: host.docker.internal:172.17.0.1</code> is set so the node container can reach the host network.</li> <li>Test connectivity from inside the node container: <code>docker exec &lt;container-name&gt; curl -k &lt;AGENT_URL&gt;/health</code>.</li> </ol> <p>Verify it worked:</p> <pre><code>docker logs &lt;container-name&gt; | grep \"Enrollment\"\n</code></pre> <p>Expected output: <code>[node-abc12345] \u2705 Enrollment Successful! Certificate Saved.</code></p> <p>If the issue persists after these steps, confirm that no firewall rule is blocking the port and that the orchestrator is listening on <code>0.0.0.0</code> (not just <code>127.0.0.1</code>).</p>"},{"location":"runbooks/nodes/#enrollment-failed-ssl-verification-failed","title":"Enrollment Failed: SSL verification failed","text":"<p>The Root CA PEM embedded in the <code>JOIN_TOKEN</code> does not match the TLS certificate the orchestrator is currently presenting. This mismatch means the node cannot verify the server's identity and refuses to connect.</p> <p>Recovery steps:</p> <ol> <li>Regenerate the <code>JOIN_TOKEN</code> from the dashboard: Admin \u2192 Nodes \u2192 Generate Join Token.</li> <li>Copy the new token exactly \u2014 including any leading/trailing characters that are part of the token \u2014 and replace the <code>JOIN_TOKEN</code> environment variable in your node's compose or environment config.</li> <li>Stop and remove the existing node container, then start a fresh container with the updated token.</li> </ol> <p>Do not regenerate tokens from a cached browser page</p> <p>If the orchestrator's TLS certificate was recently changed (e.g., Caddy renewed it), old join tokens carry the old CA. Always generate a fresh token after any TLS configuration change.</p> <p>For background on how the orchestrator's Root CA relates to the <code>JOIN_TOKEN</code>, see mTLS guide \u2192 The JOIN_TOKEN.</p> <p>Verify it worked:</p> <pre><code>docker logs &lt;container-name&gt; | grep \"Enrollment\"\n</code></pre> <p>Expected output: <code>[node-abc12345] \u2705 Enrollment Successful! Certificate Saved.</code></p> <p>If the issue persists after these steps, verify that the orchestrator's TLS certificate was not changed between token generation and node startup.</p>"},{"location":"runbooks/nodes/#token-payload-missing-t-or-ca","title":"Token payload missing t or ca","text":"<p>The <code>JOIN_TOKEN</code> is a legacy or malformed token. The Enhanced Token format is base64-encoded JSON with two keys: <code>t</code> (the actual token) and <code>ca</code> (the Root CA PEM). Any other format, including plain tokens from older versions, produces this error.</p> <p>Recovery steps:</p> <ol> <li>Regenerate the token from the dashboard: Admin \u2192 Nodes \u2192 Generate Join Token.</li> <li>Copy the token exactly as displayed \u2014 do not truncate, URL-encode, or add any whitespace.</li> </ol> <p>Token corruption is invisible</p> <p>Even a trailing newline or extra space in the <code>JOIN_TOKEN</code> environment variable will corrupt the base64 decoding and produce this error. Shell variables set with <code>export JOIN_TOKEN=$(echo \"...\")</code> may append a newline. Use <code>JOIN_TOKEN=&lt;value&gt;</code> without command substitution when setting it directly.</p> <p>Verify it worked:</p> <pre><code>docker logs &lt;container-name&gt; | grep \"Enhanced Token\"\n</code></pre> <p>Expected output: <code>[node-abc12345] \ud83d\udcdc Detected Enhanced Token. Bootstrapping Trust...</code></p> <p>If the issue persists after these steps, verify the token value by decoding it manually: <code>echo \"&lt;JOIN_TOKEN&gt;\" | base64 -d</code> should produce a JSON object with <code>t</code> and <code>ca</code> keys.</p>"},{"location":"runbooks/nodes/#node-appears-as-a-duplicate-in-the-dashboard","title":"Node appears as a duplicate in the dashboard","text":"<p>The <code>secrets/</code> volume was recreated or the <code>node-*.crt</code> and <code>node-*.key</code> files were deleted. When the node restarts without those files, <code>_load_or_generate_node_id()</code> generates a new UUID, the node enrolls with the orchestrator as a brand-new node, and the old entry remains in the Nodes view as an Offline entry.</p> <p>Recovery steps:</p> <ol> <li>Identify the current active node \u2014 it will be the one showing Online or a recent heartbeat timestamp in the Nodes view.</li> <li>Delete the old Offline entry from the dashboard: Nodes \u2192 [old node row] \u2192 Delete.</li> <li>To prevent this in future: mount the <code>secrets/</code> directory on persistent storage so cert files survive container restarts.</li> </ol> <p>Both entries are safe to have</p> <p>The duplicate entry does not affect job dispatch or security. The old node is simply Offline with a stale entry. Deleting it is cosmetic \u2014 no jobs are lost.</p> <p>For more on node identity persistence, see FAQ \u2192 Why does my node appear multiple times in the dashboard?.</p> <p>Verify it worked:</p> <pre><code>docker logs &lt;container-name&gt; | grep \"identity\"\n</code></pre> <p>Expected output: <code>[node-abc12345] No identity found. Enrolling with Server...</code> (for a fresh enrollment) or <code>[node-abc12345] Loaded existing identity: node-abc12345</code> (if cert files are present and reused).</p> <p>If the issue persists after these steps, check that the <code>secrets/</code> directory is properly mounted and that the node process has write access to it.</p>"},{"location":"runbooks/nodes/#heartbeat-loss","title":"Heartbeat Loss","text":"<p>Heartbeat loss means the node was enrolled and running, but the orchestrator is no longer receiving regular heartbeats from it. The node status transitions to Offline after the heartbeat interval expires.</p>"},{"location":"runbooks/nodes/#node-goes-offline-after-container-restart","title":"Node goes Offline after container restart","text":"<p>The heartbeat thread cannot locate the node's certificate files after a container restart. The thread loops waiting for the cert files \u2014 if the <code>secrets/</code> volume is slow to mount or was not preserved between restarts, the thread never progresses and no heartbeat is sent.</p> <p>Recovery steps:</p> <ol> <li>Inspect the node container logs: <code>docker logs &lt;container-name&gt; | grep Heartbeat</code> \u2014 look for repeated <code>[Heartbeat] Failed: &lt;exception&gt;</code> lines.</li> <li>Verify the <code>secrets/</code> volume is mounted and accessible: <code>docker exec &lt;container-name&gt; ls /app/secrets/</code> \u2014 you should see <code>node-*.crt</code> and <code>node-*.key</code> files.</li> <li>If the cert files are missing (volume was lost or not mounted), re-enroll: stop the container, ensure the <code>secrets/</code> path in your compose file points to persistent storage, and restart.</li> <li>If cert files are present but the heartbeat still fails, check that the orchestrator is reachable from within the node container: <code>docker exec &lt;container-name&gt; curl -k &lt;AGENT_URL&gt;/health</code>.</li> </ol> <p>Verify it worked:</p> <pre><code>docker logs &lt;container-name&gt; | grep \"Heartbeat Thread Started\"\n</code></pre> <p>Expected output: <code>[node-abc12345] \ud83d\udc93 Heartbeat Thread Started</code></p> <p>If the issue persists after these steps, check orchestrator logs for any rejection of the heartbeat request.</p>"},{"location":"runbooks/nodes/#node-stays-offline-after-cert-rotation","title":"Node stays Offline after cert rotation","text":"<p>After rotating the node's certificate, the old revoked certificate is still present in the <code>secrets/</code> volume. When the node restarts, <code>_load_or_generate_node_id()</code> finds the existing <code>node-*.crt</code> file and reuses the old (now revoked) certificate. The orchestrator's <code>/work/pull</code> endpoint returns 403 for revoked certs, so the node never receives jobs and eventually shows as Offline.</p> <p>Recovery steps:</p> <ol> <li>Stop the node container.</li> <li>Delete the old certificate files from the <code>secrets/</code> volume: remove <code>node-*.crt</code> and <code>node-*.key</code>.</li> <li>Generate a new <code>JOIN_TOKEN</code> from the dashboard: Admin \u2192 Nodes \u2192 Generate Join Token.</li> <li>Update the <code>JOIN_TOKEN</code> environment variable with the new token.</li> <li>Start the node container \u2014 it will enroll fresh and receive a new certificate.</li> </ol> <p>For the full certificate rotation procedure, see mTLS guide \u2192 Certificate Rotation.</p> <p>Verify it worked:</p> <pre><code>docker logs &lt;container-name&gt; | grep -E \"Enrollment|Heartbeat Thread\"\n</code></pre> <p>Expected output: <code>[node-abc12345] \u2705 Enrollment Successful! Certificate Saved.</code> followed by <code>[node-abc12345] \ud83d\udc93 Heartbeat Thread Started</code>.</p> <p>If the issue persists after these steps, confirm that the <code>secrets/</code> directory was fully cleared before restart \u2014 both <code>.crt</code> and <code>.key</code> files must be removed.</p>"},{"location":"runbooks/nodes/#node-shows-tampered-status","title":"Node shows TAMPERED status","text":"<p>The zero-trust capability guard in the orchestrator detected that the node reported a capability not present in the <code>expected_capabilities</code> recorded at enrollment time. This indicates the container environment changed after the image was built \u2014 for example, a tool was installed inside the running container, or a package version changed between container rebuilds.</p> <p>Recovery steps:</p> <ol> <li>Investigate what changed in the node's environment: <code>docker exec &lt;container-name&gt; pip list</code> or equivalent to compare the current environment against the template's declared capabilities.</li> <li>If the new capability is legitimate (deliberate change), rebuild the template via the Foundry view to capture the updated capability set, then redeploy the node from the rebuilt image.</li> <li>If the tamper flag was triggered in error (e.g., a transient detection issue), clear it from the dashboard: Nodes \u2192 [node] \u2192 Clear Tamper.</li> </ol> <p>Do not clear tamper without investigating</p> <p>The TAMPERED status is a security signal. Clearing it without understanding the cause masks a potential supply-chain or container escape issue. Always confirm what changed before clearing.</p> <p>Verify it worked:</p> <pre><code>docker ps | grep &lt;container-name&gt;\n</code></pre> <p>Expected output: the node container is running, and the Nodes view in the dashboard shows the node's status badge as Online (not TAMPERED).</p> <p>If the issue persists after these steps, review the orchestrator logs for <code>\ud83d\udea8 TAMPER DETECTED on node &lt;id&gt;</code> and compare the capability list in the log entry against the expected capabilities in the template.</p>"},{"location":"runbooks/nodes/#certificate-errors","title":"Certificate Errors","text":"<p>Certificate errors occur after successful enrollment. The node has a valid client certificate but the certificate has been revoked or the associated node template has entered a blocked lifecycle state.</p>"},{"location":"runbooks/nodes/#node-gets-http-403-and-stops-receiving-jobs","title":"Node gets HTTP 403 and stops receiving jobs","text":"<p>The node's client certificate has been revoked in the orchestrator's <code>RevokedCert</code> table. The <code>/work/pull</code> endpoint checks the certificate serial number against this table and returns 403 for any match, regardless of whether the certificate is otherwise valid.</p> <p>Recovery steps:</p> <ol> <li>Confirm the revocation: check the dashboard Nodes view \u2014 a revoked node typically shows a Revoked badge or no heartbeat activity.</li> <li>If the revocation was intentional: the node must be re-enrolled. Stop the container, delete <code>secrets/node-*.crt</code> and <code>secrets/node-*.key</code>, generate a new <code>JOIN_TOKEN</code>, and restart.</li> <li>The new enrollment produces a fresh certificate with a new serial number that is not in the <code>RevokedCert</code> table.</li> </ol> <p>Certificate revocation is permanent and cannot be undone</p> <p>Once a certificate serial number is added to the <code>RevokedCert</code> table, it cannot be removed. There is no \"un-revoke\" operation. If the revocation was accidental, the only path forward is a fresh enrollment with a new certificate. Before revoking a node, confirm the node identity in the Nodes view to avoid revoking the wrong node.</p> <p>For the full revocation procedure and CRL details, see mTLS guide \u2192 Certificate Revocation.</p> <p>Verify it worked:</p> <pre><code>docker logs &lt;container-name&gt; | grep -E \"Enrollment Successful|Work Loop\"\n</code></pre> <p>Expected output: <code>[node-abc12345] \u2705 Enrollment Successful! Certificate Saved.</code> followed by <code>[node-abc12345] Starting Work Loop...</code></p> <p>If the issue persists after these steps, verify the <code>secrets/</code> directory was fully cleared of old cert files before restarting.</p>"},{"location":"runbooks/nodes/#node-receives-concurrency-0-and-processes-no-jobs","title":"Node receives concurrency 0 and processes no jobs","text":"<p>The orchestrator is sending <code>concurrency_limit: 0</code> in the node's configuration response. This is a deliberate soft-block \u2014 the orchestrator halts job dispatch to a node without returning a hard 403 error. It occurs when the node's associated template is in a REVOKED lifecycle state, or when the node itself has TAMPERED status.</p> <p>Recovery steps:</p> <ol> <li>Check the node's template lifecycle state: go to the Templates tab in the Foundry view and find the template associated with this node. If the template shows a REVOKED status, the node will not receive jobs.</li> <li>If the template is REVOKED: rebuild the template with a valid configuration, or assign the node a different active template.</li> <li>If the template is not REVOKED: check whether the node has TAMPERED status in the Nodes view and resolve it using the steps in Node shows TAMPERED status.</li> </ol> <p>Check both template status and node status</p> <p>A node can receive <code>concurrency_limit: 0</code> from either condition independently. The Nodes view shows node-level status (including TAMPERED). The Foundry Templates view shows template-level lifecycle state. Check both.</p> <p>Verify it worked:</p> <pre><code>docker logs &lt;container-name&gt; | grep -i \"concurrency\\|work loop\\|job\"\n</code></pre> <p>Expected output: the node logs show job assignments being received and processed, with no <code>concurrency_limit: 0</code> in the work response.</p> <p>If the issue persists after these steps, check the orchestrator logs for the specific condition causing the zero concurrency response \u2014 it will indicate whether the cause is template lifecycle state or node tamper status.</p>"},{"location":"runbooks/package-mirrors/","title":"Package Mirror Setup","text":"<p>Package mirrors allow Foundry node image builds to resolve packages from internal sidecars instead of the public internet. This runbook covers devpi (PyPI), apt-cacher-ng (APT), and BaGet (PWSH/NuGet) from scratch \u2014 compose snippets, setup steps, verification commands, and common issues for each.</p>"},{"location":"runbooks/package-mirrors/#devpi-pypi-mirror-pkg-01","title":"devpi \u2014 PyPI Mirror (PKG-01)","text":"<p>devpi is a full PyPI caching proxy. It mirrors pypi.org and caches packages locally. devpi is already included in <code>compose.server.yaml</code> at port 3141 \u2014 no additional compose changes are required.</p>"},{"location":"runbooks/package-mirrors/#enable-the-mirror","title":"Enable the mirror","text":"<ol> <li>Configure the agent to point Foundry builds at devpi by setting <code>PYPI_MIRROR_URL</code>:</li> </ol> <pre><code># Option A: environment variable in compose.server.yaml agent service\nenvironment:\n  - PYPI_MIRROR_URL=http://devpi:3141/root/pypi/+simple/\n</code></pre> <p>Or via API:</p> <pre><code>curl -X PATCH https://localhost:8001/admin/mirror-config \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"pypi_mirror_url\": \"http://devpi:3141/root/pypi/+simple/\"}'\n</code></pre> <ol> <li>Rebuild all Foundry templates so the updated <code>pip.conf</code> is baked into new node images.</li> </ol>"},{"location":"runbooks/package-mirrors/#seed-packages-for-offline-use","title":"Seed packages for offline use","text":"<pre><code># Create the root/pypi caching index (first-run only)\ndocker exec puppeteer-devpi-1 devpi use http://localhost:3141\ndocker exec puppeteer-devpi-1 devpi login root --password=\"\"\ndocker exec puppeteer-devpi-1 devpi index -c root/pypi bases=root/pypi type=mirror mirror_url=https://pypi.org/simple/\n# Trigger initial cache fill (packages download on first install request)\ndocker exec puppeteer-devpi-1 devpi install requests\n</code></pre>"},{"location":"runbooks/package-mirrors/#verify-with-the-pkg-04-validation-job","title":"Verify with the PKG-04 validation job","text":"<pre><code>axiom-push job push \\\n  --script tools/example-jobs/validation/verify_pypi_mirror.py \\\n  --key signing.key --key-id &lt;your-key-id&gt; \\\n  --env PYPI_MIRROR_HOST=devpi:3141\n</code></pre> <p>Expected output on PASS:</p> <pre><code>=== Axiom PyPI Mirror Validation ===\nTesting mirror: devpi:3141\nPASS: pip resolved 'requests' from internal mirror (devpi:3141).\n      Downloading http://devpi:3141/root/pypi/+f/abc/requests-2.31.0-py3-none-any.whl (62 kB)\n</code></pre> <p>Expected output on FAIL (mirror not seeded or wrong URL):</p> <pre><code>FAIL: pip is resolving from pypi.org, not the internal mirror.\n</code></pre>"},{"location":"runbooks/package-mirrors/#common-issues","title":"Common issues","text":"<ul> <li>\"No matching distribution found\" \u2014 using wrong index path. The correct URL is <code>http://devpi:3141/root/pypi/+simple/</code>, not <code>http://devpi:3141/+simple/</code> (root index is empty).</li> <li>pip SSL error on HTTP URL \u2014 <code>trusted-host</code> must match the devpi hostname. The <code>MirrorService.get_pip_conf_content()</code> sets this automatically from the URL.</li> <li>devpi <code>root/pypi</code> index not found after fresh deploy \u2014 run the <code>devpi index -c root/pypi</code> init commands above on first start.</li> </ul>"},{"location":"runbooks/package-mirrors/#apt-cacher-ng-apt-mirror-pkg-02","title":"apt-cacher-ng \u2014 APT Mirror (PKG-02)","text":"<p>apt-cacher-ng is a transparent caching proxy for APT packages. Adding it as a sidecar lets Foundry builds install Debian/Ubuntu packages through the cache; on subsequent builds and in air-gapped environments, packages resolve from the cache rather than the internet.</p>"},{"location":"runbooks/package-mirrors/#add-the-sidecar-to-composeserveryaml","title":"Add the sidecar to compose.server.yaml","text":"<pre><code>apt-mirror:\n  image: sameersbn/apt-cacher-ng:latest\n  restart: unless-stopped\n  ports:\n    - \"3142:3142\"\n  volumes:\n    - apt-cache-data:/var/cache/apt-cacher-ng\n  environment:\n    - DISABLE_IPFORWARD_CHECK=1\n</code></pre> <p>Also add <code>apt-cache-data:</code> under the top-level <code>volumes:</code> key.</p>"},{"location":"runbooks/package-mirrors/#configure-foundry-builds-to-use-the-proxy","title":"Configure Foundry builds to use the proxy","text":"<p>The apt proxy must be injected at build time and removed from the final image. Configure the mirror URL via the API:</p> <pre><code>curl -X PATCH https://localhost:8001/admin/mirror-config \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"apt_mirror_url\": \"http://apt-mirror:3142\"}'\n</code></pre> <p>The resulting Dockerfile pattern (auto-generated by Foundry):</p> <pre><code>RUN echo 'Acquire::http::Proxy \"http://apt-mirror:3142\";' &gt; /etc/apt/apt.conf.d/01proxy \\\n    &amp;&amp; apt-get update \\\n    &amp;&amp; apt-get install -y &lt;packages&gt; \\\n    &amp;&amp; rm /etc/apt/apt.conf.d/01proxy\n</code></pre>"},{"location":"runbooks/package-mirrors/#verify","title":"Verify","text":"<p>Trigger a Foundry build and check apt-cacher-ng logs:</p> <pre><code>docker logs puppeteer-apt-mirror-1 --tail 20\n</code></pre> <p>Cache hits appear as <code>200 (ca)</code> in the log. Cache misses (first build) appear as <code>200</code>.</p>"},{"location":"runbooks/package-mirrors/#common-issues_1","title":"Common issues","text":"<ul> <li>apt-mirror hostname not resolving from inside Foundry build container \u2014 ensure <code>apt-mirror</code> service is in the same Docker network as the agent (<code>puppeteer_default</code>).</li> <li>Proxy baked into final node image \u2014 always <code>rm /etc/apt/apt.conf.d/01proxy</code> in the same <code>RUN</code> layer as the install. Node APT commands will fail outside the compose network if the proxy line persists.</li> <li>DISABLE_IPFORWARD_CHECK=1 required \u2014 without this, apt-cacher-ng may fail to start in Docker due to missing IP forwarding.</li> </ul>"},{"location":"runbooks/package-mirrors/#baget-powershell-module-mirror-pkg-03","title":"BaGet \u2014 PowerShell Module Mirror (PKG-03)","text":"<p>BaGet is a lightweight NuGet v3 server compatible with PowerShell's <code>Install-Module</code> and <code>Install-PSResource</code> commands. Adding it as a sidecar lets PWSH job scripts install modules from an internal repository rather than PSGallery.</p>"},{"location":"runbooks/package-mirrors/#add-the-sidecar-to-composeserveryaml_1","title":"Add the sidecar to compose.server.yaml","text":"<pre><code>baget:\n  image: loicsharma/baget:latest\n  restart: unless-stopped\n  ports:\n    - \"5555:80\"\n  volumes:\n    - baget-data:/var/baget\n  environment:\n    - ApiKey=AXIOM-INTERNAL-NUGET-KEY\n    - Storage__Type=FileSystem\n    - Storage__Path=/var/baget\n    - Database__Type=Sqlite\n    - Database__ConnectionString=Data Source=/var/baget/db/baget.db\n    - Search__Type=Database\n</code></pre> <p>Also add <code>baget-data:</code> under the top-level <code>volumes:</code> key. BaGet is available at <code>http://baget:5555</code>.</p>"},{"location":"runbooks/package-mirrors/#seed-a-module-example-pester","title":"Seed a module (example: Pester)","text":"<pre><code># Download Pester from PSGallery\ncurl -L \"https://www.powershellgallery.com/api/v2/package/Pester\" -o Pester.nupkg\n\n# Upload to BaGet\ncurl -X PUT \"http://localhost:5555/api/v2/package\" \\\n  -H \"X-NuGet-ApiKey: AXIOM-INTERNAL-NUGET-KEY\" \\\n  -F \"package=@Pester.nupkg\"\n</code></pre> <p>Verify via BaGet web UI at <code>http://localhost:5555</code> \u2014 Pester should appear in the package list.</p>"},{"location":"runbooks/package-mirrors/#configure-a-foundry-blueprint","title":"Configure a Foundry Blueprint","text":"<p>Add to the Blueprint's PowerShell setup script so node images have the PSRepository pre-registered:</p> <pre><code>Register-PSRepository `\n  -Name \"AxiomInternal\" `\n  -SourceLocation \"http://baget:5555/v3/index.json\" `\n  -PublishLocation \"http://baget:5555/v3/index.json\" `\n  -InstallationPolicy Trusted\n</code></pre>"},{"location":"runbooks/package-mirrors/#verify-with-a-pwsh-job-script","title":"Verify with a PWSH job script","text":"<p>Dispatch a job containing:</p> <pre><code># Prefer Install-PSResource (PowerShellGet v3, available in PS 7.4+)\nif (Get-Command Install-PSResource -ErrorAction SilentlyContinue) {\n    Install-PSResource -Name Pester -Repository AxiomInternal -TrustRepository\n} else {\n    Install-Module -Name Pester -Repository AxiomInternal -Force\n}\nImport-Module Pester\nWrite-Host \"=== PASS: Pester $((Get-Module Pester).Version) installed from AxiomInternal ===\"\n</code></pre> <p>Expected output:</p> <pre><code>=== PASS: Pester 5.x.x installed from AxiomInternal ===\n</code></pre>"},{"location":"runbooks/package-mirrors/#common-issues_2","title":"Common issues","text":"<ul> <li>\"Unable to find module Pester\" on <code>Install-Module</code> \u2014 PowerShellGet v2 has partial NuGet v3 compatibility issues. Use <code>Install-PSResource</code> (PSResourceGet/PowerShellGet v3) on PowerShell 7.4+ nodes. Check BaGet package list at <code>http://localhost:5555</code> to confirm Pester is seeded.</li> <li>Register-PSRepository already exists \u2014 if the repository was registered in a previous build or run, use <code>Set-PSRepository -Name AxiomInternal -InstallationPolicy Trusted</code> or add an <code>-ErrorAction SilentlyContinue</code> guard.</li> <li>BaGet container unhealthy on first start \u2014 the SQLite DB directory <code>/var/baget/db/</code> is created on first run. If the volume is empty, wait 10 seconds after <code>docker compose up</code> before seeding.</li> </ul>"},{"location":"runbooks/package-mirrors/#enterprise-alternatives","title":"Enterprise alternatives","text":"<p>For larger organisations, Sonatype Nexus Repository and JFrog Artifactory both expose NuGet v3 feeds compatible with <code>Register-PSRepository</code>. The same <code>SourceLocation</code> and <code>InstallationPolicy Trusted</code> registration pattern applies \u2014 replace <code>http://baget:5555/v3/index.json</code> with the Nexus or Artifactory NuGet feed URL.</p>"},{"location":"security/","title":"Security Overview","text":"<p>Axiom implements defence-in-depth security \u2014 multiple independent layers that each limit the blast radius of a compromise. The controls work together: a compromised credential does not grant script execution, and a compromised node cannot inject unsigned code.</p> <p>Each security control targets a specific threat:</p> <ul> <li>mTLS certificates \u2014 ensure only enrolled nodes can poll for work and report results; an attacker cannot impersonate a node without a valid client certificate signed by the orchestrator's Root CA</li> <li>Ed25519 job signing \u2014 ensure only scripts signed by a registered key can execute; even full orchestrator access cannot create an unsigned executable job because the private signing key lives outside the orchestrator</li> <li>RBAC \u2014 limits the blast radius of a compromised human account; a viewer account cannot submit jobs or modify the platform, and an operator account cannot access user management or system configuration</li> <li>Audit log \u2014 provides tamper-evident evidence of all security-relevant actions for forensic and compliance use; every authentication event, node revocation, and job dispatch is recorded with timestamp and actor identity</li> </ul>"},{"location":"security/#defence-in-depth-model","title":"Defence-in-Depth Model","text":"<p>The diagram below shows how the four layers interact. Each layer is independent: bypassing one does not grant access past the next.</p> <pre><code>graph TD\n    A[External Attacker] --&gt;|blocked by| B[mTLS: only enrolled nodes accepted]\n    B --&gt; C[Axiom Node]\n    C --&gt;|executes only| D[Ed25519-signed scripts]\n    D --&gt; E[Container isolation]\n    F[Compromised credential] --&gt;|scope limited by| G[RBAC: role-based permissions]\n    G --&gt;|all actions recorded in| H[Audit Log]</code></pre> <p>The key relationships are:</p> <ol> <li>mTLS gates node-to-orchestrator communication \u2014 no unenrolled client can reach the work queue</li> <li>Ed25519 signing gates script execution \u2014 a node will refuse to run any script that fails signature verification</li> <li>RBAC constrains what a logged-in user can do \u2014 permission checks apply to every API endpoint</li> <li>Audit log records all security-relevant events \u2014 provides visibility if the other layers are tested or probed</li> </ol>"},{"location":"security/#how-the-controls-fit-together","title":"How the Controls Fit Together","text":"<p>Consider a scenario where an attacker has obtained valid dashboard credentials. With RBAC in place, a viewer-role attacker cannot submit jobs, modify node configuration, or access user management. Even if they escalated to operator, they cannot submit a job with an unsigned script \u2014 the Ed25519 verification check runs on the node before execution. Even if they somehow obtained a signing key, they would need a node's client certificate to deliver the job via the mTLS-protected work queue. At every stage, a separate key material or credential stands in the way.</p> <p>This layering means that an attacker who compromises one component faces additional barriers before reaching the next. It also means that revocation or key rotation in one layer (for example, revoking a node certificate) does not require rotating all other credentials.</p>"},{"location":"security/#compromise-scenarios","title":"Compromise Scenarios","text":"<p>The table below describes what an attacker can and cannot do in common compromise scenarios. The \"Controls that limit damage\" column identifies which layer restricts the attacker's ability to cause further harm.</p> Scenario What the attacker gains Controls that limit damage Orchestrator compromised Access to job queue, node list, scheduled definitions Ed25519 signing: cannot create valid signed scripts without private keys; keys are stored outside the orchestrator Node compromised Ability to poll for work on that node Certificate revocation immediately blocks the compromised node; container isolation prevents host-level escape JWT or API key leaked Access matching the token's role <code>token_version</code>: password change immediately invalidates all tokens; API key can be revoked from dashboard Database compromised Encrypted secrets, hashed passwords Fernet encryption protects secrets at rest; bcrypt hashes cannot be reversed to recover passwords"},{"location":"security/#security-guides","title":"Security Guides","text":"<p>This section covers the following topics in detail:</p> <ul> <li>mTLS &amp; Certificates \u2014 Root CA setup, JOIN_TOKEN generation, node enrollment, certificate revocation, and the full cert rotation procedure</li> <li>RBAC Hardening \u2014 Least-privilege configuration, permission audit, and hardening recommendations</li> <li>Audit Log \u2014 Event inventory, access patterns, and compliance reporting</li> <li>Air-Gap Operation \u2014 Deploying in network-isolated environments with offline package mirrors</li> </ul> <p>For the underlying system architecture that these controls protect, see the Architecture guide. For initial deployment configuration including TLS and secrets, see Setup &amp; Deployment.</p>"},{"location":"security/air-gap/","title":"Air-Gap Operation","text":"<p>This guide covers deploying Axiom in a network-isolated environment where internet access is unavailable or restricted.</p> <p>Axiom is designed with air-gap deployments in mind. The package mirror sidecars and offline documentation plugins allow most of the stack to operate without outbound internet access. This guide documents the full setup, what is already offline-capable, what requires substitution, and how to verify isolation.</p> <p>For initial setup and deployment context, see Setup and Deployment.</p>"},{"location":"security/air-gap/#what-is-already-offline-capable","title":"What Is Already Offline-Capable","text":"<p>The following components work without internet access after initial setup:</p> Component How it achieves offline capability Documentation site MkDocs <code>privacy</code> + <code>offline</code> plugins pre-download all external assets (fonts, JavaScript) at Docker build time \u2014 zero CDN or outbound requests at runtime Foundry image builds Python packages sourced from the configured <code>pypi_mirror_url</code>; APT packages from <code>apt_mirror_url</code> when both mirror URLs are set Smelter CVE scanning Operates against the mirrored package registry Node execution Scripts run locally inside containers \u2014 the execution runtime makes no outbound calls mTLS enrollment Node-to-orchestrator mTLS uses the embedded Root CA \u2014 no external CA or OCSP required"},{"location":"security/air-gap/#package-mirror-setup","title":"Package Mirror Setup","text":"<p>For a full from-scratch setup guide, see Package Mirror Runbooks.</p> <p>The mirror sidecars must be configured and seeded before disconnecting from the internet.</p> <p>Step 1: Confirm mirror sidecars are running</p> <p>The PyPI mirror and APT mirror sidecars are included in <code>compose.server.yaml</code>. Confirm they are running:</p> <pre><code>docker compose -f puppeteer/compose.server.yaml ps\n</code></pre> <p>Step 2: Configure mirror URLs</p> <p>Update the mirror configuration via the API:</p> <pre><code>curl -X PATCH \\\n  -H \"Authorization: Bearer &lt;TOKEN&gt;\" \\\n  -H \"Content-Type: application/json\" \\\n  https://&lt;HOST&gt;/admin/mirror-config \\\n  -d '{\"pypi_mirror_url\": \"http://pypi-mirror:3141\", \"apt_mirror_url\": \"http://apt-mirror:3142\"}'\n</code></pre> <p>This action is logged as <code>mirror:config_updated</code> in the audit log.</p> <p>Step 3: Seed the mirrors</p> <p>Before disconnecting from the internet, ensure all required packages are present in the mirrors. Use the Smelter to pre-approve and mirror all ingredients your Foundry templates will need:</p> <ol> <li>Navigate to Admin \u2192 Smelter \u2192 Approved Ingredients</li> <li>Add each package your templates require</li> <li>Trigger a mirror sync from the Mirror Config panel</li> </ol> <p>Step 4: Upload packages after isolation</p> <p>When a new package is needed after internet access is removed, an admin must upload the package file directly:</p> <ul> <li>PyPI packages: navigate to Admin \u2192 Smelter \u2192 Upload Package \u2014 logs <code>smelter:package_uploaded</code></li> <li>APT packages: upload the <code>.deb</code> file to the APT mirror sidecar via its admin interface</li> </ul>"},{"location":"security/air-gap/#offline-build-validation","title":"Offline Build Validation","text":"<p>Before fully disconnecting, verify that Foundry builds succeed with the mirror configuration active and no internet access.</p> <p>Procedure:</p> <ol> <li>Configure mirror URLs as described above</li> <li>Block outbound internet access at the network or firewall level (or use a Docker network policy)</li> <li>Trigger a Foundry build with <code>--no-cache</code> to force all dependencies to be fetched from scratch:</li> <li>Navigate to Templates \u2192 select a template \u2192 click Build</li> <li>Confirm the build succeeds \u2014 a failure at this stage means a dependency is not yet present in the mirror</li> </ol> <p>Tip</p> <p>Run this validation before committing to air-gap mode. It is significantly easier to debug missing packages while internet access is still available.</p>"},{"location":"security/air-gap/#outbound-network-restrictions","title":"Outbound Network Restrictions","text":"<p>Once mirrors are seeded and validated, apply network isolation.</p> <p>Recommended firewall rules \u2014 allow only:</p> <ul> <li>Internal LAN traffic between orchestrator, nodes, and mirror sidecars</li> <li>Any required on-premises services (internal DNS, LDAP, SIEM)</li> </ul> <p>Block all other outbound connections.</p> <p>After applying restrictions, run the offline build validation procedure again to confirm no implicit internet dependency was missed.</p>"},{"location":"security/air-gap/#documentation-container-in-air-gap-mode","title":"Documentation Container in Air-Gap Mode","text":"<p>No additional steps are required \u2014 the documentation container is already offline-capable by default.</p> <p>The MkDocs build uses the <code>privacy</code> plugin to download all external assets (fonts, JavaScript) at Docker build time and embed them in the image. At runtime, the nginx container serves only local files with no outbound requests.</p> <p>Verify:</p> <pre><code># Confirm the docs container starts without network access\ndocker run --network=none &lt;DOCS_IMAGE&gt; nginx -t\n</code></pre> <p>The container should start successfully even with <code>--network=none</code>.</p>"},{"location":"security/air-gap/#what-still-requires-internet","title":"What Still Requires Internet","text":"<p>The following components cannot be fully air-gapped without substitution. This list is exhaustive \u2014 if a component is not listed here, it works offline.</p> Component Internet dependency Suggested substitution Cloudflare Tunnel Outbound to <code>*.cloudflare.com</code> for dashboard public access On-premises reverse proxy: Traefik, nginx, or HAProxy with your own TLS certificate Base image pulls (initial) Docker Hub for <code>python:3.12-alpine</code>, <code>debian:12-slim</code>, <code>nginx:alpine</code>, and similar Pre-pull all base images before isolation (<code>docker pull</code> + <code>docker tag</code> + push to local registry); update <code>compose.server.yaml</code> image references to point to the local registry PowerShell installation (DEBIAN blueprint) Downloads <code>.deb</code> from <code>github.com/PowerShell/PowerShell/releases</code> during Foundry build Mirror the specific PowerShell <code>.deb</code> release to a local artifact store and update the DEBIAN capability matrix injection recipe to reference the local URL Any <code>pip install</code> without mirror config If <code>pypi_mirror_url</code> is not configured, pip defaults to <code>pypi.org</code> Configure mirror URL before any builds \u2014 see Package Mirror Setup above <p>Warning</p> <p>This table reflects the current implementation. If you add custom blueprints or Foundry templates that install packages from external sources, those dependencies are not automatically mirrored \u2014 review each new template before deploying in air-gap mode.</p>"},{"location":"security/air-gap/#air-gap-readiness-checklist","title":"Air-Gap Readiness Checklist","text":"<p>Use this checklist before completing the air-gap transition. Print or copy it to a separate document for offline reference.</p> <pre><code>## Air-Gap Readiness Checklist\n\n### Infrastructure\n- [ ] Mirror sidecars (PyPI + APT) are deployed and running\n- [ ] Mirror URLs configured via /admin/mirror-config (logged as mirror:config_updated)\n- [ ] All required base images pre-pulled and pushed to local registry\n- [ ] Cloudflare Tunnel replaced with on-prem reverse proxy (if public dashboard access is required)\n- [ ] TLS certificate provisioned for on-prem reverse proxy\n\n### Package mirrors\n- [ ] All PyPI packages required by current Foundry templates are present in the PyPI mirror\n- [ ] All APT packages required by current Foundry templates are present in the APT mirror\n- [ ] PowerShell .deb mirrored to local artifact store (if DEBIAN blueprint is in use)\n- [ ] Mirror seed verified \u2014 no packages are fetched from internet during seeding\n\n### Validation\n- [ ] Foundry build completed successfully with internet blocked (--no-cache, all layers rebuilt)\n- [ ] Documentation site loads without external requests (browser Dev Tools \u2192 Network tab checked)\n- [ ] Node enrollment completed successfully in isolated network environment\n- [ ] A complete job run (enroll \u2192 assign \u2192 execute \u2192 result) confirmed end-to-end\n\n### Ongoing operations\n- [ ] Process defined for uploading new packages to mirrors after isolation\n- [ ] Audit log export scheduled to external immutable store (S3 with object lock, SIEM, etc.)\n- [ ] Base image update procedure documented for future security patches (pre-pull, tag, push to local registry)\n- [ ] Incident response plan updated to reflect air-gap constraints\n</code></pre>"},{"location":"security/audit-log/","title":"Audit Log","text":"<p>The audit log records all security-relevant actions in Axiom with a username, timestamp, action type, and optional resource context.</p>"},{"location":"security/audit-log/#accessing-the-audit-log","title":"Accessing the Audit Log","text":"<p>Dashboard: Navigate to Audit Log in the sidebar. Displays entries in reverse chronological order with username, action, resource ID, and timestamp.</p> <p>API:</p> <pre><code>curl -H \"Authorization: Bearer &lt;TOKEN&gt;\" \\\n  https://&lt;HOST&gt;/admin/audit-log\n</code></pre> <p>Requires <code>users:write</code> permission (admin-only by default).</p>"},{"location":"security/audit-log/#log-schema","title":"Log Schema","text":"Field Type Description <code>id</code> integer Auto-incrementing row ID <code>timestamp</code> datetime UTC timestamp of the action <code>username</code> string Human user (<code>alice</code>), service principal (<code>sp:ci-pipeline</code>), or <code>\"scheduler\"</code> for scheduled jobs <code>action</code> string Event type \u2014 see inventory below <code>resource_id</code> string or null ID of the affected resource (node UUID, job ID, template name, etc.) <code>detail</code> JSON or null Additional context specific to the action type"},{"location":"security/audit-log/#event-inventory","title":"Event Inventory","text":"<p>Complete list of all audit actions, grouped by category.</p>"},{"location":"security/audit-log/#authentication","title":"Authentication","text":"Action Triggered by <code>device_flow:token_issued</code> CLI successfully completes device flow login <code>device_flow:approved</code> Admin approves a device flow request <code>device_flow:denied</code> Admin denies a device flow request"},{"location":"security/audit-log/#user-management","title":"User Management","text":"Action Triggered by <code>user:create</code> Admin creates a new user <code>user:delete</code> Admin deletes a user <code>user:role_change</code> Admin changes a user's role <code>user:password_changed</code> User changes their own password <code>user:password_reset</code> Admin resets a user's password <code>user:signing_key_created</code> User uploads an Ed25519 signing key <code>user:signing_key_deleted</code> User or admin deletes a signing key <code>user:api_key_created</code> User creates an API key <code>user:api_key_revoked</code> User or admin revokes an API key"},{"location":"security/audit-log/#service-principals","title":"Service Principals","text":"Action Triggered by <code>sp:created</code> Admin creates a service principal <code>sp:updated</code> Admin updates a service principal (name, role, or expiry) <code>sp:deleted</code> Admin deletes a service principal <code>sp:secret_rotated</code> Admin rotates a service principal secret"},{"location":"security/audit-log/#permissions","title":"Permissions","text":"Action Triggered by <code>permission:grant</code> Admin grants a permission to a role <code>permission:revoke</code> Admin revokes a permission from a role"},{"location":"security/audit-log/#jobs","title":"Jobs","text":"Action Triggered by <code>job:cancel</code> Operator cancels a running job <code>job:retry</code> Operator retries a failed job <code>job:pushed</code> <code>axiom-push</code> submits a job to staging <code>job:cron_skip</code> Scheduler skips a cron fire (previous instance still running) <code>job:draft_skip</code> Scheduler skips a job still in DRAFT status <code>job:revoked_skip</code> Scheduler skips a REVOKED job definition <code>job:deprecated_skip</code> Scheduler skips a DEPRECATED job definition"},{"location":"security/audit-log/#nodes","title":"Nodes","text":"Action Triggered by <code>node:delete</code> Admin deletes a node record <code>node:revoke</code> Admin revokes a node's certificate <code>node:clear_tamper</code> Admin clears a node's tamper flag <code>node:upgrade_staged</code> Admin stages a node upgrade <code>node:reinstate</code> Admin reinstates a previously revoked node"},{"location":"security/audit-log/#foundry","title":"Foundry","text":"Action Triggered by <code>template:build</code> Operator triggers a Foundry image build <code>template:delete</code> Operator deletes a template <code>blueprint:delete</code> Operator deletes a blueprint <code>foundry:image_status_updated</code> Admin updates an image lifecycle status"},{"location":"security/audit-log/#signing-keys","title":"Signing Keys","text":"Action Triggered by <code>key:upload</code> Operator uploads a signing public key <code>signature:delete</code> Operator deletes a signing key"},{"location":"security/audit-log/#smelter-and-mirror","title":"Smelter and Mirror","text":"Action Triggered by <code>smelter:ingredient_added</code> Admin adds an approved ingredient <code>smelter:ingredient_deactivated</code> Admin deactivates an ingredient <code>smelter:config_updated</code> Admin updates Smelter configuration <code>mirror:config_updated</code> Admin updates mirror URLs <code>smelter:package_uploaded</code> Admin uploads a package to the mirror <code>smelter:scan_triggered</code> Admin triggers a CVE scan"},{"location":"security/audit-log/#system","title":"System","text":"Action Triggered by <code>base_image:marked_updated</code> Admin marks the base image as updated <code>signal:fire</code> Admin fires a manual signal"},{"location":"security/audit-log/#compliance-query-patterns","title":"Compliance Query Patterns","text":"<p>The following patterns address common compliance reporting requirements. All queries require <code>users:write</code> permission.</p> <p>All actions by a specific user</p> <pre><code>curl -H \"Authorization: Bearer &lt;TOKEN&gt;\" \\\n  \"https://&lt;HOST&gt;/admin/audit-log?username=alice\"\n</code></pre> <p>Useful for user access reviews and offboarding audits.</p> <p>All service principal activity</p> <p>Filter by username prefix <code>sp:</code> \u2014 all service principal audit entries use <code>sp:&lt;name&gt;</code> as the username:</p> <pre><code>curl -H \"Authorization: Bearer &lt;TOKEN&gt;\" \\\n  \"https://&lt;HOST&gt;/admin/audit-log?username=sp%3Aci-pipeline\"\n</code></pre> <p>All permission changes</p> <p>Filter by action <code>permission:grant</code> and <code>permission:revoke</code>. Review quarterly to detect permission creep \u2014 permissions granted temporarily that were never revoked.</p> <p>All node lifecycle events</p> <p>Filter by action prefix <code>node:</code> to get the complete lifecycle of all nodes (enrollment, revocation, reinstatement):</p> <pre><code>curl -H \"Authorization: Bearer &lt;TOKEN&gt;\" \\\n  \"https://&lt;HOST&gt;/admin/audit-log?action_prefix=node%3A\"\n</code></pre> <p>All events for a specific resource</p> <p>Filter by <code>resource_id</code> (node UUID, job ID, template name) to see the complete event history of a single resource:</p> <pre><code>curl -H \"Authorization: Bearer &lt;TOKEN&gt;\" \\\n  \"https://&lt;HOST&gt;/admin/audit-log?resource_id=&lt;NODE_UUID&gt;\"\n</code></pre> <p>Tip</p> <p>For compliance windows (e.g., SOC 2 audit periods), the <code>timestamp</code> field in the JSON response is UTC ISO 8601. Export the full log to a SIEM or append it to a compliance evidence store at regular intervals to build an unbroken audit trail.</p> <p>Warning</p> <p>The audit log is stored in the application database. For high-assurance deployments, export audit events to an immutable external store (e.g., S3 with object lock, or a SIEM) to prevent tampering by anyone with database access.</p>"},{"location":"security/audit-log/#audit-attribution","title":"Audit Attribution","text":"<p>Actions are attributed to the actor that initiated the request:</p> Attribution format Meaning <code>alice</code> Human user authenticated via JWT <code>sp:ci-pipeline</code> Service principal authenticated via client secret <code>scheduler</code> APScheduler triggered the action (cron job fires, skip events) <p>This means the audit log can distinguish between a human operator cancelling a job and a service principal doing the same \u2014 even if they have identical permissions. For RBAC access review guidance using audit data, see RBAC Hardening.</p>"},{"location":"security/mtls/","title":"mTLS &amp; Certificates","text":"<p>Mutual TLS (mTLS) is a security protocol where both parties in a connection \u2014 the orchestrator and each Axiom Node \u2014 present certificates to prove their identity. Unlike standard TLS, where only the server is verified, mTLS also verifies the client. This means the orchestrator will refuse connections from any node that does not hold a certificate signed by its own Root CA.</p> <p>PKI Terminology</p> <p>CA (Certificate Authority) \u2014 A trusted entity that issues and signs certificates. Axiom operates its own Root CA, making it the sole authority for trusting node identities.</p> <p>CSR (Certificate Signing Request) \u2014 A document generated by a node that contains its public key and identity information. The node sends the CSR to the CA, which signs it to produce a certificate.</p> <p>Certificate \u2014 A digitally signed document binding a public key to an identity (hostname). Nodes present their certificate on every connection to prove they are who they claim to be.</p> <p>Certificate serial number \u2014 A unique integer assigned to each certificate by the CA. Used to identify the certificate in revocation records.</p> <p>CRL (Certificate Revocation List) \u2014 A signed list of certificate serial numbers that have been revoked before their expiry date. Revoked certificates are treated as invalid even if they have not expired.</p> <p>Enrollment \u2014 The process by which a new node obtains its first signed certificate. The node generates a CSR, submits it to the orchestrator, and receives a signed certificate in return.</p>"},{"location":"security/mtls/#how-axiom-uses-mtls","title":"How Axiom Uses mTLS","text":"<ul> <li>The orchestrator operates a Root CA that signs all node certificates</li> <li>Nodes enroll by sending a CSR and receiving a signed client certificate</li> <li>Every <code>/work/pull</code> and <code>/heartbeat</code> call requires the node to present its client certificate</li> <li>Revoked certificates are tracked in a CRL served at <code>GET /system/crl.pem</code></li> <li>Node-facing endpoints (<code>/api/enroll</code>, <code>/work/pull</code>, <code>/heartbeat</code>) use mTLS instead of JWT-based auth \u2014 this is why they appear unauthenticated in the API reference</li> </ul>"},{"location":"security/mtls/#the-root-ca","title":"The Root CA","text":"<p>The Root CA is the trust anchor for the entire fleet. It is created automatically when the orchestrator starts for the first time, if no CA files already exist in <code>secrets/ca/</code>.</p> <p>CA parameters:</p> Parameter Value Key algorithm RSA 4096-bit Signature algorithm SHA-256 Validity 10 years (3650 days) Subject CN <code>Axiom Root CA</code> <p>CA file locations:</p> File Contents <code>secrets/ca/root.key</code> Root CA private key <code>secrets/ca/root.crt</code> Root CA certificate (public) <p>Protect the Root CA private key</p> <p><code>secrets/ca/root.key</code> is the most sensitive file in the deployment. Anyone with access to this file can sign arbitrary certificates and enroll rogue nodes without going through the orchestrator's enrollment API. Back it up to offline storage and restrict filesystem access to the orchestrator process only.</p> <p>Advanced: cert-manager integration</p> <p>For Kubernetes deployments using cert-manager, the Root CA can be loaded from <code>/app/global_certs/root_ca.crt</code> instead of the self-managed <code>secrets/ca/</code> path. The Docker Compose deployment described in this guide uses the self-managed CA path.</p>"},{"location":"security/mtls/#the-join_token","title":"The JOIN_TOKEN","text":"<p>The <code>JOIN_TOKEN</code> is a base64-encoded JSON object containing the Root CA certificate. Nodes use it to establish initial trust with the orchestrator \u2014 without it, a node has no way to verify it is talking to the correct orchestrator and not an impersonator.</p> <p>Generate a JOIN_TOKEN:</p> <ol> <li>Navigate to Admin \u2192 System</li> <li>Click Generate Join Token</li> <li>Copy the token \u2014 it contains the Root CA certificate embedded in base64</li> </ol> <p>Use the token when starting a node:</p> <pre><code>JOIN_TOKEN=&lt;YOUR_JOIN_TOKEN&gt; docker compose -f node-compose.yaml up -d\n</code></pre> <p>Who should receive the JOIN_TOKEN?</p> <p>The JOIN_TOKEN contains the public Root CA certificate \u2014 it is not itself a cryptographic secret. However, any node that receives a valid JOIN_TOKEN can attempt to enroll. Do not share it beyond nodes you intend to add to the fleet. If a token is shared accidentally, generate a new one; old tokens remain valid until the Root CA itself changes, so rely on the enrollment audit log to detect unexpected enrollments.</p>"},{"location":"security/mtls/#node-enrollment","title":"Node Enrollment","text":"<p>Enrollment is fully automatic when a node container starts with a valid <code>JOIN_TOKEN</code>. The steps below describe what happens under the hood:</p> <ol> <li>The node reads <code>JOIN_TOKEN</code> from its environment and decodes it to extract the Root CA certificate</li> <li>It uses the Root CA certificate to establish a trusted TLS connection to the orchestrator (verifying that the server certificate chains up to the same Root CA)</li> <li>The node generates a new RSA private key and creates a Certificate Signing Request (CSR) with its hostname as the Common Name (CN)</li> <li>The node sends the CSR to <code>POST /api/enroll</code> on the orchestrator</li> <li>The orchestrator signs the CSR and returns a client certificate valid for 825 days (~2.25 years)</li> <li>The node stores its private key and certificate in its <code>secrets/</code> volume and uses them for all subsequent <code>/work/pull</code> and <code>/heartbeat</code> calls</li> </ol> <p>After enrollment, the node appears in Nodes with status Active. If enrollment fails \u2014 for example because the JOIN_TOKEN is malformed, the orchestrator is unreachable, or the node's certificate files from a previous enrollment are still present \u2014 the container exits and logs the error.</p> <p>Node identity persistence</p> <p>The node's identity (a UUID derived from its certificate CN) is persisted in <code>secrets/</code>. If the container restarts, it scans <code>secrets/</code> for an existing <code>node-*.crt</code> file and reuses that identity rather than generating a new one. This prevents duplicate node entries in the dashboard after container restarts.</p>"},{"location":"security/mtls/#certificate-revocation","title":"Certificate Revocation","text":"<p>Revoking a certificate immediately blocks that node from polling for work or re-enrolling. Use revocation when a node is decommissioned, a certificate is compromised, or a node container is replaced with a new identity.</p> <p>Steps to revoke a node:</p> <ol> <li>Navigate to Nodes in the dashboard</li> <li>Find the node and click Revoke</li> <li>Confirm the revocation when prompted</li> </ol> <p>What revocation does:</p> <ul> <li>The node's <code>status</code> is set to <code>OFFLINE</code></li> <li>The certificate serial number is added to the CRL</li> <li>All subsequent <code>/work/pull</code> and <code>/api/enroll</code> requests from that node return HTTP 403</li> </ul> <p>The CRL is available at <code>GET /system/crl.pem</code> (unauthenticated, so external tooling can fetch it) and has a <code>next_update</code> interval of 7 days.</p> <p>Revocation is permanent</p> <p>Revoking a certificate is permanent. The node cannot re-enroll using the same certificate. If the node container is still running, it will immediately start receiving HTTP 403 errors on its next <code>/work/pull</code> poll. Stop the container before revoking unless you intend to use the 403 response as a signal.</p>"},{"location":"security/mtls/#certificate-rotation","title":"Certificate Rotation","text":"<p>Node certificates are valid for 825 days (~2.25 years). Before a certificate expires, rotate it by re-enrolling the node with a new certificate. Rotation requires temporarily stopping the node.</p> <p>Prerequisites \u2014 confirm before starting:</p> <ul> <li>[ ] You have access to stop and restart the node container</li> <li>[ ] You have orchestrator admin credentials to generate a new JOIN_TOKEN</li> <li>[ ] The node is not currently running a critical job (check the Jobs view and confirm no job is <code>running</code> on this node)</li> </ul> <p>Rotation procedure:</p> <p>Step 1 \u2014 Generate a new JOIN_TOKEN. In the dashboard, navigate to Admin \u2192 System \u2192 Generate Join Token. Copy the new token.</p> <p>Step 2 \u2014 Stop the node container.</p> <pre><code>docker compose -f node-compose.yaml down\n</code></pre> <p>Step 3 \u2014 Delete the node's certificate files.</p> <pre><code>rm -f secrets/node-*.crt secrets/node-*.key\n</code></pre> <p>Why delete the cert files?</p> <p>This forces the node to generate a new identity on next start. The node ID is derived from the certificate CN \u2014 deleting the old cert means the node will enroll fresh with a new ID. The orchestrator will register it as a new enrollment rather than trying to reuse the old (soon-to-be-revoked) cert.</p> <p>Step 4 \u2014 Start the node with the new JOIN_TOKEN.</p> <pre><code>JOIN_TOKEN=&lt;NEW_JOIN_TOKEN&gt; docker compose -f node-compose.yaml up -d\n</code></pre> <p>Step 5 \u2014 Verify the node has enrolled. In the dashboard, navigate to Nodes and confirm the node appears with status Active. The last-seen timestamp should be within the last minute.</p> <p>Point of no return \u2014 do not proceed until the new node is Active</p> <p>Do not proceed to Step 6 until you have confirmed the node is Active in the dashboard. After Step 6, the old certificate is permanently revoked. If the new enrollment has not succeeded, the node will be unable to re-enroll and you will need to investigate the enrollment failure before any work can be dispatched to it.</p> <p>Step 6 \u2014 Revoke the old certificate. In the dashboard, find the previous node entry (it may appear as Offline) and click Revoke. Confirm the revocation.</p> <p>Step 7 \u2014 Verify the CRL.</p> <pre><code>curl -s https://&lt;YOUR_HOST&gt;/system/crl.pem\n</code></pre> <p>The response should be a PEM-encoded CRL. Confirm the old node no longer appears as Active in the dashboard.</p> <p>If the node appears as a separate entry after rotation</p> <p>If the old and new node entries have different names or UUIDs, it is because the old and new certificates have different hostnames or CNs. This is expected. Delete the old offline entry from the Nodes view after confirming the new node is active and working.</p> <p>For the system architecture context behind mTLS, see Architecture. For deploying the orchestrator with TLS configured, see Setup &amp; Deployment.</p>"},{"location":"security/rbac-hardening/","title":"RBAC Hardening","text":"<p>This guide covers least-privilege configuration patterns, service principal hygiene, and how to audit access in Axiom.</p> <p>For the permission reference table and how to configure roles in the dashboard, see the RBAC guide and RBAC Permission Reference.</p>"},{"location":"security/rbac-hardening/#principle-of-least-privilege","title":"Principle of Least Privilege","text":"<p>Assign the minimum role needed for each user's actual job function. The default roles are designed for this:</p> User type Recommended role Rationale Monitoring-only operator <code>viewer</code> Read-only; cannot submit or modify anything Day-to-day job operator <code>operator</code> Can manage jobs and nodes; cannot touch users or system config Platform administrator <code>admin</code> Use only for setup and user provisioning; not for day-to-day work CI/CD pipeline Service principal (<code>operator</code>) Machine identity; no login; expiry date enforced <p>Warning</p> <p>Do not use the <code>admin</code> account for day-to-day operations. Admin bypasses all permission checks, which means admin actions provide no RBAC signal in the audit log \u2014 every admin action appears as \"permitted\" regardless of what it is. Use a named operator account for routine tasks.</p>"},{"location":"security/rbac-hardening/#service-principal-hygiene","title":"Service Principal Hygiene","text":"<p>Service principals are the recommended machine identity for all automation. They authenticate with a client secret (no login flow) and are tracked independently in the audit log using the <code>sp:&lt;name&gt;</code> username prefix.</p> <p>Hardening checklist:</p> <ul> <li>[ ] Set an expiry date (<code>expires_at</code>) on every service principal \u2014 do not create non-expiring SPs</li> <li>[ ] Assign the <code>operator</code> role, never <code>admin</code>, unless admin-level access is genuinely required</li> <li>[ ] Use one service principal per automation context (one for CI, one for monitoring scripts, etc.) \u2014 avoid shared SPs</li> <li>[ ] Rotate secrets on a schedule (quarterly or after any potential leak) \u2014 use Admin \u2192 Service Principals \u2192 Rotate Secret</li> <li>[ ] Deactivate (<code>is_active = false</code>) rather than deleting a compromised SP \u2014 deletion removes the audit trail</li> </ul> <p>Danger</p> <p>A service principal with the <code>admin</code> role has full unrestricted access. Treat its client secret with the same care as the admin password.</p>"},{"location":"security/rbac-hardening/#auditing-permissions","title":"Auditing Permissions","text":"<p>Review current role permissions regularly to catch permission creep \u2014 permissions that were granted temporarily and never revoked.</p> <p>Dashboard procedure:</p> <ol> <li>Navigate to Admin \u2192 Role Permissions</li> <li>Review the current permission set for each non-admin role</li> <li>Remove any permissions that are no longer required</li> </ol> <p>Via API (for automation):</p> <pre><code># List current permissions for the operator role\ncurl -H \"Authorization: Bearer &lt;TOKEN&gt;\" \\\n  https://&lt;HOST&gt;/admin/roles/operator/permissions\n\n# Revoke a permission\ncurl -X DELETE \\\n  -H \"Authorization: Bearer &lt;TOKEN&gt;\" \\\n  https://&lt;HOST&gt;/admin/roles/operator/permissions/webhooks:write\n</code></pre> <p>Permission changes are logged as <code>permission:grant</code> and <code>permission:revoke</code> in the audit log. These events include the acting admin's username and the affected role.</p>"},{"location":"security/rbac-hardening/#reviewing-the-audit-log-for-access-patterns","title":"Reviewing the Audit Log for Access Patterns","text":"<p>The audit log records all permission changes and security-relevant events. For a periodic access review:</p> <ul> <li>Search for <code>permission:grant</code> events in the last 90 days \u2014 review any unexpected grants</li> <li>Search for admin actions (filter by <code>username = &lt;admin username&gt;</code>) \u2014 admin bypasses RBAC; any admin action should be intentional and deliberate</li> <li>Look for <code>sp:*</code> actions from service principals that have been deactivated \u2014 these indicate a stale identity may still hold an active secret</li> </ul> <p>Cross-link: For query patterns and the complete event inventory, see Audit Log.</p>"},{"location":"security/rbac-hardening/#hardening-the-default-configuration","title":"Hardening the Default Configuration","text":"<p>The default operator and viewer role permissions are calibrated for most deployments. Consider these optional hardening steps for stricter environments:</p> <p>1. Restrict viewer permissions further Remove <code>signatures:read</code> from <code>viewer</code> if signature key visibility is sensitive in your environment:</p> <pre><code>curl -X DELETE \\\n  -H \"Authorization: Bearer &lt;TOKEN&gt;\" \\\n  https://&lt;HOST&gt;/admin/roles/viewer/permissions/signatures:read\n</code></pre> <p>2. Remove <code>tokens:write</code> from <code>operator</code> If operators should not be able to approve device flow requests, remove this permission. This limits device flow approvals to admin accounts only.</p> <p>3. Guard <code>users:write</code> carefully Granting <code>users:write</code> to <code>operator</code> effectively gives all operators the ability to manage users, create service principals, and grant permissions \u2014 do this only if you have a flat team structure where all operators also manage access.</p> <p>Warning</p> <p><code>users:write</code> is the most powerful non-admin permission. It controls user creation, deletion, role assignment, service principal management, and permission grant/revoke. Treat any role holding this permission as near-equivalent to admin.</p> <p>4. Separate read and write access by environment In multi-environment deployments (staging and production), consider creating environment-specific service principals with read-only access to production and full access to staging. This limits blast radius if a CI credential is compromised.</p>"},{"location":"security/rbac-hardening/#quick-reference-default-role-permissions","title":"Quick Reference: Default Role Permissions","text":"Permission admin operator viewer <code>jobs:read</code> bypass yes yes <code>jobs:write</code> bypass yes no <code>nodes:read</code> bypass yes yes <code>nodes:write</code> bypass yes no <code>definitions:read</code> bypass yes yes <code>definitions:write</code> bypass yes no <code>foundry:read</code> bypass yes yes <code>foundry:write</code> bypass yes no <code>signatures:read</code> bypass yes yes <code>signatures:write</code> bypass yes no <code>tokens:write</code> bypass yes no <code>alerts:read</code> bypass yes yes <code>alerts:write</code> bypass yes no <code>webhooks:read</code> bypass yes no <code>webhooks:write</code> bypass yes no <code>users:write</code> bypass no no <p>\"bypass\" means admin skips the permission check entirely \u2014 the permission is neither stored nor evaluated.</p>"}]}