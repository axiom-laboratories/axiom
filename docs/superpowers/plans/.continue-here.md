---
plan: 2026-03-27-homepage-redesign.md
task: 7
total_tasks: 7
status: completed
last_updated: 2026-03-27T22:27:45.843Z
---

<current_state>
All 7 tasks in the homepage redesign plan are complete and pushed to origin/main.
The plan file is at docs/superpowers/plans/2026-03-27-homepage-redesign.md.
No GSD phase directory — plan was tracked as a standalone superpowers plan.
</current_state>

<completed_work>

- Task 1: Sticky nav bar — Done (commit: `feat(homepage): add sticky nav bar`)
- Task 2: Problem-first hero — Done (commit: `feat(homepage): problem-first hero headline`)
- Task 3: Dashboard mockup section — Done (commit: `feat(homepage): add dashboard mockup section`)
- Task 4: Pain points grid (replaced security prose) — Done (commit: `feat(homepage): replace security prose with pain points grid`)
- Task 5: EE "Coming soon" badge + dimmed feature list + "Get in touch" link — Done (commit: `feat(homepage): EE coming-soon badge and dimmed feature list`)
- Task 6: Dual CTA section (replaced Quick Install) — Done (commit: `feat(homepage): dual CTA section — self-host and enterprise interest`)
- Task 7: Footer year 2026 + push — Done (commit: `feat(homepage): update footer year, complete redesign`)

Post-plan fixes also applied and pushed:
- Moved RBAC, audit log, Foundry from CE → EE feature list (commit: `fix(homepage): move RBAC, audit log, Foundry to EE feature list`)
- Removed "Grace period licence lifecycle management" and "Ed25519 offline licence validation" from EE list (commit: `fix(homepage): remove licence internals from EE feature list`)
</completed_work>

<remaining_work>

Nothing remaining from the plan. All tasks complete.

Possible follow-up (not captured in any plan):
- Replace `href="#enterprise-interest"` placeholder on Enterprise CTA button with real Google Form URL once created.
- The Enterprise nav button and "Register your interest" button both currently smooth-scroll to the dual CTA section.
</remaining_work>

<decisions_made>

- Moved RBAC, audit log, and Foundry out of CE — user confirmed these are EE features, not CE.
- Removed licence internals (grace period, Ed25519 offline validation) from EE list — not customer-facing selling points.
- Dual CTA "Register your interest" button uses `href="#enterprise-interest"` as a functional placeholder until Google Form URL is ready.
- Pain grid responsive rule merged into the existing `@media (max-width: 640px)` block rather than adding a second media query.
</decisions_made>

<blockers>
None.
</blockers>

<context>
The homepage redesign is fully shipped. The page now follows a Problem → Solution narrative:
nav → hero (problem-first) → dashboard mockup → pain points grid → CE/EE editions → dual CTA → footer.

CE has 4 items (job execution, node management, Ed25519 signing, cron scheduling).
EE has 7 items, all customer-facing benefits, shown dimmed with "Coming soon" badge.
The Enterprise CTA anchor (`#enterprise-interest`) is a placeholder pending a real Google Form.
</context>

<next_action>
Nothing to resume — plan is complete. If returning to homepage work, the only pending item is swapping in the Google Form URL per the note at the bottom of the plan file.
</next_action>
